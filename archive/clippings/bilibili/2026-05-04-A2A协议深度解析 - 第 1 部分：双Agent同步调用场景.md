---
title: "A2A协议深度解析 - 第 1 部分：双Agent同步调用场景"
url: "https://www.bilibili.com/video/BV1GC7hzKEX9/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1GC7hzKEX9"
cid: "30302669681"
author: "马克的技术工作坊"
upload_date: "2025-06-03"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114619255362521&bvid=BV1GC7hzKEX9&cid=30302669681&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

A2A协议深度解析 - 第 1 部分：双Agent同步调用场景

## 章节

- `00:00` 前言
- `00:18` 什么是Agent
- `01:58` A2A协议使用场景
- `06:38` 启动Agents
- `11:02` 抓包分析A2A协议
- `27:13` 核心概念回顾
- `29:54` 运行链路流程图
- `34:13` 结束语

## 字幕

### 前言 `00:00`

`00:00` 今天我们来聊一下a to a协议
`00:02` a to a协议
`00:02` 是google在今年4月9日的时候对外发布的
`00:06` 全称是agent to agent协议
`00:08` 顾名思义
`00:09` 这个协议规定了agent与agent之间的沟通规范
`00:12` 我们这就来看一下这个协议到底写了些什么
`00:15` 内容
`00:15` 与MCP的区别是什么

### 什么是Agent `00:18`

`00:20` 鉴于有些小伙伴可能不太清楚agent的是什么
`00:23` 这里我大致介绍一下
`00:25` 简单来说
`00:26` agent是一种能够自主思考
`00:28` 并调用外部工具来解决用户问题的智能程序
`00:31` 比如我们用来写代码的client
`00:33` 就是一个典型的agent
`00:35` 你让他做一个介绍AI的网页
`00:37` 它首先会通过大语言模型
`00:39` 思考解决方案的大致步骤
`00:41` 然后调用各种外部工具来完成任务
`00:44` 有的工具用来创建文件
`00:46` 有的工具则用来执行终端命令
`00:48` 但不管使用哪种工具
`00:50` 它都会不断的思考执行
`00:52` 直到帮你把任务完成
`00:54` 不过要注意
`00:55` 虽然CLINE很能干
`00:56` 但他最擅长的还是写代码
`00:58` 你如果让他去订机票
`01:00` 他可能就没那么靠谱了
`01:02` 有人可能会说不对啊
`01:04` 我装个订机票的MCP工具
`01:06` 他不也能干这件事情吗
`01:08` 确实这种做法呢有可能也是能行得通的
`01:11` 但问题也随之而来
`01:13` 比如效果可能不太理想
`01:15` 毕竟client的设计目标是写好代码
`01:18` 不是做旅行助手
`01:19` 他可能会犯一些错误
`01:21` 而且呢你还要考虑成本
`01:23` client所有可用工具的信息都写在系统提示词里面
`01:27` 今天装个工具订机票
`01:29` 明天装个工具订旅馆
`01:30` 工具越多
`01:31` 提示词越长
`01:32` 每次回答消耗的token也会增加花费
`01:35` 自然也就更多了
`01:37` 所以更合理的做法是
`01:38` 直接创建一个专门订机票的agent
`01:41` 需要订机票时就问他写代码的时候
`01:44` 还是要交给client
`01:45` 各司其职
`01:46` 效果最好
`01:47` 那么如果某些任务需要多个agent的协同合作
`01:50` 该怎么办呢
`01:51` 这时候就需要用到a to a协议了
`01:54` 接下来我们一起看看一个具体的a to a
`01:56` 使用场景

### A2A协议使用场景 `01:58`

`02:00` 假设你住在西雅图
`02:02` 你想在未来三天里面挑一天飞往纽约
`02:05` 不仅如此
`02:06` 你还有一个额外的要求
`02:09` 因为你喜欢好天气
`02:10` 所以呢你希望出发的那天西雅图阳光明媚
`02:14` 如果你生活在一个没有大模型
`02:17` 没有agent的时代
`02:18` 你需要自己来做以下三件事情
`02:21` 一查找西雅图未来三天的天气预告
`02:25` 二根据天气预告
`02:27` 从这三天里面选择西雅图天气最好的一天
`02:30` 三查询那一天从西雅图到纽约的机票信息
`02:35` 做完了这三件事情之后
`02:37` 你便可以松一口气了
`02:38` 剩下的就是去机票网站订票
`02:41` 并且乘机旅行
`02:42` 你自己查是要费点时间的
`02:45` 不过好在你生活在一个有着大模型的时代
`02:48` 你发现有个系统正好可以帮你做这几件事情
`02:51` 这个系统内部部署了三个agent
`02:54` 分别是调度agent
`02:56` 天气agents和机票agents
`02:59` 这个系统可以一次性帮你完成之前的三个任务
`03:03` 你用自然语言向这个系统发问
`03:05` 这个系统便会把问题发给调度agent
`03:08` 让他来做整体的规划和调度
`03:11` 把任务下发给其他更专业的agent
`03:13` 调度agent呢会先去调用天气查询agent
`03:17` 以便获取未来三天西雅图的天气情况
`03:20` 然后他会挑选其中天气最好的一天
`03:23` 挑好之后
`03:24` 调度agent会接着去调用机票
`03:27` agent拿到那一天从西雅图到纽约的机票信息
`03:31` 最后把机票信息也获取到了之后
`03:33` 调度agent便会把所有的内容整理一下
`03:36` 发回给你
`03:37` 这样你问一句话就可以得到答案了
`03:39` 就不需要自己上网搜索了
`03:41` 当然没准
`03:42` 机票agent还能直接帮你订机票
`03:44` 不过为了简化整个链路
`03:46` 我们就演示到查询机票这一步为止吧
`03:49` 那现在有个问题
`03:51` a to a协议作用在这个链路中的哪一部分呢
`03:54` 对就是agent与agent之间的交互部分
`03:58` 简单来说
`03:59` 只要两个agent之间需要沟通
`04:02` 他们就需要使用a to a协议
`04:04` 这个就是TV协议的使用场景了
`04:07` 那MCP作用在哪里呢
`04:10` MCP是作用在每个agent的内部
`04:12` 比如我们可以用天气查询agent来举例
`04:15` 天气查询agent内部肯定有一个大模型
`04:18` 但是除了大模型之外
`04:20` 它可能还部署了多个MCP工具
`04:23` 有些负责天气预告
`04:24` 有些负责历史气象分析
`04:27` 调度agent的请求会首先给到天气agent内部的大模型
`04:31` 大模型判断
`04:32` 需要调用天气预告
`04:33` 这个MCP工具才能够解决问题
`04:36` 因此他就调用了它
`04:38` 拿到了未来几天的天气信息
`04:40` 当然大模型是无法直接调用外部工具的
`04:44` 它需要借助agent内部的一些其他代码来实现
`04:47` 这里是把整个链路给简化了
`04:49` 大模型与MCP工具的交互流程
`04:52` 不在我们今天的讨论范围之内
`04:53` 感兴趣的话可以看一下我的MCP终极指南
`04:57` 调用完工具之后
`04:59` 大模型整理出一个答案
`05:01` 并且把答案发回给调度agent到这里
`05:04` 天气agent的任务就算是完成了
`05:07` 其中调用MCP工具的环节
`05:09` 就是MCP发挥作用的地方
`05:12` 那回想一下a to a在哪里呢
`05:14` 没错
`05:14` a to a就发生在两个ancient的交互的时候了
`05:17` 所以总结一下
`05:19` a to a协议是作用在agent与agent之间的
`05:22` MCP协议是作用在agent内部的
`05:25` 它们的作用域不同
`05:26` 这个呢就是这两个协议的最大区别了
`05:29` 在前面的环节中
`05:31` 我们聊到了一个使用了三个agent的链路
`05:33` 不过这个链路稍显复杂
`05:35` 因此我们先把机票agent从这个链路里面去掉
`05:40` 再把问题换一下
`05:41` 只问天气相关的问题
`05:44` 这样整个链路就只剩下两个agent了
`05:46` 我们来尝试跑通这个链路
`05:48` 再来通过抓包的方式分析一下
`05:50` 这两个agent之间到底说了些什么
`05:52` 这样我们就可以得知a to a协议的内容了
`05:55` 不过细心的观众可能会发现
`05:57` 如果这样的话
`05:58` 调度agent其实就没什么用了
`06:01` 因为调度agent本来的功能就是
`06:03` 把用户问题拆解为多个子任务
`06:05` 并下发给其他的agent
`06:07` 现在除了调度agent之外
`06:09` 就只剩下天气agent了
`06:11` 只剩下一个
`06:11` 还有什么可拆解和下发的呢
`06:13` 用户直接问天气agent不就完了
`06:16` 没错
`06:17` 确实是这样的
`06:18` 这个链路并没有什么太多的实际使用价值
`06:21` 但是链路简单
`06:23` 而且确实是用到了a to a协议
`06:25` 所以很适合我们分析
`06:27` 当你把这个链路研究透了之后
`06:29` 相信再往这个链路里面加几个agent
`06:31` 你也是能够搞明白的
`06:33` 我们要明白的是原理
`06:35` 明白了原理就一通百通

### 启动Agents `06:38`

`06:40` 下面我们就来启动这两个agent
`06:42` 首先是天气agent
`06:44` 这个agent是我写的
`06:45` 放在这个GITHUB仓库下面
`06:47` 我们克隆下这个GITHUB仓库之后
`06:49` 进入到这个目录中
`06:51` 然后呢再进入weather这个文件夹之后
`06:56` 我们再执行uv run
`06:58` 点这个命令就可以启用这个agent了
`07:00` 注意执行这个agent之前需要安装UV
`07:04` UV呢是Python的一个包管理器
`07:07` 大家自己搜索下安装方法
`07:09` 我这里就不再赘述了
`07:11` 每一个agent本质上呢都是一个HTTP服务器
`07:14` 只不过这个HTTP服务器的内容
`07:17` 是符合a to a协议的
`07:19` 从程序的输出中
`07:20` 我们可以看到这个agent所开放的端口呢是1万
`07:25` 记住这个数字
`07:26` 我们后面呢会用到启动了weather这个agent之后呢
`07:31` 下一步我们就要启动用于调度的agent了
`07:34` google已经写好了一个
`07:36` 我们直接用它的就好了
`07:38` 首先我们打开google的a to a samples仓库
`07:42` clone一下它的代码
`07:44` 我这里呢已经clone好了
`07:46` 不过我们还不能立即启动它
`07:48` 我们还需要做另外一件事情
`07:50` 才能够启动这个程序
`07:52` 那就是获取google的APIKE
`07:54` 因为这个调度agent用的是google的SDK
`07:57` 所以呢必须使用google的API key才能够执行
`08:01` 我们来到google的AI studio界面
`08:04` 点击上面的get API key
`08:06` 然后呢再点击这里面的create API key按钮
`08:11` 选择一个项目
`08:12` 如果你之前没有创建过任何的API key的话呢
`08:15` 那可能这个地方就不用选择了
`08:17` 它会自动给你创建一个默认的
`08:20` 然后我们再点击这里面的create API key in existing project
`08:25` 稍等一会儿
`08:26` 这个API key就会创建好了
`08:28` 我们点击这里面的copy
`08:30` 然后再回到终端这里
`08:33` 进入我们克隆好的这个a to a samples目录
`08:36` 然后呢再进入到demo ui这个文件夹
`08:40` 创建一个叫做点ENV的文件
`08:43` 在这里面我们写入google API key
`08:46` 等于后面接着我们刚才创建好的APIKE
`08:51` 然后呢我们保存退出之后呢
`08:54` 我们就可以执行uv run min点P来启动这个程序了
`09:00` 照例啊
`09:01` 你需要安装UV才能够启动它
`09:03` 另外啊
`09:04` 因为这个程序的运行过程中会请求google的API
`09:07` 所以呢需要保持网络通畅
`09:09` 启动好了之后
`09:10` 我们可以从输出中判断它开放的端口是1万2
`09:14` 我们来打开这个页面
`09:16` 网页打开了就是这个样子了
`09:19` 你可能会问我们不是要启动调度agent吗
`09:22` 这个不是一个网页吗
`09:24` agent在哪里呢
`09:25` 答案是调度agent已经创建好了
`09:28` 只不过需要你问问题之后才能开始工作
`09:31` 那在哪里问问题呢
`09:32` 当然就是这里了
`09:34` 我们在这个页面问完问题之后
`09:36` 这个页面就会先用调度agent去寻找与问题相关的
`09:40` 其他agent使用a to a协议进行沟通
`09:43` 最后把沟通后的结果返回给我们
`09:45` 所以我们刚才启动的这个程序
`09:47` 它内部其实包含两个角色
`09:50` 一调度agents用于解析用户问题
`09:53` 调用其他的agents
`09:54` 并最终完成用户的诉求
`09:56` 你可以认为回答用户问题需要多个agent进行参与
`10:00` 其中调度agent就是这些agent中的总指挥官
`10:03` 二平台
`10:05` 这个是我起的名字
`10:06` 我们现在所看到的网站去掉调度agent的部分
`10:10` 就是平台了
`10:12` 它提供了问答agents
`10:15` 注册事件列表
`10:17` 任务列表以及相关的一些配置
`10:21` 总而言之
`10:21` 我们把这一整套的功能称之为平台
`10:25` 方便后续讲解
`10:26` 如果你要做一个多agent的系统的话
`10:29` 你也必定会有这样的一个平台
`10:31` 只不过可能展示形式和包含的功能不同而已
`10:35` 既然要找agent
`10:36` 我们就得通过某种方式告诉他有哪些agent可用
`10:40` 这个网页提供了agent的注册功能
`10:43` 就在这个地方
`10:44` 正好呢可以满足我们的诉求
`10:46` 不过呢在注册之前
`10:48` 我们还需要打开网络抓包软件
`10:50` 因为agent的注册呢用的也是a to a协议
`10:53` 我们得提前开启抓包软件
`10:56` 这样才能够知道to v协议是如何规定
`10:59` agent的注册过程的

### 抓包分析A2A协议 `11:02`

`11:04` 我这里用的抓包软件呢是叫做WSHARK呃
`11:08` 安装过程呢我就不演示了
`11:10` 这个软件在各个平台上面都能用
`11:12` 也很有名
`11:13` 大家自己搜索安装下
`11:15` 打开war shark之后呢
`11:16` 点击这里面的loop back
`11:18` 表示我们要抓取本机的网络包
`11:22` 因为不论是agent还是网页都是在本机运行的
`11:26` 所以我们抓本机的网络包就够了
`11:29` 进来之后呢
`11:30` 大家可以看到玩shark就开始抓包了
`11:32` 我们到这里就可以暂时不管玩shark了
`11:35` 再回到google的这个页面里面
`11:37` 继续完成注册
`11:38` 首先点击这里面的agents来到这个页面
`11:42` 然后呢再点击这个按钮
`11:44` 输入要注册的agent的地址
`11:47` 还记得我们之前启动的那个天气agent的地址吗
`11:51` 没错就是local host10000
`11:54` 当时我还让大家记住1万这个端口号
`11:57` 输入地址之后
`11:58` 我们点击read网页呢
`12:00` 就读出了我们agent所包含的各种信息
`12:03` 可以看到这个agent是专门处理
`12:05` 负责天气相关的工作的
`12:07` 没有问题
`12:08` 我们点击save到这里为止
`12:11` 我们的天气agent就注册成功了
`12:14` 我们之前说过
`12:15` 注册agent这一步呢是用到了a to a协议
`12:19` 所以呢我们回到WISHARK这里
`12:22` 可以看到我们这里有很多的网络包
`12:25` 那我们需要筛选一下
`12:27` 因为这么多的网络包我们肯定是看不过来的
`12:29` 我们可以在这里面加一些筛选条件
`12:32` 这里我用的过滤条件是HT TP and TCP
`12:36` 点d s t port等于1万
`12:40` 代表我们只抓HTTP相关的包
`12:43` 并且这个包的端口号呢必须是1万
`12:45` 也就是我们agent所暴露出来的端口号
`12:48` 输入好了筛选条件之后
`12:50` 我们回车
`12:51` 可以看到这里面显示的网络包数量明显变少
`12:54` 只剩下两个了
`12:55` 而且这两个包的内容呢其实是一模一样的
`12:58` 应该是我们刚才那个平台他请求了两遍
`13:01` 我们只看第一个就行
`13:03` 我们点击右键再点击follow
`13:05` 然后呢再点击HTTP stream
`13:08` 然后在这里就可以看到具体的请求内容了
`13:11` 可以看到这个请求呢是发往了天气agent的点
`13:15` Well
`13:15` known agent点JSON这个路径
`13:19` 这个呢也是a to a协议所规定的路径
`13:22` 每个agent都需要在这个路径下面
`13:24` 返回它的基本信息
`13:25` 以便决定后续调用它的最佳时机
`13:28` 这后面呢就是我们天气agent的返回内容了
`13:32` 他用的格式是JSON
`13:34` 不过他没有格式化
`13:36` 所以看起来不是很方便
`13:37` 我来给大家格式化一下
`13:42` 这些都是天气agent返回给平台的
`13:46` 用于描述天气agent的基本信息
`13:48` 这里涉及到的信息有点多
`13:50` 我们来一点一点看
`13:52` capabilities代表这个agent有哪些能力
`13:55` 它这里面只有一个属性叫做streaming
`13:57` 它的值为false
`13:59` 代表这个agent不支持流失返回
`14:01` 流式返回就是分段返回
`14:03` 我们在各个大模型的聊天界面里面
`14:06` 经常看到不支持流式返回的
`14:08` 意思
`14:08` 就是说我们这个agent会一次性返回完整的答案
`14:12` default input modes代表默认接受的输入格式
`14:16` 这里面写的是text
`14:17` 也就是文本
`14:19` 这个也就是最普遍的输入格式了
`14:22` 你也可以写图片相关的格式
`14:24` 比如说是image杠PNG之类的
`14:27` default output modes呢代表是默认接受的输出格式
`14:31` 同样这里面写的是使用最普遍的text
`14:35` 有些agent支持输出图片
`14:37` 所以呢在这里面写image杠PNG也是可以的
`14:41` description呢是这个agent的描述
`14:44` name呢是这个agent的名字
`14:46` skills呢是这个agent所包含的技能列表
`14:50` 技能其实就是功能了
`14:52` 代表我们当前的这个agent能完成什么样的任务
`14:55` 我们这个天气agent呢一共是有两个技能
`14:58` 分别是做天气预报
`15:00` 以及给出某地的空气质量报告
`15:05` 这两个技能的格式都是一样的
`15:07` 我们以第一个为例来仔细看一下呃
`15:11` 首先是description
`15:12` 代表这个技能的描述
`15:14` examples代表什么样的问题可以触发这个技能
`15:18` 比如说我这里面的一个例子
`15:20` 就是给我纽约未来七天的天气预告id
`15:23` 就是这个技能的id了
`15:25` name就是这个技能的名称
`15:28` text是这个技能的标签
`15:30` 这里面的标签是天气和预告
`15:33` 到这里为止
`15:34` 技能的属性就介绍完毕了
`15:37` 我们再继续看下面的其他属性
`15:40` 后面还剩两个
`15:41` 第一个呢是URL
`15:43` 代表的是这个agent所对应的URL啊
`15:48` 这跟我们之前注册时候所使用的那个URL
`15:51` 是基本上是一样的
`15:52` 只不过是把local host换成了127.0.0.1
`15:55` 其实这两个本质上是没什么区别的
`15:58` version就代表这个agent的当前版本
`16:00` 我们的版本是1.0.0
`16:02` 这个也没有什么问题
`16:04` 到这里这个agent的返回呢就结束了
`16:09` 这个JASON呢在a to a协议里面是有一个名字的
`16:13` 它叫做agent card
`16:15` 它用于描述当前这个agent的一些基本信息
`16:18` 以便后续在合适的时候选择这个agent来办事
`16:22` 分析完了agent card的信息之后
`16:25` 我们回到平台这里
`16:27` 之前平台请求了天气agent的agent card
`16:31` 并且把相关的信息都记录了下来
`16:34` 供后续使用
`16:35` 我们这就来试试它能不能找到这个天气agent
`16:38` 首先我们来到这个主页
`16:41` 然后点击加号
`16:43` 新建一个conversation之后呢
`16:45` 输入我们的问题
`16:55` 可以看到他稍作思考便给出了答案
`16:58` 我们这个时候呢再回到玩shark这里
`17:01` 看看到底发生了什么
`17:06` 在之前执行了follow HTTP stream之后呢
`17:09` 玩shark的筛选条件就被更改了
`17:11` 我再改回原来的筛选项
`17:13` 以便我们看得更清楚一些
`17:15` 可以看到平台再次请求了我们的agent
`17:18` 就是这个请求了
`17:20` 照例我们点击右键选择follow HTTP stream
`17:25` 来看一下具体的请求和返回内容
`17:28` 这里面显示的信息分为两块
`17:31` 上面这个红色的部分代表的是请求
`17:34` 下面这个蓝色的部分代表的是返回
`17:37` 可以看出这一次呢他请求的路径呢变成了杠
`17:41` 然后这块呢是请求的具体内容
`17:45` 我们来把这块内容复制一下
`17:48` 同样的到vs code这边做一下格式化
`17:52` 我们来具体分析一下他到底请求了些什么东西
`17:57` 他这个请求遵循的是JSONRPC的格式
`18:00` JSONRPC呢是用JSON作为传输格式
`18:03` 调用远程函数的一种方法
`18:05` 它一般呢是由四个字段组成的
`18:08` 分别是id JSON rpc
`18:11` method和PARAMS
`18:13` 可以看到这四个属性
`18:15` 在我们当前的这个请求里面都能够找到
`18:18` 其中id呢用于标识当前的这个请求
`18:22` agent的返回的时候呢
`18:23` 也必须要带上这个RD
`18:25` 而且对应的值呢也必须要与请求中的id相同
`18:29` 以便表示对应的结果
`18:30` 就是处理这个请求的
`18:31` JSONRPC属性呢
`18:33` 代表当前所使用的JSONRPC协议的版本
`18:36` 我们用的是2.0
`18:38` method代表要请求的方法名称
`18:41` 这里使用的是message send
`18:43` 代表我们希望agent直接返回结果
`18:46` 它还有一个常见的值是叫做message stream
`18:50` 代表调用方希望agents以流失的方式返回结果
`18:54` 不知道你还记不记得
`18:55` 天气agent在他的agent card里面已经标识过了
`18:59` 他的streaming为false
`19:01` 代表它不支持流式返回
`19:03` 所以呢这里面请求的时候用的就是message send
`19:07` 最后还有一点需要提一下
`19:09` JSONRPC只定义了调用方的请求体
`19:12` 里面需要包含method的属性
`19:14` 并没有对method的值作出规定
`19:16` 所以无论是message send还是message stream
`19:20` 这都是a to a协议的规定
`19:22` 分别代表期望非流失返回和流失返回两种情况
`19:26` 所以呢大家需要把JSONRPC
`19:28` 和a to a协议的边界也搞清楚
`19:31` 最后要讲的是
`19:32` 这个PARAMS属性代表要请求的方法
`19:35` 所对应的参数同method一样
`19:38` JSONRPC只规定了调用方的请求体
`19:41` 里面需要包含PARAMS属性
`19:43` 但是并没有对PARAMS的值作出规定
`19:46` 所以PARAMS的值会包含哪些属性
`19:48` 这都是a to a协议所规定的
`19:50` 它这里面一共是包含了两个属性
`19:53` 分别是叫做configuration和message
`19:56` 其中configuration呢它里面只有一个属性
`20:00` 叫做accepted output mo
`20:02` 代表了调用方希望agent返回什么格式的结果
`20:05` 可以看到调用方期望接收到的输出格式为文
`20:09` 本和PNG图片
`20:11` 他这里面把文本写了两遍啊
`20:13` 一个是text
`20:14` 一个是text plane
`20:16` 其实意思都是一样的
`20:17` 后者呢可能更标准一些
`20:19` 之后呢
`20:20` 我们再看一下这个message属性
`20:23` 它代表要传给agent的消息
`20:25` 其中context的id代表的是上下文id a to a协议
`20:29` 并没有定义这个属性的具体含义
`20:32` 他把解释权交给了具体的使用场景
`20:35` 在我们这个例子里面
`20:36` 它所使用的context id
`20:38` 就是我们前面所开启的这个conversation的id
`20:42` 也就是这里大家可以看一下它
`20:44` 这两个值呢其实是一样的
`20:46` kind呢代表的就是当前这个结构体的类型了
`20:50` 它的值为message
`20:51` 也就是消息了
`20:52` message id用于唯一标识
`20:55` 当前的这个message parts是一个列表
`20:58` 这个列表的每个元素呢都叫做part
`21:02` part呢代表message中的一段内容
`21:05` 我们这里只有一个内容类型是文本
`21:08` 对应的值呢
`21:09` 就是这里面西雅图明天的天气怎么样
`21:12` 也就是我们要问天启agent的问题了
`21:15` 如果用户还上传了一张图片的话呢
`21:17` 他也可以在这里面追加一个part
`21:19` 就比如说是可以这样追加
`21:29` 可以看到这个追加的part呢
`21:31` 它的类型是文件
`21:33` 然后这里分别是文件的名称
`21:36` 文件的类型
`21:37` PNG图片以及它所对应的base64编码
`21:41` 这个base64编码呢
`21:43` 本质上就是对应了这个图片的内容了
`21:46` 如果文件太大不适合传输base64编码的话呢
`21:50` 也可以提供文件的URL
`21:53` 好我们再把这段给删掉
`21:58` 还有一个属性是肉
`22:00` 这个属性呢代表这个message是由谁来发起的
`22:04` 它的值为user
`22:05` 代表这个message呢是由调用方发起的
`22:07` 到这里为止
`22:08` 整个请求呢就结束了
`22:10` 说来也简单啊
`22:11` 就是问了天启agent一个问题
`22:14` 西雅图明天的天气怎么样
`22:17` 呃接到问题之后呢
`22:19` 天气agent也给出了回答
`22:21` 我们再回到WISHARK这边
`22:25` 天气agent的回答呢就是这段内容了
`22:28` 我们同样的把它复制下来
`22:30` 然后来到vs code这边粘贴格式化
`22:33` 我们再来分析一下
`22:35` 这段内容到底写了些什么东西呃
`22:38` 返回结果的格式呢同样也遵循JSONRPC的规范
`22:41` 它在外层呢一共是有三个属性
`22:44` 分别是IDJASSONRPC和result
`22:47` 其中id呢是与请求时所使用的那个id相同的
`22:52` 我们再来看一下请求时所使用的id是这个
`22:56` 到这里面一搜索确实是相同的
`22:59` 这个呢就是用来表示当前的这个返回
`23:02` 就是用来回答刚才的那个请求内容的呃
`23:06` JASSONRPC的值呢是2.0
`23:08` 与请求时候的值一样
`23:10` 代表我们使用的是2.0版本的JSONRPC规范
`23:13` result呢代表的就是具体的结果了
`23:16` 这个结果本质上呢是叫做一个task
`23:20` 从这里面的kind task也可以看出这一点
`23:24` task呢也是a to a协议里面的一个概念
`23:28` 代表一个任务
`23:29` 当被调用的agent接收到请求之后
`23:31` 它一般就会生成一个task
`23:34` 并且在返回结果的时候
`23:36` 填充上这个task的各种信息
`23:39` 注意啊
`23:40` task不是调用方生成的
`23:41` 而是被调用方生成的
`23:43` 也就是我们的这个天气agent的生成的
`23:45` 我们刚才在请求体里面呢是找不到task的
`23:49` 这个task里面有多个属性
`23:52` 我们来挨个看一下
`23:53` 首先是这个artifacts
`23:55` artifacts是一个列表
`23:57` 其中的每个元素都叫做一个ARTIFECT
`24:00` artifact呢是代表了agent的产物
`24:03` 这个产物呢可以是一个文本
`24:05` 可以是个图片
`24:07` 还可以是一个文件等等
`24:08` 在我们的这个例子里面
`24:10` 天气agent的产出的是一个文本类型的artifacts
`24:15` 它不包含图片文件或者是其他的内容
`24:19` 这个对应的是artifact id
`24:22` 这个呢是artifacts的描述
`24:24` 这个是artifacts的名称
`24:26` 然后呢这块是artifacts的parts列表
`24:30` parts列表里面的每一个元素都是一个part
`24:34` 我们之前在请求体里面其实见过part
`24:38` 不知道大家是否还记得
`24:40` 我再给大家看一下
`24:41` 这个呢是之前请求体里面的message部分
`24:45` 而message里面的这一部分呢
`24:48` 就是我们之前所看到的pass列表了
`24:51` 我当时说过啊
`24:52` part代表的是message里面的一段内容
`24:55` 这个说法呢其实并不十分准确
`24:58` 大家也看到了
`24:59` 我们现在在这个artifacts里面呢
`25:02` 眼界到part了
`25:03` 所以呢严格来说part代表的就是message
`25:07` 或者是artifacts里面的一段内容
`25:11` 之所以当时没有提及到artifacts
`25:14` 就是因为当时还没有聊到
`25:15` 他们
`25:15` 相信你现在可以对part有一个更清晰的认识了
`25:19` 好那这块呢就是artifact的内容了
`25:23` 它代表的就是agent的一个产物
`25:25` 我们的产物呢就是一个text
`25:28` 这个呢就是text的内容了
`25:30` 可以看到它基本上就是对我们的请求
`25:33` 做了一个回答
`25:34` 描述了一下未来几天的天气情况
`25:38` 在后面的这个context的id
`25:40` 也是task的一个基本属性
`25:43` 代表的是上下文id
`25:44` 我们在请求里面的也是会干过他的
`25:47` 这个就是我们的请求内容了
`25:50` 这里面有一个context的id
`25:52` 跟这里面的context id的值是一样的
`25:55` 呃那后面呢是这个history
`25:57` 它代表的是历史消息列表
`25:59` 其中的每一条都是一个历史消息
`26:02` 我们现在只有一条历史消息
`26:05` 而且这条历史消息就是直接从请求的message
`26:08` 那里搬过来的
`26:09` 我再把请求的message展示出来
`26:11` 大家看一下
`26:13` 这个是请求的message内容
`26:16` 这个是返回结果里面的历史消息内容
`26:19` 这两部分的内容基本上是一模一样的
`26:22` 只有一个区别
`26:23` 返回结果里面的历史消息里多了一个task id
`26:27` 属性
`26:28` 代表这个消息所对应的task的id
`26:31` 我们之前聊过这个task
`26:33` 它是我们的天气agent所创建的
`26:36` 除了这个task id以外
`26:37` 这两个结构体就没有什么不同的地方了
`26:41` 这个id呢代表的是我们当前这个task的id
`26:44` 可以看出这个id呢是与history里面的task id
`26:47` 是相同的
`26:48` kind代表的是当前这个结构体的类型
`26:51` 这个结构体类型就是一个task
`26:53` 然后是status
`26:55` status里面呢只有一个属性叫做state
`26:58` 它的状态呢是completed
`26:59` 代表我们当前的这个task已经完成了
`27:03` 到这里返回就结束了
`27:05` 之后调度agent便拿到了天气agent的结果
`27:08` 总结了一下
`27:09` 返回给用户
`27:10` 整个流程呢也就结束了

### 核心概念回顾 `27:13`

`27:15` 相信你刚才一定是接受了大量的信息
`27:18` 其中涉及到多个概念
`27:19` 也许你已经有点乱了
`27:21` 没关系
`27:22` 我们来梳理一下
`27:23` 首先是agent card
`27:25` 它用于描述一个agent的基本信息
`27:28` 一般可以通过agent的点
`27:29` well known agent点JSON这个地址访问message
`27:33` 它适用于在agent之间交换信息用的结构体
`27:36` 比如我们请求天气agent的数据的时候
`27:39` 请求体里面就包含这样的一个message
`27:42` artifacts代表被调用agent的产物
`27:45` 比如这里显示的就是天气agent
`27:48` 返回结果中的artifacts列表
`27:50` 部分
`27:50` 天启agent通过artifacts给出了他对问题的解答
`27:54` artifacts应该算是返回结果里面
`27:56` 最重要的部分了
`27:57` 相信没有什么比答案更重要
`28:00` part代表message或者是artifacts里面的一段信息
`28:04` 比如说我这里面放的代码截图
`28:06` 代表的就是请求体里面的part列表部分
`28:09` 用于承载用户的问题
`28:11` 类型为text
`28:12` parts的类型除了text以外
`28:15` 还可以是file data等等
`28:17` task代表被调用agent根据请求所生成的任务
`28:21` 在我们的这个例子中
`28:22` 任务就返回了一次
`28:24` 直接显示状态为已完成
`28:26` 但是对于流式返回
`28:28` 或者是长时间运行的场景来说
`28:30` 任务的状态呢可能是已提交工作中
`28:34` 需要用户输入等等
`28:35` 被调用的agent需要返回多次
`28:38` 每次的任务状态都不一样
`28:40` 只有最后一次的任务状态才有可能是已完成
`28:43` 另外在之前版本的a to a协议里面
`28:46` 曾经把task定义为调用方agent的产物
`28:49` 而不是我们这里所说的被调用方的产物
`28:52` 这个呢似乎与实际task的生成流程相违背
`28:56` 所以呢在后面的版本里面
`28:58` a to a协议又把这句话给删掉了
`29:00` 如果你看到类似的表述的话
`29:02` 那有可能是历史版本
`29:03` 我个人认为不太正确
`29:05` 最后还有两个概念
`29:07` 我们之前没有提过
`29:09` 但是a to a协议里面会经常提及
`29:12` 所以这里面我也想提一下
`29:14` 我们之前的例子里面涉及到两个agent
`29:17` 分别是调度agent和天气agent
`29:20` 请求是由调度agent发起的
`29:22` 而回答是由天气agent完成的
`29:24` 一个发起
`29:25` 一个返回
`29:26` 这两种agent在a to a协议里面都有一个专属名词
`29:30` 像调用agent这样的调用方
`29:32` 我们称之为a to a client
`29:34` 像天气agent这样的被调用方
`29:36` 我们称之为a to a server
`29:38` 这个呢是一种说法
`29:40` 不过有些时候你可能还会遇到另外一种说法
`29:43` 分别是client agent和remote agent
`29:47` 意思都是一样的
`29:48` 你自己感受下
`29:49` 最后我们可以给整个过程画个流程图
`29:52` 帮助你理解

### 运行链路流程图 `29:54`

`29:56` 首先这个过程的运行分为两个阶段
`30:00` 分别是agent注册阶段和用户问答阶段
`30:03` 我们先来看看agent注册阶段
`30:06` agent的注册阶段呢一共是有四个角色参与
`30:09` 分别是用户平台调度agent
`30:13` 天气agents
`30:15` 我们来看看这段流程里面发生了什么
`30:17` 首先用户在平台上输入了天气agent的URL
`30:21` 并且提交平台请求了天气agent的点
`30:25` Well
`30:25` known agent点JSON路径以获取天气agent的信息
`30:29` 天启agent通过这个路径返回了自己的agent card
`30:33` 随后平台会去创建调度agent
`30:36` 在创建的过程中
`30:37` 平台会给调度agent这三个信息
`30:40` 第一他会告诉调度agent的职责
`30:43` 是做任务的拆解和下发
`30:45` 第二他会告诉调度agent
`30:47` 他可以下发的agent列表
`30:49` 目前只有一个
`30:51` 就是天气agent
`30:52` 第三目前可用的工具
`30:54` google的代码里面呢有两个工具
`30:57` 不过我研究了一下
`30:58` 实际上能用到的只有一个叫做send message
`31:01` 用于向其他agent发消息
`31:03` 还有一个工具叫做list remote agents
`31:06` 用于列举可用的agents列表
`31:08` 不过因为可用的agents列表
`31:10` 已经放到提示词里面了
`31:11` 所以呢这个工具并没有调用的必要
`31:14` 因此我也没有把它放到流程图里面
`31:16` 在调度agent创建成功之后
`31:18` 注册流程呢就基本结束了
`31:21` 在这个过程里面
`31:22` 我们不仅注册了天气agent的信息
`31:25` 还创建了调度agent
`31:26` 这两个agent后面都会用到
`31:29` 然后我们来到问答阶段
`31:31` 这个阶段同样是包含四个角色
`31:33` 分别是用户平台
`31:36` 调度agents和天气agent跟之前是一样的
`31:39` 首先用户向平台问了一个天气相关的问题
`31:43` 平台把这个问题转发给了调度agents
`31:46` 调度agent拿到问题之后
`31:48` 第一步呢
`31:49` 就是去寻找有哪些agent可以解决用户的问题
`31:53` 还记得我们之前说过
`31:55` 平台会把可用的agent列表给调度agent吗
`31:58` 调度agent仔细看了一下
`32:01` 发现这里面有一个天气agent
`32:03` 他有个技能叫做天气预告
`32:05` 正好可以解决用户的问题
`32:07` 因此调度agent的下一步的操作呢
`32:09` 就是去调用这个天气agent了
`32:11` 怎么调
`32:12` 我们之前聊过调度agent手里有两个工具
`32:16` 其中一个叫做send message
`32:18` 可以用来与其他的agent的沟通
`32:20` 沟通时用的协议就是a to a协议
`32:23` 因此调度agent就使用了这个工具
`32:25` 把问题转发给天气agent
`32:28` 让他来解答天启agent接收到请求之后
`32:31` 内部运作了一番
`32:33` 它怎么运作的
`32:34` 我们不管可能是调用了MCP工具
`32:37` 也有可能是直接调用了天气相关的API
`32:39` 实际上如果你看一下代码的话
`32:41` 你就会发现我写的这个填写HT的返回
`32:44` 是一段固定的文案
`32:45` 无论是问什么
`32:46` 都只会返回未来三天的天气
`32:49` 但无论怎样
`32:50` agent内部的实现呢
`32:51` 不在a to a协议的讨论范围之内
`32:54` a to a协议只是要求agent接收一个请求
`32:57` 给出一个回答
`32:58` 这就足够了
`32:59` 总而言之
`33:00` 天气agent把未来三天的天气信息都返回了回去
`33:03` 调度agent拿到了天气agent的返回之后
`33:06` 从这三天的天气信息里面抽取出了明天的返回
`33:10` 给了平台
`33:10` 平台再返回给用户
`33:12` 整个流程到这里就结束了
`33:15` 我们了解了系统运行时涉及的两个阶段
`33:17` 但是这两个阶段的所有流程
`33:19` 都与a to a协有关吗
`33:21` 显然不是的
`33:22` 我们不妨来一个一个看
`33:24` 找找每个阶段哪个环节涉及到了a to a协议
`33:27` 首先来看一下agent的注册阶段
`33:30` 在这一阶段中只有注册agent card
`33:33` 这个过程与a to a协议相关
`33:36` 其他的与a to a协议都无关
`33:38` 有人可能会觉得创建调度agent
`33:41` 与a to a协议有些关联
`33:43` 但是实际上
`33:44` 你可以用任何可行的方式来创建这个调度agent
`33:48` 这个创建过程跟a to a协议并没有什么关系
`33:52` 再来看看用户问答阶段
`33:54` 这一阶段只有调度agent与天气agent的沟通过程
`33:57` 用到了a to a协议
`33:59` 这个呢可能更好理解一些AHA协议嘛
`34:02` 那agent与agent之间的沟通
`34:04` 使用AHV协议是再正常不过的事情了
`34:07` 除此之外
`34:08` 别的环节与HV协议都没有什么太大关系

### 结束语 `34:13`

`34:15` 今天的视频就到此结束了
`34:17` 说实话
`34:18` 虽然a to a协议已经发布了一个多月了
`34:21` 但是在网上依然很难找到一个
`34:23` 详细阐述AHV协议内容的资料
`34:25` 不论是视频还是文章
`34:27` 不论是中文还是英文
`34:29` 大多数都是浅尝辄止
`34:31` 浮于表面
`34:32` 不仅如此
`34:33` 在这一个多月的时间里
`34:34` google的a to a仓库也是变来变去
`34:37` 一开始只有一个仓库
`34:38` 后来拆成了多个协议的内容
`34:41` 也在这个过程里发生了一些变化
`34:43` 好在现在似乎是趋于稳定了
`34:45` 除此之外
`34:46` 虽然google的a to a协议与模型无关
`34:49` 但是他的例子是用自家的ADK来写的
`34:52` 这是google自己研发的agent的开发框架
`34:55` 谁没事会研究这个东西呀
`34:57` 但是没有办法
`34:58` 为了确保我的理解没有问题
`35:00` 我也去专门学了一下ADK
`35:02` 把a to a的示例代码看了看
`35:04` 不过说实话
`35:05` 我觉得a to a的示例代码还是有些小问题的
`35:08` 有时候会报错
`35:09` 可能还是得打磨下
`35:11` 但不管怎么样做
`35:12` 这个视频确实是花费了我不少时间
`35:15` 如果对你有帮助
`35:16` 欢迎点赞并订阅
`35:18` 如果有疏漏或错误也请见谅
`35:20` 毕竟能拿到的资料太少了
`35:22` 假如这个视频效果好的话
`35:24` 我后面还会出第二期
`35:26` 给大家讲讲a to a协议是如何处理
`35:28` 流失结果返回的
`35:29` 最后如果你对a to a协议有任何疑问的话
`35:33` 建议直接参考这个页面
`35:34` 这个呢是google出的a to a协议规范
`35:37` 这里面的内容应该就是最权威的了
`35:40` 好了
`35:40` 今天呢就到这里了
`35:42` 我们下次再见
`35:43` 拜拜
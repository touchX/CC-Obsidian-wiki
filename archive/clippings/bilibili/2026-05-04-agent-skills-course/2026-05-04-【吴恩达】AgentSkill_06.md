---
title: "【吴恩达】2026年公认最好的【Agent Skills】教程！大模型入门到进阶，一套全解决！Agent Skills with Anthropic—附课件代码"
url: "https://www.bilibili.com/video/BV1qv6eBZErD?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=6"
bvid: "BV1qv6eBZErD"
cid: "35698773541"
author: "吴恩达深度学习"
upload_date: "2026-01-30"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115981733136330&bvid=BV1qv6eBZErD&cid=35698773541&page=6&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1、使用开放标准格式和最佳实践创建可重复使用的技能，并组合以创建复杂的工作流程。
2、建立定制代码生成技能，审核你的代理按需加载的工作流程、数据分析和研究。
3、结合MCP和子代理的技能，创建强大的代理系统，具备专业知识和外部数据源的访问权限

## 字幕

`00:00` 我们现在将更详细地探讨技能的结构以及创建技能的最佳实践
`00:05` 接下来我们将通过两个实例应用所学知识
`00:08` 第一个是基于讲义创建练习题
`00:11` 另一个是分析时间序列数据特征
`00:15` 让我们开始本节课的学习
`00:17` 我们将稍作重点讲解技能的结构
`00:20` 介绍相关最佳实践
`00:23` 然后我们将查看两个自定义的技能示例
`00:25` 观察它们在技能创建器中的运行效果
`00:29` 检验其是否符合最佳实践标准
`00:32` 每个制作的技能都需包含带有yaml前置内容的技能md文件
`00:38` 该文件需要在技能md中定义名称和描述
`00:42` 这是技能的核心内容部分
`00:45` 以及对脚本或附加文本文件的引用
`00:49` 仅在必要时加载的必要资源
`00:54` 在了解名称和描述的最佳实践时
`00:57` 可以想象这至关重要
`00:59` 名称和描述不仅是claude分析技能功能的方式
`01:05` 还能帮助检测何时使用该技能
`01:08` 名称字符数有最大限制
`01:11` 描述同样我们曾简要提及
`01:14` 名称必须包含小写字母
`01:16` 数字和连字符
`01:17` 通常采用动词加ing形式作为技能名称
`01:22` 描述部分需要说明
`01:24` 不仅功能是什么
`01:25` 还包括使用场景
`01:27` 若存在触发该技能的关键字
`01:31` 要重点突出这些关键词
`01:34` 除了必需字段外
`01:36` 代理技能规范还支持可选字段
`01:40` 这可能包括许可证兼容性和元数据的任意键值对
`01:46` 需要注意的是虽然存在标准的代理技能规范
`01:51` 仍会遇到一些技能
`01:53` 部分由anthropic开发
`01:55` 其他可能不完全遵循规范
`01:59` 这些技能仍在持续开发中
`02:01` 技能规范本身也在不断完善
`02:03` 随着我们跨多个模型提供商和代理工具生态
`02:09` 当我们从yaml前置内容转向技能主体内容
`02:13` 技能格式没有底层限制
`02:17` 然而
`02:18` 构建可预测工作流时
`02:21` 必须确保步骤清晰明确
`02:24` 正如其他技能所示
`02:25` 尤其是技能创建器技能
`02:28` 明确说明边界条件非常重要
`02:30` 分步说明
`02:32` 若某步骤需要跳过
`02:35` 必须非常清晰
`02:36` 为什么这通常是普遍情况
`02:37` 保持在五百行以内是最佳实践
`02:40` 因为我们可以随时引用外部文件
`02:43` 在必要时使用资源脚本
`02:46` 一般来说
`02:46` 清晰简洁非常重要
`02:49` 使用正向斜杠是关键
`02:52` 即使在Windows系统上也是如此
`02:53` 确保这项技能能在多种环境中运行很重要
`02:57` 当我们考虑创建技能时
`02:59` 需要思考给予该技能多少自由度
`03:03` 我们应该允许通用方法和方向
`03:08` 还是应该专注于遵循最佳实践的具体步骤
`03:13` 我们可能需要较低的自由度
`03:15` 但对于更创意的输出
`03:17` 多种颜色
`03:18` 多种风格
`03:19` 多种字体
`03:20` 我们可以允许高自由度
`03:22` 当我们开始考虑包含多个技能的复杂流程时
`03:27` 将任务分解为顺序步骤比拥有一个庞大的单一技能更有价值
`03:32` 而不是一个试图一次性完成所有任务的大型技能
`03:34` 这些系统可以处理上百个技能
`03:37` 确保命名恰当避免混淆很重要
`03:41` 并在规范中遵循可预测的模式
`03:45` 存在可选的目录空间
`03:47` 正如我们看到的许多不同技能
`03:50` 脚本有子文件夹
`03:52` 引用和资源文件
`03:54` 你的脚本包含需要读取和执行的代码
`03:58` 还需确保有错误处理和清晰文档
`04:02` 我们的引用包含附加文档或参考文件
`04:06` 通常建议技能读取整个参考文件
`04:12` 如果文件较长的话
`04:15` 最后是底层资源
`04:17` 可能包括输出图像模板
`04:20` 标志
`04:20` 数据文件
`04:21` 模式
`04:22` 等等
`04:23` 需要注意这些目录
`04:26` 脚本
`04:26` 引用和资源遵循代理技能标准
`04:30` 但你可能会遇到许多不遵循该标准的技能
`04:36` 然而标准正在快速演变
`04:39` 技能也在快速进化
`04:41` 展望未来
`04:42` 我们期望新技能遵循此标准
`04:45` 但可能遇到不同命名或规范的技能
`04:50` 现在我们对最佳实践有了清晰认识
`04:52` 可选目录
`04:54` 以及如何编写生产级技能
`04:56` 让我们看看我们创建的两个技能示例
`05:00` 逐步分析它们
`05:01` 然后通过技能创建器运行以分析最佳实践
`05:05` 并讨论评估这些技能以确保生产就绪
`05:10` 我现在在VS Code中
`05:12` 这里我们有两个将要深入的自定义技能
`05:15` 第一个是生成练习题的技能
`05:19` 如果我们查看这个技能
`05:21` 可以看到描述是生成教育练习题
`05:25` 从讲义到测试理解
`05:28` 你可以想象自己是教师或讲师
`05:30` 希望提供特定的输入和输出格式
`05:34` 并生成全面的问题以测试理解
`05:38` 让我们开始逐步分析这个技能
`05:41` 我们支持的输入格式
`05:43` 指定使用哪些特定库
`05:46` 并指定要提取的文本
`05:49` 然后再次遵循我们的问题结构
`05:52` 我们需要非常具体
`05:54` 因此明确指定问题生成的精确顺序
`05:59` 从真伪题逐步到实际应用
`06:03` 对于每个问题
`06:04` 下方有子指南
`06:07` 可以看到这个技能代码不足500行
`06:11` 但如果需要进一步扩展
`06:14` 可以随时引用底层文件
`06:18` 如有必要
`06:19` 查看这些真伪题示例
`06:23` 甚至编程题等
`06:25` 可以看到这里
`06:26` 我们对范围和结构非常明确
`06:29` 以及这些问题所需的输出
`06:33` 深入查看输出格式
`06:36` 我们指定其依赖用户请求
`06:39` 而非为每种输出提供直接示例
`06:43` 实际引用资产文件夹中的模板
`06:48` 如果处理LATEX或Markdown
`06:51` 明确指定所需样式
`06:54` 例如在Markdown中
`06:56` 真伪题可能如下所示
`06:58` 在LATEX中
`06:59` 真伪题及示例可能呈现如下
`07:04` 如果你需要特定输出格式
`07:08` 无需全部写入技能MD文件
`07:10` 在外部资源或文件中引用
`07:14` 请记住这些文件
`07:15` 这些模板仅在必要时加载
`07:18` 因此我们可以高效使用令牌和上下文窗口
`07:22` 仅加载我们需要的数据格式特定文件
`07:27` 如果需要领域特定的示例资源
`07:31` 我们也可以链接到这些资源，就像我们在参考文件夹中做的那样
`07:35` 我们正在采用渐进披露的概念
`07:39` 仅加载绝对必要的内容并仅引用外部文件
`07:44` 当我们需要时
`07:45` 我们将要学习的第二个技能是分析时间序列数据
`07:50` 我们将提供一个csv文件
`07:52` 并希望在进行多种预测前理解其特征
`07:57` 需要注意的是，随着我们进行这项技能的学习
`08:02` 我们需要遵循一个非常特定的确定性工作流程
`08:06` 我们使用多个Python脚本来执行该操作
`08:11` 首先
`08:12` 我们有一个用于可视化数据的Python脚本
`08:16` 绘制时间序列
`08:17` 直方图
`08:18` 滚动统计
`08:20` 箱线图
`08:21` 以及更多用于自相关分析的脚本
`08:24` 我们还可以使用分解方法绘制类似的图表
`08:29` 当我们查看诊断pi时
`08:32` 我们有分析数据的基础功能
`08:37` 虽然这里有很多函数
`08:39` 我想重点说明我们最终的操作
`08:42` 当我们运行诊断时
`08:44` 我们使用这些函数分析数据质量分布
`08:49` 平稳性检验
`08:50` 季节性分析
`08:51` 趋势检测
`08:51` 自相关分析
`08:52` 最后给出转换建议
`08:56` 这里有一个我们需要每次运行的可预测工作流程
`09:00` 按特定顺序
`09:02` 现在回到技能部分，看看具体是如何实现的
`09:07` 首先我们将确定输入格式
`09:10` 我们将明确指定需要
`09:12` 应查找列名和特定数据类型
`09:16` 接下来我们将进入该技能最重要的部分
`09:20` 此处的工作流程说明
`09:22` 我们非常明确地定义步骤，告知特定技能
`09:27` 并让Claude在开始诊断时运行此脚本
`09:33` 然后可以选择生成必要图表并向用户报告数据
`09:38` 处理这些数据
`09:39` 查找摘要txt中的内容
`09:42` 并展示相关图表
`09:44` 我们还可以看到，针对可能需要的问题
`09:48` 我们有解释说明的md文件
`09:51` 查看部分脚本选项时
`09:53` 必要时可添加额外参数
`09:56` 当我们开始考虑输出内容
`09:59` 我们可以精确指定输出的文件树
`10:02` 文本文件
`10:03` 图像文件
`10:04` 等等我们输出的内容
`10:05` 我们希望对输入的数据保持高度可预测性
`10:09` 我们执行的操作
`10:11` 最后输出结果始终如一
`10:14` 如果存在外部引用
`10:16` 我们可以在此列出这些引用
`10:18` 鉴于我们使用依赖Python库的脚本
`10:22` 必须明确标注这些依赖项
`10:26` 并确保它们已安装
`10:27` 以便脚本正确运行
`10:29` 现在我们已经查看了这两个自定义技能
`10:33` 让我们通过技能创建器技能运行测试
`10:37` 并确定是否遵循最佳实践
`10:40` 我们可以在多个环境中进行
`10:42` 我们可以返回Claude桌面
`10:44` 但我想展示如何使用Claude代码与技能
`10:47` 我们将在后续课程中深入讲解
`10:50` 但此刻我将打开Claude代码
`10:53` 我将安装所需的技能（在本例中为）
`10:56` 技能创建器
`10:57` 然后我们将并行使用两个子代理
`11:00` 评估我们的时序分析和生成练习题技能
`11:05` 这是启动评估流程的便捷方式
`11:09` 检验我们编写技能的效果如何
`11:12` 我们将继续进入Claude代码
`11:15` 与Claude AI不同
`11:17` Claude代码未预装技能创建器等内置技能
`11:22` 因此需要手动安装
`11:24` 我们将通过市场进行安装
`11:27` 我们将前往市场页面
`11:29` 添加Anthropic技能市场
`11:32` 这是之前看到的包含两个集合的仓库
`11:37` 首先是文档处理技能，包括处理Excel文件
`11:42` 演示文稿
`11:43` Word文档和PDF
`11:45` 另一个集合是示例技能
`11:48` 包含我们之前见过的其他技能
`11:50` 包括技能创建器技能
`11:52` 让我们在项目范围内安装
`11:55` 安装完成后需要重启Claude代码
`11:59` 同时在我们的.config/clade/settings.json中
`12:04` 会看到已启用的插件包含这些技能
`12:07` 现在重启Claude代码查看技能列表
`12:12` 我们可以这样做
`12:14` 使用/skills命令
`12:17` 我们操作正确
`12:18` 应该看到技能创建器技能已正常显示
`12:22` 我们继续利用这个技能
`12:25` 所以我们要询问claude code
`12:26` 使用技能创建者技能
`12:28` 评估这些技能是否遵循最佳实践以更快完成任务
`12:33` 我们将并行使用子代理
`12:35` 每个子代理评估我拥有的每个自定义技能
`12:39` 为了做到这一点
`12:40` 我们将提示使用技能创建者技能
`12:43` 这很棒
`12:44` 运行正常
`12:45` 成功加载该技能
`12:47` 读取必要文件并启动子代理检查最佳实践
`12:54` 我们可以看到
`12:55` 已找到正确技能
`12:56` 生成练习题
`12:57` 分析时间序列
`12:59` 现在启动两个代理评估这些技能是否符合最佳实践
`13:03` 就是这样
`13:05` 看看我们做得如何
`13:07` 还不错
`13:08` 生成练习题九成达标
`13:10` 我们可以稍微提升简洁性
`13:13` 这里还有一些良好建议
`13:15` 好消息是我们分析时间序列更出色
`13:18` 这里可以看到一些观察结果
`13:21` 在避免重复方面表现优异
`13:24` 前言部分
`13:25` 质量和简洁性
`13:26` 通过技能创建者评估技能是一个很好的方法
`13:30` 内置了最佳实践
`13:33` 我们已运行技能通过技能创建者
`13:36` 分析底层技能的最佳实践
`13:39` Md及相关文件
`13:41` 如何确保技能正常运行
`13:44` 这里有一个例子
`13:46` 我们可以围绕它构建测试框架来编写技能单元测试
`13:50` 类似于为软件编写单元测试
`13:53` 首先处理生成练习题
`13:56` 考虑评估可能的流程
`13:59` 我们将使用几个不同查询
`14:01` 生成问题并保存到markdown文件
`14:05` 保存到lotech文件
`14:06` 保存到pdf
`14:07` 确保正确传递文件格式
`14:13` 确认预期行为符合需求
`14:17` 使用正确的pdf输入库
`14:20` 提取我们指定的学习目标
`14:23` 生成不同类型问题并遵循正确输出结构
`14:29` 使用资产文件夹中的正确输出模板
`14:34` 确保在lotech等特定数据格式中成功编译
`14:38` 然后最后确保我们的问题生成到正确的文件和正确的格式
`14:45` 我们也需要确保在这个过程中收集人类反馈
`14:48` 并测试这些流程在所有计划使用的模型中是否适用
`14:53` 对于分析时间序列的第二项技能
`14:56` 我们使用了三个不同的Python脚本
`14:59` 所以
`14:59` 我们将做出一个假设
`15:00` 我们已经用软件的传统单元测试验证过这些Python脚本
`15:05` 假设这些脚本能够实现我们的预期功能
`15:08` 现在让我们测试所有步骤是否按正确顺序执行
`15:12` 确保输入输出及预期行为符合要求
`15:16` 你可能需要分析并生成某些时间序列的图表
`15:20` 我们需要传入一些潜在的CSV文件
`15:24` 确保我们展示的可视化和诊断脚本
`15:29` 能够正确运行
`15:30` 更重要的是
`15:31` 确保工作流程中的所有步骤顺序正确
`15:35` 我们要求生成图表
`15:36` 需要确保可选步骤被包含在内
`15:39` 然后返回一个总结
`15:41` 解读这些发现
`15:43` 最后创建包含所有必要文件的文件夹
`15:48` 如果在输出中记得
`15:50` 我们为不同文件设定了特定位置
`15:53` 不同文件夹
`15:54` 和底层资产
`15:56` 类似我们其他技能
`15:57` 我们需要获取人类反馈并测试下一课中使用的模型
`16:02` 我们将把这两个技能整合到Jupyter笔记本中
`16:06` 使用Claude消息API运行这些技能
`16:10` 通过代码执行工具生成程序化输出
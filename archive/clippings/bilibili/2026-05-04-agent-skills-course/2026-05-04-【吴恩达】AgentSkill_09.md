---
title: "【吴恩达】2026年公认最好的【Agent Skills】教程！大模型入门到进阶，一套全解决！Agent Skills with Anthropic—附课件代码"
url: "https://www.bilibili.com/video/BV1qv6eBZErD?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=9"
bvid: "BV1qv6eBZErD"
cid: "35698835592"
author: "吴恩达深度学习"
upload_date: "2026-01-30"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115981733136330&bvid=BV1qv6eBZErD&cid=35698835592&page=9&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1、使用开放标准格式和最佳实践创建可重复使用的技能，并组合以创建复杂的工作流程。
2、建立定制代码生成技能，审核你的代理按需加载的工作流程、数据分析和研究。
3、结合MCP和子代理的技能，创建强大的代理系统，具备专业知识和外部数据源的访问权限

## 字幕

`00:00` 在本节最后一课中
`00:01` 我们将使用Claude代理SDK创建一个研究代理
`00:05` 该代理将利用技能为开源工具生成学习指南
`00:09` 基于其文档资料
`00:10` GitHub
`00:11` 仓库和网络搜索
`00:13` 现在我们已经了解了如何在网页上使用技能
`00:17` 借助Claude
`00:18` 使用消息API和Claude代码
`00:20` 让我们讨论如何使用Claude代理SDK中的技能
`00:24` 作为复习
`00:25` Claude代理SDK是构建自主应用的编程方式
`00:31` 使用与Claude代码相同的内部框架
`00:35` 我们即将构建的是通用研究代理
`00:39` 主代理将能够从多个来源获取信息并生成摘要
`00:45` 它将派遣三个不同子代理进行文档分析
`00:49` 分析和下载仓库
`00:52` 并通过网络搜索研究信息
`00:55` 让我们看看这些提示
`00:56` 然后我们将查看用于引导主代理的技能
`01:00` 包含研究方法和需要提取与合成的内容
`01:05` 这是我们主代理的提示
`01:07` 这是协调器，可访问三个子代理及其功能
`01:13` 从文档中获取信息
`01:15` 分析仓库结构
`01:17` 查找文章
`01:18` 视频
`01:18` 以及社区内容以整合所有信息
`01:21` 在这个特定应用中
`01:23` 我们提到如果提供技能
`01:26` 希望遵循特定模式
`01:28` 技能可能或可能不适用于我们正在构建的应用
`01:33` 但在我们案例中将提供一个
`01:35` 如果技能匹配用户请求
`01:37` 必须严格遵循技能指示
`01:41` 由于这是从零开始的应用
`01:44` 我们需要明确处理提供技能时的操作
`01:48` 或未提供时的情况
`01:49` 继续下去
`01:50` 我们有几个高层次的代理分配指南
`01:54` 在接收结果后
`01:56` 如何整合所有信息片段
`02:00` 让我们简要了解子代理的提示
`02:03` 对于文档研究者
`02:05` 我们将访问网络搜索和网页获取功能
`02:07` 提供定位文档的流程
`02:10` 特定输入格式
`02:12` 指南
`02:12` 并返回符合要求的发现结果
`02:17` 我们还提供网络搜索
`02:19` 要查找仓库
`02:20` Bash命令
`02:21` 克隆并运行Git
`02:22` 然后能够读取和查找文件及文件内的数据
`02:26` 同样我们提供了一个流程
`02:28` 一种输入格式
`02:30` 指南和输出结果
`02:32` 最后我们的网络研究者也会使用网络搜索和网页抓取
`02:36` 这使我们能够搜索与该主题相关的内容
`02:40` 同时也能接收主代理的提取指令
`02:44` 我们也提供必要的指南和输出格式
`02:48` 如果没有指定输出格式
`02:50` 遵循默认结构
`02:52` 所有这些提示将共同使用
`02:54` 当我们设置代码使代理正常运行时
`02:58` 最后让我们谈谈这里将要使用的技能
`03:01` 我们有一个名为学习工具的技能
`03:04` 该技能的目的是指导主协调器
`03:08` 我们不会在子代理中使用此技能
`03:11` 但我们将此技能用于创建可预测的模式
`03:15` 以便主代理了解理想的工作流程及如何调度子代理
`03:21` 我们为该技能命名并添加描述
`03:23` 在此我们希望为编程工具创建学习路径
`03:27` 确定需要研究的信息
`03:29` 并指定最佳研究方法
`03:32` 从研究到创建全面学习路径
`03:37` 我们这里有一个特定的工作流程
`03:40` 我们从研究阶段开始
`03:42` 并为该子代理指定官方文档
`03:44` 明确为仓库分析器查找的内容
`03:50` 类似的处理方式
`03:51` 对于我们的网络研究者也是如此
`03:54` 我们使用此技能提供稳定可预测的工作流程
`03:59` 以便主代理与子代理协同工作
`04:03` 一旦获得数据
`04:05` 我们将内容组织成渐进层级
`04:08` 在这里我们使用渐进披露加载另一个Markdown文件
`04:13` 作为事实来源
`04:14` 在我们的渐进学习文件中
`04:16` 可以看到我们希望的各个层级内容很多
`04:20` 从概述和动机开始
`04:23` 一直到安装核心概念
`04:25` 实用模式
`04:26` 然后下一步去哪里
`04:28` 这种渐进学习使我们能够构建层级
`04:33` 从而知道如何从头开始
`04:36` 并最终深入探索
`04:39` 虽然此初始技能适用于学习工具
`04:42` 也可以想象我们可能有其他技能用于
`04:44` 比如比较不同工具
`04:46` 当我们进入与这项技能合作的后续阶段时
`04:50` 我们获取数据并指定结构
`04:53` 然后定义输出格式
`04:55` 我们非常
`04:56` 对使用的精确格式要求很高
`04:59` 目标是获得提供资源概览的学习环境
`05:04` 一条路径和代码示例
`05:07` 目标是将所有子代理的研究成果
`05:10` 整合到所需的特定输出格式中
`05:13` 并保持一致性和可预测性
`05:16` 现在我们已大致了解要构建的应用程序
`05:19` 还有一个最后的组件需要添加
`05:22` 我们可以想象需要将输出写入集中位置
`05:26` 以便与界面更友好的团队成员共享
`05:29` 为此我们将使用Notion连接到Notion
`05:32` 我们将使用MCP服务器
`05:34` 并引入执行所需工具
`05:37` 现在我们已分析主代理和子代理的基础提示
`05:43` 以及将要使用的技能
`05:45` 现在让我们开始运行uv
`05:48` 初始化并创建项目，添加必要依赖如Claude代理
`05:53` SDK
`05:54` Python
`05:55` Env和异步
`05:56` I/O
`05:58` 安装这些依赖后
`06:00` 我们创建一个名为agent.py的文件
`06:03` 我将新建一个文件
`06:05` 在agent.py内命名为agent.py
`06:09` 将添加必要代码开始
`06:13` 通过小型示例
`06:14` 使用Claude代理SDK
`06:17` 这里的基础代码包含异步
`06:20` I/O以运行环境和加载环境变量
`06:24` 并从util导入显示消息函数
`06:28` 仅用于提供上下文显示消息
`06:30` 提供截断和格式化输入的辅助功能
`06:34` 并且
`06:35` 为我们提供展示主代理和子代理信息的可视化方式
`06:40` 这与我们使用API时看到的代码非常相似
`06:44` 并获得每个工具运行的清晰输出
`06:47` 操作和迭代过程
`06:51` 首先
`06:52` 我们设置Claude代理
`06:53` 传入系统提示
`06:55` 此处的提示
`06:56` 这将会改变
`06:57` 传入允许的工具
`06:58` 这也是需要调整的
`07:00` 但我们先从基础开始
`07:02` 开始一个简单对话
`07:04` 我们设置一个循环
`07:06` 接受用户输入
`07:08` 将输入通过模型处理并返回响应，再发送回用户
`07:13` 让我们看看这会是什么样子
`07:15` 我再次打开终端
`07:17` 接下来我们将运行
`07:19` Uv run agent
`07:20` 我 e
`07:21` 这将为我们提供终端环境
`07:24` 我们可以在此开始对话
`07:27` 我们先问‘你好吗’
`07:29` 在此情况下
`07:30` 这里不会获取大量有价值信息
`07:32` 因为我只是一个有帮助的助手
`07:34` 接下来我们将开始叠加功能
`07:36` 现在需要让代理访问MCP服务器
`07:41` 以及正确的工具
`07:43` 我们继续修改主函数
`07:47` 正如之前所说
`07:48` 允许的工具将发生变化
`07:50` 我们将开始添加子代理所需的工具
`07:56` 以确保正常运行
`07:58` 只读工具如read、grep和glob默认允许
`08:02` 但当我们需要执行写文件操作时
`08:05` 进行网络搜索
`08:06` 以及通过bash执行命令
`08:08` 需要明确指定这些工具
`08:10` 我们将引入正确工具
`08:12` Bash工具
`08:14` 以及我们的网络搜索和获取工具
`08:19` 之前我们看到子代理将使用这些工具
`08:23` 负责分析仓库的代理
`08:26` 需要Bash运行Git命令和写文件
`08:28` 以及我们的文档
`08:29` 研究员和网络研究员将使用搜索和获取功能
`08:33` 现在我们引入了这些工具
`08:34` 接下来我们将添加连接的MCP服务器
`08:38` 使用MCP服务器的关键词参数
`08:41` 指定MCP服务器名称
`08:44` 在我们这里为Notion
`08:46` 我们将传入默认配置
`08:50` 并指定运行Notion服务器的命令
`08:54` 同时使用已有的Notion环境变量
`08:58` 所以在继续之前
`09:03` 从环境文件加载Notion令牌
`09:04` 并导入os模块以正确读取文件
`09:09` 现在已正确加载MCP服务器
`09:12` 我们需要使用Notion提供的工具
`09:15` 如果我们需要
`09:16` 我们现在可以立即询问claude
`09:18` 这是mcp服务器提供的所有工具有哪些
`09:21` 或者我们可以直接通过使用mcp服务器名称显式添加
`09:28` 接着输入工具名称
`09:30` 在这个案例中我们将使用notion提供的所有工具
`09:35` 需要确保mcp notion存在于允许的工具列表中
`09:39` 以便为主代理授予使用这些工具的权限
`09:43` 我们可以显式添加工具名称
`09:45` 在我们的情况中
`09:46` 我们将包含mccp notion提供的所有可用工具
`09:51` 现在我们已配置好mcp服务器和允许的工具
`09:54` 接下来引入子代理及其定义
`09:59` 我们提到系统提示将发生变化
`10:02` 首先
`10:03` 我们将加载所有现有提示
`10:10` 需要加载所有这些提示
`10:13` 我们将调用该函数
`10:16` 以加载这些提示
`10:18` 在主函数内部
`10:20` 我们将使用markdown文件加载必要文本
`10:24` 并将它们传递给代理选项后再更新主代理
`10:30` 我们将添加一个引用所有代理的字典
`10:33` 通过引入代理定义类
`10:37` 需确保正确导入
`10:40` 在代理定义中
`10:42` 我们为子代理编写了描述
`10:44` 指定了代理的指令提示
`10:47` 然后是代理所需工具的配置
`10:51` 与claude代码中的配置类似
`10:53` 可以看到这里
`10:54` 仍需使用主代理提示和代理字典
`10:59` 我们将系统提示更新为主代理提示
`11:04` 然后确保传递额外的agents关键字参数
`11:08` 引用包含代理定义的字典
`11:12` 如您所见
`11:13` 我们的研究员
`11:14` 分析员和网络研究员也在使用此处定义的工具
`11:20` 必须确保列出所有工具
`11:24` 主代理和子代理需要使用的工具
`11:29` 否则子代理将无法使用
`11:31` 即使在此处包含工具
`11:34` 现在我们已配置好代理
`11:35` 还需包含至关重要的任务工具
`11:39` 以实现分配子代理和任务
`11:43` 最后需要添加的技能部分
`11:48` 只需再添加一个工具
`11:51` 那就是技能工具
`11:53` 由于此处存在文件系统环境
`11:56` 并能通过bash工具执行代码
`11:59` 我们需要添加的只是这个技能工具
`12:01` 这样我们才能正确读取技能并理解如何最佳使用
`12:06` 类似claude，代码技能定义在dot clade文件夹内
`12:10` 接着是名为skills的文件夹
`12:13` 确保你的markdown文件是skill.md
`12:16` 并且文件夹名称为复数形式skills
`12:19` 现在我们已添加了技能操作工具
`12:22` 还需要传递一个额外的关键字参数
`12:25` 这里需要指定找到这些技能的位置
`12:30` 通过名为setting_sources的关键字参数
`12:34` 在这里我们将指定
`12:35` 希望在用户目录中查找技能
`12:39` 如果我们同时在主目录和项目中都有技能
`12:43` 这就是我们为该应用加载技能的位置
`12:47` 现在将所有内容整合完毕
`12:49` 让我们测试一下这个代理
`12:51` 我将再次打开终端
`12:53` 我将退出并重新运行这个应用
`12:57` 包含我们所做的更改
`12:59` 我们先简单了解minor u，对于不熟悉的用户
`13:04` Minor u是一个用于PDF提取的开源库
`13:07` 我们使用这个例子的原因
`13:09` 是因为claude可能并不了解
`13:12` 这并非其初始训练数据的一部分
`13:14` 这需要外部研究
`13:16` 分析代码仓库
`13:18` 社区文档和其他来源
`13:21` 我们将要求创建学习指南
`13:23` 然后展示计划
`13:24` 首先可以看到技能已被调用
`13:28` 输入是名为learning_tool的技能及指定参数
`13:34` 这里可以看到我们首先指定了计划
`13:37` 仍需运行子代理的具体操作
`13:41` 就像claude代码和计划模式一样
`13:43` 我们可能想先查看计划
`13:45` 在开始执行消耗令牌和时间之前
`13:48` 可以看到并行研究阶段的结构
`13:53` 根据技能要求的结构
`13:56` 最终输出看起来是个好计划
`14:01` 我们只需继续执行
`14:04` 将启动文档研究子代理
`14:07` 启动子代理
`14:07` 启动仓库分析器和网络研究员并并行执行
`14:12` 使用我们添加的工具
`14:14` 在子代理允许的工具范围内
`14:19` 可以看到并行处理
`14:20` 文档研究员前往文档
`14:23` 仓库分析器在GitHub上搜索
`14:25` 网络研究员在教程和YouTube指南中检索
`14:30` 从GitHub仓库提取信息
`14:32` 使用bash命令
`14:34` 同时在YouTube频道搜索视频演示
`14:38` 这些代理正在并行交互
`14:40` 从不同数据源获取信息整合成完整教程
`14:46` 现在子代理已完成工作
`14:49` 我们将创建综合指南
`14:51` 根据此处研究整理必要文件
`14:56` 按照仓库分析器的指示
`14:59` 我们克隆了minor u仓库并保留在此
`15:03` 并开始构建学习所需的文件夹结构
`15:06` 我们可以看到这里
`15:07` 我们有readme和资源文件
`15:09` 以及正在整合的代码示例
`15:12` 在readme文件中可以看到
`15:14` 它为我们提供了学习路径
`15:16` 我们将学习如何使用这份指南
`15:19` 更重要的是
`15:20` 可能需要的时间估算
`15:22` 我们可以看到这里
`15:23` 已经为我们创建好了
`15:24` readme文件
`15:25` 资源和学习路径仍在资源中完善
`15:29` 我们有minor you的链接和参考
`15:32` 让我们查看资源中的内容
`15:35` 我们有文档
`15:36` 仓库内容
`15:37` Pi包和该库的论文
`15:41` 有快速入门指南
`15:42` 文档及相关项目，随着社区信息的整合
`15:47` 我们有各种深度解析的文章和新闻报道
`15:53` 现在学习路径已创建完成
`15:56` 是时候创建代码示例了
`15:59` 让我们看看这个学习路径
`16:02` 从概述和动机开始
`16:04` 它解决了什么问题
`16:06` 我们描述了该库的起源故事，之前的状况
`16:10` 以及这些库存在的问题
`16:13` 这是一个相当深入的指南和学习路径
`16:15` 可以想象这将持续很长时间
`16:18` 随着你逐步上手
`16:20` 从一无所知到成为该库专家
`16:24` 我们进入该库后端的特色功能
`16:28` 一直到代码示例和高效使用方法
`16:35` 我们可以看到hello world示例代码正在编写
`16:39` hello world示例的概念和模式
`16:43` 我们有不错的readme文件开始第一步
`16:46` 简单提取以了解如何使用该库
`16:49` 以及安装步骤
`16:52` 如果需要特定库或安装模式
`16:55` 我们可以随时添加到技能列表
`16:57` 但目前这将为我们提供一个绝佳的起点，开始使用这个库
`17:02` 当我们查看当前正在创建的一些核心概念
`17:06` 现在这些都已完成
`17:07` 我们可以看到readme中接下来的步骤
`17:10` 一旦我们成功搭建了库的环境
`17:12` 我们可以开始探索
`17:13` 以及该库所具备的一些基本概念
`17:16` 同时比较不同后端的速度差异
`17:19` 最后我们将在这个第三文件夹中创建实用模式和示例
`17:25` 我们可以查看这个文件夹
`17:26` 可以看到这里包含实际处理流程和生产使用案例
`17:31` 这包括某些模式的示例
`17:33` 以及相当深入的代码示例
`17:36` 使用该库配合文档字符串
`17:39` 注释和所有必要内容以充分利用该库
`17:43` 我们将通过验证并创建总结文档来收尾
`17:47` 并确保所有步骤正确完成
`17:50` 我们可以查看输出结果
`17:51` 这将提供完整的学习指南
`17:53` 我们技能中指定的目录结构
`17:56` 我们请求的学习路径及等级
`17:59` 然后是关键特性和快速入门指南
`18:03` 接下来我们要做的最后一件事是编写这个特定文件
`18:07` 将resources.md文件上传到Notion的子页面
`18:11` 这个页面已经存在
`18:12` 让我们看看它的样子
`18:14` 然后我们将提示使用mccp服务器执行
`18:17` 所需的编写操作
`18:19` 在Notion的学习部分可以看到
`18:22` 我有一个名为资源的子页面
`18:25` 目标是使用mcp服务器填充资源内容
`18:29` Md文件
`18:30` 放到这里
`18:31` 因此让我们让代理将该文件写入Notion的子页面
`18:39` 我们将明确使用Notion中的工具
`18:41` 并允许其调用可用资源
`18:44` 我们已找到资源页面
`18:45` 将读取resources.md并转换为Notion的正确格式
`18:50` 使用丰富的Notion块
`18:52` 可以看到这里
`18:53` 我们正在使用Notion的多个工具
`18:55` 分批处理这些操作
`18:56` 添加快速入门API文档
`18:59` 并将资源内容填充到现有页面
`19:03` 在资源文件中可以看到
`19:05` 内容会根据resources.md动态更新
`19:11` 当处理完成后
`19:12` 我们将看到所有内容出现在Notion页面上
`19:16` 现在处理完毕
`19:17` 让我们查看Notion页面的最终效果
`19:20` 我们可以在这里看到
`19:21` 我们拥有官方文档
`19:23` 我们的教程
`19:24` 视频资源
`19:25` 社区频道
`19:26` 来自该markdown文件的所有数据
`19:28` 我们现在已写入Notion
`19:30` 我们已运用技能
`19:32` Mcp
`19:32` 服务器
`19:33` 代理和子代理
`19:34` 全部使用代理SDK
`19:36` 可以想象叠加更多技能以实现更复杂的流程
`19:41` 或添加更多子代理执行多样化任务
`19:44` 我们刚刚开始探索功能
`19:47` 仍需注意一些安全问题
`19:52` 我们允许无需用户授权即可执行如右键和bash命令
`19:57` 下一步是构建界面
`20:00` 就像Claude代码
`20:01` 确保用户确认使用特定工具
`20:04` 以执行某项操作
`20:08` 我们同样刚刚开始探索
`20:10` 甚至可以添加中断功能给代理和子代理
`20:14` 类似于Claude代码
`20:15` 我们已为您提供构建强大应用的基础
`20:20` 我们迫不及待想看到你接下来的成果
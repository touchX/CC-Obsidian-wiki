---
title: "【吴恩达】2026年公认最好的【Agent Skills】教程！大模型入门到进阶，一套全解决！Agent Skills with Anthropic—附课件代码"
url: "https://www.bilibili.com/video/BV1qv6eBZErD?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=5"
bvid: "BV1qv6eBZErD"
cid: "35698773235"
author: "吴恩达深度学习"
upload_date: "2026-01-30"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115981733136330&bvid=BV1qv6eBZErD&cid=35698773235&page=5&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1、使用开放标准格式和最佳实践创建可重复使用的技能，并组合以创建复杂的工作流程。
2、建立定制代码生成技能，审核你的代理按需加载的工作流程、数据分析和研究。
3、结合MCP和子代理的技能，创建强大的代理系统，具备专业知识和外部数据源的访问权限

## 字幕

`00:00` 在第一课中
`00:01` Claude AI 使用 Excel 技能创建了展示营销结果的电子表格
`00:06` Excel 技能是 Anthropic 预置的技能之一
`00:09` 还包括 PowerPoint
`00:11` Word 和 PDF 技能
`00:13` 以及技能创建技能
`00:16` 让我们看看这些
`00:17` 现在我们已经了解了技能在整个 AI 生态系统中的位置
`00:22` 让我们看看一些可以直接使用的预
`00:24` 置技能
`00:27` 在 Claude AI 和 Claude 桌面中
`00:29` 并且可以通过 Claude Code 等工具自行安装
`00:33` 在这个位于 GitHub.com 的仓库中
`00:36` Slash anthropic-skills
`00:38` 让我们查看技能文件夹并查看其中内置的
`00:42` 我们拥有的
`00:42` 所有这些都已准备好生产环境使用
`00:45` 我们在之前的课程中实际看到了这个 Excel 技能的用例
`00:51` 需要注意的是，虽然这些技能列表是按需创建的
`00:55` Anthropic 实际上分为两个不同部分
`00:58` 微软相关技能
`01:00` 文档
`01:01` PDF
`01:02` PowerPoint 和 Excel 被称为文档技能
`01:06` 这些是内置的并在 Claude AI 等工具中始终使用
`01:10` 其余技能是我们创建的示例
`01:14` 您可以在 Claude 中开启或关闭这些技能
`01:17` 但默认情况下
`01:18` 除技能创建者外均处于关闭状态
`01:21` 首先我们分析 PowerPoint 技能，可以看到
`01:26` 与其他结构一样，我们有一个技能 md 文件
`01:30` 以及用于引用此技能 md 的其他文件和文件夹
`01:36` 我们有相同的 YAML 前置内容，包含名称和描述
`01:42` 这里展示的是 GitHub 如何渲染此 Markdown 文件
`01:46` 但底层代码与我们之前创建的非常相似，可以这样查看
`01:53` 如果您熟悉 Markdown 文件
`01:55` 我切换回预览模式
`01:57` 因为这样看起来更整洁
`01:59` 当我们查看该技能的工作原理和功能时
`02:02` 我们有概述部分
`02:04` 用户可能要求创建
`02:06` 编辑
`02:07` 分析
`02:07` PowerPoint 文件内容
`02:09` 这就是它的表现形式
`02:10` 这就是它的读取方式
`02:12` 如果需要执行特定任务
`02:15` 有底层脚本可以执行
`02:18` 请记住这些不会立即执行
`02:22` 这些仅在必要时加载和执行
`02:25` 在 PowerPoint 演示文稿中我们可以做很多事情
`02:29` 颜色拓扑结构
`02:31` 正如你所想象的
`02:32` 这就是我们开始制作更美观、更接近
`02:36` 开箱即用的真实世界演示文稿的方法
`02:38` 我们有一些设计原则和必要要求
`02:42` 还可以选择配色方案
`02:45` 当用户未指定时可从中选择
`02:48` 这个技能 md 文件相当长
`02:50` 因为我们在 PowerPoint 演示文稿中可以做很多事情
`02:54` 但稍后我们将看到
`02:55` 在本课中
`02:56` 如何实际使用这个技能将现有数据转化为美观的演示文稿
`03:03` 接下来要介绍的技能是一个元概念
`03:08` 这就是所谓的技能创建器
`03:10` 技能创建器是一个用于程序化创建技能的工具
`03:17` 无需从头开始创建必要文件和文件夹结构
`03:23` 技能创建器可以自动完成这些操作
`03:25` 让我们查看技能 md 文件并了解其中内容
`03:29` 与我们的其他技能类似
`03:31` 我们有名称和描述
`03:33` 我现在要查看底层代码
`03:36` 因为这样更容易理解
`03:38` 我们在技能 md 文件中指定
`03:41` 什么是技能
`03:43` 它提供什么功能
`03:44` 然后包含与技能相关的最佳实践
`03:49` 我们将在下一课深入探讨这些最佳实践
`03:52` 但可以想象当 Claude 程序化创建技能时
`03:57` 我们希望利用这些最佳实践
`03:59` 当我们查看技能创建过程时
`04:03` 这里我们非常明确步骤
`04:06` 因为我们希望使用此技能创建可预测的工作流程
`04:10` 我们必须非常明确步骤内容
`04:14` 如何执行并仅在必要时跳过
`04:17` 如果存在某些原因
`04:19` 我们从具体示例开始
`04:21` 规划可复用的技能内容
`04:24` 你可以开始看到有助于 Clawed 模式匹配的示例
`04:30` 当需要创建新技能时
`04:32` 当我们开始初始化技能时
`04:35` 正在运行底层 Python 脚本执行必要任务
`04:40` 让我们看看这些脚本的作用
`04:44` 在脚本文件夹内
`04:46` 这里有三个 Python 文件：初始化技能并提供基础文本的脚本
`04:53` 一个打包该技能的 Python 文件
`04:56` 还有一个验证技能的脚本
`04:59` 让我们看看底层代码如何初始化技能
`05:03` 我们使用带有 YAML 前置内容和占位符的模板
`05:08` 以及待办事项并填充相关内容
`05:11` 基于传入的数据，此基础脚本允许我们创建必要的文本文件
`05:18` 在构建我们的技能时
`05:20` 生成必要文件后
`05:23` 我们可以在这里进行打包
`05:25` 你可以看到我们正在引入必要的模块
`05:27` 将我们的技能进行压缩打包
`05:29` 并确保我们在正确的文件夹和目录结构中操作
`05:34` 最后我们有一个最终脚本来验证我们的技能
`05:38` 确保存在技能md文件
`05:41` 验证部分yaml前置内容
`05:44` 并确认我们文件夹和文件内的内容正确
`05:49` 我们将使用这个技能创建器技能
`05:52` 处理我们已有的内容
`05:54` 将其打包成可复用且模块化的脚本
`05:59` 现在让我们切换回claude，看看如何整合
`06:03` 内置技能
`06:04` 我们自己的技能
`06:05` 并在claude的mcp服务器中建立可预测的工作流程
`06:10` 让我们检查并确保正确启用了所需技能
`06:14` 并在设置中的能力部分查看它们的位置
`06:21` 我们可以在该部分创建新技能
`06:23` 我想展示的是我们现有的示例技能
`06:27` 这应该看起来很熟悉
`06:29` 这是我们之前在github上看到的内容
`06:31` 默认情况下
`06:32` 这些技能处于关闭状态
`06:34` 如果我们想启用它们
`06:35` 完全可以操作
`06:37` 默认启用的技能是之前看到的技能创建器
`06:42` 需要注意的是
`06:44` 虽然技能创建器非常擅长生成底层技能和必要结构
`06:50` 但我们仍需明确提供的提示内容
`06:54` 以及输入到目标技能中的数据
`06:58` 现在我们将整合所有关于技能的思路
`07:02` 结合mcp和提示技术
`07:05` 首先我们将修改之前创建的用于分析活动的技能
`07:11` 不再使用csv作为数据源
`07:14` 而是使用bigquery
`07:16` 如果你不熟悉
`07:17` Bigquery是谷歌提供的数据存储服务
`07:20` 为了引入必要的工具和上下文以使用bigquery
`07:25` 我们将连接mcp服务器
`07:28` 因此我们将使用技能创建器技能修改之前的营销
`07:33` 分析技能
`07:34` 以使用bigquery
`07:35` 接下来我们将使用技能创建器创建另一个技能
`07:40` 此技能用于品牌指南
`07:43` 我们将包含指定指南和logo的文件
`07:48` 并构建另一个执行该任务的技能
`07:52` 最后我们将整合用于提取和分析数据的两个技能
`07:57` 并利用品牌指南
`07:59` 并结合内置的创建PowerPoint演示文稿技能
`08:04` 创建一个利用提示技能和模型上下文协议的工作流程
`08:10` 在我们开始之前
`08:11` 你可能会想知道之前看到的Excel和PowerPoint
`08:15` 以及其他文档技能是否仍然有效
`08:18` 这些已内置到Claude AI中
`08:20` 无法开启或关闭这些功能
`08:23` 考虑到这一点
`08:25` 我们在修改分析营销活动技能前先启动此工作流程
`08:30` 为了使用BigQuery
`08:32` 我们也需注意我们正在使用Claude桌面
`08:35` 以连接本地MCCP服务器并利用BigQuery
`08:40` 让我们看看BigQuery服务器的配置
`08:44` 我将前往设置开发者选项
`08:47` 这里可以查看特定项目的底层命令、参数和环境变量
`08:55` 以及我的凭证存储位置
`08:57` 在此示例中
`08:58` 我们无需使用BigQuery
`08:59` 也可以使用数据库
`09:01` 或其他外部数据存储
`09:02` 但我们只想展示其效果
`09:04` 展示技能与MCP服务器协同工作的效果
`09:08` 如果你想查看底层配置文件
`09:12` 这就是该配置文件的内容
`09:14` 我们指定了要连接的服务器
`09:17` 并定义了Claude桌面启动时执行的命令
`09:23` 我们现在修改之前的技能
`09:27` 改用BigQuery而非CSV进行数据访问
`09:32` 确保功能正常运行
`09:34` 首先请Claude列出BigQuery中存在的表
`09:42` 这将调用我们配置的MCP服务器
`09:45` 我们将允许此操作
`09:47` 应返回表列表
`09:49` 在此案例中只有一个表
`09:53` 可以看到
`09:54` 有一个名为marketing的数据集包含单个表
`09:57` 现在请Claude展示该表的结构
`10:05` 希望Claude能纠正这个小拼写错误
`10:08` 这样我们就准备好了
`10:10` 这里定义了表结构，效果很好
`10:14` 我们将使用此结构
`10:16` 在更新分析营销活动技能时
`10:21` 接下来要做的
`10:22` 是请Claude更新我们的分析营销活动技能
`10:27` 改用从BigQuery获取数据而非CSV上传
`10:30` 指定来自BigQuery表的数据
`10:34` 特别是刚才看到的表结构
`10:37` 由于我们处于同一对话中
`10:39` Claude应能轻松查看表结构
`10:42` 我们正在为这个设定一些要求
`10:45` 就像我们现有的技能一样
`10:47` 我们需要确保对预算调整规则的引用不会被修改
`10:52` 正如我们之前讨论的
`10:54` 技能创建者技能非常高效且实用
`10:57` 但我们仍需提供必要的上下文
`11:01` 请注意这里
`11:02` 它首先会分析所需的技能结构
`11:07` 并使用技能创建者技能修改现有技能并遵循最佳实践
`11:14` 我们现在将创建带有新技能的更新版本
`11:19` 在此md文件中我们可以看到类似之前技能的内容
`11:25` 但底层使用bigquery而非csv上传
`11:30` 我们使用文件系统和bash工具
`11:34` 为我们创建必要的文件和文件夹结构
`11:39` 我们可以看到这里不再使用csv或bigquery
`11:43` 我们遵循使用mcp服务器的最佳实践
`11:48` 其中指定服务器和工具名称
`11:52` 技能创建者正按照最佳实践修改现有技能
`11:58` 正如我们在指定输入时指示的那样
`12:03` 我们现在正在实际看到这一点
`12:05` 最佳实践是避免使用模糊的时间范围或全部范围
`12:10` 因此我们要求用户澄清
`12:12` 当我们展示查询示例时
`12:14` 我们指定了时间范围
`12:16` 我们设置的工具和要求正在直接应用于此技能更新
`12:23` 我们的技能看起来状态良好
`12:25` 为确保保存到后续对话
`12:29` 我们现在复制此技能
`12:36` 现在我们将切换到创建品牌指南新技能
`12:40` 用于与当前技能配合制作有说服力的数据驱动型演示文稿
`12:47` 现在开始新对话
`12:50` 我们将要求claude创建品牌指南技能
`12:54` 从我们上传的文件
`12:56` 首先我将上传品牌指南文件
`13:01` 以及演示中使用的logo
`13:05` 在创建此技能前
`13:08` 让我展示这些品牌指南内容
`13:10` 我有支持色的配色方案
`13:13` 版式设计
`13:14` Claude擅长设计
`13:16` 但技能真正发挥作用的地方
`13:18` 或让Claude明确
`13:19` 如何按照公司要求制作logo
`13:24` 颜色字体
`13:25` 很好的例子
`13:26` 现在创建此文件的技能
`13:30` 可用于未来演示和文档
`13:34` 我们将看到技能创建者
`13:37` 技能再次运行
`13:39` 我们利用现有工具和技能遵循最佳实践
`13:44` 同时提供指南和标志，以创建可重复且可移植的技能
`13:50` 我们将分析现有技能，观察它们使用的模式
`13:54` 确保新创建的技能能够与它们互补
`13:59` 这一点非常有价值，因为我们将在PowerPoint演示中使用它
`14:05` 现在我们对需要完成的工作有了清晰的思路
`14:07` 运行之前看到的初始化技能Python脚本
`14:11` 这将创建底层技能
`14:13` 现在我们可以开始向技能资源文件夹添加资产
`14:20` 我们将开始看到颜色
`14:21` 填充
`14:22` 强调色
`14:23` 字体
`14:24` 排版
`14:25` 稍后我们将拥有一个可以添加到所有未来对话的技能
`14:30` 当需要设计时
`14:33` 我们的标志正在被调用
`14:35` Word文档和PDF已指定
`14:38` 演示布局符合我们的要求
`14:42` 技能创建器已完成运行
`14:44` 这里生成了一个遵循最佳实践的技能md文件
`14:49` 包含名称和描述
`14:51` 以及包含必要数据和标志的底层文件夹
`14:57` 还有一个步骤需要完成
`14:59` 确保将其添加到未来对话
`15:02` 为确保保存到后续对话
`15:06` 我们继续复制这个技能
`15:10` 完成之后
`15:11` 应该能在创建的技能列表中看到它
`15:17` 现在我们已将技能从CSV更新为BigQuery
`15:22` 并创建了品牌指南新技能
`15:25` 将其整合以构建与内置PowerPoint技能并行的工作流
`15:31` 首先分析数据然后生成演示
`15:35` 所以
`15:35` 我们将首先在BigQuery中分析不同周的营销数据
`15:40` 查看各渠道表现
`15:42` 然后基于这些数据
`15:43` 生成符合品牌指南的演示
`15:47` 让我们看看效果如何
`15:50` 首先我们将读取相关技能文件
`15:53` 包括营销活动分析
`15:56` 还将包含BigQuery指南
`15:59` 我们将确保使用正确的PowerPoint演示技能
`16:03` 以及用于PowerPoint技能内部样式的品牌技能
`16:08` 还有演示创建的附加文档
`16:12` 首先我们将开始使用BigQuery
`16:14` 我们将查询所需内容
`16:17` 好的
`16:20` 我们可以查看正在生成的底层SQL
`16:23` 以及之前看到的日期范围
`16:27` 现在我们有了所需的数据
`16:29` 我们将使用这些指标生成演示文稿
`16:33` 我们将采用我们建议的品牌风格进行设计
`16:37` 并将此转化为演示文稿
`16:39` 你可以看到这里
`16:40` 正在为幻灯片编写的基础CSS和HTML代码
`16:44` 然后我们将利用内置的演示文稿创建技能
`16:51` 现在我们已准备好正确的HTML文件
`16:53` 让我们在此创建演示文稿
`16:56` 我们使用原生PowerPoint技能编写必要代码
`17:04` 我们可以看到这里
`17:04` 即使存在特定问题
`17:07` 模型会回溯修改
`17:08` 调整必要内容并遵循精确流程
`17:12` 不仅执行必要代码
`17:14` 还要验证所需操作
`17:18` 模型能够回溯并遵循特定模式的能力
`17:22` 使我们能创建无
`17:25` 需要立即修复的内置问题
`17:27` 然后进行修正
`17:29` 因此我们看到Clo已完成验证
`17:31` 幻灯片看起来很棒
`17:33` 现在将生成基础PowerPoint演示文稿
`17:37` 我可以在Google Drive中打开并作为Google幻灯片使用
`17:40` 或者直接下载
`17:43` 我们可以看到这里
`17:44` 我得到了色彩美观的幻灯片
`17:47` 字体
`17:48` 标志等公司所需元素
`17:52` 我们有效率分析
`17:53` 漏斗分析和执行摘要
`17:57` 需要审查的内容
`17:58` 表现良好的部分
`17:59` 我可以下载此演示文稿
`18:01` 我可以继续在此基础上完善
`18:03` 再次在Google Drive中打开与团队共享
`18:06` 我可以继续提示并与演示文稿协作
`18:10` 但这里展示的是基于内置技能生成的PowerPoint演示文稿
`18:15` 结合我们与MCP服务器开发的两项技能
`18:20` 下一课将从BigQuery获取数据
`18:23` 我们将探讨创建技能的最佳实践
`18:27` 并查看另外两个自定义技能
`18:30` 检查是否遵循最佳实践
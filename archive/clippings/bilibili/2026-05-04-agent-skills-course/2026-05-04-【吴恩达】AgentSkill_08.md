---
title: "【吴恩达】2026年公认最好的【Agent Skills】教程！大模型入门到进阶，一套全解决！Agent Skills with Anthropic—附课件代码"
url: "https://www.bilibili.com/video/BV1qv6eBZErD?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=8"
bvid: "BV1qv6eBZErD"
cid: "35698773769"
author: "吴恩达深度学习"
upload_date: "2026-01-30"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115981733136330&bvid=BV1qv6eBZErD&cid=35698773769&page=8&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1、使用开放标准格式和最佳实践创建可重复使用的技能，并组合以创建复杂的工作流程。
2、建立定制代码生成技能，审核你的代理按需加载的工作流程、数据分析和研究。
3、结合MCP和子代理的技能，创建强大的代理系统，具备专业知识和外部数据源的访问权限

## 字幕

`00:00` 我们现在将切换到claude代码并使用代码生成技能
`00:03` 正在审查和测试
`00:05` 我们还将设置子代理并为其配备技能
`00:09` 到目前为止让我们玩得开心些
`00:10` 我们已经了解了如何在clauai中使用技能以及使用clawed消息
`00:14` API现在让我们更深入地讨论如何在claude代码中使用技能
`00:19` 我使用的应用程序是一个用于创建的命令行应用
`00:25` 需要完成的任务和列出它们
`00:28` 最终进行编辑和清除
`00:30` 我将展示claude md文件
`00:32` 让您了解这个项目的作用
`00:34` 现在我们将进行一个小演示
`00:36` 在深入每个claude代码文件之前
`00:40` 您可以创建claude md文件
`00:43` 该文件通过init命令或手动创建
`00:48` 该文件始终处于您的上下文中且特定于项目
`00:52` 您可以在此指定关于正在开发的代码库项目的通用说明
`00:58` 技术栈以及claude在每次对话中都需要了解的内容
`01:03` 所以再次
`01:04` 我们正在构建一个命令行任务管理应用
`01:07` 使用python typer作为CLI框架
`01:10` 使用data classes rich
`01:12` 我们使用json文件存储信息以实现持久化
`01:16` 并使用uv进行依赖管理
`01:18` 我们的架构遵循
`01:20` 根据此模式
`01:21` 我们有入口点
`01:23` 所有命令都有各自的独立python文件
`01:26` 我们在models pi中设置数据类
`01:29` 我们的存储逻辑
`01:31` 序列化
`01:32` 在storage pi中进行序列化
`01:34` 然后在终端中美观显示
`01:36` 我们的display pi
`01:38` 我们有几个常量
`01:39` 然后在该文件中可以看到我们的测试
`01:42` 我们有优先级
`01:43` 任务数据模型
`01:45` 数据如何持久化并请记住
`01:47` 对于这个claude
`01:48` Md
`01:48` 这些数据始终在上下文中可用
`01:51` 在每次对话中
`01:54` 这些信息有助于claude
`01:56` 确定如何查找内容
`01:58` 以及如何最佳组织信息
`02:00` 基于此
`02:01` 让我们先进入应用进行体验
`02:04` 我将激活虚拟环境
`02:08` 所以我将执行source
`02:10` 然后然后激活一次
`02:13` 我已经设置好了
`02:14` 确保我的依赖项用uv同步按顺序排列
`02:25` 一旦我完成这些
`02:26` 我就可以直接在命令行使用这个任务命令
`02:30` 如果我查看这个命令
`02:32` 我可以看到像add、done和list这样的命令，还有一些额外选项
`02:37` 那我们来看看我现在有哪些任务
`02:42` 这里没有任何任务被找到
`02:43` 我将清空终端
`02:44` 这样我可以从头开始，现在添加一个任务
`02:48` 我们称之为
`02:49` 撰写最终报告
`02:51` 并为此任务设置高优先级
`02:54` 并为此任务设定完成日期为以下
`02:58` 我们可以看到
`02:59` 我已成功添加了该任务
`03:00` 那我们去看看
`03:02` 我们的任务就在那里
`03:04` 显示模块正在对信息进行良好格式化
`03:08` 我们继续完成它
`03:09` 我将用正确ID标记为已完成
`03:12` 然后如果我查看我的任务列表
`03:16` 通过该标志可以看到已完成的任务，如本课计划
`03:22` 这里要添加另一个命令行命令edit
`03:26` 所以我们需要进入src/task目录
`03:30` 并在此处添加编辑命令
`03:32` 但我们也需要确保新增命令时
`03:36` 遵循正确的流程
`03:38` 采用规范的命令添加方式
`03:41` 正确的模式
`03:43` 与代码库中的现有实现匹配
`03:46` 为此
`03:47` 我们将使用已添加的技能
`03:49` 在点clawed文件夹内定义添加cli命令技能
`03:55` 接着进入skills子文件夹
`03:57` 首先查看这个技能
`04:00` 我们不仅可以看到项目级可用性
`04:04` 还可以在主目录创建用户级技能
`04:07` 如果需要的话
`04:09` 在此示例中
`04:10` 我们将专注于项目特定技能
`04:13` 现在深入分析该技能的具体实现
`04:16` 就像之前看到的，这里包含名称和描述
`04:20` 在这里
`04:20` 我们将深入采用所需的编码风格和功能
`04:25` 在创建新命令时
`04:27` 我们将首先确定必要的工作流程
`04:31` 并在相应目录创建文件
`04:34` 就像我们已有add、done和list命令一样
`04:37` 我们希望确保在创建新命令文件时
`04:40` 它就驻留在那里
`04:42` 我们也需要确保这些命令在我们的双下划线初始化文件中注册
`04:45` 位于命令文件夹中的初始化点pi文件
`04:49` 当我们考虑如何创建不同命令时
`04:51` 可能会有很多不同种类
`04:53` 可能会有涉及子命令或标志的命令
`04:57` 或者额外参数
`04:58` 你可以向Claude提供纯文本指令
`05:01` 但尤其是对于代码示例
`05:03` Claude表现非常出色
`05:04` 当你明确告知所需模式和风格时
`05:09` 在Typer库中
`05:10` 有许多不同的任务实现方式
`05:13` 例如
`05:14` 有许多不同的方式为被装饰的参数添加类型注解
`05:19` 在这个特定案例中
`05:21` 这是我们想要遵循的约定
`05:23` 因为它更现代一些
`05:24` 你可以想象在使用的库和代码库中
`05:27` 有许多不同的实现方式
`05:30` 但技能的有用之处在于
`05:32` 它为我们提供了一套任务的可预测工作流程
`05:35` 我们也需要确保使用这个显示对象
`05:39` 并调用success和info等方法以确保添加命令时
`05:44` 我们不仅执行业务逻辑
`05:46` 还能在最后向用户正确显示信息
`05:49` 当我们处理标志时
`05:51` 我们需要特定的简写方式
`05:52` 也需要特定的长格式调用方式
`05:55` 还需确保有帮助文本
`05:57` 所有这些组件
`05:58` 类型注解
`05:59` 默认参数
`06:01` 某些返回值值得在技能中添加
`06:04` 以便你知道如何最佳匹配
`06:06` 匹配并遵循可预测的工作流程
`06:08` 当我们考虑带有子命令的命令时
`06:11` 你不仅能在这里看到
`06:12` 我们如何构建单个命令结构
`06:15` 还能看到当迁移或版本变更时的显示方式
`06:21` 当我们设想添加清理功能时可能涉及的破坏性命令
`06:25` 我们可能需要指定删除类型
`06:29` 如果我们选择硬删除
`06:30` 必须确保在删除特定任务前进行确认
`06:35` 这种模式可以遵循
`06:37` 当Claude开始处理可能需要删除操作的其他命令时
`06:42` 当我们考虑注册时
`06:44` 需要有意规划如何添加单个命令和命令组
`06:48` 这样我们不仅为注册命令提供了明确指导
`06:52` 我们在遵循的具体规范方面非常明确
`06:55` 最后当我们讨论规范时
`06:57` 这就是我们可以利用文档字符串要求的地方
`07:01` 注意退出代码并遵循常量
`07:03` 注意破坏性命令
`07:06` 这是一个非常有用的场景，当我们构建可预测的工作流程时
`07:10` 我们清楚自己遵循的具体规范
`07:13` 这些规范不需要存在于代码库的每个地方
`07:16` 也不需要在上下文中全部加载
`07:18` 如果这样我们可以将其放在clawed md中
`07:21` 但在这种情况下
`07:21` 仅针对添加单个命令
`07:23` 需要遵循规范的子集
`07:26` 只需在必要时加载这些
`07:28` 我们不仅使用通用命名如cli app
`07:32` 我们可以将此技能应用到遵循代理规范的任何平台
`07:37` 根据你正在构建的任何CLI
`07:39` 如果你想让claw遵循这些特定模式
`07:41` 这个技能可以轻松适配实现
`07:44` 现在我们有了添加CLI命令的技能
`07:47` 让我们确保在执行添加命令的特定工作流程时
`07:52` 我们注意测试并验证所写的命令
`07:57` 这也是另一个生成CLI测试技能的绝佳用例
`08:03` 我们为hyper命令生成pi测试
`08:06` 这里包含所需的测试夹具和边缘情况处理
`08:12` 非常重要的是要明确说明操作步骤
`08:15` 以及如何触发该技能，如此处所示
`08:18` 当用户要求为CLI编写测试或增加测试覆盖率时
`08:23` 必须非常明确
`08:25` 不仅在描述内容上
`08:28` 还要让clade能检测如何运行
`08:30` 与其他技能类似
`08:31` 我们指定编写测试时的流程
`08:35` 通常最佳实践是利用测试夹具
`08:38` 可以将夹具视为每次运行时执行的信息
`08:42` 在安排测试时设置相关信息
`08:45` 设置模拟数据
`08:47` 以及所需的任何模拟或测试基础设施
`08:51` 针对每个编写的测试
`08:53` 例如
`08:53` 这里我们指定临时存储可能的结构
`08:57` 并指定一些示例数据
`09:00` 在运行每个测试时
`09:02` 这些信息将被暴露以安排和设置必要测试
`09:08` 当我们测试单个文件时
`09:10` 文件夹和工作流程
`09:12` 在查看测试结构时
`09:14` 我们遵循与夹具相关的模式
`09:17` 调用某种操作
`09:19` 然后断言结果是否符合预期
`09:21` 在这里，我们只是试图构建模式，供clog使用并遵循
`09:26` 当技能被充分利用时
`09:27` 我们可以继续按照我们希望测试运行器实现的方式进行
`09:31` 以及如何通过命令类型测试场景
`09:33` 随着我们阅读
`09:34` 随着我们添加
`09:35` 这是我们希望测试案例的示例
`09:38` 如同许多测试库
`09:40` 有许多方法可以完成这些示例的类似任务
`09:44` 这是我们想要遵循的模式
`09:47` 在技能接近尾声时
`09:49` 我们需要考虑一些边缘情况以覆盖测试编写
`09:53` 我们需要考虑无效输入
`09:54` 任何状态确认
`09:57` 或未找到或不存在的情况
`10:00` 我们需要确保遵循检查清单
`10:03` 当我们开始编写测试时
`10:05` 最后确保正确运行测试
`10:08` 提供正确命令以及如何以详细模式运行
`10:12` 以及针对特定文件
`10:13` 这里最后一个技能是收尾和复审
`10:17` 并确保正确执行命令
`10:20` 在生成测试并运行后
`10:23` 确保遵循正确规范
`10:26` 就像我们看到的其他技能
`10:28` 有执行必要任务的方法
`10:30` 然后返回验证一切正常
`10:34` 在回顾这些命令时
`10:36` 我们不仅考虑底层结构
`10:39` 这是否在正确位置
`10:40` 是否使用了正确装饰器
`10:42` 是否正确注册
`10:44` 但我们也要确保那些我们希望的实践
`10:48` 类型注解或参数选项或标志
`10:51` 在可能时
`10:52` 始终包含
`10:53` 提供正反例通常有帮助
`10:57` 正如我们提到的
`10:58` 使用这种注解类型
`10:59` 与另一种参数类型方式对比
`11:04` 当我们更关注错误处理和输出时
`11:07` 确保此检查清单存在
`11:09` 以便技能能确认所有组件
`11:13` 符合预期
`11:14` 我们没有告诉claude如何执行这些操作
`11:18` 比如添加命令和生成测试
`11:20` 我们确保其按预期工作
`11:23` 可以将其视为一种评估
`11:25` 几乎适用于其他技能
`11:27` 并将此技能纳入工作流程
`11:31` 当我们查看底部内容时
`11:32` 确保我们的最佳实践得到遵循
`11:35` 以及我们可能遇到的错误示例及其修复方法
`11:39` 现在我们来看技能的输出格式
`11:42` 我们需要确保所有检查项都得到处理
`11:45` 包括摘要和建议的修复方案
`11:48` 进行这种底层审查非常有用
`11:50` 这样当功能完成后
`11:51` 我们可以先查看这份审查
`11:54` 包含此内容
`11:54` 甚至作为代码审查的一部分
`11:56` 确保我们构建生产级功能
`11:58` 并经过测试验证
`11:59` 遵循最佳实践
`12:01` 考虑到这一点
`12:02` 让我们开始整合所有这些技能
`12:04` 首先我们要添加一个新命令
`12:07` 允许编辑单个任务
`12:10` 我们需要确保修改标题和优先级
`12:13` 并传入当前有效的ID
`12:16` 让我们进入clade代码并使用这些技能
`12:20` 确认我已正确注册这些
`12:22` 我先输入skills命令
`12:25` 这将列出我需要满足的可用技能
`12:28` 这些是项目技能
`12:30` 我们之前也提到
`12:31` 我们可以在主目录中添加技能
`12:34` 但目前我们只处理项目技能
`12:36` 我们还可以看到这些技能占用的tokens数量
`12:39` 当我们考虑必要的名称和描述时
`12:42` 在claude代码中创建新技能时
`12:45` 务必关闭并重新打开claude代码
`12:49` 以便识别该技能
`12:51` 如果你查看技能列表时
`12:53` 但发现缺少刚创建的技能
`12:56` 请关闭该clade代码实例
`12:58` 重新打开它
`12:59` 现在我们的技能正确加载，状态良好
`13:03` 我们继续添加新编辑命令以允许用户修改标题和优先级
`13:09` 这里添加一个示例
`13:11` 并确保遵循创建新CLI命令的规范
`13:15` 现在让我们试试看
`13:17` 我们可以看到clog代码提示使用添加CLI命令技能
`13:22` 这很棒
`13:23` 我们将来要确保
`13:25` 不再提示此操作
`13:27` 在此处可以看到
`13:28` 我们将读取现有文件和存储
`13:30` 以理解规范
`13:32` 同时参考其他命令的示例
`13:37` 现在我们已经了解了这些约定
`13:38` 接下来我们创建该特定文件
`13:41` 我们可以看到这里
`13:42` 正在创建新的文件edit_pi
`13:44` 我们现在要进行这项修改
`13:46` 可以看到edit_pi已出现
`13:49` 我们现在要注册该命令
`13:51` 按照技能设定的顺序执行
`13:54` 可以看到命令正在我们的__init__中按预期注册
`13:59` 我们也进行这项修改
`14:02` 我们将继续运行该命令进行测试
`14:06` 并确保其正常运行
`14:07` 看起来符合预期
`14:10` 状态良好
`14:11` 现在将运行add命令
`14:13` 然后运行list命令确认添加成功
`14:17` 正在播种数据以验证edit命令效果
`14:22` 我们将编辑该任务
`14:24` 这里未指定标题或优先级
`14:28` 然后输入该标题
`14:30` 可以看到已成功编辑
`14:35` 系统再次提示编辑该任务
`14:37` 在测试各种示例时会重复出现
`14:42` 虽然可以继续重复操作
`14:45` 这可能会逐渐填满上下文窗口
`14:48` 在更大规模系统中可能非常耗时且计算密集
`14:53` 接下来我们将采用不同方法
`14:56` 利用Claude Code的子代理功能
`15:01` 一个子代理负责代码审查和遵循标准
`15:07` 使主代理专注于开发
`15:09` 另一个子代理生成并运行测试
`15:13` 利用我们已有的技能功能
`15:17` 这样主代理可专注于开发
`15:20` 子代理在各自上下文中
`15:23` 专注于生成测试和审查代码
`15:26` 然后获取生成的反馈和测试
`15:29` 以更高效的方式返回主代理
`15:33` 需注意子代理不继承父代理技能
`15:38` 需明确为每个子代理分配技能
`15:43` 有多种方式传递技能给子代理
`15:46` 一种方法
`15:47` 我们将演示
`15:48` 在子区域明确指定技能名称
`15:52` 另一种选项是通过备注链接代理名称
`15:57` 并直接从技能中运行的最佳方式
`16:01` 接下来创建子代理
`16:05` 首先创建代码审查代理
`16:08` 使用agents命令创建这些代理
`16:12` 创建新代理
`16:13` 基于项目进行创建
`16:15` 但与其使用分支生成
`16:17` 我们将采用手动配置
`16:19` 这样你可以看到如何添加名称描述工具和
`16:24` 最重要的是每个代理的技能
`16:26` 我将首先创建一个名为代码审查员的代理
`16:29` 这将是我们的代理唯一标识符
`16:32` 然后我们将向其传递提示
`16:34` 我们将粘贴一个稍后查看的提示
`16:37` 我们已经创建了这个
`16:37` 但它看起来非常熟悉我们的审查方式和具体可操作性
`16:43` 在我们的见解中
`16:44` 在进行代码审查时
`16:45` 我们现在将为该代理添加描述
`16:49` 它何时应被使用
`16:51` 我们将进行代码质量审查
`16:52` 安全等
`16:54` 我们将尽量使此代理通用化
`16:57` 针对我们所需的工具集
`17:00` 我们需要非常具体地定义子代理可访问的工具
`17:04` 因此如果需要执行代码
`17:07` 我们将提供bash
`17:08` 使用grep查找文件
`17:10` 并读取待审查的文件
`17:14` 选择这些工具后
`17:16` 我们可以继续
`17:18` 我们将继承父代理使用的模型
`17:22` 并为其设置紫色颜色
`17:25` 完成设置后
`17:27` 保存此代理
`17:29` 现在在我们的代理文件夹中可以看到代码审查员
`17:33` 代理已就绪
`17:35` 我们拥有代码审查员
`17:36` 描述工具及所有添加的组件
`17:40` 现在需要确保
`17:42` 指定此子代理所需技能
`17:45` 通过技能字段实现
`17:47` 指定所用技能名称
`17:52` 在我们创建下一个代理前执行CLI命令审查
`17:57` 快速回顾我们创建的内容
`17:59` 代理具有名称、描述工具和可用性
`18:02` 继承的模型、颜色及引入的技能
`18:07` 我们可以添加多项技能
`18:08` 但现阶段仅使用一项
`18:10` 在提示中
`18:11` 我们提到这是代码审查员
`18:12` 确保高标准
`18:14` 明确定义代理功能时
`18:17` 指定其被调用时执行常规质量检查
`18:20` 当我们处理Python时
`18:22` 需明确CLI命令同理
`18:25` 我们需要一个特定的输出格式，如之前提到的
`18:28` 这个代理稍微通用一些
`18:29` 虽然我们是在特定应用的上下文中使用它
`18:33` 技能可以帮助我们更具体化
`18:35` 但这个代理可能需要在不同应用中使用
`18:39` 因此我们在这里要稍微通用一些
`18:43` 需要注意技能在Claude代码中作为子代理的工作方式略有不同
`18:49` 当这个子代理被调用并创建时
`18:51` 技能不仅加载名称和描述
`18:55` 但整个技能模板
`18:57` 这些技能在代理被调用时会预先加载
`19:03` 其他文件或命令的读取并未执行
`19:07` 但子代理被调用时整个技能md会被读取
`19:12` 现在我们已经有了代码
`19:13` 审查代理
`19:14` 让我们添加另一个用于生成和运行测试的代理
`19:19` 我们继续创建第二个代理
`19:21` 我们将使用手动配置在项目中新建代理
`19:27` 并将其命名为测试生成器执行器
`19:29` 我们继续添加一个提示
`19:31` 稍后会详细审查
`19:33` 它将遵循与之前代理相似的模式
`19:37` 我们为这个代理指定描述
`19:41` 说明它应运行并生成测试
`19:44` 当用户要求测试或运行时若缺失
`19:48` 确保这个代理像之前一样被调用
`19:53` 而不是提供所有工具访问权限
`19:55` 转到高级选项
`19:57` 在高级选项中
`19:58` 我们将禁用所有工具
`20:01` 并确保包含bash工具
`20:03` glob和grep读取功能
`20:05` 但我们还需要编辑和写入单个文件
`20:09` 并编辑可能已存在的文件
`20:13` 配置好这些工具后
`20:14` 我们继续讨论将要使用的模型
`20:18` 和之前一样继承父类
`20:21` 我们使用yellow配置此子代理
`20:24` 看起来没问题
`20:25` 我们保存它
`20:27` 创建此代理后
`20:29` 还需指定使用的技能
`20:33` 在此使用生成CLI测试的技能
`20:38` 技能部分可添加多个
`20:41` 但此处仅使用单一技能
`20:44` 如之前所示
`20:46` 当被调用时
`20:48` 展示如何发现测试和所需输出格式
`20:51` 并设置底层规则确保正常运行
`20:55` 就像我们的其他代理一样
`20:57` 为了更通用并依赖技能来提供一致的工作流程
`21:02` 现在我们已经创建了子代理和技能
`21:04` 接下来我们将整合所有内容，确保添加新命令时
`21:08` 必要时使用所需技能调度子代理
`21:13` 我们先使用代码审查员
`21:15` 子代理来审查我们创建的edit pi命令
`21:19` 这不仅会调度子代理
`21:21` 还会使用为该子代理提供的技能
`21:25` 我们可以看到代码审查员代理已被调度
`21:29` 现在我们将继续
`21:30` 使用我们定义的必要技能和工具
`21:35` 这里可以看到
`21:36` 哪些功能正常哪些存在问题
`21:38` 没有严重错误
`21:39` 但存在一些警告
`21:41` 需要修复的问题和建议方案
`21:43` 我们可以使用主代理实施这些修复
`21:46` 如果需要的话
`21:47` 接下来使用测试运行器
`21:49` 子代理为edit pi命令生成测试
`21:53` 我们将确保引用edit.pi文件
`21:59` 然后调度第二个子代理
`22:02` 可以看到这里
`22:03` 已生成必要命令
`22:05` 并提示确认是否执行此编辑
`22:09` 我们将继续操作
`22:10` 在添加编辑测试时
`22:13` 可以使用此命令确认测试正常运行
`22:16` 运行uv run
`22:17` 然后以详细模式运行所有测试
`22:20` 确保流程通过
`22:22` 并避免回归问题
`22:24` 完成之后
`22:25` 所有测试均通过
`22:27` 无失败情况
`22:28` 并附有总结
`22:30` 我们使用了两个不同子代理和多项技能
`22:34` 随着新功能的增加
`22:37` 可以整合所有组件
`22:39` 接下来我们将观察代码审查员
`22:41` 子代理和测试运行器
`22:44` 子代理的实际运行
`22:45` 假设存在一个clear.pi文件
`22:47` 团队成员新增了一个未遵循最佳实践的命令
`22:50` 可能未充分利用我们搭建的技能和基础设施
`22:52` 我们将使用代码审查员子代理
`22:56` 以及测试运行器子代理
`22:59` 来确定如何最佳修复clear.pi文件
`23:01` 调度代码审查员审查clear.pi命令
`23:06` 我们将调度代码审查员审查clear.pi命令
`23:10` 然后我们将使用测试生成器运行器来生成此命令所需的测试
`23:15` 最后我们将验证一切是否按预期正常运行
`23:18` 并确保所有测试通过且符合我们标准化的最佳实践
`23:23` 我们可以在这里看到
`23:24` 存在相当多的问题和一些警告
`23:26` 既然我们已经发现了这些问题
`23:27` 让我们确保主代理正在读取文件并修复它们
`23:31` 我们需要确保允许这些编辑清除pi
`23:34` 在这里我们可以看到控制台输出已正确实现
`23:38` 使用正确的方法如最佳实践中所述
`23:43` 我们也需要确保正确注册此命令
`23:46` 在我们的双下划线初始化方法中
`23:48` 主代理本身未为审查和生成测试添加额外上下文
`23:54` 它只是采用子代理的输出以更好地执行后续任务
`23:59` 是时候生成遵循最佳实践的clear命令测试了
`24:03` 我们可以在这里看到
`24:04` 正在为测试此clear命令添加文件
`24:07` 我们继续批准该操作
`24:09` 既然我们已经创建了这个文件
`24:11` 我们现在运行测试
`24:12` 确保它们按预期工作
`24:15` 现在我们可以获取已完成内容的总结
`24:18` 六个关键问题
`24:19` 四个警告
`24:20` 并全部修复了错误方法的使用
`24:24` 而是使用了格式不正确的标志
`24:27` 我们想要错误的退出代码
`24:29` 我们已修复了许多不同问题
`24:31` 在此基础上又添加了测试
`24:33` 以确保所有最佳实践都按预期完成
`24:38` 并确认功能符合下一课所需
`24:42` 我们将停止使用claude代码
`24:43` 转向claude代理SDK
`24:46` 并展示如何在构建自定义代理时使用技能
`24:49` 使用与claude代码相同的执行框架
---
title: "【吴恩达】2026年公认最好的【Agent Skills】教程！大模型入门到进阶，一套全解决！Agent Skills with Anthropic—附课件代码"
url: "https://www.bilibili.com/video/BV1qv6eBZErD?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=7"
bvid: "BV1qv6eBZErD"
cid: "35698773715"
author: "吴恩达深度学习"
upload_date: "2026-01-30"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115981733136330&bvid=BV1qv6eBZErD&cid=35698773715&page=7&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1、使用开放标准格式和最佳实践创建可重复使用的技能，并组合以创建复杂的工作流程。
2、建立定制代码生成技能，审核你的代理按需加载的工作流程、数据分析和研究。
3、结合MCP和子代理的技能，创建强大的代理系统，具备专业知识和外部数据源的访问权限

## 字幕

`00:00` 在第一课中你了解了技能如何与Claude AI配合使用
`00:03` 现在我们将通过Claude API测试这两个技能
`00:07` 我们需要从上一课学习如何使用Claude API调用技能
`00:11` 我们需要使用代码执行工具和文件API
`00:15` 这将赋予Claude文件系统访问权限以读写文件
`00:20` 并配合Bash执行代码
`00:22` 让我们开始操作
`00:24` 我们已经详细讨论了技能的工作原理及创建方法
`00:27` 同时也简要提到了技能在不同环境中的可移植性
`00:33` 在Claude生态系统中
`00:34` 以及许多其他智能应用
`00:38` 我们首先研究了Claude AI和桌面版中的技能
`00:41` 现在我们将转向讲解如何使用技能
`00:44` 通过Claude消息API
`00:46` 需要注意两点重要内容
`00:48` 首先
`00:49` 在Claude AI和桌面版中创建的技能不会共享
`00:54` 在Claude API或Clade代码中
`00:56` 第二个关键点是技能要正常运行
`01:01` 需要Claude具备代码执行能力
`01:04` 创建和编辑文档
`01:06` 演示文稿
`01:06` PDF和数据报告，并操作文件系统
`01:10` 这是我们使用Claude API时必须手动完成的
`01:13` 当我们使用Claude API时
`01:14` 实际上这些配置会自动为你完成
`01:18` 在Claude桌面或Claude AI中
`01:20` 在Claude桌面或Claude AI中
`01:23` 如果我进入设置
`01:25` 并查看能力选项
`01:28` 可以看到代码执行和文件创建的相关选项
`01:32` 这是我们直接使用API时将深入探讨的内容
`01:36` 此设置默认启用
`01:39` 允许Claude执行代码
`01:41` 创建文档
`01:42` 电子表格
`01:42` 演示文稿等
`01:44` 这实际上为Claude AI和Claw桌面
`01:47` 提供了一个计算机或虚拟机来执行代码并完成各项技能任务
`01:54` 如果此功能关闭
`01:56` 我们将需要开启它才能使用技能
`02:01` 现在回到代码执行工具和文件创建机制
`02:07` 因为我们使用API时需要手动启用
`02:11` 在使用Clade代码和Clawed代理时
`02:15` 可以直接访问文件系统
`02:18` 而使用Claude API时
`02:20` 我们无法直接访问
`02:20` 需要容器执行代码
`02:23` 并配备文件系统与Claude AI和Claw桌面协作
`02:27` 该容器化环境和文件系统由系统提供
`02:30` 这不是你必须在一天结束时才需要实现的功能
`02:33` 功能本身完全相同
`02:35` 但我们运用技能的方式略有不同
`02:39` 技能本身没有变化
`02:42` 技能的格式也没有变化
`02:44` 但根据你所处的环境
`02:46` 可能会以略微不同的方式运用技能机制
`02:49` 当我们开始探索消息API时
`02:52` 我们将使用代码执行工具
`02:54` 代码执行工具允许Claude运行bash或shell命令
`02:59` 执行我们在技能创建过程中看到的所有操作
`03:04` 查看
`03:05` 编辑文件和编写代码
`03:07` 全部在一个沙盒环境中
`03:09` 代码执行工具赋予我们的应用独立容器的能力
`03:15` 用于执行代码和操作文件系统
`03:19` 正如你所见所有技能的功能
`03:22` 这对理解我们的技能至关重要
`03:24` 在技能中执行代码
`03:26` 并处理可能需要编辑、查看和创建的其他文件
`03:31` 为了让你直观了解这看起来是什么样子
`03:34` 当我们包含代码执行工具时
`03:37` 我们为Claude提供
`03:38` 一个执行沙盒或容器
`03:41` 当我们要求Claude创建和执行文件时
`03:44` 这些操作会在安全隔离的环境中进行
`03:47` 存在内存限制
`03:50` 磁盘空间
`03:50` CPU资源
`03:51` 更重要的是
`03:52` 不提供互联网连接
`03:55` 并且预装了基础库
`03:58` 因此并非所有编程环境都适用
`04:02` 这里存在一些需要注意的限制
`04:04` 同时
`04:05` 我们还能访问可添加目录的文件系统
`04:10` 你可能在与Claude协作时见过相关提示
`04:13` 桌面版和Claude
`04:14` AI这种无网络连接的限制是消息API特有的
`04:20` 当我们使用Claude AI或桌面版的代码执行工具时
`04:24` 我们确实有互联网连接
`04:26` 可以下载和安装包
`04:29` 代码执行工具与其他API配合良好
`04:33` Claude API允许我们使用这些接口
`04:36` 可想而知
`04:37` 在处理文件时
`04:39` 添加
`04:39` 创建
`04:40` 编写
`04:41` 修改文件
`04:42` 我们需要一种实际存储这些底层文件的机制
`04:46` Claude API 包含一组名为文件API的接口
`04:51` 用于上传和下载可在容器内运行和处理的文件
`04:57` 可以想象用户要求总结某些输入内容的场景
`05:01` 并将摘要保存为文本文件
`05:03` 我们上传该输入文件
`05:06` 将其发送到容器
`05:07` 使用此文件API下载生成的文件
`05:10` 我们很快会在代码中看到这一点
`05:12` 当我们看到上传和下载文件返回的ID时
`05:16` 以及这些功能如何与技能和代码执行工具完美配合
`05:21` 这就是技能发挥作用的地方
`05:24` 我们从Claude AI等工具中获得的技能库
`05:29` 或通过API自行添加的技能
`05:32` 这些技能存在于容器驱动的目录中
`05:35` 当我们开始读取这个技能目录时
`05:38` 当我们开始向技能库添加信息时
`05:41` 或使用底层技能创建可下载/上传的新文件
`05:47` 这就是技能发挥作用的地方
`05:49` 在使用API时我们将看到一个要求
`05:52` 当我们想要使用技能时
`05:53` 也需要使用代码执行工具
`05:57` 现在我们对代码执行工具和文件API的功能有了清晰认识
`06:02` 让我们看看实际应用
`06:04` 我们将重新访问之前为生成练习题和时间序列分析创建的两个自定义技能
`06:08` 所以让我们前往Jupyter笔记本进行探索
`06:11` 我这里有之前使用的两个自定义技能
`06:15` 我还准备了一个数据文件夹
`06:18` 用于分析时间序列数据
`06:21` 我还准备了讲义笔记文件夹
`06:24` 当我在笔记本中使用生成练习题技能时
`06:27` 我将加载所需的环境变量
`06:32` 以及帮助从目录中查找特定文件的辅助工具
`06:34` 我们将在实际操作中看到这一点
`06:38` 当我们开始使用技能时
`06:40` 我将开始使用生成练习题技能
`06:42` 所以我
`06:45` 让我们先看看需要执行的第一部分
`06:46` 我需要上传技能目录，如您所见
`06:50` 我们正在使用files_from_derhelper函数
`06:53` 以及必要的技能Beta头信息
`06:56` 完成这一步后
`06:59` 我应该能看到创建的Beta列表技能ID
`07:00` 这些是我在向消息API发送请求时添加的特定头信息
`07:06` 这些会转换为请求头
`07:10` 以确保获取正确的数据
`07:12` 并正确与API通信查看所有技能
`07:14` 通过这些请求头确保数据正确返回
`07:16` 并与API恰当交互查看所有技能
`07:21` 我可以使用列表方法查看所有技能
`07:23` 我将传入一个自定义源
`07:25` 这样我们就不加载所有内置技能
`07:28` 而是只需确认已创建预期的技能
`07:32` 在这里可以看到标题
`07:34` 以及即将使用的唯一技能ID
`07:38` 为了确保正常运行
`07:40` 我们需要使用法律技术文件
`07:43` 我们将从此处生成练习题
`07:46` 我将使用文件API上传此法律技术文件
`07:51` 确保设置为可读并获取文件对象
`07:56` 我将使用此文件对象
`07:58` 结合必要技能确保一切正常
`08:04` 我在这里使用Sonnet
`08:06` 并传入必要的Beta头
`08:08` 不仅针对技能
`08:10` 还要确保技能正常运行
`08:12` 在与模型交互时
`08:14` 还需确保启用代码执行Beta
`08:17` 由于我正在发送文件
`08:19` 必须确保有文件权限
`08:21` 还需API头
`08:23` 在使用技能时
`08:24` 这些技能通过名为container的参数设置
`08:27` 这里传入技能列表
`08:30` 这些可以是自定义或内置技能
`08:33` 随着创建不同技能版本
`08:36` 我可以引用特定时间戳
`08:38` 或直接使用最新版本
`08:41` 开始与模型通信时
`08:43` 我要求生成练习题
`08:45` 并指定正在使用的文件对象（此前已创建）
`08:51` 在上传法律技术文件时
`08:53` 最后确保引入代码执行工具
`08:57` 并向API发送消息
`09:00` 现在查看返回的响应
`09:02` 可以看到多个组件被使用
`09:06` 服务器代码执行工具
`09:09` 其他工具也被使用
`09:11` 最终是Bash代码执行结果
`09:14` 为了更清晰查看
`09:16` 添加美观格式
`09:17` 以便分析不同文本响应和工具使用
`09:23` 我们将逐步查看这一系列
`09:26` 逐步分析响应内容
`09:30` 包括文本、工具使用和结果
`09:33` 模型首先告知可从笔记生成问题
`09:38` 并将开始读取技能文件和讲座笔记
`09:42` 此处检测到所需技能
`09:46` 但仅读取技能MD文件后续可见
`09:50` 若需读取其他文件
`09:52` 我们将利用渐进式披露功能
`09:55` 我们还将审查输入中的lotech文件
`10:00` 我们将查看这些文件产生的底层数据
`10:03` 这是之前见过的yaml前置内容
`10:06` 以及ltech的笔记和技术文件接下来
`10:10` 我们将检查Markdown模板以使用正确结构
`10:14` 因为我们希望输出为Markdown格式
`10:17` 这里我们将更充分地运用渐进式披露
`10:21` 这里是读取资产文件夹的内容
`10:24` 这个Markdown模板
`10:26` Md
`10:26` 我们将获取已读取的响应
`10:28` 现在我们将根据已传递的讲义生成问题
`10:33` 我们将使用代码执行工具创建特定文件
`10:38` 我们将写入Markdown格式的文件内容
`10:40` 并将获取该文件的结果
`10:43` 我们将复制到输出目录
`10:46` 并使用文件API获取可后续下载的文件ID
`10:52` 获取该结果后
`10:54` 我们可以查看生成的底层文件
`10:58` 并利用文件ID程序化下载
`11:02` 可以看到已保存并准备好使用
`11:06` 使用上述看到的文件ID
`11:08` 我们开始下载文件
`11:11` 我们将检查此响应
`11:13` 确保正确提取文件ID
`11:16` 如果我们已正确提取
`11:18` 应能下载该文件
`11:21` 我们将写入名为notes_for_md的文件
`11:26` 内容包含文件ID
`11:28` 以及必要的API beta头信息
`11:33` 可以看到已下载notes_for_md文件
`11:36` 来自文件API与代码执行工具
`11:41` 全部由模型和此文件中的技能生成
`11:46` 可以看到我们遵循了技能中的确切部分
`11:50` 从真题和假题开始
`11:52` 继续进行
`11:53` 到解释性问题
`11:54` 到编码问题
`11:56` 最后到用例应用
`11:59` 我们可以在Markdown中预览
`12:00` 查看效果
`12:02` 可以看到我们的用例应用
`12:04` 所有必要内容
`12:06` 现在是评估输出的好时机
`12:09` 我们是否完全符合技能要求
`12:11` 初步看起来不错
`12:12` 引入单元测试
`12:14` 能大幅提升质量
`12:16` 如果需要
`12:17` 我们可以像之前使用API一样返回并修改技能
`12:21` 代码执行工具和文件API也是如此
`12:25` 我们还可以程序化删除技能
`12:28` 为了删除一个技能
`12:29` 首先需要找到与该技能关联的所有版本
`12:33` 然后删除这些版本
`12:34` 一旦这些版本被删除
`12:37` 接下来应该可以在这里删除底层技能
`12:41` 我们将继续使用分析时间序列技能和另一个技能
`12:47` 这看起来和之前看到的非常相似
`12:49` 所以先让我们完成这些步骤
`12:52` 我们将上传自定义技能
`12:53` 获取技能ID
`12:55` 并在此确认操作成功
`12:59` 我们还可以看到不仅加载了自定义技能
`13:02` 也能看到内置技能
`13:04` 这些应该和之前看到的非常相似
`13:07` 在Claudiai中接下来我们将上传输入文件
`13:11` 这是我们的零售销售CSV文件
`13:15` 我们将构建发送到API的消息
`13:18` 和之前一样使用我们的技能
`13:21` 但这里还会包含Dock技能
`13:26` 我们将使用它来创建Word文档
`13:29` 总结结果和图表
`13:31` 这里可以看到自定义技能的组合
`13:34` 包含技能ID和版本
`13:37` 以及使用Anthropic内置技能
`13:40` 在此案例中是Doc X技能
`13:42` 我们传递与之前技能代码执行相同的请求头
`13:47` 和文件API
`13:50` 现在运行完成
`13:52` 我们可以检查得到的响应类型
`13:55` 会看到类似之前的结果
`13:58` 但这次有更多内容
`14:01` 让我们查看底层操作
`14:03` 使用良好格式化
`14:04` 模型将帮助我们分析时间序列数据
`14:09` 和之前一样开始阅读这些技能MD文件
`14:14` 阅读自定义技能和内置Doc X技能
`14:19` 我们需要使用这些
`14:20` 可以看到结果包含从文件开头的内容
`14:25` 并包含整个技能MD文件
`14:28` 我们将检查数据以运行时间序列分析
`14:32` 查看CSV文件的前二十行
`14:35` 检查列名和数据类型
`14:40` 由于操作正常
`14:42` 我们将运行诊断并创建可视化图表
`14:47` 这些需要执行的命令直接来自我们的技能
`14:52` 接下来我们将
`14:54` 读取这些底层文件
`14:56` 执行该代码并将结果返回给claude进行处理
`15:01` 我们将获取这些执行的结果
`15:03` 同时将结果返回用于诊断和可视化
`15:07` 然后我们将阅读摘要和诊断信息
`15:11` 这是脚本运行后生成的文件summary.txt中的结果
`15:17` 一旦该文件创建完成
`15:19` 我们可以继续创建Word文档
`15:23` 内置的docx技能包含处理Word文档的正确内容
`15:29` 我们将查看如何最佳生成该文档
`15:33` 并在此处应用渐进披露策略
`15:35` 我们不需要docx技能的所有功能
`15:38` 只需获取Markdown文件的方法
`15:42` 获取该文件后
`15:43` 我们将创建完整的Word文档
`15:45` 使用必要的技能
`15:47` 执行代码生成文档并创建基础Word文件
`15:52` 拥有该Word文档后
`15:54` 将其复制到输出目录
`15:56` 就像之前看到的那样获取可使用的文件ID
`16:00` 如果我们想下载此Word文档
`16:02` 我们可以查看数据概览
`16:05` 现在可以下载该文件
`16:07` 同样地
`16:08` 我们将查找该文件ID
`16:10` 如果存在的话
`16:11` 我们将下载该特定文件
`16:14` 以docx格式包含必要内容
`16:18` 查看此文件内容
`16:20` 现在我们有了包含分析结果的Word文档
`16:23` 我们的概述
`16:24` 我们的统计数据
`16:25` 可以看到我们已导入这些图表和可视化内容
`16:28` 以及我们请求的统计分析结果
`16:31` 这也是评估技能是否符合预期的好时机
`16:34` 检查我们正在执行的操作是否符合技能设计
`16:37` 并在持续评估中保持可预测性
`16:42` 我们可以随时修改该技能
`16:45` 但所有数据均来自该技能和docx技能
`16:50` 如同之前所示创建独立文件
`16:54` 如果我们想删除该技能
`16:55` 可以列出所有版本并删除这些版本
`16:58` 删除这些版本后
`17:00` 删除本课中的基础技能
`17:03` 我们结合了消息API、代码执行工具、文件API和技能的知识
`17:07` 将自定义技能与程序进行交互
`17:08` 通过文件API和技能来操作自定义技能
`17:14` 在下一课中
`17:15` 我们将转向Claude代码
`17:17` 并学习如何在doclawed文件夹中添加自定义技能
`17:21` 构建更复杂的命令行应用程序
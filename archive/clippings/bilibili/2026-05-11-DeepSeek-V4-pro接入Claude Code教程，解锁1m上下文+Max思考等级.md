---
title: "DeepSeek-V4-pro接入Claude Code教程，解锁1m上下文+Max思考等级"
url: "https://www.bilibili.com/video/BV1pQRNBsEGs/?spm_id_from=333.1007.tianma.6-2-20.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1pQRNBsEGs"
cid: "38000002729"
author: "小陈同学c_z"
upload_date: "2026-05-01"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116497699577459&bvid=BV1pQRNBsEGs&cid=38000002729&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

这期视频分享如何把 DeepSeek 最新的 V4 模型接入 Claude Code，并完成从安装、配置到实际使用的完整流程。内容包括 Claude Code 的安装、CC Switch 配置、DeepSeek API Key 创建、模型切换、1M 上下文与思考等级设置，以及最后用真实任务测试这套组合的效果和成本。

deepseek api开放平台：https://platform.deepseek.com/api_keys
deepseek 官方文档：https://api-docs.deepseek.com/zh-cn/quick_start/agent_integrations/claude_code

## 章节

- `00:00` 介绍
- `00:41` 安装 claude
- `02:12` 安装 cc-switch
- `03:00` 购买 api 并配置
- `06:05` 演示操作
- `09:01` 查看花费

## 字幕

### 介绍 `00:00`

`00:00` 本期视频来分享
`00:01` 如何将DeepSeek的V4模型接入claude code
`00:04` 并解锁1m的上下文以及Max思考等级
`00:08` 我目前已经将DeepSeek的最新模型
`00:10` V4 Flash和V4 Pro
`00:12` 通过API的方式来接入了claude code
`00:15` V4 Pro模型在降价之后
`00:17` 性价比也越来越高了
`00:18` 并且对Agent也有做专门的适配
`00:21` 1m的上下文对于大多数人来说
`00:23` 也更加友好
`00:24` 视频内容主要分为四个部分
`00:26` 第一需要先安装一下claude code
`00:29` 第二安装开源工具CC switch
`00:31` 需要购买一下DeepSeek的API并完成配置
`00:34` 再来测试一下DeepSeek V4 Pro加claude code
`00:37` 这套组合表现怎么样
`00:39` 废话不多说
`00:40` 我们现在开始

### 安装 claude `00:41`

`00:42` 首先来说如何安装claude code
`00:44` 大家常说claude code经常被封号
`00:47` 那其实
`00:47` 封的是拥有模型能力的个人账号
`00:50` 但claude code作为一个单独的软件
`00:52` 是可以正常下载和安装的
`00:55` 没有订阅官方的模型
`00:56` 我们依旧可以使用它的框架
`00:58` 当成是openclaw或者Hermes这种
`01:01` 在他们的项目官方网站这里
`01:03` 也有明确的说明
`01:05` 终端
`01:05` cli和vs code也支持第三方提供商
`01:09` 本期视频演示的是安装CLI版本
`01:11` 也就是最通用的版本
`01:13` 这里有一行终端命令
`01:14` 它支持Mac OS
`01:16` Linux和Windows这几种不同的系统版本
`01:19` Windows这里分为Powershell命令和CMD命令
`01:23` 并且Windows用户需要先下载安装下git
`01:26` 如果没有安装的话可以到
`01:27` git的官方网站下载安装包进行安装
`01:30` 这里复制这一行命令
`01:32` 然后打开终端APP或者power shell
`01:35` 输入他们提供的命令
`01:36` 回车执行就可以了
`01:38` 我这里已经安装过
`01:39` 所以不再演示具体的步骤
`01:41` 安装好之后可能会出现一个提示
`01:44` 大致意思是安装已经完成
`01:46` 但是claude code的安装位置
`01:48` 并没有加入到电脑的环境变量中
`01:51` 这里直接复制这行他提供的命令
`01:53` 在终端执行一下
`01:55` 搞定之后
`01:56` 输入claude code  --version
`01:57` 来确定当前的版本号
`01:59` 后续使用的话
`02:00` 直接在终端输入claude命令
`02:02` 就可以打开了
`02:04` 但是你那里可能会提示
`02:06` 不能连接官方服务
`02:07` 接下来我们来安装第二个工具CC switch

### 安装 cc-switch `02:12`

`02:12` CC switch是一个开源工具
`02:14` 它能够让claude code
`02:16` codex openclaw这类的AI agent
`02:18` 方便地切换模型
`02:20` 累计下载量有300多万
`02:22` 在GitHub上面也有5万多个star
`02:24` 它有很多实用的功能
`02:26` 比如一份配置同步到多个应用
`02:29` 支持热切换
`02:30` 不需要退出应用切换模型
`02:32` 还有用量仪表盘
`02:33` 能够查看你的请求数和token用量等等
`02:36` 我们在项目的首页
`02:37` 这里有一个已发布的安装包链接
`02:40` 点击进去
`02:41` 然后在这个界面直接滑到最下面
`02:44` 这里有很多的安装包版本
`02:46` 如果是Mac OS系统就下载这个Mac OS
`02:49` 点DMG的版本
`02:50` 如果是Windows就下载这个版本
`02:52` 下载好之后
`02:53` 直接点击安装包进行安装就可以了
`02:56` 它这个项目的说明文档
`02:58` 也有比较详细的安装教程

### 购买 api 并配置 `03:00`

`03:01` 第三步到deepseek的开放平台购买API
`03:05` 我们来到DeepSeek官网
`03:06` 点击API开放平台
`03:08` 我这里之前已经充值了一些
`03:11` 在网页和DeepSeek对话是完全不收费的
`03:14` 但是想要调用API就需要进行充值
`03:17` 他们目前没有推出类似token plan这样的
`03:19` 按月订阅的套餐
`03:21` 好处就是用多少花多少
`03:23` 目前他们对V4 Pro模型打2.5折
`03:26` 每百万tokens缓存命中情况下
`03:29` 输入是0.025元
`03:31` 未命中是三元
`03:32` 输出是6元
`03:34` 这个折扣目前是到5月31号截止
`03:37` V4 Flash模型和Pro模型的价格对比
`03:40` 可以在官方的API文档里查看
`03:42` 这里点击充值按钮
`03:43` 然后选择金额和支付方式
`03:46` 建议先小额买一笔
`03:48` 用完之后根据自己的实际使用情况
`03:50` 再进行补充购买
`03:52` 付款完毕之后
`03:53` 点击左侧的API keys
`03:56` 点击创建api key
`03:58` 随便输入一个k的名称
`04:00` 点击创建
`04:01` 然后复制这个key的密钥
`04:04` 注意这个key的密钥
`04:05` 只能够在创建的时候查看
`04:07` 关闭这个页面就看不到了
`04:09` 如果丢失的话就需要重新创建一个k
`04:12` 然后打开CC switch这个应用
`04:14` 我这里
`04:15` 已经添加好一个DeepSeek的模型了
`04:17` 选中这里的claude图标
`04:19` 然后点击添加
`04:21` 在预设供应商这里找到DeepSeek
`04:24` 在api key这里填写key的密钥
`04:27` 然后这里需要修改一下这几个模型
`04:30` 可以直接参考我这个填写
`04:32` 默认模型就是DeepSeek
`04:34` V4 Pro
`04:35` 后面加上1m是因为之前的公告有说明
`04:38` 这样才能够开启1m的
`04:40` 上下文然后点击添加就可以了
`04:43` 这里就会多一个DeepSeek的模型
`04:46` 点击这个按钮
`04:47` 来测试当前API是否可用
`04:50` 点击这里可以配置用量查询
`04:52` 查看当前还剩多少余额
`04:54` 勾选这里
`04:55` 然后点击保存配置
`04:57` 这样的话就能够看到还剩下多少钱了
`05:00` 然后点击启用
`05:01` 就能够正常的使用claude code了
`05:03` 左上角有一个设置按钮通用
`05:05` 这里建议打开开机自启使用统计
`05:08` 这里也能够查看
`05:09` AI模型的使用情况和成本
`05:12` 我们打开终端应用
`05:13` 输入claude指令
`05:15` 那这个呢
`05:15` 就是DeepSeek V4 Pro的模型
`05:17` 并且是100万的上下文
`05:20` 我们输入指令/context
`05:22` 能够查看这里确实是100万的上下文
`05:25` tokens这里的默认思考等级是medium
`05:28` 可以使用命令/effort
`05:30` 然后空格后面的话
`05:32` 就会显示哪些等级可选
`05:35` 这里输入Max
`05:36` 回车确认
`05:37` 这样的话思考等级就会调到最高
`05:39` 还有一个命令可以快速的切换模型
`05:42` 输入/model
`05:43` 然后回车
`05:45` 默认模型其实就是V4 Pro
`05:47` 我们之前配置的opus和sonnet都是V4 Pro
`05:50` haiku模型 Flash
`05:52` 通过键盘的上下按键来选择
`05:55` 选中这个模型
`05:56` 然后回车确认
`05:57` 这样的话模型就切换到了V4 Flash
`06:00` 这里输入命令 /context
`06:02` 能够看到它的上下文显示是200K的tokens

### 演示操作 `06:05`

`06:07` 最后一部分来测试一下
`06:08` claude code搭配DeepSeek V4 Pro到底贵不贵
`06:12` 干活效果怎么样
`06:14` 首先说一下
`06:14` 它是基于文件夹的工作模式
`06:17` 所以你需要先
`06:18` 通过CD命令
`06:19` 跳转到你想要它打开的文件夹
`06:22` 比如我的项目文件夹的路径是这个
`06:25` 就需要输入屏幕上完整的指令回车
`06:28` 到了这个文件夹后
`06:29` 输入claude命令来启动它
`06:31` 如果路径很长的话就比较麻烦
`06:34` 有一种方法可以简化一下
`06:36` Windows用户应该可以直接在文件夹右键
`06:39` 从当前文件夹位置打开终端
`06:42` Mac用户右键的话是没有的
`06:44` 但是可以直接将文件夹拖拽到终端APP
`06:47` 那当前终端打开的文件夹
`06:49` 就是这个项目文件夹
`06:51` 输入PWD命令
`06:53` 可以查看当前文件夹的路径
`06:55` 然后输入claude
`06:57` 启动claude code
`06:59` 第一次打开的时候
`07:00` 需要信任一下这个文件夹
`07:02` 点击yes
`07:03` 后续退出的话需要连按两次Ctrl+c
`07:06` 这里安装一个
`07:07` 歸藏老师最近开源的一个PPT skill
`07:10` 设计是比较美观的
`07:12` 来到他的项目仓库
`07:13` 这是一个电子杂志风的网页
`07:16` PPT skill纯网页形式
`07:18` 适合线下分享
`07:19` 但是不适合培训课件
`07:21` 这个skill的名称叫做归档PPT skill
`07:24` 这里提供了多种的安装方式
`07:26` 最方便的就是直接复制这一段话
`07:29` 然后发给AI
`07:30` 选中这一段话
`07:32` command加c复制
`07:33` 然后来到Ctrl对话界面
`07:35` command加v复制
`07:37` 然后直接发送claude
`07:38` 在执行任务过程中
`07:40` 会需要一些权限的许可
`07:42` 遇到的时候直接选中yes就可以了
`07:44` 它的框架对于安全保护还是比较好的
`07:47` 整个的执行过程我就直接跳过了
`07:50` 这里提示安装好了
`07:51` 触发词就是
`07:52` 帮我做一份杂志风的PPT
`07:54` OK我在这里输入这句话
`07:56` 然后告诉他
`07:57` 要做的内容就在当前文件夹中
`08:00` 在当前的项目文件夹中
`08:01` 我放入了一个markdown文件
`08:03` 内容是关于Codex的使用方法论
`08:06` 然后回车执行
`08:08` 它会先查看项目文件夹的内容
`08:10` 和skill的使用说明
`08:12` 执行过程中可能会问一些问题
`08:14` 根据个人的需求选择就行
`08:17` 我这里也跳过过程
`08:18` 大概执行了6分钟左右
`08:20` 一共生成了9页PPT
`08:22` 并告诉了我每页的布局和内容
`08:24` 以及怎么操作
`08:26` OK我们直接打开浏览器
`08:27` 来查看一下这个网页的PPT
`08:30` 这是第一页Codex的方法论
`08:33` 整体的设计风格确实是比较美观的
`08:36` 如果是个人制作的话
`08:37` 可能要花费比较长的时间
`08:39` 并且效果还不一定有它这个好
`08:42` 第二页这里的话可能会有一点点问题
`08:45` 下方的文字有一部分被遮挡了
`08:47` 后面的页数大家可以具体去看一下
`08:49` 内容觉得怎么样

### 查看花费 `09:01`

`09:01` 最后再来看一下安装这个skill
`09:03` 以及制作这9页的PPT
`09:05` 一共花费了多少钱
`09:07` 我在做之前是9.89的余额刷新一下网页
`09:11` 那现在还有9.39的余额
`09:13` 一共是花了5毛钱
`09:15` 这里有一个每月用量的图表
`09:17` 展示每个模型花了多少钱
`09:19` 下面也有token的使用详情
`09:21` 包括输入和输出的具体数量
`09:23` 大家觉得DeepSeek V4 Pro的性价比怎么样
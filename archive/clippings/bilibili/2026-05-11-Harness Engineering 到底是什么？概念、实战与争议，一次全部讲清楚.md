---
title: "Harness Engineering 到底是什么？概念、实战与争议，一次全部讲清楚"
url: "https://www.bilibili.com/video/BV12LR1B3EUt/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV12LR1B3EUt"
cid: "38105713056"
author: "马克的技术工作坊"
upload_date: "2026-05-05"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116521690995238&bvid=BV12LR1B3EUt&cid=38105713056&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

继 Prompt Engineering、Context Engineering 之后，AI 圈最近又冒出了一个新名词，叫做 Harness Engineering。本期视频，我会带大家了解关于 Harness Engineering 的一切，具体包括：

Harness Engineering 是什么？
Harness Engineering 与 Prompt Engineering、Context Engineering 有什么关系
OpenAI 和 Anthropic 在 Harness Engineering 方面的实战
Harness Engineering 的来源
Harness Engineering 是不是噱头？它是软件工程领域的一次技术突破，还是 AI 圈的又一次概念炒作？

## 章节

- `00:00` 视频内容介绍
- `00:51` 提示问工程和上下文工程
- `04:50` Harness 工程是什么
- `09:06` 来自 OpenAI 的实战
- `18:41` 来自 Anthropic 的实战
- `27:19` 是噱头还是技术突破？

## 字幕

### 视频内容介绍 `00:00`

`00:00` 继prompt engineering
`00:01` context engineering之后
`00:03` AIHN最近又冒出了一个新名词
`00:05` 叫做harness engineering
`00:07` 从今年2月份开始
`00:09` 这个词频繁的在AI圈里面出现
`00:12` opening i专门发了一篇文章
`00:14` 讲他们怎么用harness engineering
`00:16` 在五个月内写了将近100万行代码
`00:18` ANTHROPIC也紧接着发文
`00:20` 分享了自己如何使用精心设计的harness架构
`00:23` 来驱动agent的开发应用
`00:24` 不仅如此
`00:25` 就连技术大牛martin bowler创立的技术网站
`00:28` martin baller go com也开始公开讨论起了harness engineering
`00:31` 但与此同时
`00:32` 也有不少人认为
`00:34` 这不过是个噱头而已
`00:35` 换汤不换药
`00:36` 那harness engineering到底是什么
`00:38` 它跟prompt engineering和context engineering又有什么关系呢
`00:42` harness engineering是真正的技术突破
`00:45` 还是说只是AI圈越的炒概念
`00:47` 这期视频我们就来把这个事情彻底搞明白

### 提示问工程和上下文工程 `00:51`

`00:54` 在讲harness engineering之前
`00:56` 我们不妨先来讲讲他的两个前任
`00:58` 分别是prompt engineering和context engineering
`01:01` 对这两个概念比较熟悉的同学呢
`01:03` 可以直接跳到下一个章节
`01:05` 首先是prompt engineering
`01:07` 这里的prompt呢
`01:08` 你可以简单理解成用户发给大模型的话
`01:10` 而prompt engineering呢就是一门研究
`01:13` 怎么把这句话说清楚的技术
`01:15` 举个具体点的例子啊
`01:16` 比如说我们可以向大魔星发问
`01:19` 帮我的猫起个名字
`01:20` 这个问题呢就是prompt了
`01:22` 接到prompt之后
`01:23` 大模型就会给你一个答案
`01:25` 比如说是什么花花呀
`01:27` 小白呀之类的
`01:28` 不过这些答案可能都无法让你满意
`01:31` 因为你家的猫呢可能是橘色的
`01:33` 无论是花花还是小白
`01:35` 都与橘色这个颜色相冲突
`01:37` 那为什么大模型会给你错误的答案呢
`01:40` 这是因为我们没有在prompts里面
`01:42` 给大模型充足的信息
`01:44` 既然问题出在prompt上面
`01:46` 那解决问题的关键自然也在prompt上面了
`01:50` 说的再具体一点
`01:51` 那就是我们需要学会
`01:53` 如何更精准的表达自己的需求
`01:55` 这个呢就引出prompt engineering了
`01:58` prompt engineering就是专门用来研究
`02:01` 怎么把话说清楚的
`02:02` 还是用之前的例子
`02:03` 让我们重新设计一下这个问答流程
`02:06` 按照prompt engineering的理念
`02:08` 我们需要发送的prompt就应该是这样子的
`02:11` 帮我的橘色小猫起名两个字
`02:13` 需要体现出它活泼爱玩的性格
`02:16` 这个时候
`02:17` 大模型就可以给出一些更让我满意的名字了
`02:19` 比如说是橘宝
`02:21` 代表橘色的大活宝
`02:23` 橙豆
`02:24` 橙色的小豆子
`02:25` 你想小豆子掉在地上蹦蹦跳跳的
`02:28` 那也能够体现出猫活泼的性格嘛
`02:30` 你看这两个名字就跟你的猫更贴切了
`02:33` 没错说白了呢
`02:35` prompt engineering就是一门调整大模型提示词的技术
`02:38` 对就是这么简单
`02:40` 不过如今prompt engineering已经很少被单独提起了
`02:44` 一方面它的门槛实在是太低了
`02:46` 另一方面呢模型本身的能力呢也变得更强了
`02:49` 很多时候不需要在prompt上调来调去
`02:52` 能给出不错的回答
`02:53` 好这就是prompt engineering了
`02:56` 下面呢我们来看看context engineering
`02:59` 我们还是用小猫来举例啊
`03:01` 假设你拿到了小猫的名字之后
`03:03` 还继续跟大魔仙聊天
`03:05` 比如你问它
`03:06` 那它平时吃什么好呢
`03:08` 这个呢就是我们的prompt了
`03:10` 我们来把它单独标出来
`03:12` 那现在重点来了
`03:14` 我们此时要发给大模型的
`03:16` 其实不仅仅有这个prompt
`03:18` 还有之前的对话历史
`03:20` 这样的大魔情
`03:21` 才知道这个新问题里面的它指代的是什么
`03:24` 那无论是prompt还是对话历史
`03:26` 它们呢都是大模型所接收到的信息
`03:29` 我们把大模型所接收的所有信息起个名字
`03:32` 就叫做context
`03:34` 当然context的内容呢还不只有这两个
`03:37` 它还包含工具列表
`03:38` skill列表等等
`03:39` 我们就不一一列举了
`03:41` 你也不用太关心
`03:42` 你只需要知道context是有容量上限的
`03:45` 所以我们不可能无止境的往里面塞东西
`03:48` 我们需要精心设计context里面的内容
`03:51` 这个呢就叫做context engineering
`03:54` context engineering有很多具体的方法
`03:56` 比如说其中一个非常经典的技术
`03:59` 就是上下文压缩之前
`04:01` 不是说我们会把对话历史放在context里面吗
`04:04` 我们跟模型越聊越多
`04:06` 对话历史呢也会越来越多
`04:08` 当超过某个阈值的时候
`04:10` 我们就可以使用上限文压缩技术
`04:12` 把之前的对话历史做个总结
`04:14` 以防止context里面的内容过多
`04:16` 影响回答效果
`04:18` 当然除了上下文压缩之外
`04:20` context engineering还有很多其他的方法
`04:23` 比如说是什么动态检索外部资料啊
`04:26` 间接式披露啊等等
`04:28` 这里呢就不一一列举了
`04:30` 可以看出context engineering还是挺能整活的
`04:32` 搞出了这么多的东西
`04:34` 不过吧这依然不是重点
`04:37` 因为大家发现啊
`04:38` contact engineering这门技术的效果呢是有一定的上限的
`04:41` 为了进一步乍看大模型的潜力呢
`04:44` AI却又整出了新花样
`04:46` 这个就引出了我们今天真正的主角harness engineering

### Harness 工程是什么 `04:50`

`04:53` 要搞明白harness engineering这个概念
`04:56` 我们就得先从harness这个单词说起
`04:59` 这个词在日常生活中其实不太常见
`05:02` 很多人可能也是第一次听说
`05:04` harness ys这个词的本意啊
`05:06` 其实是马具的意思
`05:08` 大家看啊
`05:09` 这是一匹马
`05:10` 而harness或者说是马具
`05:12` 就是套在马身上
`05:13` 用来控制马的那些装备
`05:15` 比如说是缰绳啊
`05:16` 头套啊
`05:17` 这些虽然马非常强大
`05:19` 但是我们必须借助马具的力量来限制马的活动
`05:23` 这样呢我们才能够让马为我们人类所用好
`05:26` 现在呢我们把马具从马身上单独拆下来
`05:30` 做一个类比
`05:31` 左边这匹脱掉马具的马
`05:33` 对应的就是AI领域里面的大模型
`05:36` 你想大模型是不是特别强
`05:38` 尤其是像GB t opus这样的顶级模型
`05:41` 能干的事情可太多了
`05:43` 但大模型就像马一样
`05:45` 如果我们不对它加以干预
`05:47` 任由大模型自己去运行和发挥
`05:49` 那他就会像脱缰的野马一样发散思维
`05:52` 甚至产生严重的幻觉
`05:54` 最终根本无法稳定的给我们想要的结果
`05:57` 所以呢我们必须要把大模型给控制住
`06:00` 就像用马具来控制马一样
`06:02` 而这套用来控制大模型的系统
`06:04` 就被称为了harness
`06:06` 没错
`06:07` harness就对应了这个马具好
`06:10` harness呢
`06:10` 就是agent里面用来控制和驾驭大模型的系统
`06:13` 所以呢从这一点出发
`06:15` 我们就能推导出harness的公式
`06:18` 也就是harness就等于agent减去model
`06:21` 换句话说
`06:22` 一个完整的agent减去里面的大模型
`06:25` 剩下的所有东西都是harness
`06:27` 不过需要注意的是啊
`06:29` harness engineering是一个非常新的概念
`06:31` 目前业界呢还没有形成严格的定义
`06:34` 这个公式
`06:35` 只是目前大多数人比较认可的一种说法
`06:37` 并非是严格的学术定义
`06:39` 所以只要不是大模型
`06:41` 就是harness
`06:42` 关于这点呢
`06:43` 我相信你已经明白了
`06:45` 下面呢我们来看个具体的例子
`06:47` 我们可以用cloud code来举例
`06:49` 在cloud code里面
`06:50` 所有不属于cloud模型的部分都是harness
`06:53` 比如说是写在cloud md里面
`06:55` 那些纳模型要遵循的规则啊
`06:57` cloud code可以使用的工具呀
`06:59` 或者是它的定时调度机制啊等等
`07:02` 这些呢都是harness
`07:03` 当然harness涉及的范围很广
`07:06` 我这里只是举了三个例子而已
`07:07` 总而言之啊
`07:08` 只要不是模型
`07:10` 我们都可以将它视为harness的一部分
`07:13` 那harness了解了顺理成章的
`07:15` harness engineering的概念也就呼之欲出了
`07:18` harness engineering呢就是一门专门研究
`07:20` 如何构建与设计harness的技术
`07:23` 换句话说就是除了大模型本身不研究别的
`07:27` 什么都研究
`07:28` 它
`07:28` 不再是紧紧盯着模型输的那点提示词
`07:31` 或者是上下文
`07:32` 而是站在更高的系统层面上
`07:34` 研究
`07:34` 怎么给大模型设计一套可以稳定运行的系统
`07:37` 让大模型能够踏踏实实地为我们人类做事
`07:41` 所以从这里可以看出
`07:42` Prompt engineering
`07:44` context engineering和harness engineering更像是一种层层递进
`07:47` 研究范围不断向外扩展的关系
`07:50` 他们关注的问题呢是越来越大
`07:52` 越来越过
`07:54` prompt engineering研究的是怎么问问题
`07:56` 具体来说呢
`07:57` 就是如何组织prompt
`07:59` 把发给大模型的话说得更清楚
`08:01` 更准确
`08:02` 让模型能够更容易理解你的真实意图
`08:05` 并给出理想的结果
`08:06` context engineering呢研究的内容比prompt engineering更广一些
`08:11` 他研究的是怎么给信息
`08:12` 具体来说呢
`08:13` 就是怎么在最合适的时机
`08:15` 把最合适的内容放到模型的context里面
`08:18` context里面的内容呢不仅包括prompt
`08:21` 还包括工具列表
`08:22` 对话历史等等
`08:24` 所以呢context engineering的研究范围会更广一些
`08:27` harness engineering的研究范围呢就更加激进了
`08:30` 他研究的是如何搭建系统
`08:32` 也就是如何围绕着大模型
`08:34` 搭建一个完整可靠的agent
`08:36` 他的研究对象呢直接就覆盖了
`08:38` 除了大模型之外的所有内容
`08:40` 比如说是什么权限管控啊
`08:42` 工具管理呀等等
`08:44` 都是harness engineering要研究的内容
`08:46` 相信现在你已经了解了harness engineering是什么了
`08:50` 那harness engineering具体要做哪些事呢
`08:52` 有没有一些实战的例子
`08:54` 说实话这个概念实在是太新了
`08:57` 目前业界呢也没有一个公认的体系
`08:59` 与其我在这里自说自话
`09:01` 我们不如来直接看看大厂是怎么做的
`09:04` 我们首先从OpenAI开始

### 来自 OpenAI 的实战 `09:06`

`09:10` 2025年8月
`09:11` OpenAI内部启动了一个疯狂的实验
`09:13` 那就是用AI从零开始写一个真实的软件产品
`09:20` 对没错
`09:21` 这个产品的所有的组成部分都是由AI生成的
`09:24` 具体是包括业务逻辑啊
`09:26` 测试啊
`09:27` CI配置啊
`09:27` 文档啊
`09:28` 内部工具等等
`09:29` 所有的东西都是AI生成的
`09:31` 靠着AI这个项目的代码规模
`09:33` 直接是干到了将近100万行
`09:35` 而且注意咯
`09:36` 这可不是一个玩具
`09:37` 它是一个真正在线上跑
`09:39` 有真实用户的生产系统
`09:41` 达到这样的规模呢
`09:42` 总体耗时只用了五个月左右
`09:45` 团队规模一开始呢是三个人在主岛
`09:47` 后来呢也只不过是扩张到了七个人
`09:50` 算下来呢开发效率差不多是纯人工的十倍了
`09:53` 但有意思的是
`09:54` 这个实验一开始的进展并不顺利
`09:56` 这并不是因为大模型不够聪明
`09:58` 而是因为harness没有搭建好
`10:00` 工程师们发现agent经常走错方向
`10:02` 甚至重复犯同一个错误
`10:04` 于是他们意识到
`10:05` 要想让agent的可靠的工作
`10:07` 真正的功夫呢在于把harness设计好
`10:10` 为此呢他们做了大量的优化
`10:12` 并且写了一篇文章
`10:13` 详细记录了这个过程
`10:14` 这篇文章呢我反复读了好几遍
`10:17` 它的信息量非常大
`10:18` 涉及到很多harness engineering的优化点
`10:21` 所以呢这期视频我们就来重点聊一聊OPENAI
`10:24` 在harness engineering上面到底做了什么
`10:26` 原文呢是从多个具体的优化点里面展开的
`10:29` 信息密度非常高
`10:31` 所以呢这里我尝试给这些优化点大致分了个类
`10:36` 验证与反馈和技术摘清理
`10:39` 当然需要强调的是
`10:41` 这只是我个人所做的一个分类
`10:43` 主要是为了帮助大家理解
`10:45` 下面呢我们就来一一看看
`10:47` 这三大类到底是在做什么
`10:52` 是让A证的获取到足够充足的信息
`10:54` 你可以想象一下
`10:55` 一个新入职的工程师
`10:57` 如果对项目一无所知
`10:58` 不清楚模块怎么划分
`10:59` 不知道代码规范是什么
`11:01` 不了解团队过去做过哪些技术决策
`11:03` 那他是根本就没有办法开始工作的
`11:05` agent呢也是如此
`11:07` 为了解决这个问题
`11:08` OpenAI最初的尝试是
`11:10` 把所有的项目规范和相关信息
`11:12` 塞进一个超大的agent dmd文件
`11:14` 这个文件呢会随着用户的问题一起发给大模型
`11:18` 这样的大模型就有了充足的信息了
`11:20` 不过OPENAI后来发现呢
`11:21` 使用一个大而全的AGENDMD文件
`11:24` 根本无法解决问题
`11:25` 原因呢是有很多
`11:27` 这里呢说两个最关键的
`11:29` 第一个呢是内容太多
`11:30` 使得模型的效果变差
`11:32` 设想一下
`11:33` 你第一天去新公司报到
`11:34` hr直接砸给你一个巨厚的员工手册
`11:37` 说规矩全在这里
`11:38` 你自己看吧
`11:39` 那我猜你肯定是一脸懵的
`11:41` 完全不知道该从哪里看起
`11:43` 也完全搞不清楚重点在哪
`11:45` AI呢也是一样
`11:46` 一股脑的把所有的信息全部都喂给他
`11:49` 那他就迷失了
`11:50` 只能抓到一些碎片
`11:51` 真正关键的内容反而被淹没在了废话里
`11:55` 第二点呢是这个文件会逐步的孵化
`11:57` 项目是在不断演进的
`11:59` 文件里面的内容却没有人及时更新
`12:02` 时间一长呢就变成了一堆过时信息的垃圾堆
`12:05` 更糟糕的是啊
`12:06` 这个文件乱到连人都懒得去整理
`12:08` 那agent呢也就没有办法判断哪些内容还有效了
`12:11` 所以呢他们后来改变了策略
`12:13` 把agents点MD文件压缩到只有100行左右
`12:16` 大体结构呢差不多就是这样子的
`12:18` 可以看出
`12:18` AGEN点MD里面呢
`12:19` 已经没有什么太多实质性的内容了
`12:21` 就是一个目录而已
`12:22` 对应的文件系统呢大致是这个样子的
`12:25` 可以看出相关的文档和目录呢
`12:27` 会跟agents点MD放在一起
`12:29` 这样能用到哪块
`12:30` 再给agent看哪块效果呢就会好很多
`12:33` 看来大模型跟人一样
`12:34` 还是要把信息分门别类地放好才行
`12:37` 除此之外
`12:38` OpenAI还发现了一个问题
`12:40` 项目里面有很多重要的信息
`12:41` 其实并不在代码仓库里面
`12:43` 他们可能是散落在slack的聊天记录里
`12:46` 可能是躺在某个google dog的文档里
`12:48` 甚至呢是只存在于某个老员工的脑子里面
`12:51` 这点呢我相信大家也深有体会啊
`12:54` 只不过可能用的是国内的软件生态
`12:56` 而不是说是什么slack
`12:57` google dog这些对于agent来说
`13:00` 他只能是看见仓库里面有什么
`13:02` 仓库
`13:03` 外面的一切对他来说都跟不存在没有什么区别
`13:06` 所以opening i是怎么做的呢
`13:07` 他们是强制要求把所有重要的决策和约定
`13:11` 都搬进代码仓库
`13:13` 让仓库成为唯一的事实来源
`13:15` 这样呢agent就可以了解到这些外部的信息了
`13:22` 下面我们来看看验证和反馈部分在做什么
`13:27` 有了充足的信息之后
`13:28` agent就可以写代码了
`13:30` 后面的重点呢就是在AHS写完代码之后
`13:32` 让他能够验证自己的成果是否正确
`13:35` 不然呢他写完之后没法验证
`13:37` 那这肯定是没有办法保证准确率的
`13:40` open air的做法呢
`13:41` 是给codex配上足够完善的工具和skill
`13:45` 在质量值的帮助下
`13:46` codex就能够在任务进行中随时验证自己的输出
`13:50` 让我们举个例子啊
`13:51` 比如说他们把chrome dev tools
`13:53` 介入到了codex的运行环境里面
`13:55` 这样呢codex就可以自己截图
`13:58` 自己查看DOM结构
`13:59` 并且自己模拟用户操作
`14:01` 从而去验证UI是否符合用户的要求
`14:04` 如果发现这里面有问题
`14:06` 那codex就可以原地修复
`14:08` 整个过程呢就不需要人去介入了
`14:10` 除了UI之外
`14:11` OpenAI还给codex接入了完整的可观测性工具站
`14:14` 以便让codex可以读取日志
`14:16` 读取指标
`14:17` 并在必要的时候追踪运行链路
`14:20` 以排查问题
`14:21` 为了确保日志和输出的准确性
`14:23` codex的每个任务都跑在一个完全隔离的环境里
`14:26` 有自己独立的日志和指标
`14:28` 任务结束之后呢
`14:28` 也能自动销毁
`14:30` 这样做了之后
`14:31` OpenAI甚至可以让codex
`14:33` 对系统做一些可量化的性能调优
`14:35` 比如说是要确保服务启动时间
`14:38` 不能够超过800ms之类的
`14:40` 上面所讲的这些呢都是为了保证codex生成代码
`14:43` 可以实现产品诉求
`14:44` 但很多时候
`14:45` 我们对codex生成的代码本身还有一定的要求
`14:48` 比如说呢
`14:49` 这些代码至少要符合项目架构上的规范
`14:51` OpenAI把他们的系统分成了好几层
`14:53` 并且规定了严格的依赖关系
`14:55` 从上到下分别是ui run time service
`14:58` Rapple conftypes
`15:00` 每一层都只能依赖它
`15:02` 下面的层依赖关系不能反了
`15:04` 比如说是像raffle层依赖UI层
`15:06` 这样的事情是万万不能发生的
`15:08` OPENAI呢是使用LINTER和测试
`15:10` 来避免类似的情况发生
`15:12` 我们一起来看看他们是怎么保证架构规范的
`15:15` 在agent的生成代码之后
`15:16` LINTER或者是测试便会开始检测代码是否合规
`15:19` 如果不合规的话
`15:20` 他便会报错
`15:21` 报错信息呢会发回到agent那里
`15:23` agent呢会根据报错信息去修改
`15:26` 改完之后再跑LINTER或者测试
`15:28` 这样呢就形成了一个完整的自动闭环
`15:30` 不需要人工去介入
`15:32` 这个流程会重复个几次
`15:34` 直到某次检测之后
`15:35` 所有的规则
`15:37` 这样呢
`15:38` 我们就拿到了一份符合架构要求的代码了
`15:41` 好这些呢就是验证与反馈这部分的内容了
`15:44` 下面我们来看看技术债清理这部分在做什么
`15:47` agent在大规模生成代码的过程中
`15:50` 会不可避免地引入一些糟糕的设计模式
`15:52` 比如试试重复的代码
`15:54` 偏离架构规范的写法
`15:55` 不一致的命名之类的
`15:57` 慢慢积累下去的话呢
`15:58` 会把整个代码库搞得一团糟
`16:00` OpenAI的解法呢是给技术债做一些垃圾回收
`16:03` 把这些问题统统解决掉
`16:05` 具体来说呢就是设置一个后台的codex任务
`16:08` 定期去扫描整个代码库
`16:10` 找出其中偏离规范的地方
`16:12` 自动修改并提交
`16:13` 以便确保代码的质量
`16:15` 始终维持在一个比较高的水准
`16:17` 这个呢是对代码的清理和优化
`16:19` 除了代码之外
`16:20` 他们还对文档做了同样的事情
`16:22` 具体来说呢是他们设置了一个后台任务
`16:24` 定期扫描整个文档库
`16:26` 找出那些过时的和实际代码对不上的文档
`16:29` 自动提交修复
`16:30` 所以你看啊
`16:31` 无论是代码还是文档
`16:32` OpenAI都有着一套对应的维护方案
`16:35` 两边呢都不会放任自流
`16:37` 以上呢就是OpenAI所做的一些核心的harness engineering
`16:41` 实践了
`16:42` 看完这些
`16:42` 你可能有一个强烈的感觉
`16:44` 这哪里是在写代码呀
`16:46` 这完全就是在给AI构建干活的环境啊
`16:49` 人负责定方向搭框架
`16:51` 具体干活的事情就全由AI来做了
`16:54` 没错
`16:55` 这正是OpenAI这篇文章想要传达的最核心的理念
`16:58` 通过这五个月的疯狂实验
`16:59` OpenAI不仅跑通了这套100万行代码的系统
`17:03` 更重要的是
`17:03` 他们在这个过程中
`17:04` 重新定义了人类和AI在未来的工作边界
`17:07` 在文章中
`17:08` OpenAI抛出了一个非常关键的断言
`17:11` Human deer agents execute
`17:13` 翻译过来就是人类负责掌舵
`17:15` Agent
`17:16` 负责干活
`17:17` 说白了到了harness engineering这一步
`17:19` 人和AI的分工就彻底变了
`17:22` 以前工程师要亲自下场
`17:23` 一行一行的写代码
`17:25` 遇到报错自己查测试呢也要自己跑
`17:31` 给上下文制定规则
`17:32` 在关键的地方做判断
`17:34` 而那些真正重复的琐碎的开发工作就交给agent
`17:38` 在harness里面跑就好了
`17:39` 基于这个全新的边界
`17:41` OpenAI紧接着又提出了第二个非常重要的观点
`17:44` 这个观点呢点明了
`17:45` 软件工程师在AI时代的新职责
`17:48` 对应文章里面呢是这一段话
`17:50` 这大致意思就是在说啊
`17:51` 虽然人类不再需要亲自手写代码
`17:53` 但软件工程的工作并没有消失
`17:55` 而是演变成了完全不同的形态
`17:58` 如今软件工程师的核心职责变了
`18:00` 变成了为agent的搭建稳定可靠的系统与支撑框架
`18:03` 以此来尽可能的提高代码产出效率
`18:06` 这两个观点呢可以说是OPENAI那篇文章的灵魂
`18:09` 他直接告诉我们
`18:10` harness engineering不仅仅是如何写好prompt
`18:13` 或者是如何管理上下文这么简单
`18:15` 它是在重塑整个软件工程的开发流程
`18:18` 那以上就是OPENAI这场harness engineering
`18:21` 实战的核心精髓了
`18:22` 最后呢我想跟大家说明一下
`18:24` 为了帮助大家快速理清脉络
`18:26` 抓住核心思路
`18:28` 这一篇文章呢我是做了一定的提炼和简化的
`18:31` 但必须要说OPENAI的这篇文章写得非常的精彩
`18:34` 如果你对里面的技术细节感兴趣
`18:37` 强烈建议亲自去读一遍原文
`18:39` 相信一定会让你大受启发

### 来自 Anthropic 的实战 `18:41`

`18:45` 在这一章节里
`18:46` 我们来看看ANTHROPIC的两篇
`18:48` 与harness engineering相关的文章
`18:50` 第一篇呢是去年11月发表的effective harnesses
`18:54` For long running agents
`18:55` 它讲述了如何配置环境
`18:57` 以便让agent长时间自主运行
`18:59` 第二篇呢是今年3月份发表的
`19:02` Harness design for long running application development
`19:05` 这篇文章呢可以理解为是第一篇文章的续集
`19:09` 他在第一篇文章的基础上
`19:10` 对harness架构做了进一步的优化和调整
`19:13` 使其能够处理更多类型的任务
`19:15` 达到更好的效果
`19:17` 这两篇文章的信息量很大
`19:19` 不过总结下来最核心的地方呢就两点
`19:21` 一个呢是跟任务规划有关
`19:23` 另外一个呢是跟质量评估有关
`19:26` 我们来一个一个看
`19:27` 首先来看一下任务规划这一部分
`19:30` 在第一篇文章中
`19:32` ANTHROPIC做了一个实验
`19:33` 直接让agent执行一个任务
`19:35` 克隆cloud点AI
`19:37` cloud点AI就是cloud的聊天界面
`19:39` 大致呢就是这个样子的
`19:41` 它跟chat gbt是同类型产品
`19:44` 虽然看起来只是一个聊天界面而已
`19:46` 但说实话它背后的功能还是挺多的
`19:49` 一口气做出来是一件几乎不可能做到的事情
`19:52` 这个呢也是ANTHROPIC1开始所遇到的问题
`19:55` 在ANTHROPIC的实验里
`19:56` AGH的接到需求之后立马就开干了
`19:58` 干劲非常的足啊
`20:00` 但是效果也非常不好
`20:01` 主要是因为这个需求的工作量实在是太大了
`20:05` 直接给到agent的话
`20:06` agent就会急于求成
`20:08` 从而引发一系列的问题
`20:10` 比如说他总想一口气把所有的功能全部做完
`20:13` 结果干到一半
`20:14` 上下文就满了
`20:15` 直接抛下了个烂摊子
`20:16` 等到下一个agent的接手的时候
`20:18` 完全不知道前面发生了什么
`20:20` 只能靠猜
`20:21` 这一猜呢就坏事了
`20:23` 虽然有些功能只做了一半
`20:25` 但接受的agent呢并不知道啊
`20:27` 粗略的扫了一眼
`20:28` 还以为已经大功告成
`20:29` 于是直接宣布完工
`20:31` 草草就收工了
`20:32` ANTHROPIC在第一篇文章里面写了对应的解法
`20:35` 他们呢是引入了一个叫做INITIALIZER的agents
`20:39` 从这个名字就可以看出来
`20:41` 这个agent就是用来初始化执行环境的
`20:43` 比如说是拆解用户需求啊
`20:45` 编写启动脚本啊
`20:47` 添加进度文件啊等等
`20:49` 这里面最核心的就是拆解用户需求
`20:51` 这一点
`20:52` 具体来说就是把用户的需求
`20:54` 拆解为一个详细的功能列表
`20:56` 后续负责干活的agent呢
`20:58` 就可以直接拿着这个功能列表去干活了
`21:01` 而且呢这个干活的agent会一个功能点
`21:04` 一个功能点的做
`21:05` 做完一个标记一个
`21:06` 这样稳扎稳打
`21:07` 整个流程的可控性就高了很多
`21:10` 后来在写第二篇文章的时候
`21:12` ANTHROPIC对这个思路做了一些演进
`21:14` 他们把INITIALIZER里面最核心的一件事情
`21:17` 也就是拆解用户需求这个事情单独给拿了出来
`21:20` 做成了一个新的agent
`21:21` 叫做planner
`21:22` 他负责把用户一句模糊的需求
`21:25` 扩展成一份完整清晰的功能列表
`21:28` 这样后面的agent在写代码的时候
`21:30` 就不用对着用户的需求猜了
`21:32` 照着功能点一个个做就行好
`21:34` 那规划的问题解决了
`21:36` 这一部分的产物呢就是planner
`21:38` 下面呢我们再来看第二点
`21:40` 质量评估
`21:41` 一般来说光是让agent的生成代码是不够的
`21:44` 我们还需要对它生成的代码做一些质量评估
`21:46` 看看产出的东西到底行不行
`21:48` 如果产出质量不行的话
`21:50` 我们需要把对应的问题列表发回给agent
`21:52` 以便让他做相应的修改
`21:54` 这个呢才是一个比较合理的流程
`21:57` 现在我们来看看具体是怎么做质量评估的
`22:00` 这里面呢有两种评估方案
`22:02` 一种呢是人工评估
`22:03` 这个就不太行了
`22:05` 效率太低了
`22:06` 都AI时代了
`22:07` 能交给AI的就都交给AI吧
`22:09` 那这就引出了第二个方案
`22:11` 让agent的自评
`22:12` 也就是自己评估自己的产出
`22:14` 有问题就修修完再评
`22:16` 循环往复
`22:17` 直到合格为止
`22:18` 听起来挺合理的是吧
`22:20` 但ANTHROPIC发现这个方案根本不好用
`22:23` 原因很简单啊
`22:24` A证自评这件事情
`22:25` 本质上就是王婆卖瓜自卖自夸
`22:28` 他对自己做的东西天然就有滤镜
`22:30` 所以呢即使产出里面有明显的bug
`22:32` 他也能做到视而不见
`22:34` 给自己打个高分之后就草草收工了
`22:36` 所以呢
`22:37` ANTHROPIC就直接把前面两种方案都给废弃了
`22:39` 搞出了第三个方案
`22:40` 那就是做一个专门的评估agents来评估产出质量
`22:44` 由于这个评估agent是一个独立的第三方
`22:47` 他自然就没有理由去替别的agent的产出护短评估
`22:51` 结果呢也就客观多了
`22:52` 而且把评一阵子单独拎出来
`22:54` 还有一个好处
`22:55` 那就是我们可以单独去优化
`22:57` 去训练这个评估一阵子
`22:58` 让他的评估效果做到最好
`23:00` 对这个呢就是ANTHROPIC的最终方案了
`23:03` 换句话说
`23:04` 我们最终需要把生成代码和质量评估
`23:06` 这两件事情给拆开
`23:08` 分别交给两个不同的agent来做
`23:10` 其中负责生成代码的那个叫做generator
`23:13` 负责质量评估的那个叫做evaluator
`23:16` 这个呢就是最终的质量评估流程了
`23:19` 所以质量评估这一环节我们提到了两个agent
`23:22` 一个是评估用的evaluator
`23:24` 一个是生成代码用的generator
`23:27` 再加上之前说过的planner
`23:29` 我们就有三个agent了
`23:31` 下面呢我们来画一下时序图
`23:32` 看看这三个agent是怎么分工合作完成用户需求的
`23:36` 首先是planner
`23:38` 它会把用户的需求呢拆解为具体的功能列表
`23:41` 然后发送给generator
`23:43` generator接收到功能列表之后
`23:45` 它会从中挑选出一个功能点
`23:47` 然后呢
`23:47` 他就就着这个功能点去跟evaluator
`23:50` 讨论下交付标准
`23:51` 也就是讨论下到底做到什么程度
`23:54` 才算是完成了这个功能点
`23:55` generator呢首先会把他的想法发过去
`23:58` evaluator呢
`23:59` 一开始可能会对这个提议提出一些小改意见
`24:02` 然后再发回给generator
`24:04` generator呢会根据意见再次提交新的交付标准
`24:07` 所以呢这个过程会重复个几次
`24:09` 直到evaluate确认generate的提议没问题为止
`24:13` 确认好交付标准之后
`24:15` generator便开始生成代码来实现这个功能点了
`24:18` 实现完毕之后呢
`24:19` generator会把它的实现结果提交给evaluator
`24:22` evaluator会对结果做出评估反馈
`24:25` 比如说是一开始可能评估不通过
`24:27` 那如果不通过的话呢
`24:29` generator就要修改代码了
`24:30` 所以呢这个提交结果评估反馈的过程啊
`24:33` 也会重复个几次
`24:35` 直到evaluate评估通过为止
`24:37` 到这里一个功能点就算是开发完了
`24:40` 但是我们不只有一个功能点啊
`24:43` 所以呢我们就需要再次重复之前的这个流程
`24:46` 把后面的功能点全部都逐步做完
`24:48` 这个呢就是大致的流程了
`24:50` ANTHROPIC把这个包含了三个agent的方案
`24:53` 叫做full harness方案
`24:55` 相比之下
`24:56` 那种只靠一个generator
`24:57` 独立完成所有需求的传统单agent模式
`25:00` 被ANTHROPIC称为solo方案
`25:02` ANTHROPIC拿了一个具体的任务
`25:04` 来验证这两个方案的差距
`25:06` 这个任务呢就是做一个游戏制作工具
`25:09` 从效果上来看啊
`25:10` solo和for harness这两个方案的差距还是很明显的
`25:14` solo方案的问题呢很多
`25:16` 比如说是布局不合理
`25:18` 产品逻辑难以理解
`25:19` bug到处都是
`25:21` 基本上呢是没有办法用的
`25:23` 而for harness方案呢就有了明显的改善了
`25:26` 无论是布局还是整体的产品逻辑
`25:28` 都达到了可用的水准
`25:30` 虽然还是存在一些问题
`25:31` 但比起solo方案来说
`25:33` for harness的效果呢是明显要好不少的
`25:36` 当然这样做也不是没有代价的
`25:38` Full harness
`25:39` fun的耗时和花费都要明显的高于solo方案
`25:43` ANTHROPIC给出了一个对比表格
`25:45` 大家可以感受一下
`25:47` solo方案耗时20分钟
`25:49` 花费九美元
`25:50` 而four harness方案呢是耗时六个小时
`25:54` 花费高达200美元
`25:56` 所以可以看出啊
`25:57` for harness的方案
`25:58` 无论是耗时还是花费都要远高于solo方案
`26:01` 虽然如此
`26:02` 但不得不承认
`26:03` for harness的方案效果确实是好了不少
`26:06` 毕竟精雕细琢是有代价的呀
`26:09` 这对于我们人类来说也是一样
`26:11` 考到60分可能只需要复习三天
`26:13` 但是想考到90分
`26:15` 那可能就得复习一个月了
`26:17` 这个大家多少都会有些体会吧
`26:19` 最后再提一个ANTHROPIC后来做的优化点
`26:22` 让我们重新看一下这个流程图
`26:24` 注意其中的这一部分
`26:26` 这一部分说明了
`26:27` generator每次只会选取一个功能点
`26:30` 做完这个功能点再做下一个循环往复
`26:33` 直到完成所有的功能点为止
`26:35` 这个逻辑呢是ANTHROPIC
`26:36` 在提示词里面强制generator这么处理的
`26:39` 否则让generator自行发挥的话
`26:41` 他还是会急于求成
`26:43` 最后留下一堆烂摊子
`26:44` 不过在OPPO4.6发布了之后
`26:46` 这个约束就不怎么需要了
`26:48` ANTHROPIC后面就把这一部分给去掉了
`26:51` 最后呢就简化成了这个样子
`26:53` 那为什么后面就可以这么做了呢
`26:56` 因为基于OPPO4.6做的generator变得更强了
`26:59` 它可以一次把所有的功能点全部都拿过来
`27:02` 自己决定先做哪个再做哪个
`27:05` 稳步地向前推进
`27:06` 不需要别人再对他的执行流程指指点点
`27:09` 而在这种情况下
`27:10` evaluator也直接评估最终产出就可以了
`27:13` 不需要再分功能点评估了
`27:15` 关于这一部分
`27:16` 我们在下一章节还会提到这里
`27:18` 你有个大体的概念就行

### 是噱头还是技术突破？ `27:19`

`27:22` 在这一章节里
`27:23` 我们来聊聊目前争议最大的一个问题
`27:25` harness engineering到底是不是一个噱头
`27:28` 要回答这个问题
`27:29` 我们不妨先来扒一扒
`27:31` 这个词到底是怎么火起来的
`27:33` 首先单就harness这个词来说
`27:36` 它其实并不算是一个彻头彻尾的新词
`27:39` 一般大家用它来指代
`27:40` 为了支持某个功能所做的一套框架
`27:43` 让我们来举几个具体的例子
`27:45` 比如在传统的软件测试领域
`27:48` 就有一个概念叫做test harness
`27:50` 它代表为了支持测试代码运行而做的一套框架
`27:54` 这个框架里面可能会包含测试运行器
`27:57` 测试环境等等
`27:58` 而在AI领域
`27:59` 很多开发者其实也早就用到harness这个词了
`28:02` 比如有个开源的项目叫做l m m evaluation
`28:05` Harness
`28:06` 它呢就是为了支持模型
`28:08` 效果评估而做的一套框架
`28:09` 不仅如此
`28:10` 我们刚才重点讲过
`28:12` ANTHROPIC去年11月发表的一篇文章
`28:14` 叫做effective harnesses for long running agents
`28:17` 这里的harness就代表
`28:19` 为了支持agent的长时间运行而做的一套框架
`28:22` 所以你看harness这个词一直都在
`28:24` 那大家呢也都在默默的用
`28:26` 谁也没有觉得这是一个需要大吹特吹的新概念
`28:30` 可以说harness这个词本身并不是重点
`28:33` 重点是harness engineering
`28:35` 把这两个词组合在一起
`28:37` 其实是最近才发生的事情
`28:39` 目前比较公认的起点
`28:41` 就是我现在屏幕上所展示的这篇文章
`28:44` My ai adoption journey
`28:46` 这篇文章在今年2月5号的时候发表
`28:48` 作者是mitchell harsh mode
`28:50` 可能国内有些同学对这个人不太熟悉
`28:53` 但在海外技术圈
`28:55` 他绝对是响当当的人物
`28:56` 很多大公司的底层工具都是他做的
`28:59` 在这篇博客里
`29:00` 他写了这么一段话
`29:02` 这段话的大意就是说
`29:03` 我也不知道业界有没有公认的叫法
`29:06` 我就姑且叫它是harness engineering
`29:09` 它的核心理念就是只要agent的犯了错
`29:12` 你就去改造系统
`29:13` 让他绝不再犯同样的错误
`29:15` 要是有更好的词
`29:16` 我随时改口
`29:18` 你看大佬呢还是很实诚的
`29:20` 所以呢这个词的起点其实非常朴素
`29:23` 甚至带着点随意
`29:24` 跟后来大家讨论的宏大概念还是有所区别的
`29:28` 从传播情况来看
`29:29` 这篇文章的讨论热度其实并不算很高
`29:32` 那harness engineering这个词到底是怎么火起来的呢
`29:36` 在很多人的认知里
`29:37` 真正引爆这个概念的是几天后
`29:40` 也就是2月11号
`29:41` OpenAI发的那篇harness engineering文章
`29:43` 就是我们之前讲过的那个
`29:45` 这个文章的信息量极大
`29:47` 迅速就在业界引起了巨大的反响
`29:50` 紧接着整个AI圈就像触发了连锁反应
`29:53` 仅仅六天后
`29:54` 也就是2月17号
`29:55` 软件工程界鼎鼎大名的martin baller
`29:58` 网站就发表了一篇文章
`29:59` 作者是thought works里面一个非常资深的工程师
`30:03` 文章的标题呢是叫做harness engineering
`30:05` First thoughts
`30:06` 讲的就是他读完了OPENAI那篇文章之后
`30:09` 的第一反应
`30:10` 作为顶级技术博客
`30:12` 这篇文章一发出来
`30:13` 自然就在圈内引发了广泛的讨论
`30:16` 但抛开技术观点不谈
`30:17` 他在文章里面还点出了一个很耐人寻味的细节
`30:21` 虽然OPENAI的这篇文章的标题
`30:23` 有harness engineering这个概念
`30:25` 但如果你仔细去翻OPENAI的文章
`30:27` 你会发现这篇文章的正文里面
`30:29` 其实只提了一次harness这个词
`30:31` 因此啊他就推测OpenAI
`30:33` 搞不好就是受到了mitchell hash model的启发
`30:36` 事后呢才临时把harness
`30:38` engineering这个词放到了标题里面
`30:40` 虽然这只是个猜测
`30:42` 但也给人带来了很大的联想空间
`30:44` 随后到3月10号的时候
`30:47` long unchain发表了一篇文章
`30:48` 叫做the anatomy of an agent
`30:50` Harness
`30:51` 这篇文章第一次明确给出了关于harness的公式
`30:55` 就是这个agents等于model加上harness
`30:58` 这个公式呢
`30:59` 其实就是我们前面所聊过的那个等式的变体
`31:02` 当时呢我们是把harness移到了等号的左边
`31:05` 我们讲的是harness等于agent解去model
`31:08` 这两个等式呢其实本质上就是一回事
`31:11` 公式一出
`31:12` 概念呢就算是定调了
`31:14` 随后在3月24号的时候
`31:16` ANTHROPIC发表了那篇harness的文章
`31:18` 拿出了planner
`31:20` generator和evaluator的经典架构
`31:22` 这个我们之前也讲过
`31:24` 虽然ANTHROPIC自己比较克制
`31:26` 通篇只用了harness这个名词
`31:28` 并没有生搬硬套
`31:29` harness engineering这个刚刚炒热的新词
`31:32` 但在当时那个氛围下
`31:33` 整个AI全可以说是心照不宣
`31:36` 直接就把这套三个agent的架构
`31:38` 当成了harness engineering的教科书级案例
`31:41` 就这样一传十
`31:42` 十传百
`31:43` harness engineering就从一个私人说法
`31:45` 变成了一个大家都在用的词
`31:47` 不过如果你复盘完这段历史
`31:51` 再仔细的琢磨一下
`31:53` 就会发现一件非常微妙的事情
`31:55` 那就是harness engineering里面所用到的所有技术
`31:59` 竟然没有一个是新的
`32:01` 你看我们前面所讲的LINTER代码检查任务
`32:04` 拆解规划
`32:05` 质量评估机制
`32:06` 这些东西其实早就有了
`32:08` 相信看这个视频的很多观众
`32:11` 甚至都在做相关的工作
`32:12` harness engineering真正做的呢
`32:14` 只不过是把这些技术重新组织了一下
`32:16` 统一放到了一个新词的下面
`32:18` 换句话说
`32:19` 它提供的是一套新的系统思维架构
`32:22` 而不是发明了一批颠覆性的新技术
`32:24` 既然没什么新技术
`32:26` 那难怪有些人会觉得
`32:28` harness engineering这个概念被高估了
`32:30` 甚至带着点炒作的成分
`32:32` 仔细听听这些人的想法
`32:34` 你会发现他们的攻击点呢主要是有两个
`32:37` 一方面
`32:38` 正像我们前面所聊过的
`32:40` Harness engineering
`32:41` 根本就没有什么新东西
`32:42` 全都是新瓶装旧酒
`32:44` 在这种情况下
`32:45` 特意造个新词到处宣传
`32:47` 这不就是噱头吗
`32:49` 不仅如此
`32:49` 这些人呢还提出了一个更扎心的观点
`32:53` 所有的harness engineering都是迟早要被淘汰的
`32:56` 他们认为
`32:56` 随着大模型自身能力的持续优化
`32:59` 今天看起来必不可少的这些harness设计
`33:02` 未来很有可能会被模型能力本身逐步吸收
`33:05` 最终变得不再需要
`33:06` 而这种担忧
`33:07` 其实连ANTHROPIC自己的文章里都有据可循
`33:10` 前面我们讲过anthropic harness核心方案
`33:13` 但文章里面还有很多细节值得细细品味
`33:16` 这里我们仔细研究一下其中的两个问题
`33:19` 然后分别看看ANTHROPIC
`33:21` 一开始用harness engineering是怎么解决的
`33:23` 以及后来模型强大了之后
`33:25` 是如何用模型来解决的
`33:27` 我们第一个要探讨的问题就是上下文焦虑
`33:30` 这个呢是sonnet4.5的一个问题
`33:32` 具体来说就是当上下文过长时
`33:34` 模型会急于结束任务
`33:36` 以更少的token完成交付
`33:38` 而这个呢往往会影响最终质量
`33:41` ANTHROPIC呢
`33:42` 一开始是使用了一种叫做上下文重置的
`33:44` harness engineering技术来解决这个问题
`33:46` 但后来当模型升级到更强的OPPO4.5之后
`33:50` 这种现象被大幅缓解
`33:52` 因为OPPO4.5没有明显的上下文焦虑问题了
`33:54` 也就不怎么需要这方面的harness设计
`33:57` 第二个相关的问题呢是长任务的执行效果差
`34:00` 这一点呢其实我们在上一个章节也提过
`34:03` 让我们一起来回忆一下ANTHROPIC的链路呢
`34:06` 包含了planner
`34:07` generator和evaluator3个agent
`34:10` 一开始的设计呢是逐个功能点执行
`34:12` 也就是说
`34:13` ANTHROPIC会在提示词里面强制generator
`34:15` 每次只选取一个功能点
`34:17` 做完一个再做下一个
`34:19` 以便确保整个产品开发流程稳步向前推进
`34:22` 不过等用到更强的OPPO4.6之后
`34:25` 这种强制分布执行的机制呢就不再需要了
`34:27` 因为OPPO4.6的全局统筹能力够强
`34:30` 它可以一次把所有的功能点都拿过来
`34:33` 自己决定先做哪个再做哪个
`34:35` 稳步向前推进
`34:36` 不再需要别人对他的执行流程指指点点了
`34:39` 你看这就说明了一个非常现实的趋势
`34:42` 模型越强
`34:43` 所需要的harness就越少
`34:44` 模型能力的提升
`34:46` 正在一点点吃掉harness engineering的生存空间
`34:49` 当然为了严谨起见
`34:50` 我需要补充一句
`34:51` Anthropic
`34:52` 官方在文章里面其实并没有这么悲观
`34:55` 他们认为随着模型变强
`34:57` harness的形态也会跟着进化
`34:59` 以便去解锁更复杂的任务
`35:01` 也就是说harness只会变形而不会消失
`35:04` 但我们不妨大胆推演一下
`35:06` 如果未来的模型真的强到离谱呢
`35:09` 这并不是说未来连读写文件
`35:11` 联网搜索这种基础的工具都不需要了
`35:14` 而是说
`35:14` 也许只要给大模型配置上最基础的harness
`35:17` 他自己就能够把剩下99%的问题
`35:20` 全部都给搞定
`35:22` 真到了那一天
`35:23` Harness engineering
`35:24` 可能就不再是一门
`35:25` 需要大家专门去钻研的技术了
`35:27` 它会退化成一个单纯的环境接口
`35:29` 一个底层的基础设施
`35:31` 仔细想想
`35:32` 这件事情发生的概率恐怕没有那么低吧
`35:35` 那说了这么多
`35:37` 我们还得回归原来的问题
`35:38` harness engineering到底是不是噱头
`35:41` 这里我来聊一下我个人的看法
`35:43` 可能有误
`35:44` 仅供参考啊
`35:45` 我的观点是
`35:46` harness engineering不是噱头
`35:48` 但应该也不是终局
`35:50` 说他不是噱头
`35:51` 是因为它已经实实在在的带来了效果
`35:54` 无论是OpenAI还是ATHROPIC
`35:55` 都通过harness engineering把agent的稳定性
`35:58` 自动化程度和生产力往前推了一大步
`36:01` 这些呢都是可以被验证的工程成果
`36:04` 而不是概念炒作
`36:05` 当然你也可以说这只不过是新瓶装旧酒嘛
`36:08` 用的都是一些老技术
`36:10` 但问题在于
`36:11` 工程领域真正的进步
`36:13` 往往不在于发明了什么新技术
`36:15` 而在于有没有一套统一的框架
`36:17` 把这些零散的能力组织起来
`36:19` 变成可以系统设计
`36:21` 可以持续优化的工程方法
`36:23` harness engineering的意义恰恰就在这里
`36:26` 但我不得不承认啊
`36:27` harness engineering大概率也不是终局
`36:30` 随着模型能力继续增强
`36:32` 今天这些用来约束模型
`36:33` 纠正模型
`36:34` 给模型兜底的系统设计方案
`36:36` 很有可能会被模型自身逐步吸收到
`36:39` 那个时候
`36:40` 很多harness可能会变得不再需要这个词呢
`36:43` 也许也会慢慢的淡出大家的视野
`36:46` 所以呢我更愿意把harness engineering
`36:49` 看成是一个过渡期的关键技术
`36:52` 它呢可能不是未来的终极答案
`36:54` 但它是当下最现实的答案
`36:57` 因为在今天
`36:58` 模型依然会犯错
`36:59` 依然会有幻觉
`37:00` 依然会在复杂的任务中偏离轨道
`37:03` 在这种现实下
`37:04` harness engineering的重要性就不容忽视
`37:07` 可以说谁能够把harness搭的更稳
`37:10` 谁就能够更早的把AI的能力
`37:12` 转换为真正的生产力
`37:14` 从而从中受益好了
`37:16` 今天的视频就到此结束了
`37:18` 我是马克
`37:19` 用最通俗的语言讲最硬核的技术
`37:21` 我们下次再见
`37:22` 拜拜
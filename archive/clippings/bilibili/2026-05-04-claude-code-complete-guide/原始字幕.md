---
title: "Claude Code 从 0 到 1 全攻略：MCP / SubAgent / Agent Skill / Hook / 图片 / 上下文处理/ 后台任务"
url: "https://www.bilibili.com/video/BV14rzQB9EJj/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV14rzQB9EJj"
cid: "35602042286"
author: "马克的技术工作坊"
upload_date: "2026-01-25"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115954889596221&bvid=BV14rzQB9EJj&cid=35602042286&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

时间戳如下，方便大家跳转观看：
 
第一部分：环境搭建与基础交互
- 01:09 安装 Claude Code
- 01:43 登录与授权
- 02:55 第一个实战问题
- 03:12 三种模式详解 (默认/自动/规划)
 
第二部分：复杂任务处理与终端控制
- 06:00 执行终端命令 (Bash)
- 06:49 使用规划模式 (Plan Mode)
- 11:06 跳过所有权限检测 (dangerously-skip-permissions)
- 13:53 后台任务管理 (Background Tasks)
 
第三部分：多模态与上下文管理
- 15:35 版本回滚 (Rewind)
- 19:42 图片处理
- 21:09 安装 MCP Server (以 Figma 为例)
- 21:39 恢复历史会话 (Resume)
- 23:06 使用 MCP 工具还原设计稿
- 24:49 上下文压缩与清除
- 26:30 项目记忆文件 (CLAUDE.md)
 
第四部分：高级功能扩展与定制
- 29:46 Hook
- 33:16 Agent Skill
- 36:00 SubAgent
- 39:17 Skill 与 SubAgent 的区别
- 40:38 Plugin

## 章节

- `00:00` 视频内容介绍
- `01:09` 安装并登录
- `02:50` 基础功能演示
- `06:18` 规划模式
- `10:04` 权限检测与后台任务管理
- `15:35` 图片, MCP 与上下文管理
- `29:46` Hook
- `33:16` Agent Skill
- `36:00` SubAgent
- `40:38` Plugin

## 字幕

### 视频内容介绍 `00:00`

`00:00` 最近cloud code非常火
`00:01` 相信关注编程agent的同学
`00:04` 应该都已经上手体验过了
`00:06` 不过要把这个工具从入门真正落地到生产环境
`00:10` 光会敲几个简单的命令是远远不够的
`00:13` 所以这期视频咱们不整虚的
`00:16` 直接带大家从头到尾
`00:17` 把cloud code的实战流程彻底走一遍
`00:20` 这个视频呢一共是分为以下四个部分
`00:23` 第一环境的搭建与基础交互
`00:26` 第二复杂任务处理与终端控制
`00:31` 最后呢是高级功能的扩展与定制
`00:34` 大家可以看到屏幕上密密麻麻的知识点和时
`00:37` 间戳
`00:38` 这期视频的含金量呢绝对是拉满的
`00:41` 只要你花点时间看完这期视频
`00:43` 我保证你能够彻底吃透cloud code
`00:46` 把它变成你手心里最顺手的生产力工具
`00:49` 另外我知道市场上还有其他类似的编程agent
`00:52` 比如codex
`00:53` open code等等
`00:55` 其实他们无论从功能上还是使用上
`00:57` 都跟cloud code没有什么太大区别
`00:59` 所以我相信在看完了这期视频之后
`01:02` 你一定会一通百通
`01:03` 同类的产品
`01:04` 基本上都能够直接上手
`01:06` 好话不多说
`01:07` 那我们直接开始

### 安装并登录 `01:09`

`01:09` 首先我们来到cloud code的官方网站
`01:12` 就是这个页面了
`01:14` 然后呢我们点击这里面的复制按钮
`01:17` 再回到终端粘贴
`01:19` 这样呢就开始安装cloud code了
`01:22` 安装完成后
`01:23` 我们试着用它来做一个代办软件
`01:26` 首先使用命令MKDIR来创建一个目录
`01:29` 就叫做my to do就好了
`01:31` 我们所有的代码呢都放在这个目录里面
`01:34` 然后我们进入到这个目录里
`01:36` 再执行cloud这个命令来打开cloud code
`01:39` 刚进来的时候
`01:40` cloud code可能会提示你进行登录
`01:43` 如果你像我这样没有被提醒的话
`01:45` 可以执行杠login命令来主动触发登录流程
`01:48` 呃
`01:49` 可以看出啊
`01:50` cloud code官方一共是提供了两种标准的接入方式
`01:53` 第一种呢是订阅制
`01:55` 如果你购买了cloud的pro或者是max会员
`01:58` 那就直接选这个就好了
`02:00` 第二个呢是使用官方的API key
`02:02` 按照token的用量计
`02:04` 费
`02:04` 用多少花多少
`02:05` 我呢是订阅用户
`02:07` 所以我选择第一项选择之后
`02:09` cloud code会弹出一个网页
`02:11` 提示我授权
`02:12` 我们同意
`02:14` 可以看出登录成功
`02:15` 我们关掉当前页面
`02:16` 回到终端这里按下回车登录呢就结束了
`02:20` 这里顺便提一下
`02:21` 有些同学可能没有办法使用cloud的官方订阅
`02:25` 或者是API
`02:26` 这个时候呢你也可以使用国产模型来驱动cloud code
`02:30` 比如说是g l m mini max等等
`02:33` cloud code是一个通用的编程agent
`02:35` 它本身其实并不跟cloud模型绑定
`02:38` 你完全可以使用其他的模型来驱动cloud code
`02:41` 具体使用国产模型的方法呢
`02:43` 这里就不再赘述了
`02:44` 其实很简单
`02:45` 设置几个环境变量就行了
`02:47` 网上一搜一大堆
`02:48` 有需要的同学可以自己搜索下好

### 基础功能演示 `02:50`

`02:51` 言归正传
`02:52` 我们再回到cloud code这里
`02:54` 开始使用它
`02:55` 前面的我们说过
`02:56` 我们要做的是一个代办软件
`02:58` 那我们现在就把这个需求告诉cloud code
`03:00` 让他帮我们实现
`03:01` 给我做一个代办软件
`03:03` 使用HTML实现
`03:04` 可以看到cloud code开始工作了
`03:07` 让我们稍作等待
`03:12` cloud code想要创建一个叫做index点HTML的文件
`03:16` 询问我们是否同意
`03:17` 这里面呢一共有三个选项
`03:19` 第一项yes是单词授权
`03:22` 意思就是说只同意创建当前的这么一个文件
`03:25` 如果他接下来还需要创建其他文件的话
`03:27` 他还会再次向我们询问确认
`03:30` 第二项呢是yes
`03:31` Allow all edits
`03:32` During this session
`03:33` 选中了它
`03:34` 就意味着在本次的对话期间
`03:36` 后续所有的文件操作都会自动通过
`03:39` 不会再反复打扰我们
`03:41` 第三项是不同意
`03:42` 选择了它之后
`03:43` 你可以继续输入你的想法
`03:45` cloud code会根据你的输入生成代码
`03:47` 并再次向你确认呃
`03:49` 为了演示方便
`03:50` 我们这里就选择第二项
`03:52` 开启自动模式
`03:53` 把后续的工作全权交给他好
`03:56` 选完之后注意看输入框下方多了一行字
`03:59` 就是这个accept edits on
`04:01` 这个呢就表示目前的自动同意模式已经开启了
`04:04` 那如果说你后悔了
`04:05` 想换一个模式怎么办呢
`04:07` 这个时候就要用到shift加tab键来切换模式了
`04:10` 我们来按一下试试看
`04:12` 现在变成了plan mode
`04:14` 也就是规划模式
`04:16` 这个模式主要用来探讨复杂的方案
`04:18` 只聊天不执行
`04:19` 具体用法呢我们后面再细讲
`04:21` 我们再按一次
`04:23` 注意看底部的play mode消失了
`04:26` 取而代之的是一行灰色的提示问号
`04:29` For shortcuts
`04:30` 也就是按问号显示快捷键
`04:32` 大家千万别误会
`04:33` 这个呢并不是什么快捷键模式
`04:36` 这行字呢只是cloud code的一个小提醒而已
`04:39` 跟当前的模式没有关系呃
`04:41` cloud code其实在这个时候没有标注当前的模式
`04:45` 而这种没有标注的模式就是默认模式
`04:48` 不知道你还有没有印象
`04:50` 我们刚进入cloud code的时候
`04:52` 用的就是默认模式
`04:53` 在默认模式下
`04:54` cloud code表现的最为谨慎
`04:56` 每次创建文件或者是修改文件的时候
`04:59` 他都会先去询问用户的意见
`05:01` 所以大家看出来了吗
`05:03` shift加tab就在这三种模式之间循环
`05:06` 让我们稍微总结一下
`05:08` 第一个是默认模式
`05:09` 也就是显示问号for shortcuts的那个模式
`05:12` 在这个模式下创建和修改文件之前
`05:15` 一定会询问用户最为稳妥
`05:17` 第二个呢是自动模式
`05:19` 也就是那个accept at it on
`05:21` 在这个模式下
`05:22` cloud code会自动创建或修改文件
`05:24` 不会去询问用户最为方便
`05:26` 第三个是规划模式
`05:28` 也就是那个plan moon
`05:30` 这个模式只讨论不修改文件
`05:32` 适合构思这个模式
`05:34` 我们后面会详细解释好
`05:36` 这个呢就是cloud code的三种模式了
`05:38` 我们再按一次shift加tab来到accept edits on模式
`05:42` 选择好模式之后
`05:43` 我们再回头看看之前cloud code给我们写好的文件
`05:46` 我们要打开它
`05:48` 那怎么打开呢
`05:49` 你可以去文件管理器里面找到这个文件
`05:52` 双击打开它
`05:53` 不过这里我想要教另外一个方法
`05:55` 我们可以直接在cloud code里面执行终端命令
`05:59` 来打开它
`06:00` 首先呢我们输入一个叹号
`06:02` 看cloud code有反应了
`06:03` 现在我们处在wash模式下
`06:05` 可以运行任意的终端命令了
`06:07` 紧接着我们来输入open index点HTML
`06:11` 来打开这个HTML文件代码
`06:14` 软件做的还算不错
`06:15` 一次成功
`06:16` 这不得不给cloud code点个赞

### 规划模式 `06:18`

`06:18` 不过这里面有个小坑
`06:20` 他把所有的代码都写到index点HTML里面了
`06:23` 小项目还好
`06:24` 要是项目做大了
`06:26` 维护起来简直是个灾难
`06:28` 所以咱们最好趁早是换成react
`06:30` typescript和white这种现代架构
`06:33` 把代码分模块管理
`06:34` 我们可以直接向cloud code提出这个请求
`06:37` 让他改掉
`06:38` 不过呢改架构是个大工程
`06:40` 最好是先确定细节再动手
`06:43` 这个呢就是play mode登场的时候了
`06:46` 它就是专门用来讨论方案
`06:48` 确定细节的
`06:49` 让我们先关掉当前页面
`06:51` 回到终端这里
`06:52` 然后按一下shift加tab进入到plan mode
`06:55` 然后呢输入我们的请求
`06:58` 将当前的代办应用重构为使用react加
`07:02` typescript加white的项目
`07:04` 呃
`07:05` 问题到这里还没有结束
`07:06` 这个时候呢我们想换行
`07:08` 怎么换行
`07:09` 敲回车吗
`07:10` 敲回车显然是不行的
`07:12` 敲回车的话
`07:13` 我们的问题就提交了
`07:14` 换行呢是需要按shift加回车
`07:17` 然后我们就可以继续写了
`07:19` 保留所有的现有功能呃
`07:22` 这里顺便提一句啊
`07:23` 如果你按shift加回车不好用的话
`07:26` 那你大概率用的cloud code版本比较旧
`07:28` 需要升级一下呃
`07:30` 另外还有一点
`07:31` 有些同学可能会觉得
`07:32` 这个终端的输入框实在是太难用了
`07:35` 想用一些比较现代化的编辑器来替代它
`07:38` 这个呢其实也是可以的
`07:39` 我们可以按一下CTRL加G
`07:42` 这个时候cloud code就会打开一个vs code的标签页
`07:46` 在这里面编辑就方便多了
`07:48` 回车随便按
`07:49` 也不用担心不小心提交问题了
`07:51` 当然这要求你先事先装好vs code
`07:54` 我们就在这里把要求补充完
`07:56` 再加一句
`07:57` 且UI风格保持一致
`08:00` 写完了之后我们保存
`08:02` 然后关掉这个标签页
`08:04` 此时cloud code就会把vs code里面的内容
`08:07` 全部放到输入框里面
`08:08` 我们直接再按个回车就可以提交请求了
`08:11` 可以看到cloud code开始工作了
`08:13` 让我们稍等一下
`08:18` 计划似乎是缠好了
`08:19` 我们把滚动条往上移一移
`08:21` 看一看他这个计划具体是个什么样子的
`08:24` 这个计划呢是从这里开始
`08:26` 看起来还是挺完善的
`08:28` 目标项目清单
`08:30` 目录结构之类的信息全部都有覆盖
`08:32` 到最后他询问我们是否要执行
`08:35` 他一共给了我们三个选项
`08:37` 第一个是执行计划
`08:39` 并且进入到同一模式
`08:40` 后续修改文件前就不再询问用户了
`08:43` 第二个也是执行计划
`08:45` 只不过后续会使用默认的模式
`08:48` 也就是说之后每次写入文件前都需要询问用户
`08:51` 第三项是继续修改计划
`08:54` 如果你对计划不满意的话
`08:55` 那可以在这里面继续输入cloud code
`08:58` 会根据你的要求再修改这个计划
`09:00` 产出一份新的
`09:01` 这里假设我们对计划不满意
`09:03` 选择第三项
`09:04` 提出我们的修改意见
`09:06` 给每个蛋白事项增加一个优先级
`09:09` 比如高中低
`09:11` 并且用不同的颜色标记出来
`09:13` 然后我们按一下回车
`09:15` 这个时候呢cloud code就开始修改他的计划了
`09:19` 让我们再稍微等待一下
`09:24` 好cloud code又产出了一份计划
`09:27` 具体内容呢我们就不看了
`09:28` 单从最后的测试部分
`09:30` 我们就可以看到
`09:31` 他确实是把我们的优先级的需求考虑进去了
`09:34` 这次差不多了
`09:35` 要不我们就同意吧
`09:36` 让我们选择第一项执行这个计划
`09:38` 并且进入到自动同意模式
`09:41` 也就是说后续修改文件的时候
`09:43` 就不要再询问我们了
`09:44` 好回车
`09:45` 可以看到当前模式切换到了accept edits on
`09:48` 没问题
`09:49` 这个呢跟我们的选择是一样的
`09:51` 后面写入文件的时候呢
`09:52` 便不会再麻烦我们了
`09:54` 现在cloud code开始执行计划了
`09:56` 时间估计会比较长
`09:58` 我们慢慢等待一下

### 权限检测与后台任务管理 `10:04`

`10:04` cloud code暂停了
`10:06` 他现在想用MKDAR来创建目录
`10:08` 然后询问我们是否同意
`10:11` 稍微等一下这个是什么情况
`10:14` 我们不是跟cloud code说过了吗
`10:17` 不需要每次都询问用户的
`10:18` 还记不记得我们是在accept editson这个模式下面
`10:22` 那怎么现在又开始询问了呢
`10:25` 对我们确实是说过
`10:27` 不过那只不过是写入文件的时候
`10:30` 不需要询问用户
`10:31` 这个呢是在执行终端命令
`10:34` cloud code认为
`10:35` 执行终端命令呢是一个比较危险的操作
`10:38` 所以需要征得用户同意才会继续
`10:41` 不仅如此
`10:42` 这里面还没有一个
`10:43` 自动执行所有终端命令的选项
`10:45` 即使是第二项
`10:46` 那也只不过是告诉cloud code
`10:49` 以后都可以自由的访问SRC目录
`10:51` 不需要询问用户
`10:52` 至于执行别的命令
`10:54` 那还是要问的
`10:55` 如果你觉得每次选择都太麻烦的话
`10:58` cloud code其实是提供了一个比较隐蔽的选项
`11:01` 可以跳过这个选择的步骤
`11:03` 让他想执行什么命令就执行什么命令
`11:06` 这个呢是需要在启动cloud的时候加上一个选项
`11:10` 叫做dangerously skie permissions
`11:12` 我来给大家新开一个终端标签页
`11:14` 演示一下
`11:15` 我们先进入到原来的这个my to do目录里面
`11:19` 然后我们来输入cloud
`11:22` 再加上dangerously cape permissions
`11:24` 意思就是跳过所有的权限检测
`11:27` 大家注意看这个参数里面的单词dangerously
`11:30` 也就是危险的
`11:32` 官方把危险两个字写在了脸上
`11:35` 意思非常明确
`11:36` 一旦加上了这个参数
`11:37` cloud code就彻底放飞自我了
`11:40` 进来之后你会发现模式变成了by pass
`11:43` Permissions on
`11:44` 这就意味着接下来他执行任何终端命令
`11:47` 都不会再征求你的意见了
`11:49` 无论是安装依赖还是删除文件还是创建目录
`11:53` 都不会再问了
`11:54` 这个呢其实是一把双刃剑
`11:57` 往好了说
`11:58` 它能够极大的提升开发效率
`12:00` 全自动干活
`12:01` 不用你一直盯着点同意
`12:03` 但是往坏了说
`12:04` 他理论上呢就拥有了和你一样的终端权限
`12:07` 虽然cloud code只有在极度发疯的情况下
`12:10` 才能去破坏你的电脑
`12:11` 这种概率呢可以说是微乎其微
`12:13` 但是作为一个负责任的博主
`12:15` 我必须要提醒大家
`12:17` 这个选项会让cloud code彻底的放飞自我
`12:20` 所以理论上呢还是有一定的危险性的
`12:22` 是否要为了效率承担这一丢丢的理论上的风险
`12:25` 决定权是在你们手里
`12:27` 好演示完毕
`12:29` 回到我们原的例子里
`12:31` 我们呢还是不用这个选项了
`12:33` 我们来选择第二项
`12:34` 只同意他以后可以自由的访问SRC目录回车
`12:37` 让cloud code继续
`12:46` cloud code询问我们能不能执行npm install命令
`12:49` 我们选择以后都同意
`12:54` 这里cloud code想要使用npm run dev来启动服务器
`12:58` 启动了服务器就可以查看网页的效果了
`13:00` 启动也行
`13:01` 不过呢这里我们先取消
`13:03` 待会儿我想用它来给你演示如何手动启动它
`13:06` 并且借这个机会来解释任务相关的一些概念
`13:09` cloud code看我们拒绝了
`13:11` 在询问我们应该要做什么
`13:13` 我们来跟他说一下这个命令呢
`13:15` 等会我自己执行
`13:16` 你确保其他部分都完成了就可以了
`13:18` 然后呢cloud code就开始确认了
`13:21` 好确认完毕
`13:23` 看起来一切正常
`13:24` 现在我们就可以自己来运行这个命令了
`13:27` 我们来试一下
`13:31` 服务器启动成功
`13:33` 我们来点击这里面的链接
`13:34` 看一下效果怎么样
`13:35` 不错效果还可以
`13:38` 我们来随便点点
`13:39` 看起来没有什么问题
`13:41` 增加个待办事项也是可以的
`13:44` 然后呢我们可以再增加一个其他的待办事项
`13:47` 调一下优先级
`13:49` 再添加一切完美
`13:50` 我们再回到cloud code这里
`13:52` 这里有一点需要给大家强调下
`13:55` 这个服务的运行呢是会阻塞cloud code的
`13:58` 比如说我们在这里输入一个hi
`14:00` 你看cloud code没有给我们任何回应
`14:03` 那是因为服务还在运行
`14:05` cloud code就没有办法处理这个新的请求
`14:08` 那怎么办呢
`14:09` 很简单
`14:10` 看这里按CTRL加B可以把这个服务放置在后台
`14:14` 我们按一下试试
`14:15` 好像是起作用了
`14:17` cloud code开始处理我们的请求了
`14:19` 他给了我们一个回复
`14:21` 而且注意这里有一个后台任务正在运行
`14:25` 我们输入杠tasks就可以查看这个任务
`14:28` 在这里面可以看出
`14:29` 这确实是我们所启动的那个npm run dev的命令
`14:33` 注意这行提示按K可以关掉这个服务
`14:36` 不过我们目前还不打算关掉它
`14:39` 我们按ESC回到原来的那个界面里面
`14:42` 就让这个服务先一直跑着吧
`14:44` 这样的话呢
`14:45` 我们后续的修改也能够实时看到效果
`14:48` 那现在假设我们想加一个切换语言的功能
`14:51` 目前使用的是中文
`14:53` 我们希望它能够在右上角切换为英文
`14:56` 让我们来输入请求
`14:57` 在页面右上角增加一个切换语言的选项
`15:00` 用户可以选择中文或者是英文
`15:02` 默认为中文回车
`15:05` cloud code开始运作了
`15:06` 让我们稍作等待
`15:15` 好可以看到cloud code改完了
`15:17` 我们回到页面这里看看
`15:19` 效果不错
`15:21` 确实是加上了切换语言的选项
`15:23` 而且切换的效果呢也是符合预期的
`15:26` 不过你转念一想不对
`15:28` 我的用户都能看懂中文啊
`15:30` 我加这个功能干什么呢
`15:32` 要不就回滚吧好吧

### 图片, MCP 与上下文管理 `15:35`

`15:35` 那cloud code能回滚吗
`15:37` 当然是可以的
`15:39` 对应的命令呢就是杠rewind
`15:42` 或者是说呢有个更简单的办法
`15:44` 你可以直接按两下ESC
`15:46` 这样呢就进入到了回滚页面
`15:48` 我们每次输入请求的时候
`15:50` cloud code都会创建一个回滚点
`15:52` 比如说我们不是想回滚到增加语言选项
`15:55` 之前的那个版本吗
`15:57` 那就选择这个回归点就好了
`15:59` 选好之后按下回车
`16:01` 然后呢cloud code会给我们四个选项
`16:04` 是回滚代码和绘画
`16:06` 还是说是指回滚绘画还是指回滚代码
`16:10` 或者说呢我们就放弃回滚
`16:13` 我们来选择第一个代码和绘画都回滚好
`16:17` 现在回滚成功了
`16:18` 让我们来验证下打开页面没问题
`16:21` 确实是回滚成功了
`16:23` 没有那个切换语言的选项了
`16:25` 是不是很棒呢
`16:26` 好假设这个时候啊
`16:28` 你觉得react加typescript加white
`16:31` 这套架构好像是有点过于复杂了
`16:33` 你在想
`16:34` 要不干脆我们就回滚到只有index点
`16:37` HTML的那个版本就好了
`16:39` 好
`16:39` 那继续用回滚功能就行了
`16:41` 不过呢在这之前
`16:43` 我们最好把npm run dev这个后台任务给关掉
`16:46` 毕竟回滚之后相关的文件都没了
`16:48` 这个后台任务呢也就没有什么用了
`16:51` 我们回到cloud code这里
`16:53` 输入杠tasks来查看后台任务
`16:58` 然后再按K结束掉当前的这个后台服务
`17:04` 呃这个时候cloud code提示我们开发服务器运转正常
`17:07` 这个呢纯属是cloud code
`17:09` 晕了啊
`17:10` 大家忽略它
`17:11` 我们的开发服务器现在实际上已经被关掉了
`17:14` 服务器关掉之后
`17:15` 我们就可以开始回滚流程了
`17:17` 先按两下ESC
`17:19` 然后选择一开始重构代码的那个回滚点
`17:22` 然后呢我们再选择第一项
`17:24` 恢复代码和绘画好
`17:27` 看起来呢是已经回滚完成了
`17:30` 我们来看看当前目录下
`17:31` 是不是只有index点HTML这个文件
`17:33` 我们使用ls s这个命令
`17:36` 这个命令呢可以用来列举当前目录下的文件
`17:39` 列表运行
`17:40` 看结果好像是不太对啊
`17:43` 除了index点HTML文件
`17:45` 这个目录下还有很多其他的文件
`17:48` 它默认只显示了一部分
`17:49` 我们可以按一下CTRL来显示所有的文件列表
`17:54` 看起来总的文件数量还不少呢
`17:57` 这个是怎么回事呢
`17:59` 难道是cloud code出bug了吗
`18:02` 其实不是
`18:03` 这些文件呢是之前用终端命令创建的
`18:06` 比如说是MKDIR呀
`18:09` npm install之类的
`18:11` cloud code呢只能回滚他自己写入的那些文件
`18:14` 至于由终端命令生成的文件
`18:17` cloud code是没有办法回滚的
`18:19` 所以呢我建议大家还是不要太依赖cloud code
`18:22` 这个回滚功能了
`18:23` 如果要精准回滚的话
`18:25` 大家还是使用git会更好一点好
`18:28` 不过呢问题其实不大
`18:30` index点HTML呢是cloud code自己把控的
`18:33` 所以呢这个文件一定是回滚成功了
`18:36` 我们把别的文件都删掉就行了
`18:38` 说干就干
`18:39` 让我们打开文件管理器
`18:41` 删掉除了index点HTML之外别的文件
`18:43` 然后呢再回到clck code这里执行一下IOS命令
`18:47` 可以看到文件确实只剩一个了
`18:50` 然后这个时候呢我们可以使用open命令
`18:53` 打开这个HTML来验证一下它的效果
`18:56` 没问题
`18:57` 跟我们之前的那个index点HTML的效果呢是一样的
`19:01` 到这里回滚才算是彻底结束了
`19:04` 好回滚呢
`19:05` 我们就讲到这里
`19:06` 现在假设你对cloud code做的页面一直都不太满意
`19:10` 所以呢你去FIGMA上面自己画了一个界面
`19:14` 就大概是这个样子的了
`19:15` 你希望cloud code仿照这个界面来做
`19:19` 那具体该怎么实现呢
`19:21` 很简单
`19:21` 我们只要把这个设计稿图片传给cloud code就可以了
`19:26` 首先我们需要在FIGMA上面操作下
`19:29` 把当前的这个设计稿导出为一个PNG图片
`19:33` 导出的方法很简单
`19:35` 就按这个export frame就可以了
`19:38` 然后呢我们回到访达这里
`19:40` 可以看到图片导出的非常成功
`19:42` 下面我们的任务呢就是把这个图片传给cloud code
`19:46` 那怎么做到这一点呢
`19:48` 有两个方法
`19:49` 其中第一个方法就是直接把这个图片拖到cloud code
`19:53` 这里
`19:53` 看到这里面的image2了吗
`19:55` 这就代表cloud code已经接收到我们的图片了
`19:58` 这个呢只是其中的一个方法
`20:00` 还有另外一个方法呢就是复制这个文件
`20:06` 然后来到卡拉扣这里
`20:07` 按CTRL加V粘贴
`20:10` 注意啊
`20:11` 这里面我说的这个快捷键呢是CTRL加V
`20:14` 不是command加V
`20:16` 即使你用的是Mac os
`20:18` 你也要用control加V来粘贴这个图片
`20:21` 按command加V是不起作用的
`20:23` 这一点要记住了
`20:25` 这样呢我们的图片就粘贴上去了
`20:27` 紧接着呢我们就可以继续输入请求
`20:30` 让cloud code根据图片来修改代码
`20:33` 具体的过程我就不演示了
`20:34` 这个方法肯定行得通
`20:36` 不过说实话
`20:37` 很多时候呢可能还原的并没有那么精确
`20:40` 比如说字体啊
`20:41` 间距啊之类的
`20:43` cloud code很难通过图片做到非常精确的把握
`20:46` 所以这个时候呢我们其实还有另外一个方法
`20:49` 一个更为精确有效的方法
`20:51` 那就是使用MCP来实现
`20:53` 这个还原菲格玛设计稿的需求
`20:56` MCP是大模型与外界沟通的渠道
`20:59` 我之前讲过MCP的使用方法和相关原理
`21:02` 感兴趣的同学可以自己看一下
`21:04` FIGMA提供了一个很好用的MCP server
`21:07` 我们可以接入进来用用
`21:09` 首先呢我们是需要安装这个m CV server
`21:12` 根据FIGMA官方的要求
`21:14` 我们需要执行这一行命令
`21:16` 因此呢我们先把它复制一下
`21:20` 然后回到cloud code这里
`21:22` 先按两下CTRLC退出
`21:24` 然后执行这行命令
`21:27` 可以看到mc p server已经是安装成功了之后呢
`21:31` 我们需要重新打开cloud code
`21:34` 不过好像之前的对话全都没了呀
`21:37` 这可怎么办呢
`21:38` 别担心
`21:39` 我们可以使用杠resume命令来回到之前的对话
`21:44` 这里面的第一个就是我们刚才的那个对话了
`21:47` 我们按回车来选择它
`21:48` 你看这个对话不就回来了吗
`21:51` 呃另外啊还有一种更为简单的办法
`21:54` 那就是在启动cloud code的时候呢
`21:57` 加上一个参数
`21:58` 我们来试一下
`21:59` 首先退出cloud code
`22:01` 然后呢我们执行命令cloud空格杠C
`22:04` 这里面的C呢就是continue的缩写
`22:07` 它的功能就是打开cloud code
`22:09` 并自动恢复上一次的对话
`22:11` 好对话恢复了
`22:13` 我们执行杠MCP命令
`22:15` 来查看目前所安装的MCP工具
`22:18` 目前呢只有一个就是我们刚刚安装的FIGMA
`22:21` 可以看到我们需要健全才能够使用这个工具
`22:25` 我们来选择这个MCP工具
`22:27` 然后呢再选择authenticate
`22:29` 这个时候呢会自动弹出一个页面
`22:32` 让我们授权我们同意
`22:36` 然后再回到cloud code这里执行杠MCP
`22:40` 选择fig a
`22:41` 这个时候呢可以看到MCP server呢
`22:44` 就是一个可用的状态了
`22:45` 我们选择view tools
`22:47` 就可以看到这个MCP server内部所包含的工具列表
`22:50` 其中有用来截图的
`22:52` 有创建设计规则的等等
`22:54` 具体呢我们就不看了
`22:56` 我们其实也不用太关心
`22:58` 到底该使用哪个工具来完成我们的需求
`23:00` 我们让cloud code来判断
`23:02` 所以呢我们按ESC退出这个界面
`23:06` 然后输入我们的需求
`23:08` 修改当前的页面
`23:10` 使它与FIGMA稿件保持一致
`23:12` 呃问题还没完
`23:14` 我们现在回到FIGMA页面这里
`23:16` 复制这个设计稿的链接
`23:24` 就点击这里面的copy link to selection就好了
`23:27` 然后呢再回到cloud code这里粘贴再回车
`23:32` 这样呢应该就可以了
`23:34` 可以看出cloud code开始工作了
`23:37` 他首先呢是发现了我们的FIGUMMCP
`23:40` 可以解决这个问题
`23:42` 请求调用get design context这个MCP工具来实现需求
`23:47` 我们同意
`23:49` 然后呢cloud code请求调用gas screenshot工具
`23:53` 获取对应设计稿的截图
`23:55` 我们也同意
`23:57` 现在调完两工具之后呢
`23:59` cloud code就获取到了全部的设计稿信息了
`24:02` 其中不仅包括设计稿的截图
`24:04` 还有各种组件的间距
`24:05` 字体样式等
`24:06` 非常的详细
`24:07` 拿到这些信息后
`24:09` cloud code就开始紧锣密鼓的修改现有的HTML代码
`24:13` 使他与菲格玛设计稿相同
`24:15` 让我们稍作等待
`24:19` 好看起来是完成了
`24:20` 我们来到浏览器那边看一下效果怎么样
`24:24` 这个呢就是cloud code根据FIGMA设计稿所搞出的页面
`24:28` 我把原始的设计稿也放在这里
`24:31` 大家可以比较一下
`24:32` 看看效果怎么样
`24:33` 反正我觉得还原程度还是挺高的
`24:36` 当然这个页面还有一些细节需要打磨
`24:38` 比如说里面的undefined的NAN之类的
`24:41` 可能需要修改一下
`24:43` 但整体效果我觉得真的还是可以了
`24:45` 好那MCP呢我们暂时就讲到这里
`24:49` 下面我们来看一下上下文
`24:51` 压缩在之前我们写了很多的代码
`24:54` 然后cloud code呢也调用了很多的工具
`24:57` 相信这个时候呢
`24:59` cloud code的上下文里面就有了非常多的信息
`25:01` 这里面有一些是有用的
`25:03` 有一些其实没什么太大用处
`25:06` 我们可以根据需要对上下文做一些压缩
`25:09` 这里需要用到的命令是杠compact
`25:12` 我们可以直接去执行这个命令
`25:14` 也可以选择性的在它后面
`25:15` 追加一些具体的压缩策略
`25:17` 比如说是重点保留用户提出的需求之类的
`25:22` 呃不过我们就不在后面加需求了
`25:25` 我们就直接执行这个杠compact命令
`25:28` 看一下它的效果怎么样
`25:33` 压缩完成了
`25:34` 我们按一下CTRL加O
`25:35` 就可以看到压缩后的上下文内容
`25:38` 这个呢就是压缩之后的结果了
`25:41` 呃我们之前呢在上下文里面有很多的信息
`25:44` 有代码
`25:45` 有MCP的调用结果之类的
`25:47` 现在呢全部的内容就只剩这么一点了
`25:51` 这样的话呢不仅cloud code的性能有了保障
`25:55` 后面在执行任务时
`25:56` token的消耗量也会少很多
`25:58` 好现在我们按一下CTRL加O
`26:00` 再回到原来的这个界面里
`26:02` 这里再提一下关于上下文的另外一个命令
`26:05` 就是这个GUCLEAR
`26:07` 他呢做得更为极端
`26:09` 就会直接把所有的上下文内容都给清空掉
`26:12` 一般来说
`26:13` 如果我们后面的任务
`26:15` 跟之前的上下文并没有什么关联的话
`26:17` 我们就可以使用这个clear命令
`26:19` 来清空所有的上下文内容
`26:22` 这个命令我们就不演示了
`26:23` 毕竟我们还需要之前的上下文
`26:25` 演示了之后
`26:26` 那就什么都没了
`26:28` 我们还是保留这个压缩后的结果好
`26:30` 现在压缩完成了
`26:31` 但是压缩结果的可控性并没有那么强
`26:34` 比如假设你想手动改改这里的压缩结果
`26:38` cloud code可并没有给你提供这个选项
`26:40` 另外无论压不压缩上下文呢
`26:43` 都跟某个会话绑定
`26:44` 我们下次进入到cloud code的时候
`26:47` 还必须要来到这个会话
`26:48` 否则cloud code是不知道之前发生了什么的
`26:51` 那有没有什么办法可以解决这些问题呢
`26:54` 有没有一种方案可以让cloud code每次进来的时候
`26:57` 都读取一些我们自己设定的一些信息
`27:00` 这样cloud code就知道这是一个什么项目
`27:02` 用户有什么需求
`27:04` 我们甚至可以把各种注意事项都写在这里面
`27:06` 了解了这些信息之后
`27:08` cloud code就可以更好地为我们工作了
`27:10` 有这种方案吗
`27:11` 当然是有的
`27:13` 这个呢就是cloud点MD
`27:15` 我们来尝试使用一下
`27:17` 我们首先让cloud code自己生成一份cloud点MD文件
`27:20` 用的是杠INIT命令
`27:26` 好cloud code创建完毕了
`27:28` 我们来打开cloud点MD文件
`27:30` 看一下
`27:30` 它就放在当前目录里面
`27:34` 看起来内容是有模有样的
`27:37` 不过很可惜他的语言呢是英文
`27:40` 看起来不太方便
`27:41` 我们要不让cloud code再把它给转成中文
`27:50` 转化完毕我们再回来看一下
`27:52` 没问题
`27:53` 确实是中文了
`27:54` 另外提一下
`27:55` 这里面的内容呢是可以随便修改的
`27:58` 比如说我们可以在最后面加上一句注意事项
`28:03` 每次回答的最后必须要追加这么一句happy coding
`28:08` 然后我们回到cloud code这里先退出
`28:11` 然后再重新进入
`28:14` 这样cloud code就会重新加载
`28:16` 我们那份最新的cloud点MD文件
`28:18` 我们来随便给cloud code说一句
`28:21` 比如说是hi
`28:26` cloud code回答了他
`28:28` 最后呢确实是加上了happy coding
`28:30` 可以看到我们的cloud点MD真的是起作用了
`28:34` 所以呢
`28:35` 如果你有什么东西是希望cloud code每次都读取的
`28:38` 那就直接放到cloud点MD文件里就好了
`28:41` 试验完毕
`28:42` 现在我们把cloud点MD结尾中的那个注意事项去掉
`28:46` 要不每次都出现happy coding会影响我们后续的演示
`28:49` 我们可以直接找到vs code编辑cloud md文件
`28:53` 不过这里嘛
`28:54` 我想顺便教大家另外一个打开cloud md的方法
`28:58` 我们在这个输入框里面输入杠memory
`29:02` 在这里可以看出cloud点MD文件呢一共是有两种
`29:05` 一种是项目级别的对应的文件
`29:08` 就放在当前的目录里
`29:09` 对当前项目生效
`29:11` 第二个呢是用户级别对应的文件
`29:13` 放在用户目录里
`29:15` 对当前用户生效
`29:16` 我们之前用的是第一个
`29:17` 所以选择第一项
`29:19` 选择好了之后
`29:20` 对应的cloud md文件就自动打开了
`29:22` 这样呢
`29:23` 就不用每次都自己在文件管理器里面找了
`29:26` 会稍微方便一点
`29:27` 打开cloud md文件之后
`29:29` 我们删掉最后面的注意事项
`29:31` 保存
`29:32` 再回到cloud code这里
`29:34` 重启一下
`29:37` 然后再随便问一句
`29:40` 可以看到happy coding已经没了
`29:42` 这说明我们的修改已经生效了
`29:44` 那cloud点MD文件就讲到这里

### Hook `29:46`

`29:47` cloud code还有个hook的功能
`29:48` 允许用户在运行工具前后
`29:50` 等时机执行一段自己指定的逻辑
`29:53` 比如说我们可以用它来做自动格式化
`29:55` 也就是说在cloud code写完代码之后
`29:57` 自动执行我们设定的格式化函数
`29:59` 以便让最终的代码更加美观
`30:01` 更加符合我们的需求
`30:02` 首先我们执行杠hooks命令
`30:05` 进入到hook的配置页面
`30:06` 这里我们可以配置hook的执行时机
`30:09` 比如说是工具使用前
`30:10` 工具使用后工具使用失败
`30:12` 发送通知等等
`30:14` 我们来选择第二项post to use
`30:16` 也就是工具使用后来执行这个hook
`30:19` 然后呢我们再选择ADD new mater
`30:22` 这里面呢我们需要选择对应的工具
`30:24` 也就是说我们希望在哪个工具执行之后再运行
`30:28` 我们的hook逻辑
`30:29` 我们填写的是write或者是edit
`30:32` 也就是说在创建或者是编辑文件的时候
`30:35` 来执行这个hook
`30:37` 然后呢我们再选择ADD new hook
`30:40` 这里输入我们具体的格式化命令
`30:44` 这个命令看起来很长
`30:46` 我们来仔细分析一下
`30:47` 首先在运行的时候
`30:48` cloud code会给我们传这么一份JASON过来
`30:51` 其中的BPATH就是cloud code刚刚编辑好的文件路径
`30:55` 因此我们需要解析这个JSON结构
`30:57` 把其中的file past的值给取出来
`31:00` 我们刚才命令里面的这一部分
`31:02` 就是用来干这个活的
`31:04` 其中JQ是解析JSON的一个程序
`31:06` 不熟悉的同学可以自己查下
`31:09` 获取到文件路径之后
`31:10` 我们把这个文件路径
`31:11` 通过XARGS传递给premier命令
`31:14` 然后剩下的工作呢
`31:15` 就是只用premier来格式化这个文件的内容了
`31:18` 所以总结下来
`31:20` 这段命令其实就是使用JQ来获得
`31:22` 当前编辑好的文件路径
`31:24` 然后再使用premier来格式化这个文件
`31:27` 好讲完了
`31:28` 让我们再回到cloud code这里
`31:30` 写好代码之后
`31:31` 我们按回车确认
`31:33` 此时cloud code会询问我们应该把这个hook保存在哪
`31:36` 一级
`31:37` 一共是有三个选项
`31:38` 第一个呢是本地的项目级别
`31:41` 也就是说这个hook只会在本机本项目生效
`31:46` 选择这个选项之后
`31:47` cloud code会把配置放在项目目录里面的
`31:50` settings点local点JASON里
`31:51` 然后呢还会把settings点local点JASON加入到get的点
`31:55` get it ignore文件里面
`31:56` 所以呢这个文件不会共享给别人
`31:59` 第二个呢是项目级别
`32:01` 也就是说所有使用这个项目的用户呢
`32:04` 都能够用到这个hook
`32:06` 它对应的配置文件呢是settings点JASON
`32:09` 这个文件呢会随着git分发给所有人
`32:12` 第三个呢是用户级别
`32:14` 对当前的用户生效
`32:15` 对应的配置保存在用户的目录里面
`32:17` 每一个用户都有一份
`32:19` 不会互相影响
`32:20` 也不会跟着项目保定
`32:22` 我们来选择第二个
`32:23` 所有使用这个项目的人呢都能够用到这个hook
`32:26` 然后呢这个hook就算是创建好了
`32:29` 我们按ESC退出
`32:31` 最后呢输入请求来试一下
`32:33` 我们的请求是创建一个新的文件
`32:36` test点HTML里面随便写点HTML就行
`32:39` 所有的内容都写在一行里面回车
`32:42` 我们来稍微等一下
`32:44` 通过这个写入文件的请求就可以看出
`32:47` cloud code确实是把所有的内容都写入到一行里面了
`32:50` 我们同意执行完毕
`32:53` 我们来看看最终生成的test点HTML的文件内容
`32:59` 可以看到这个文件的内容呢已经被格式化好了
`33:02` 并不是像一开始cloud code写入的那样
`33:04` 只有一行
`33:05` 这说明我们刚才写的那个hook生效了
`33:07` 在cloud code写入完代码之后
`33:09` 我们的hook启动把那个文件给格式化了
`33:12` 所以呢我们现在看到的就是格式
`33:14` 非常漂亮的HTML代码

### Agent Skill `33:16`

`33:16` hook的功能呢就讲到这里
`33:18` 现在假设你每天都想写一个总结
`33:20` 记录下今天开发了哪些功能
`33:22` 而且呢这个总结必须要遵循一定的格式
`33:25` 比如一定要包含日期开发摘要
`33:28` 开发详情之类的
`33:29` 你可以把对应的格式要求
`33:30` 直接粘贴在这个输入框里面
`33:32` 让cloud code帮你写一份
`33:34` 只不过这样的话你每天都要重复粘贴一遍
`33:37` 很麻烦
`33:38` 这种事情其实非常适合使用agent skill来解决
`33:41` 我之前出过一个系统性讲agent skill的视频
`33:44` 有兴趣的同学可以看一下
`33:46` 不过没看过也没关系
`33:48` 你可以大致把它理解为
`33:49` 一个给大模型看的说明书
`33:51` 一个动态加载的prompt
`33:53` 我们来创建一个agent skill试一下
`33:56` 首先我们新开一个终端tab
`33:58` 使用MKDAR命令
`34:00` 在用户目录下的their cloud skills
`34:02` 文件夹下面创建一个新的文件夹
`34:05` 就叫做dai reports
`34:06` 我们使用vs code来打开这个文件夹
`34:13` 然后呢在这个文件夹下面创建一个叫做skill
`34:16` 点MD的文件
`34:17` 在这里面填入这样的一些内容
`34:22` 这个文件呢一共分为两部分
`34:24` 前面的name和description
`34:25` 分别代表这个agent skill的名称和描述
`34:28` Cloud code
`34:29` 会根据这一部分的内容
`34:30` 来决定是否要使用这个agent skill
`34:32` 后面呢就是这个agent skill的具体描述了
`34:35` 这里主要是写了日报需要遵循的格式
`34:38` 写好了之后
`34:39` 我们回到终端
`34:40` 关掉这个新开的标签页
`34:42` 然后呢再重启一下这个cloud code
`34:49` 然后输入杠skills
`34:51` 可以看到cloud code已经发现了我们的agent skill
`34:55` 然后呢我们回到输入框这里面打入我们的请求
`34:58` 写一份每日总结
`35:00` 回车开始执行
`35:04` 可以看出cloud code发现了这个请求
`35:07` 与我们刚才录的那个agent skill相关请求
`35:10` 使用这个agent skill
`35:11` 我们同意
`35:16` 然后呢我们的每日总结就写好了
`35:19` 跟我们要求的格式是一模一样的
`35:21` 这个呢就是agency go的使用方法了
`35:23` 是不是很简单呃
`35:25` 另外啊
`35:26` 这个agent skill的调用请求呢
`35:27` 是由大模型发现并且发起的
`35:30` 除此之外呢
`35:31` 我们也可以这样来主动的发起这个agent skill
`35:34` 就是先输入杠daily reports
`35:37` 然后呢后面加上具体的请求
`35:40` 这个跟之前的效果呢其实是一样的
`35:42` 只不过是省去了大模型意图识别的过程
`35:45` 直接由用户调用了这个agent skill
`35:47` 结果更加可控一些
`35:49` 具体我们就不演示了
`35:51` 我们来把它给删掉呃
`35:53` agent skill呢就大致讲到这里了
`35:55` 当然agent skill还有很多高级的用法
`35:57` 感兴趣的同学可以看下我的上一个视频

### SubAgent `36:00`

`36:00` 下面我们再讲讲cloud code的另外一个重要功能
`36:04` Sub agent
`36:05` 这个呢其实就是一个独立的agent
`36:07` 有着自己独立的上下文
`36:09` 独立的工具
`36:09` 独立的skill可以独立完成某一件事情
`36:12` 我之前讲过agent的原理
`36:15` 有感兴趣的同学呢可以自己来看一下呃
`36:19` 下面呢让我们来创建一个用于代码审核的
`36:22` Sub agents
`36:23` 首先输入命令杠agent
`36:25` 然后选择create new agents
`36:28` 这里面要选择agent的类型
`36:30` 是项目级别还是用户级别
`36:33` 我们来选择项目级别
`36:35` 也就是说使用这个项目的人呢都能用
`36:37` 接下来选择agent的创建方法
`36:40` 一种是用cloud code初始化
`36:42` 另外一种呢是完全手动创建
`36:45` 第一种是推荐方法
`36:46` 所以我们选它
`36:47` 接下来我们描述一下这个agent要做的事情
`36:51` 我们填入以下内容
`36:52` 这是一个用于代码审核的sub agents
`36:55` 在用户要求代码审核的时候
`36:56` 调用它回车
`36:58` 可以看到cloud code正在生成这个sub agent
`37:01` 让我们稍等一下
`37:03` 接下来呢我们选择这个sub agent能用的工具
`37:06` 我们选择read only tools就好
`37:09` 也就是说只能够使用只读工具
`37:11` 其余的都去掉
`37:12` 模型呢就选择默认的sonnet
`37:15` 接下来选择这个sub agent的颜色
`37:17` cloud code在运行这个sub agent的时候
`37:20` 会使用我们选择的颜色来展示它
`37:23` 我们用绿色吧
`37:26` 呃然后呢cloud code就会给我们生成这个sub agent
`37:32` 可以看到这个sub agent的描述呢是英文的
`37:35` 而且里面的内容大概率也不会跟我们期望的
`37:38` 完全相同
`37:39` 我们按一下E来编辑一下这个sub a gt的描述
`37:43` 这个呢就是cloud code给我们生成的sub agent描述了
`37:47` 不过呢它跟我们想要的那个版本差距有点大
`37:51` 所以这里呢我来给这个sub agent的整体替换一下
`37:55` 换成适合我们这个场景的
`37:57` sub agent的结构与agent skill类似
`38:00` 一共是分为两部分
`38:02` 上面呢是原数据写明了这个sub agent的名称
`38:05` 描述
`38:06` 所使用的模型颜色等等
`38:08` 下面呢就是这个sub agent具体要干的事情了
`38:11` 呃我的要求呢有两条
`38:13` 一个是审查的准则里面有两项
`38:17` 一项是针对JS的
`38:19` 一项是针对CSS的
`38:20` 最后呢会有一个输出格式方面的一个要求啊
`38:24` 我们后面就看一下cloud code
`38:26` 能否遵循这个sub agent的规范额
`38:29` 填完了这个sub agent的描述之后呢
`38:32` 我们来到cloud code这里给它重启一下
`38:41` 重启完后
`38:42` 我们提交请求给我做一下代码审核
`38:49` 可以看出cloud code调用了我们刚才创建的sub agent
`38:52` 并把对应的任务描述传给了他
`38:54` 让他处理
`38:55` 而且看这里cloud code是用绿色来表示这个sub agent
`38:59` 这跟我们之前的配置也是相符的
`39:01` 他估计还要再运行一会儿
`39:03` 让我们稍作等待
`39:06` 它运行了一会儿之后给出的代码审核报告
`39:08` 可以看出
`39:09` 他检查的内容
`39:10` 确实是我们在sub agent描述文件里面要求的
`39:14` 这个呢就是sub bent的使用方法了
`39:17` 有人可能会问
`39:18` agent skill跟sub agent很像啊
`39:21` 他俩什么区别
`39:22` 其实吧
`39:23` 他俩最大的区别就在于
`39:24` 对上下文的处理方式不同
`39:27` agent skill运行的时候
`39:28` 它会完全继承并且共享你当前主对话的上下文
`39:32` 这就意味着它执行过程中的每一行日志
`39:35` 每一个思考过程都会进入到你的当前上下文
`39:38` 想象一下
`39:39` 如果你让skill去审核一个
`39:41` 有着几万行代码的项目
`39:43` 这些项目会逐步塞满你的上下文窗口
`39:45` token消耗飙升
`39:47` agent也会因为记忆过载而变慢变傻
`39:50` 所以呢
`39:51` agent skill最适合处理那些与上下文关联比较大
`39:54` 而且对上下文影响不大的任务
`39:56` 比如说是根据今天的开发过程
`39:59` 写一个每日总结之类的
`40:00` 而sub agent呢则拥有自己完全独立的上下文
`40:04` 当你启动它时
`40:05` 它会开辟一个全新的对话窗口
`40:07` 它在这个窗口里面看的所有的代码生成的
`40:10` 所有的中间分析过程
`40:11` 都不会回传到你的主对话里面
`40:14` 只有当他把活干完了
`40:15` 他才会拿着一个最终的执行结果来向你汇报
`40:19` 这样一来
`40:20` 你的主对话依然干干净净
`40:22` 永远不会被琐碎的中间过程所撑爆
`40:24` 所以sub agent比较适合处理
`40:27` 那些与上下文关联比较小
`40:28` 而且对上下文影响比较大的任务
`40:31` 因此agent skill与sub agent的最大区别就在于
`40:34` 对上下文的处理方式不同
`40:35` 大家要根据具体的场景来选择合适的方案

### Plugin `40:38`

`40:38` 下面我们再讲讲plugin这个东西
`40:41` 你可以把plugin想象成一个全家桶的安装包
`40:44` 有点像是Mac os的DMG或者是windows下面的EXE文件
`40:49` 他把一系列的skill
`40:50` Sub
`40:51` agent hook等能力全部打包在一起
`40:54` 你只需要一键安装cloud code
`40:56` 就能够瞬间获得整套高级能力
`40:58` 下面呢我来给大家演示一下
`41:01` 我们先输入杠plugin
`41:02` 进入到插件管理器
`41:04` 这里面呢有三个选项
`41:06` 分别是discover
`41:07` 也就是发现新插件
`41:09` installed已安装的插件和market places呃
`41:13` 插件市场我们在discovery里面找到这个friend and design
`41:17` 按回车安装
`41:19` 接下来要选择安装范围
`41:21` 有三个可选范围
`41:22` 分别是对当前用户生效
`41:24` 对当前项目生效
`41:26` 或者是对当前用户的当前项目生效
`41:29` 我们维持默认就好了
`41:31` 确定后安装就完成了
`41:32` 对就是这么快
`41:34` 这里简单说明一下
`41:35` front and design是一个用来做前端设计的插件
`41:39` 一般来说大模型做的前端呢都有一定的共性
`41:42` 比如说是用深紫色的主题啊等等
`41:44` 这个插件呢据说可以打破这个共性
`41:47` 让界面看起来更加好看一点
`41:50` 我们等会儿来看看是不是这个样子的
`41:52` 安装好了之后
`41:53` 我们重启cloud code
`41:54` 然后使用MKDL命令新建一个目录
`41:57` 就叫做my to do2
`41:59` 然后呢我们进入到这个目录里面
`42:03` 在启动cloud code
`42:06` 启动好了之后
`42:07` 我们输入杠plugin
`42:09` 再次进入到插件的管理页面
`42:12` 然后再选择installed
`42:14` 可以看到installed这一个tab下面多了一项
`42:17` 就是我们刚才安装的这个front and design
`42:20` 我们按回车看一下它的详情
`42:22` 可以看到这个plugin的主要的组成元素
`42:27` 就是一个叫做frank and design的agent skill
`42:30` 既然我们已经安装了这个plugin
`42:32` 那对应的agent skill应该也安装了
`42:35` 我们不妨验证下
`42:36` 让我们回到输入框这里面
`42:38` 然后打杠skills
`42:40` 你看这里面是不是多了一个叫做front and design
`42:43` 的agent skill呢
`42:44` 所以呢安装这个插件
`42:46` 本质上就是安装了这个agent skill
`42:48` 当然这个plugin比较特殊
`42:50` 就只有一个组成元素
`42:52` 有些plugin里面包含了agent skill
`42:54` m cp hook等多个组成元素
`42:56` 你可以把它理解为整套解决能力
`42:58` 一次性全部安装了进来
`43:00` 下面呢我们就来运用这个fronting design
`43:03` 看看它跟原装的前端设计有没有什么区别
`43:06` 让我们回到输入框这里
`43:08` 输入我们的请求
`43:09` 按照front and design的要求做一个代办软件
`43:12` 使用HTML来实现
`43:17` 注意看cloud code并没有立即开始写代码
`43:20` 他首先是意识到
`43:21` 用户要求使用front and design的规范
`43:25` 于是呢他会先请求使用这个agent skill呃
`43:28` 我们点同意
`43:29` 读取完了之后呢
`43:31` 他就拥有了ANTHROPIC
`43:33` 官方沉淀的一整套UI的设计直觉
`43:35` 接着呢他开始写代码了
`43:38` 让我们稍微等一下嗯
`43:43` 写好了
`43:44` 我们先用IOS命令看看当前目录下有哪些文件
`43:48` 没问题
`43:49` 只有一个index点HTML文件
`43:51` 然后呢我们就可以使用open命令来打开这个文件
`43:55` 看一下效果怎么样
`43:57` 大家看这个呢
`43:59` 跟我们一开始写出来的那个demo相比
`44:01` 风格就完全不一样了
`44:03` 它的排版更加高级
`44:04` 色彩更加协调
`44:06` 交互呢也更符合现代审美
`44:09` 这个就是france design这个插件的力量了
`44:12` 目前cloud code的插件市场还在迅速的增长
`44:16` 除了UI设计之外
`44:18` 还有一些针对特定编程语言的LSP插件等等
`44:22` 呃当然如果你觉得自己的配置写的非常好的话
`44:25` 也可以参考官方的文档
`44:26` 把你的skill
`44:27` Sub agent
`44:28` MCP等等东西打包成插件
`44:30` 分享给你的团队或者是社区好
`44:33` cloud code到这里就讲完了
`44:35` 如果我的视频对你有帮助
`44:37` 别忘了点赞关注
`44:38` 我是马克
`44:39` 用最通俗的语言讲最硬核的技术
`44:42` 我们下期再见
`44:43` 拜拜
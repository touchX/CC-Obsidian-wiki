---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=19"
bvid: "BV1DfrdByE2H"
cid: "35394224536"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394224536&page=19&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 在本模块中
`00:00` 我想分享几个构建高效AI工作流的实用技巧
`00:04` 我希望这些技巧能让你在构建这类系统时效率大幅提升
`00:08` 相比普通开发者更具优势
`00:13` 我发现开发AI系统时
`00:16` 很难预先知道
`00:18` 哪些地方会有效哪些地方效果不佳
`00:21` 这才是你应该重点投入的地方
`00:23` 非常常见的建议是尝试构建一个快速简易的系统作为起点
`00:29` 这样你就可以实际测试并观察
`00:32` 看看哪些部分尚未达到预期效果
`00:35` 以便后续能更有针对性地进行深入开发
`00:40` 相比之下
`00:41` 我发现有时
`00:44` 你知道的
`00:44` 不要花太多周时间
`00:46` 空谈假设
`00:48` 如何构建
`00:49` 通常更好的做法是用安全合理的方式快速构建系统
`00:53` 以合理的方式
`00:54` 处理泄漏数据
`00:55` 以负责任的方式
`00:57` 但只需快速构建
`00:59` 这样你就可以查看
`01:00` 然后利用这个初始原型来优先级排序并推动进一步开发
`01:05` 让我们从构建原型后的示例开始
`01:10` 我想用第一个例子
`01:12` 之前展示过的发票处理工作流
`01:16` 包含提取四个必填字段的任务
`01:18` 然后保存到数据库记录
`01:21` 在构建此类系统后
`01:23` 你可以找到十几张或二十张发票
`01:26` 可能十张或二十张发票
`01:28` 逐一查看输出结果并检查哪些部分有效
`01:32` 以及是否存在任何错误
`01:34` 所以让我们查看两张发票
`01:36` 发现第一张发票正常
`01:38` 第二张发票的输出看起来正确
`01:40` 可能混淆了发票日期
`01:42` 即发票的开具日期和到期日期
`01:45` 在这个任务中我们需要提取到期日期
`01:48` 以便后续按时付款
`01:50` 因此我可能在文档或表格中记录
`01:52` 或在电子表格中注明对于第二张发票
`01:54` 日期存在混淆
`01:56` 第三张发票正常
`01:57` 第四张发票正常等等
`01:59` 但当我继续这个示例时
`02:01` 发现有很多类似日期混淆的例子
`02:05` 这正是通过此类示例进行分析的基础
`02:09` 这
`02:09` 在这种情况下，你可能会得出一个常见错误模式，即系统在处理日期时存在困难
`02:16` 在这种情况下
`02:17` 你可以考虑的一个方法是
`02:20` 当然，首先要弄清楚如何改进你的系统
`02:22` 使其更好地提取到期日期
`02:25` 同时可能需要编写评估指标来衡量提取准确性
`02:28` 与实际提取的到期日期进行对比
`02:32` 如果你发现系统错误提取了账单地址
`02:36` 谁知道也许你有发音奇特的账单名称
`02:40` 因此系统可能在处理账单时遇到困难
`02:41` 尤其是当国际账单方名称可能包含非英文字母时
`02:46` 这时你可能需要专注于构建账单地址的评估系统
`02:50` 之所以快速构建简易系统
`02:54` 并检查输出结果非常有帮助
`02:55` 因为它能帮助你决定当前应优先评估哪些环节
`03:02` 如果你决定修改系统以提高准确性
`03:07` 在提取发票到期日期时的准确性
`03:10` 那么要推进下一步
`03:12` 创建评估指标可能是个好主意
`03:16` 以衡量数据提取的准确性
`03:19` 可能有多种方法可以实现
`03:21` 但让我分享
`03:22` 我是如何操作的
`03:24` 创建测试集或评估集
`03:27` 我可能会找到10到20张发票并手动记录
`03:31` 正确的到期日期是什么
`03:32` 例如某张发票的到期日期是八月
`03:35` 2025年20日
`03:36` 并以标准年月日格式记录
`03:40` 以便后续代码评估更方便
`03:44` 我会编写提示语让语言模型始终按
`03:48` 年月日格式输出到期日期
`03:50` 然后编写代码提取模型输出中的日期部分
`03:56` 这就是我们需要关注的到期日期
`03:56` 因此只需关注这个日期
`03:58` 这需要正则表达式模式匹配
`04:01` 你知道的
`04:01` 四位数字表示年份
`04:02` 两位数字表示月份
`04:03` 两位数字表示日期
`04:03` 然后提取这部分内容
`04:05` 接着编写代码进行测试
`04:07` 判断提取日期是否等于实际日期
`04:11` 即我手动标注的真实日期
`04:14` 因此使用包含
`04:16` 约20张发票的评估集
`04:17` 通过修改系统观察正确提取日期的比例
`04:23` 是否有所提升
`04:25` 随着我调整提示或系统其他部分
`04:28` 所以总结一下我们到目前为止看到的内容
`04:30` 我们构建了一个系统
`04:32` 然后分析输出结果
`04:32` 以发现系统可能存在的不足之处
`04:36` 例如截止日期错误
`04:38` 然后针对这个重要输出进行改进
`04:41` 实施
`04:42` 一个小规模评估只需两个示例来帮助我们跟踪进展
`04:46` 这让我可以回到两个优点
`04:48` 尝试不同算法等以提升截止日期准确性这一指标
`04:53` 这就是改进整个工作流程通常的感觉
`04:58` 查看输出结果
`04:58` 发现问题所在
`04:59` 如果你知道如何修复
`05:00` 直接进行修正
`05:01` 但如果需要更长的改进过程
`05:03` 则建立评估并以此驱动进一步开发
`05:06` 另一个需要考虑的是工作一段时间后
`05:10` 如果最初两个示例不够理想
`05:13` 可能无法覆盖所有需求场景
`05:15` 或者二十个示例数量太少
`05:17` 可以随时扩展评估集以确保更全面
`05:20` 反映系统性能是否达到满意标准的个人判断
`05:27` 这只是第二个示例
`05:29` 让我们构建一个Instagram文案助手
`05:34` 保持内容同步
`05:36` 假设营销团队要求我们生成的文案需
`05:39` 最多十词以内
`05:40` 展示产品图片
`05:43` 比如我们要推广的太阳镜
`05:45` 然后用户查询如
`05:47` 请为这些太阳镜撰写销售文案
`05:50` 然后使用语言模型或大型多模态模型
`05:54` 分析图片和查询生成产品描述
`05:59` 营销文案可能出错的方式有很多
`06:03` 但假设查看输出结果
`06:05` 发现生成的文案大部分听起来正常
`06:09` 但有时可能过长
`06:11` 对于太阳镜输入
`06:12` 生成十七词若咖啡机则无问题
`06:15` 时尚
`06:16` 可以
`06:16` 蓝衬衫十四词
`06:18` 搅拌机十一词
`06:19` 看起来在这个例子中
`06:20` 模型难以遵循长度限制
`06:25` 营销文案助手可能出现很多问题
`06:29` 但若发现输出长度不达标
`06:33` 则可建立评估指标进行跟踪
`06:36` 以便持续改进
`06:38` 确保它在听力长度指南上有所改进
`06:42` 因此创建一个评估来衡量文本长度
`06:44` 你可以创建一组测试任务
`06:47` 所以标记一副太阳镜
`06:48` 一台咖啡机等等
`06:49` 可能创建十个到二十个示例
`06:52` 然后将每个示例输入系统
`06:55` 编写代码测量输出的词数
`06:59` 这是测量文本词数的Python代码
`07:04` 最后比较生成文本与十词目标限制的长度
`07:10` 如果词数等于十
`07:12` 正确计数加一
`07:14` 这与之前的发票处理示例不同之处在于没有每个示例
`07:21` 真实标注
`07:22` 目标对每个示例都相同
`07:25` 相比之下在发票处理示例中
`07:27` 需要生成自定义目标标签
`07:30` 即发票的正确到期日期
`07:33` 并将其与每个示例的真实标注进行对比
`07:37` 我知道我使用了非常简单的流程生成这些标题
`07:41` 但这类方法也可应用于更复杂的生成流程
`07:46` 让我再讲一个最终示例
`07:48` 我们将重新审视之前研究的代理
`07:52` 查看研究代理在不同输入提示下的输出
`07:57` 假设要求它撰写关于黑洞研究的最新突破文章
`08:01` 在黑洞领域
`08:02` 发现它遗漏了新闻报道中的一些重大成果
`08:07` 这是令人不满意的结果
`08:09` 或者当你要求它研究
`08:10` 西雅图租房与购房
`08:12` 或在机器人采摘水果方面表现良好
`08:15` 嗯
`08:16` 未提及领先设备公司
`08:19` 基于此评估
`08:21` 看起来有时会遗漏人类专家会捕捉到的重要点
`08:28` 因此我将创建评估来衡量其捕捉关键点的频率
`08:33` 例如为黑洞、机器人采摘等主题准备多个示例提示
`08:39` 以及相关领域
`08:40` 并对每个示例
`08:42` 为每个主题列出三到五个黄金标准讨论点
`08:46` 注意这里需要每个示例的标注
`08:51` 因为黄金标准讨论点
`08:52` 即最重要的讨论点
`08:55` 每个示例不同
`08:57` 通过这些真实标注
`08:59` 可以使用语言模型统计提及的黄金标准讨论点数量
`09:05` 例如提示可能要求
`09:08` 确定提供的文章中包含多少个五项黄金标准讨论点
`09:13` 你有可选提示
`09:14` 文章
`09:14` 采用标准评分点等
`09:16` 并返回包含两个键的JSON对象
`09:18` 有分数
`09:19` 有多少个0到5的评分点对应分数
`09:22` 以及解释说明
`09:24` 这允许为评估集中的每个提示获取分数
`09:31` 我用om作为评判者统计提到的要点数量
`09:36` 因为这些要点的讨论方式多种多样
`09:40` 因此简单的正则表达式或模式匹配可能效果不佳
`09:45` 这也是为何可能使用a作为评判者
`09:48` 并将其视为更主观的评估标准，例如
`09:52` 事件视界是否被充分提及
`09:55` 这是构建评估指标的第三个示例
`09:59` 在思考如何构建评估系统时
`10:02` S针对您的应用场景
`10:03` 您构建的评估系统通常需要反映实际需求
`10:08` 或您在应用中可能出现的错误
`10:11` 实际上，评估存在两大维度，顶部维度
`10:18` 是评估输出的方式
`10:21` 有时通过编写代码进行客观评估
`10:26` 有时则使用o作为评判者进行主观评估
`10:32` S另一维度是是否具有单例真实标签
`10:38` 例如检查发票数据提取
`10:42` 我们编写代码验证是否获取实际日期
`10:45` 这具有单例真实标签
`10:49` 因为每张发票的实际日期不同
`10:51` 但在检查营销文案长度时
`10:55` 每个示例都有10字限制，无需单例真实标签
`11:02` 相比之下，统计标准要点
`11:06` 存在单例真实标签
`11:08` 因为每篇文章的重要要点不同
`11:11` 但我们使用元素作为评判者阅读文章
`11:14` 判断是否充分提及这些主题
`11:17` 因为提及要点的方式多种多样
`11:20` 四个象限的最后一类是消息评判者
`11:24` 无需单例真实标签
`11:26` 例如在按评分标准批改图表时
`11:31` 我们正在可视化咖啡机销售数据
`11:35` 若要求根据评分标准生成图表
`11:38` 如是否标注清晰等
`11:40` 所有图表使用相同的评分标准
`11:43` 这将使用元素作为评判者
`11:45` 但无需单例真实标签
`11:47` 我发现这个二维网格
`11:49` 可能是思考不同评估类型的有效方式
`11:52` S您可能为应用构建的评估系统
`11:54` 顺便说这些有时也称为端到端评估S
`11:58` 因为一端是输入和用户查询提示
`12:01` 另一端是最终输出
`12:04` 因此这些都是完整端到端系统的评估指标
`12:10` 所以总结一下这个视频
`12:12` 我想分享几个设计端到端评估的最终建议
`12:15` 首先快速粗糙的评估是完全可以起步的
`12:19` 我觉得看到很多团队几乎陷入瘫痪
`12:23` 因为他们认为构建评估是需要数周的巨大工程
`12:27` 因此他们花费了理想时间之外的更久才开始
`12:31` 但正如你迭代优化代理工作流并逐步改进
`12:36` 你也应该计划迭代优化你的评估系统
`12:39` 所以如果你准备十个、十五个或二十个示例
`12:43` 作为你的首个评估测试并编写和编码
`12:45` 或尝试以裁判身份进行提示测试
`12:47` 只需做些事情开始获取指标数据
`12:49` 这可以补充人工观察输出结果
`12:53` 两者结合能驱动你的决策过程
`12:56` 随着评估系统逐渐智能化
`12:58` 你可以逐步将更多信任交给基于指标的评估
`13:03` 而非每次都要查看数百个输出结果
`13:06` 你只需调整某个参数
`13:08` 在这一过程中
`13:10` 你可能会找到持续改进评估的方法
`13:14` 所以如果你最初有二十个示例
`13:17` 可能会遇到评估无法捕捉系统优劣判断的情况
`13:25` 因此你更新系统并观察结果
`13:28` 感觉这应该能显著提升效果
`13:30` 但旧评估未能显示新系统得分更高
`13:34` 如果出现这种情况
`13:36` 这通常是机会
`13:38` 或许扩大评估样本集
`13:40` 或改变评估输出的方式
`13:42` 使其更符合你的判断标准
`13:44` 以准确反映哪个系统更有效
`13:47` 因此你的评估会随时间提升
`13:49` 最后关于利用评估获取灵感
`13:53` 以确定下一步工作方向
`13:54` 许多创新工作流被用于自动化人类可完成的任务
`14:00` 因此对于此类应用
`14:02` 我会寻找性能较差的环节
`14:04` 而作为专家人类
`14:05` 这常为我提供专注方向的灵感
`14:09` 或者哪些类型的示例
`14:11` 可能让我的代理工作流表现更优
`14:16` 所以希望你在搭建快速系统后
`14:19` 思考何时开始
`14:20` 引入评估以跟踪系统潜在问题
`14:22` 这将帮助你推动系统改进
`14:26` 同时帮助你驱动系统优化
`14:30` 此外还有一种评估方法
`14:33` 能帮助你聚焦整个系统核心
`14:36` 哪些组件最值得投入注意力
`14:40` 系统中最值得专注的组件是什么
`14:44` 因为基因系统通常包含许多组件
`14:47` 那么哪个组件将是最具生产力的，值得你投入时间改进
`14:53` 事实证明，能够做好这一点是一项对驾驶至关重要的技能
`14:57` 在下一视频中高效开发enic工作流程
`15:00` 我想深入探讨这一主题
`15:02` 因此让我们进入下一视频
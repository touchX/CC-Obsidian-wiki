---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=16"
bvid: "BV1DfrdByE2H"
cid: "35394162516"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394162516&page=16&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 让我们看看如何编写代码来拥有自己的get工具
`00:03` 这里是我们之前的
`00:05` 不带时区参数的获取当前时间函数
`00:09` 让我展示如何使用ai套件开源库
`00:12` 顺便说一下，要拥有自己的调用工具
`00:15` 从上一个视频中可以看到
`00:17` 语言模型不会调用工具
`00:20` 语言模型只是请求你调用工具
`00:22` 但在构建生成工作流的开发者中
`00:25` 我们很多人偶尔会直接说语言模型调用工具
`00:28` 尽管这在技术上并不准确
`00:31` 但因为这是更简洁的说法
`00:34` 这里的语法与调用语言模型的OpenAI语法非常相似
`00:40` 不过这里我使用的是ai套件库
`00:43` 这是一个我和一些朋友开发的开源包
`00:47` 它能轻松调用多个语言模型提供商
`00:51` 所以代码语法
`00:52` 如果你觉得这看起来很多
`00:54` 不用担心
`00:55` 在代码实验室中你会看到更多
`00:57` 但简而言之这与OpenAI语法非常相似
`01:01` 你会说
`01:01` 响应等于客户端检查完成
`01:03` 创建
`01:04` 然后选择模型
`01:05` 这里我们将使用OpenAI模型
`01:08` Gp四消息等于消息假设你已将其放入数组
`01:13` 要传递给语言模型的消息
`01:15` 然后等于
`01:17` 你希望语言模型访问的工具列表
`01:20` 这里只有一个工具
`01:22` 即获取当前时间
`01:23` 无需过多担心最大轮次参数
`01:27` 这是因为工具调用返回后
`01:30` 模型可能会决定再次调用工具
`01:32` 之后工具调用返回后
`01:34` 模型可能再次调用另一个工具
`01:36` 最大轮次只是设置一个上限
`01:38` 你希望语言模型连续调用工具的次数
`01:42` 在无限循环中停止
`01:45` 实际上你几乎不会遇到这个限制
`01:48` 除非代码有非常激进的操作
`01:51` 所以我通常不关心最大轮次参数，一般设为五
`01:55` 但实际上这并不重要
`01:58` 使用ai套件后发现
`01:59` 获取当前时间函数会自动描述给语言模型
`02:04` 以便模型知道何时调用它
`02:08` 无需手动编写长提示
`02:11` 告诉模型获取当前时间
`02:13` ai套件的语法会自动处理
`02:16` 为了让它看起来不那么神秘
`02:18` 其实它是通过查看与get_current_time关联的文档字符串来实现的
`02:24` 通过这些注释
`02:25` 获取当前时间以便向语言模型描述此函数
`02:31` 为了说明它是如何工作的
`02:33` 这里是函数代码
`02:35` 这里是一段代码片段
`02:37` 使用AI套件在后台调用语言模型
`02:41` 这将创建一个详细描述函数的JSON模式
`02:47` 而这里右侧实际传递的是
`02:51` 并将提取函数名称
`02:54` 即当前时间
`02:55` 然后从文档字符串中提取函数描述
`03:00` 告知语言模型此函数的功能
`03:02` 使其能决定何时调用
`03:04` 有些API需要手动构建此JSON模式
`03:09` 然后将JSON模式传递给
`03:12` 但该包会自动为您处理以展示更复杂的示例
`03:17` 如果你有包含输入时区参数的复杂get_current_time工具
`03:24` AI套件将创建更复杂的JSON模式
`03:28` 同样提取函数名称get_current_time
`03:32` 从文档字符串中提取描述
`03:33` 并识别参数并描述给语言模型
`03:38` 基于左侧显示的文档
`03:41` 因此在生成函数调用参数时
`03:46` 它知道应使用类似美国
`03:49` 纽约或太平洋奥克兰
`03:51` 或其他时区
`03:53` 如果执行左侧下方的代码片段
`03:58` 将使用OpenAI GPT-4模型调用函数
`04:03` 如果需要则调用该函数
`04:04` 获取函数输出并反馈给模型
`04:08` 最多进行五轮交互
`04:10` 然后返回响应注意当语言模型请求调用get_current_time函数
`04:17` AI套件或客户端
`04:20` 将自动调用get_current_time函数
`04:22` 无需手动执行
`04:25` 所有操作均在单个函数调用中完成
`04:29` 需注意其他实现可能需要手动完成此步骤
`04:36` 但使用此特定包
`04:39` 所有操作均封装在客户端中
`04:40` 聊天完成
`04:41` 创建函数调用
`04:43` 现在您已了解如何让语言模型调用函数
`04:47` 希望您在实验室中尝试并享受这个过程
`04:51` 当提供几个函数时效果尤为惊人
`04:56` 模型会采取行动获取更多信息以满足需求
`05:00` 如果您之前未尝试过
`05:02` 相信您会发现这非常酷
`05:05` 事实证明所有工具中最强大的是
`05:08` 这里有一个
`05:09` 这有点特别
`05:10` 这是一个代码执行工具
`05:13` 结果非常强大
`05:14` 如果你能开启
`05:16` 你可以编写代码
`05:17` 我将为你提供一个执行代码的工具
`05:20` 因为代码能做很多事情
`05:22` 我们赋予它编写代码并执行代码的灵活性
`05:28` 这实际上极其强大
`05:30` 提供一个工具给手臂
`05:32` 所以代码执行很特别
`05:34` 让我们进入下一视频，讨论警报的代码执行工具
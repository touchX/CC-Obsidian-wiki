---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=39"
bvid: "BV1DfrdByE2H"
cid: "35394227368"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394227368&page=39&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 现在你已经定义了目标并指定了文件
`00:03` 下一步是确定结构化数据的图模型
`00:07` 即确定构成领域图的节点和边的类型
`00:12` 你将设置一个子代理循环来优化模型
`00:15` 让我们深入探讨
`00:17` 你正在使用结构化数据代理
`00:19` 通过从用户意图开始的工作流程
`00:22` 然后是文件建议
`00:23` 现在我们准备好提出图模式的可能方案
`00:28` 这就是这个代理的任务
`00:30` 这个代理在代理组合方式上引入了一些新思路
`00:34` 这个代理实际上包含多个内部代理
`00:38` 其中有一个代理负责提出方案
`00:42` 这里显示我建议构建的图模型
`00:45` 另一个代理会检查并批评该方案
`00:48` 这是一种在多代理系统中常见的批评模式
`00:52` 在顶层协调器中
`00:54` 这里只使用了少数几个工具
`00:57` 有一个我们称为细化循环的工具
`01:00` 还有两个常规工具：获取建议构建计划
`01:04` 以及批准建议构建计划
`01:06` 这些与之前看到的文件建议和批准工具类似
`01:11` 但这个细化循环非常有趣
`01:14` 实际上是一个包含多个协作组件的代理
`01:19` 让我们看看内部结构
`01:21` 细化循环是一个协调子代理的代理
`01:25` 该循环代理包含三个子代理
`01:28` 模式提案代理
`01:30` 模式批评代理
`01:31` 以及状态检查模块
`01:34` 升级代理
`01:35` 这些代理会持续循环运行
`01:39` 直到得到最终结果
`01:41` 模式提案代理生成提案
`01:43` 模式批评代理进行批评
`01:46` 状态检查和升级代理负责分析批评结果
`01:51` 判断批评是否满意
`01:53` 或是否需要重新尝试
`01:54` 如果批评满意
`01:56` 正是这个代理触发最终确认
`01:59` 从而终止循环代理
`02:02` 这个过程可能无限循环
`02:03` 当你调用循环代理时
`02:05` 我们可以设置循环迭代次数限制
`02:08` 若无法达成共识
`02:11` 将返回用户请求
`02:12` 说
`02:13` 我们对此不确定
`02:14` 顶层协调器将负责获取更多用户输入
`02:19` 你将从常规导入开始
`02:22` 当这些准备就绪
`02:23` 你可以定义我们将要使用的llm并查看它
`02:29` 所有准备就绪，太好了
`02:30` 我们现在可以开始工具定义
`02:33` 现在可以开始编写代理的指令
`02:36` 我们将重点研究的代理
`02:39` 或者模式提案代理
`02:40` 然后是批评者
`02:41` 让我们看看我们将提供给它们的指令
`02:45` 对于提案代理在其角色和目标
`02:48` 我们将告诉它擅长图数据建模
`02:51` 使用属性图建模
`02:53` 你可以在这里查看详细描述
`02:57` 但这里的新颖部分
`02:58` 在这段提示中真正关键的是我们将注入
`03:02` 现在反馈初始为空
`03:06` 因此我们告诉代理仅在反馈可用时考虑
`03:10` 但由于可能为空
`03:12` 我们不希望混淆其余提示成为反馈部分
`03:16` 因此我们在此处用xml包裹
`03:18` 使用伪xml表示此处的反馈
`03:21` 然后是反馈结束标记
`03:22` 因为大多数llm能很好地理解这种分隔符
`03:26` 所以我们在此处使用分隔符
`03:28` 这些花括号反馈成为模板变量
`03:34` 由google eighty k google edk注入
`03:37` 在组装时
`03:38` 提示内容
`03:38` 会查找这些文本部分并替换为上下文
`03:44` 一个上下文变量
`03:45` 如果在工具上下文状态
`03:47` 如果状态中定义了反馈变量
`03:51` 该变量的值将被注入此处
`03:53` 如果上下文中有反馈
`03:56` 将传递到此提示部分
`03:59` 好的
`04:00` 你已为目标代理设定了目标和角色
`04:03` 现在我们将提供执行任务的提示
`04:07` 这里的提示比之前更长
`04:11` 原因在于大多数llm包括GPT
`04:16` 在图数据建模方面表现良好
`04:19` 但并非特别擅长
`04:21` 我们将提供额外的思考指导
`04:24` 如何处理可用的数据文件
`04:27` 以及这些数据文件可能呈现的图结构
`04:30` 你可以查看所有指导内容
`04:31` 并注意到我们包含了知识图谱构建的最佳实践
`04:35` 这些最佳实践已编码在提示中
`04:38` 因此所有批准文件列表中的文件都应成为图的一部分
`04:43` 在这些文件中这些是csv文件
`04:46` 通常你会看到唯一的标识符
`04:48` 我们让它去查找这些唯一标识符
`04:51` 尝试识别这些标识符是什么
`04:54` 并利用它们来理解该文件在构建图中的作用
`04:59` 然后我们提供一些指导
`05:01` 基本是一些设计规则，用于思考某个文件是否可能代表节点
`05:07` 或可能代表关系
`05:08` 你可以查看具体细节
`05:10` 但文件本身的命名通常包含语义提示
`05:14` 出现的标识符数量
`05:16` 这些标识符是否真正唯一属于该文件
`05:19` 或是否唯一属于其他文件
`05:21` 所有这些都需要我们或我来做
`05:25` 当我们作为数据工程师工作时
`05:27` 当我们收到一堆数据文件时
`05:29` 我们基本上用grep和cat处理
`05:31` 也许我们会将其导入编辑器查看文件
`05:34` 这就是我们现在要做的
`05:37` 这些内容在提示中明确表述
`05:39` 帮助代理理解如何进行相同的数据建模
`05:43` 这是我们已知的，将其分解
`05:46` 是节点还是关系特定
`05:48` 你知道的
`05:49` 节点的设计规则
`05:50` 节点通常有单个标识符
`05:52` 而非多个标识符
`05:54` 但如果文件包含多个标识符
`05:56` X光标识符可能是引用关系
`06:00` 这在关系数据库中会发生
`06:02` 图中可能会有外键关系
`06:05` 这些将转化为关系
`06:07` 如果关系本身
`06:09` 它们有两种表现方式
`06:11` 这里我们称之为
`06:13` 完整关系和引用关系
`06:17` 然后进一步分解
`06:18` 我们向LLM描述
`06:21` 什么是完整关系
`06:22` 什么是引用关系
`06:25` 这里有两个组成部分
`06:27` 我们解释如何识别它们
`06:29` 但在识别后
`06:31` 如何将它们转化为图
`06:34` 最后在提供所有设计指导后
`06:37` 建议生成的模式应为完整连接的图
`06:42` 这很重要，因为显然可能导入大量数据
`06:46` 却无法连接
`06:48` 就不是真正的图
`06:49` 此时几乎无用
`06:51` 因此必须完全连接
`06:53` 如果存在任何孤立组件
`06:54` 这很可能是一个问题，需要考虑
`06:58` 然后可以给代理一些思维链指导方向
`07:01` 这也是比之前更复杂的步骤
`07:04` 可以逐步查看这个过程
`07:06` 首先让代理为即将进行的任务做准备
`07:10` 我们知道该任务基于已建立的用户目标和批准文件列表
`07:16` 同时还要检查是否有批准的施工计划或现行施工计划
`07:21` 即使提出这个建议
`07:22` 这可能不是该代理首次提出施工计划
`07:27` 如果已有现有计划
`07:29` 可以获取当前施工计划
`07:32` 并决定是否需要修改
`07:35` 思维链过程与常规相同
`07:38` 鼓励其仔细思考，分步使用可用工具
`07:43` 对于任何操作需逐个检查批准文件
`07:46` 判断是否为节点或关系
`07:49` 这呼应了我们之前的架构指导
`07:51` 每次发现标识符时
`07:54` 或认为发现了标识符时
`07:56` 应使用工具验证是否找到唯一标识符
`08:01` 使用名为搜索文件工具
`08:02` 这相当于基于Python的grep工具
`08:05` 对于刚发现的标识符
`08:07` 可尝试验证其是否唯一
`08:09` 通过检查同一文件中的重复出现
`08:14` 我们鼓励其考虑我们之前提供的指导
`08:17` 以判断是否为节点或关系
`08:21` 对于节点文件
`08:22` 有专门工具用于提出如何处理该节点文件
`08:27` 我们称之为施工操作
`08:28` 有特殊工具可记录特定文件的处理方式
`08:33` 如何将该文件转化为图中的节点
`08:37` 同样如果认为发现关系
`08:40` 可调用名为提议关系构建工具
`08:44` 用于提议关系构建
`08:47` 使用这两个工具
`08:49` 将遍历所有文件
`08:50` 无论是节点还是关系
`08:52` 调用相应工具生成构建规则
`08:55` 将文件转化为图的一部分
`08:58` 如有必要还可移除构建规则
`09:02` 完成后
`09:03` 使用获取提议施工计划工具向用户展示最终方案
`09:08` 获取提议施工计划工具将包含所有构建规则的完整列表
`09:15` 可将所有内容整合为最终指令
`09:25` 现在可以开始定义工具
`09:28` 我们已在之前的笔记本中定义了许多常用工具
`09:31` 因此直接导入这些工具，如获取批准用户目标、批准文件等
`09:35` 以及示例文件工具
`09:38` 代理的指令还提到了一个搜索文件工具
`09:42` 这个搜索文件工具本质上是一个类似grep的功能，可以读取文件
`09:47` 浏览文件的几行内容并查找特定模式
`09:51` 这不是一个非常复杂的查询
`09:52` 就像grep的工作方式
`09:53` 但足以让LLM实际查找可能出现的内容
`09:56` 例如
`09:57` 用于唯一标识符
`09:59` 可以将这个常量传入此处
`10:01` 然后在文件中查找该常量是否存在
`10:04` 该工具的结果将包含行数以及包含该内容的行
`10:13` 接下来要定义的工具是提议的节点构建
`10:17` 现在这描述了如何从源数据文件
`10:21` 所以我们传入一个改进后的文件
`10:23` 文件路径
`10:24` 然后基于该文件路径
`10:26` 我们需要在图中创建一个节点
`10:29` 我们描述节点应具备的特征
`10:32` 通过几个不同方面来描述节点
`10:35` 首先是应应用的标签是什么
`10:38` 节点标签通常用于描述节点类型，如是否为人物
`10:43` 地点
`10:43` 或事物，描述节点代表的类型
`10:47` 此外还需确定唯一列名称
`10:50` 因此在导入的CSV文件中
`10:52` 某一列应为唯一列
`10:55` 如果我们识别出该列并传入此处
`10:57` 最后可能不会接受所有列作为有效构建节点的属性
`11:03` 提议的属性实际描述了CSV文件
`11:07` 哪些列需要导入并作为节点属性创建
`11:13` 查看该函数的实际实现非常直接
`11:16` 会进行常规的合理性检查
`11:19` 如果我们通过了合理性检查
`11:21` 接下来将描述或构建构造规则
`11:27` 这实际上是一个描述数据对象的工厂函数
`11:32` 此处的数据对象是一个构造规则，类型为节点
`11:37` 基于已传入的源文件生成
`11:41` 赋予一个标签
`11:42` 具有唯一列名称
`11:44` 并创建一组属性
`11:47` 将其添加到现有构造规则列表中
`11:51` 为明确说明
`11:52` 我们之前已从内存中获取当前构造计划
`11:57` 如果不存在
`11:58` 这将是一个空字典
`12:01` 但若存在时添加
`12:04` 我们将使用标签进行添加
`12:05` 因为所有标签应唯一
`12:07` 使用标签作为键
`12:09` 我们将添加该构造规则
`12:11` 因此，整体构建计划将是一组唯一键
`12:15` 其中每个键都是一条规则，要么是节点构建，要么是关系构建
`12:19` 包含所需信息
`12:21` 现在你可以定义一个用于创建关系构建规则的类似工具
`12:27` 如果你查看传入的参数
`12:30` 这些参数与节点类似，是关系特有的
`12:33` 它以文件名开头，表示关系将从中提取的位置
`12:38` 而不是使用标签
`12:39` 它们使用类型
`12:40` 将传入建议的关系类型
`12:43` 由于关系连接其他节点
`12:46` 你需要知道该关系可能来自的节点标签是什么
`12:51` 以及它连接到什么
`12:52` 因此你会得到来源节点和目标节点的标签
`12:57` 然后对于来源节点和目标节点
`12:59` 你也需要知道其唯一标识
`13:01` 在用于创建关系构建的CSV文件中
`13:07` 应有一个列标识来源节点
`13:10` 该节点的唯一标识是什么
`13:13` 来源节点列
`13:14` 还有一个目标节点列
`13:15` 应包含节点的唯一标识
`13:18` 关系连接的目标节点及其相关信息
`13:21` 然后你可以像节点实现一样构建关系
`13:27` 如果来源和目标节点都存在
`13:29` 那么我们可以继续创建构建规则本身
`13:32` 该构建将再次是一个字典
`13:35` 字典类型表明它是关系
`13:38` 因此这是一个关系构建规则
`13:40` 它包含源文件、关系类型、来源节点、目标节点
`13:45` 以及可能应用于关系本身的属性集合
`13:48` 所有内容也添加到关系构建计划中
`13:52` 根据关系类型
`13:54` 如同节点标签
`13:55` 在数据导入过程中，我们期望关系类型唯一
`13:59` 因为该代理将在精炼循环中运行
`14:03` 可能需要移除之前创建的构建规则
`14:08` 因此我们将提供移除工具
`14:10` 适用于节点构建和关系构建
`14:13` 这是节点版本
`14:16` 你也可以定义移除关系构建的类似工具
`14:20` 基于关系类型
`14:22` 而非节点标签
`14:25` 代理与这些工具交互以提议构建规则的结果
`14:29` 然后移除这些构建规则
`14:31` 形成完整的构建计划，将CSV数据文件转换为知识图谱
`14:36` 因此，通过让LM了解其构建的整体计划
`14:38` 我们将创建知识图谱
`14:42` 我们也为此提供工具
`14:44` 获取提议的构建计划工具
`14:46` 我们将从这里获取当前的施工计划
`14:50` 它将继续使用之前使用的同一密钥
`14:52` 用于之前施工计划的构建
`14:54` 如果没有进行任何操作，默认值为空
`14:58` 但当前的施工计划可能已经
`15:00` 没有计划
`15:03` 就像你之前做的那样
`15:04` 结合用户意图和文件建议生成架构提案
`15:08` 你还有一个专门处理基于施工计划提出的或建议的架构方案的工具
`15:15` 然后批准该方案
`15:19` 然后将所有这些工具添加到列表中
`15:21` 这就是你要使用的列表
`15:22` 在定义实际提供所有工具的代理时使用
`15:26` 你可以看到工具集合随着时间推移在这里逐渐增加
`15:30` 因为它结合了我们之前达成的共识
`15:34` 在工作流程的前期阶段
`15:36` 与当前代理在当前工作阶段的任务相结合
`15:40` 其意图
`15:41` 当然
`15:41` 是创建图结构模式
`15:45` 你现在可以定义代理本身
`15:47` 负责执行架构提案
`15:49` 你将在此处添加一个新的实用函数
`15:53` 在创建代理时使用
`15:55` 该实用函数仅记录代理被调用的情况
`15:58` 并打印代理名称到屏幕
`16:03` 这里处理当前对话的内容
`16:06` 稍后创建代理时你会看到添加位置
`16:10` 现在可以定义架构提案代理
`16:13` 如果你直接说
`16:14` 代理在这里
`16:15` 这是一个语言模型代理
`16:17` 即推理代理
`16:18` 能够基于现有工具做出决策
`16:22` 和之前一样，我们需要为其命名
`16:25` 为其添加描述
`16:26` 我们将传入指令
`16:28` 但这次调用的独特之处在于代理实际被调用前
`16:33` 有一个回调函数
`16:34` 在调用前
`16:36` 并希望回调指向我们刚刚定义的日志代理函数
`16:40` 这将让我们看到
`16:42` 当代理被调用时的下一步操作
`16:47` 你需要继续使用我们的老朋友
`16:51` 创建代理调用器
`16:53` 如同工作流程的前期部分
`16:56` 需要设置代理的初始状态
`16:59` 如同前期工作流程已完成
`17:02` 因此需要获取已批准的用户目标
`17:04` 还需要获取已批准的文件
`17:06` 因为这个代理将在反馈循环中运行
`17:09` 我们将初始化反馈机制
`17:11` 同时保持为空白
`17:12` 这能让代理顺利运行而无任何问题
`17:16` 现在可以调用这个代理
`17:18` 通过用户消息提示如何将这些文件导入构建知识图谱
`17:24` 我们将查看会话内容
`17:25` 完成后打印会话状态
`17:29` 预期看到提出的构建计划
`17:32` 这里已经给出
`17:34` 最终结果再次呈现
`17:35` 我们获得了最终状态为真的事件
`17:38` 让我们看看
`17:39` 它决定创建组装相关的节点
`17:43` 基于组装数据
`17:44` Csv文件
`17:44` 为零件创建节点
`17:47` 为产品创建节点
`17:48` 为供应商创建节点
`17:50` 非常直接
`17:51` 它在这方面做得很好
`17:52` 现在它开始建立关系
`17:55` 让我们看看它的决策
`17:57` 它认为从组装
`17:59` Csv文件中存在从组装到产品的关联
`18:03` 可以说明组装包含在某个产品中
`18:07` 将创建从组装到产品的关联
`18:10` 两个产品
`18:11` 并称这些关系为'包含'
`18:14` 属性将包括组装名称
`18:17` 以及数量
`18:18` 当前处理的产品是家具
`18:21` 例如桌腿
`18:22` 这可能是一个组装组件
`18:24` 可能存在四个这样的组件
`18:25` 这样数量看起来合理
`18:28` 它对零件也做了类似决策
`18:30` 零件Csv似乎指向组装
`18:34` 因此零件属于某个组装
`18:38` 包含零件名称和合理数量
`18:41` 还完成了供应商映射
`18:45` 现在注意这些关系
`18:46` 这里有两种不同类型的关系
`18:49` 前两个是定义节点的源文件
`18:54` 但由于节点存在外键关联
`18:57` 得益于我们的精准提示
`19:00` 然后它决定一切顺利
`19:02` 我将定义创建关系的规则
`19:04` 其中'supplied by'稍有不同
`19:08` 因为零件供应商映射Csv只是简单映射
`19:11` 从零件到供应商
`19:14` 这就像关系型数据库中的联合表
`19:17` 零件表和供应商表
`19:20` 而这里的零件供应商是连接这两张表的关联表，形成关系
`19:25` 而不是关联表
`19:27` 这非常完美
`19:29` 它还解释了所有这些内容
`19:31` 并且显得非常满意
`19:33` 所以这很好
`19:35` 会话状态相当复杂
`19:36` 你可以查看其中内容
`19:37` 如果你想查看所有细节
`19:39` 但代理生成的markdown输出已很好地表达了这些信息
`19:45` 好的
`19:46` 一个提出模式的代理已完成
`19:51` 现在我们将转向批评者
`19:53` 这是优化循环的第二部分
`19:55` 模式批评者与提案代理类似
`19:58` 作为知识图谱建模专家
`20:01` 但它的任务不是创建模型
`20:03` 而是批评已创建的模型
`20:05` 因此批评者有特殊角色和目标
`20:08` 当然它就是个批评者
`20:10` 我们将定义其角色为
`20:12` 同样精通属性图知识图谱建模
`20:15` 就像提案代理一样
`20:18` 但它的任务是批评已提出的方案
`20:23` 因此这是它的目标：批评提议的模式
`20:27` 确保其与用户目标及可用文件相关
`20:32` 还会为该代理提供执行任务的提示
`20:35` 在批评提议的模式时
`20:38` 需要检查哪些内容
`20:40` 这实际上是关于
`20:42` 如何进行良好的知识图谱建模
`20:45` 但现在从批评他人模型的角度
`20:47` 如果你要批评他人的模型
`20:49` 会提出哪些意见
`20:50` 需要关注哪些方面
`20:52` 唯一标识符是否真正唯一
`20:54` 可以使用搜索工具验证这一点
`20:58` 已定义的节点是否可能为关系
`21:01` 需再次确认这一点
`21:03` 如果这是一个连通图
`21:05` 应能手动从源数据追溯到构建的图
`21:10` 确保所有源CSV文件都映射到图中
`21:13` 并保证图本身完全连通
`21:16` 还有一些其他提示
`21:17` 当然还包括层级容器关系是否存在
`21:20` 是否存在冗余关系
`21:23` 方案提案可能过于激进，创建了不必要的关系
`21:29` 或者那些在实际实现结果时逻辑上必需的部分
`21:32` 这也需要检查一下
`21:35` 提示的下一部分是思维链方向
`21:38` 和之前一样，我们要向批评者解释如何准备任务
`21:42` 它拥有
`21:43` 它使用哪些工具来实际准备执行任务
`21:46` 然后应该如何执行任务
`21:48` 一如既往，它应该仔细思考
`21:50` 并使用工具执行操作
`21:52` 首先你应该检查每个构造规则
`21:56` 包括节点构造规则和关系构造规则
`21:59` 然后使用工具验证构造规则是否相关且正确
`22:04` 如果所有检查都通过且一切正常
`22:07` 只需回复单个词valid
`22:11` 如果架构存在任何问题
`22:13` 回复retry并提供反馈
`22:16` 这里的反馈要求简洁的项目符号列表
`22:20` 如果批评者对提案有任何疑虑
`22:23` 我们应该看到这个清晰的项目符号列表
`22:24` 说明需要做出哪些修改
`22:27` 这些反馈将最终进入循环
`22:30` 并返回给提案代理以调整其操作
`22:34` 并尝试考虑任何反馈意见
`22:38` 接下来可以将所有提示部分合并为单一提示
`22:41` 这就是批评者代理
`22:42` 稍后我们将用此定义代理的指令
`22:48` 现在架构批评者也需要工具
`22:51` 但架构批评者没有新工具
`22:53` 它可以使用提案代理相同的工具
`22:56` 我们只需将其包含在此列表中
`22:59` 获取已批准的用户目标
`23:00` 已批准的文件
`23:01` 然后是采样文件或搜索文件等实用工具
`23:08` 你已具备批评者代理所需的所有组件
`23:10` 现在可以创建该代理
`23:12` 它将是一个LLM代理
`23:14` 其名称为架构批评者代理
`23:16` 版本一
`23:17` 这个代理有一个之前未见过的新特性
`23:21` 与其他只需完成工作的代理不同
`23:23` 然后使用工具进行
`23:25` 你知道的
`23:25` 产生某些输出
`23:26` 这个代理只需返回文本
`23:30` 该文本将通过此输出键存入状态
`23:35` 因此此输出键我们称为反馈
`23:40` 当此代理完成时
`23:42` 当收到此代理的最终消息时
`23:47` 最终消息将使用反馈键存入状态
`23:49` 将包含由批评者生成的值
`23:52` 因为这会在之后出现
`23:54` 如果你回到提案者那里
`23:56` 提案将能够从状态中获取该信息
`24:01` 你需要定义两个关键组件
`24:03` 这是此细化循环中的两个关键子代理
`24:07` 现在我们必须定义细化循环本身
`24:10` 在我们这样做之前
`24:11` 还会有最后一个代理
`24:12` 这将是我们自定义的代理
`24:14` 如果我们回到架构图
`24:16` 我们有提案代理
`24:18` 批评代理
`24:19` 现在我们要添加这个检查状态和升级代理
`24:23` 这个代理只有一个任务
`24:24` 它的任务是决定循环是否完成
`24:28` 让我们看看它是如何工作的
`24:30` 这是一个名为base agent的基础类，我们将继承它
`24:34` 我们将其命名为检查状态和升级类
`24:37` 这将是一个自定义代理，带有自定义运行函数
`24:41` 运行函数将被调用
`24:43` 它会获取被调用时的当前上下文
`24:46` 我们不必过多关注这一点
`24:49` 除了查看当前会话状态
`24:51` 因此从该上下文中查找当前会话
`24:54` 查看状态
`24:55` 并获取反馈
`24:57` 如果有任何反馈
`24:58` 如果没有则假设反馈有效
`25:01` 如果批评代理没有反馈
`25:03` 我们将假设其有效
`25:05` 或者代理已声明有效
`25:07` 结果将是反馈有效，因此应停止
`25:11` 标志将根据
`25:12` 反馈是否包含有效内容
`25:15` 这是一个非常简单的检查
`25:16` 我们可以对此更复杂
`25:18` 但这样已经可行
`25:19` 对于大多数代理来说已经足够
`25:21` 如果所有条件成立
`25:22` 我们将生成一个由
`25:24` 当然这是我们构建的特殊代理
`25:27` 它将执行一个非常具体的操作
`25:29` 我们将在事件中添加一个名为升级的动作
`25:33` 升级的值将基于
`25:36` 是否应停止
`25:37` 所以应停止为真
`25:38` 如果当前反馈有效或为空
`25:41` 如果非空
`25:42` 则升级为假
`25:44` 这意味着我们仍会发出此事件
`25:46` 但升级不会发生，因为我们设置了升级为假
`25:50` 这足以跳出循环或继续循环
`25:55` 你现在可以创建循环代理了
`25:57` 循环代理是一个特殊的工作流代理，这里接收子代理列表
`26:02` 提案代理
`26:03` 评审代理
`26:03` 以及自定义检查状态和升级代理
`26:06` 并将它们放入循环中
`26:08` 这个过程完全不需要推理
`26:10` 它只是协调这些其他代理的执行
`26:13` 你可以看到这里有一个有趣的地方
`26:15` 它有一个新参数叫最大迭代次数
`26:18` 我们将其设为两
`26:19` 这意味着循环最多执行两次
`26:22` 因此要么我们已达成共识
`26:24` 评审代理同意提案有效
`26:27` 或若未达成则退出循环
`26:29` 我们不会无限循环
`26:30` 当执行完成后
`26:31` 结果将是确认的架构方案
`26:35` 或若未确认可能需要用户提供更多关于如何处理架构的输入
`26:41` 你可以创建这个代理二
`26:44` 现在可以为该代理创建执行环境
`26:47` 然后调用它并传入用户消息
`26:50` 这些文件现在如何导入与之前相同
`26:53` 当我们创建执行环境时
`26:55` 将初始化状态包含一些反馈
`26:58` 即为空
`26:58` 用户目标和已批准的文件
`27:01` 这里要运行的代理
`27:03` 不过
`27:03` 是将细化方案纳入细化循环
`27:06` 调用者
`27:07` 我们将传入用户消息
`27:08` 等待其执行完成
`27:09` 获取会话状态并查看
`27:13` 你可以看到这需要一些时间
`27:15` 因为我们实际会在最多两次迭代中循环
`27:19` 同时为了进行评估
`27:21` 有两个代理会检查这些文件
`27:24` 首先是
`27:24` 当然
`27:25` 提案代理会分析每个文件
`27:28` 并决定如何构建知识图谱
`27:30` 然后评审代理会查看相同文件集
`27:34` 判断提案是否有效
`27:37` 你可以看到我们从细化循环开始
`27:40` 进入提案代理v一
`27:42` 评审代理最终回到提案代理
`27:47` 再次回到评审代理
`27:49` 啊
`27:50` 这位评论家在这里仍不满意
`27:53` 最终回复是评论家表示反对
`27:55` 你必须重试
`27:56` 它对提案内容的某些方面感到不满
`28:00` 它认为存在关系重叠问题
`28:03` 数据可能不完整
`28:05` 我不打算全部阅读这些反馈
`28:07` 但这些反馈正是应回传给提案代理的
`28:11` 以真正提升结果
`28:13` 所以在此次调用中
`28:15` 尽管我们进行了两次迭代
`28:16` 通过优化循环
`28:18` 评论家仍未满意
`28:20` 这在整体架构中意味着循环终止
`28:25` 整体控制流程将返回给模式提案协调者
`28:30` 现在
`28:30` 这是需要人类参与的代理
`28:33` 因此协调者可以对人类说
`28:36` 不确定该生成什么模式
`28:38` 这是我目前的成果
`28:40` 你觉得我们下一步该怎么做
`28:41` 是否继续尝试
`28:42` 或者你有无需人类介入的建议吗
`28:45` 你可以再次运行此流程
`28:46` 你可以进入笔记本
`28:48` 重置这些单元格并重新运行
`28:50` 你可以尝试调整此处传递的信息
`28:54` 这些是你直接操作时会做的事
`28:57` 将有一个可选版本
`28:59` 你将直接进行
`29:00` 并整合这个顶层协调器供你交互
`29:04` 并实际看到通过单次优化或多轮优化
`29:08` 或你的建议
`29:09` 如何达成评论家和你都认可的模式提案
`29:13` 从而推进构建知识图谱
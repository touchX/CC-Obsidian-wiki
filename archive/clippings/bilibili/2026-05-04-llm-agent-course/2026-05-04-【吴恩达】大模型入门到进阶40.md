---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=40"
bvid: "BV1DfrdByE2H"
cid: "35394227827"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394227827&page=40&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 现在让我们专注于非结构化数据
`00:02` 你将设计两个专家代理来决定图模型
`00:06` 可以从markdown文件中提取的模型
`00:08` 我们开始吧
`00:10` 你已经构建了一个完整的流程来定义施工计划
`00:14` 可以从csv文件构建知识图谱
`00:17` 现在你可以转向非结构化数据流程
`00:21` 这个流程以类似方式开始，包含用户意图和文件建议代理
`00:25` 但现在针对markdown文件
`00:27` 这些代理是完整解决方案所需
`00:30` 但在本课中将模拟它们的输出
`00:32` 你将专注于新概念
`00:35` 实体和事实类型提案代理
`00:38` 实体和事实提案代理本身由两个专用子代理组成
`00:44` 命名实体识别或nr架构代理和事实类型提取代理
`00:51` 请注意这些代理的输出是知识提取的执行计划
`00:57` 并非执行提取本身
`00:59` 架构代理将读取markdown文件
`01:02` 寻找命名实体
`01:04` 这些是人物
`01:05` 文本中突出的事物或地点
`01:08` 在我们的示例中
`01:09` 你应期待与家具和评论相关的内容
`01:12` 事实类型提取代理是文本的二次分析
`01:17` 寻找关于这些事物的陈述类型
`01:20` 例如
`01:21` 某些家具存在共同问题
`01:25` 这些代理将制定支持用户目标的初始信息提取计划
`01:35` 然后它将打开
`01:36` AI设置并检查是否正常运行
`01:39` 看起来不错
`01:41` 我们也将检查new for是否可用
`01:43` 也准备好了现在
`01:46` 你首先要定义的代理是命名实体识别代理
`01:50` 命名实体识别是常见的自然语言处理操作，存在于许多框架中
`01:56` 事实证明
`01:56` 当然
`01:57` 大语言模型在语言处理方面非常出色
`01:58` 因此也非常擅长此任务
`02:00` 现在命名实体识别
`02:02` 正如所说
`02:03` 是指识别实体并为其命名
`02:07` 确实
`02:07` 这就是正在发生的事情
`02:09` 实体可以是任何人、地点或事物
`02:11` 一个地点或物品
`02:12` 在这里我们将要求llm寻找与
`02:15` 用户目标相关的文本中可发现的内容
`02:19` 你将从定义代理的指令开始
`02:22` 和之前一样
`02:23` 我们将将其拆分为几个不同部分，这些部分随后将组合在一起
`02:26` 首先
`02:27` 定义亚洲的角色和目标
`02:29` 在这里你要描述代理是一个顶级算法
`02:32` 专为进行自然语言处理而设计
`02:35` 其目标是在文本中找到命名实体
`02:39` 但它实际上不会提取这些实体
`02:41` 这里的目的是让其识别可用的实体类型
`02:46` 然后你可以给代理更多提示
`02:48` 让它明确我们在这里寻找的内容，再次
`02:51` 我们将描述什么是实体
`02:53` 然后将其分为两类实体
`02:57` 我们将描述这里所说的知名实体
`03:00` 这些是众所周知的
`03:01` 因为它们存在于我们之前课程中描述的结构化数据中
`03:06` 所以如果该结构化数据
`03:07` 如果其中定义的某些实体出现在文本中
`03:10` 我们希望文本中的这些部分也被提取
`03:13` 此外我们还有所谓的发现实体
`03:16` 这些实体可能不存在于我们描述的图数据中
`03:21` 但可能符合用户的使用目标
`03:23` 如果它们频繁出现
`03:24` 可能就有用处
`03:27` 然后我们提供更多细节关于
`03:28` 了解什么是知名实体的设计规则
`03:32` 以及什么是发现实体
`03:34` 和之前一样通常给出几个例子帮助代理理解目的
`03:42` 最终需要整合的指令部分是思维链方向
`03:46` 这里我们将描述如何准备执行当前任务
`03:50` 即识别这些实体
`03:52` 这里需要使用的工具来确定其职责
`03:56` 提供完整上下文
`03:57` 然后是建议的步骤序列
`04:01` 它知道可用的文件
`04:02` 有一个文件采样工具
`04:04` 先使用该工具查看部分文件
`04:07` 并发现已知实体
`04:10` 同时发现频繁提及的新实体
`04:13` 通过这些整理出所有合适的实体列表
`04:18` 使用建议实体工具获取该列表
`04:21` 和之前一样返回用户确认
`04:24` 这看起来正确吗
`04:26` 然后会调用单独的审批工具
`04:29` 这里称为审批建议实体工具
`04:32` 然后将所有内容组合成单个字符串
`04:35` 这将成为命名实体识别代理的指令
`04:40` 定义完指令后
`04:41` 再提供工具本身的定义
`04:44` 这些将非常直接
`04:46` 这里并没有太多巧妙的操作
`04:48` 我们将要定义的工具
`04:50` 遵循我们之前课程中的相同模式
`04:52` 我们将让代理首先提出一些内容
`04:55` 然后有其他工具将这些提案转化为批准版本
`05:00` 所以在这里
`05:01` 我们提出的事项是首先提案然后批准的实体列表
`05:05` 然后你也可以
`05:06` 当然
`05:07` 获取这些提案或批准结果
`05:11` 下一个需要的工具稍有不同
`05:13` 这将是另一个获取工具
`05:15` 它将从提议的方案中获取已知类型
`05:20` 在之前的课程中
`05:21` 从结构化数据中
`05:23` 因此已知类型最终将成为节点定义的标签
`05:27` 在之前的提议方案中
`05:29` 它们将作为批准标签使用
`05:32` 我们将从建设计划中提取并返回为列表
`05:38` 你可以导入之前课程中定义的预设工具
`05:42` 并将此处的新增工具合并为单个列表
`05:46` 这就是我们在代理定义中使用的内容
`05:49` 现在为了了解代理的工作内容
`05:51` 可以使用样本文件函数
`05:53` 直接调用其中一个可用文件并查看内容
`05:59` 查看这个markdown文件
`06:00` 可以看到这是大量以markdown格式的评论
`06:03` 包含标题
`06:04` 还有一些嵌入在此处的值
`06:06` 比如评分
`06:07` 可以看到有用户名
`06:09` 地点
`06:10` 还有
`06:10` 当然还有评论本身的内容
`06:12` 这里只有少量评论
`06:16` 但足以演示整个流程
`06:21` 好的
`06:21` 你已定义好指令
`06:23` 现在也定义好了工具
`06:25` 现在可以构建代理本身来运行此代理
`06:29` 因为它属于更长的工作流程
`06:31` 它对状态中已积累的内容有假设
`06:34` 因此需要创建初始状态来测试代理
`06:38` 初始状态需要包含批准后的用户目标
`06:43` 此处的批准文件
`06:44` 可以看到markdown文件
`06:45` 它将查看
`06:46` 然后还需要建设计划
`06:48` 建设计划用于已知实体类型
`06:53` 注意我们省略了关系建设部分
`06:55` 仅仅因为这一步不需要那个内容
`06:57` 所以这只是在模拟你现在需要的操作
`07:01` 你已经准备好运行这个代理了
`07:03` 我们将使用helper模块中的make_agent_caller
`07:06` 并发送一个简单请求
`07:09` 我们只需告诉它
`07:10` 你知道的
`07:11` 嘿，代理
`07:11` 你能完成你的工作吗
`07:12` 将产品评论添加到知识图谱中，将产品投诉反馈到制造流程
`07:17` 如果在这里遇到任何问题
`07:19` 当然你可以运行这个替代版本
`07:22` 这是一个非常简短的句子
`07:23` 但这里包含debug逗号true
`07:25` 这会显示所有输出
`07:27` 当代理完成时
`07:28` 大致展示它想要提出的方案
`07:30` 并生成提案
`07:31` 我们将检查会话状态以确认已生成提案
`07:35` 并且
`07:36` 希望它不会自动批准
`07:38` 它应该在等待我们确认
`07:39` 是的
`07:39` 看起来不错
`07:41` 这个过程需要几分钟
`07:43` 因为代理会离开
`07:44` 查看这些不同文件
`07:46` 并尝试推断
`07:48` 认为最适合实体的标签集合
`07:52` 好的
`07:53` 完成得相当不错
`07:54` 它识别出这里有产品
`07:56` 存在报告的问题
`07:58` 对常规体验的投诉
`08:00` 组装说明
`08:01` 以及可能的客户反馈
`08:03` 可能存在一点冗余
`08:04` 但对我来说已经足够好了
`08:06` 这也是重要的
`08:08` 不仅响应良好
`08:09` 还正确更新了会话状态
`08:12` 记忆记录正确
`08:13` 我们有了提议的实体
`08:15` 但我们尚未获得批准
`08:17` 我得到了良好的结果
`08:19` 如果还没运行过请随时再次执行该单元
`08:21` 看看是否能得到更好的实体集合
`08:24` 所有设置均假设在交互式环境中
`08:27` 但如果有效
`08:28` 你可以继续向同一代理发送新消息
`08:32` 我批准这些提议的实体
`08:35` 完成这一步后
`08:36` 我们应该确认能够将提议的实体转换为已批准实体
`08:40` 并在会话状态中完美呈现这一点
`08:45` 已批准实体与之前提议的内容完全匹配
`08:50` 然后可以继续处理第二个代理
`08:53` 这将是我们的事实类型提取子代理
`08:56` 正如名称所示
`08:57` 当然
`08:58` 与前一个代理类似
`09:00` 我们将寻找可提取的事物类型
`09:02` 我们不会实际执行提取操作
`09:04` 可以先从指令开始
`09:06` 因为这是这两个代理最重要的部分
`09:09` 对于这个代理
`09:10` 角色和目标再次说明
`09:12` 我们将称这个代理为顶层算法
`09:14` 它将进行文本分析
`09:17` 但目标略有不同
`09:19` 它将寻找可提取的事实类型
`09:22` 不要实际提取这些事实
`09:24` 只需确定可能的事实类型
`09:27` 以帮助下级代理理解我们讨论的内容
`09:30` 这里
`09:30` 我们将提供一些提示
`09:32` 这里的关键部分是
`09:34` 与之前代理的指令类似
`09:36` 前一个代理不要提出具体事实
`09:40` 而是提出与用户目标相关的一般事实类型
`09:44` 在此处提供示例总是个好主意
`09:47` 我的示例是不要提出abk喜欢咖啡
`09:50` 而是提出一个人喜欢饮料的一般事实类型
`09:54` 这些形成非常简洁的句子
`09:56` 我们将这些句子称为三元组
`09:58` 它们的形式为主语
`10:00` 谓语-宾语
`10:01` 在这里我们将其放在括号中
`10:04` 这是常见做法
`10:05` 我们将观察代理是否采用此格式并返回结果
`10:09` 在响应中
`10:10` 我们提供额外的设计规则以指导思考和查找内容
`10:14` 同时说明如何使用工具与前一个代理不同
`10:18` 前一个代理在此处有一个完整的提议实体列表
`10:23` 针对每个独立事实
`10:24` 我们将逐一添加为独立事实
`10:26` 一次一个
`10:27` 而非提供一组提议的事实集合
`10:32` 关键在于代理的行为方式
`10:36` 以及token成本，更多往返次数
`10:40` 会增加一定费用
`10:42` 但如果结果更好
`10:44` 那或许是个不错的权衡
`10:47` 最后你可以继续添加思维链方向
`10:50` 和之前一样，我们将遵循这些方向的固定模式
`10:55` 这是任务准备方法
`10:57` 这是我们建议的分步执行方案
`11:01` 以这种方式使用工具
`11:03` 抽样部分文件
`11:04` 寻找与文本相关的主体和客体
`11:08` 然后为这些主体和客体添加建议事实
`11:11` 最终你会得到几句话
`11:15` 三词短句
`11:18` 然后将所有内容整合成一个字符串
`11:21` 我们现在准备进入工具定义
`11:24` 现在
`11:24` 我将花更多时间分析工具定义
`11:26` 针对事实类型提取代理
`11:29` 因为它在过程中会做一些合理性检查
`11:33` 这也是将它们分代理的意图之一
`11:36` 在之前的代理中
`11:37` 命名实体识别代理
`11:39` 我们给了它如何查找实体的指导
`11:42` 但并未严格限制其发现内容
`11:45` 我们将非常严格
`11:46` 所提议的事实必须与已有实体类型匹配
`11:51` 由前代理定义的类型
`11:53` 这种拆分方式
`11:54` 能提供更好的保障
`11:56` 确保事实与实体类型一致
`11:59` 正如我们所希望的
`12:02` 查看提议事实的定义时
`12:05` 我们得到的是
`12:06` 甚至通过参数名称
`12:08` 是已批准的主体标签
`12:11` 提议的谓词标签
`12:12` 然后是已批准的客体标签
`12:14` 这再次形成三元组结构
`12:17` 主体和客体应已由前代理生成
`12:21` 我们将在此设置一些限制
`12:24` 我们将进行检查
`12:25` 已批准的实体
`12:26` 从状态中获取
`12:27` 确保传入的主体和客体标签
`12:31` 在列表中存在
`12:33` 若不存在则触发工具调用错误
`12:37` 并提示标签不存在
`12:39` 让它重新尝试
`12:41` 我们分步处理的原因之一
`12:43` 以便逐步调整
`12:45` 逐步修正代理行为
`12:49` 如果这些内容看起来都没问题
`12:50` 然后我们将其重新整理成一个三元组
`12:53` 将其保存到当前谓词列表并生成事实列表
`12:58` 这里的另外两个函数只是遵循常规模式
`13:01` 获取提议的事实列表
`13:03` 然后将提议的事实添加到已批准的事实列表
`13:07` 你可以将这些工具列表整合起来
`13:11` 随时可供事实代理使用
`13:13` 现在我们可以开始构建了
`13:15` 构建代理
`13:16` 既然我们已经完成了所有繁重工作
`13:18` 真正定义事物其实非常直接
`13:20` 现在你可以运行它了
`13:23` 这个代理的初始状态与之前代理相同
`13:28` 我们将利用这一点
`13:29` 直接复制前代理的最终状态作为当前代理
`13:34` 在完整的多代理系统中
`13:36` 这些操作会按顺序执行
`13:37` 它们会共享同一状态
`13:40` 但你也可以单独运行它们
`13:42` 我们将进行复制
`13:43` 然后创建代理的调用者
`13:46` 我们将使用的启动消息
`13:49` 是要求其生成提议
`13:52` 这里输出内容很多
`13:53` 所以我将改用这种方式
`13:56` 去掉末尾的逗号true
`13:58` 这样代理可以正常工作而不显示全部输出
`14:01` 如果遇到问题
`14:03` 使用带有逗号true的版本
`14:05` 当代理完成所有事实提议后
`14:09` 我们将检查会话状态
`14:11` 假设它已生成提议
`14:14` 若未生成则发出警告
`14:16` 同时假设未批准该提议
`14:19` 希望一切顺利
`14:22` 好的
`14:23` 查看它生成的提议
`14:25` 提出的事实
`14:26` 例如产品存在问题及收到客户反馈
`14:31` 产品有组装时间
`14:33` 这很有趣
`14:35` 这里有很多不同内容
`14:36` 看起来是个不错的列表
`14:38` 检查会话状态啊
`14:41` 这里出现了问题
`14:43` 它确实返回了提议
`14:45` 但未将其保存到会话状态
`14:48` 为什么会这样
`14:50` 看看是否有投诉
`14:51` 这确实令人遗憾
`14:54` 因此决定正式提出这个提案并采用纯文本
`14:57` 但它并未真正实现我们预期的工具调用功能
`15:01` 所以
`15:01` 我提议
`15:02` 你继续运行这个流程
`15:03` 当然在完整的智能体系统中
`15:06` 当它提出这种建议时
`15:08` 最终我们发现没有生成事实提案
`15:12` 我们将自动重新尝试
`15:14` 这次重运行应该能顺利执行
`15:17` 因为它会重新初始化状态
`15:18` 然后按相同步骤继续
`15:23` 好的
`15:23` 我们看到效果类型与之前基本一致
`15:28` 符合用户目标
`15:30` 询问我们是否继续
`15:32` 让我们看看是否真的
`15:34` 已正确提出这些事实
`15:36` 但仍在等待批准
`15:37` 好的
`15:38` 智能体干得好
`15:39` 加一分
`15:40` 这次同样
`15:43` 如果这次不成功
`15:44` 只需再次运行
`15:45` 取决于OpenAI的状态
`15:47` 在任何一天
`15:48` 可能第一次或第二次成功
`15:50` 通常不需要三次
`15:52` 我将发送另一条消息
`15:54` 批准该提案
`15:55` 这将直接转为已批准事实
`16:01` 查看会话状态
`16:02` 最终看到已批准的事实类型，完美
`16:06` 这次运行效果很好
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=5"
bvid: "BV1DfrdByE2H"
cid: "35394160935"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394160935&page=5&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 让我们来看一些Enic AI应用的例子
`00:04` 许多企业执行的任务之一是发票处理
`00:09` 所以给定这样一张发票
`00:11` 你可能需要编写软件提取最重要的字段，对于这个应用场景
`00:16` 支柱可能是技术解决方案
`00:19` 公司地址
`00:20` 应付金额为三千美元
`00:22` 并到期日看起来是2025年8月
`00:27` 在许多财务部门
`00:29` 也许人工会查看发票并识别最重要的字段
`00:35` 我们需要在
`00:36` 何时记录这些信息以确保及时付款
`00:41` 如果使用Enic工作流实现
`00:44` 你可以这样做
`00:46` 你可能输入一张发票
`00:47` 然后调用PDF转文本API将PDF转换为格式化文本
`00:54` 例如Markdown文本供语言模型处理
`00:56` 然后语言模型会分析PDF并判断这是否是实际发票
`01:01` 或是否为其他类型的文档应忽略
`01:04` 如果是发票
`01:05` 则提取所需字段
`01:08` 同时调用API或工具更新数据库
`01:13` 以保存最重要的字段到数据库记录
`01:17` 此工作流的一个方面是存在明确的流程遵循
`01:23` 识别所需字段并记录到数据库
`01:26` 这类有明确流程的任务
`01:28` 可能更适合由自主工作流执行
`01:32` 因为它能可靠地分步完成任务
`01:37` 这里还有一个稍难的例子
`01:39` 如果你想构建处理基础客户订单查询的代理
`01:45` 可能需要提取关键信息
`01:48` 确定客户具体订购了什么
`01:51` 客户的姓名是什么
`01:52` 然后查找相关客户记录
`01:55` 最后草拟回复供人工审核后再发送邮件
`02:02` 同样
`02:03` 这里存在明确的流程
`02:04` 我们将分步实施：接收邮件
`02:07` 输入语言模型进行验证
`02:09` 或提取订单详情，假设客户邮件涉及订单
`02:14` 语言模型可能调用数据库获取信息
`02:20` 这些信息再传递给语言模型草拟邮件回复
`02:24` 语言模型可能使用请求-响应工具
`02:29` 将草拟的邮件放入人工审核队列
`02:33` 审核通过后即可发送
`02:36` 这类客户订单查询代理正在许多企业中部署
`02:42` 来看一个更具挑战性的例子
`02:44` 如果你想构建客服代理回应
`02:47` 不仅仅是客户下单的提问
`02:50` 而是回应更广泛的问题集
`02:53` 客户可能提出的问题
`02:55` 也许客户会询问
`02:57` 你们有黑色牛仔裤或蓝色牛仔裤吗
`03:00` 要回答这个问题
`03:01` 可能需要多次调用数据库API
`03:06` 首先检查黑色牛仔裤库存
`03:08` 然后检查蓝色牛仔裤库存
`03:09` 再向客户作出回应
`03:11` 这是一个需要更复杂查询的例子，根据用户输入
`03:15` 实际上需要规划数据库查询的顺序以检查库存
`03:20` 或者当用户询问
`03:22` 我要退货我买的沙滩毛巾
`03:24` 要回答这个问题
`03:26` 可能需要验证客户是否确实购买了沙滩毛巾
`03:30` 然后再次核对退货政策
`03:32` 可能您的退货政策仅限购买后30天内
`03:35` 且毛巾必须未使用
`03:37` 如果允许退货
`03:38` 则让代理开具退货凭证
`03:41` 并将数据库记录设为退货待处理
`03:45` 在这个例子中
`03:46` 如果处理客户请求所需的步骤事先未知
`03:51` 就会导致更复杂的流程
`03:54` LM基应用需自行决定
`03:57` 这些是完成任务所需的关键步骤
`04:03` 但你将了解最新研究如何解决此类问题
`04:08` 太
`04:08` 再举一个例子
`04:10` 这可能是最难构建的视觉类型
`04:13` 有很多关于代理使用的计算机研究
`04:16` 其中代理会尝试使用网页浏览器和阅读网页
`04:21` 以执行复杂任务
`04:24` 在这个例子中
`04:25` 我让代理检查两家特定联合航空公司的座位是否可用
`04:30` 从旧金山到华盛顿特区的航班
`04:33` 或DCA机场
`04:34` 代理可以访问网页浏览器来执行此任务
`04:39` 在视频中可以看到代理独立浏览联合网站
`04:43` 点击页面元素并填写文本
`04:46` 查看页面内容以执行我的搜索请求
`04:50` 代理分析页面内容
`04:53` 确定完成任务所需的操作及下一步行动
`04:57` 在此案例中联合网站查询遇到困难
`05:01` 于是转而访问Google航班网站搜索可用航班
`05:07` 这里发现多个符合用户查询的航班选项
`05:11` 代理选择其中一个并返回联合网站
`05:16` 此时显示在正确网页上
`05:19` 因此能确定
`05:22` 我询问的航班座位有空位
`05:25` 计算机使用是当前激动人心的研究前沿领域
`05:29` 许多公司正试图让计算机使用代理发挥作用
`05:32` 而你刚才看到的代理最终还是找到了答案
`05:35` 我经常看到代理在使用网页浏览时遇到困难
`05:39` 例如
`05:40` 如果网页加载速度慢
`05:41` 代理可能无法理解发生了什么
`05:43` 许多网页仍超出代理准确处理或阅读的能力
`05:49` 但我觉得计算机使用代理虽然目前还不足以可靠应用
`05:54` 关键应用领域今天仍是未来发展的激动人心且重要方向
`05:58` 所以当我考虑构建遗传AI工作流时
`06:02` 顶部也会更简单
`06:04` 通常会是步骤最明确的流程
`06:08` 或者企业已有标准操作程序需要遵循
`06:13` 然后需要将该流程
`06:15` 编码成AI代理
`06:18` 但这样往往能实现更易实施
`06:21` 使用纯文本资产会更容易
`06:25` 因为大型语言模型擅长处理文本
`06:29` 如果需要处理其他输入模态
`06:32` 可能也是可行的
`06:33` 但可能难度稍高
`06:37` 如果任务步骤无法提前预知
`06:40` 如更高级客服代理所需步骤
`06:43` 代理可能需要边执行边规划
`06:46` 这往往更难且不可预测
`06:50` 然后
`06:50` 正如我之前提到的
`06:51` 如果需要处理丰富多模态输入
`06:53` 如声音和视觉
`06:55` 音频处理可靠性通常低于纯文本
`07:00` 所以这能让你了解可能构建的工作流类型
`07:07` 在自行实现这类应用时
`07:09` 最重要的技能是分析复杂工作流
`07:13` 并拆解出具体步骤
`07:15` 以便在遗传工作流中逐个执行
`07:19` 接下来视频我们将一步步讲解
`07:22` 我们将讨论任务分解，即面对复杂任务
`07:27` 如撰写研究报告或让客服代理回复客户
`07:31` 如何将其拆解为离散步骤以实现工作流
`07:37` 让我们进入下一视频看看
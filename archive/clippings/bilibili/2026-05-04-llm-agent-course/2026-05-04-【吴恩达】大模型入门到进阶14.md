---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=14"
bvid: "BV1DfrdByE2H"
cid: "35394162198"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394162198&page=14&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 在本模块中，你将学习如何使用LMS
`00:03` 这意味着允许你的模型自行决定何时可能需要请求调用函数
`00:08` 以执行某些操作
`00:09` 或获取某些信息
`00:11` 或执行其他操作
`00:12` 就像我们
`00:13` 人类可以借助工具完成远超自身能力的事
`00:17` 仅凭双手无法做到
`00:18` 手臂
`00:19` 也是如此
`00:19` 同样，通过访问工具也能完成更多任务
`00:23` 但与其使用锤子、扳手和钳子
`00:26` 当我们提供工具时
`00:28` 即让模型请求调用的函数
`00:31` 正是这使得模型能实现更多功能
`00:33` 让我们来看一个例子
`00:34` 如果你想询问一个已训练的LLM
`00:37` 可能是在几个月前训练的
`00:38` 现在时间是什么
`00:40` 嗯
`00:40` 该训练模型并不确切知道当前时间
`00:44` 因此希望你的回应
`00:46` 抱歉
`00:46` 我没有当前时间的访问权限
`00:48` 但如果你编写一个函数并赋予模型调用该函数的权限
`00:52` 则能让模型生成更有用的回答
`00:56` 当我们允许模型调用函数时
`00:58` 或更准确地说，允许请求调用函数
`01:01` 这就是我们所说的工具使用
`01:03` 而工具仅仅是提供给模型的函数
`01:06` 模型可以详细请求调用
`01:09` 这就是本例中的工具使用方式
`01:12` 我将向模型提供获取当前时间的函数
`01:15` 如之前幻灯片中展示的那样
`01:18` 当你随后提示模型
`01:19` 现在几点了
`01:20` 模型可以决定调用获取当前时间的函数以返回当前时间
`01:25` 该结果将反馈到对话历史中供模型使用
`01:29` 最终模型输出可能是'下午3:20'
`01:33` 步骤序列如下
`01:35` 输入提示是否
`01:36` 在此案例中，模型会查看可用工具集
`01:40` 本例中仅有一个工具
`01:42` 但会检查所有可用工具
`01:44` 并决定在此例中调用该工具
`01:47` 该工具是一个函数
`01:48` 随后返回一个值
`01:49` 该值被反馈给模型
`01:51` 最后模型生成输出
`01:54` 现在工具使用的一个重要方面
`01:58` 我们可以让模型自行决定是否使用任何工具
`02:03` 所以对于相同的设置
`02:04` 如果我要问绿茶中的咖啡因含量是多少
`02:07` 模型不需要知道当前时间就能回答这个问题
`02:10` 因此可以直接生成答案
`02:12` 绿茶含有这种量的咖啡因
`02:14` 并且无需调用我幻灯片中的获取当前时间函数
`02:19` 我将使用这个带横线的方框标注在模型上方
`02:24` 表示我们为模型提供了一组工具
`02:27` 供模型在适当时候选择使用
`02:30` 这与之前视频中的一些示例不同
`02:33` 作为开发者我已硬编码了
`02:37` 例如在研究代理的这个阶段我总是进行网络搜索
`02:41` 与获取当前时间函数调用不同
`02:45` 是否请求调用取决于你的决定
`02:49` 向获取当前时间函数发起调用
`02:51` 同样
`02:52` 我们将使用这个方框标注表示
`02:55` 当我们提供一个或多个工具给模型
`03:00` 让模型决定是否调用
`03:02` 这里还有一些更多示例
`03:04` 何时使用
`03:05` 可能帮助应用生成更好的答案
`03:08` 如果你询问它
`03:09` 能否在加州山景城找到意大利餐厅
`03:11` 如果它具备网络搜索工具
`03:14` 模型可能会选择调用网络搜索引擎
`03:18` 查询山景城的餐厅
`03:20` 并利用获取的结果生成输出
`03:23` 或者如果你经营零售店
`03:26` 是否希望回答问题
`03:29` 例如展示购买过白色太阳镜的客户
`03:31` 如果模型被授予访问数据库查询权限
`03:35` 则可以查询销售表中的相关条目
`03:39` 查找购买过白色太阳镜的记录
`03:41` 然后利用这些数据生成输出
`03:44` 最后
`03:45` 如果你想进行利率计算
`03:47` 如果我存入五百美元
`03:49` 十年后利率为百分之五
`03:52` 我会得到多少
`03:53` 如果你拥有利率计算工具
`03:57` 则可以调用该函数进行计算
`04:01` 或者你会发现后续内容中让模型编写代码
`04:06` 我们只需编写这样的数学表达式
`04:09` 然后进行计算
`04:11` 这也是让模型计算正确答案的方式
`04:16` 作为开发者
`04:17` 你需要思考应用需要实现哪些功能
`04:20` 应用程序真正需要执行的任务
`04:22` 然后需要创建所需的函数或工具，使其对语言模型可用
`04:28` 让其能够调用合适的工具完成任务
`04:31` 比如餐厅推荐或零售问答系统
`04:36` 或者财务助手可能需要这样做
`04:39` 根据你的应用场景，可能需要实现并提供不同的工具给语言模型
`04:46` 到目前为止
`04:46` 我们之前讨论的示例大多只提供一个
`04:50` 或两个/一个函数供语言模型使用
`04:52` 但在许多用例中需要为语言模型提供多个工具或函数
`04:57` 以便它能够选择调用其中任意一个
`04:59` 例如
`05:00` 如果你在构建日历助手代理
`05:03` 可能需要让它执行请求处理
`05:05` 比如请在日历中查找周四的空闲时段
`05:09` 并与艾丽丝预约
`05:11` 在这个例子中，我们可以为语言模型提供
`05:15` 创建预约的工具，即发送日历邀请
`05:18` 检查日历状态
`05:19` 查看何时有空闲时间，以及删除预约
`05:22` 如果需要取消现有日历条目
`05:26` 根据指令集
`05:28` 系统会首先决定从可用工具中
`05:31` 最可能首先使用的是检查日历
`05:35` 调用检查日历函数以返回周四的空闲时间
`05:40` 基于这些信息
`05:41` 将反馈结果返回给语言模型
`05:43` 然后可以决定下一步选择时段
`05:47` 比如下午三点，再调用预约函数
`05:51` 发送日历邀请
`05:53` 同时邀请艾丽丝并添加到我的日历
`05:56` 该操作的输出应为确认信息
`05:59` 表明日程已成功发送
`06:02` 反馈给语言模型
`06:03` 最后语言模型会告知用户
`06:05` 您的预约已与艾丽丝在周四下午三点安排妥当
`06:08` 赋予语言模型工具访问权限非常重要
`06:12` 这将显著增强应用功能
`06:15` 在下一视频中
`06:16` 我们将学习如何编写函数
`06:18` 如何创建工具
`06:19` 并使其对语言模型可用
`06:22` 让我们进入下一视频
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=4"
bvid: "BV1DfrdByE2H"
cid: "35394161008"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394161008&page=4&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 我认为自主工作流最大的优势在于
`00:03` 它能有效完成仅靠个人电脑无法实现的多项任务
`00:08` 当然还有其他优势
`00:10` 包括并行处理能力，能让某些任务快速完成
`00:13` 以及模块化设计，可整合不同组件的最佳部分
`00:17` 从多个来源构建高效工作流
`00:20` 让我们看看具体案例
`00:21` 我的团队收集了一些编码基准测试数据
`00:25` 用于测试不同模型编写代码完成特定任务的能力
`00:30` 这个基准测试称为Human Eval
`00:33` 结果显示GPT-3.5
`00:36` 这是首个公开版本的Chi GPU所基于的模型
`00:40` 若直接要求它编写代码
`00:43` 计算机程序在该基准测试中正确率仅40%
`00:46` 这是Parse K指标
`00:48` GPT-4表现更优
`00:50` 准确率跃升至67%
`00:53` 即使使用非自主工作流
`00:55` 但改进幅度如此之大
`00:59` 从GPT-3.5到GPT-4
`01:01` 这种提升可通过包装GPT-3.5
`01:05` 并结合自主工作流技术实现
`01:10` 这部分内容稍后会讲到
`01:11` 在本课程中
`01:12` 可提示GPT-3.5编写代码
`01:15` 然后反思代码并优化改进
`01:18` 通过这类技术
`01:20` 实际上能让GPT-3.5达到更高性能
`01:24` 同样GPT-4在自主工作流中表现更佳
`01:30` 所以
`01:31` 即使使用当今最佳语言模型
`01:36` 自主工作流仍能显著提升性能
`01:42` 事实上我们在此例中看到的
`01:44` 模型迭代带来的改进
`01:49` 仍不及在旧模型上应用自主工作流
`01:55` 另一个优势是能并行处理任务
`01:58` 从而比人类更快完成某些工作
`01:59` 例如
`02:03` 要求自主工作流撰写黑洞论文
`02:09` 可同时运行三个模型生成搜索关键词
`02:11` 输入搜索引擎
`02:12` 基于首次搜索结果
`02:13` 筛选出
`02:14` 三个最佳结果
`02:17` 根据第二次搜索识别第二组网页
`02:22` 依此类推
`02:22` 结果发现
`02:24` 人类需按顺序阅读九个网页
`02:29` 或逐一处理
`02:32` 可并行下载所有九个网页
`02:36` 然后最后将所有这些内容输入现在来撰写一篇论文
`02:40` 因此尽管工作流确实比纯非生成式工作流耗时更长
`02:45` 或者通过直接生成
`02:46` 并且只需提示一次
`02:48` 如果要比较这种生成式工作流
`02:51` 与人类完成任务的方式
`02:54` 瘫痪能力
`02:55` 下载大量网页实际上可以让它完成某些任务
`02:59` 比单个人类按顺序处理数据构建的方式快得多
`03:05` 在这个示例中
`03:06` 我发现构建工作流时经常做的一件事
`03:10` 是查看各个组件
`03:12` 比如语言模型并添加或替换组件
`03:16` 例如
`03:17` 我可能会检查这里使用的网络搜索引擎
`03:20` 并决定替换为新的网络搜索引擎
`03:24` 在构建通用工作流时
`03:25` 实际上存在多个网络搜索引擎
`03:27` 包括谷歌
`03:28` 可通过Surper访问
`03:30` 以及如必应、DuckDuckGo、TiYu.com等
`03:34` 实际上有很多专为MS设计的网络搜索引擎选项
`03:38` 或者除了进行三次网页搜索
`03:41` 或许在此步骤可替换为新的新闻搜索引擎
`03:45` 以便获取近期突破的最新新闻
`03:49` 关于黑洞信号
`03:51` 最后，不再使用同一语言模型处理所有步骤
`03:56` 我通常会尝试不同的大型语言模型
`04:00` 并尝试不同的模型提供商
`04:03` 以观察哪个在系统不同步骤中效果最佳
`04:07` 总结来说
`04:09` 我使用生成式工作流的主要原因
`04:11` 是显著提升多种应用的性能
`04:15` 此外
`04:16` 还能并行处理人类需按顺序完成的任务
`04:22` 许多生成式工作流的模块化设计也允许我们添加更新工具
`04:28` 有时替换模型
`04:30` 我们将详细讨论构建工作流的关键组件
`04:34` 现在让我们看看一系列AI应用
`04:38` 让你了解人们已构建的类型
`04:42` 以及你可以自行构建的类型
`04:44` 我们进入下一视频
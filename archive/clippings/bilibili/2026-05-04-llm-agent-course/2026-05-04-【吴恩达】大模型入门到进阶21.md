---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=21"
bvid: "BV1DfrdByE2H"
cid: "35394224881"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394224881&page=21&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 我发现对于许多开发者来说，只有通过观察多个示例
`00:04` 你才能进行练习并磨练自己对错误分析的直觉
`00:09` 那么让我们再看看两个更多示例
`00:12` 我们将分析发票处理和回复客户邮件的场景
`00:16` 这是我们设计的发票处理流程
`00:19` 其中包含明确的步骤遵循特征化工作流，识别四个必要字段
`00:25` 然后将数据录入数据库
`00:28` 在示例中
`00:29` 从本模块的第一个视频
`00:31` 我们提到系统经常在发票到期日判断上出错
`00:36` 因此我们可以进行分析
`00:37` 试图确定可能出错的组件
`00:41` 所以
`00:42` 例如
`00:42` PDF转文本是否出错
`00:44` 或是否出错
`00:44` 语言模型是否提取了错误日期
`00:47` 所有来自PDF转文本组件的输出
`00:50` 传递给卡尔并进行错误分析
`00:53` 我会寻找数据提取错误的多个示例
`00:58` 如同前一视频所示，重点分析表现不佳的案例
`01:03` 试图找出这些案例出错的原因
`01:05` 忽略日期正确的示例
`01:08` 但需收集十到一百份发票
`01:11` 其中日期有误
`01:12` 然后我会逐一检查以确定
`01:15` 问题根源是否是PDF转文本获取日期错误
`01:19` 或是语言模型在处理PDF转文本输出时
`01:23` 提取了错误日期
`01:25` 或许可以制作类似表格并分析两份发票
`01:29` 统计PDF转文本
`01:32` 是否错误提取日期或文本
`01:35` 导致人类也无法辨认到期日
`01:38` 与PDF文本清晰可辨的情况相比
`01:40` 但语言模型却错误提取了日期
`01:44` 比如误将发票日期识别为到期日
`01:48` 在此示例中
`01:49` 数据提取环节导致了更多错误
`01:53` 这表明我应优先优化数据提取组件
`01:57` 而非PDF转文本环节
`01:59` 这一点至关重要，因为若没有错误分析
`02:02` 某些团队可能耗费数周甚至数月调优PDF转文本
`02:08` 最终发现这些努力对系统性能影响微乎其微
`02:14` 哦
`02:14` 顺便说一句
`02:15` 底部的百分比可能不加到100%
`02:19` 因为这些错误并非互斥
`02:22` 最后看一个案例
`02:23` 回到客户邮件回复的工作流
`02:28` 其中语言模型接收客户邮件
`02:31` 像这样
`02:32` 下达指令
`02:33` 调取订单详情
`02:36` 从数据库获取信息
`02:38` 然后草拟供人工审核的回复
`02:40` 所以我又找到了一些例子
`02:43` 无论出于什么原因
`02:44` 最终输出结果令人不满意
`02:47` 然后尝试找出哪里出错了
`02:50` 可能出现的问题包括
`02:52` 可能模型生成了错误的数据库查询
`02:56` 当查询发送到数据库时
`02:58` 未能成功获取客户信息
`03:02` 或者数据库存在数据损坏
`03:05` 即使模型编写了完全合适的SQL查询
`03:09` 查询语言部分
`03:10` 数据库没有正确信息
`03:13` 或者根据客户订单的正确信息
`03:16` 模型编写了邮件内容却有所偏差
`03:20` 再次说明
`03:20` 我会查看几封邮件
`03:22` 最终输出不满意的案例并分析问题
`03:26` 比如一封邮件
`03:28` 发现模型在查询中调用了错误的表
`03:31` 在邮件二中错误查询了数据
`03:35` 可能发现数据库本身存在错误
`03:38` 在给定输入的情况下
`03:40` 模型甚至编写了子邮件等
`03:44` 在这个案例中
`03:46` 经过多封邮件分析后
`03:48` 可能发现最常见的错误在于
`03:52` 模型编写数据库查询的方式
`03:54` 你说SQL查询
`03:55` 为了获取相关数据
`03:58` 而数据库本身基本正确
`04:00` 仅存在少量数据错误
`04:02` 模型生成邮件的方式也有问题
`04:05` 可能表达不够准确
`04:07` 大约30%的时间
`04:08` 这表明改进方向
`04:10` 可能需要优化模型的查询编写方式
`04:14` 其次
`04:14` 最关键的是
`04:15` 可能需优化高阶提示词
`04:18` 撰写最终邮件
`04:19` 此类分析能显示75%的错误
`04:23` 系统多数情况下表现正确
`04:25` 但所有未达标的案例中
`04:27` 75%的问题源于数据库查询
`04:31` 这些信息能精准指导改进方向
`04:35` 我正在开发AI工作流程
`04:38` 我经常会使用这种错误分析来告诉我该将注意力集中在何处
`04:42` 关于接下来该做什么工作
`04:45` 当你做出那个决定时
`04:46` 事实证明，为了补充我们之前在本模块中讨论的端到端元音
`04:52` 评估整个端到端系统往往很有用
`04:57` 但也需要单独分析各个组件
`04:59` 因为这能让你在改进过程中更高效
`05:02` 那个通过错误分析让你决定重点关注的组件
`05:08` 所以让我们进入下一个视频学习组件层面
`05:13` 邪恶的s
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=34"
bvid: "BV1DfrdByE2H"
cid: "35394226473"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394226473&page=34&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 现在你已经了解了要构建的知识图谱的目标
`00:03` 接下来让我们详细探讨多智能体系统的设计
`00:06` 你将首先设计图模型
`00:09` 然后为大量定义构建图结构
`00:12` 这是我的定义
`00:14` 我喜欢从工程角度思考智能体
`00:16` 对我来说这就像一种新型的控制流操作符
`00:20` 基本上你有一个循环结构
`00:22` 循环内部会调用LLM来确定下一步行动
`00:26` 然后根据确定的行动目标
`00:28` 将结果返回到计算机端
`00:31` 传回客户端
`00:32` 真正执行到一个开关语句处理潜在操作
`00:37` 在这个循环中这就是真正的智能行为
`00:40` 这一切都得益于LLM的调用
`00:42` 但智能行为的执行仍通过代码完成
`00:47` 这种架构的优势
`00:48` 当然在于智能体非常强大
`00:50` 因为LLM能进行推理并调用各种工具
`00:53` 可以执行任何代码操作
`00:54` 并且具备灵活性
`00:55` 因为LLM拥有记忆功能
`00:56` 能够学习已发生事件
`00:58` 无论是通过对话还是存储的重要信息
`01:00` 这些信息对决定未来结果至关重要
`01:03` 并且非常容易上手
`01:06` 因为你只需用自然语言描述
`01:08` 通过提示定义智能体的任务
`01:10` 但这种强大与便捷也存在缺点
`01:13` 使用智能体可能会很缓慢
`01:16` 因为需要远程调用
`01:19` LLM的调用既昂贵又耗时
`01:20` 这也是非确定性的
`01:22` 因为LLM本身具有不确定性
`01:23` 长期来看
`01:26` 每次调用的token成本会累积很高
`01:27` 如果在生产环境中持续运行智能体
`01:30` 处理成千上万次调用
`01:35` token成本会迅速飙升
`01:39` 这就是采用多智能体系统的优势之一
`01:41` 可以改善这些缺点
`01:45` 那么什么是多智能体系统
`01:47` 本质上是多个智能体协作完成单一目标的系统
`01:52` 智能体通常按层级结构组织
`01:55` 顶层智能体负责整体流程管理
`01:58` 然后可以添加任意数量的子级智能体
`02:01` 负责不同工作阶段或特定任务
`02:05` 智能体通过多种方式交互
`02:08` 存在主要的对话线程与用户交互
`02:12` 用户可能发起启动工作的消息
`02:15` 然后每个代理都可以决定
`02:17` 这是我可以做的工作还是应该由别人来做
`02:21` 所以我可以将这项任务委派给其他代理
`02:23` 你可以在这张示意图中看到
`02:25` 根代理能够内部委派给代理A或代理B
`02:30` 代理A和代理B可以使用分配给它们的工具执行任务
`02:34` 代理B中的一个工具是独特的
`02:36` 这里的P代理
`02:37` 这个P函数实际上会成为另一个代理
`02:41` 但被调用时像工具一样使用
`02:43` 工具之间有两种交互方式
`02:45` 要么代理之间互相委派任务
`02:47` 或者作为工具的代理被另一个代理调用
`02:51` 过程中还会有与用户的特定检查点
`02:55` 每当发生这些转换时
`02:57` 你构建的多代理系统是什么
`03:00` 你将构建知识图谱代理的部分
`03:03` 专注于执行知识图谱构建的子代理
`03:07` 你将学习使用谷歌ADK构建代理的基础
`03:10` 你将了解记忆是什么以及如何记录关键信息
`03:14` 你将找到一些工具
`03:16` 当然这一切都将从开放式对话转化为可执行代码
`03:21` 这张精美的示意图展示了所有代理及其交互流程
`03:26` 我们逐步解析这个整体结构
`03:30` 这是管理用户整体介绍的代理
`03:35` 代理的责任是什么
`03:38` 并帮助用户理解
`03:40` 这里可能的工作流程是什么
`03:42` 从知识图谱构建
`03:43` 一直到图检索
`03:45` 这实际上是一个不会执行任务的对话代理
`03:49` 但它会引导用户完成各个工作阶段
`03:53` 真正管理具体工作流程的是下一层代理
`03:58` 这里有三个主要工作流
`04:00` 在左侧
`04:01` 我们有两个工作流
`04:03` 结构化数据代理和非结构化数据代理
`04:06` 它们负责引导用户从想法到构建图谱
`04:09` 比如他们可能想要完成的目标到构建图谱
`04:13` 如果这些工作完成
`04:15` 第三个代理
`04:16` 右侧的图RAG代理
`04:18` 负责帮助用户利用该图回答问题
`04:22` 但结构化和非结构化数据代理是工作流代理
`04:27` 它们通过与用户互动完成工作
`04:29` 并协助用户完成所需步骤
`04:32` 从用户目标到描述图谱
`04:36` 它们通过委派专门处理各阶段的子代理来实现
`04:40` 这里会有一些重复
`04:42` 结构化和非结构化数据共享一些共同代理
`04:46` 然后在深入一级时我会提到这些
`04:49` 好的
`04:50` 这些子代理实际上正在处理结构化数据的工作流程
`04:54` 我们逐个分析这些内容
`04:55` 需要特别注意的是
`04:57` 这是这些亚洲团队实际工作的输出结果
`05:01` 如果你把自己想象成数据工程师
`05:03` 被赋予执行这项工作的任务
`05:06` 每个代理都相当于你自己会做的事情
`05:09` 你正在参与这项工作
`05:11` 第一个代理
`05:12` 用户意图代理
`05:13` 当你被要求执行数据分析任务时会做的事
`05:17` 并需要向提问者回应
`05:21` 嘿
`05:21` 你能澄清我需要做什么吗
`05:23` 给我
`05:24` 你知道的
`05:24` 你追求的目标是什么
`05:26` 你希望我进行哪种分析
`05:28` 必须尽早明确这一点
`05:30` 这是任务的整体要求或方向
`05:34` 因此用户意图代理
`05:36` 尽管是协作且对话式的
`05:39` 这个输出非常重要
`05:41` 我们正在捕捉用户的最终目标
`05:43` 基于用户目标，他们希望通过这项工作得到什么
`05:46` 接下来是文件建议代理
`05:50` 将综合用户意图代理确定的方向和目标
`05:54` 作为工作的方向和目标
`05:57` 然后查看可用的数据文件
`05:59` 从这些文件中找出对实现目标有用的
`06:04` 可能还有更多可用文件
`06:05` 甚至可能存在多个数据源
`06:09` 我们简化为仅建议磁盘上的可用文件
`06:13` 输出是用户批准的建议文件列表
`06:18` 然后输入到下一个代理
`06:21` 即模式提案代理
`06:23` 模式提案代理实际上是采用批评模式的双代理组合
`06:28` 其中一个代理负责提出可能的模式方案
`06:32` 下一个代理则作为批评者
`06:34` 比如
`06:35` 或许这样修改不太合适
`06:37` 或者我们可以稍后再讨论细节
`06:41` 核心思想是这些代理会内部循环评估
`06:47` 自我批评
`06:48` 最终产出良好的图模式
`06:50` 使用前代理批准的文件
`06:53` 并符合首个代理设定的用户目标
`06:57` 生成能回答用户问题的图模式
`07:01` 因此，所有这些输出我们称之为图构建计划
`07:05` 它本身并不是一个图
`07:07` 但它是关于如何构建该图的描述
`07:10` 中间的第二个工作流程
`07:11` 该工作流程的前两部分与结构化数据工作流程完全相同
`07:16` 在这里我们处理的是非结构化数据
`07:18` 但仍需首先理解用户引入非结构化数据的意图
`07:23` 这里的数据是markdown文件
`07:25` 然后好吧
`07:26` 如果你理解用户为何要引入某些markdown文件
`07:29` 可以再次识别相关文件
`07:33` 文件建议代理会执行与结构化数据相似的行为
`07:37` 但第三步在此有所不同
`07:40` 不再基于csv文件设计模式
`07:44` 你只有纯文本
`07:46` 从文本中
`07:47` 如何创建一个图
`07:48` 嗯
`07:49` 此处的方法是使用两个专门的代理分析文本
`07:53` 并识别所谓的实体
`07:55` 如人物
`07:55` 文本中出现的地点和事物
`07:58` 同时为这些实体识别描述性事实
`08:03` 文本中关于这些实体的事实
`08:05` 例如文本中提到
`08:07` 我知道我在文本中
`08:08` 我有一些本地咖啡店的评论
`08:11` 可能会发现
`08:12` 阿布克非常喜爱飞利浦咖啡
`08:15` 因为谁不喜欢呢
`08:17` 所以
`08:17` 这些可能是从文本中提取的事实
`08:20` 该代理的目标是确定可提取的事实类型
`08:24` 而不是实际提取
`08:26` 只是描述可能性
`08:28` 因此我们将称之为知识提取计划
`08:31` 知识提取计划与结构化数据的图构建计划结合
`08:36` 提供所需的所有规则
`08:38` 通过图构建计划和知识提取计划
`08:41` 角落的红色方框
`08:43` 只是一个包含多个工具的单一工具
`08:46` 能够执行这些计划并完成提取与构建
`08:51` 遍历所有构建规则
`08:53` 生成领域图
`08:55` 遍历所有markdown文件
`08:57` 进行分块处理
`08:58` 对内容进行向量嵌入
`08:59` 同时提取实体和事实
`09:02` 并将它们连接到结构化数据
`09:05` 在第四至第八课中
`09:07` 你将完整体验结构化数据的工作流程
`09:10` 从用户意图文件建议到模式提案
`09:14` 接下来我们将转向非结构化数据
`09:17` 仅进行实体事实类型提案
`09:19` 最后我们将查看第八课中的特定工具
`09:22` 该工具负责执行图结构构建
`09:24` 它完成了所有繁重工作
`09:25` 之前代理已在此课中完成对后续工作的推理
`09:31` 我们将实际进行编码实践
`09:33` 构建你的第一个代理
`09:34` 我们将使用谷歌的ADK
`09:36` 如果你已熟悉谷歌ADK
`09:38` 可以跳过该课直接构建多代理系统
`09:42` 但我认为这是一个很好的概述
`09:44` 同时帮助你理解我的思考方式
`09:46` 以及我在该多代理系统中的实现方法
`09:50` 因此值得逐步讲解
`09:52` 我们那时见
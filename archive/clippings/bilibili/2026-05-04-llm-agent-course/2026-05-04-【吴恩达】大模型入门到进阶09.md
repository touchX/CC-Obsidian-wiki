---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=9"
bvid: "BV1DfrdByE2H"
cid: "35394161455"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394161455&page=9&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 反射设计模式是我曾在许多应用中使用过的一种模式
`00:03` 而且实现起来意外地简单
`00:06` 让我们看看
`00:08` 就像人类有时会反思自己的输出并找到改进方法
`00:13` 所以可以
`00:14` 例如
`00:15` 我可能会写这样一封邮件
`00:17` 如果我快速打字
`00:18` 可能会得到一个质量不高的初稿
`00:21` 当我通读一遍时
`00:23` 可能会说下个月的日期表述不够清晰
`00:27` 汤米可能有空吃晚餐
`00:28` 这里有一个我打错的单词
`00:30` 还忘记签上我的名字
`00:33` 这将让我修改草稿，使其更明确地表达问候
`00:37` 汤米
`00:37` 你五号到七号有空吃晚饭吗
`00:40` 类似的流程
`00:41` 让我们也让语言模型改进输出
`00:44` 你可以提示它们先写邮件初稿并提供邮件版本
`00:49` 邮件版本一
`00:50` 可以将其传递给同一模型
`00:53` 同一个大型语言模型
`00:54` 但使用不同提示并要求其反思并撰写改进后的第二稿
`00:59` 然后获取最终输出
`01:01` 邮件版本二
`01:02` 我已硬编码了这个提示流程
`01:06` 先提示生成初稿，再提示反思改进
`01:10` 从而得到邮件版本二
`01:12` 类似的流程可用于改进其他类型输出
`01:18` 例如
`01:19` 如果你需要编写代码
`01:21` 可以提示它们编写代码
`01:23` 完成特定任务
`01:24` 可能会得到代码版本一
`01:28` 然后传递给同一语言模型
`01:30` 或不同模型检查漏洞
`01:33` 并撰写改进后的代码第二稿
`01:36` 不同模型各有优势
`01:38` 因此有时我会选择不同模型处理初稿
`01:43` 以及反思改进环节
`01:46` 例如
`01:47` 推理模型
`01:48` 有时也称为思考模型
`01:50` 在发现漏洞方面表现优异
`01:53` 因此我通常先直接生成代码初稿
`01:57` 然后使用推理模型检查漏洞
`02:01` 而不是仅仅生成代码
`02:02` 反思代码逻辑
`02:04` 实际上如果能获取外部反馈
`02:07` 来自外部的新信息
`02:10` 在代码情况下，lm反射能力会变得更强
`02:15` 你可以直接执行代码查看其功能
`02:20` 并通过检查输出结果
`02:22` 包括代码的任何错误信息
`02:24` 这对lm进行反思提供了极其有用的信息
`02:27` 并找到改进代码的方法
`02:29` 在这个例子中
`02:30` 生成了代码的初稿
`02:33` 但当我运行时
`02:34` 生成了语法错误
`02:36` 当传递这段代码输出
`02:38` 错误日志返回给lm并要求其反思反馈
`02:43` 并编写新代码
`02:44` 这为其生成更优版本提供了大量有用信息
`02:49` 两段代码
`02:50` 反思设计模式并非魔法
`02:53` 它并不能保证
`02:54` 我总能百分之百正确
`02:57` 但它可能带来适度的性能提升
`03:01` 需要牢记的设计要点是完美性更具威力
`03:07` 当有新的外部信息补充时
`03:10` 这些信息可以被纳入反思过程
`03:13` 在这个例子中
`03:14` 如果能够运行代码
`03:15` 并将代码输出或错误信息作为反思步骤的额外输入
`03:20` 这能让反思更深入地分析问题所在
`03:25` 发现问题并生成更优的第二版代码
`03:29` 相比没有外部信息输入的情况
`03:32` 需要记住的一点
`03:33` 每当反思有机会获取增强其能力的额外信息时
`03:42` 让我们进入下一视频
`03:43` 我想与大家分享使用反思与直接生成的系统性对比
`03:50` 我们有时称为零样本
`03:53` 提示
`03:54` 让我们进入下一视频
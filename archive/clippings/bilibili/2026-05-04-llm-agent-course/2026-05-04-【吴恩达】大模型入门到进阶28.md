---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=28"
bvid: "BV1DfrdByE2H"
cid: "35394225732"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394225732&page=28&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 通过代码执行进行规划的核心思想是
`00:03` 与其让算法输出JSON格式的计划
`00:07` 逐步执行每个步骤
`00:09` 为什么不让机械臂
`00:10` 直接尝试编写代码
`00:12` 而这段代码可以涵盖计划的多个步骤
`00:15` 比如调用这个函数
`00:16` 然后调用这个函数和这个函数
`00:18` 通过执行语言模型生成的代码
`00:21` 我们实际上可以执行较为复杂的计划
`00:24` 让我们看看何时需要使用这种技术
`00:27` 假设要构建一个回答咖啡机销售问题的系统
`00:32` 基于包含历史销售数据的电子表格
`00:36` 可能有一个配备这些工具的语言模型，比如获取列最大值
`00:41` 查看特定列并获取最大值
`00:44` 这有助于回答最贵的咖啡或获取列平均值、过滤行
`00:50` 获取列最小值
`00:51` 获取列中位数
`00:52` 某些行等等
`00:53` 这些都是各类工具的示例
`00:55` 可能让语言模型处理这个电子表格
`00:58` 或以不同方式处理这些行列数据
`01:02` 如果用户询问哪种热巧克力销量最高
`01:06` 发现可以使用这些工具回答此问题
`01:09` 但过程相当复杂
`01:11` 需用过滤行提取1月热巧克力交易数据
`01:16` 然后进行统计分析
`01:17` 接着对2月重复操作
`01:19` 计算该月统计结果
`01:20` 接着对3月重复操作
`01:21` 对4月重复操作
`01:22` 对5月到12月全部重复
`01:24` 然后取最大值
`01:25` 因此可以结合这些工具完成复杂流程
`01:30` 但这并非理想方案
`01:33` 更糟糕的是
`01:34` 若有人询问上周唯一交易量
`01:38` 嗯
`01:38` 这些工具无法获取答案
`01:40` 可能需要创建新工具
`01:42` 获取唯一条目
`01:43` 或遇到另一个查询
`01:45` 最后五笔交易的金额是多少
`01:47` 则需创建新工具获取数据
`01:53` 实际中团队遇到更多查询时
`01:57` 会不断创建更多工具
`02:00` 试图覆盖所有需求
`02:04` 有人可能询问类似数据集
`02:06` 这种方法易碎且低效
`02:09` 我见过团队持续处理边界情况并创建新工具
`02:15` 但事实证明还有更好的方法
`02:17` 那就是提示lm生成代码
`02:20` 请编写代码解决用户的查询
`02:23` 并将答案以Python代码返回
`02:25` 可能用这些开头和结尾包裹
`02:28` 执行Python XML标签
`02:30` 然后只需加载电子表格到数据处理库
`02:37` 这里使用pandas库
`02:39` 然后它实际上正在制定计划
`02:42` 计划是在加载CSV后
`02:44` 首先
`02:45` 必须确保日期列以正确方式处理
`02:48` 然后按日期排序
`02:49` 选择最后五笔交易
`02:51` 仅显示价格列等内容
`02:53` 但这些就是步骤
`02:54` 第一步
`02:54` 第二步
`02:55` 第三、第四和第五步
`02:56` 说明这个计划
`02:57` 因为像Python这样的编程语言
`03:00` 在这个例子中
`03:02` 同时导入了pandas数据处理库
`03:05` 因为其中包含大量内置函数
`03:08` 数百甚至数千个函数
`03:10` 而且这些函数lm见过大量调用数据
`03:17` 当让自己的模型编写代码时
`03:20` 可以从数百或数千个相关函数中选择
`03:24` 这些函数已有大量数据表明何时使用
`03:27` 因此可以组合不同函数调用来自大型库
`03:33` 从而为回答复杂查询制定计划
`03:38` 就像这样
`03:39` 再举一个例子
`03:40` 如果有人询问上周有多少笔唯一交易
`03:44` 嗯
`03:45` 它可以制定读取CSV文件的计划
`03:48` 处理日期列
`03:49` 定义时间窗口过滤行
`03:51` 删除重复行
`03:52` 这些细节并不重要
`03:54` 但希望从注释中可以看出
`03:58` lm正在制定四步计划
`04:02` 并将每一步用可执行代码表达
`04:06` 这样就能得到用户答案
`04:09` 对于可通过编写代码完成的任务
`04:14` 让算法用可执行代码表达计划
`04:20` 对lm来说是一种强大方式
`04:24` 当然还需考虑模块和工具使用时的注意事项
`04:30` 如果需要安全执行环境
`04:32` 如沙盒运行代码并应用限制
`04:36` 虽然我知道即使这可能不是最佳实践
`04:39` 我也知道很多开发者不使用沙盒环境
`04:43` 从这个图表可以看出，基于代码的规划效果很好
`04:47` 改编自孙耀王等人的研究论文
`04:50` 可以看到对于他们研究的多种任务模型，代码作为行动
`04:57` 其中让语言模型编写代码并通过代码执行操作
`05:02` 这比让其生成JSON再转换为行动或文本更优
`05:10` 你也看到编写代码表现更优的趋势
`05:14` 编写JSON计划同样更好，而纯文本计划效果也不错
`05:18` 现在他们开始用纯文本编写计划
`05:21` 当然有些场景需要提供自定义工具供模型学习使用
`05:26` 因此编写代码并非适用于所有场景
`05:31` 但当适用时
`05:32` 这对神经网络表达计划非常有效
`05:35` 今天的规划部分到这里就结束了
`05:39` AI最强大的应用场景之一
`05:42` 就是高度软件编码规划
`05:46` 事实证明
`05:47` 如果你询问顶尖的软件编码辅助工具
`05:51` 让它为你编写复杂软件
`05:53` 它可能会先制定构建软件组件的详细计划
`05:58` 然后构建第二个组件，再构建第三个
`06:00` 甚至可能计划测试组件的进展
`06:03` 随后形成检查清单逐步执行
`06:08` 这对构建日益复杂的软件应用非常有效
`06:15` 我认为规划的使用仍在增长
`06:17` 规划的一个缺点是
`06:21` 由于开发者未明确指示系统具体操作
`06:25` 控制起来稍显困难
`06:26` 且无法预知运行时的具体情况
`06:29` 但放弃部分控制
`06:32` 显著扩展了模型可尝试的范围
`06:37` 这项关键技术仍处于前沿
`06:41` 在某些领域尚未完全成熟
`06:43` 可能在特定场景中表现良好
`06:46` 尽管仍有很大发展空间
`06:48` 但希望你在应用中能享受其价值
`06:52` 今天的规划部分到此结束
`06:56` 还有一个最后的设计模式
`06:57` 希望在这模块与你分享
`06:59` 如何构建多智能体系统
`07:02` 我们不仅有一个智能体
`07:03` 而是多个协作完成任务的智能体
`07:08` 让我们在下一视频中了解
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=35"
bvid: "BV1DfrdByE2H"
cid: "35394226498"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394226498&page=35&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 要构建多智能体系统
`00:01` 你将使用谷歌的ADK或智能体开发工具包
`00:05` 这是一个用于开发智能体的框架
`00:07` 在本课中
`00:08` 你将学习如何创建和运行单个智能体以及整个智能体团队
`00:14` 让我们开始本课的学习
`00:16` 你将熟悉谷歌的智能体开发工具包
`00:20` 并将使用它来构建整个多智能体系统
`00:23` 在本课中
`00:24` 你将学习如何使用ADK创建和运行智能体
`00:27` 以及我们正在导入的内容
`00:29` 当然包括标准的os模块
`00:30` 然后是从谷歌ADK本身导入的大量内容
`00:33` 我们将导入这个智能体
`00:35` 这将是描述智能体核心功能的关键
`00:39` 与大型语言模型交互
`00:40` 我们将使用轻量级LLM库
`00:42` 所以在这里进行导入
`00:43` 这是一个让谷歌80K与OpenAI通信的小封装
`00:47` 同时我们还需要为智能体准备记忆功能
`00:51` 我们将使用记忆会话服务
`00:52` 最后智能体还需要一个运行方式
`00:55` 因此导入运行器
`00:56` 我们还有一些类型相关的代码
`00:58` 这些对整体功能影响不大
`01:00` 让我们确保所有导入都正确
`01:03` 所有库都已导入
`01:05` 接下来是定义将要使用的模型
`01:09` 本课程将使用OpenAI
`01:12` 现在开始导入
`01:13` 我们将定义模型本身为GPT-4o
`01:17` 再次使用GPT-4o
`01:19` 我们将通过轻量级LLM调用OpenAI API
`01:23` 现在尝试一个简单的聊天完成示例
`01:28` 如果你查看这里的聊天内容
`01:30` 你会注意到我们正在设置一条消息
`01:33` 即将发送给OpenAI
`01:34` 并询问
`01:35` 你准备好了吗
`01:39` 如果一切顺利
`01:40` 我们将收到OpenAI的成功响应
`01:42` 确认系统正常运行并查看响应模型
`01:46` 这里包含大量信息
`01:48` 但关键部分是响应包含OpenAI的消息
`01:52` 回答是
`01:52` 我已准备就绪
`01:53` 今天如何协助您
`01:54` OpenAI已就绪
`01:56` 我们今天还将使用new for j
`01:58` 因此导入辅助库
`01:59` 这将新四j与谷歌avk以优雅的方式结合
`02:03` 这个库是我们可以查看以了解细节的
`02:06` 正在发生的具体细节
`02:08` 我们将要使用的导入内容
`02:10` 仅从新四j for eighty k导入图数据库库
`02:14` 让我们看看这些细节
`02:16` 好的
`02:17` 现在我们进入了新四j for adk
`02:19` 你可以看到它开始得相当简单
`02:21` 我们从操作系统导入了一些内容
`02:23` 当然加载了环境
`02:25` 然后从you for j的python包
`02:27` 我们加载了图数据库
`02:29` 你还会得到一个结果类定义
`02:32` 现在
`02:32` 这里许多函数都非常适合
`02:34` 封装如何与me四j交互
`02:37` 并以谷歌aday喜欢的方式呈现结果
`02:42` 其中一个关键
`02:42` 点是结果应返回字典形式，状态为成功
`02:48` 或作为错误
`02:50` 这两个辅助函数可帮助实现，你可以调用success或error
`02:54` 连同其他参数
`02:56` 并从任何调用中获取格式良好的结果
`03:00` 我们随后整合了由谷歌adk处理的辅助库结果
`03:05` 需要易于持久化
`03:07` 此处调用python的函数
`03:09` 遍历新四j返回的各种结果类型
`03:13` 并将其转换为谷歌adk可读的序列化格式
`03:17` 你可以查看细节以了解具体情况
`03:20` 关键部分
`03:21` 当然
`03:21` 是这个处理新四j结果的函数
`03:26` 基本调用python包
`03:29` 将其整合为谷歌adk可轻松使用的结果
`03:32` 每个辅助函数也被上层类使用
`03:37` 名为d四j for adk
`03:38` 这实际上是一个包装器
`03:40` 整合新四j驱动程序，初始化环境变量
`03:44` 提供驱动程序访问权限
`03:46` 然后有一个特殊函数用于发送查询
`03:49` 可以发送任何类型的查询
`03:51` 但使用这些辅助函数重新格式化结果
`03:55` 最终生成谷歌aday友好的结果
`03:58` 由于我们只有一个驱动和连接
`04:01` 我们将使用该类的单例
`04:04` 这是我们将暴露的图数据库变量
`04:07` 并通过所有笔记本使用
`04:09` 好的
`04:10` 我们查看了新四j for eighty k
`04:12` 我们继续使用笔记本
`04:13` 现在一切已就绪，我们可以开始定义智能体本身
`04:17` 好的
`04:18` 我们的新四j包装器已准备就绪
`04:19` 我们继续尝试一下
`04:21` 你可以输入类似四j查询语句
`04:23` 只要这里语法正确
`04:26` 我们发送的四j语句是一个非常简单的声明
`04:28` 除了直接返回结果外不做其他操作
`04:30` 在这里返回结果
`04:32` 结果是一个字符串
`04:33` 新四j已就绪
`04:34` 将结果作为消息变量返回
`04:37` 如果我们运行这个
`04:38` 然后打印结果
`04:39` 你会看到它被封装成一个整洁的字典
`04:44` 包含状态成功
`04:45` 以及包含数据库返回所有行的数组的查询结果
`04:51` 太好了，open ai已就绪
`04:53` 四j已就绪
`04:54` 我们可以开始定义工具和智能体
`04:57` 最终工具定义是定义无工具智能体的重要部分
`05:03` 智能体可以进行大量思考
`05:04` 也许进行一些对话
`05:05` 但无法与环境或周围世界交互
`05:09` 我们将创建一个非常简单的工具
`05:11` 仅用于说明智能体能做什么
`05:14` Hello World是经典示例
`05:15` 当然
`05:16` 我们将开发智能体工具的Hello World版本
`05:20` 并命名为say_hello
`05:22` 工具可以简单定义为函数
`05:25` 函数只需说Hello
`05:26` 接受单个参数person_name作为字符串
`05:29` 并返回一个字典
`05:30` 当然这个字典是符合Google AK规范的
`05:34` 其中包含几个重要部分
`05:37` 这里有一个文档字符串
`05:39` 编写代码时总是有用的
`05:41` 在开发Google ADK工具时尤其关键
`05:45` 这个文档字符串让Google ADK理解工具功能
`05:50` 会传递给语言模型
`05:52` 当模型被告知
`05:53` 这里列出所有可用工具
`05:55` 以及这些工具的功能描述
`05:58` 所以
`05:58` 我们需要了解几个重要部分
`06:01` 在这里描述工具功能
`06:03` 仅格式化给定姓名的欢迎消息
`06:05` 当然，输入的person_name参数
`06:07` 你描述了正在传递的参数
`06:10` 还有结果
`06:11` 这里的结果在所有工具中保持一致
`06:13` 因为结果是一个谷歌喜欢看到的字典
`06:17` 但你可以在这里添加一些额外细节
`06:20` 例如
`06:20` 如果你知道可能出现的错误类型
`06:22` 也可以在函数内解释这些错误
`06:26` 函数内部操作非常简单
`06:31` 我们将调用新的four j发送查询
`06:34` 我们仍然返回一个问候语给你
`06:37` 然后将其与这个人名连接
`06:40` 这个人名作为函数参数传入
`06:43` 这将作为查询参数传递
`06:47` 每当在new four j语句中看到美元符号时
`06:50` 这意味着这是一个查询参数
`06:52` 在这个传递的字典数组中
`06:56` 你会看到人名被传入为person name
`06:59` 这里会被替换为该变量
`07:01` 并非模板替换
`07:03` 而是作为变量值传递
`07:04` 然后整个查询将执行
`07:07` 现在我们定义这个函数
`07:08` 当然因为这是一个普通函数
`07:11` 我们可以尝试运行它
`07:12` 我们可以打印'向abk问好'
`07:15` 预期看到格式良好的谷歌adk成功结果
`07:21` 发送到four的查询结果
`07:23` J有回复
`07:24` 向你问好abk完成拼接真棒
`07:28` 现在
`07:29` 关于查询参数的补充说明
`07:31` 我们这样做部分原因
`07:32` 这对任何查询语言都是良好规范
`07:34` 大多数查询语言
`07:36` 都支持查询参数
`07:38` 这样做原因之一是避免注入攻击
`07:41` 没错
`07:41` 防止用户传入类似sql或cipher语句的值
`07:47` 或任何查询语言语句作为值
`07:49` 并将其拼接到字符串中
`07:51` 可能导致各种混乱
`07:53` 因此传入查询参数
`07:56` 在这里
`07:56` 例如
`07:57` 如果传入名称并说实际名称是hey
`08:00` 此处返回字符串
`08:01` 这可能被恶意代码替换
`08:05` 如果直接拼接为字符串
`08:08` 各种坏事可能发生
`08:10` 因为它是一个仍然作为变量行为的查询参数
`08:14` 它会被传递进来
`08:15` 它会与其他字符串进行字符串拼接
`08:18` 结果就是简单的hello对稍显友好的恶意注入攻击
`08:23` 当然这已经被避免了
`08:25` 现在我们有了一个基础工具
`08:28` 我们可以定义一个使用该工具的代理
`08:31` 现在定义代理有几个组件
`08:33` 所有框架中都有共同点
`08:35` 它们会以相同理念的不同变体实现
`08:37` 让我们看看谷歌内部的情况
`08:39` 今天定义一个代理
`08:43` 如果你还记得之前
`08:45` 我们从google a k导入了这个代理类
`08:48` 我们将创建该类的实例
`08:50` 它需要大量参数
`08:51` 首先重要的是每个代理都需在此命名
`08:56` 我们将其命名为hello
`08:56` Agent_v_one
`08:58` 以防同时需要多个版本代理
`09:03` 或需要不同版本的指令
`09:06` 明确代理版本对调试非常有帮助
`09:10` 稍后
`09:11` 你还需要传入使用的模型
`09:13` 如果你还记得
`09:14` 我们之前定义llm为通过light调用
`09:18` Llm连接到open ai
`09:19` 这里我们将使用该模型
`09:21` 接下来这两个对谷歌adk的协调执行至关重要
`09:25` 和执行代理
`09:27` 首先是代理的描述
`09:30` 类似于工具的文档字符串
`09:33` 描述该代理的功能
`09:36` 让谷歌adk
`09:37` 也让其他代理理解该代理的目的
`09:40` 以及何时应调用该代理
`09:43` 而非自行处理
`09:45` 这称为代理委派
`09:48` 这是其他代理如何处理该代理的依据
`09:51` 对于代理本身
`09:52` 需提供一些指令
`09:54` 指令与你在提示工程中所做的类似
`09:57` 嗯
`09:57` 当你进行提示工程时
`09:58` 并定义系统提示
`10:00` 这是为llm设定的提示
`10:02` 与你在那里的操作相同
`10:04` 这是一个hello world代理
`10:06` 当然非常有用
`10:08` 这是一个乐于助人的助手将与用户聊天
`10:11` 它实际上只有一个想要使用的工具
`10:14` 在这里描述这个工具
`10:15` 因此代理不仅从工具定义中理解
`10:18` 还要从你的指令中了解如何思考该工具
`10:21` 以及何时使用该工具，所以在这里
`10:24` 如果用户提供他们的姓名
`10:25` 你知道
`10:26` 使用say hello工具提供个性化问候
`10:30` 哀悼
`10:33` 然后最后一部分
`10:34` 当然我们需要让这些工具可用
`10:36` 工具作为函数名数组传递
`10:40` 所以我们需要传入
`10:41` Say hello
`10:42` 然后拥有该工具的代理
`10:44` 知道如何使用该工具
`10:46` 因为我们提供了指令
`10:47` 和其他代理
`10:48` 如果有其他代理现在知道这个代理的角色
`10:53` 代理拥有一个工具
`10:55` 并且有关于如何使用该工具的指令
`10:57` 现在我们需要运行代理
`10:59` 这是创建基础遗传系统最后一步
`11:03` 代理必须有一个执行环境
`11:06` 所以你看这个图表
`11:07` 你会看到在这个框里
`11:09` 这就是执行环境发生的地方
`11:11` 有一个运行器类管理所有事件循环
`11:15` 基本上用于调用LLM
`11:17` 将LLM结果传递给特定代理
`11:20` 并协调所有代理本身
`11:22` 以及它们的执行方式
`11:23` 每个运行器还访问各种服务
`11:26` 无论是内存服务例如
`11:29` 进行实际内存存储
`11:31` 或者可以使用数据库技术作为记忆
`11:34` 这提供存储访问
`11:36` 执行逻辑在执行循环内
`11:40` 这里是实际运行和协调代理的步骤
`11:43` 代理是并行、顺序还是循环执行
`11:47` 所有这些共同构成代理的执行环境
`11:50` 好的
`11:51` 我们将手动实现这一点
`11:53` 然后展示如何封装为便捷方法
`11:56` 在使用Google ADK进行开发时
`11:59` Google ADK提供了优秀的工具定义代理
`12:02` 并运行代理的实际工具
`12:04` 并提供所需完整执行环境
`12:07` 我们将手动完成这些步骤
`12:09` 在笔记本中进行
`12:10` 那么我们一步一步来
`12:11` 然后将其封装成一个小助手
`12:14` 你需要准备几个实际设置执行环境的事项
`12:18` 每个运行时实际上都在一个会话中运行
`12:21` 当然
`12:21` 当然可以有多个在生产环境中运行的会话
`12:25` 我们将使用单一会话
`12:26` 接下来我们将设置内存服务
`12:29` 为代理提供上下文和状态
`12:32` 在运行过程中
`12:33` 我们可以基于该内存创建会话服务
`12:37` 以及针对我们编写的特定代理
`12:39` 同时假设我们有一个用户
`12:41` 我们将假设只有一个用户
`12:42` 一个在单一会话中运行的应用程序
`12:46` 所有这些都将在这里使用
`12:47` 在创建会话时用于生成可执行状态
`12:53` 然后运行器将运行该状态
`12:55` 基于它拥有的代理
`12:57` 这里运行器结合了我们要运行的hello world代理
`13:01` 应用名称将与我们传递给会话的名称相同
`13:05` 同时会话服务将是该代理运行的会话
`13:10` 在生产环境中
`13:11` 可能会有多个会话
`13:12` 可能会有多个使用该代理的应用程序
`13:15` 这就是这些运行参数的不同之处
`13:19` 好的
`13:19` 现在有很多内容需要讲解
`13:20` 仅为了实际运行代理
`13:23` 让我们逐步进行，慢慢来
`13:25` 我们知道已经有了这个运行器
`13:27` 我们刚刚定义的
`13:28` 我们将进行一次单循环处理单用户消息
`13:32` 让运行器执行代理以响应用户消息
`13:37` 我们将定义用户消息
`13:39` 我简单说一声'你好'
`13:40` 我可能是K，然后以友好方式打印该消息
`13:44` 这样我们可以看到运行过程
`13:46` 了解发生了什么
`13:47` 这个纯文本消息
`13:49` 需要打包成Google ADK运行时期望的数据结构
`13:52` 运行时环境需要
`13:54` 通常这是后台自动处理的
`13:56` 当然
`13:57` 但我们手动进行
`13:58` 我们将在此创建内容类
`14:01` 我们创建的内容角色来自用户
`14:05` 这是一个用户消息
`14:06` 内容可以包含多个部分
`14:08` 我们将创建一个部分数组
`14:10` 其中一个部分包含一些文本
`14:13` 这里的文本是用户消息
`14:15` 这些内容被打包起来以便执行更复杂的操作
`14:18` 我们只是尝试运行单个用户的单条消息
`14:21` 当有大量额外操作时，这能提供很大灵活性
`14:25` 这基本上是在创建内容事件
`14:28` 然后需要由代理运行时系统处理
`14:31` 我们还将设置代理系统的响应
`14:36` 以防它没有执行任何操作
`14:38` 所以输入一条消息后
`14:40` 我们将实际执行
`14:41` 运行该消息并让代理有机会响应
`14:44` 但代理可能不会响应
`14:47` 存在最终响应的概念
`14:49` 这是代理通过不同方式设置的标志以表明
`14:54` 好的
`14:54` 我已完成对此的思考
`14:56` 或已完成处理
`14:57` 如果代理无反应，可返回用户
`15:01` 最终响应可能未设置
`15:03` 因此我们预先设置默认值以表明无操作
`15:08` 目前可以不显示详细输出
`15:11` 稍后您可以看到
`15:12` 详细输出可能是什么样子
`15:14` 这就是事件循环本身
`15:17` 应该说明事件的一个步骤
`15:19` 比如针对这条用户消息
`15:22` 我们将异步使用运行器并调用LLM
`15:28` 使用传入的代理和指定模型
`15:32` 以及所有提供的指令
`15:34` 首先注意到当我们调用此异步方法时
`15:37` 我们还传递了创建会话时的所有信息
`15:40` 这是因为可以重复运行
`15:43` 可能存在多个会话
`15:44` 因此多个事件循环在异步运行时传递
`15:49` 这是当前的操作上下文
`15:51` 针对该用户和会话
`15:54` 这是传入的内容
`15:56` 使用LLM执行一次
`15:58` 在此进行处理
`16:00` 这在循环内部
`16:02` 因为这是一个事件系统
`16:04` 当代理收到此消息后
`16:07` 可能在最终响应前执行多项操作
`16:11` 事件循环在此提取代理的异步事件
`16:16` 每个事件是代理的更新信息
`16:20` 当事件到来时
`16:21` 如果启用了详细模式
`16:23` 我们将打印
`16:24` 这里发生了一个事件，这是谁创建的事件
`16:27` 初始事件
`16:28` 当然应该传递用户消息
`16:31` 我们应该看到该事件
`16:32` 作者是用户自己
`16:34` 你也会看到事件内容
`16:37` 你可以查看调试语句中的情况
`16:39` 大量信息被打印出来
`16:41` 有助于理解当前状态
`16:43` 这是保持事件循环运行的关键概念
`16:46` 只要你想让它运行
`16:48` 希望代理只会执行必要操作
`16:51` 但每个事件到来时
`16:53` 有一个标志表明
`16:55` 好的
`16:55` 这是最终事件
`16:56` 我已完成处理
`16:58` 代理已完成处理
`17:00` 如果它设置了这个
`17:01` 那么你就知道
`17:02` 处理已完成
`17:03` 可以查看内容
`17:05` 决定最终响应可能是什么，我们查看内容
`17:09` 如果存在多个部分
`17:12` 如果返回的文本数组非空
`17:15` 我们将取第一个消息部分
`17:19` 提取该部分的文本
`17:20` 这里我们做了一些假设
`17:22` 第一部分的文本响应实际上是正确的，转换为最终响应文本
`17:28` 或者代理可能决定这就是最终响应
`17:32` 顺便说一句，我实际上没有响应
`17:34` 我要求运行时系统执行的是升级
`17:38` 现在
`17:39` 升级最终结束
`17:40` 意味着代理正在运行
`17:42` 知道它是子代理
`17:43` 无法处理
`17:44` 或无法继续处理现有内容
`17:47` 基本上在说
`17:48` 将此升级给他人
`17:49` 可能是上级或其他代理
`17:51` 可能更合适
`17:52` 在这种情况下
`17:54` 最终响应文本将基于此
`17:57` 我们将说
`17:58` 亚洲团队决定升级当前消息处理
`18:01` 我们没有具体消息
`18:03` 或存在错误消息
`18:04` 如果有则返回该消息作为最终结果
`18:09` 所有操作是为了继续一轮事件循环
`18:13` 由单个用户消息触发
`18:15` 然后最后我们将打印出最终的代理响应
`18:19` 这有点内容需要处理
`18:20` 但请稍微记住这一点
`18:22` 在浏览所有内部笔记本时
`18:24` 这正是让所有运行的机器实际运作的关键
`18:29` 所有这些工作
`18:30` 我们进入简单聊天
`18:31` 你好
`18:32` 我是abk
`18:33` 你好abk
`18:35` 当然
`18:36` 因为我们将会大量手动调用代理
`18:39` 我们将设置一个辅助类来帮助我们
`18:42` 更轻松地完成这些操作
`18:44` 我将其命名为代理调用器
`18:47` 这个代理调用器封装了我们之前讨论的那些函数
`18:51` 这里逐一处理每个函数
`18:53` 并将事件循环本身整合到一个类中
`18:58` 如果你查看细节
`19:01` 它完成了我们之前看到的所有操作
`19:04` 保存单个用户ID
`19:07` 单个会话ID
`19:08` 当前要运行的代理
`19:10` 以及已设置的运行器
`19:12` 它可以访问用于内部管理的会话
`19:17` 这里的调用函数会触发一次事件循环往返
`19:24` 基于用户消息
`19:25` 它执行我们之前看到的操作
`19:26` 接收用户消息
`19:28` 将其打包
`19:29` 使其可被代理处理
`19:33` 然后运行事件循环
`19:35` 同样可用调试选项
`19:39` 这纯粹是风格选择
`19:41` 我不是原生Python开发者
`19:43` 所以我喜欢这样操作
`19:44` 偏好为复杂构造创建工厂方法
`19:49` 所以
`19:49` 而不是在类中使用复杂构造器
`19:52` 我创建了一个单独的构造类
`19:57` 这里的实用函数make_agent_caller
`20:01` 将创建代理调用器实例
`20:03` 你需要传入所有必要信息
`20:06` 指定要运行的代理
`20:09` 以及代理的初始状态
`20:11` 其中状态是代理的内部记忆
`20:14` 所有内容应与之前运行的一致
`20:17` 但现在封装成不同组件
`20:20` 应整合到代理调用器中的主要组件
`20:23` 当前应用是什么
`20:24` 会话ID的使用者
`20:26` 为内存创建会话服务
`20:29` 同时用于管理会话本身
`20:31` 最后还要创建一个运行器
`20:33` 在构建完所有组件后
`20:36` 将它们传递给代理外壳
`20:37` 该外壳将所有内容整合到执行循环中
`20:42` 好的
`20:42` 现在我们有了运行代理的实用函数
`20:45` 可以运行一个问候对话
`20:48` 嗯
`20:48` 我们之前创建的代理
`20:51` 要为我们的问候代理创建新代理调用器
`20:54` 代理
`20:54` 我们将调用辅助函数
`20:57` 创建代理调用器并传入问候代理
`21:00` 我们将其命名为问候代理调用器
`21:03` 要运行对话
`21:05` 我们将创建一个异步函数
`21:06` 传入多个用户消息到代理调用器
`21:11` 首先使用call方法
`21:13` 我说你好
`21:13` 我可能在
`21:14` 然后我说我很兴奋
`21:16` 我们运行这个对话并查看效果，太棒了
`21:20` 我们创建了一个友好的聊天代理
`21:23` 做得很好
`21:24` 现在你已经学到这里
`21:27` 已经掌握了使用Google ASDK的基础
`21:29` 如何创建代理
`21:31` 如何为代理添加可用工具
`21:33` 同时如何为代理创建执行环境
`21:36` 最终掌握这些内容后
`21:38` 可以在笔记本中编写与代理的交互脚本
`21:42` 传入用户消息以测试代理的工作方式
`21:47` 我们创建的这个优秀的代理调用器类
`21:50` 以及工厂方法make_agent_caller
`21:54` 我们将在后续笔记本中使用它以简化操作
`21:57` 找到代理后
`21:58` 可以轻松使用该函数创建运行时环境
`22:01` 你会频繁使用这个功能
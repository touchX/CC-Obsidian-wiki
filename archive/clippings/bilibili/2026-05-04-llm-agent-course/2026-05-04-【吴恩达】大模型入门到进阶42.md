---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=42"
bvid: "BV1DfrdByE2H"
cid: "35394228093"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394228093&page=42&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 在本节课中
`00:01` 你将继续构建知识图谱
`00:03` 上一节课根据构建计划从CSV文件创建了领域图
`00:09` 现在你将处理Markdown文件
`00:11` 将其切分为词汇图并提取实体到主体图
`00:17` 你将学习如何使用新的vj图RAG库
`00:20` 执行切分和实体提取操作
`00:23` 你还将学习实体解析技术
`00:27` 最终这不会是工作流程的遗传部分
`00:30` 这将完全由工具处理
`00:32` 因此我们将定义一些工具
`00:33` 并编写这些工具所需的一些辅助函数
`00:37` 我们需要的两个工具之一是实际构建知识图谱的工具
`00:42` 我们将为每个文件创建一个构建器
`00:43` 并让其处理文件进行切分和提取
`00:47` 你将定义的另一个函数是关联主体与领域节点
`00:51` 这两个工具将从Markdown文件构建图谱
`00:56` 然后获取提取的实体结果
`00:59` 并在主体图之间进行关联
`01:01` 与之前从CSV文件创建的领域图
`01:06` 现在可能有点难以理解
`01:08` 随着逐步讲解会更清晰
`01:10` 如同之前的笔记本教程
`01:11` 你将首先导入所需库
`01:14` 让我们进行常规设置
`01:16` 所有需要的库
`01:18` 等待导入完成
`01:22` 确保Open Eyes已就绪
`01:24` 并检查新FourJ是否准备就绪
`01:27` 现在一切正常
`01:30` 在此设置中
`01:31` 我们需要的不仅仅是前几课创建的初始状态
`01:36` 因为现在我们不再只是执行工作流程
`01:38` 上一课实际上在eFourJ中创建了部分图谱
`01:41` 由于新FourJ加载时会是全新状态
`01:45` 我们需要添加
`01:46` 之前课程中创建的部分产品节点
`01:49` 我们有一个辅助函数用于此
`01:51` 这个辅助函数是load_product_nodes
`01:54` 我们可以直接调用它
`01:55` 然后执行调用
`01:56` 我们预期的是
`01:58` 图中仅存在标签为product的节点
`02:02` 好的
`02:02` 操作正确
`02:03` 太好了
`02:04` 现在
`02:05` 你需要为代理准备初始状态
`02:07` 初始状态应包含工作流程前期创建的部分
`02:12` 与之前加载的内容类似，我们需要构建计划
`02:15` 我实际处理的已批准文件
`02:18` 此外我们已完成实体提取的初步规划
`02:21` 我们需要已批准的实体和事实类型
`02:24` 我们将创建这些初始状态
`02:27` 这是已批准的施工计划
`02:32` 这是已批准的文件
`02:36` 已批准的实体和事实类型
`02:42` 现在可以开始定义创建管道所需的各种功能
`02:47` 这将处理所有Markdown文件
`02:50` 进行分块处理
`02:52` 然后提取实体
`02:53` 新的Four J图RAG库有一个便捷的简单知识图谱管道
`02:58` 可用于处理分块和实体提取
`03:02` 对于所有要处理的Markdown文件
`03:04` 需要创建辅助函数正确配置
`03:08` 但首先让我们看看简单知识图谱管道的接口
`03:12` 这只是示例代码
`03:14` 当然无法运行
`03:15` 因为此处没有有效值
`03:17` 但仅展示结构
`03:19` 实际创建简单知识图谱管道实例的过程
`03:22` 需要一个m
`03:24` 当然进行实体提取时
`03:26` 需要Four J的驱动程序以输出图谱
`03:30` 需要更好的接口，可能是同一LLM或其他模型
`03:34` 如果需要的话
`03:35` 由于我们处理的是Markdown文件而非PDF
`03:40` 通常设置为处理PDF为false
`03:42` 但实际上并非如此
`03:44` 我们将使用自定义PDF加载器而非加载PDF
`03:47` 我们将加载Markdown文件
`03:49` 这只是接口设计的一个小细节
`03:52` 但最终我们会设为true
`03:53` 在这里
`03:54` 我们将定义自定义加载器并集成到此部分
`03:57` 对于PDF加载器部分
`03:59` 还需要自定义分块器
`04:01` 因为我们了解Markdown的结构
`04:03` 如果你有分块处理经验
`04:05` 知道纯文本分割本身就是一门艺术
`04:06` 可能有很多深度学习课程
`04:08` 可以深入学习该主题
`04:11` 这里有很多深度学习课程
`04:14` 我们将采用简化版本
`04:15` 基于现有数据做出假设
`04:18` 还可以传入模式
`04:19` 而模式是
`04:20` 当然
`04:21` 将基于前一课设置的建议模式
`04:24` 关于能从文本中提取的事实和实体类型
`04:28` 然后当然还有
`04:29` 将所有这些内容整合起来
`04:30` 我们将有一个自定义提示输入
`04:33` 这将明确告知语言模型我们需要查找的内容
`04:36` 以及如何进行提取思考
`04:38` 让我们逐步讲解新四J KG构建器的端到端流程
`04:42` 帮助你理解这些组件的功能
`04:46` 每个文档都会被加载
`04:47` 内置支持PDF格式
`04:49` 但你会使用自定义Markdown加载器
`04:52` 接下来
`04:52` 文本分块组件将执行分块处理
`04:55` 这是许多框架中常见的分块方法
`04:58` 对于每个分块
`04:59` 分块嵌入将计算向量
`05:01` 嵌入
`05:02` 然后对分块进行分析
`05:04` 使用语言模型按实体和关系提取器的指令
`05:08` 此组件将配置以匹配我们的知识提取计划
`05:12` 图谱构建完成后
`05:14` 可选的图谱清理器用于优化图谱
`05:17` 所有操作都在内存中完成
`05:19` KG写入器负责将内存中的图谱保存到四J系统
`05:25` 最后
`05:25` 实体解析组件将合并可能相同的实体节点
`05:32` 好的
`05:33` 现在你可以开始定义这些自定义函数
`05:35` 需要输入到流程中
`05:37` 首先设置自定义文本分块器
`05:41` 如果你还记得我们之前的火星飞行员Markdown文件
`05:45` 开头有一个H1标题
`05:47` 然后有这些分页符
`05:49` 主要用于分割评论内容
`05:51` 因此我们将使用简单正则表达式分块器
`05:54` 在新四J中
`05:56` 有一个名为文本分块器的基础类
`05:58` 可扩展自定义功能
`06:00` 这就是我们要做的
`06:01` 我们将扩展文本分块器
`06:03` 在文本分块器的运行函数中
`06:05` 因为它是流程的一部分
`06:07` 这里将放置自定义功能
`06:10` 只需使用正则表达式进行文本分割
`06:14` 基于已定义的正则表达式
`06:16` 将其传递给函数本身
`06:19` 对于任何传入的正则表达式
`06:21` 均可作为文本的分割点
`06:26` 最后一步是继续
`06:27` 使用列表推导将每个纯文本分块
`06:32` 转换为四J图谱RAG库期望的对象
`06:36` 称为文本块
`06:37` 文本块仅包含字符串本身的文本内容
`06:40` 该文本段落的索引
`06:42` 这将生成包含这些文本块对象的单个列表
`06:46` 该列表将被命名为文本块
`06:49` 你还将使用特殊的数据加载器加载Markdown文件
`06:53` 与文本分割器类似
`06:55` 这将继承新vj grarag库的基类
`07:00` 这里有几个可用的选项
`07:02` 但我们自己会找到一个特殊版本
`07:05` 我们将导入数据加载器基类
`07:08` 这些操作类型需要用于输出
`07:11` 我们将模拟解析PDF文档的过程
`07:14` 在Markdown数据加载器中加载PDF文档类型
`07:18` 这里最关键的是
`07:21` 我们知道Markdown中包含某些元数据
`07:24` 我们需要提取并添加到数据加载的上下文中
`07:28` 在分块处理时
`07:29` 类中有一个名为提取标题的辅助函数
`07:33` 将使用非常基础的正则表达式
`07:36` 假设首次找到单个H1标题
`07:40` 将其作为文档标题
`07:43` 当调用运行函数时
`07:45` 运行函数将从数据源加载数据
`07:49` 将从文件系统加载
`07:51` 读取整个Markdown文件
`07:54` 提取标题
`07:56` 然后将其转换为此文档信息
`08:00` 文档信息才是关键部分
`08:02` 这是与文档文本结合的元数据
`08:08` 我们已将Markdown文本加载到此PDF文档类中
`08:14` 构建知识图谱管道还需
`08:18` 当然需要一个语言模型
`08:20` 我们准备好了
`08:20` 我们已有
`08:21` 但我们将继续使用OpenAI
`08:22` 当然也会使用OpenAI
`08:25` 然后获取新的四J驱动器
`08:28` 这里使用
`08:29` j graph rag库
`08:30` 四J支持
`08:32` 当然也支持OpenAI和其他模型
`08:35` 我们将使用OpenAI作为语言模型
`08:37` 同时用于嵌入生成
`08:38` 创建新四J的语言模型实例
`08:41` 以及四J better
`08:43` 从图数据库获取四J驱动器
`08:46` 我们一直使用的单例，用于发送查询
`08:50` 该单例允许获取内部驱动器
`08:53` 这就是我们在这里的操作
`08:55` 好的
`08:55` 你已经定义了知识图谱管道所需的一些实用函数
`08:59` 同时也定义了其他组件，比如语言模型和嵌入器
`09:03` 现在我们将转向实际进行知识图谱实体提取所需的上下文
`09:08` 我们首先从实体模式开始
`09:11` 新四j图RAG包所需的实体模式包含几个不同组件
`09:16` 其中一个是要知道应在文本中查找的节点类型
`09:20` 这里我们只需复制或创建approved_entities的别名
`09:24` 我们还将获取关系类型模式
`09:26` 为此我们将从批准的事实类型中提取
`09:30` 所有批准的事实类型实际上都存在于字典中
`09:34` 字典的键正是关系类型本身
`09:38` 因此我们只需提取所有键
`09:39` 这将成为关系类型模式
`09:41` 你可以在这里看到
`09:43` 你还会使用批准的事实类型
`09:45` 作为知识图谱构建管道所说的模式模式
`09:50` 这其实只是信息的重新包装
`09:53` 从每个事实中
`09:54` 我们将生成包含主语标签的列表
`09:57` 谓词标签和宾语标签
`09:59` 我们将处理谓词标签
`10:01` 将其转换为大写
`10:02` 这是new for j的约定
`10:06` 知识图谱构建所需的完整模式在此
`10:11` 包含节点类型
`10:12` 关系类型
`10:13` 这些如何组合的模式
`10:15` 并将此标志设为false
`10:17` 仅使用我们定义的这些类型
`10:20` 不要添加新内容
`10:24` 接下来获取实体提取上下文的重要部分
`10:28` 我们将创建自定义提示
`10:29` 将每个块内的文本注入其中
`10:33` 同时还会添加模式
`10:35` 我们还将使用特殊实用函数
`10:38` 用于添加文件上下文
`10:41` 管道将处理单个块
`10:43` 文件的整体上下文将添加到提示中
`10:47` 以便LLM在查看该块时
`10:50` 知道它所属的文档是什么
`10:53` 因此对于文件上下文我们创建这个小辅助函数
`10:56` 所有操作其实只是
`10:57` 获取文件文本的前几行
`11:01` 包含文件标题
`11:03` 以及一些简介和信息
`11:05` 为每个块提供足够上下文
`11:08` 然后定义提示本身
`11:10` 我们来看这个提示
`11:12` 作为一大段文本
`11:14` 它将大致遵循我们之前采用的相同格式
`11:17` 我们有一些关于
`11:19` 当前工作中LLM的角色是什么
`11:22` 以及实际追求的目标是什么
`11:24` 但其中大部分是搭建
`11:26` 一些设计提示
`11:27` 同时也包括上下文和上下文包含模式定义
`11:31` 以及我们将要提取的文件部分内容
`11:34` 当你查看这个提示时
`11:36` 你会发现它包含
`11:37` 关于角色和目标的说明
`11:39` 这里还有一些设计指令
`11:41` 关于它应该执行什么
`11:42` 还包含输出格式的具体要求
`11:46` 在这里以及每个片段的模式都将被注入
`11:49` 一旦所有内容就位
`11:52` 作为输出的一部分
`11:54` 我们将要求它为每个节点生成独特想法
`11:57` 在创建这些节点时
`12:00` 同时我们还会给出此处的指令
`12:02` 关于如何处理输出中的每个属性
`12:05` 我们需要节点输出包含某些ID
`12:08` 我们需要知道标签是什么
`12:09` 它所选择的内容
`12:10` 以及应分配给该实体的属性是什么
`12:14` 接下来是来自文件的上下文
`12:17` 文档级上下文将被注入
`12:20` 在这里因为这是一个辅助函数
`12:22` 用于创建整体提示
`12:24` 我们将直接将其作为静态文本注入
`12:28` 因此不会被注入
`12:29` 每次创建片段时
`12:31` 它将是静态文本的一部分
`12:33` 最后将把片段数据放入此模板格式
`12:38` 好的
`12:39` 整合所有已设置的上下文
`12:40` 结合知识图谱构建器所需的各类辅助函数
`12:45` 我们可以创建知识图谱构建器
`12:48` 然后使用它
`12:50` 因为我们将为每个文件创建知识图谱构建器
`12:53` 从而生成针对文件的专用提示
`12:55` 基于文件内容
`12:57` 这里是我们在辅助函数中设置的方法
`12:59` 我们将创建一个生成知识图谱构建器的辅助函数
`13:02` 其输出是一个简单的知识图谱处理管道
`13:06` 这是图RAG类的新功能
`13:08` 首先在该辅助函数内部
`13:10` 获取文档级上下文
`13:13` 使用我们创建的文件上下文工具
`13:16` 再次获取数据文件的前几行内容
`13:19` 然后从这里生成上下文化的提示
`13:23` 传递该上下文
`13:24` 这是我们能得到的完整提示
`13:28` 因此当我们创建简单的知识图谱管道
`13:30` 我们将获取定义的lm模型
`13:33` 我们使用的驱动程序是embe
`13:35` 我们将假装在处理PDF文件
`13:37` 因为我们有一个自定义的PDF加载器
`13:39` 实际上会加载Markdown文件
`13:41` 我们的自定义分词器
`13:42` 将使用正则表达式
`13:44` 在这里正则表达式只是向前查找
`13:47` 短横线
`13:47` 短横线
`13:48` 短横线
`13:48` 这就是Markdown页面
`13:49` 如果查看之前看到的Markdown文件
`13:52` 这就是每个评论的分割方式
`13:54` 因此我们将使用这些文本
`13:56` 将组装的模式拆分为实体模式
`13:59` 然后最终构建的上下文化提示
`14:03` 将所有内容整合
`14:04` 得到一个完整的管道进行分块
`14:08` 然后进行实体提取
`14:10` 现在你有了生成知识图谱v管道的辅助函数
`14:14` 针对特定文件
`14:15` 现在只需遍历导入目录中的所有文件
`14:21` 为每个文件获取完整路径
`14:24` 输出提示语句
`14:26` 告知当前进度
`14:27` 创建知识图谱构建器
`14:29` 然后运行知识图谱构建器
`14:32` 这需要几分钟时间
`14:33` 我们有
`14:34` 我想处理大约十个文件
`14:37` 每个文件将需要
`14:38` 根据OpenAI的响应速度
`14:41` AI
`14:42` 可能需要一分钟
`14:43` 完成文档的自然语言处理和提取
`14:47` 同时进行分块处理
`14:51` 好的
`14:51` 当循环完成后
`14:53` 将得到完整的词汇图
`14:56` 这是图的组成部分
`14:57` 包含相互连接的块
`14:59` 并连接到文档注释
`15:02` 同时还有主体图
`15:07` 包含所有提取的实体
`15:10` 以及它们之间的关系
`15:12` 已完成
`15:14` 现在你拥有了一个词典图
`15:16` 一个主体图和一个领域图
`15:19` 然而知识图谱并不完整
`15:22` 因为主体图和领域图尚未连接
`15:26` 而且如果你还记得领域图是基于CSV文件创建的
`15:30` 而我们刚刚从Markdown文件中提取数据生成了主体图
`15:35` 下一步是将从Markdown文件中提取的实体连接起来
`15:40` 整合到主体图中
`15:41` 我们希望将这些与领域图连接
`15:44` 所以
`15:44` 如果领域图中存在与主体图提取实体相同的产品
`15:48` 作为主体图中的提取实体
`15:51` 我们希望将它们连接
`15:52` 因此我们将定义几个工具来实现这一目标
`15:55` 现在对于主体图中的每种实体类型
`15:58` 你需要制定与领域图中正确节点关联的策略
`16:02` 例如
`16:03` 你应该预期主体图中的产品名称实体存在
`16:08` 这些应与领域图中的产品对应
`16:11` 为了做到这一点
`16:13` 你将首先进行几项操作
`16:15` 找到主体图中所有唯一的实体标签
`16:20` 同样找到领域图中所有唯一的节点标签
`16:24` 然后尝试在这两个标签集合之间关联属性键
`16:29` 这样你可以确定
`16:30` 如果主体图中有产品具有多个属性
`16:33` 如何与领域图中具有多个属性的产品对应
`16:38` 所以
`16:38` 最终步骤是执行实体解析
`16:41` 通过分析属性值的相似性
`16:45` 好的
`16:45` 第一步是找到主体图中的唯一实体标签
`16:49` 现在让我们查看主体图
`16:50` 观察节点的结构
`16:52` 在新的j grafrag库完成工作后
`16:55` 主体图中的节点将带有额外标识标签
`17:01` 它们将被标记为双下划线实体
`17:03` 下划线标签
`17:05` 你还会看到额外标签标识实体类型
`17:10` 如果我们运行匹配任意节点的查询
`17:13` 并查找具有该标签的节点
`17:16` 即可返回不同标签的集合并称为实体标签
`17:21` 现在查看结果
`17:23` 可以看到这里添加了几个特殊标签
`17:26` 有双下划线实体标签
`17:28` 还有双下划线kg builder
`17:30` 表明kg builder负责创建这些节点
`17:33` 但我们真正关心的是看到产品位置
`17:37` 问题和功能
`17:38` 现在我们已经知道这一点，因为这是我们从之前的步骤中预期的结果
`17:43` 但重要的是，这些步骤可能原本试图寻找类似位置之类的信息
`17:48` 但这些可能实际上未能在图中发生
`17:51` 因为语言模型未能真正找到它们
`17:53` 因此我们将采取以下做法
`17:55` 我们将查询图中实际发生的内容
`17:57` 然后根据我们在图中找到的标签
`18:00` 我们将这些称为主体图中的唯一实体标签
`18:04` 让我们通过几个查询示例来详细说明
`18:07` 首先，如果你查看这些行，你会发现每一行
`18:10` 实际上是一个标签列表
`18:12` 对于每一行
`18:13` 如果我们继续使用展开子句
`18:16` 在这里，我们说要展开实体标签，这些标签将属于每个节点
`18:20` 并将这些单独的行称为实体标签
`18:24` 然后我们可以返回唯一的实体标签
`18:26` 我们应该看到所有相同的值
`18:28` 但现在不再是多个列表
`18:30` 我们将看到一个包含所有值的单一列表
`18:33` 好的
`18:34` 现在列表实际上只是几行
`18:36` 每行只有一个标签
`18:38` 知识图谱构建器
`18:39` 产品实体你可以直接看到
`18:42` 让我们进一步详细说明这个查询
`18:45` 我们需要过滤掉这些下划线标签
`18:47` 我们只需通过相同的查询来实现
`18:51` 我们从匹配开始
`18:52` 仅针对实际是实体的项设置谓词
`18:55` 展开所有这些
`18:57` 这些只是单独的实体标签
`18:59` 然后我们添加另一个谓词
`19:02` 好的
`19:02` 现在删除所有以下划线开头的实体
`19:05` 这将移除知识图谱构建器和下划线实体
`19:09` 这就是我们想要的产品位置列表
`19:12` 问题和功能
`19:14` 你可以定义一个实用函数来封装对new four j的调用
`19:19` 如果你尝试使用这个实用函数
`19:22` 你应该看到
`19:22` 当然
`19:23` 仅仅这个列表
`19:23` 正是我们现在需要的内容
`19:26` 对于每个唯一的实体标签
`19:28` 我们也想找到每个标签的唯一键
`19:31` 对于每一个，我们将执行类似的操作
`19:34` 我们将定义一个实用函数
`19:36` 用于查找具有该特定标签的所有节点
`19:39` 但一旦找到这些样本集合
`19:42` 我们将仅提取这些节点上出现的唯一键
`19:45` 让我们看看这个效用函数在这里是什么样子的
`19:49` 它将接收一个参数
`19:50` 也就是我们正在寻找的实体标签是什么
`19:53` 现在进行匹配
`19:54` 而不是查找所有节点
`19:56` 我们只查找具有该特定实体标签的节点
`19:59` 在这里我们将稍微调整一下思路
`20:02` 我们实际上想要查找
`20:03` 当然
`20:04` 仅在该标签与下划线实体共同出现的位置
`20:08` 而不是查找不同的标签集合
`20:12` 我们将查找这些节点的唯一键并返回
`20:16` 和之前类似
`20:18` 我们将将这些键列表展开为几行键
`20:22` 这就是展开操作在这里的作用
`20:24` 然后我们将这些全部收集回列表
`20:27` 然后将其作为单个结果返回
`20:29` 现在定义这个函数
`20:31` 然后运行它
`20:32` 仅查找与主体图中产品相关的唯一键
`20:38` 好的，看起来我们
`20:40` 在进行实体提取时并没有太多限制
`20:43` 这次语言模型实际上发现了许多不同键
`20:46` 在创建这些实体时
`20:48` 在不同产品之间
`20:50` 这里展示了从评论中推导出的不同属性
`20:54` 它识别出了材料等信息
`20:56` 关于不同特征的
`20:57` 我不确定书架深度似乎出现在某个地方
`21:00` 必须查看Markdown文本
`21:02` 才能看到语言模型为何发现这些不同属性
`21:06` 根据提取时使用的具体评论
`21:09` 有些评论可能提到了这些属性
`21:12` 其他则没有
`21:13` 因此这些不会在所有属性中一致
`21:16` 但其中一些可能是特别的
`21:19` 我认为名称会共同出现
`21:21` 稍后你会看到我们如何将这个键列表
`21:25` 与领域图中的可用键进行关联
`21:28` 这就是我们如何同步所有内容的方法
`21:30` 接下来定义的效用函数非常相似
`21:34` 但现在我们将注意力转向领域图
`21:36` 在领域图中我们已经知道如何查找领域图中的标签
`21:42` 我们知道存在没有实体标签的标签
`21:47` 如果我们匹配特定标签
`21:49` 比如产品
`21:50` 要查找产品的唯一领域键
`21:53` 我们将进行头部匹配
`21:55` 然后过滤掉带有实体的项
`21:58` 因为这些属于主体图
`22:00` 然后我们按照之前处理函数的方法
`22:03` 接下来我们将获取这些域节点的唯一键
`22:07` 然后将这些键收集到列表中
`22:08` 因此如果我们查找域标签的唯一属性键
`22:12` 这应该是一致的，因为这些都来自同一个CSV文件
`22:16` 这是一个更小的列表
`22:18` 它很可能与CSV文件中的内容完全对应
`22:21` 这是产品名称
`22:22` 价格描述
`22:23` 产品ID
`22:26` 为了辅助这一过程
`22:28` 我们将定义一个名为normalize_key的辅助函数
`22:30` 你可以为特定标签
`22:33` 输入需要归一化的键是什么
`22:35` 这个函数的作用
`22:37` 会将键转为小写
`22:39` 去除多余空格
`22:41` 这没什么帮助
`22:42` 如果存在前缀的话
`22:45` 其中前缀是标签本身
`22:47` 将其移除
`22:49` 例如产品名称会变为name
`22:52` 这样可以轻松与主体图的name键关联
`22:58` 项目名称会变为name
`23:01` 产品空间名称也会变为name
`23:03` 当然像价格这样的属性键仍保持price
`23:07` 不会被改变，所以再次
`23:10` 实现非常直接
`23:11` 但这样做的目的是让属性键更容易比较
`23:16` 然后查看一些示例
`23:17` 仅作合理性检查看起来正常
`23:22` 接下来要编写的实用函数是用于关联特定标签的键
`23:27` 这将利用我们已有的实用函数
`23:30` 现在看看实现代码
`23:33` 我们将在这里导入Python的新库rapidfuzz
`23:37` 主要用于文本相似度评分
`23:40` 这是基于编辑距离的简单评分
`23:42` 实际上有多种方法来判断
`23:45` 文本字符串是否高度相似
`23:47` rapidfuzz是该领域功能强大的库
`23:51` 给定特定标签
`23:53` 实体节点和域节点的键
`23:56` 我们将查看相似度阈值
`24:00` 这些键如何相互关联
`24:02` 根据rapidfuzz的fuzz函数评分
`24:06` 对于高度相关的键
`24:09` 我们将配对并说明
`24:10` 这些键值得比较值
`24:14` 现在详细看一下
`24:17` 相关键
`24:18` 所以我们想要配对的键
`24:19` 初始状态为空
`24:21` 我们将遍历实体键集合中的所有键
`24:25` 采用经典风格
`24:26` 我们将使用for循环
`24:27` 在for循环中遍历所有实体键
`24:30` 然后在其中遍历所有域键
`24:33` 对于每一对组合
`24:34` 我们将考虑它们的相关性程度
`24:37` 我们将对每个键进行归一化处理
`24:40` 由于快速模糊相似度分数从0到100
`24:44` 我们将反转这个值使其更像相似度评分
`24:48` 我们现在
`24:50` 计算归一化域键与实体键的比率
`24:55` 这应该就是模糊相似度
`24:57` 现在
`24:58` 我们希望模糊相似度超过相似度阈值
`25:01` 这是传递给函数的参数
`25:04` 当前默认值设为0.9
`25:07` 如果超过该阈值
`25:09` 我们将将其添加到相关键
`25:12` 作为有效配对
`25:14` 我们继续对这些键进行排序
`25:16` 然后
`25:17` 以便更容易查看高度相关的项
`25:21` Verse的相关性较低
`25:22` 我们将取最高相关性的一项后续使用
`25:26` 所以
`25:27` 在定义该函数后
`25:28` 我们将继续
`25:29` 这部分只是尝试演示
`25:34` 让我们定义产品函数
`25:37` 将查找所有唯一实体键
`25:39` 查找域键
`25:41` 将其传递给此函数并查看结果
`25:44` 对于产品
`25:45` 可以看到名称与产品名称完全匹配
`25:48` 价格与价格完全匹配
`25:50` 描述与描述完全匹配
`25:52` 但设计描述的相关性较低
`25:56` 尺寸与描述的相关性低
`25:58` 这些效果不佳
`26:00` 我们设置的阈值是多少
`26:02` 是的
`26:02` 我们要求相似度为0.5
`26:04` 可以尝试不同值查看结果差异
`26:07` 你得到
`26:08` 默认值0.9
`26:09` 是经典高阈值设置
`26:13` 好的
`26:14` 这里还有一个背景信息
`26:16` 接下来我们将把这些内容整合起来
`26:18` 我们现在能够关联特定标签
`26:21` 在主体图和领域图中
`26:23` 这些标签的键值
`26:25` 这些良好配对的作用
`26:27` 实际判断两个节点是否为同一节点
`26:30` 这一切都是基于实体解析技术
`26:34` 这是一种实现目标的技术方法
`26:37` 这是一个不错的基准方案
`26:39` 它完美适配当前数据集，可能对其他数据集也有效
`26:43` 你需要采用多种技术手段
`26:45` 当然可以想象构建一个完整代理系统
`26:48` 只需针对现有数据
`26:50` 确定最适合的解析技术
`26:53` 但今天我们
`26:54` 需要做出一些假设
`26:56` 接下来要使用的函数是
`26:59` 在处理new four j数据时调用加密方法
`27:03` 加密库也支持字符串比较方法
`27:07` 可用的值相似度函数之一是jo winkler距离
`27:11` 这本质上是一种字符串比较方法
`27:14` 类似于
`27:15` 你可能使用向量相似度需要计算嵌入向量
`27:19` 也可以通过多种方式计算文本距离
`27:22` gerald winkler通过计算两字符串的编辑距离
`27:27` 主要评估需要进行的微小修改次数
`27:30` 只需比较字符串a和字符串b
`27:32` 需要进行多少次编辑使两者相同
`27:36` 结果值介于0到1之间
`27:39` 数值呈反向关系
`27:41` 0表示完全匹配
`27:42` 意味着精确一致
`27:43` 1表示完全无匹配
`27:47` near for j的技术相似度库
`27:50` 包含gerald winkler计算方法
`27:53` 还可以使用汉明距离
`27:54` 莱文斯坦距离
`27:56` 还有瑟尔森-迪斯相似度
`27:58` 以及模糊匹配
`27:59` 当然如果需要
`28:00` 也可以使用向量嵌入进行余弦相似度
`28:04` 这些各有不同
`28:05` 特征特性
`28:06` 就像我们之前所做的所有工作
`28:08` 在日志raft构建过程中
`28:10` 可以自行选择最适合的方案
`28:12` 如果
`28:12` 针对特定数据集的数据
`28:14` jo winkler距离完全适用
`28:17` 所以你可以看看杰瑞·温克勒在行动中的样子
`28:20` 仅使用纯密码
`28:22` 如果你查看此处匹配子句中的密码查询
`28:25` 实际上我们将匹配特定标签下的实体和域名对
`28:30` 这里我们将寻找双方具有相同实体标签
`28:34` 当然实体节点还带有额外标签_实体
`28:38` 然后结合实体和域名
`28:40` 然后计算这个乔温克勒距离得分
`28:43` 我们将通过过滤器传递该得分
`28:45` 得分必须小于0.4
`28:47` 如果你记得得分为零表示完美匹配
`28:50` 所以我们在这里采用相反的评分方式
`28:53` 我们寻找低值
`28:55` 在此进行过滤
`28:56` 然后返回这些值进行查看
`28:58` 在这个例子中
`29:00` 我们将传入一些查询参数
`29:02` 我们寻找实体标签为产品
`29:05` 在主体图和域名图上均适用
`29:08` 在主体图中
`29:09` 我们将查找实体键名
`29:11` 域名键将为产品_名称
`29:15` 好的
`29:15` 可以看到这些是完美得分
`29:17` 哥德堡桌子和哥德堡桌子
`29:19` 这些完全相同
`29:20` 当然你会预期它们得分很低
`29:23` 你可以尝试不同值
`29:25` 观察效果
`29:26` 如果使用稍低的阈值
`29:30` 比如0.5
`29:32` 可以看到哥德堡电缆和沃斯塔书架
`29:35` 显然并不差
`29:39` 哥德堡桌子和斯德哥尔摩椅子
`29:40` 我不确定文本上有多相似
`29:43` 但距离计算显示如此
`29:45` 这就是为什么实践中通常需要
`29:47` 设置非常低的阈值
`29:48` 比如1.1
`29:49` 使结果尽可能接近完全相同
`29:54` 让我们详细解释这个查询
`29:56` 这正是相同的查询
`29:58` 我们添加了一个额外部分
`30:00` 当我们找到两个低阈值实体时
`30:03` 现在我们稍作调整
`30:06` 在实体与域名节点的配对中
`30:09` 我们将使用where子句
`30:11` 仅对温克勒距离进行约束
`30:14` 将阈值设为0.1
`30:16` 对于通过对应关系的任何配对
`30:19` 我们将实际创建从实体到域节点的关系
`30:24` 因此我们将使用固定类型
`30:26` 它不会参数化，对应于
`30:29` 因此关系将是该实体节点与该域节点对应
`30:34` 现在我们将使用两个子句
`30:36` 以防您多次运行此操作
`30:38` 在执行合并时这非常有用
`30:41` Merge在Neo4j中类似于Upsert操作
`30:44` 它实际上基于行为有两个子句
`30:48` 如果合并找到匹配项
`30:51` 实际上你所描述的内容
`30:52` 则存在匹配时的子句
`30:55` 然后后续操作将被执行
`30:58` 或者首次创建时
`31:01` 当执行合并后会调用创建子句
`31:05` 如果首次运行时
`31:07` 将调用创建操作
`31:08` 我们将设置此值
`31:09` 创建时间并添加时间戳
`31:12` 如果已创建过
`31:14` 再次进行匹配时
`31:15` 我们将为更新时间添加时间戳
`31:18` 每次运行都会更新
`31:20` 好的
`31:21` 此查询覆盖所有对应产品效果良好
`31:28` 此Cypher查询完全符合需求
`31:30` 现在将其封装为函数调用
`31:33` 传入实体键和域键
`31:36` 以及所需的相似度阈值
`31:40` 此处查询基本相同
`31:42` 可以看到参数作为查询参数传递
`31:45` 从函数参数中传入
`31:48` 同样
`31:49` 我们直接运行此操作测试产品名称
`31:53` 这两个实体的不同键
`31:55` 以及域图中的对应键
`31:58` 很好
`31:58` 在主体图和域图间找到十项合理关系
`32:04` 为完整性考虑
`32:07` 虽然我们知道可以连接所需产品
`32:09` 对所有可用实体执行此操作
`32:11` 以关联并连接所有主体节点
`32:14` 到对应域节点
`32:16` 我们只需添加循环
`32:18` 遍历所有唯一实体标签
`32:21` 从实体侧关联到域侧
`32:25` 主体节点与域节点将被连接
`32:29` 可以看到产品已成功连接
`32:31` 当然
`32:32` 位置实际上没有关联
`32:34` 现在它确实可以发行了
`32:35` 或者两者都没有功能
`32:36` 但这是我们预期的
`32:38` 现在发生了什么
`32:40` 经过所有这些工作
`32:42` 你终于得到了一个完整的领域图，它是从CSV文件构建的
`32:47` 你还拥有一个词汇图和一个主题图，它们是由Markdown文件创建的
`32:52` 随着这最后一步
`32:53` 你已将主题图中的实体与领域节点中的实体连接
`32:58` 现在你拥有了完全连接的知识图谱
`33:00` 做得很好
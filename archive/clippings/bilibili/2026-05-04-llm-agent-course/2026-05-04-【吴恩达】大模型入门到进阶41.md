---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=41"
bvid: "BV1DfrdByE2H"
cid: "35394228133"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394228133&page=41&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 现在你拥有了构建知识图谱的完整规范
`00:04` 让我们进入这最后一课
`00:06` 在这里你将定义执行构建计划的工具
`00:09` 按照代理工作流中已有的图谱构建计划和知识抽取计划
`00:17` 你现在已准备好构建图谱
`00:19` 在本课中
`00:20` 你将深入探讨知识图谱构建工具的细节
`00:24` 如果你慷慨大方
`00:26` 可以称其为神经符号代理
`00:29` 语言模型与基于规则系统的混合体
`00:33` 让我们暂时后退一步看看这些计划将产生什么
`00:37` 图谱构建计划
`00:39` 将加载CSV文件生成领域图
`00:43` 几乎每个CSV文件都对应一个节点类型
`00:47` 通过额外映射创建关系
`00:50` 你可以让拥有大上下文窗口的LLM执行此任务
`00:55` 但这是一个非常直接的机械过程你已转化为代码
`01:01` 这项工作将由你定义的单一工具完成
`01:04` 称为构建领域图
`01:06` 该工具将包含大量辅助函数
`01:09` 用于图构建的不同部分
`01:11` 我们将逐一讲解这些工具
`01:14` 一如既往
`01:15` 我们导入一些库
`01:18` 检查OpenAI是否运行
`01:22` 同时确保四j仍存在完美现在
`01:26` 开始构建领域图的工具
`01:30` 我们可以创建一系列工具
`01:32` 每个工具职责明确
`01:34` 首先为数据库创建唯一性约束
`01:38` 现在我们从
`01:39` 仅思考数据文件
`01:41` 必须准备数据库以应对即将进行的数据导入
`01:46` 数据库准备的第一步是确保唯一性约束
`01:50` 在最终的数据库中
`01:52` 对于具有特定标签的节点
`01:55` 当该标签出现时
`01:58` 某个标签属性需要唯一
`02:02` 这与具有唯一列的CSV文件完全对应
`02:05` ID
`02:06` 将成为每个的唯一约束
`02:09` 此实用函数将处理此事
`02:12` 只需传入标签和唯一属性键
`02:16` 将自动在四j创建约束
`02:20` 下方的Cypher查询
`02:21` 可以看到创建约束
`02:24` 传入的约束名称仅在不存在时创建
`02:28` 为特定标签创建
`02:31` 在该标签下要求此唯一属性键唯一
`02:37` 在此之前我们大量使用查询参数
`02:40` 当我们设置这样的约束时，而不是新的四j
`02:43` 无法为此传递查询参数
`02:46` 我们将在这里进行一些不安全的操作
`02:49` 通过实际进行字符串拼接
`02:51` 我们知道这不是推荐的做法
`02:54` 我们应该在此处有一个净化函数以确保安全
`02:57` 这就是
`02:57` 你知道这是不好的
`02:58` 但暂时这样就可以了
`03:02` 所以下一个函数你想要
`03:03` 如果你有能力为节点创建唯一性约束
`03:07` 我们接下来需要能够从CSV文件加载节点
`03:10` 你可以为此定义一个函数
`03:13` 这里就是加载节点的CSV函数，它接受源文件
`03:17` 该源文件的标签
`03:19` CSV文件中哪个列是唯一的
`03:23` 然后还需要一个包含所有应从源文件创建的属性列表
`03:28` 现在看下面生成的密码
`03:30` 有专门的语法用于从CSV文件加载
`03:33` 位于导入目录中的CSV文件
`03:36` 这就是为什么我们处理的都是相对路径
`03:39` 当我们使用load cas fee在u四j中时，此文件URL
`03:44` 这里的路径将相对于新四j的导入目录
`03:48` 我们将从该目录加载带标题的CSV文件
`03:51` 然后对于该文件中的每一行
`03:55` 我们将在此调用子查询
`03:57` 在子查询中
`03:58` 它将执行这些行的合并操作
`04:02` 当然会先检查节点是否存在
`04:06` 如果不存在
`04:07` 将根据传入的唯一列创建该节点
`04:11` 接下来的这行代码
`04:13` 四
`04:14` each是一种利用传入属性列表的独特方式
`04:20` 它只是某些属性的名称
`04:21` 我们最终会遍历每个属性
`04:24` 对于每个属性名称
`04:26` 我们将为刚创建的节点设置属性值
`04:32` 来自CSV文件的行值
`04:35` 实际上我们将遍历所有名称并继续
`04:37` 逐步设置这些值
`04:40` 通过在子查询中使用此方法
`04:42` 我们能够批量处理
`04:45` 因此我们将分批次处理，每次一千行
`04:47` 无论CSV文件多大
`04:50` 我们将分批处理，每次一千行
`04:53` 你可以看到我们将调用的新四j
`04:55` 传入查询
`04:56` 我们将传递该查询
`04:58` 并附带大量查询参数以供使用
`05:02` 现在对于你拥有的每个CSV文件
`05:04` 你实际上需要执行我们刚刚定义的两个函数
`05:07` 我们首先要调用这两个函数
`05:09` 调用创建唯一性约束的功能
`05:11` 以确保每个节点都有唯一值
`05:14` 这样ID才能被正确识别
`05:15` 然后在为每个CSV文件创建唯一性约束后
`05:19` 我们将继续执行实际的数据导入
`05:21` 使用我们刚才看到的从CSV加载节点工具
`05:26` 这就是当前发生的情况
`05:27` 在导入节点过程中
`05:28` 只是应用构建规则
`05:30` 然后基于构建规则中的值
`05:33` 将调用创建唯一性约束工具
`05:37` 如果没有错误发生
`05:39` 将继续从CSV文件导入节点
`05:42` 这里只是从构建规则中提取
`05:45` 需要传递给函数的不同值
`05:50` 现在可以更简单地导入关系
`05:53` 在关系中不需要为它们应用唯一性约束
`05:57` 因为它们本身没有独立标识
`05:59` 它们将直接连接到两侧的节点
`06:03` 无论是来源节点还是目标节点
`06:05` 这些节点应该是唯一的
`06:06` 但关系本身也会是唯一的
`06:08` 因为每行只有一个对应关系
`06:11` 在导入的文件中
`06:13` 这特别适用于我们当前的数据文件
`06:17` 如果需要的话需要更加谨慎
`06:19` 你知道的
`06:19` 数据文件可能设置得更复杂
`06:22` 但对于本例中的数据文件
`06:24` 这种方法无法正确完成导入
`06:28` 如同节点加载一样
`06:30` 我们将使用导入目录中的带标题的LOAD CSV
`06:34` 并将所有内容作为行加载
`06:36` 然后对于每一行
`06:37` 我们需要在子查询中进行更多处理
`06:41` 首先找到之前加载的现有节点
`06:45` 找到来源节点和目标节点
`06:48` 在使用MATCH子句找到这些节点后
`06:51` 将使用MERGE子句创建关系
`06:55` 从来源节点到目标节点
`06:57` 这样只会为每对来源和目标创建一个关系
`07:02` 以及特定类型的关系
`07:06` 例如如果有两个ABK喜欢咖啡的关系
`07:09` 玫瑰咖啡的情况只会出现一次
`07:11` 即使CSV文件中有重复数据
`07:16` MERGE会检查节点
`07:18` 两侧的节点已经是唯一的
`07:21` 并且该关系已有abk的点赞
`07:23` 所以点赞已经存在了
`07:24` 不会创建另一个关系
`07:27` 节点的情况也是如此
`07:29` 我们将在关系上设置属性
`07:31` 如果存在可用属性
`07:33` 我们将使用这里的每个循环结构遍历它们
`07:36` 并将所有可用值设置好
`07:39` 然后继续调用查询
`07:40` 并将所有查询参数直接从构造规则传入
`07:47` 所有这些函数都完成了大部分实际工作
`07:50` 然后构建主图本身
`07:52` 实际上只需接收一个参数
`07:56` 完整的构造计划
`07:57` 然后按顺序先构建所有节点
`08:01` 现在节点已存在
`08:03` 然后可以使用已定义的主图创建关系
`08:08` 我们现在可以尝试运行此代码
`08:11` 当然我们需要完整的构造计划
`08:14` 这里是完整的构造计划
`08:17` 这将与之前的课程不同
`08:19` 取决于lm的决定
`08:21` 当此笔记本创建时
`08:23` 这里是有效的构造计划
`08:26` 你可以查看其中内容
`08:28` 这有点预期中的结果
`08:29` 我认为这里需要重点关注
`08:32` 稍后检查创建的关系
`08:35` 存在一个包含关系
`08:37` 从产品出发
`08:39` 包含一个装配组件，听起来合理
`08:41` 这是一个部分关系，从零件出发
`08:45` 零件属于一个装配组件
`08:47` 好的
`08:47` 这有点冗长
`08:49` 但这是正确的
`08:50` 这里的源节点是零件，由供应商提供
`08:54` 看起来完全合理
`08:57` 这将成为我们主图函数的输入
`09:01` 现在运行该函数
`09:03` 当函数执行完毕
`09:04` 没有输出结果
`09:06` 可以添加打印语句提示完成
`09:09` 好的
`09:09` 已完成所有工作
`09:10` 接下来我们将实际操作图数据
`09:13` 就像仍在进行查询一样
`09:16` 让我们用更复杂的查询语句检查领域图
`09:21` 我们将首先查找构造计划中的所有关系规则
`09:27` 我们使用列表来实现
`09:30` Python中的表达式理解
`09:32` 我们现在只需遍历所有构造计划值
`09:35` 每当构造类型是关系时
`09:38` 就会被放入这个列表
`09:39` 这里是我们之前看到的所有构造
`09:42` 但不包括节点构造
`09:44` 然后我们在这里进行一点密码学魔法
`09:48` 所以我将逐步解释这个过程
`09:50` 在这复杂的Cypher查询中
`09:52` 我们的目标是大致对图进行采样
`09:56` 所以我们不会获取整个图来查看
`09:58` 但我们要做的是我们期望
`10:00` 对于这里所有的关系构造规则
`10:04` 结果图中至少应存在该关系类型
`10:09` 如果我们收集所有不同的关系类型
`10:12` 并对它们进行模式匹配
`10:14` 然后查看输出结果
`10:16` 应该能看到所有预期创建的内容都已创建
`10:21` 我不确定我是否表达正确
`10:23` 但让我们实际走一遍看看计划如何
`10:26` 第一步我们将传入这个构造规则列表
`10:30` 在Cypher中
`10:31` 你可以将列表展开为多行数据
`10:37` 原本是一个包含规则的列表
`10:40` 我们将其转换为多行
`10:42` 每行包含单个规则元素
`10:47` 这样关系构造列表将被拆分为多行
`10:52` 每行包含单个构造值
`10:57` 接下来我们要对构造规则
`11:01` 进行从某个节点的模式匹配且无条件
`11:06` 这里
`11:11` 关系类型来自构造规则
`11:15` 提取构造规则的关系类型值
`11:18` 在这里
`11:19` 美元符号引用查询参数
`11:22` 我们将提取该值到此关系类型
`11:26` 如果这里
`11:28` 你会看到有'supplied by'关系类型
`11:32` 该值将由查询参数替换
`11:35` 从某个节点通过'supplied by'连接另一节点
`11:39` 我们将返回来源节点的标签
`11:43` 以及目标节点的标签
`11:44` 以及关系本身的类型
`11:47` 最终应得到一个三元组
`11:50` 仅包含关系标签和两侧节点标签
`11:54` 我们限制为仅一条结果
`11:56` 因为我们不需要查看整个图
`11:57` 只需验证存在性
`12:00` 我们将将其放入子查询中
`12:02` 这样我们只需在此处应用一次匹配
`12:05` 对之前展开的每个元素进行操作
`12:08` 这就是这里发生的情况
`12:09` 在这一部分我们将调用子查询
`12:11` 并将刚刚定义的匹配路径放入其中
`12:15` 然后在完全组装的密码中展开列表
`12:20` 在子查询内执行匹配
`12:22` 然后从所有结果中返回源节点关系和两个节点的三元组
`12:27` 这将是标签
`12:29` 它们的类型
`12:30` 以及两个节点上的标签
`12:32` 好的
`12:33` 你可以看到这将打印出值
`12:36` 你可以看到运行前的密码结构
`12:38` 然后我们将最终运行密码
`12:42` 这里是完整的密码语句
`12:46` 这里是我们从plotter找到的关系
`12:50` 包含组装
`12:51` 零件属于组装部件
`12:55` 零件由供应商提供
`12:57` 完美，这与之前构造池中的结果一致
`13:01` 看来这个图以良好的方式构建完成
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=38"
bvid: "BV1DfrdByE2H"
cid: "35394227082"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394227082&page=38&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 你现在将设置系统中的第二个代理
`00:02` 文件建议代理
`00:05` 该代理将帮助你从结构化数据中选择相关文件以构建领域图
`00:11` 让我们进入你正在处理的工作流程
`00:15` 你正在第四课中构建结构化数据代理工作流程
`00:19` 你在此第五课中创建了用户意图代理
`00:22` 你将创建文件建议代理
`00:25` 你将采用与第四课用户意图代理类似的模式
`00:29` 该代理本身有一个主要目标
`00:31` 创建此批准文件列表
`00:34` 这将是代理自身需要查看的文件列表
`00:38` 并决定这些文件对用户要完成的任务是否相关
`00:41` 然后询问用户
`00:43` 你批准这些吗
`00:44` 如果同意，输出将是执行该操作的批准文件
`00:48` 文件建议代理包含许多工具
`00:50` 用于查看可用的文件
`00:53` 这是所有文件的列表
`00:56` 它可以对文件进行采样，进行少量读取
`01:00` 它只能读取约一百行
`01:02` 然后基于此使用该工具进行推荐
`01:06` 称为设置建议文件工具
`01:07` 如果用户批准
`01:09` 将调用批准建议文件工具
`01:12` 这将更新记忆
`01:13` 以便在上下文中包含批准文件
`01:16` 而不仅仅是建议文件
`01:19` 所有这些
`01:19` 当然受前一代理工作的指导
`01:23` 所有操作均基于用户要完成的整体目标
`01:27` 该代理从记忆中获取这些信息而非对话历史
`01:32` 因此不依赖于该工具被调用前的对话记录
`01:36` 或该代理被调用前的对话
`01:38` 此代理将使用获取批准用户目标工具
`01:41` 确定用户要完成的任务是什么
`01:44` 然后查看文件
`01:46` 确定哪些相关
`01:48` 并向用户提出建议
`01:50` 让我们看看代码
`01:52` 你将从常规设置开始
`01:53` 导入所需库和之前定义的辅助函数
`01:59` 已有这些库
`02:00` 我们将连接OpenAI并确保其正常运行
`02:07` 哦
`02:07` 今天非常稳定
`02:08` 是的
`02:09` 我准备好了
`02:09` 如何帮助您
`02:12` 你将开始构建代理自身的指令
`02:16` 现在我们将逐步进行
`02:19` 你想要定义代理的角色
`02:21` 同时也要设定其目标
`02:22` 对于负责建议相关文件的代理
`02:27` 我们将称其为建设性批评者
`02:29` 正在审查文件列表
`02:31` 其目标是建议可用于构建知识图谱的相关文件
`02:38` 然后你可以提供一些执行任务的提示
`02:42` 特定的任务
`02:43` 当然就是审查文件列表并查看其中
`02:47` 哪些文件对基于已批准用户目标的描述中的图谱相关
`02:53` 对于每个文件
`02:55` 有一个名为样本文件的工具可用
`02:58` 可以让代理实际查看该文件内容
`03:01` 以确认文件是否包含对实现目标有用的信息
`03:05` 我们正在提供
`03:07` 同时提示它只需查看结构化数据文件
`03:09` 如csv或json
`03:11` 可以用思维链方向收尾
`03:15` 此处的思维链包含两个部分
`03:17` 首先是准备阶段
`03:20` 先调用获取已批准用户目标工具
`03:24` 从会话记忆中获取用户目标
`03:26` 我们将调用该工具
`03:27` 而非直接将信息传递到提示中
`03:30` 这鼓励代理使用工具
`03:32` 在获取这些信息后
`03:34` 我们将提供思维链指令
`03:37` 仔细重复这些步骤直到完成
`03:39` 以下是代理需要遵循的步骤
`03:42` 获取所有可用文件的列表
`03:44` 然后针对该文件列表
`03:46` 评估每个文件的相关性
`03:49` 然后使用建议文件工具将建议文件记录到记忆中
`03:55` 此步骤
`03:56` 第三步
`03:57` 鼓励八和二
`03:58` 使用获取建议文件工具获取建议文件列表非常重要
`04:03` 代理可能在自身推理和对话历史中意识到
`04:09` 文件列表可能是什么以及它认为的建议文件
`04:13` 但与其依赖这一点
`04:14` 我们希望代理仅使用已保存到建议文件的记忆
`04:20` 此处的记忆
`04:21` 获取建议文件工具会访问该记忆
`04:24` 完成此循环实际上促使模型专注于记忆内容
`04:29` 而非对话历史中的记忆
`04:32` 然后
`04:33` 从工具调用返回的结果是
`04:36` 将呈现给用户供其审批
`04:38` 如果用户批准
`04:39` 则继续说这很棒
`04:42` 通常建议的文件
`04:44` 但如果用户有反馈
`04:45` 返回步骤一并根据反馈重复流程
`04:51` 现在可以定义代理使用的工具
`04:55` 第一个工具是获取已批准用户目标
`04:58` 此功能在提供的工具模块中定义
`05:02` 它仅返回存储在状态中的已批准用户目标
`05:06` 我们还将导入一个小助手函数
`05:08` 这些即将定义的工具
`05:10` 将使用这些工具与文件系统交互
`05:14` 但并非整个文件系统
`05:16` 它们仅限于查看可导入的文件
`05:19` 新四J本身可以访问导入目录
`05:24` 这个助手将告知导入目录的位置
`05:28` 所有工具均可使用该导入目录确定可用文件
`05:32` 并在这些文件中选择需要使用的
`05:35` 然后定义列出导入文件的工具
`05:37` 并将结果保存到此键
`05:39` 所有可用文件
`05:41` 工具调用需要工具上下文
`05:43` 因为我们将使用一些辅助工具
`05:46` 实际获取磁盘上的目录
`05:48` 这些文件存储的位置
`05:49` 因此列出的文件仅为相对路径而非绝对路径
`05:55` 在实现部分可以看到
`05:56` 我们将获取此辅助函数
`05:58` 获取四J导入器
`06:00` 这是四J可访问的数据导入目录
`06:04` 数据无法访问整个磁盘空间
`06:06` 因此不能使用绝对路径
`06:07` 必须基于此导入目录
`06:10` 在此处获取该目录
`06:11` 然后列出所有文件
`06:14` 这只是一个使用导入目录的字符串
`06:17` 我们继续对目录进行全局列表
`06:20` 从该目录递归向下
`06:22` 并通过列表推导生成相对文件列表
`06:27` 将结果保存到当前状态的可用文件
`06:31` 然后将此作为工具调用结果返回
`06:34` 接下来定义样本文件工具调用
`06:37` 样本文件工具同样使用工具上下文
`06:42` 并使用文件路径
`06:44` 这是目标文件的相对路径
`06:46` 语言模型决定要查看的文件
`06:52` 可能调用操作系统执行类似cat的操作
`06:56` 我们将全部用Python实现
`06:58` 此工具调用结果仅为文本前100行
`07:02` 假设为文本文件
`07:04` 获取100行文本
`07:05` 返回该结果
`07:06` 现在这个工具调用的值
`07:08` 这里我们设置了几个防护措施
`07:10` 因为语言模型可能生成路径
`07:12` 它可以生成各种不同的内容
`07:14` 当它调用这个功能时
`07:15` 我们将对传入的文件路径进行合理性检查
`07:19` 确保它不是绝对路径
`07:21` 如果它是绝对路径
`07:22` 我们将返回一个错误
`07:23` 因为它必须是相对文件
`07:25` 并且预期这些文件相对于导入目录
`07:28` 最后这部分
`07:30` 我们也鼓励代理记住传入的文件路径
`07:35` 应出现在可用文件列表中
`07:37` 所以它应该实际检查这一点
`07:40` 基于导入目录
`07:41` 我们获取该文件的完整路径
`07:43` 再次确认文件存在
`07:45` 返回有用的错误信息
`07:46` 如果文件不存在
`07:47` 以便各方采取纠正措施
`07:51` 如果文件存在
`07:52` 我们将读取前100行并保存到内容中
`07:56` 作为成功的工具调用返回
`07:59` 如果发生任何错误
`08:00` 我们将传递该错误作为对黄色语言模型的响应
`08:04` 代理可以决定是否能处理
`08:07` 或只需向用户报错
`08:10` 你已赋予代理列出所有文件的能力
`08:13` 查看这些文件
`08:14` 现在它能基于这些文件和内容提出建议
`08:19` 但代理认为用户需要的相关文件
`08:23` 我们有一组工具实现此功能
`08:25` 第一个是设置建议文件列表
`08:27` 接受应为文件路径的字符串列表
`08:30` 你可以在此添加额外检查
`08:32` 确保传入的列表都是实际存在的文件
`08:38` 我们在此稍作信任处理
`08:39` 这个工具存在一定的冒险性
`08:41` 当然可以改进
`08:42` 你可以添加此检查
`08:44` 另一方面
`08:45` 我们将有一个获取建议文件的工具
`08:49` 查看已建议的文件列表
`08:53` 当代理成功确定相关文件后
`08:59` 并向用户提出建议
`09:01` 如果用户已批准
`09:02` 当然
`09:02` 将调用批准建议文件工具进行状态转移
`09:07` 将建议文件状态转为已批准
`09:10` 正如上一课中
`09:11` 在调用此功能前我们将再次确认
`09:15` 确保建议的文件确实存在于状态中
`09:18` 若未建议则需让代理先查明情况再调用此工具
`09:23` 如果已设置
`09:24` 我将复制建议文件到批准文件并返回结果
`09:29` 作为此工具调用的成功响应
`09:31` 然后你可以将所有工具定义
`09:34` 整理成列表
`09:35` 并保存为文件建议代理工具
`09:38` 我们将用于代理定义
`09:40` 稍后即会看到
`09:41` 现在你可以定义你的代理
`09:43` 代理定义非常直接
`09:45` 我们已完成了工具设置和提示定义
`09:49` 只需将这些内容传入并创建代理
`09:53` 拥有代理后
`09:54` 现在可以与代理交互
`09:57` 导入
`09:57` 我们之前定义的make agent color助手
`10:00` 然后使用该助手创建调用者
`10:03` 这里只有一个细微不同之处
`10:06` 当我们使用make agent color时
`10:08` 只需直接调用代理
`10:10` 我们想要执行
`10:12` 注意这里我们用批准的用户目标初始化状态
`10:17` 正如之前所说
`10:18` 在工作流程开始时
`10:19` 批准的用户目标
`10:20` 是所有代理的总体方向
`10:23` 以及它们要完成的任务
`10:24` 因此在最终生产系统和多代理系统中必须设置此内容
`10:30` 到此工作流程阶段时
`10:32` 状态中已包含用户目标
`10:35` 因为我们已完成了前期工作流程
`10:38` 而此处尚未经历该阶段
`10:40` 通过设置初始状态模拟此过程
`10:42` 如同已批准一个需要进行供应链分析的用户目标
`10:48` 描述涉及制造产品的多层次物料清单
`10:53` 然后可以运行简短对话
`10:56` 用户询问可用文件用于导入
`10:59` 文件建议代理将执行所有处理
`11:03` 确定可用文件及相关文件
`11:07` 最终可用文件列表应包含目录中所有文件
`11:11` 建议文件是其中子集
`11:13` 希望仅为CSV文件
`11:16` 这可能需要几秒钟处理
`11:18` 因为语言模型
`11:19` 当然
`11:20` 正在分析所有找到的文件以判断相关性
`11:24` 我们可以看到这个响应做得相当不错
`11:27` 它已经识别出这些装配件的csv文件
`11:31` 一些组件
`11:32` 从零件到供应商的映射
`11:34` 一些产品本身
`11:36` 供应商本身
`11:37` 还有一些完美的零件
`11:40` 如果查看可用文件
`11:41` 有很多额外文件
`11:43` 特别是所有这些markdown文件
`11:45` 它已经忽略了这些
`11:46` 并正确建议了这些装配文件
`11:49` 组件文件
`11:50` 所有的csv文件
`11:51` 太棒了
`11:53` 由于代理提出了这些优秀建议
`11:56` 你可以继续向代理发送新消息
`11:58` 只需说同意
`11:59` 我们来做吧
`12:00` 可以重新表述这句话
`12:01` 或许根据代理的回应
`12:04` 这可能不是批准的好方法
`12:05` 让我们试试这个
`12:07` 代理对此非常满意
`12:10` 应该调用工具以批准这些文件
`12:13` 从会话状态可见，已批准文件与之前一致
`12:17` 建议文件成功通过
`12:20` 你现在已构建了两个代理
`12:22` 构建结构化数据知识图谱的流程
`12:26` 第一个确定用户意图
`12:28` 下一个基于用户意图
`12:30` 寻找可用的数据文件
`12:33` 支持用户意图的文件
`12:35` 在下一课中
`12:36` 你将进行下一步操作
`12:37` 在给定用户意图的情况下
`12:39` 文件已可用
`12:40` 知识图谱可能是什么样子
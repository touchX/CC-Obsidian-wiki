---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=37"
bvid: "BV1DfrdByE2H"
cid: "35394226883"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394226883&page=37&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 你现在可以开始定义系统中的第一个代理
`00:03` 用户意图代理
`00:05` 其主要目标是帮助你构思可以构建的知识图谱类型
`00:10` 以及你希望从图中回答的问题
`00:13` 让我们开始编码
`00:15` 回想一下我们在第二课中看到的示意图
`00:18` 该图展示了整体知识图谱代理架构
`00:21` 我们将逐步讲解结构化数据代理
`00:24` 这是负责从CSV文件提取信息的子代理
`00:30` 将数据转换为图谱
`00:32` 现在我们将逐步完成这一过程
`00:35` 我们将以这种方式逐步推进
`00:38` 第一步是理解用户意图，这一阶段会发生什么
`00:43` 当我们建立
`00:45` 用户的目标是什么
`00:46` 用户描述的意图将影响所有后续代理的操作
`00:51` 这将为我们的目标设定总体方向
`00:54` 所有代理都会据此做出响应
`00:57` 这个代理只有一个主要任务
`01:00` 其输出是保存到记忆中
`01:03` 已批准的用户目标是什么
`01:05` 为此它有两个工具可用
`01:08` 它有一个工具用于初步感知
`01:11` 用户的意图是什么
`01:12` 基于与用户的对话互动
`01:16` 根据其理解
`01:18` 用户希望
`01:19` 它只能回应
`01:20` 好的
`01:21` 这就是我感知到的需求
`01:22` 因此它会调用工具来捕捉这一信息
`01:26` 然后告知用户
`01:28` 这就是我理解的需求
`01:29` 用户可以选择确认
`01:31` 正确或不正确
`01:32` 不太准确
`01:34` 再试一次
`01:35` 如果你想重新尝试
`01:36` 代理需要继续使用感知到的用户目标
`01:40` 直到用户认为
`01:41` 这就是正确答案
`01:42` 我批准了
`01:44` 只有当用户批准时
`01:46` 代理应调用已批准的感知用户目标
`01:49` 我们将看到这些工具如何协作
`01:51` 更新记忆并捕获已批准的用户目标
`01:56` 注意我通过批判性思考设定感知用户目标
`02:00` 无法直接设定已批准用户目标
`02:03` 只能通过调用此工具
`02:05` 这个机制最终起到守门员作用
`02:07` 确保我们有一个与用户确认的检查点
`02:09` 用户是否明确亲自表示同意
`02:12` 这很好
`02:13` 我们就这样做吧
`02:14` 好的
`02:15` 考虑到代理的详细信息
`02:16` 我们将继续进行常规设置并导入所需库
`02:23` 并设置ln和快速验证检查
`02:28` 让我们看看
`02:28` 这次它的回应如何
`02:30` 相当一致
`02:31` 是的
`02:32` 我准备好了
`02:32` 今天我能为您做些什么
`02:36` 因此我们可以定义用户意图代理本身
`02:40` 我们将从逐步描述开始
`02:43` 我们实际想要的提示是什么
`02:44` 这个代理的指令是什么
`02:47` 需要定义的第一个提示部分
`02:49` 这个代理的角色是什么
`02:52` 还有它的目标是什么
`02:54` 在这里我们将说明
`02:55` 这个代理是知识图谱用例专家
`03:00` 主要目标是帮助用户构思知识图谱用例
`03:04` 这基本上说明你的职责
`03:06` 代理的任务是帮助用户进行创意构思
`03:10` 它试图实现什么
`03:11` 用户试图实现什么
`03:13` 代理的目标是帮助用户明确自身目标
`03:18` 接下来要添加的提示部分
`03:20` 我称之为
`03:21` 对话提示
`03:22` 这有助于代理理解
`03:24` 在其角色和目标的背景下
`03:28` 应该如何开展工作
`03:31` 在此情境下我们正在确定用户意图
`03:35` 致力于与用户进行创意协作
`03:37` 我们有一些基本想法
`03:39` 如果用户不确定该怎么做
`03:41` 可以提供建议
`03:42` 特别是关于知识图谱的经典用例
`03:45` 因为
`03:45` 当然这个代理现在是知识图谱用例专家
`03:50` 也许LMS已经了解很多知识图谱用例
`03:53` 但如果你在构建内部生产系统
`03:56` 某些企业L1可能不了解你的业务
`04:00` 这是一个绝佳机会
`04:01` 向代理解释你们业务的运作方式
`04:05` 以及什么重要什么不重要
`04:06` 在这里我列举了几种知识图谱的用例
`04:10` 你可以将它们用于社交网络
`04:12` 物流
`04:13` 推荐系统
`04:14` 欺诈检测
`04:15` 或者
`04:15` 当然像流行文化这类事物
`04:17` 如果你想追踪电影
`04:18` 书籍或音乐
`04:21` 因为这对确定多亚洲系统的整体方向至关重要
`04:26` 我进一步强化了
`04:27` 比如
`04:28` 用户目标实际上由什么构成
`04:31` 在这里我通过提示描述用户目标包含两个组成部分
`04:35` 一个是图的类型
`04:37` 你是在创建社交图谱吗
`04:40` 还是在创建物流图谱
`04:42` 这可能是什么
`04:43` 因此图的类型需用最多三个词描述
`04:50` 我们正在创建的图是什么
`04:52` 除了你创建的图的类型外
`04:55` 还有描述该图的组件
`04:58` 请提供几句话说明该图的意图
`05:02` 例如
`05:03` 如果我们说美国货运物流
`05:07` 描述可能是动态路由和货物配送系统
`05:12` 这是通过少量示例学习实现的
`05:15` 明确向代理及语言模型强调
`05:19` 你试图实现的目标是什么
`05:21` 因为这非常重要
`05:23` 值得重复强调
`05:24` 既在提示中也将在后续工具描述中
`05:27` 这些工具将实际使用用户目标
`05:30` 在定义其内容时
`05:32` 同时确认理解是否正确
`05:35` 我们将整合提示的最后部分
`05:38` 是思维链的指导方向
`05:40` 思维链可以很简单，比如
`05:43` 仔细思考，分步分析
`05:45` 但这里需要更具体
`05:48` 我们希望语言模型执行某些步骤
`05:51` 因此我们将明确说明
`05:53` 我需要你一步步执行以下操作
`05:56` 代理可能会有不同变体
`05:58` 但通过具体说明
`06:00` 当我们明确
`06:01` 希望代理执行的内容
`06:03` 有助于集中注意力并确定处理方式
`06:06` 根据用户的初始互动
`06:09` 最重要的是需再次理解用户目标
`06:13` 我们将再次强调
`06:14` 这就是那种图表
`06:16` 连同描述一起
`06:17` 如果不能确定代理是否需要询问澄清问题
`06:22` 然后只有当代理认为自己理解了用户目标时才调用集合
`06:27` 感知用户目标工具
`06:29` 这就是实际要记录到内存中的内容
`06:31` 用户目标的理解包含两种组件的图表
`06:36` 连同描述一起
`06:38` 最后呈现这个感知用户目标
`06:40` 通过让用户确认
`06:43` 这就是我刚才听到的内容
`06:44` 我说对了吗
`06:46` 如果用户同意
`06:48` 那么
`06:48` 代理可以调用批准的流程目标工具吗
`06:52` 我们提供了大量关于调用此工具的额外说明
`06:55` 以便代理真正理解这个工具的作用
`06:58` 调用此工具时当前感知目标将保存到状态
`07:04` 在批准用户目标键下
`07:07` 好的
`07:07` 所有这些部分将组成提示
`07:10` 我们将提供给代理以整合这些内容
`07:14` 我们将使用Python字符串模板
`07:16` 然后直接插入之前定义的变量
`07:20` 代理角色和目标
`07:22` 对话提示
`07:23` 输出定义
`07:24` 然后还有思维链指导
`07:27` 我们将其打印到屏幕
`07:28` 你可以看到整体结构
`07:36` 现在可以定义这些工具
`07:40` 第一个工具是设置感知用户目标
`07:44` 在这里的注释中可以看到
`07:45` 这是通过工具设置来促进与用户的协作
`07:49` 仅通过该工具将感知值保存到内存
`07:55` 我们真正帮助代理聚焦
`07:57` 比如如何理解用户目标
`08:00` 因为它必须调用此工具
`08:02` 它知道需要这两个组件
`08:04` 我们在提示中已说明
`08:05` 然后在工具定义中
`08:07` 可以看到这两个参数
`08:09` 必须传入图表类型
`08:10` 以及图表描述
`08:12` 同时注意到工具上下文作为最后一个参数
`08:18` 如果你记得之前的课程
`08:20` 当最后一个参数是工具上下文时
`08:22` Adk会自动注入
`08:24` 当此工具在工具描述中被调用时
`08:28` 我们再次说明这是在保存感知用户目标
`08:32` 包括这种类型的图表
`08:33` 以及对其的描述
`08:35` 我们还会描述这些参数
`08:37` 这些必须与我们告诉代理的内容完全一致
`08:41` 在之前的指令中
`08:42` 这将成为提示的一部分
`08:44` 现在工具定义本身会再次说明
`08:47` 重复的次数越多
`08:48` 语言模型越不可能出错
`08:51` 在调用工具和工具内部
`08:53` 其实很简单
`08:54` 它只需组装一个小字典
`08:57` 这是由这两个组件组成的用户目标数据
`09:01` 这种类型的图表
`09:02` 以及传入工具的图表描述
`09:05` 这实际上只是简单封装如何访问记忆的方法
`09:10` 同时聚焦记忆访问
`09:13` 上下文状态会更新该字典
`09:16` 因为这是更新操作
`09:18` ADK会检测到状态变化
`09:21` 会将该差异传递给需要感知的其他方
`09:24` 嗯
`09:25` 在运行时环境中
`09:29` 然后定义下一个工具
`09:31` 用于批准感知到的用户目标
`09:34` 这应在设置感知用户目标后触发
`09:39` 然后用户也明确同意
`09:41` 如果这两项都成立
`09:44` 现在应调用此工具
`09:46` 在可以时
`09:47` 在可行且合理时
`09:49` 在工具内部
`09:50` 值得信任
`09:52` 但需验证语言模型在此工具中的正确性
`09:56` 这仅在用户批准后调用
`09:59` 我们将再次迭代
`10:00` 与语言模型
`10:01` 嘿
`10:02` 仅在用户批准后调用
`10:05` 如果用户通过调用此工具批准
`10:07` 工具将记录感知目标为已批准目标
`10:12` 再次告知代理
`10:14` 仅调用此工具
`10:15` 若用户已在工具内明确批准感知目标
`10:22` 因为我们已设置流程
`10:25` 可在执行其他操作前先检查是否已设置
`10:29` 现在
`10:29` 如何处理此设置至关重要
`10:31` 因为工具调用可能成功或出错
`10:36` 这里我们可能返回特定错误
`10:39` 如果感知到的用户目标不在当前上下文中
`10:42` 如果它不在工具上下文状态中
`10:45` 因此，代理的记忆
`10:46` 如果感知到的用户目标不存在
`10:49` 我们将继续从该工具返回错误
`10:51` 我们将提供的错误信息旨在帮助语言模型理解
`10:56` 这就是哪里出错了
`10:58` 这就是你应该如何解决问题
`11:00` 因此我们告知LLM过程用户目标未被设置
`11:04` 并应先设置感知用户目标或询问澄清问题
`11:08` 如果不确定用户的意图
`11:12` 因此在所有这些地方反复强调给代理
`11:16` 从提示到工具的定义
`11:18` 再到工具返回的错误信息
`11:21` 鼓励LLM根据实际情况采取正确行动
`11:25` 如果成功的话
`11:26` 我们只需将感知用户目标的状态复制到批准用户目标
`11:32` 请注意只有在以下情况下才会发生
`11:34` 由于我们没有向批准感知用户目标传递参数
`11:38` 批准用户目标仅会被设置
`11:40` 如果存在感知用户目标
`11:42` 则会被复制
`11:43` 此时感知和批准用户目标都应在状态中可用
`11:49` 我们将这两个都添加到列表中
`11:51` 因为我们知道在创建实际代理时需要传递列表
`11:55` 我们有两个工具：设置感知用户目标和批准感知用户目标
`12:00` 因此你可以定义用户意图代理
`12:03` 你需要为其命名
`12:04` 使用唯一的版本名称
`12:07` 我们将使用之前定义的LLM
`12:10` 这将是所有笔记本中相同的LLM
`12:12` 未来可以直接跳过这一步
`12:15` 描述非常重要
`12:17` 该用户意图代理的目的是帮助用户构思知识图谱用例
`12:24` 这有助于整个多代理系统
`12:26` 判断何时使用该代理
`12:29` 你之前从图表中回忆
`12:31` 通过描述匹配该代理在工作流中的角色
`12:36` 这将帮助协调者管理整个工作流
`12:44` 然后我们使用之前组装的完整代理指令
`12:48` 我们将传入这些指令
`12:49` 当然我们还会传入
`12:51` 可用的工具
`12:55` 现在你已准备好完整的用户代理
`12:58` 让我们与之交互
`13:00` 你可以导入你的朋友
`13:01` 创建代理调用器
`13:03` 然后为用户意图代理创建一个调用器
`13:07` 我们将调用用户意图调用器
`13:09` 你可以注意这里的这个便签
`13:11` 当然
`13:12` 如果你要多次运行这个流程
`13:13` 值得回来重新初始化状态
`13:16` 如果事情有点失控的话
`13:22` 现在我们来设置一个与用户意图代理的对话
`13:26` 在实际交互前先获取会话
`13:29` 这样可以看到当前会话状态
`13:31` 这应该为空
`13:32` 最初就像往常一样
`13:34` 然后我们将创建一个脚本化对话
`13:37` 这里只有两个调用步骤
`13:40` 第一个调用
`13:42` 这将是用户
`13:43` 声明用户想要物料清单图或BM图
`13:48` 并包含物料清单的所有层级
`13:51` 从供应商到成品
`13:54` 用户还指定需要这个物料清单图
`13:59` 能够支持根本原因分析
`14:01` 我现在去掉详细输出
`14:05` 但你可以保留
`14:05` 如果你想的话
`14:07` 这是对话的重要部分
`14:09` 有时当代理收到用户的初始消息时
`14:13` 它可能会决定
`14:15` 我需要进一步澄清
`14:17` 如果发生这种情况
`14:19` 则用户目标未被设置
`14:21` 代理可能回应说嘿
`14:24` 告诉我更多关于你的需求
`14:26` 我们在此做假设
`14:28` 如果会话状态中用户目标未设置
`14:31` 假设LLM被要求提出澄清问题
`14:34` 我们以用户身份提供澄清回答
`14:40` 我担心可能的制造或供应商问题
`14:43` 希望这些细节足够让代理满意
`14:46` 并促使它调用用户目标工具
`14:51` 乐观地
`14:52` 如果我们走到这一步
`14:53` 则流程目标已设置
`14:55` 然后我们将说
`14:57` 嗯
`14:57` 听起来不错
`14:58` 我批准这个目标
`14:59` 你可能需要多次运行以防代理未设置用户目标
`15:05` 它可能在某天决定先与用户多交流
`15:10` 确定真正需要什么
`15:12` 用户目标没问题
`15:15` 我在最终调用批准目标时启用了调试
`15:19` 但你可以看到我们在此会话中的过程
`15:21` 会话开始时会话为空
`15:24` 内存状态为空
`15:26` 用户发送了初始消息并收到代理回应
`15:29` 实际上在询问澄清问题
`15:32` 所以它说嘿
`15:33` 你是在尝试从供应商追溯产品的组件
`15:36` 通过制造过程的各个阶段
`15:38` 这是代理的相当长的回答
`15:40` 这没问题
`15:41` 由于我们设置了检查机制
`15:44` 我们继续决定
`15:46` 好的
`15:46` 我们意识到用户的感知目标未设置
`15:49` 我们发送了额外消息给
`15:51` 我担心可能出现制造或供应问题
`15:55` 看来代理随后回应并决定
`15:59` 好的
`15:59` 这已足够继续
`16:01` 然后理解了我们的目标
`16:04` 并告诉我们
`16:06` 好的
`16:06` 这就是你想要的图表类型
`16:08` 我想你在尝试构建
`16:09` 这就是我的理解
`16:11` 你希望该图表描述的内容
`16:14` 最后它说道
`16:15` 正确
`16:16` 嘿
`16:16` 这是否符合你的意图
`16:18` 正是我们想要做的
`16:20` 我们想说
`16:21` 这就是我认为你在询问的内容
`16:23` 这正确吗
`16:24` 然后作为用户我们将批准该目标
`16:28` 这是我们之前决定的对话记录
`16:31` 可以看到在优化目标时
`16:33` 用户意图代理设置了感知用户目标
`16:37` 实际上尚未设置流程目标
`16:41` 或再次调用它
`16:43` 看起来实际上
`16:44` 然后在设定感知用户目标后
`16:49` 因为用户已批准
`16:51` 调用已批准的感知用户目标
`16:55` 好的
`16:56` 你可以看到底部的最终回复
`16:58` 用户目标已成功批准
`17:00` 一切正常
`17:02` 工作流程现在明确了用户的目标方向
`17:05` 在多代理系统中构建知识图谱
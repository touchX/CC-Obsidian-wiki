---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=36"
bvid: "BV1DfrdByE2H"
cid: "35394226797"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394226797&page=36&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 上节课你学习了如何使用Google Adk创建一个代理
`00:04` 现在我们将创建多个代理
`00:07` 一组协同工作的代理团队
`00:09` 我们将有一个根代理
`00:10` 然后构建两个子代理，基于之前的内容
`00:13` 我们需要说
`00:14` 你好
`00:15` 代理
`00:15` 我们将引入一个新代理来执行告别
`00:18` 然后我们将创建另一个代理，将其整合为一个完整的多代理系统
`00:23` 我们开始导入所需的库
`00:26` 当然
`00:27` 接下来设置我们将继续使用的语言模型
`00:30` 这是OpenAI
`00:31` 我们定义语言模型
`00:33` 并通过传递消息进行快速验证
`00:36` 确保系统已就绪
`00:38` 总是有趣看看OpenAI当天会说什么
`00:42` 通常会发送友好消息
`00:43` 它会友好回应
`00:46` 可以设置代理调用器
`00:48` 我们将使用之前定义的同一类
`00:51` 在第三课第一部分
`00:53` 无需重复定义所有内容
`00:55` 只需导入所需工具
`00:57` 如果你还记得第一部分
`00:58` 我们有一个辅助函数
`00:59` 创建代理颜色
`01:00` 将生成包含运行所需功能的代理颜色类
`01:04` 所有准备就绪
`01:06` 现在开始构建多代理系统
`01:08` 创建多个专业代理
`01:11` 每个负责特定功能
`01:14` 创建一个负责问候的代理
`01:16` 即说你好
`01:17` 之前的代理负责问候，新代理将处理告别
`01:22` 通过根代理将两者结合
`01:25` 你会听到协调者或调度者等术语
`01:28` 顶层代理
`01:30` 本质上是封装其他代理的代理
`01:33` 并管理其执行
`01:36` 根代理将接收初始用户请求
`01:39` 无论是问候还是用户指定的内容
`01:42` 然后决定如何处理，自行执行或委托
`01:46` 转交给子代理处理
`01:48` 因此根代理
`01:49` 这个顶层协调者将委托问候和告别任务
`01:54` 希望由专门处理这些任务的子代理执行
`01:57` 好的
`01:58` 现在开始组装我们的多代理系统
`02:01` 我们将使用之前提到的打招呼代理
`02:03` 我们现在要添加一个道别代理
`02:05` 然后我们将它们结合起来
`02:07` 首先定义子代理的工具
`02:10` 第一个工具是来自前一课的相同打招呼工具
`02:14` 这是hello world函数
`02:16` 接收人名并生成问候消息作为响应
`02:21` 接下来我们将加入道别函数
`02:25` 这个工具比打招呼更简单
`02:28` 因为它不接受任何参数
`02:29` 无论什么情况都会返回固定的回复
`02:34` 无论你想做什么
`02:35` 你将得到这样的响应
`02:37` 用这两个工具向用户道别
`02:41` 现在可以定义使用这些工具的代理
`02:43` 现在我们将创建专门的问候代理和道别代理
`02:47` 但你会发现非常简单
`02:51` 在定义这些子代理时
`02:53` 我特别强调最佳实践
`02:55` 每个代理的详细描述非常重要
`03:00` 以及传递给它们的指令
`03:02` 一旦建立多代理系统
`03:03` 大部分时间将用于
`03:06` 在设置任何多代理系统后
`03:07` 大部分时间将用于
`03:09` 优化这些指令
`03:11` 这回到经典提示工程
`03:15` 同时重复前一课的内容
`03:18` 描述让其他代理知道
`03:20` 这个代理的功能
`03:23` 以及何时调用该代理进行委派
`03:26` 指令是让代理自身理解
`03:27` 其目的
`03:28` 要实现的目标
`03:30` 可用的工具
`03:32` 以及何时使用它们
`03:33` 这是问候子代理
`03:38` 它只能访问打招呼工具
`03:40` 然后我们将添加道别代理
`03:43` 与问候代理类似
`03:45` 可以查看这里的指令描述
`03:48` 这些指令值得仔细研究
`03:52` 看起来很明显
`03:53` 好的
`03:54` 你的任务是作为道别代理回应
`03:57` 当用户以某种方式说再见时
`04:00` 人们用各种方式道别
`04:02` 这是一个在代理指令中进行少量学习的小例子
`04:07` 这里括号内有示例
`04:09` 例如当用户使用像'再见'这样的词
`04:12` 再见
`04:13` 谢谢再见
`04:14` 或者下次见
`04:15` 这些都是告别的方式
`04:17` 现在大多数LLM不需要额外的例子
`04:21` 但总体来说这是一个好习惯，需要思考
`04:24` 我该如何帮助LLM理解这个代理的目的
`04:28` 以及何时执行和在角色中该做什么
`04:31` 当然我们要交给它说再见
`04:33` 我们之前定义的工具很棒
`04:35` 你知道的
`04:36` 我有两个子模块准备就绪
`04:38` 现在我们需要将它们合并
`04:39` 我们将通过定义根代理来实现
`04:42` 它将理解这两个子代理
`04:44` 并判断何时委派任务
`04:47` 类似于调用工具
`04:49` 但代理知道在与另一个代理对话
`04:51` 因此整个对话历史会被传递
`04:54` 并且对话控制权会转交给子代理
`04:58` 这与调用略有不同
`04:59` 但核心思想相同
`05:01` 当前工作流将从负责的代理开始
`05:05` 根代理到子代理之一
`05:07` 这类似于调用工具
`05:09` 但让我们仔细看看代理委派如何运作
`05:13` 我们知道代理自身已描述了角色
`05:16` 它们会说
`05:17` 你好
`05:17` 亚洲代理与告别代理
`05:19` 两者都清楚自身职责
`05:22` 它们的描述告知其他代理职责
`05:25` 他们的工作是
`05:26` 我们将进一步强调这一点
`05:28` 在给根代理的指令中
`05:30` 根代理需被告知其职责是协调子代理团队
`05:35` 你可以在指令中看到这一点
`05:37` 其主要目标是保持友好
`05:39` 为了保持友好
`05:41` 它拥有两个专用子代理
`05:43` 因此需描述这些子代理
`05:44` 以便代理知道何时使用子代理
`05:48` 这类似于描述
`05:50` 何时使用特定工具
`05:51` 问候代理用于处理简单问候如你好或嗨
`05:56` 当然告别代理用于说再见
`05:59` 无论是'再见'或其他表达
`06:02` 这里明确说明
`06:04` 当收到用户消息时
`06:06` 请委派子代理实际回应用户
`06:10` 与其直接作为协调者回应
`06:12` 所以如果这个方案有效
`06:14` 协调者只需回答一般性问题
`06:17` 当你说问候或告别时
`06:19` 应转交给子代理执行响应
`06:22` 或生成对用户的回复
`06:25` 顶层协调者没有任何工具可用
`06:28` 只能进行对话或转交子代理
`06:31` 我们也将在此分发子代理列表
`06:34` 在子代理的键中
`06:35` 因此子代理包括问候子代理和告别子代理
`06:39` 现在我们将所有代理整合成一个多代理系统
`06:43` 让我们开始与之互动
`06:45` 这与第三课第一部分类似
`06:48` 我们有一个管理对话的异步函数
`06:52` 因此对话可以和之前一样
`06:55` 你好
`06:55` 我 abbk
`06:56` 然后稍后
`06:57` 而不是说
`06:57` 我很兴奋
`06:58` 这段转录将显示
`07:01` 谢谢再见
`07:01` 这将是第二个用户消息
`07:04` 我们还添加了这个true
`07:07` 所以当你调用这个代理调用者时
`07:10` 传入的第二个参数是是否开启详细响应
`07:14` 我们将启用详细模式
`07:16` 这样可以看到后台运行情况
`07:18` 可以理解用户输入信息
`07:21` 观察转交过程
`07:23` 查看工具调用
`07:24` 并看到响应结果
`07:25` 内容较多需要梳理
`07:27` 但至少走一遍有助于熟悉
`07:30` 在调试代理时
`07:31` 交互过程
`07:34` 好的
`07:35` 这开始看起来很正常
`07:37` 接收用户消息
`07:39` 你好
`07:39` 我可能k，可以看到初始事件来自友好代理团队
`07:44` 这就是顶层协调者
`07:46` 将调用子代理的团队协调者
`07:49` 可以看到它的第一个动作是转接到代理
`07:54` 即转交或委托给子代理
`07:57` 并将转接到问候子代理
`07:59` 完美对应问候回应
`08:01` 我可能k 顶层协调者意识到
`08:04` 我有一个擅长问候的代理
`08:07` 让我将控制权转交给那个代理
`08:10` 所以响应的转移没有任何
`08:12` 你知道的价值
`08:13` whatsoever
`08:13` 它只是一个修女
`08:14` 但接下来我们需要看到问候子代理接管控制
`08:17` 所以这里又有一个事件
`08:19` 但现在由问候子代理处理
`08:21` 它将查看对话记录并分析用户消息
`08:25` 并意识到
`08:26` 现在我需要实际处理这个问题
`08:29` 问候子代理将通过调用工具进行响应
`08:33` 你可以看到此处的函数调用及目标函数名称
`08:37` Called
`08:37` Say hello
`08:38` 并将传递一些参数
`08:40` 其中传入的参数是人员名称为abk
`08:44` 代理已从当前语句中识别出
`08:47` 当然
`08:48` 你好
`08:48` 我是abk
`08:49` 它成功提取了abk应为人员姓名
`08:52` 并将调用工具函数传入该名称
`08:57` 然后接收响应
`08:59` 这里是函数响应，回复是向abk问好完美
`09:06` 现在问候子代理已完成
`09:07` 继续执行，最终消息为true
`09:11` 这是表示已完成处理的标志
`09:13` 我们可以终止此事件循环
`09:17` 最终代理响应即为工具返回的结果
`09:21` 向你问好abk
`09:24` 我们发送第二个查询
`09:26` 或用户的留言，用户说谢谢再见
`09:31` 顶层代理将继续处理
`09:33` 哦其实不是
`09:34` 仍由问候子代理控制
`09:36` 问候子代理自身会意识到
`09:39` 好的
`09:39` 这不是我该处理的内容
`09:40` 请转交给告别子代理
`09:43` 它已知晓可用的其他代理
`09:45` 转交给告别子代理
`09:48` 此处函数响应结束
`09:53` 然后是告别子代理的留言
`09:56` 再次查看对话记录
`09:58` 在接管当前对话后
`10:01` 首先会意识到
`10:03` 好的
`10:03` 用户说了再见
`10:05` 我将调用说再见工具来生成响应
`10:09` 这就是用于道别的函数调用
`10:12` 当然它像预期的那样不接受任何参数
`10:16` 但它的响应是我们之前在工具定义中看到的常量
`10:20` 它将获取告别信息
`10:22` 这是来自cipher的再见
`10:24` 这再次是此事件循环的最终响应
`10:28` 因此这将是最终的代理响应
`10:30` 来自cypher的再见
`10:32` 好的
`10:32` 刚才讲解的内容很多
`10:34` 花时间理解这些内容对后续工作很有帮助
`10:38` 你可以看到所有工具调用的交互过程
`10:41` 代理委托机制
`10:42` 以及整体状态的变化和响应
`10:45` 非常值得这样做
`10:47` 我们不会在每个笔记本中都这样做
`10:49` 但如果你有时间
`10:50` 请务必花时间深入理解背后的原理
`10:54` 因为幕后发生的事情会影响
`10:56` 当然整体架构的定义方式
`11:00` 以及代理编排流程
`11:02` 你将在多代理系统中再进一步
`11:05` 我们有一个代理的执行环境
`11:08` 我们拥有多代理团队协同工作
`11:11` 它们可以互相委托任务
`11:13` 每个代理还拥有工具
`11:15` 所有代理系统最重要的组件是记忆功能
`11:21` 记忆就是内部状态
`11:22` 在特定会话中涉及的任何代理之间持续存在
`11:27` 当然还有用户自己
`11:30` 什么是会话状态
`11:31` 在Google Adk中默认的会话状态其实就是一个可用字典
`11:36` 你可以更新该字典中的键值
`11:39` 当你更新这些键的值时
`11:42` Google hk会追踪这些变化
`11:44` 基本追踪状态的增量变化
`11:46` 并更新整体会话状态以保持一致性
`11:51` 无论代理是并行还是顺序运行
`11:54` 因为系统本质上是异步的
`11:56` 它管理所有状态更新
`11:59` 代理有几种不同的状态交互方式
`12:02` 其中一种我更推荐的方式
`12:04` 与状态交互的主要方法是通过工具上下文
`12:08` 每当调用工具时
`12:10` 会有一个额外的参数可用
`12:12` 即该工具被调用时的上下文
`12:15` 基于此工具上下文
`12:17` 工具本身可以访问当前状态或代理的记忆
`12:22` 并利用这些记忆做出不同决策
`12:25` 生成不同输出
`12:26` 无论对内存是否合适
`12:29` 代理还可以通过使用输出键与状态交互
`12:33` 因此无需使用工具调用来更新某些内存
`12:36` 以及访问内存时
`12:38` 你可以获取代理最终响应的实际输出
`12:42` 而不是仅将此结果作为代理的响应返回
`12:45` 你可以通过定义输出键保存中间状态
`12:48` 当你看到这个最终结果时
`12:51` 与最终结果相关的信息
`12:53` 此处应为文本内容
`12:54` 再见信息可用于更新自身状态
`12:58` 这有时非常方便
`13:00` 稍后在这些笔记本中我们会稍作演示
`13:04` 好的
`13:04` 在下一步中我们将设置一些内存
`13:06` 我们始终使用当前的内存会话
`13:10` 因此内存仅保存在RAM中
`13:13` 并未持久化到数据库
`13:15` 当然这是生产系统的最佳做法
`13:18` 但出于便利性
`13:19` 使用内存状态完全没问题
`13:22` 我们将更新两个工具以利用内存状态
`13:27` 无论是打招呼还是道别
`13:29` 我们将更新这些工具以利用工具上下文
`13:33` 用于更新会话状态或内存
`13:36` 应逐一进行并仔细对比差异
`13:40` 你会发现我们导入了google adk的工具上下文
`13:44` 工具上下文是管理会话状态的类
`13:48` 除了其他对工具有用的功能
`13:52` 我们将更新打招呼函数为状态化版本
`13:57` 状态化
`13:57` 仍接受用户姓名参数
`14:00` 但新增了工具上下文参数
`14:04` 工具上下文将让我们访问会话状态
`14:08` 由执行环境传递的会话参数
`14:12` 在工具上下文中有一个状态字典
`14:15` 我们将更新用户名键
`14:18` 使用函数传入的名称
`14:20` 用于更新会话内存
`14:23` 或会话状态中的用户名键
`14:26` 这非常直接，用户名用户名
`14:29` 现在有一个所有代理共享的字典
`14:32` 所有工具均可访问此函数传入的值
`14:37` 这是一种通过函数自然使用来跟踪信息的方式
`14:42` 同时为后续调试保留这些信息
`14:47` 当调用此工具时
`14:48` 我们将打印会话状态的更新
`14:51` 特别是用户名键
`14:53` 当然我们仍将执行之前的操作
`14:55` 说 hello
`14:56` 我们将向near发送一个查询请求，使用查询参数j
`15:00` 这就是用户名
`15:01` 现在你已经定义了，我们继续定义这个
`15:05` 你可以为say goodbye定义相同的内容，再次操作
`15:09` 在定义这个过程中
`15:10` 这只是对say goodbye的更新
`15:12` 我们之前定义的那个
`15:13` 现在不同的是这个say goodbye
`15:15` 尽管它不接受任何与用户相关的参数
`15:19` 这里没有自定义参数
`15:21` 但它会接收工具上下文
`15:24` 我需要说明这是Google ADK自动处理的
`15:27` 当检测到工具的最后一个参数是工具上下文时
`15:32` 会自动注入该上下文到工具调用中
`15:35` 当执行say goodbye
`15:36` Stateful被调用时
`15:38` 将获取工具上下文但无其他内容
`15:40` 但因为say hello
`15:42` Stateful设置了状态
`15:45` 将用户名存入状态
`15:48` say goodbye
`15:49` 可以实际访问用户名
`15:50` 现在我们记住了用户的姓名
`15:53` 可以访问状态键为username的值
`15:56` 并设置默认值以防用户未自我标识
`16:01` 但可以从字典中获取
`16:03` 用户名或默认值
`16:04` 赋值给用户名变量
`16:06` 然后用于生成告别语句
`16:12` 你可以定义新的stateful代理处理告别
`16:15` 这与之前的联邦代理相同
`16:19` 但现在会调用say goodbye stateful工具
`16:23` 如同之前
`16:24` 可通过根代理或协调者将它们组合成多代理系统
`16:29` 由根代理或协调者使用这两个子代理
`16:33` 这与之前的根代理完全相同
`16:37` 现在仅使用核心基于状态的子代理
`16:42` 这些具有状态管理的工具
`16:45` 现在拥有利用记忆的多代理系统
`16:49` 你可以尝试一下
`16:51` 使用朋友创建代理调用者，传入新root代理stateful
`16:56` 这是协调者的顶层
`16:57` 为该代理创建调用者以观察会话变化
`17:02` 我们直接从调用者获取会话
`17:04` 记得有获取会话的实用函数
`17:07` 查看亚洲调用者类
`17:09` 我们获取会话
`17:11` 基于该会话
`17:12` 打印创建时的初始状态
`17:15` 应看到当前状态为空
`17:18` 正如预期的那样
`17:19` 初始状态为空
`17:22` 你现在可以定义一个对话
`17:24` 这是我们一直在进行的对话，比如说你好
`17:27` 说再见
`17:28` 让我们看看会发生什么
`17:30` 实际上我现在要移除这些冗长的语句
`17:33` 你可以选择保留这些或添加它们
`17:35` 如果你想的话可以任选其一运行
`17:37` 但为了减少输出内容
`17:39` 我们先不带调试语句运行这段代码
`17:42` 这里真正重要的是我们看到初始状态为空
`17:46` 并且在这些调用完成后
`17:49` 初始状态应变为最终状态
`17:52` 现在我们再次获取它
`17:54` 最终状态应包含由调用定义的用户姓名
`18:00` 说你好
`18:04` 太棒了
`18:05` 我们的工具函数在这里打印了用户名更新
`18:10` 当我们获取会话时
`18:13` 然后从会话中获取状态，最终状态包含用户名abk
`18:20` 太好了
`18:21` 一切按预期运行
`18:24` 现在
`18:25` 如果你想的话
`18:25` 你可以在交互环境中运行这段代码
`18:28` 甚至在这个笔记本中
`18:29` 可以设置一个小的辅助函数
`18:32` 用于循环获取用户输入的消息
`18:35` 然后调用我们的caller函数传入该消息
`18:38` 只要用户不输入退出消息就会持续运行
`18:44` 当你设置好并运行它时
`18:45` 会弹出一个小窗口
`18:47` 当你运行交互会话时
`18:49` 可以输入消息并观察结果
`18:55` 我先不透露我的名字
`18:56` 看看会发生什么
`18:59` 好的
`19:00` 这有点有趣
`19:01` 语言模型推断出我的名字在这里
`19:03` 基于这条消息
`19:05` 这并不理想
`19:06` 让我们纠正它
`19:07` 让我们说
`19:12` 只需传入我的名字是abb
`19:14` K 工具上下文已更新为abk
`19:18` 有一个名为say hello的工具
`19:20` 针对abk
`19:21` 因此语言模型正确识别
`19:24` 我的名字是abk
`19:25` 将此保存到内存中
`19:27` 这太棒了
`19:28` 如果我说再见
`19:30` 现在我们应该通过abk收到一条好消息
`19:32` 和之前一样
`19:33` 试试这个
`19:34` 与它互动并看看你能想到什么
`19:36` 而且
`19:36` 当然
`19:36` 如果你想修改代码结尾并观察其影响
`19:41` 好的
`19:41` 你已经完成了第三课的所有内容
`19:45` 你创建了一个基本的单体代理，它会说你好
`19:49` 然后你创建了一个多代理系统
`19:52` 包含两个子代理，分别擅长说你好或再见
`19:55` 然后根代理负责协调与用户的交互
`19:59` 有了这些
`20:01` 你已准备好继续进行遗传知识图谱构建
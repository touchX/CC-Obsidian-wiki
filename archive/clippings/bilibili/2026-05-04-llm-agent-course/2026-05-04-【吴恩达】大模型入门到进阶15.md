---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=15"
bvid: "BV1DfrdByE2H"
cid: "35394162427"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394162427&page=15&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 大型语言模型决定调用函数的过程可能显得有点神秘
`00:05` 最初因为语言模型只是被训练生成输出文本或文本标记
`00:11` 那么这个过程在这个视频中是如何运作的
`00:13` 我想一步步带你们了解
`00:15` 让机械臂能够调用函数的过程到底是什么样子的
`00:21` 让我们先看看
`00:22` 工具其实就是代码或函数
`00:25` 然后一个元素可以像这样请求执行
`00:28` 我们之前视频中看到的获取当前时间函数
`00:32` 当今领先的模型都是直接训练使用工具的
`00:36` 但我想带你们了解
`00:37` 如果需要自己编写
`00:40` 提示指令来告诉它何时使用工具
`00:43` 这就是我们早期需要做的事情
`00:46` 在模型直接训练使用工具之前
`00:49` 尽管我们现在不再完全这样操作
`00:51` 这能帮助你更好地理解整个过程
`00:54` 接下来视频会讲解更现代的语法
`00:57` 如果你已经实现了获取当前时间的函数
`01:00` 为了将这个工具提供给语言模型
`01:03` 你可能会写出这样的提示
`01:05` 你告诉模型：你有一个名为获取当前时间的工具可以使用
`01:09` 请输出以下文本
`01:12` 打印全部大写函数
`01:14` 然后打印获取当前时间
`01:15` 如果我看到这段全部大写函数和获取当前时间
`01:19` 这就表明你需要调用获取当前时间函数
`01:23` 当用户询问时间时
`01:25` 模型将意识到需要调用获取当前时间的请求
`01:29` 因此模型会输出被指示的全部大写函数
`01:34` 现在调用获取当前时间
`01:36` 我不需要编写代码检查模型输出
`01:40` 查看是否存在全部大写函数
`01:42` 如果存在则需要提取参数
`01:46` 通过获取当前时间确定要调用的函数
`01:49` 然后需要编写代码实际调用该函数
`01:53` 并提取输出结果
`01:55` 例如输出为八点
`01:57` 这属于开发者编写的代码
`02:00` 我的代码需要将八点时间处理
`02:03` 将八点返回给模型作为计算历史的一部分
`02:07` 并记录对话历史
`02:08` 当然包括初始提示内容
`02:10` 函数调用请求等信息
`02:13` 最后需要知道之前发生了什么
`02:16` 你是否提出了这个问题
`02:18` 请求函数调用
`02:19` 然后调用函数并返回八点
`02:23` 最后可以综合所有信息生成最终回复
`02:27` 因此现在时间是八点，明确说明
`02:29` 为了调用一个函数
`02:31` 语言模型不会直接调用函数
`02:34` 相反
`02:35` 输出特定格式的内容
`02:37` 比如这样来告诉我需要调用该函数
`02:41` 然后告知函数的输出结果是什么
`02:45` 在这个例子中我们只提供了一个函数
`02:49` 但可以想象如果提供三个或四个函数
`02:52` 可以让它输出全大写的函数名称
`02:55` 然后是被调用的函数名称
`02:57` 甚至可能包含这些函数的参数
`03:00` 实际上现在让我们看一个稍微复杂一点的例子
`03:03` 获取当前时间函数接受时区参数
`03:08` 以获取所需时区的当前时间
`03:10` 在第二个例子中
`03:11` 我编写了一个获取指定时区当前时间的函数
`03:16` 此处时区是传递给获取当前时间函数的输入参数
`03:22` 为了让模型使用此工具回答问题
`03:25` 比如新西兰现在几点
`03:27` 因为我的答案在那里
`03:28` 所以如果查询
`03:29` 我会查找新西兰当前时间
`03:31` 为了让模型使用此工具
`03:33` 你可能需要修改系统提示
`03:36` 你可以使用获取当前时间工具指定时区
`03:39` 使用时需输出以下内容获取当前时间
`03:41` 然后包含时区信息
`03:43` 这是一个简化的提示
`03:45` 实际上
`03:45` 你可能需要在提示中添加更多细节
`03:49` 说明函数使用方法等
`03:50` 在这个例子中
`03:51` 模型将意识到需要获取新西兰时间
`03:55` 因此会生成输出
`03:58` 例如函数：获取当前时间
`04:01` 太平洋/奥克兰
`04:02` 这是新西兰时区
`04:04` 因为奥克兰是新西兰主要城市
`04:06` 然后我需要编写代码检查
`04:10` 输出中是否全大写出现函数
`04:13` 如果出现则提取需要调用的函数
`04:17` 随后调用带有指定参数的获取当前时间
`04:20` 该参数由模型生成
`04:22` 可能是奥克兰并返回凌晨四点
`04:25` 然后像往常一样
`04:26` 将结果输入模型并处理
`04:28` 总结为新西兰现在四点
`04:31` 以下是让模型使用工具的流程
`04:34` 必须向模型提供这两部分
`04:37` 实现该函数
`04:38` 然后告诉lm该功能可用
`04:40` 当机械臂决定调用它时
`04:42` 生成特定输出提示你需要调用该函数
`04:48` 对于机械臂
`04:48` 你需要调用该函数
`04:50` 获取此输出
`04:51` 将刚调用的函数输出取回并返回给lm和机械臂
`04:55` 然后利用该输出继续执行下一步操作
`04:59` 在本视频示例中则是生成最终输出
`05:03` 但有时你甚至可能决定下一步再调用另一个工具
`05:09` 现在这种全大写函数语法
`05:12` 显得有点笨拙
`05:13` 这是我们过去在elm未原生训练前的做法
`05:17` 或让模型自行知晓如何请求工具
`05:20` 符合现代规范
`05:22` 无需告知它输出所有猫函数
`05:25` 然后搜索所有猫函数等
`05:27` 而是训练使用特定语法明确请求
`05:32` 当需要调用工具时
`05:33` 在下一视频中讲解
`05:35` 我想与你分享现代语法的实际形式
`05:39` 让elm的工具请求被调用
`05:42` 我们进入下一视频
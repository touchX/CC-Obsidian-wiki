---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=25"
bvid: "BV1DfrdByE2H"
cid: "35394225345"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394225345&page=25&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 在构建基因工作流时
`00:01` 我通常会建议团队专注于获取高质量输出
`00:05` 并优化成本和延迟
`00:08` 但后来发现延迟成本并不那么重要
`00:11` 但我认为提升性能或输出质量通常是最难的部分
`00:18` 直到它真正运行起来
`00:20` 那时或许可以关注其他方面
`00:22` 我曾多次遇到团队构建了一个基因工作流
`00:26` 并将其交付给用户使用
`00:28` 幸运的是有大量用户使用它
`00:31` 导致成本实际成为问题
`00:33` 然后我们不得不
`00:34` 你知道的
`00:35` 匆忙降低成本
`00:37` 但这也是个好问题
`00:38` 所以
`00:39` 我通常会把成本放在次要位置
`00:42` 嗯，不是完全忽视
`00:44` 只是优先级较低需要关注
`00:47` 直到用户量庞大到需要降低人均成本
`00:51` 而延迟问题我会稍加关注
`00:55` 但同样
`00:55` 不如确保输出质量重要
`00:59` 但当你达到目标时
`01:00` 优化延迟和成本的工具会很有用
`01:04` 让我们看看如何实现这些优化
`01:06` 若要优化基因工作流的延迟
`01:11` 我通常会先进行基准测试或计时
`01:16` 在这个研究代理中包含多个步骤
`01:20` 如果我要计时
`01:22` 每个步骤
`01:22` 生成搜索可能需要七秒
`01:26` 网络搜索耗时五秒
`01:27` 这一步三秒
`01:28` 这一步十一秒
`01:30` 最后撰写论文平均十八秒
`01:33` 通过观察整体时间线
`01:36` 可以判断哪些组件有优化空间
`01:43` 在此例中
`01:44` 可能有多种改进方案
`01:46` 如果尚未利用并行处理
`01:49` 对于某些步骤
`01:49` 比如数据获取
`01:51` 或许可以尝试部分操作并行化
`01:56` 或发现某些步骤耗时过长
`02:00` 如果第一步耗时七秒
`02:02` 最后一步十八秒
`02:04` 我可能考虑使用更小的模型
`02:06` 尝试稍低智能的模型看是否仍有效
`02:11` 或寻找更快的语言模型提供商
`02:13` API的损失在线不同接口
`02:16` 有些公司有专用硬件以实现更快处理特定元素
`02:22` 因此有时尝试不同服务商以找到最快返回令牌的更值得
`02:29` 但至少
`02:29` 进行此类时间分析可帮助确定需重点优化的组件
`02:34` 在降低延迟方面
`02:36` 在优化成本方面
`02:39` 类似的计算需评估每一步的成本
`02:42` 也能帮助基准测试并确定需优化的步骤
`02:45` 许多服务商按令牌计费
`02:48` 基于输入和输出长度
`02:50` 许多API服务商按调用次数计费
`02:53` 计算步骤成本可能因服务器容量付费方式不同而异
`02:58` 以及服务费用
`02:59` 对于此类流程
`03:02` 在此例中可能决定该步骤平均每个令牌成本为零点零
`03:08` 四美分
`03:09` 每次网络搜索
`03:10` API可能成本一点
`03:12` 六美分令牌成本如此
`03:14` API调用成本如此
`03:15` PDF转文本成本如此
`03:17` 最终论文生成令牌成本如此
`03:19` 这将再次帮助您了解
`03:21` 是否存在更便宜的组件可用
`03:23` 或寻找成本优化最大机会所在
`03:28` 我发现这些基准测试非常清晰
`03:32` 有时会明确告知某些组件无需过多关注
`03:37` 因为它们影响不大
`03:38` 并非成本或延迟的主要因素
`03:42` 所以当成本或延迟成为问题时
`03:46` 只需测量每一步的成本或延迟
`03:50` 通常能为您提供优化组件的依据
`03:56` 我们即将结束本模块
`03:59` 我知道我们已涵盖很多内容
`04:00` 感谢您的坚持
`04:01` 让我们继续本模块最后一节收尾
---
title: "【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码"
url: "https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=00c261324d44a5cd7366302129b53bde&p=17"
bvid: "BV1DfrdByE2H"
cid: "35394162552"
author: "吴恩达Agent"
upload_date: "2026-01-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115897075242856&bvid=BV1DfrdByE2H&cid=35394162552&page=17&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

视频来源：DeepLearning.AI
课件代码：评论区自取
本课程我们将学习到：
1. 构建智能体设计模式：反射、工具使用、规划与多智能体工作流；
2. 将人工智能与外部工具集成：数据库、API、网络搜索与代码执行；
3. 评估并优化人工智能系统：性能指标、错误分析与生产部署

## 字幕

`00:00` 在几个我参与的项目中
`00:02` 我提供了编写代码的选项
`00:05` 然后执行任务
`00:06` 我希望它能够
`00:07` 现在我已经尝试过几次
`00:09` 我对生成的代码解决方案的巧妙程度感到非常惊讶和欣喜
`00:13` 为了完成各种任务而生成的代码
`00:17` 如果你还没有经常使用代码执行功能
`00:20` 你可能会对这个能让你的应用实现的功能感到惊喜和欣喜
`00:26` 让我们看看
`00:27` 让我们以构建一个能处理数学题的应用为例
`00:32` 文字问题并为你解答
`00:35` 你可能会创建加法工具
`00:37` 减法工具
`00:38` 乘法工具和定义数字工具
`00:40` 如果有人要求请计算十三点二加十八点九
`00:43` 就会触发加法工具
`00:45` 然后得到正确答案
`00:47` 但假如有人输入
`00:48` 什么是√2的平方根
`00:50` 嗯
`00:50` 你可以编写一个新的平方根工具
`00:53` 但可能还需要处理指数运算
`00:58` 事实上
`00:59` 观察现代科学计算器的按键数量
`01:03` 需要为每个按键创建单独的工具
`01:06` 以及我们想要进行的更多数学计算
`01:10` 与其逐一实现这些功能
`01:14` 另一种方法是让系统自动生成并执行代码来指导算法
`01:20` 你可以这样编写提示
`01:22` 编写代码解决用户的查询
`01:24` 将答案作为Python代码返回
`01:26` 用执行python和闭合标签包裹
`01:30` 例如当用户询问√2的值时
`01:36` 然后使用模式匹配
`01:38` 例如
`01:39` 用正则表达式查找起始和结束标签
`01:42` 提取中间的代码
`01:46` 这里可以看到绿色方框中的两行代码
`01:50` 接着执行这段代码并获取结果
`01:54` 在这种情况下得到1.4
`01:56` 一
`01:56` 四二
`01:56` 依此类推
`01:57` 最后
`01:58` 这个数值结果会返回给主程序
`02:01` 然后生成格式美观的答案
`02:06` 代码执行步骤有多种实现方式
`02:09` 一种方法是使用Python的exec函数
`02:13` 这是Python内置函数
`02:15` 将执行你传入的任何代码
`02:17` 这对于编写自己的正确代码非常强大
`02:21` 并让你执行这段代码
`02:24` 尽管存在一些安全影响，我们稍后会在视频中看到
`02:28` 然后
`02:29` 还有一些工具可以让你在更安全的沙盒环境中运行代码
`02:34` 而且
`02:35` 当然
`02:35` 平方根二是一个相对简单的例子
`02:38` 我也能准确编写代码来
`02:41` 例如
`02:41` 进行利息计算并解决比这更复杂的数学问题
`02:48` 这一理念的一个改进
`02:49` 正如我们在反射部分看到的，如果代码执行失败
`02:55` 所以如果出于某种原因
`02:57` 生成的代码并不完全正确
`02:59` 将错误信息返回给语言模型，让它进行反思
`03:04` 并可能修改代码再试一两次
`03:07` 这有时也能得到更准确的答案
`03:12` 运行任意代码确实存在小概率引发问题
`03:19` 最近我的团队成员使用了一个高度编码器
`03:24` 它实际上选择删除了项目目录中的星号派
`03:30` 确实有一个真实案例
`03:31` 最终该解码器确实道歉了
`03:34` 是的
`03:35` 确实没错
`03:35` 这真是个极其愚蠢的错误
`03:36` 可能吧
`03:37` 呃
`03:37` 我很高兴这个代理解码器真的道歉了
`03:40` 但我已经删除了很多Python文件
`03:43` 不幸的是团队成员有GitHub仓库备份
`03:46` 所以没有造成实际损害
`03:47` 但这样会很糟糕
`03:49` 如果这个
`03:49` 你知道的
`03:50` 任意代码
`03:51` 因错误删除大量文件的代码被执行且没有备份
`03:56` 代码执行的最佳实践是运行在沙盒环境中
`04:01` 实际上单行代码的风险并不高
`04:06` 如果我说实话
`04:07` 许多开发者会未经仔细检查就执行来自语言模型的代码
`04:11` 但如果你想更安全
`04:14` 最佳实践是创建沙盒，以防语言模型生成的代码有误
`04:20` 可降低数据丢失或敏感数据泄露等风险
`04:25` 沙盒环境如Docker
`04:27` 或e2b是轻量级沙盒环境
`04:31` 可降低任意代码执行的风险
`04:35` 避免损害系统或环境
`04:39` 事实证明代码执行非常重要
`04:42` 许多语言模型的训练师实际上在做特殊工作
`04:46` 确保代码执行在其应用中运行良好
`04:50` 但我希望
`04:51` 当你将其作为另一个工具添加到可提供给
`04:56` 或让你的应用程序变得远更强大
`05:01` 而我们讨论的内容
`05:02` 你需要创建工具并逐步将其提供给你的语言模型
`05:08` 结果发现许多团队在构建类似的工具
`05:11` 不得不完成构建函数并使其对应用可用的所有工作
`05:17` 但最近出现了一个新标准，称为MCP模型上下文协议
`05:24` 这让开发者更容易访问大量工具集
`05:28` 供ELM使用
`05:29` 这是一个越来越被团队采用的重要协议，用于开发基于语言模型的应用
`05:36` 让我们在下一视频中学习MCP
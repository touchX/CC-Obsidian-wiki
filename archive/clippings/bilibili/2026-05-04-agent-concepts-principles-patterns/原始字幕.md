---
title: "Agent 的概念、原理与构建模式 —— 从零打造一个简化版的 Claude Code"
url: "https://www.bilibili.com/video/BV1TSg7zuEqR/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1TSg7zuEqR"
cid: "31193827342"
author: "马克的技术工作坊"
upload_date: "2025-07-22"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114894200380730&bvid=BV1TSg7zuEqR&cid=31193827342&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Agent 的概念、原理与构建模式 —— 从零打造一个简化版的 Claude Code
 
我们将以 ReAct 和 Plan-And-Execute 这两种模式为例，为大家讲解 Agent 的概念、原理与构建模式，并在这个过程中为大家演示如何从零打造一个简化版的 Claude Code，让大家彻底明白 Agent 是如何运作的。
 
时间轴：
00:00 视频内容介绍
00:33 什么是 Agent
03:43 ReAct 模式的运行流程
05:27 ReAct 模式的实现原理
10:49 动手实现一个 ReAct Agent
18:53 ReAct 运行时序图
20:21 Plan-And-Execute 模式介绍
21:20 Plan-And-Execute 运行流程

## 章节

- `00:00` 视频内容介绍
- `00:34` 什么是 Agent
- `03:44` ReAct 模式的运行流程
- `05:28` ReAct 模式的实现原理
- `10:50` 动手实现一个 ReAct Agent
- `18:54` ReAct 运行时序图
- `20:22` Plan-And-Execute 模式介绍
- `21:21` Plan-And-Execute 运行流程

## 字幕

### 视频内容介绍 `00:00`

`00:00` 今天我们来聊聊agent
`00:01` 它也叫AI agent
`00:03` 这是一个随着大模型热潮而兴起的重要概念
`00:06` 虽然agent这个词现在被频繁的提起
`00:09` 但它究竟是什么
`00:10` 是如何运作的
`00:11` 很多人其实并不清楚
`00:13` 所以在这个视频里面
`00:15` 我会带你彻底搞明白这两个问题
`00:17` 在正式开始前
`00:19` 我需要先提醒一下视频前半部分的一些内容
`00:22` 和我之前发过的MCP终极指南番外篇有所重合
`00:27` 如果你已经看过那一篇了
`00:28` 那这个视频就建议从react代码的实现部分
`00:32` 开始观看

### 什么是 Agent `00:34`

`00:36` 大家都知道现在的大模型
`00:38` 比如GPT4o deep seek之类的
`00:40` 他们回答问题很厉害
`00:42` 逻辑也很强
`00:43` 但平时我们用它们的时候呢
`00:45` 会发现一个限制
`00:47` 他们无法感知或者是改变外界环境
`00:51` 这句话是什么意思呢
`00:53` 我举一个例子来给你说明一下
`00:55` 比如你想让GBT4O帮你写一个贪吃蛇游戏
`00:59` 它确实可以给你代码
`01:01` 但是写完之后
`01:02` 像把代码写入到文件这种事情
`01:05` 还是得你自己动手
`01:06` 也就是说大模型无法改变外界环境
`01:10` 而且有没有一种可能
`01:12` 你其实已经有一些贪吃蛇的代码了
`01:14` 你只是想让模型帮你基于这些代码来改写
`01:18` 增加一些功能
`01:19` 在这种情况下
`01:20` 你就必须把你已有的代码复制给GBT4O才行
`01:24` 我们不主动告诉GBD4O的话
`01:26` 它是无法自己查到这些代码的
`01:29` 换句话说
`01:30` 这就是大模型无法感知外界环境的体现
`01:33` 所以综合来看
`01:35` 大模型是无法感知或者是改变外部环境的
`01:39` 那有没有办法解决掉这个问题呢
`01:42` 其实是有的
`01:44` 我们给它接上对应的工具就可以了
`01:46` 比如说是读写文件内容的工具
`01:49` 查看文件列表的工具
`01:51` 运行终端命令的工具
`01:53` 工具呢就像是大模型的感官和四肢
`01:56` 有了它大模型就可以自己查询已有文件
`01:59` 自己写入代码
`02:01` 自己运行程序
`02:02` 整个过程不需要我们插手
`02:04` 完全自动化
`02:05` 像这样把一个大模型和一堆工具组装起来
`02:10` 变成一个能感知和改变外界环境的智能程序
`02:14` 我们就称它为agent
`02:17` 通常agent用一个机器人来表示
`02:19` 这与大模型的大脑图标形成了鲜明的对比
`02:23` 毕竟agent有了感官和四肢嘛
`02:25` 能自己独立做事了
`02:27` 就像一个机器人一样
`02:29` agent呢有很多类型
`02:31` 前面举的是编程类的agent
`02:33` 它可以用来开发程序
`02:35` 除此之外
`02:36` 还有一些agent可以做PPT
`02:38` 有一些agent可以深度搜索等等等等
`02:41` 总的来说
`02:42` agent的类型有很多
`02:44` 擅长的领域呢也各不相同
`02:46` 下面我们来举几个具体点的例子
`02:48` 第一个例子便是大名鼎鼎的cursor
`02:51` 相信即使你没有使用过
`02:53` 它
`02:53` 也多多少少听说过它的存在
`02:55` cursor是一个用于编程的agent
`02:58` 我们只需要给他提交任务
`02:59` 它便会调用大模型和各种工具来帮我们写代码
`03:03` 直至完成任务
`03:05` 整个过程中你顶多点个确定按钮
`03:07` 别的基本上什么也不用动
`03:09` 再举个例子
`03:11` 前一阵子比较火的MANUS
`03:13` 它也是一个agent
`03:14` 在这个例子中
`03:15` 用户希望minus帮他比较几个手机的性能
`03:19` 照相等能力
`03:20` 为了解决用户的问题
`03:22` minus会生成执行计划
`03:24` 搜索并浏览相关网页
`03:26` 最后把报告整理成一个页面展示给用户看
`03:29` 整个过程基本上也不需要用户插手
`03:32` Minus
`03:32` 利用大模型和一些工具
`03:34` 就可以解决掉用户的问题
`03:36` 好相信到这里呢
`03:38` 你对agent就有一个大致的了解了
`03:40` 下面呢我们就来讲讲agent的运行模式

### ReAct 模式的运行流程 `03:44`

`03:46` agent的运行有很多种模式
`03:48` 其中最有名的一种是react
`03:51` react本身是一个缩写
`03:53` 它的全称是reasoning and acting
`03:56` 也就是思考与行动
`03:58` react可能是目前使用最为广泛的agent运行模式
`04:01` 如果你要学习agent的实现原理
`04:04` 那你就绝对绕不开react这个模式
`04:07` 最初由2022年10月份的一篇论文提出
`04:10` 虽然距离现在已经有接近3年的时间了
`04:13` 但是他所提出的agent的运行模式
`04:16` 仍然有着非常广泛的使用
`04:17` 说它是目前使用最为广泛的AH的运行模式
`04:20` 也不为过
`04:21` 在这种模式下
`04:23` 用户先提交任务
`04:24` 然后agent先做思考
`04:26` 英文是thought
`04:28` 他思考后会决定是否调用工具
`04:30` 如果是的话
`04:32` 他便会去调用合适的工具
`04:34` 比如读取文件
`04:35` 写入文件内容之类的
`04:37` react称这一步是行动
`04:39` 英文是action
`04:40` 在行动后agent会去查看工具的执行结果
`04:44` 比如所读取的文件内容写入是否成功等等
`04:48` react称这一步是观察
`04:50` 也就是观察工具执行结果
`04:52` 英文是observation
`04:53` 在观察之后
`04:55` reacts会继续思考
`04:56` 它会再次判断是否需要调用工具
`04:59` 如果还是需要的话
`05:01` 他就会继续重复之前所说的行动
`05:04` 观察思考的流程
`05:06` 直到某个时刻
`05:07` 他认为不需要再调用工具了
`05:09` 可以直接给出结论了
`05:10` 此时他就输出了最终答案
`05:13` 英文是final answer
`05:14` 整个流程到此结束
`05:16` 所以从这个流程图里面也可以看出
`05:19` react流程的核心步骤是thought action
`05:22` observation和final answer
`05:24` 记住这几个词
`05:25` 我们后面呢会用到

### ReAct 模式的实现原理 `05:28`

`05:30` 了解了react模式的流程之后
`05:33` 下一个问题就是这种react模式是如何实现的
`05:36` 为什么模型拿到用户问题之后会先思考再行动
`05:40` 他为什么不直接行动
`05:42` 是因为模型就这么训练的吗
`05:44` 不是的
`05:45` 这跟模型的训练过程关系不大
`05:47` 大部分的奥秘呢其实都集中在系统提示词上
`05:51` 系统提示词是跟用户问题一起
`05:54` 送给模型的提示词
`05:55` 它规定了模型的角色运行时要遵守的规则
`05:59` 以及各种环境的信息等等
`06:01` 比如我们在系统提示词里面写
`06:03` 你的回答必须包含两个XML标签
`06:06` 一个叫做question
`06:07` 用于存放用户的问题
`06:09` 一个叫做answer
`06:10` 用于存放你的回答
`06:12` 你把这个系统提示词
`06:13` 和用户问题一起发给大模型
`06:15` 在这种情况下
`06:17` 大模型便会遵循这种规范来输出答案
`06:20` 上面举的是一个简单的例子
`06:22` 如果你想要模型按照react模式返回答案的话呢
`06:26` 那你的系统提示词就会更加复杂一些
`06:28` 我这里呢就有一个具体的例子
`06:30` 这个系统提示词大致有五个部分
`06:33` 分别是职责描述示例
`06:36` 可用工具
`06:37` 注意事项和环境信息
`06:39` 我们来仔细读一下
`06:40` 首先看职责描述部分
`06:43` 你需要解决一个任务
`06:44` 为此你需要将任务分解为多个步骤
`06:47` 对于每个步骤
`06:48` 首先使用salt思考要做什么
`06:50` 然后使用action调用一个工具
`06:53` 工具的执行结果会通过observation返回给你
`06:56` 持续这个思考和行动的过程
`06:58` 直到你有足够多的信息来提供final answer
`07:02` 这段话呢
`07:03` 其实就是在描述
`07:04` 我们刚才的那个react执行流程图
`07:06` 我们希望大模型按照react这个标准来运作
`07:10` 后面呢则是专门说明了每个标签的功能
`07:14` 紧接着我举了几个例子
`07:16` 比如说第一个用户的问题是埃菲尔铁塔有多高
`07:20` 模型呢
`07:21` 就先用salt标签做了一些思考
`07:24` 然后再使用action调用了get height工具
`07:28` 传入的参数呢是埃菲尔铁塔工具的返回结果
`07:32` 通过observation返回给了模型模型
`07:36` 接到结果之后呢
`07:37` 他再做了一些思考
`07:39` 然后就给出了最终的答案
`07:41` 这个就是一个非常典型的react流程
`07:45` 后面的例子二呢其实也是类似
`07:47` 只不过是他调用工具调了两遍啊
`07:50` 这个我们就不细说了
`07:52` 再往后我这里列举了一些可用的工具
`07:54` 分别用于读取文件内容
`07:57` 写入文件内容和运行终端命令
`08:00` 都是非常常用的功能
`08:03` 然后呢我们列举了一些注意事项
`08:06` 就是在这里
`08:07` 而且告诉了大模型相关的一些环境信息
`08:11` 比如说是当前的操作系统
`08:13` 目录和目录下的文件列表等等
`08:17` 下面我们就来演示一下
`08:18` 如何使用这个系统提示词
`08:21` 我们用deep seek来举例
`08:22` 我们先把我们的系统提示词复制一下
`08:26` 然后粘贴进来
`08:28` 作为用户输入的一部分
`08:30` 之后再在后面加上具体要完成的任务
`08:33` 写一个贪吃蛇游戏
`08:34` 使用HTMLCSS和JS实现代码分别放在不同的文件中
`08:39` 有一点需要提一下
`08:40` 按照规范的做法
`08:42` 系统提示词和用户任务应该分开传给模型
`08:45` 但deep seek并没有提供
`08:46` 单独提交系统提示词的地方
`08:48` 所以呢
`08:49` 我们就把系统提示词和用户任务合在一起
`08:52` 当成一条消息提交给他
`08:53` 这样的处理方式
`08:54` 在大多数的情况下也是没有问题的
`08:56` 模型依然能够按照预期运行好
`08:59` 让我们提交任务
`09:00` 可以看到deep seek开始运行了
`09:02` 让我们稍等一下
`09:03` 让它运行完毕
`09:04` 可以看出他按照我们的要求
`09:07` 先在salt标签里面思考了一下
`09:09` 然后他使用action标签
`09:11` 请求调用write to fall工具来写入index点HTML文件
`09:15` 这后面就是具体的文件内容了
`09:18` 大家注意我的措辞啊
`09:19` 大模型请求调用工具
`09:22` 这里体现的是请求两个字
`09:24` 大模型本身是不能调用工具的
`09:26` 调用工具的是agent的工具调用组件
`09:28` 这里大模型只能是请求
`09:31` 现在如果运行的是一个真的agent的话
`09:34` 它便会去调用工具背后的right to for函数
`09:37` 写入HTML文件内容
`09:39` 不过我们现在在模拟嘛
`09:41` 我们就假设调用已经完成了
`09:43` 并且假设工具的返回结果是写入成功
`09:46` 所以我们回复observation写入成功
`09:50` 拿到这个结果后
`09:51` deep sick又开始运行了
`09:53` 这次他还是先用salt标签思考了一下
`09:56` 然后再使用action标签请求写入CSS文件的内容
`10:00` 我们照例回复写入成功
`10:02` deep seip又开始返回了
`10:04` 让我们稍等一下
`10:08` 可以看出deep sick还是先用thought思考
`10:11` 再用action请求写入JS文件的内容
`10:14` 我们还是回复写入成功
`10:17` 然后deep seek的返回就有了些变化
`10:19` 因为三个必要的文件都已经写入完成了
`10:22` 不需要再调用工具了
`10:24` 因此deep sick在salt之后返回了一个final answer
`10:27` 整个回答就彻底结束了
`10:29` 你看这就是react模式真正运行时的节奏
`10:32` 每一步都按照系统提示词的要求来
`10:35` Thought action observation
`10:37` Thought action observation
`10:39` 一直到任务完成
`10:40` 此时会输出thoughts和final answer
`10:43` 系统提示词
`10:44` 就相当于给模型安排了一个迷你剧本
`10:46` 他会严格按照这个剧本一步一步的走完

### 动手实现一个 ReAct Agent `10:50`

`10:52` 前面我们用deep sick演示了一个agent的运行流程
`10:56` 可以看到整个流程的关键在于系统提示词
`10:59` 它决定了模型该如何一步步运行
`11:02` 其实在这个系统提示词的基础上
`11:04` 再加上一些配套的代码
`11:06` 我们就可以搭建出一个真正可用的react agent
`11:09` 实际上我已经把这个agent写好了
`11:11` 就放在我的GITHUB仓库里
`11:13` 有需要的同学可以自行获取
`11:16` 接下来我先演示一下这个agent的使用过程
`11:19` 随后再带大家一起看一遍它的代码
`11:22` 我已经进入到这个agent所处的项目目录了
`11:25` 我们先执行一下train命令
`11:27` 看一下这个项目目录里面都有什么文件
`11:31` 这里文件很多啊
`11:32` 但实际上你只有两个文件需要留意一下
`11:35` 第一个是agent的pi文件
`11:37` 这个文件里面就写了我们的agent的代码
`11:41` 我们等会要运行的就是这个文件
`11:43` 另外一个是snake文件夹
`11:45` 它里面什么也没有
`11:47` 我用tree命令给你证实下
`11:52` 可以看出确实没有任何文件
`11:54` 等会儿我就会让agent
`11:56` 把代码写入到这个文件夹里面
`11:58` 好介绍完毕
`12:00` 下面我们来执行一下这个agent
`12:02` 让你看看它是如何运作的
`12:04` 首先我们启动一下这个agent
`12:06` 我们用的命令是uv run agender pie
`12:10` Snake
`12:10` 前面的uv run agender pi
`12:13` 就是用来启动agent点pie文件的
`12:15` 后面的snake是agender pi这个脚本的第一个参数
`12:19` 意思是告诉agent点pi
`12:21` 他要操作的项目目录是snake
`12:23` 代码就写在那里面
`12:25` agent点pie
`12:26` 首先向我们询问需要完成的任务
`12:28` 我们的任务就是写一个贪吃蛇游戏
`12:31` 使用HTMLCSS和JS实现代码分别放在不同的文件中
`12:36` 回车agent点pie已经开始运行了
`12:39` 他现在正在请求大模型
`12:42` 我这里采用了同步返回的机制
`12:44` 所以需要等模型把所有内容都生成完毕之后
`12:47` 才能看到结果
`12:48` 其实也可以用流式返回的模型
`12:51` 返回几个字就能看到几个字
`12:53` 这样可能效果更好一点
`12:54` 不过代码的复杂度会增加
`12:57` 所以综合权衡之后
`12:58` 我就没有使用流失返回
`13:01` 好看到第一轮的结果了
`13:03` 我们的结果一共是包含三个部分
`13:05` Thought ts action observation
`13:09` 跟我们之前在deep seek那里看到的一模一样
`13:12` 这里的action是请求调用right to fall工具
`13:16` 写入index点
`13:17` HTML文件后面的observation
`13:19` 显示的就是具体的调用结果了
`13:21` 写入成功
`13:22` 注意啊
`13:23` 这个写入成功可不是模拟的
`13:25` 这是真的执行了
`13:27` right to fall工具
`13:28` 工具也真的返回了写入成功这几个字
`13:31` 好
`13:31` 这个呢是第一部分
`13:33` 我们把滚动条往下拉一拉
`13:35` 看一下剩下的部分是个什么样子的
`13:37` 后面的流程呢也基本类似
`13:40` 可以看到在observation之后
`13:42` agent会再次请求模型
`13:43` 然后agent就又进行了一段thought action observation
`13:50` 这一轮写入的是CSS
`13:52` 我们再把滚动条往下拖一拖
`13:57` 在这里我们就可以看到第三轮的thoughts action
`14:04` Observation
`14:05` 这次写入的是JS
`14:07` 最后所有文件都写完之后
`14:09` 他会给出sorts和final answer
`14:13` 整个流程就结束了
`14:15` 再看看snake文件夹
`14:18` 确实三个文件都有了
`14:20` 执行index点HTML
`14:22` 看看游戏能不能玩
`14:26` 可以看到界面出来了
`14:28` 我们动一下啊
`14:29` 确实也是能动的
`14:31` 然后吃一个红色的方块呢也是可以吃的
`14:34` 左上角是分数
`14:35` 看来运转的非常顺畅
`14:37` 从这个结果中也可以判断出
`14:39` 我们这个agent做的非常成功
`14:41` 完全可以作为一个简化版的cloud code来使用
`14:44` 下面我们来看一下这个agent的具体代码
`14:47` 我们首先从入口处看起
`14:49` 这里面的project directory
`14:51` 就是我们传给agent的pie文件的第一个参数
`14:54` 也就是snake那个文件夹
`14:56` tools代表可用的工具列表
`14:58` 我们这里给出了三个
`14:59` 分别用于读取文件
`15:01` 写入文件和运行终端命令
`15:04` 这些都是很实用的函数
`15:05` 我们可以大体看一下
`15:09` 这个呢是读取文件
`15:11` 这个是写入文件函数
`15:13` 这个呢是运行终端命令
`15:16` 从这里也可以看出
`15:17` 工具确实就是函数
`15:19` 让我们回到原来的主链路
`15:21` 继续往下看
`15:24` 这里的react agent便是这个文件的核心了
`15:27` 它是一个类构造这个类的时候呢
`15:29` 需要提供三个参数
`15:31` 第一个是工具列表
`15:32` 这个我们前面已经构建好了
`15:34` 这里直接传了进来
`15:35` 第二个是我们要用的模型
`15:37` 我们这里用的是GBD4O
`15:39` 第三个是项目目录
`15:41` 也就是snake文件夹
`15:43` 传好了这三个参数之后
`15:44` 我们便获取到了一个agent变量之后
`15:48` 我们会提示用户输入任务内容
`15:51` 然后我们把用户任务传入agent点run函数
`15:54` 这个函数是react agent的核心调用
`15:57` 它就相当于是启动了这个agent
`15:59` 之前提到的thoughts action observation和final answer
`16:03` 都是在这个函数内部依次处理的
`16:05` 他处理好了之后会给出一个最终答案
`16:08` Final answer
`16:09` 并且把这个final answer输出到屏幕上
`16:12` 到这里主链路就结束了
`16:14` 可以看到这段代码的核心是react agents
`16:16` 我们来看一下它里面写了些什么
`16:19` 首先它这里定义了一些自身的属性
`16:22` 分别是工具列表
`16:24` 模型
`16:24` 项目录和模型
`16:26` 调用客户端到这里构造函数呢就结束了
`16:30` 后面我们要看的函数便是这个agent的重点run函数
`16:36` 这个函数的参数呢是用户输入的任务
`16:39` 在函数的内部
`16:40` 它先构建了一个message列表
`16:42` 里面有两个元素
`16:43` 分别是系统提示词和用户问题
`16:47` 系统提示词是用render system prompts
`16:50` 这个函数来渲染的
`16:51` 他接受一个参数是系统提示词
`16:54` 模板
`16:54` 模板里面的内容是这个样子的
`16:56` 跟我们之前讲的那个系统提示词
`16:58` 基本上是一样的
`17:00` 只不过这个模板里面有一些占位符
`17:02` 比如说是工具列表
`17:04` 操作系统当前目录下的文件列表等等
`17:08` 这些占位符都是render system prompt函数
`17:10` 在运行的时候填进去的
`17:12` 在拼接好了message列表之后
`17:14` 我们使用call model函数调用了模型
`17:17` 拿到了模型的执行结果
`17:18` 然后我们提取出返回结果中的salt部分
`17:22` 并且打印了出来
`17:23` 然后代码会检测salt之后的内容
`17:26` 是不是final answer
`17:27` 如果是的话
`17:28` 我们返回这个final answer函数执行到此结束
`17:32` 如果不是的话
`17:33` 那constant里面一定就包含action了
`17:35` 我们此时就把action给解析出来
`17:38` 提取出其中的函数名和参数列表
`17:42` 然后判断了下当前工具
`17:44` 是不是运行终端命令的工具
`17:46` 如果是的话
`17:47` 我们会提示用户是否继续
`17:49` 因为运行终端命令比较危险
`17:51` 所以现在一般用于编程的agent的
`17:53` 都会在运行终端命令之前
`17:54` 主动询问用户是否要执行之后
`17:57` 没有问题的话呢
`17:58` 我们就会去执行工具背后的函数了
`18:01` 并且把执行结果放到observation里面
`18:04` 再把observation放到message列表里面
`18:06` 因为我们在一个while循环里面
`18:08` 所以呢我们下一步还会来到循环的开头这里
`18:12` 继续请求模型
`18:13` 我们给call model这个函数传了message列表
`18:16` 作为参数工具
`18:17` 执行结果
`18:18` 不是作为observation放到了message列表里面了吗
`18:21` 而message又传给了模型
`18:23` 这样模型就可以拿到工具的执行结果了
`18:26` 它进而就可以根据工具的执行结果
`18:28` 推测下一步要干什么
`18:30` 所以总结一下这个while循环做的事情
`18:33` 请求模型提取thoughts检测final answer
`18:41` 提取action并执行其中的工具
`18:44` 这个过程会一直重复下去
`18:46` 直到模型返回了final answer为止
`18:49` 回想一下
`18:50` 这正是我们之前所提到的react运行流程

### ReAct 运行时序图 `18:54`

`18:57` 为了确保你彻底明白这其中发生了什么
`18:59` 我们来画个agent的流程图
`19:01` 整个流程图里面有两个角色
`19:04` 用户和agent
`19:05` 而agent又可以分成三个部分
`19:08` 分别是模型工具
`19:10` 也就是函数
`19:11` 还有agent的主程序
`19:14` agent主程序这个词我们之前没有提过
`19:17` 其实就是agent里面负责串联整个流程的代码逻辑
`19:21` 它会在合适的时候调用工具或者是模型等等
`19:25` 你可以大致理解为
`19:26` 我们刚才代码里面的那个run函数
`19:28` 下面我们就来画个流程图
`19:30` 看看这四个角色之间是怎么沟通的
`19:33` 在用户提交任务之后
`19:35` 任务先到了agent的主程序
`19:37` 这里agent主程序会先去调用模型模型
`19:40` 返回THOUTS和action
`19:42` agent主程序把thought和action打印给用户看
`19:45` 然后去调用action里面的指定工具
`19:48` 工具执行完毕之后返回结果
`19:51` agent的主程序把结果发回给用户看
`19:54` 然后把这个工具执行结果
`19:56` 加入到历史消息列表里面
`19:57` 然后再次重复这个框中的流程
`20:00` 也就是请求模型并处理thoughts
`20:02` action和observation的逻辑
`20:04` 直到某个时刻
`20:06` 在请求模型后
`20:07` 模型认为用户的任务已经完成了
`20:09` 不需要再调用工具了
`20:11` 它就会返回salts和final answer
`20:13` agent的主程序
`20:14` 把source和final answer展示给用户看
`20:16` 整个流程就结束了
`20:18` 这就是一个完整的react agent的问答流程

### Plan-And-Execute 模式介绍 `20:22`

`20:24` 前面我们讲了如何使用react模式来构建一个agent
`20:28` react是目前最常见
`20:30` 使用最广泛的agent的构建模式
`20:32` 但它不是唯一的方案
`20:34` 除了react之外
`20:35` 还有很多其他的运行模式
`20:37` 其中很多agent的运行过程就是先规划再执行
`20:41` 比如我们之前演示过的minus
`20:43` 如果你仔细看的话
`20:44` 就会发现
`20:45` 他在一开始回答的时候会构建一个代办列表
`20:48` 后面的执行过程呢都是遵循这个待办列表来
`20:51` 而cloud code中也会经常看到这种先创建to do
`20:55` 再去执行的情况
`20:56` 这种先规划再执行的模式
`20:58` 目前并没有一个统一的名字
`21:00` 而且每个agent的实现多多少少也会有一些差别
`21:04` 我们今天来讲一个
`21:05` 其中比较有名的实践是LANGCHEN提出来的
`21:08` planned and execute模式
`21:10` 从总体上来看
`21:11` 它也是遵循了先规划再执行的流程
`21:14` 只不过它的流程引入了一些动态修改
`21:17` 规划的环节
`21:18` 这使得他的方案有了很大的灵活性

### Plan-And-Execute 运行流程 `21:21`

`21:23` 我们先用实习图来画一下
`21:25` planning execute模式的运行流程
`21:27` 首先我们要搞清楚
`21:29` 这个时序图里面有多少个角色
`21:30` 粗分下来的话
`21:32` 那肯定只有两个了
`21:33` 一个是用户
`21:34` 另外一个呢是planned execute agents
`21:37` 不过既然要研究plan execute agent的运行流程
`21:41` 我们就肯定要搞清楚这个agent的组成部分
`21:44` 首先它里面有一个负责出执行计划的模型
`21:47` 我们称它为plan模型
`21:49` 我们在运行的过程中
`21:51` 还需要根据每一步的执行结果
`21:53` 来动态的调整计划
`21:54` 因此我们还需要一个负责修改执行计划的模型
`21:58` 我们称它为reply模型
`22:00` plan和REPLAN模型可以是同一个
`22:03` 也可以分成两个都是可以的
`22:05` 我们暂且将它们列为两个
`22:07` 除了这两个模型之外
`22:08` 我们还需要一个负责执行这个计划中
`22:11` 每一个步骤的agent
`22:12` 我们称它为执行agent
`22:14` 对你没看错
`22:16` 这个plan execute agent内部呢还有一个agent
`22:19` 这种agent套agent的设计方案其实也是比较常见的
`22:24` 最后跟react那个流程一样
`22:26` 我们还需要一个agent的主程序负责串联整个流程
`22:30` 这就是planning execute agent的全部模块了
`22:33` 下面我们就把它们放在流程图里面
`22:36` 看看各个模块之间是如何运作的
`22:39` 首先用户会把问题提给agent的主程序
`22:42` 比如我们的问题就可以是
`22:44` 今年澳网男子冠军的家乡是哪里
`22:47` 这里的澳网
`22:48` 指的是每年举办的澳大利亚网球公开赛
`22:51` 也就是个体育赛事了
`22:53` agent的主程序接到这个问题之后
`22:55` 会把这个问题发给plan模型
`22:57` 让他给出具体的执行步骤
`22:59` 比如一个可能的执行步骤就是这样的
`23:02` 先查询当前日期
`23:04` 然后查询在当前年份下澳网男子冠军的名字
`23:08` 比如当前时间是2025年的话
`23:10` 就查询2025年的澳网男子冠军的名字
`23:13` 如果当前年份是2024年的话
`23:16` 那就查询2024年的澳网男子冠军的名字
`23:19` 查出名字后
`23:20` 我们再根据这个名字来查询这个冠军的家乡
`23:23` 没错
`23:24` 这就是一个非常合理的执行步骤
`23:27` 那计划有了之后
`23:28` agent的主程序便会把这个计划传给执行agent
`23:32` 让他去执行这个计划中的第一步
`23:35` 也就是查询当前日期的那个步骤
`23:37` 这个执行agent可以用我们之前讲的
`23:40` react模式来运行
`23:42` 它内置一个网络搜索工具
`23:44` 这样呢
`23:45` 它就可以通过搜索网络来查询当前日期了
`23:48` 当然执行agent也完全可以用别的模式来运行
`23:52` plan and execute模式呢
`23:53` 只要求执行agent能够完成指定的步骤就行
`23:57` 至于它的运行模式是不是react内置工具有哪些
`24:00` 他完全不关心执行agent的内部一顿操作之后
`24:04` 就突出了一个执行结果并返回了回去
`24:06` 然后AH的主程序会把用户问题
`24:09` 执行计划和执行记录都发给reply模型
`24:12` 让它生成一个新的执行计划
`24:14` 毕竟我们拿到了第一步的执行结果了嘛
`24:17` 多了一些信息
`24:18` 情况呢可能会发生些变化
`24:20` 把原计划改改是再正常不过的事情了
`24:23` 那A证的主程序接到新的执行计划之后
`24:26` 他便会回头再重复这个框中的流程
`24:29` 在我们这个例子中
`24:30` 这个框中的流程一共会运行三轮
`24:33` 对应了执行计划里面的三步
`24:35` 每一轮都包含两个环节
`24:37` 一个是执行环节
`24:40` 一个是reply环节
`24:42` 为了能够让你彻底明白
`24:44` 我们模拟一下这个循环中的三轮
`24:46` 让你看看每一轮的执行环节和reply环节
`24:49` 都具体发生了些什么事情
`24:51` 在模拟开始前
`24:52` 我先打个预防针
`24:53` 为了节省时间
`24:54` 我在模拟时讲解的速度会稍微快一些
`24:57` 如果你听不懂
`24:58` 建议在合适的时候稍微停一下
`25:00` 好让我们开始
`25:02` 首先是第一轮
`25:03` 在执行阶段
`25:04` 我们把执行计划发给执行agent
`25:07` 让他处理其中的第一步
`25:09` 执行agent的返回之后
`25:10` 我们把它给出的执行结果
`25:12` 加入到历史执行记录里
`25:15` 然后把用户问题
`25:16` 第一个执行计划和历史执行记录一起
`25:19` 发给reply模型
`25:20` 让他给出第二个执行计划
`25:22` 第一个执行计划和第二个执行计划有两点不同
`25:26` 这里我专门说一下
`25:27` 首先原来查询当前日期的那一步
`25:30` 就不用出现在第二个执行计划里面了
`25:33` 毕竟已经执行完了
`25:34` 不用再执行了
`25:36` 另外查询澳网男子冠军名字的这一步呢
`25:39` 也发生了一些变化
`25:40` 在第一个执行计划中
`25:42` 它叫做查询对应日期的澳网男子冠军名字
`25:46` 在第二个执行计划中
`25:47` 它叫做查询2025年的澳网男子冠军名字
`25:51` 毕竟日期已经查出来了
`25:53` 因此我们可以直接把具体的年份
`25:55` 放到执行计划里
`25:56` 这样执行agent的接到的任务就更加精确了
`26:00` 然后进入到第二轮
`26:02` 在这一轮中
`26:03` 我们同样先取出最新的执行计划发给执行agent
`26:07` 让它执行其中的第一步
`26:09` 拿到执行结果后
`26:10` 我们再把执行结果加入到历史执行记录中
`26:13` 然后在REPLAN阶段把用户问题
`26:16` 执行计划和历史执行记录发给reply an模型
`26:19` 拿到第三个执行计划
`26:21` 在第三轮中
`26:22` 我们还是先取出执行计划
`26:25` 让执行agent处理其中的第一步
`26:27` 当然现在也只剩一步了
`26:29` 执行之后我们就可以拿到一个执行结果
`26:32` 把执行结果加入到历史执行记录里
`26:34` 然后再把用户问题
`26:36` 执行计划和历史执行记录都发给reply模型
`26:39` 让它再生成一个诶
`26:42` 好像所有的任务都已经完成了
`26:44` 没有步骤要做了吧
`26:45` 没错
`26:46` 在最后一轮中
`26:47` REPPLAN模型会发现所有的步骤都已经做完了
`26:50` 用户的问题可以回答出来了
`26:52` 此时reply模型返回的就不是最新的执行计划了
`26:56` 而是最终的答案
`26:58` 所以在流程图里面
`26:59` 我们也要把执行计划换成最终答案
`27:02` A证的主程序接到这个最终答案之后
`27:04` 便会把这个答案转发给用户
`27:07` 整个流程也就结束了
`27:08` 所以回头看一下这个流程图
`27:10` 我们之前说agent的主程序请求reply模型
`27:14` 给出一个新的执行计划
`27:16` 这个说法呢其实并不准确
`27:18` 更准确的说法是
`27:19` agent的主程序请求reply模型给出一个新的执行计划
`27:24` 或者是返回最终答案
`27:26` 如果还有步骤要执行的话呢
`27:28` 那就给出一个新的计划
`27:30` 如果没有步骤要做了
`27:31` 用户的问题已经回答出来了
`27:33` 那reply模型就返回最终答案就好了
`27:35` 因此reply模型的返回呢也有两种可能性
`27:39` 新的执行计划或者是最终答案
`27:42` 这样呢才是一个准确的流程图
`27:44` 相信到这里为止
`27:46` 你对plan execute流程就有一个非常清晰的认识了
`27:49` 不过有些人可能想要plan execute的具体实现代码
`27:53` long chain官方提供了一份
`27:55` 你可以到这个页面里面自行获取
`27:58` 啧啧
`28:01` 今天的视频呢就到此结束了
`28:03` 别忘了点赞关注我们
`28:05` 下次再见拜拜
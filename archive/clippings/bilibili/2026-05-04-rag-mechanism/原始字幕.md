---
title: "RAG 工作机制详解——一个高质量知识库背后的技术全流程"
url: "https://www.bilibili.com/video/BV1JLN2z4EZQ/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1JLN2z4EZQ"
cid: "30617043026"
author: "马克的技术工作坊"
upload_date: "2025-06-21"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114720791073589&bvid=BV1JLN2z4EZQ&cid=30617043026&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

RAG 工作机制详解——一个高质量知识库背后的技术全流程

## 章节

- `00:00` 视频内容简介
- `01:22` RAG 的使用场景
- `02:55` RAG 的基本运行流程
- `04:19` 分片
- `04:53` 索引
- `09:40` 召回
- `13:09` 重排
- `15:09` 生成
- `15:27` RAG 的整体流程

## 字幕

### 视频内容简介 `00:00`

`00:00` 你是否想做一个靠谱的知识客服
`00:02` 或者是搭建一个能回答问题的知识库
`00:05` 那你就一定绕不开一个技术
`00:07` Rag
`00:07` 它的全称是retrieval augmented generation
`00:10` 翻译过来就是检索增强生成
`00:13` 听起来挺高大上
`00:15` 但说白了也就这么两件事
`00:17` 先从资料库里检索相关的内容
`00:20` 再基于这些内容来生成答案
`00:22` 也就是说它先检索再生成
`00:25` 所以叫做检索增强生成
`00:28` rag是目前最常用的AI问答方案之一
`00:31` 很多企业内的知识助手
`00:32` 智能客服用的都是这项技术
`00:35` 在本视频里
`00:36` 我将会为你介绍rag的实现原理
`00:38` 主要包括以下三个部分
`00:41` 首先我们将总体看一下rag的使用场景
`00:44` 和大致链路
`00:45` 让你对这门技术有个感性的了解
`00:47` 然后我们会逐步拆解这个链路里面的
`00:50` 每一个环节
`00:51` 深入理解它的原理
`00:53` 最后我们会从提问前和提问后两个角度出发
`00:57` 复习一下整个链路的运行过程
`00:59` 加深你的理解
`01:01` 在这个过程里面
`01:02` 我还会解释一下rag用到的各种专业名词
`01:06` 比如说是向量
`01:07` embedding模型
`01:08` 向量数据库
`01:10` 向量相似度等等
`01:11` 相信在看完这个视频之后
`01:13` 你就会明白
`01:14` 一个高质量的智能客服或者是知识库
`01:17` 是如何构建的
`01:18` 好话不多说
`01:19` 让我们赶快开始吧

### RAG 的使用场景 `01:22`

`01:24` 假设你想做一个智能客服
`01:26` 这个智能客服可以回答
`01:27` 各种关于你们公司产品的问题
`01:29` 那应该怎么实现它呢
`01:31` 首先这个客服的内部一定要有个模型
`01:34` 比如说是GPT4o deep sick这种的
`01:37` 不过光有个模型可不够
`01:39` 因为模型可不知道你们公司的产品信息
`01:42` 你想这个问题好办
`01:44` 在给模型发送问题的时候
`01:46` 我把产品手册一起发个模型不就好啦
`01:48` 没错
`01:49` 这确实是一个解决方案
`01:51` 不过如果产品手册的字数特别多
`01:54` 比如有个上百页乃至上千页的话
`01:57` 这就会带来很多问题
`01:58` 首先模型可能无法读取所有的内容
`02:01` 因为每个模型都只能存储一定量的信息
`02:04` 我们通常称这个量为上下文窗口大小
`02:07` 如果你的产品手册字数过多
`02:09` 超过了这个上下文窗口大小的话
`02:11` 模型就会读了
`02:12` 后面忘了
`02:13` 前面回答的准确率也就无法得到保障
`02:16` 除此之外
`02:17` 模型的推理成本也会很高
`02:19` 输入越多
`02:20` 成本越高
`02:21` 每次回答问题的时候都要带上一本厚厚的手册
`02:24` 那成本可想而知
`02:25` 不可能少了
`02:26` 最后模型的推理速度也会受到影响
`02:29` 输入越多
`02:30` 模型需要消化的内容就越多
`02:32` 模型的输出就会越慢
`02:34` 一本上百页的手册扔进来
`02:36` 那大概率会对模型的推理速度产生严重的影响
`02:40` 看来直接把文档丢给模型是行不通的
`02:43` 那我们是不是可以考虑
`02:45` 只把文档中相关的内容发给模型呢
`02:48` 可以的
`02:49` 这就需要rag登场了
`02:50` 我们一起来看看rag是如何解决这个问题的

### RAG 的基本运行流程 `02:55`

`02:57` 首先rag会把文档切分为多个片段
`03:00` 当用户提出问题后
`03:02` 我们就用这个问题
`03:03` 在所有的片段中寻找相关内容
`03:05` 比如在一份上百页的产品手册中
`03:08` 可能只有三个片段
`03:10` 真正与用户的问题相关
`03:11` 我们就把这三个片段单独挑出来
`03:14` 把他们和用户的问题一起发给大模型
`03:17` 这样模型就只会感知三个相关的片段
`03:20` 而不是整个文档之前的问题也就会迎刃而解了
`03:23` 对rag就是这样的了
`03:25` 是不是很简单
`03:26` 不过话说回来
`03:28` 这只是一个过度简化的链路
`03:30` 我隐藏了很多实现细节
`03:32` 比如说是如何分片
`03:33` 如何选择相关的片段
`03:35` 这里面的学问都不少
`03:37` 所以呢下面我把整个react的流程具体来拆分下
`03:40` 通常来说rag的整体流程包含两个部分
`03:44` 一个是数据准备部分
`03:45` 这个发生在用户提问前
`03:47` 我们要在这一部分里把相关的文档都给准备好
`03:51` 并完成相应的预处理
`03:52` 它一共是包含分片和索引两个环节
`03:56` 另外一个是回答部分
`03:58` 这一部分当然是发生在用户提问之后了
`04:01` 在用户问完问题之后
`04:02` 我们便会触发回答问题的各个环节
`04:05` 分别是召回重排和生成
`04:08` 接下来我们就逐步拆解分片索引
`04:12` 召回重排和生成这五个环节
`04:16` 看看他们分别是如何工作的

### 分片 `04:19`

`04:21` 分片顾名思义就是把文档切分成多个片段
`04:25` 我们之前在基本运行流程环节里面
`04:27` 也演示过这个分片的动画
`04:29` 分片的方式有很多种
`04:30` 我们可以按照字数来分
`04:32` 比如说1000个字
`04:34` 一个片段
`04:34` 按照段落来分
`04:36` 比如说是一个段落
`04:37` 一个片段
`04:38` 或者是按照章节分
`04:40` 按照页码分
`04:41` 除此之外还有很多的切分方式
`04:43` 但不管怎么做
`04:44` 我们最后都需要把一篇文档切分为多份
`04:47` 切好后
`04:48` 这个环节就结束了
`04:50` 然后我们就要进入到下一个环节
`04:52` 所以

### 索引 `04:53`

`04:55` 索引就是通过embedding
`04:57` 将每一个片段文本转换为向量
`05:00` 然后再将片段文本和对应向量
`05:02` 都存储在向量数据库的一个过程
`05:04` 没错
`05:04` 它一共就只有这两步
`05:06` 但这两个步骤所包含的信息量其实是巨大的
`05:09` 比如什么是embedding
`05:11` 什么是向量数据库
`05:12` 可能什么是向量
`05:13` 你也记不清楚了
`05:14` 别急
`05:15` 我们先把这三个概念解释清楚
`05:17` 然后再回来看看索引这个流程
`05:19` 相信你就会清楚很多
`05:21` 首先我们来讲讲这其中最基础的向量
`05:24` 向量是数学里面的一个概念
`05:26` 相信大家多多少少都学过
`05:28` 从概念上来讲
`05:30` 它代表一个有大小有方向的量
`05:32` 在通常情况下
`05:33` 我们可以用一个数组来表示它
`05:35` 每个向量都有维度
`05:37` 维度的大小就等于数组中数字的个数
`05:40` 比如这些都是一维向量
`05:41` 这些都是二维向量
`05:43` 而这些呢都是三维向量等等等等
`05:47` 对于低维度的向量
`05:48` 我们可以直接把它们在坐标轴里面画出来
`05:51` 比如最简单的一维向量
`05:53` 我们可以把它放置在一个一维坐标轴上
`05:56` 一这个向量可以这么画
`05:58` 它的大小呢是一方向朝右三
`06:01` 这个向量可以这么画
`06:02` 它的大小是三方向朝左
`06:05` 要表示二维向量的话
`06:06` 我们就必须使用一个二维坐标轴
`06:08` 比如说二二可以放在这个地方
`06:11` 一二可以放在这里
`06:14` 同理三维向量呢需要放在一个三维的坐标轴里
`06:18` 当然维度再大一点的话
`06:20` 我就没法给你演示他在坐标轴里面的位置了
`06:23` 毕竟我们生活在一个三维世界里
`06:25` 但无法可视化并不代表就不存在
`06:28` 实际上我们在rag里面用到的向量维度
`06:30` 通常情况下都会比较大
`06:32` 比如是几百甚至几千
`06:34` 一般来说维度越大
`06:36` 每个向量所包含的信息也就会越丰富
`06:39` 用这些向量做各种工作的可靠性也就越强
`06:43` 讲完了向量
`06:44` 再来看看embedding
`06:45` embedding就是把文本转换为向量的一个过程
`06:48` 比如我们以二维向量为例
`06:50` 假设马克喜欢吃水果
`06:52` 对应的向量是一二
`06:54` 马克爱吃水果
`06:55` 对应的向量是一一
`06:57` 天气真好
`06:58` 对应的向量是三负一
`07:00` 你会发现前两个句子的向量非常接近
`07:03` 而天气真好则距离比较远
`07:06` 这说明前两个句子的语义是相近的
`07:08` 而后者则完全不相关
`07:10` 这正是embedding的目的
`07:12` 含义相近的文本在经历了embedding之后
`07:15` 它们对应的向量也是相近的
`07:17` 因为这样的话
`07:18` 当用户询问马克喜欢吃什么的时候
`07:21` 我们就可以先把这个问题做给embedding
`07:24` 将其转换为向量
`07:25` 然后再根据向量相似度
`07:27` 把与这个问题相关的文本也找出来
`07:30` 最后我们就可以把这两个相关文本
`07:32` 以及用户的问题一起扔给大模型
`07:34` 大模型就可以告诉我们
`07:36` 马克喜欢吃水果了
`07:37` embedding这个操作是模型来完成的
`07:40` 不过这个模型可不是我们通常所使用的
`07:42` GBT4o deep sick这样的模型
`07:45` 而是专门的embedding模型
`07:46` 如果你想知道哪些embedding模型最好用的话
`07:49` 可以看一下这个m tap排行榜
`07:51` 它会对各种embedding模型做评测
`07:53` 并且把结果做个排行
`07:55` 方便我们挑选和使用
`07:57` 聊完了embedding的概念
`07:58` 我们再来看看向量数据库
`08:00` 向量数据库就是用来存储和查询向量的数据库
`08:03` 它为存储向量做了很多优化
`08:05` 并且还提供了计算向量相似度等相关的函数
`08:08` 方便我们使用向量
`08:10` embedding后的向量呢
`08:11` 就可以放在向量数据库里面
`08:13` 方便后续查询
`08:14` 比如我们还是以马克喜欢吃水果这句话为例
`08:17` 在我们给这句话做了embedding之后
`08:20` 就得到了一个向量
`08:21` 然后我们需要把这个向量存入到向量数据库中
`08:24` 不过注意我们要存的不仅有向量
`08:27` 还有原始的文本
`08:28` 所以原始文本也要发给向量数据库
`08:31` 因为只有这样
`08:32` 我们才能够在通过向量相似度
`08:34` 查询出相似的向量之后
`08:35` 把对应的原始文本也抽取出来八个大模型
`08:39` 让它处理
`08:40` 我们最终需要的还是原始的文本
`08:42` 向量呢只是一个中间结果
`08:44` 所以一般的向量数据库表格里面
`08:47` 至少都会有原始文本和向量两列内容
`08:50` 就像这样
`08:52` 讲完了向量embedding和向量数据库
`08:55` 我们再回头看看我们之前提到过的索引
`08:58` 这个概念
`08:58` 索引就是通过embedding
`09:00` 将每个片段文本转换为向量
`09:03` 并且把片段文本和对应的向量
`09:06` 都存储在向量数据库的过程
`09:08` 这句话的意思想必大家都有个概念了
`09:11` 其实就是我们刚才聊的这个过程
`09:13` 只不过我们要把一开始的这个文本
`09:16` 换成每一个片段的内容
`09:17` 比如说我们一开始要处理的是片段一
`09:20` 片段一处理完了之后呢
`09:21` 我们要处理片段二
`09:22` 以此类推
`09:23` 直到所有的片段都处理完毕
`09:26` 这整个索引的流程就都结束了
`09:28` 不管是分片还是索引
`09:30` 它们都发生在用户提问之前
`09:32` 属于要提前准备的步骤
`09:34` 下面我们就来看看用户提问之后发生了什么
`09:38` 首先是召回

### 召回 `09:40`

`09:42` 召回就是搜索与用户问题相关片段的过程
`09:45` 这个环节从用户问题开始
`09:47` 首先用户的问题会发给embedding模型
`09:50` embedding模型会将它转化为向量
`09:53` 然后我们把它发送给向量数据库
`09:55` 让他查询与用户问题最为相关的十个片段内容
`09:59` 没错
`10:00` 召回的结果呢就是十个与用户问题相关的片段
`10:03` 当然十这个数字呢并不是固定的
`10:06` 你也可以选择十五二十等等
`10:08` 具体是多少呢
`10:09` 不是很重要
`10:10` 只要数量不是很多都可以
`10:12` 那不管是多少销量数据库呢
`10:15` 都要返回与用户问题最相似的一批片段
`10:18` 那向量数据库是怎么知道哪些片段
`10:20` 与用户问题最相关的呢
`10:22` 这就要计算向量相似度了
`10:24` 我来给大家模拟下整个过程
`10:27` 这个呢是向量数据库里面的数据
`10:29` 为了方便演示
`10:31` 我这里只写了三条
`10:32` 然后我们把用户的问题和对应的向量
`10:35` 也放在这里
`10:37` 我们最后呢要计算下
`10:39` 每个片段与用户问题的向量相似度
`10:42` 因此我们先把向量三四度的表头写在这里
`10:45` 然后我们剩下的任务就是
`10:47` 把片段向量和用户问题向量
`10:49` 分别代入到一个相似度计算公式中
`10:52` 得出向量相似度
`10:53` 这个计算公式的第一个参数
`10:55` 永远是用户问题所对应的向量
`10:57` 因此我们先把第一个参数放进来
`11:01` 这个公式的第二个参数呢
`11:03` 则是每个片段的向量了
`11:05` 首先我们把第一个片段向量带进来
`11:07` 算出一个数字
`11:09` 我们把这个数字放入到向量相似度这一栏中
`11:12` 然后呢我们再把第二个片段的向量代入进来
`11:15` 算出第二个片段与用户问题的相似度
`11:18` 然后同样的
`11:19` 我们也把计算结果放入到向量相似度这一栏中
`11:22` 以此类推
`11:23` 我们再算出第三个片段与用户问题的相似度
`11:27` 我们需要按照这个方法
`11:28` 把所有的片段都计算完毕
`11:30` 在都计算完毕了之后呢
`11:32` 我们就把这个向量相似度排个序
`11:34` 取前十个最大的就好了
`11:36` 那下一个问题就是这个公式是怎么算出来的呢
`11:40` 答案是有很多种
`11:42` 目前比较流行的方案呢是包括余弦相似度
`11:45` 欧式距离和点击
`11:47` 我这里大致说一下
`11:49` 余弦相似度呢是主要在算两个向量之间
`11:53` 夹角的cos值
`11:54` 然后根据这个cos值来判断夹角的大小
`11:57` 夹角越小
`11:58` 相似度就越高
`12:00` 欧式距离呢主要是在计算两个向量之间的距离
`12:03` 也就是这段白线的距离
`12:05` 距离越小
`12:06` 相似度越高
`12:07` 点击呢是一种通过代数方式
`12:10` 衡量两个向量相似度的方法
`12:12` 它不仅要考虑两个向量之间的方向关系
`12:15` 也要考虑它们的长度
`12:17` 比如我们要计算这张图里面A和B的点击
`12:21` 我们首先从A向B引入一条垂线
`12:25` 然后A和B的点击呢
`12:26` 就是这段距离和这一段距离的乘积乘积越大
`12:33` 就代表相似度越高
`12:34` 如果两个向量方向一致的话
`12:37` 那么这两个向量越长
`12:39` 点击值就越大
`12:40` 如果方向相反
`12:42` 那么点击就是负的
`12:43` 如果垂直点击就为零
`12:46` 所以呢我们可以通过点击的值来判断
`12:49` 两个向量是否在同一个方向上努力
`12:51` 以及他们努力的程度有多大
`12:54` 向量相似度就讲到这里
`12:56` 我们再回到前面的这张图
`12:58` 稍微回顾一下
`12:59` 在这一阶段
`13:00` 我们查询出了与用户问题最为匹配的十个片段
`13:03` 记住这个结论
`13:04` 因为这十个片段会发送到重排阶段
`13:07` 继续处理

### 重排 `13:09`

`13:11` 重排全称是重新排序
`13:13` 他做的事情其实跟召回是一样的
`13:16` 前面我们说过
`13:18` 召回是从所有的片段里面挑十份
`13:20` 与用户问题最相似的
`13:22` 而重排呢则是从召回的这十份里面
`13:25` 再挑三份与用户问题最相似的
`13:28` 作为重排的结果
`13:29` 你可能会想
`13:30` 那直接在召回阶段挑三个不就好了
`13:33` 这样就不用重排了
`13:34` 同样的事情搞两遍干什么呢
`13:36` 一次挑出三个呢
`13:38` 当然是可以的
`13:39` 不过这样做的效果没有召回家重排的方案好
`13:42` 为什么呢
`13:43` 因为召回与重排阶段使用的文本相似度
`13:47` 计算逻辑不一样
`13:48` 下面呢我们来比较一下
`13:50` 首先看一下召回
`13:52` 召回阶段使用的是向量相似度
`13:54` 我们还列举了三个常见的向量相似度计算方法
`13:58` 但无论是使用哪一种方法
`14:00` 它们的特点都是成本低
`14:02` 耗时短
`14:02` 准确率低
`14:04` 所以呢适合做初步的筛选
`14:06` 也就是在短时间内把上千条片段的相似度
`14:09` 数值都计算出来
`14:11` 从中挑出十个最高的
`14:13` 而重排就不是了
`14:14` 重排阶段一般是使用一种叫做CROSSENCODER的模型
`14:18` 计算每个片段与用户问题的相似度
`14:21` 相比之下
`14:22` CROSSENCODER的成本会比较高
`14:24` 耗时呢也会比较长
`14:25` 那为什么用它呢
`14:27` 因为cross coder的准确率会高很多
`14:30` 所以呢他很适合做精挑细选
`14:33` 你可以把它类比成公司筛选人才
`14:35` 众所周知
`14:37` 公司面试呢一共是分为两个环节
`14:39` 简历筛选加面试召回
`14:42` 就跟简历筛选有点像
`14:43` 候选人太多
`14:44` 公司只能用一些粗略的方法
`14:46` 从成千上万份简历里面挑出十个
`14:49` 看起来最优秀的
`14:50` 准确率呢可能会大打折扣
`14:52` 但这也是没有办法的事情
`14:54` 那重排呢就跟面试很像了
`14:56` 公司对这十个人进行面试
`14:58` 仔细挑选
`14:59` 尽可能地保证判断正确
`15:02` 从中挑出最优秀的三个人入职
`15:04` 好重排呢就讲到这里
`15:06` 下面我们进入到生成阶段

### 生成 `15:09`

`15:11` 生成什么呢
`15:12` 那当然是生成答案了
`15:14` 现在我们有了用户问题
`15:15` 也有与用户问题相关的三个片段
`15:18` 我们就可以把这两部分一起发给大模型
`15:21` 让它根据片段内容来回答用户问题
`15:24` 到此整个流程就结束了

### RAG 的整体流程 `15:27`

`15:29` 这个流程里面的所有环节我们都讲完了
`15:32` 下面呢我们把所有的流程给串联一下
`15:35` 整体讲一遍
`15:36` 整个流程分为两个部分
`15:38` 一个呢是准备部分
`15:39` 它发生在提问前
`15:41` 包括分片和索引两个环节
`15:43` 一个是回答部分发生在提问后
`15:46` 包括召回
`15:47` 重排和生成三个环节
`15:50` 由于整个流程分为两个部分
`15:52` 所以呢我们的整体流程也会有两个
`15:54` 首先看提问前的准备部分
`15:56` 首先我们把相关的资料做个分片
`15:59` 然后把所有的片段都扔给embedding模型
`16:02` 让他给每个片段都产出一个对应的向量
`16:05` 最后我们把向量存入到向量数据库中
`16:08` 到这里提问前的准备流程就结束了
`16:11` 这就相当于我们的知识库构建已经完毕了
`16:14` 就等用户来用了
`16:16` 再来看看用户提问之后发生了什么
`16:18` 首先用户的问题会给到embedding模型
`16:21` embedding模型呢
`16:22` 会把用户的问题转化为一个向量
`16:25` 然后我们会把这个向量传给向量数据库
`16:28` 让他给我们找到十个与用户问题最相近的片段
`16:32` 找到之后
`16:32` 我们再把这十个片段送给CROSSENCODER模型
`16:35` 让他做个重排
`16:36` 从十个里面再筛选出三个相关程度最高的
`16:40` 然后呢
`16:41` 我们把这三个与用户问题相关程度最高的片段
`16:44` 外加用户的问题一起发给大模型
`16:47` 大模型呢就可以给我们产出最终答案了
`16:49` 到这里
`16:50` 所有的流程就算是结束了
`16:55` 今天的视频呢就到此结束了
`16:57` 别忘了点赞关注我们
`16:59` 下次再见拜拜
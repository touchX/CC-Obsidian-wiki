---
title: "MCP终极指南 - 从原理到实战，带你深入掌握MCP（基础篇）"
url: "https://www.bilibili.com/video/BV1uronYREWR/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1uronYREWR"
cid: "29420487301"
author: "马克的技术工作坊"
upload_date: "2025-04-15"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114339210073708&bvid=BV1uronYREWR&cid=29420487301&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

MCP终极指南 - 带你深入掌握MCP（基础篇）

时间轴：
01:05 MCP简要介绍
02:47 安装 MCP Host（Cline）
03:15 配置 Cline 用的 API Key
06:01 第一个 MCP 问题
06:31 概念解释：MCP Server 和 Tool
09:13 配置 MCP Server
14:19 使用 MCP Server
15:24 MCP 交互流程详解
17:46 如何使用他人制作的 MCP Server：uvx 部分
23:54 如何使用他人制作的 MCP Server：npx 部分

## 章节

- `00:00` 前言
- `01:03` MCP简要介绍
- `02:46` 安装 MCP Host（Cline）
- `03:14` 配置 Cline 用的 API Key
- `05:59` 第一个 MCP 问题
- `06:29` 概念解释：MCP Server 和 Tool
- `09:15` 配置 MCP Server
- `14:19` 使用 MCP Server
- `15:24` MCP 交互流程详解
- `17:44` 使用他人的 MCP Server

## 字幕

### 前言 `00:00`

`00:00` MCP无疑是近期AI领域的热点话题
`00:03` 你可能已经看过一些介绍视频了
`00:06` 但有没有觉得关于它的运行原理
`00:08` 你还总是有点摸不着头脑
`00:11` 特别是对于想深入使用
`00:13` 甚至自己动手开发MCP相关工具的开发者来说
`00:17` 很多教程可能只是浮于表面
`00:19` 看完之后似乎懂了
`00:21` 又似乎没懂
`00:23` 这个呢就是我今天在这里的原因
`00:25` 我想做一期与众不同的MCP视频
`00:29` 一期真正能够让你从底层理解
`00:32` 到动手实践的深度教程
`00:34` 我们会先打好基础
`00:36` 弄懂MCP的核心概念
`00:38` 数据流转过程和基本用法
`00:41` 然后我们将一起挑战进阶
`00:44` 不仅要从零构建一个mc p server
`00:47` 还要像侦探一样
`00:48` 通过截获并分析MCP server的输入输出
`00:52` 把MCP协议的细节挖个底朝天
`00:55` 我的目标是在看完视频之后
`00:57` 你不仅能知道如何使用MCP
`01:00` 更能透彻的理解它
`01:02` 好废话少说

### MCP简要介绍 `01:03`

`01:03` 不如我们现在就开始吧
`01:08` MCP全称是model context protoco
`01:11` 是ANTHROPIC公司
`01:13` 在2024年11月25号发布的一个协议
`01:16` Model context protocol
`01:17` 翻译成中文是模型上下文协议
`01:20` 这几个字你可能都认识
`01:22` 但是拼到一起可能就不知道是什么意思了
`01:25` 没关系
`01:26` 说实话我也觉得这个名字起的奇怪
`01:29` 有一种故弄玄虚的感觉
`01:31` 实际上
`01:32` MCP领域里面还有很多这种故弄玄虚的词汇
`01:35` 我们后面都会一一解释
`01:37` 所以你不用太担心
`01:38` 我建议啊
`01:39` 我们暂时就先不用纠结MCP到底是什么意思
`01:43` 我们先来看看MCP能做什么
`01:45` 简单来说
`01:46` M c p
`01:47` 就是能够让大模型更好地
`01:48` 使用各类工具的一个协议
`01:50` 比如说借助MCP
`01:52` 我们可以让模型使用浏览器上网查询信息
`01:56` 可以让模型操作unity编写游戏
`01:58` 也可以让模型查询实时路况
`02:01` 总之MCP能干的事情可太多了
`02:04` 这些可都不是什么小事情
`02:06` 要知道大模型本身其实只会问答
`02:09` 他自己并不会使用外部工具
`02:11` 而MCP的出现
`02:13` 就等于是
`02:13` 让大模型拥有了使用各种外部工具的能力
`02:17` 是不是有点心动呢
`02:18` 别着急
`02:19` 要想使用MCP
`02:20` 你还得用到一个东西
`02:22` 叫做MCP host
`02:23` 听这个名字啊
`02:25` 你可能也会有点懵
`02:26` 不过没有关系
`02:27` 其实就是个名字而已
`02:28` 它本质上就是一个支持MCP协议的软件
`02:32` 常见的MCP host包括cloud desktop
`02:35` Cursor
`02:36` Klein
`02:37` cherry studio等等
`02:38` 相信大家多多少少的都听过这些软件
`02:42` 本期视频里我们就以client为例
`02:44` 给大家讲讲MCP的使用方法

### 安装 MCP Host（Cline） `02:46`

`02:50` client其实是vs code的一个插件
`02:53` 大家先到这个网站上自行安装下vs code
`02:57` 然后我们来打开它
`02:59` 打开它之后呢
`03:00` 是这个样子
`03:01` 我们来点击这里搜索client插件
`03:06` 然后点击安装
`03:08` 安装好了之后呢
`03:10` 这里面就会出现一个图标
`03:11` 我们来点击一下
`03:12` 这里就是使用client的地方了

### 配置 Cline 用的 API Key `03:14`

`03:18` 紧接着我们需要配置模型
`03:20` 我们来点击这里
`03:23` client支持不同的API接入方和不同的模型呃
`03:28` 目前对MCP支持最好的模型是cloud3.7
`03:31` 不过说实话这个模型也挺贵的
`03:34` 如果大家囊中羞涩的话
`03:35` 也可以选择deep seek v30324
`03:38` 它的效果也不错
`03:39` 价格更是低多了
`03:40` deep seek的官方网站就有提供API
`03:43` 大家可以到deep seek的官方网站注册登录并充值
`03:46` 获取API key
`03:47` 我这里用的呢是open router的
`03:49` 因为open router提供了非常多的模型可供选用
`03:52` 几乎覆盖了市面上所有的主流模型
`03:55` 甚至有些还可以免费用
`03:57` 这里啊
`03:58` 我给大家演示一下open router获取key的方法
`04:01` 首先我们来到open router的页面
`04:04` 点击右上角的sign in进行登录
`04:06` 登录好了之后
`04:07` 把鼠标移到右上角
`04:09` 点击里面的keys
`04:12` 再点击这里面的create key
`04:15` 这里啊我们需要输入两个信息
`04:17` 一个是API key的名称
`04:19` 这个我们随便输一个就好了
`04:21` 比如我的就叫做default
`04:23` 另外一个是这个API key所能够使用的金额
`04:27` 上限单位是美元
`04:29` 这个可填可不填
`04:30` 不过一般为了保险起见
`04:32` 我还是会填入一个数字
`04:34` 比如说是五
`04:35` 然后我们点击create创建好这个key
`04:38` 大家记住啊
`04:40` 一定要找个地方把这个key给记下来
`04:43` 因为后面是无法在open router的页面里面
`04:46` 再找回这个key的
`04:48` 这个key记好之后
`04:49` 如果大家只想要使用免费模型的话
`04:52` open router的操作就到此结束了
`04:55` 如果大家想要使用付费模型的话
`04:57` 需要点击这里面的credits进行充值
`04:59` 具体的充值过程我就不演示了
`05:02` 一切都准备好了之后
`05:03` 我们回到client这里
`05:05` 点击这个配置图标
`05:07` Api provider
`05:08` 选择open router key呢
`05:11` 就填写我们刚才记下来的那个model呢
`05:15` 这里面就选择deep seek v30324就好了
`05:18` 注意啊
`05:19` 这里面有两种deep seek v30324可以选
`05:22` 其中一个是以free结尾代表
`05:24` 这个模型是可以免费使用的
`05:26` 不过据我的测试
`05:28` 免费使用的模型多多少少都有些问题
`05:30` 不太稳定
`05:31` 所以如果有能力的话
`05:32` 尽量还是选择收费的
`05:34` 毕竟deep stick也很便宜
`05:36` 还有一点
`05:36` 我这里面配置的是play mode
`05:39` 这里还有一个act mode
`05:41` 大家在x mode里面也要配置好同样的信息
`05:44` 一切都填好之后
`05:45` 我们点击蛋克莱呢会引导我们来到聊天页面
`05:50` 我们随便问他一个问题
`05:52` 给他打个招呼
`05:53` 看他能不能够正常回复
`05:57` 可以看到他正常回复了

### 第一个 MCP 问题 `05:59`

`05:59` 那就基本上没有什么问题了
`06:04` 一切都准备就绪
`06:05` 那我们就试着问他一个真正的问题
`06:08` 明天纽约的天气怎么样
`06:17` 他给我们的建议呢
`06:18` 是要创建一个天气的MCP服务器
`06:23` 好这里面又来了一个新的名词啊
`06:25` 叫做MCP服务器
`06:27` 我们来详细探讨一下

### 概念解释：MCP Server 和 Tool `06:29`

`06:29` 这个名词到底是个什么意思
`06:34` MCP服务器的英文原名呢是叫做MCP server
`06:38` 这个名字听起来好像很高大上的样子啊
`06:40` 而且好像是跟我们传统网络协议里面的server
`06:44` 有点联系
`06:44` 似乎是一个远程的服务器
`06:47` 必须要联网才能使用呃
`06:49` 比如说我们打开google的首页
`06:51` 就是访问了google的服务器
`06:53` 不过实际上MCP server跟我们传统意义上的server
`06:57` 并没有什么太大的关系
`06:59` 它就是一个程序而已
`07:00` 只不过这个程序的执行是符合MCP协议的
`07:04` 比如说啊
`07:05` 大部分的MCP server都是本地
`07:07` 通过node或者是Python启动的
`07:10` 只不过在使用的过程中可能会联网
`07:12` 当然它也可能不联网
`07:14` 纯本地使用呢也是可以的
`07:16` 不管是联不联网
`07:17` 它都可以叫做mc p server
`07:19` 所以啊我觉得MCP server的这个名字里面
`07:22` 带server这个词是有一定的误导性的
`07:24` 你不要觉得这个玩意儿很高端很玄妙
`07:27` 它其实本质上就是一个程序
`07:30` 跟你手机上面的应用呢没有什么太大的区别
`07:33` 不管是MCP server还是手机应用
`07:35` 它们都内置了一些功能模块来解决你的问题
`07:38` 我用IPHONE上面的时钟进行举例
`07:41` 它内置了四个功能模块
`07:43` 分别是世界时钟
`07:44` 闹钟
`07:45` 钟表计时器
`07:47` 分别可以给我们解决四个场景的诉求
`07:50` 而刚才client想要给我们安装的这个open weather map
`07:54` 的MCP server呢也内置了一些模块
`07:56` 这些模块在MCP领域的专业名词叫做to
`08:00` 翻译成中文就是工具to
`08:03` 这个词呢也不太好理解
`08:05` 我换个词你马上就明白了
`08:07` 函数对没错
`08:09` 一个to其实就是编程语言里面的一个函数
`08:12` 如果你不懂编程
`08:14` 不太理解函数是个什么东西的话
`08:16` 我可以这样给你解释下
`08:18` 函数就是一个执行特定任务的机器
`08:21` 或者是工具箱
`08:22` 想象你有一个神奇的盒子
`08:24` 你可以放入一些材料
`08:26` 也就是输入盒子内部呢
`08:29` 会按照预先约定的规则处理这些材料
`08:31` 然后再给你一个成品
`08:33` 也就是输出
`08:34` 比如一个处理天气的mc p server
`08:37` 它内部可能会包含两个函数
`08:39` 分别是get forecast和get alert
`08:42` 前者的功能是传入经纬度返回未来几天的天气
`08:46` 后置的功能可能是传入地区
`08:49` 返回未来几天的气象预警
`08:51` 我们不是想查纽约明天的天气吗
`08:54` 那就可以让client安装这个处理天气的mc p server
`08:57` 并且让他使用其中的get forecast
`09:00` 就能够拿到纽约明天的天气了
`09:02` 当然他想要给我们创建的这个天气
`09:05` m cp server可能里面有别的to
`09:08` 不过不管怎么样
`09:09` 他这个to一定是可以解决
`09:11` 我们获取天气数据的这个问题的

### 配置 MCP Server `09:15`

`09:15` 那讲完了mc p server和two的概念
`09:19` 我们再回到client这里继续实操
`09:21` 前面我们讲到了client
`09:23` 希望我们安装一个查询天气用的m cp server
`09:26` 你此时可以提供相关的密钥
`09:28` 让它创建并且安装
`09:30` 除此之外呢
`09:31` 我们其实也可以点击这里自己查找
`09:35` 这是一个MCP server的市场
`09:37` 就跟我们手机的应用市场呢有点像
`09:39` 这里面的MCP server也是可以直接点击安装的
`09:43` 点击这里面的install就可以
`09:45` 不过你要注意
`09:46` 不管是我在原来的聊天界面里面
`09:49` 让他去创建对应的mc p server
`09:52` 还是说在这个应用市场里面
`09:54` 让他去安装mc p server
`09:56` 这两种办法
`09:57` 本质上都是让模型自己去主导整个安装过程的
`10:02` 比如说我这里面的mc p server市场
`10:05` 每一个mc p server呢都有一个对应的GITHUB仓库
`10:09` 每个GITHUB仓库都有一个说明文档
`10:12` 告诉大家如何去安装这个m cp server模型呢
`10:16` 就是通过阅读这个说明文档来运行程序
`10:19` 新增文件
`10:20` 从而完成MCP server的安装的
`10:22` 你可能会想这样很好啊
`10:24` 我就不用费劲去搞了
`10:25` 让模型自己去安装吧
`10:27` 你当然也可以让模型自己去安装
`10:30` 不过我相信也有很多人跟我一样
`10:32` 对安装的程序路径版本有一定的要求
`10:36` 不希望模型在我的电脑里面胡搞乱搞
`10:38` 何况模型还有可能安装失败不对
`10:41` 安装过程上心的话
`10:43` 你可能连模型干了什么都不知道
`10:45` 我啊不喜欢这种不确定性
`10:47` 我还是希望知道自己在做什么
`10:50` 而且重要的是
`10:51` 虽然client是支持这种自动安装的方式
`10:54` 但是呢有些MCB host是不支持的
`10:57` 只能够手动处理
`10:59` 所以啊这里我教你一种更加通用
`11:02` 确定性更高的办法
`11:03` 我们回到client这边
`11:05` 点击这里面的install
`11:07` 然后再点击configure MCP server之后
`11:12` client就会给我们打开一个文件
`11:14` 这个文件是JSON格式的
`11:16` 如果我们想新增一个M4p server的话
`11:18` 我们只需要在里面填入对应的启动命令就行了
`11:22` 比如说我仿照官方事例
`11:23` 写了一个查询天气用的mc p server
`11:26` 我把内容粘贴到这里面来
`11:28` 然后简单做个格式化
`11:30` 就变成这个样子的
`11:31` 我粘贴之后保存client就会自动识别
`11:35` 并且加载我的mc p server
`11:37` 大家不要被这些配置所吓倒
`11:40` 其实很简单的
`11:42` 这里代表的是MCP server的名字
`11:44` 我的名字呢很简单
`11:45` 就叫做weather
`11:47` 这个disabled呢
`11:48` 是代表这个MCP server是否被禁用
`11:51` 我目前呢是被禁用了
`11:52` 如果我们把它去掉的话
`11:54` 它会就会恢复默认值
`11:55` 也就是启动
`11:56` 大家看到这个绿色的开关已经显示启动成功了
`12:00` time out代表的就是连接这个MCP server的超时时间
`12:04` 单位是秒
`12:05` 也就是说如果超过60秒
`12:07` 还没有成功的连接这个MCP server的话呢
`12:10` 它就会放弃连接了
`12:11` 一般情况下不用太关注这个参数
`12:14` 后面的command和ARS是这个MCP server配置的精髓
`12:19` command代表的是我用哪一个程序可以运行
`12:22` 这个mc p server
`12:23` 后面的ARX呢就是对应的参数了
`12:26` 大家可以看到我这里用的command是UV
`12:29` 也就是说这个mc p server是使用UV
`12:31` 这个程序来启动的
`12:33` 你有可能不太清楚UV是个什么东西
`12:35` 你先暂时不要管
`12:36` 反正就是一个程序
`12:37` 我们后面会讲呃
`12:39` 在后面的transports type呢
`12:41` 就是client与这个MCP server沟通的方式
`12:44` mc p server一共有两种沟通方式
`12:47` 一种是STDIO
`12:48` 像我现在写的这个mc p server就是这种模式的
`12:52` 在这种模式下面
`12:53` mc p server会使用它的标准输入和标准输出
`12:56` 来与client进行沟通
`12:58` 目前大部分的mc p server用的都是这种类型
`13:01` 还有一种是s s e servicant event
`13:05` 我们先暂时忽略
`13:06` 这种用的比较少
`13:07` 如果你还是对transport type1知半解的话
`13:10` 不要急
`13:11` 你先大体有个印象就行
`13:13` 教程的后半部分会有演示
`13:15` 看完你就明白了
`13:16` 所以你也看到了一个MCP server
`13:18` 背后只是一个程序而已
`13:20` 只不过这个程序的运行符合MCP规范
`13:23` 下面我们说说这个mc p server里面到底写了些什么
`13:27` 注意我这里会展示一些代码
`13:29` 你不用细究代码的详细含义
`13:31` 有个概念就可以
`13:33` 代码我后面会带你写
`13:35` 我刚才填入的这个MCP server呢
`13:37` 它内部包含了两个to
`13:39` 其中一个叫做get forecast
`13:41` 接受两个参数
`13:42` 分别是latitude和longitude
`13:45` 也就是纬度和经度传入后
`13:47` 它会调用天气相关的API返回所传经纬度地区
`13:51` 未来几天的天气
`13:52` 另外一个to呢叫做get alerts
`13:55` 它接收的参数是美国的州代码
`13:58` 返回这个州的气象预警信息
`14:00` 这两个to背后调用的API都是美国气象局提供的
`14:04` 所以目前只有美国的数据
`14:06` 这可能对你用处不是很大
`14:08` 不过我们测试还是需要他们的
`14:10` 这两个to里面
`14:11` 我们真正需要的是get forecast
`14:13` 这个
`14:14` 因为它可以给我们解答
`14:16` 纽约明天的天气这个问题

### 使用 MCP Server `14:19`

`14:21` 好说了这么多
`14:23` 现在我们回到KLEIN这里
`14:25` 确认一下
`14:26` 我们已经开启了这个MCP server
`14:28` 我们再问问client相同的问题
`14:38` 这个时候KLEIN应该是可以发现我们的MCP server
`14:41` 并且用它来解决我们的问题
`14:42` 我们拭目以待
`14:46` 好可以看到KLEIN确实是发现了weather
`14:49` 这个MCP server
`14:50` 而且也发现了其中的get forecast to
`14:54` 是可以解决我们的问题的
`14:55` 他帮我们填入了纽约的经纬度信息
`14:59` 需要让我们确认一下是否同意他执行这个to
`15:02` 我们点击approve
`15:06` 可以看到调用已经完成了
`15:08` 他拿到了未来几天的天气信息
`15:10` 这个时候呢模型是在总结
`15:12` 给出一个符合我们问题的答案
`15:14` 我今天的日期呢是4月13号
`15:17` 他把纽约
`15:17` 明天4月14号的天气信息都总结出来了
`15:20` 包括白天夜间以及相关的建议
`15:23` 可以看出效果还是不错的

### MCP 交互流程详解 `15:24`

`15:27` 我们刚才配置了一个mc p server
`15:29` 并且使用了它
`15:30` 不过到底内部发生了什么呢
`15:33` 我来给大家画个图
`15:34` 大家就明白了
`15:35` 首先我们用JSON配置好一个MCB server
`15:38` 并且保存之后
`15:39` client就会使用配置中的命令去执行这个程序
`15:43` 执行了之后
`15:44` 这个程序就会等待client的指令
`15:46` KLEIN首先跟他说
`15:47` 你好啊
`15:48` 我是clean MCP server
`15:50` 回答道
`15:51` 你好啊
`15:52` 我是weather
`15:53` 寒暄之后
`15:54` 克劳in继续问你有什么two可供我使用啊
`15:57` MCP server回答道
`15:58` 我有两个to
`15:59` 一个是get forecast
`16:01` 你给我经纬度
`16:02` 这个to就可以告诉你对应地区的天气预告
`16:05` 另外一个是get alerts
`16:07` 你传入美国的州名
`16:09` 我就可以告诉你对应州的实时预警信息
`16:11` KLEIN记住了MCP server的回答
`16:13` 因为后面可能会用到到此为止
`16:16` MCP server的注册就结束了
`16:18` 这些事情都发生在开启这个MCP server的一瞬间
`16:22` 然后我们不是问问题了吗
`16:24` 纽约明天的天气怎么样
`16:26` 克莱拿到问题之后
`16:27` 把这个问题传给模型
`16:29` 让他解答
`16:30` 与往常不同的是
`16:32` client发给模型的除了用户问题
`16:34` 还有注册好的mc p server
`16:36` 以及每个mc p server所包含的to列表
`16:39` client告诉模型
`16:41` 这些to可能会帮助你解决用户的问题
`16:43` 需要调用哪个
`16:44` 跟我说
`16:45` 我来帮你调魔性一看用户的问题
`16:48` 我回答不了啊
`16:49` 我哪知道纽约明天的天气
`16:50` 我的知识到去年12月就截止了
`16:53` 不过weather这个mc p server里面有个叫做get forecast to
`16:58` 好像是可以回答这个问题
`17:00` 要不就让KLEIN调用一下这个to试试
`17:03` 因此模型告诉KLEIN
`17:05` 我要调用get forecast
`17:06` 这个图参数是北纬40度
`17:08` 西经74度
`17:09` Cliline in
`17:09` 拿到模型的请求之后
`17:11` 紧接着就跟weather这个MCP server沟通
`17:13` 你不是说你有get forecast这个to吗
`17:16` 那给我调用一下参数是北纬40度
`17:19` 西经74度
`17:20` weather接收到client请求之后
`17:22` 用指定的参数调用了get forecast这个函数
`17:26` 拿到了结果
`17:26` 并且把它传给了client klein
`17:29` 继而把结果再传回给了模型
`17:31` 告诉他你要的to结果到了
`17:33` 模型接到后
`17:34` 根据结果回答了用户问题
`17:36` 并且把他的答案发回给了klein climb
`17:39` 把结果再发回给用户
`17:41` 这个呢就是我们最终看到的那个结果了
`17:43` 没错

### 使用他人的 MCP Server `17:44`

`17:44` 这个就是整个过程了
`17:48` 前面我们说的mc p server是我仿照官方案例写的
`17:52` 那如何使用别人写的呢
`17:54` 在哪里能找到别人写的MCP server呢
`17:57` 目前呢有很多的MCP server市场
`17:59` 比如说MCP点
`18:01` So m c p market
`18:03` 点com smedery
`18:04` 点AI等等
`18:05` 大家需要的话可以去这几个网站找找看
`18:09` MCP server呢大多是使用Python或者是node进行编写
`18:13` 对应的启动程序
`18:14` 一般是NVX或者是NPX
`18:17` 对于这两种启动方式
`18:19` 我们分别举一个例子
`18:20` 其他的大家举一反三就好了
`18:22` 首先是UVX
`18:24` UVX呢是u v to run这个命令的缩写
`18:29` 代表使用UV来运行一个to
`18:32` 不过大家需要注意啊
`18:33` 这里面的to是UV领域的to
`18:35` 跟我们前面所说的MCP的to不是一个东西
`18:39` 不过啊我相信大家可能还会有另外一个问题
`18:42` 你可能不太清楚UV是什么
`18:44` 简单来说UV就是Python界的一个包管理软件
`18:48` 而其中的UVX呢可以用来直接启动Python程序
`18:52` 比如uv x rap这个命令就可以用来安装
`18:56` 并且启动rap这个程序
`18:58` UVX会帮你把rap需要的依赖
`19:00` 执行环境全部都配置好
`19:02` 不需要你自己去处理
`19:04` 当然使用UVX之前
`19:06` 我们需要安装它
`19:07` 大家可以去UV的GITHUB仓库找到对应的安装命令
`19:11` 比如说我用的是Mac os
`19:13` 用的就应该是这个命令
`19:15` 大家可以把它复制一下
`19:17` 到命令行这里面进行粘贴
`19:19` 我这里面之前已经安装过了
`19:21` 所以呢就不给大家执行了
`19:23` 安装完毕之后
`19:24` 你可以试着执行一下这个命令
`19:26` U v x pi calsay
`19:28` Hello world
`19:30` 这个命令
`19:31` 就是让pi calc这个程序做一个牛在叫的图片
`19:35` 并且在上面显示hello world这句话
`19:37` 如果你的图片也像我这样显示出来了
`19:40` 那就没问题了
`19:41` 接下来我们使用fetch这个MCP server
`19:44` 给大家演示一下
`19:45` fetch呢是用来抓取网页内容的
`19:48` 它是用UVX来启动的
`19:49` 符合我们的场景
`19:51` 我们首先打开MCP点
`19:53` so在这里面搜索fetch
`19:58` 然后点击进入
`20:02` 再点击这里面的content
`20:08` 这里有配置的命令
`20:10` 我们选择UVX这个
`20:12` 然后复制里面的内容
`20:15` 再到client这边这里加个逗号
`20:19` 然后粘贴稍微格式化一下
`20:24` 然后我们点击这里再点击install
`20:27` 可以看到client已经识别出我们家的fetch了
`20:30` 不过有没有注意到这里面的红点呢
`20:33` 这代表尚未加载完成
`20:35` 大家不要被前面的绿色开关迷惑
`20:37` 这只是代表mc p server没有被禁用
`20:40` 是否禁用和是否加载完成是两回事儿
`20:43` 既然还在加载
`20:44` 那我们就再等一会儿
`20:45` 啧啧啧啧啧啧啧啧好
`20:48` 我等了一分钟了
`20:50` 大家可以看到没有安装成功啊
`20:52` 显示的报错是request timed out呃
`20:56` 也就是超时了
`20:57` 这是为什么呢
`20:58` 这是因为UVX首次去执行某个程序的时候
`21:02` 需要先去下载它
`21:04` 而下载呢就需要一些时间
`21:06` 默认的超时时间是一分钟
`21:07` 如果一分钟之内你还是没有下载好的话
`21:10` 就会报超时的错误
`21:12` 针对这种情况
`21:13` 我建议你先复制对应的命令
`21:15` 到终端里面去执行一遍
`21:17` 比如说像我这个例子
`21:19` 我就可以先去复制这里面的UVX
`21:22` 然后呢再复制2X里面的内容回车
`21:28` 稍微等一段时间
`21:29` 等他把整个的程序下载完毕
`21:34` 啧啧啧啧啧
`21:36` 安装完了就是这个样子
`21:37` 显示34个依赖全部都安装完成
`21:40` 并且会在这里面等待
`21:42` 大家不要以为安装卡住了
`21:44` 它正常启动就是这个样子
`21:46` 没有什么提示
`21:47` 因为大部分的MCP server
`21:49` 是通过输入输出来沟通的
`21:51` 所以他不会随便输出东西
`21:53` 既然都下载完了
`21:54` 我们退出就好了
`21:56` 一般情况下我们应该是用CTRL加C退出程序
`22:00` 不过你可能按CTRL加C没有反应
`22:03` 这个时候呢也可以按CTRL加反斜杠退出
`22:07` 只不过这种退出方式会显示一个系统提醒
`22:10` 没关系
`22:11` 其实关掉就好了
`22:12` 然后我们再回到vs code这里
`22:15` 点击retry connection
`22:19` 可以看到这次加载的速度就快多了
`22:22` 而且没有报错
`22:23` 已经加载成功了
`22:24` 既然已经安装好了fetch这个MCP server
`22:27` 我们不妨就用用看
`22:29` 首先我们打开一个新的绘画
`22:32` 输入我们的问题
`22:34` 请抓取下面这个网页的内容
`22:36` 并将其转换为markdown后
`22:37` 放到项目目录里面的guys
`22:39` 点MD文件中
`22:41` 然后我就给了一个URL
`22:42` 这个URL在页面里面的展示呢是这个样子的
`22:46` 是UV的一个说明网页
`22:48` 我们回车看一下client会如何处理这个问题
`22:57` 可以发现
`22:58` client确实是找到了我们的fetch MCP服务器
`23:02` 并且开始提取参数
`23:04` 准备调用
`23:11` 当然他也做了其他的准备工作
`23:13` 比如说文件写入
`23:14` 可以用它内部的right to file工具进行处理
`23:19` 那他现在已经拼接好工具的调用参数了
`23:23` 正在征得我们同意
`23:24` 我们点击approve
`23:30` 可以看出他已经成功地使用fetch MCP server
`23:35` 获取到了网页的内容
`23:36` 他下面呢就开始把这个网页的内容转换成markdown
`23:41` 并且写入到我所规定的guys点MD文件中
`23:47` 不错CLINE已经完成了我们的任务
`23:51` 而且整个过程非常丝滑
`23:53` 没有出现一点问题
`23:57` 聊完了UVX
`23:59` 我们再来看看另外一种启动MCP server的方式
`24:02` NPXNPX跟UVX类似
`24:05` 也是可以自动下载
`24:07` 并且安装程序
`24:08` 只不过UVX安装的是Python程序
`24:11` 而NPX安装的是node程序
`24:13` 由于NPX是node的一部分
`24:16` 所以我们直接安装node js就可以
`24:19` 大家打开NO js的官方网站
`24:21` 点击这里的下载NO js进行安装就行了
`24:25` 安装的过程呢很简单
`24:26` 就跟安装正常软件一样
`24:28` 大家自己处理一下
`24:30` 安装好了之后
`24:31` 我们打开mc p market这样的一个网站
`24:35` 在里面搜索一个MCP server
`24:38` 叫做hot news
`24:41` 然后我们点击这个这个mc p server呢
`24:44` 是用来给我们拉取新闻的
`24:46` 你看他支持了很多的新闻
`24:50` 在这里面都有显示呃
`24:52` 如果想要使用它的话呢
`24:53` 我们需要选中MCP server
`24:56` 配置里面的对应部分复制
`24:59` 然后来到client这边呃
`25:01` 点击这里面的configure MCP service
`25:04` 然后粘贴粘贴好了之后呢
`25:08` client就会给我们加载上这个MCP server了
`25:11` 同之前一样
`25:12` 如果说你的加载过程非常长
`25:15` 以至于超时的话
`25:16` 你可以把这里面的命令放到终端里面去执行
`25:20` 让它提前下载完
`25:22` 然后呢再跑到client这边再加载一次呃
`25:26` 因为我这里面下载比较快
`25:27` 所以我就不用到终端那边再去试一次了
`25:29` 那既然安装完了
`25:31` 我们就不妨来试一试
`25:32` 首先新建一个对话
`25:34` 然后我们给他问题
`25:43` 看来呢
`25:44` KLEIN是发现了我们刚安装的这个MCP服务器呃
`25:48` 并且找到了一个合适的工具
`25:51` 在征求我们的同意
`25:52` 看看是否能运行这个工具
`25:54` 我们点击approve
`25:59` 好执行完了
`26:00` 而且模型给我们总结了一下呃
`26:03` 这样的话我们就拿到了最终的结果了
`26:05` 这个由NPX安装的MCP server呢
`26:08` 我们也演示完了
`26:09` 除了UVX和NPX之外呢
`26:12` 可能还会有一些别的安装方式
`26:14` 比如BONEX
`26:16` 或者是直接使用node启动
`26:18` 大家仔细看看
`26:19` MCP server对应的安装文档都能解决
`26:21` 这里呢我就不再赘述了
`26:26` 那MCP教程的第一部分也就是基础部分
`26:29` 到这里就结束了
`26:31` 我后面还会出个进阶教程
`26:33` 给大家演示下如何编写自己的MCP server
`26:36` 并通过截取MCP server输入输出的方式
`26:39` 把MCP server的运行原理挖个底朝天
`26:43` 当然这也要看这个视频的效果
`26:45` 如果这个视频效果不好的话
`26:47` 我就不打算做第二部分了
`26:49` 毕竟谁也不想在没人关注的领域浪费时间
`26:52` 所以如果你觉得这个视频对你有帮助
`26:55` 也请多多点赞关注
`26:57` 这些都会成为我做视频的动力
`26:59` 好了
`27:00` 今天的视频就到此结束了
`27:02` 我们下期再见
`27:03` 拜拜
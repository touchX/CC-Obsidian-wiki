---
title: "MCP终极指南 - 带你深入掌握MCP（进阶篇）"
url: "https://www.bilibili.com/video/BV1Y854zmEg9?spm_id_from=333.788.videopod.sections&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1Y854zmEg9"
cid: "29504309644"
author: "马克的技术工作坊"
upload_date: "2025-04-19"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114364979876707&bvid=BV1Y854zmEg9&cid=29504309644&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

MCP终极指南   带你深入掌握MCP（进阶篇） #mcp #llm #ai #编程 #代码 #教程 #deepseek #cline #vscode 

时间轴：
00:00 视频内容介绍
01:12 自己动手写一个 MCP Server
11:00 MCP 底层协议分析的原理与方法
12:48 实战解析：MCP 底层协议的完整剖析过程
20:54 使用 MCP 底层协议直接与 MCP Server 交互
23:08 超越表象：MCP 协议的真实含义与定位
26:46 结束语

## 章节

- `00:00` 视频内容介绍
- `01:12` 自己动手写一个 MCP Server
- `11:01` MCP 底层协议分析的原理与方法
- `12:49` 实战解析：MCP 底层协议的完整剖析
- `20:54` 在终端直接与 MCP Server 交互
- `23:09` MCP 协议的真实含义与定位
- `26:47` 结束语

## 字幕

### 视频内容介绍 `00:00`

`00:00` 我几天前发布了MCP终极指南的基础篇
`00:02` 给大家讲解了MCP的原理
`00:04` 并进行了一些实战
`00:06` 今天我们继续来讲这个系列的进阶篇
`00:09` 进阶篇大致分为三个部分
`00:11` 第一我会带大家动手自己写一个MCP server
`00:14` 第二我会截获MCP server的输入和输出
`00:18` 并逐行分析
`00:19` 让大家彻底明白MCP协议是如何运作的
`00:22` 在第二部分的结尾
`00:24` 我甚至会给大家演示
`00:25` 如何在不借助mc p host和任何编程语言的情况下
`00:29` 根据MCP协议的规范
`00:31` 直接与mc p server沟通
`00:33` 第三我们会在掌握了协议细节的基础上
`00:36` 回头再想想
`00:37` M c p
`00:37` 也就是模型上下文协议到底是个什么意思
`00:40` 它在大模型相关的应用中到底扮演了什么角色
`00:44` 因为是进阶篇
`00:45` 所以这个视频的内容有一些难度
`00:47` 你最好有一定的编程基础
`00:49` 不用很厉害
`00:50` 但一定要有
`00:51` 否则你可能很难看懂
`00:53` 另外提一点
`00:54` 这个视频会用到很多基础篇
`00:56` 已经提过的知识内容
`00:58` 如果你还没有看过基础篇的话
`01:00` 建议还是看完基础篇之后再来看这个进阶篇
`01:03` 除此之外
`01:04` 我把本视频涉及到的代码
`01:06` 都放到了这个GITHUB仓库中
`01:07` 方便大家自行尝试
`01:09` 好废话少说
`01:11` 那我们就开始吧

### 自己动手写一个 MCP Server `01:12`

`01:14` 下面我们来讲讲如何创建自己的mc p server
`01:17` 你可以使用Python
`01:19` Node
`01:19` Java
`01:20` c#等语言或者是框架来实现
`01:23` 考虑到使用人数和编程难度
`01:25` 我们这里选择Python来给大家讲解
`01:28` 在正式开始前
`01:29` 我先给大家打个预防针
`01:31` 这个呢是讲MCP的教学视频
`01:33` 不是讲Python的
`01:35` 所以我不会特别解释
`01:36` 各种Python内置函数的使用方法
`01:38` 另外呢还有一些无关紧要的内容
`01:40` 比如说HTTP接口
`01:42` 输入输出格式的转换
`01:43` 我也会一带而过
`01:44` 如果你熟悉Python开发
`01:46` 那很好
`01:47` 这个视频很适合你
`01:48` 如果你不熟悉的话
`01:49` 你可能会在这个过程中有些焦虑
`01:52` 因为有些细节不是特别明白
`01:54` 如果出现了这个情况
`01:55` 我建议你分清主次
`01:57` 你要搞清楚你不懂的是Python还是MCP
`02:00` 如果是前者
`02:01` 那不清楚也没关系
`02:03` 你不是来学Python的
`02:04` 你是来学习MCP协议的
`02:06` 你只要明白了MCP协议的精髓
`02:08` 你就可以使用任何编程语言来开发mc p server
`02:11` 而这个也正是我想通过代码传达给你的内容
`02:14` 实际上你后面就可以看到
`02:16` 了解了MCP的本质之后
`02:18` 你甚至可以用bash脚本来写mc p server
`02:21` MCP协议与语言无关
`02:23` 好这里说的有点多了
`02:25` 我们现在就进入正题
`02:26` 首先我们来安装Python
`02:28` 如果你是MCOS用户
`02:30` 那么恭喜你
`02:31` MACOS自带Python
`02:32` 你只需要保障Python的版本是在3.10
`02:35` 或者是以上就可以了
`02:37` 那如果你是windows用户呢
`02:39` 你需要到Python的官方网站进行下载安装
`02:42` 安装的过程很简单
`02:44` 跟安装普通软件并没有什么太大的区别
`02:46` 所以呢大家自己处理一下
`02:48` 我这里就不细说了
`02:50` 除此之外
`02:50` 我们还需要三样东西
`02:52` UV这个是Python的包管理器
`02:55` Vs code
`02:56` 这个是写代码用的软件
`02:58` Client
`02:59` 这个是vs code的一个插件
`03:01` 这三样东西在我们这个系列的基础篇里面
`03:04` 也都提到过
`03:05` 这里呢就不再重复说了
`03:07` 请大家自行安装好
`03:09` 下面我们就来正式讲解下
`03:12` 如何编写你的第一个MCP server
`03:14` 为了方便大家复制粘贴代码
`03:16` 我这里就使用官方的例子
`03:18` 对应的页面
`03:19` 就是这个我写的代码呢与官方基本上是相同的
`03:24` 只是会有一些小改动
`03:26` 这些改动呢我都会专门说明
`03:28` 所以不必担心
`03:29` 首先我们打开终端
`03:31` 使用uv in its weather
`03:32` 新建一个名为weather的项目
`03:35` 进入到项目目录里
`03:37` 再执行UVVENV新建一个虚拟环境
`03:41` 新建虚拟环境是为了防止我们安装的各种依赖
`03:44` 影响到系统环境
`03:45` 然后呢我们再执行source点v e n v ban activate
`03:49` 进入到这个虚拟环境中
`03:52` 看到前面的这个括号weather了吗
`03:54` 这就表示
`03:55` 我们已经进入到whether的虚拟环境里了
`03:57` 然后呢我们再执行uv ADD MCP CLI
`04:02` HTTPX安装
`04:04` 开发MCP必备的依赖
`04:05` 其中MCPCLI就不用说了
`04:08` 看上去呢就是开发MCP要用的核心依赖
`04:12` 后面的HTTPX呢
`04:13` 是给我们调用HTTP用的
`04:15` 要查询天气嘛
`04:17` 调用HTTP来查询是一件很正常的事情
`04:20` 两个依赖安装完成后
`04:22` 我们用vs code打开weather这个项目目录
`04:25` 新建一个叫做weather点PY的文件
`04:28` 我们的主要工作就在这个文件中完成
`04:31` 首先我们导入必要的包和变量
`04:37` 这个呢就不用多说了
`04:39` 导入进来的对象后面都是要用的
`04:41` 然后我们调用fast MCP函数
`04:44` 这是一个能够帮助我们快速构建
`04:47` MCP服务器的函数
`04:49` 我们传入我们正在创建的这个MCP server的名称
`04:52` 它会给我们返回一个对象
`04:53` 我们把这个对象起名叫做MCP
`04:56` 后面无论是注册to还是启动这个mc p server
`05:00` 我们都会用到MCP这个对象
`05:01` 注意我这里的代码与官方不同
`05:04` 官方写的代码中没有log level等于error这一部分
`05:08` 这个呢是我加上去的
`05:10` 我试了一下
`05:11` 如果不加这部分的话
`05:13` 你做的MCP server很有可能是运行不起来的
`05:16` 因为默认情况下
`05:17` 程序会输出各种乱七八糟的日志
`05:20` 干扰MCP server的运行
`05:22` 然后我们导入两个常量
`05:24` 一个呢是美国气象局的地址
`05:27` 我们就请求这个URL来拿美国的天气信息
`05:30` 第二个是我们请求数据时所用的标识
`05:33` 说白了就是告诉美国气象局接口
`05:35` 我们到底是谁
`05:37` 比如说如果我们是浏览器请求的话
`05:39` 那这里很有可能就是chrome相关的一些信息
`05:42` 因为我们是在weather这个MCP server里
`05:44` 调用美国气象局这个接口的
`05:46` 所以我们写名我们是weather app版本号1.0
`05:50` 然后我们引入两个后续会用到的函数
`05:53` 一个是make n w s request
`05:55` 用于请求天气数据
`05:57` 他接受一个参数就是请求天气数据时所用的URL
`06:01` 然后它内部就使用HTTP库调用了这个指定的URL
`06:05` 返回了他从HTTP那里拿到的结果
`06:08` 除了这个函数之外
`06:10` 我们还需要导入另外一个工具类函数
`06:12` 叫做format alerts
`06:14` 这个函数用于对告警数据做格式化之后
`06:17` 我们引入这个m cp server中的第一个to
`06:20` 叫做get alerts
`06:23` 它用于接收美国某个州的天气预警
`06:26` 它的参数是美国的州代码
`06:28` 这个函数呢会返回预警相关的一些信息
`06:31` 这个to的实现很简单
`06:33` 其实就是直接调用了美国气象局的接口
`06:36` 用的呢就是我们刚才加入进来的
`06:39` make n w s request函数
`06:41` 调用完之后
`06:42` 这个函数对结果简单做了下检测
`06:45` 这里是确保调用没有失败
`06:47` 这里是检查下我们调用的地区
`06:49` 是否不存在预警信息
`06:50` 如果不存在
`06:51` 就立即返回结果
`06:53` 最后这一部分是对预警信息做格式化
`06:55` 只拿接口返回中我们需要的那一部分
`06:58` 写完了第一个to我们引入第二个
`07:00` 第二个to呢叫做get forecast
`07:05` 这个兔子参数呢是latitude和longitude
`07:09` 分别是纬度和经度
`07:11` 同样啊
`07:11` 你别看他写了那么一大堆
`07:13` 其实也就干了跟get alerts类似的事情
`07:16` 他也调用了美国气象局的接口
`07:18` 不过他调用了两次
`07:20` 第一次是为了获取到对应的天气预告
`07:22` 办公室的信息
`07:24` 因为美国很大
`07:25` 所以不同地区的天气预告
`07:26` 由不同的天气预告办公室负责
`07:29` 所以我们要先获取到对应的天气预告办公室
`07:32` 这个信息就放在point data里面
`07:35` 拿到了这个信息后
`07:36` 我们从中提取出对应天气预告的URL
`07:39` 放到forecast url这个变量里面
`07:43` 然后再拿着这个forecast ul再次请求美国气象局
`07:47` 这之后呢才算是真正拿到了天气预告
`07:50` 这个天气预告就放在forecast data里面
`07:54` 在后面这一堆啊
`07:56` 就是对天气预告信息做格式化
`07:58` 并且返回写好了这两个函数之后
`08:01` 我们再回头看看他们
`08:04` 不知道大家有没有注意到
`08:06` 这两个函数的头上都有一个装饰器
`08:08` 这个装饰器的用途是把函数注册为to
`08:12` 它会从函数的注释里面提取这个函数的用途
`08:15` 以及每个参数的含义
`08:17` 以便模型决定调用这个函数的最佳时机
`08:22` 比如get alerts这个函数
`08:24` 它提取的内容就包括如下几点
`08:26` 第一函数名是get alerts
`08:29` 第二参数是state类型是字符串
`08:32` 第三这个函数的功能
`08:35` 第四每个参数的功能
`08:37` 这些提取的内容最终都会转换为to的信息
`08:41` 并在实际调用的时候传给模型
`08:43` 以便帮助模型做决策
`08:46` 最后我们加上这些代码
`08:48` 以便程序启动的时候能够运行这个MCP server
`08:51` 对于最后加入的这几行代码
`08:54` 我们需要注意的只有这里面的transports
`08:57` 这里的transport值是STDIO表示
`09:01` MCP server与client的沟通方式
`09:03` 是MCP server的输入和输出
`09:06` 据我观察
`09:07` 目前市面上大部分的MCP server
`09:09` 用的都是这种方式
`09:11` 如果你不知道使用输入和输出沟通
`09:13` 具体是个什么意思
`09:15` 我建议你暂时先不要纠结
`09:17` 我们后面呢会截获MCP server的输入输出
`09:20` 并对MCP协议做一个分析
`09:23` 那个时候呢我会详细解释
`09:25` 到时候你应该就明白了
`09:27` 到这里我们的MCP server就算是写完了
`09:30` 剩下的工作就是把它注册到client里面
`09:34` 这一步呢我们在基础篇里面已经做过一次了
`09:37` 我再给大家演示一下
`09:39` 首先点击侧边栏里面的client
`09:41` 再点击这个图标
`09:43` 打开mc p server配置
`09:45` 再点击install
`09:46` 然后再点击configure mc p server
`09:49` 然后输入我们MCP server的启动方式
`09:52` 这个配置很好理解
`09:54` 其实就是用UV来启动weather
`09:56` 点PY
`09:57` 也就是我们刚才写的文件
`09:59` 这里面有一个杠杠directory
`10:01` 就是告诉UV我们的项目目录在这里
`10:04` 如果要用到什么外部依赖
`10:05` 或者是虚拟环境配置的话
`10:07` 就在这个项目目录里面找
`10:09` 大家注意这个目录呢要改成你自己的
`10:11` 用我的呢大概率是行不通的
`10:14` 然后我们保存文件
`10:15` 注意左边在保存完成的时候
`10:18` client就会帮助我们注册好这个MCP server
`10:20` 我们现在就可以使用它了
`10:22` 我们新建一个对话
`10:23` 输入我们的问题
`10:25` 纽约明天的天气怎么样
`10:32` 可以发现模型找到了我们的MCP server
`10:35` 并且发现其中的get forecast to
`10:37` 是可以解决这个问题的
`10:39` 我们点击approve同意它调用
`10:51` 模型应该是拿到了get forecast的调用结果
`10:54` 并且把它整理成了答案返回给了我们
`10:57` 这说明我们写的mc p server是没有问题的

### MCP 底层协议分析的原理与方法 `11:01`

`11:02` 我们自己动手创建了一个MCP server
`11:05` 不过呢我相信还是有很多人不理解
`11:07` MCP这个协议到底是如何运作的
`11:10` 因为虽然这个MCP server是我们自己写的
`11:13` 但是呢我们其实是使用了MCP的库来完成
`11:16` 它的这个库呢帮助我们做了很多事情
`11:19` 我们并不太清楚他到底做了什么
`11:21` 那怎么才能知道呢
`11:23` 首先大家需要理解的是
`11:26` 大部分的m cp server都是通过输入和输出
`11:29` 与client沟通的
`11:31` 如果这个MCP server是在我们终端里面启动的
`11:34` 那我们就可以直接看到它的输入和输出
`11:38` 不过现在麻烦的是
`11:39` 这个MCP server是client启动的
`11:42` 它的输入和输出只有client才能看到
`11:45` 我们是看不到的
`11:46` 这怎么办呢
`11:47` 我的想法是啊
`11:49` 我们来写一个脚本
`11:50` 我们暂且把这个脚本的名称叫做MCP log点pi
`11:54` 这个脚本的参数呢
`11:55` 就是用于启动我们这个MCP server的命令
`11:58` 比如用我们刚才的weather举例
`12:00` 它原来是用这个命令来启动的
`12:02` 前面加上Python MCP log点pi
`12:05` 就是我们新的启动命令
`12:07` M c p log
`12:07` 点拍的功能是
`12:09` 截取后面这一部分程序的输入和输出
`12:12` 也就是MCP server的输入和输出
`12:14` 我们再来看看这个MCP server与client的交互图
`12:18` 并把MCP log的拍加入到这个流程里面
`12:21` 我们让KLEIN与mc p log点拍沟通
`12:25` 再让MCP log点pi与真正的mc p server沟通
`12:29` MCP logger点pi在其中只充当中间人的角色
`12:33` 它在中间插了一层作用呢
`12:35` 就是获取到client与MCP server的输入和输出
`12:38` 并且把它们写入到一个外部的文件里面
`12:41` 这样我们查阅文件内容
`12:43` 就可以知道client具体是怎么跟MCP server沟通的了

### 实战解析：MCP 底层协议的完整剖析 `12:49`

`12:50` 好方案有了
`12:51` 那我们去哪里找这个MCP log点拍呢
`12:54` 现在大模型写代码这么强
`12:56` 所以我直接让GEMINI2.5pro给我写了一个
`13:00` 就是这个文件了
`13:01` 我试了试啊
`13:03` 跑的还算不错
`13:04` 我也来给你演示一下它的用法
`13:06` 我们再回到client这里
`13:08` 跟之前一样
`13:09` 找到MCP server的配置
`13:11` 然后呢我们把启动参数来改一下
`13:14` 把这里面的UV挪到下面去
`13:21` command改成Python
`13:23` 然后参数中再加上我们的mc p log
`13:26` 点拍的路径保存
`13:28` 这个时候
`13:29` KLEIN就已经跟我们的mc p server有一些交互了
`13:33` 那输入输出呢
`13:35` 输入输出是写到了MCPIO点log文件里
`13:38` 这个文件与MCP log点pi同处一个目录中
`13:42` 大家可以发现它的每一行输入输出都很长
`13:46` 我们来给它做个自动换行
`13:48` 关于这个日志内容
`13:50` 有一点要注意
`13:51` 每行最前面的输入和输出
`13:53` 以及后面的冒号是MCP log
`13:56` 点拍
`13:56` 这个脚本加的不属于client与MCP server的交互内容
`14:00` 它们的交互内容就是后面的JSON
`14:03` 这里标识出输入和输出
`14:06` 是为了方便大家查看数据流向
`14:08` 输入的呢是client到MCP server发送的数据
`14:12` 而输出的呢是MCP server向client发送的数据
`14:16` 首先我们看一下第一行的输入
`14:19` 这个呢是client发给我们的MCP server的
`14:22` 相当于KLEIN向我们的mc p server打了个招呼
`14:26` 告诉他
`14:26` 你好呀
`14:27` 我是KLEIN
`14:28` 我的软件版本是3.12.3
`14:31` 我用的MCP协议版本是
`14:33` 2024年11月5号出品的
`14:36` 人家都打招呼了
`14:37` 我们的MCP server也不能干
`14:39` 坐着呀
`14:39` 所以呢他就回复了
`14:41` 你好呀
`14:42` 我用的协议版本跟你一样呢
`14:44` 也是2024年11月5号的协议
`14:47` 另外我有这些能力不支持
`14:49` 你要记好哦对了
`14:52` 我叫weather
`14:52` 版本号是1.6.0
`14:54` 很高兴认识你
`14:56` 第三行呢是client发给我们MCP server的
`15:00` 大意就是收到
`15:01` 随后KLEIN就又发了一个消息过来
`15:04` 他想要知道我们的MCP server里面包含了哪些to
`15:08` 我们的MCP server的回复是这样的
`15:11` 这段很长
`15:12` 为了方便大家观看
`15:13` 我来格式化一下呃
`15:15` 格式化之后呢是这个样子
`15:17` 可以看出里面有两个图
`15:18` 一个呢是叫做get alerts
`15:21` 另外一个呢是叫做get forecast
`15:24` 这两个to的定义遵循的是同一套格式
`15:27` 因为我们最终是用get forecast
`15:29` 来预测纽约明天的天气的
`15:31` 所以我们以它为例着重看一下
`15:34` get forecast的功能是获取某个地区的天气预告
`15:38` 并且把每个参数的含义也都给了出来
`15:41` 有没有觉得这段很熟悉呢
`15:43` 对这就是我们函数里面的注释
`15:46` 实际上在Python领域里面
`15:48` 这叫做dog string
`15:50` 它是一种特殊的注释
`15:52` 不过考虑到教程的通用性
`15:53` 我们还是沿用注释这个词
`15:56` 还记得我们在函数上面加了at MCP点
`15:59` to这个装饰器了吗
`16:01` 对它就会提取出函数的注释
`16:03` 并且把它放在这里面的description字段里
`16:06` 作为to的描述
`16:08` 这样模型就可以从description中了解到
`16:11` tool的用途
`16:12` 方便到时候选择与用户问题最匹配的to
`16:15` 再看看下面的input stma
`16:17` 我们要不先来猜猜它是什么意思
`16:19` 它看起来好像是定义了两个参数
`16:21` 一个是叫做latitude
`16:23` 也就是纬度类型是number
`16:25` 另外一个呢是叫做longitude
`16:27` 也就是精度类型呢也是number
`16:30` 这里面还有一个required
`16:31` 好像是在说这两个属性都必须要存在
`16:34` 没错猜的八九不离十了
`16:36` input schema遵循的是一个叫做jason schema的规范
`16:40` 不过jason schema是什么东西
`16:42` 跟JASON是什么关系
`16:43` 实际上jasson schema本身就是一段JSON
`16:46` 只不过呢这个JSON有个特殊的功能
`16:48` 它可以用来描述另外一个JSON的结构
`16:51` 比如我们现在正在讨论的这个input chema
`16:54` 它描述的JASON呢是这样子的
`16:56` 它有两个属性
`16:57` 一个是latitude
`16:59` 一个是longitude
`17:00` 它们的类型都是number
`17:02` 而且这两个属性都得存在
`17:04` 缺一不可
`17:05` 符合这个格式要求的JASON有很多
`17:07` 比如这个JSON就是符合定义的
`17:10` 另外呢这个JSON呢也符合定义
`17:13` 因此这里面的input schema
`17:15` 就是用来给出to的入餐规范的
`17:18` 大家知道
`17:19` 模型不仅要选择与用户问题最匹配的to
`17:22` 还要从用户的问题里面把two的参数提取出来
`17:25` 而且这个参数必须要符合这个
`17:27` input schema的规定
`17:28` 这样呢后面才能够成功调用to背后的函数
`17:32` 另外值得提的一点是
`17:34` 这个input schema也是ADMCP点two
`17:37` 这个装饰器从我们的参数里面提取出来的
`17:40` 它是根据我们的参数定义
`17:42` 来生成这个input schema的好
`17:46` 那我们回来继续看我们MCP server的输入和输出
`17:49` 前面我们聊了这一部分的内容
`17:52` 那我们来继续看一下后面的部分
`17:54` 这一部分是KLEIN在询问
`17:56` 这个mc p server有没有资源和资源模板可以用
`18:00` 可以看到这里面的resources和resource
`18:03` templates的结果都是空列表
`18:05` 也就是说MCP server的回答是都没有resource的
`18:09` 中文呢就是资源
`18:10` resource templates呢
`18:11` 你可以理解为是一个动态的资源
`18:14` 资源呢就是一个文件
`18:15` 一个报告之类的东西
`18:17` 我们的MCP server不涉及就不细讲了
`18:20` 感兴趣的同学呢可以自己去查下KLEIN
`18:23` 到此时摸底就结束了
`18:25` 他就闭嘴了
`18:26` 这一切呢都发生在我们注册工具的一瞬间
`18:29` 后面就是要等待合适的时机
`18:32` 再使用这个MCP server了
`18:34` 然后我们打开一个新的对话
`18:36` 再问一下之前的问题
`18:37` 纽约明天的天气怎么样啊
`18:42` 好可以看出client找到了对应的to
`18:45` 我们允许它执行
`18:52` 这个是执行结果
`18:53` 他从执行结果里面抽取了纽约明天的天气
`18:57` 最后展示了出来
`18:58` 这个对话就到此结束了
`19:00` 我们再回到日志这里看看发生了什么啊
`19:07` 我把滚动条往下拖一拖
`19:09` 只看新增的部分
`19:10` 首先可以看出client通知了这个mc p server
`19:14` 我要使用get forecast
`19:15` 这个to参数是40.7
`19:18` 128和-74.006
`19:21` 也就是北纬40度
`19:22` 西经74度
`19:24` 这个地方我们稍微停一下
`19:25` 我再给大家看一下
`19:27` 刚才展示的这个to的详情信息
`19:29` 记不记得我们之前说过
`19:31` get forecast这个错误的规范
`19:32` 必须要符合一个叫做input schema的要求
`19:36` 然后呢我们举了这两个符合要求的例子
`19:39` 请大家记住这两个参数的样子
`19:42` 我们再回头看一下实际的参数结构
`19:44` 这里面实际拿到的to参数
`19:47` 也是符合之前说的这个规范的
`19:49` 看来input schema不是白定义的模型
`19:52` 提取参数的时候呢
`19:54` 就会遵守这个input schema的规范
`19:56` 拿到请求之后
`19:57` 我们的mc p server就会去执行对应的函数
`20:00` 然后输出结果对应的也就是第二段的输出内容
`20:04` 这一段的核心就是里面的text字段的值
`20:07` 不过我们很难一眼看清楚它里面写了些什么
`20:10` 因为它里面的换行符都被换成了杠N不太好看
`20:14` 我来给你格式化一下
`20:15` 格式化之后是这个样子的
`20:17` 可以看出它是返回了未来几天的天气
`20:20` 包括今晚的星期五的星期五
`20:23` 晚上的星期六和星期六晚上的这些
`20:26` 都是我们之前写的那个get forecast函数
`20:28` 返回的结果
`20:29` client拿到了to的执行结果之后
`20:31` 它与MCP server的交互就算是结束了
`20:34` 所以我们的日志文件到这里也算是结束了之后
`20:38` client就会把结果发给模型
`20:40` 让模型去总结
`20:41` 我们最后看到的就是模型总结的结果
`20:44` 我们再回头看看client这边
`20:46` 其实每次to的请求参数和请求结果
`20:49` 都可以在这里面找到
`20:51` 跟我们在日志里面看到的是一样的

### 在终端直接与 MCP Server 交互 `20:54`

`20:56` 刚才我们深挖了MCP协议的底层细节
`20:59` 明白了细节之后
`21:00` 你甚至都不需要一个mc p host
`21:02` 就可以直接与mc p server沟通
`21:04` 你只需要保证你发给m cp server的数据
`21:07` 符合这个格式就行
`21:09` 为了让大家更透彻的理解MCP协议
`21:12` 我来给大家演示一下如何直接与MCP server沟通
`21:16` 不经过我们的client
`21:17` 首先我们在终端里面运行这个mc p server
`21:22` 我们用的命令呢就是MCP server里面的配置的
`21:26` 这些内容跟他是一模一样的
`21:28` 就是从这个UV开始到weather点PY结束
`21:31` 前面的这个MCP log
`21:34` 点P呢是我们用来记录它的输入输出日志的
`21:38` 我们现在不需要记录
`21:39` 所以呢就不需要加它了
`21:41` 那现在我们先跟他来打个招呼
`21:44` 我们可以直接使用这里面第一行的输入呃
`21:47` 我们可以把我们自己的名字改一下
`21:50` 比如说我们不叫克莱
`21:51` 我们叫做马克的技术工作坊
`21:54` 版本号是1.0.0
`21:57` 然后我们复制这一行到这里面粘贴
`22:01` 你看MCP server是不是回复了呢
`22:04` 他也给我们打了个招呼
`22:05` 跟日志里面的格式是一样的
`22:08` 接着我们来给他发送个消息
`22:10` 告诉他
`22:11` 我已经收到了他的消息
`22:12` 可以开始工作了
`22:14` 他呢是不会回复这个消息的
`22:16` 这跟日志里面的表现也是一样的
`22:18` 然后我们请求一下工具列表
`22:21` 复制的是这一行的内容
`22:23` 到这里粘贴
`22:25` 你看他是不是也把工具列表给我们了
`22:28` 那调用一下工具呢
`22:30` 我们来复制这一行
`22:31` 这一行原来就是用来调用工具的
`22:35` 你看他把工具的调用结果呢也给到我们了
`22:39` 所以呢调用工具也是没有问题的
`22:41` 因此你看啊我们可以直接跟他交互
`22:44` 不需要mc p host也是可以的
`22:47` 或者说在这种情况下
`22:49` 我们自己就是mc p host
`22:51` 到这里啊
`22:52` 我相信你已经完全了解MCP协议了
`22:55` 你甚至不需要调用MCP库
`22:57` 也可以开发一个mc p server了
`22:59` 你只需要保证你的mc p server
`23:01` 符合MCP的这个规范就可以了
`23:04` 当然自己写会麻烦一些
`23:06` 不如调用MCP的库简单

### MCP 协议的真实含义与定位 `23:09`

`23:10` 相信大家现在对MCP的细节
`23:13` 都有一个非常清楚的认识了
`23:15` 我们再拿出我在基础篇里面画的交互流程
`23:18` 再仔细看看
`23:19` 是不是很熟悉呢
`23:21` KLEIN先跟MCP server打招呼
`23:23` MCP server做了自我介绍
`23:25` KLEIN询问MCP server它有哪些to MCP server
`23:29` 说他有get forecast和get alerts
`23:32` 后来经过了一系列流程之后
`23:34` client向MCP server请求调用工具
`23:37` mc p server调用完后就把结果发给了client
`23:40` 这些都是我们之前在日志里面看到的内容
`23:43` 这一部分的每一个交互
`23:45` 都可以在我们日志里面找到
`23:46` 那有人可能会问
`23:48` 为什么我没有在MCP日志里面看到模型的
`23:51` 影子
`23:51` 模型是怎么使用MCP协议的
`23:54` 这是个非常好的问题
`23:55` 如果你去翻别的教学视频
`23:57` 他们基本上不会去谈论这一点
`24:00` 但是为了能够让你彻底明白MCP协议的本质
`24:03` 我这里会着重说明
`24:05` MCP协议规定的内容仅限于这个环节的交互
`24:09` 你可以看出这一部分与模型并没有什么关系
`24:13` 如果你仔细分析一下的话
`24:15` 你就会发现
`24:16` MCP协议主要是规定了两部分的内容
`24:19` 一每一个MCP server有哪些函数可以用
`24:23` 二如何调用这些函数
`24:25` 当然有些MCP server内部呢还有资源可以使用
`24:29` 这个我们暂时忽略掉这两点
`24:31` 可以总结为函数的注册与使用
`24:34` 对你可以看出这里面确实是没有模型什么事
`24:38` MCP协议规定的是如何发现和调用函数的
`24:42` 这套协议脱离大模型也是能够用的
`24:44` 只不过没有人这么用而已
`24:46` MCP协议本身并没有规定与模型的交互方式
`24:51` 回到我们这个图里面
`24:53` 这也就是说
`24:54` MCP协议并没有规定这一部分要如何处理
`24:58` 实际上
`24:59` 不同的mc p host与模型的交互
`25:01` 确实是会有很大的差异
`25:03` 比如说client是用XML与模型沟通的
`25:07` 而cherry studio是用function calling的格式与模型沟通的
`25:11` function calling呢是OpenAI提出的一套协议
`25:14` 用来规定模型是如何调用函数的
`25:17` 总之MCP协议并没有规定
`25:19` 如何与模型进行交互
`25:21` 我再重复一遍
`25:23` MCP协议并没有规定如何与模型进行交互
`25:28` 这一点很重要
`25:29` 明白了这一点你就明白了MCP协议的本质
`25:33` 我们再回头看看MCP这个名字
`25:37` 模型上下文协议
`25:39` 什么是模型上下文上下文就是环境
`25:42` 那什么是环境
`25:44` 环境就是周围有哪些函数可以用来调用
`25:47` 从而获取到外界的信息
`25:49` 比如获取天气信息
`25:51` 网络信息
`25:53` 文件信息等等
`25:55` MCP就是让模型感知外部环境的一个协议
`25:58` 所以呢它叫做模型上下文协议
`26:02` 这个名字合适吗
`26:03` 说实话
`26:04` 我个人觉得这个名字起的不太好
`26:06` 首先它有一定的误导性
`26:08` 很容易就让人以为
`26:09` 这个协议规定的是模型交互的内容
`26:12` 但实际上我们最多只能说
`26:14` 这个协议是给模型服务的
`26:17` 再者这个名字太过于玄妙
`26:19` 乍一看都不知道是什么意思
`26:21` 我不知道是不是创造MCP的人
`26:23` 故意想把这个名字起的这么高大上
`26:25` 好让别人以为这个东西确实是很高端
`26:28` 大家也都看到了MCP领域的各个专业名词
`26:32` 都蒙着一层雾
`26:33` 不打开了
`26:34` 揉碎了
`26:34` 你都不知道是怎么回事
`26:36` 我做教学视频的时候也很小心
`26:38` 每次都是一个概念一个概念的解释
`26:41` 一下子全部都扔出来
`26:42` 谁都会发懵
`26:43` 当然我相信你到这里就彻底明白了

### 结束语 `26:47`

`26:47` 到这里为止
`26:49` 我们的MCP进阶教程就告一段落了
`26:51` 相信大家对MCP都有了非常清晰而具体的理解
`26:55` 不过有些小伙伴可能还想知道
`26:57` 模型是如何与我们的mc p host交互的
`27:00` 也就是这一部分的内容
`27:02` 我们之前说过
`27:03` 这一部分与MCP协议无关
`27:05` 那他们到底是如何通信的呢
`27:07` 如果你对这个话题感兴趣
`27:09` 欢迎在评论区留言
`27:10` 我会根据大家的兴趣和反馈
`27:12` 来决定是否制作这一部分的内容
`27:14` 今天的视频呢就到此结束了
`27:16` 别忘了点赞关注我们
`27:18` 下次再见
`27:19` 拜拜
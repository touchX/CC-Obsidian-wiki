---
title: "MCP终极指南 - 番外篇：抓包分析 Cline 与模型的交互协议（内含 Agent 的实现原理）"
url: "https://www.bilibili.com/video/BV1v9V5zSEHA?spm_id_from=333.788.videopod.sections&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1v9V5zSEHA"
cid: "29755244966"
author: "马克的技术工作坊"
upload_date: "2025-05-02"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114438380265366&bvid=BV1v9V5zSEHA&cid=29755244966&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

MCP终极指南 - 番外篇：抓包分析 Cline 与模型的交互协议（内含 Agent 的实现原理）

## 章节

- `00:00` 前言
- `01:03` 截获模型出入参的原理
- `02:30` 中转服务器代码解释
- `07:16` 配置中转服务器
- `09:24` 简单场景下Cline发往模型的请求
- `23:45` 简单场景下模型发往Cline的返回
- `26:44` 调用MCP工具时模型的请求和返回
- `34:11` Cline 的 XML 协议与 ReAct 的关系

## 字幕

### 前言 `00:00`

`00:00` 大家好
`00:00` 今天我们来聊聊
`00:01` mc p host是如何与模型进行沟通的
`00:04` 之前我们出的MCP终极指南的基础篇和进阶篇
`00:08` 里面都提到过
`00:09` M c p
`00:10` 只规定了mc p host与mc p server之间的沟通协议
`00:14` 并没有对模型的输入和输出格式提出要求
`00:17` 因此不同的mc p host
`00:19` 就可能会用不同的格式来与模型沟通
`00:22` 比如client用的就是XML
`00:24` 在本期视频中
`00:26` 我将以client为例
`00:27` 给大家演示一下
`00:28` mc p host是如何与模型进行沟通的
`00:31` 为了让大家能够彻底理解
`00:33` 我将截获模型的输入和输出
`00:35` 为大家展示client与模型交互的具体细节
`00:39` 在视频的结尾
`00:40` 我还会给大家讲解
`00:41` 如何构建AGH的常用的react模式
`00:44` 以及client用的这种XML协议与react模式之间的关系
`00:48` 让大家能够透过现象看本质
`00:51` 我们的目标不是单纯的了解client的XML协议
`00:54` 而是通过学习这个协议
`00:56` 来了解如何编写一个像client一样
`00:58` 能够与模型持续交互的程序
`01:00` 好话不多说
`01:02` 让我们赶快开始吧

### 截获模型出入参的原理 `01:03`

`01:05` 我们还是用之前这个叫做weather的MCP server举例
`01:09` 这是我们基础篇里面提到过的交互图
`01:11` 我们今天要了解的就是这一部分的内容
`01:14` 既然要了解client和模型是如何交互的
`01:18` 我们就最好能够抓取到client发给模型的
`01:20` 请求和模型给client返回
`01:23` 那怎么才能做到这一点呢
`01:25` 很简单
`01:26` 我们原来不是client与open reader直接沟通吗
`01:29` 现在我们不这么做了
`01:31` 我们起一个本地服务器作为中间人
`01:34` 无论是client发送请求给模型还是模型返回
`01:37` 答案给client
`01:38` 都要先经过这个本地服务器才行
`01:41` 本地服务器在接收到client请求和模型的返回之后
`01:45` 会把具体的内容写入到一个日志文件中
`01:48` 这样我们查询文件的内容
`01:50` 就可以知道client与模型说了些什么
`01:53` 那client支持连接我们的本地服务器吗
`01:55` 答案是肯定的
`01:57` client的API provider中除了常见的OpenAI
`02:00` Deep seek
`02:01` open Ruler等模型提供商之外
`02:04` 还有一种特殊的选项叫做openai compatible
`02:07` 他的意思是对应模型提供商虽然不是OpenAI
`02:10` 但是呢它的API完全兼容OpenAI的格式
`02:14` 我们选择它之后
`02:15` 把本地服务器的地址填到base url里面
`02:18` 然后呢再填好API key model id等信息就可以了
`02:22` 剩下要做的事情就是去编写这个本地服务器
`02:25` 并且确保这个本地服务器的输入和输出
`02:28` 符合OpenAI的格式规范

### 中转服务器代码解释 `02:30`

`02:31` 我已经按照这个思路
`02:33` 把这个本地服务器的代码写好了
`02:35` 你可以在我的GITHUB仓库中找到对应的源代码
`02:37` 仓库的地址就是这样的
`02:40` 这段代码不是很长
`02:41` 就是一段Python脚本
`02:43` 我先给大家简要说明一下这个脚本在干什么
`02:46` 最上面的app log呢是用来写日志的
`02:49` 他的这个message就是要写的日志内容
`02:52` 他会把message写入到指定的log文件中
`02:55` 这里呢则是对应了两个变量
`02:57` 其中一个叫做app是用fast API生成的
`03:01` fast API是Python的一个web开发框架
`03:04` 用于快速生成外部应用
`03:06` 我们后面会使用这个app变量
`03:08` 快速定义一个post接口
`03:10` 而这个log呢则是用了我们前面所说的app log
`03:14` 生成的
`03:14` 用于记录模型的请求和返回
`03:17` 所有的日志内容呢
`03:18` 都会记录到LLM点log文件中
`03:20` 再往后呢我们就使用app定义了一个post接口
`03:24` 路径是杠chat杠completions
`03:26` client会去请求这个路径
`03:28` 并且把具体的模型请求放入到post body里面
`03:32` 在这个接口内部
`03:34` 我们首先获取了client发给模型的请求
`03:37` 并且记录到了日志文件中之后呢
`03:40` 则都是处理模型返回的代码了
`03:42` 我们首先打了一行日志作为标记
`03:45` 后面的这一段呢看起来有点复杂
`03:47` 他写的这么复杂
`03:48` 是因为他要处理模型的流失
`03:50` 返回结果对应这里面的accept text gun
`03:54` Event stream
`03:55` 也就是要求模型使用流式返回
`03:57` 这个流式返回的专业名词叫做servicant events
`04:00` 简称SSE
`04:02` 鉴于很多人可能不太清楚SSE是什么
`04:04` 我这里简单解释一下
`04:06` 一般情况下我们使用HTTP访问一个网站的时候
`04:10` 我们的浏览器会发送给目标服务器一个请求
`04:13` 比如说要请求一个HTML网页
`04:15` 一个图片
`04:16` 一个JS脚本之类的
`04:18` 目标服务器会返回对应的结果
`04:20` 一去一回
`04:21` 一次交互就完成了
`04:23` 这个交互方式有个缺陷
`04:25` 他处理不了服务器连续发回多次响应的情况
`04:28` 比如我们经常用的大模型聊天页面
`04:31` 模型的结果呢都是几个字
`04:33` 几个字的返回只是一去一回的话
`04:35` 显然无法做到这种效果
`04:37` 所以目前主流的大模型聊天页面
`04:39` 用的都是SSE
`04:41` 它的特点是浏览器只需要请求一次
`04:44` 服务器接收到请求之后会连续发送多次响应
`04:47` 每次想要的内容都是几个字
`04:49` 而浏览器接到几个字就显示几个字
`04:52` 这样用户就可以及时接收到模型的返回
`04:55` 出来几个字就看几个字
`04:56` 体验呢就会好很多
`04:58` 等到所有的结果都显示完毕之后
`05:00` 服务器会发送一个完成的标识
`05:03` 浏览器接收到标识之后
`05:05` 关闭SSE连接页面
`05:07` 显示模型回答完毕
`05:08` 整个流程就结束了
`05:10` 知道了原理之后
`05:11` 我们再回来看代码
`05:12` 这里是请求了open ruder的接口
`05:15` 把client的请求转发给了open ruder
`05:18` 再之后呢
`05:19` 这里面的每一个line就代表服务器的一次返回
`05:22` 我们把这些返回记录下来
`05:24` 然后这里面的yield
`05:25` 就会把这个响应再发回给client
`05:27` 比如说假如模型最终的完整返回信息是纽约
`05:32` 明天的温度是24℃
`05:34` 那么这里面的yield就可能会执行六次
`05:37` 分别是YELD的纽约ELD的
`05:39` 明天的YELD的温度
`05:41` ELD的是EU的24
`05:43` 最后呢在EU的一个结束的标识符
`05:46` 说实话这里面的代码确实是有点复杂
`05:49` 而且是用了一些Python的语法糖
`05:52` 在别的编程语言里面不一定会见到
`05:55` 如果你现在还没有完全理解的话
`05:57` 我建议你先不用管
`05:58` 知道个大概意思就行
`05:59` 我们不是来学Python的
`06:01` 而且我后面会给你展示实际的SSE返回结果
`06:04` 每一条消息都有
`06:06` 那个时候呢我们会再详细解释一下代码的
`06:09` 最后呢则是启动这个本地服务器端口是8000
`06:12` 这个呢就是整个代码的结构了
`06:14` 简单来说就是先记录请求
`06:17` 再记录返回给整个过程
`06:19` 画一个流程图的话
`06:20` 就是这个样子的
`06:21` client请求
`06:22` 我们本地服务器的gun cheat杠n completions接口
`06:26` 我们记录client请求
`06:27` 把请求转发给open reader
`06:30` open rer处理请求返回结果
`06:32` 本地服务器接收到结果之后
`06:34` 把它记录到文件之中
`06:36` 再返回给client
`06:38` 当然正如前面所说
`06:40` 由于使用了s s open Ruler
`06:42` 会连续发送多个消息
`06:44` 但我们对每个消息的处理过程都是一样的
`06:47` 先发给中转服务器
`06:48` 中转服务器写入日志
`06:50` 再转发给client
`06:51` open reader的每一个返回都是如此
`06:54` 为加深你的记忆
`06:55` 我再重复一次
`06:56` 先发给中转服务器
`06:58` 中转服务器写入日志
`07:00` 然后再转发给client
`07:02` 如此循环下去
`07:03` 这样当整个对话结束的时候
`07:05` 我们就可以到日志文件里面
`07:07` 去找我们所需要的内容了
`07:09` 一共是包含两部分
`07:10` 分别是模型的请求和模型的返回
`07:13` 这个就是我们的整个流程了

### 配置中转服务器 `07:16`

`07:17` 本地服务器有了
`07:18` 下面我们要做的事情就是启动并且使用它
`07:21` 首先进入到项目目录里
`07:23` 执行Python杠MVENV
`07:25` 点VENV
`07:27` 新建一个虚拟环境
`07:28` 使用虚拟环境是为了防止我们后续安装的依赖
`07:31` 影响到系统
`07:32` 虚拟环境创建好了之后
`07:34` 我们执行source点VN v ban activate
`07:37` 启动这个虚拟环境
`07:39` 然后再执行p IP install杠r requirements
`07:42` 点TXT安装相关的依赖
`07:44` 我们的依赖非常简单
`07:46` 只有fast API u v corn和HTTPX
`07:50` 其中fast API用于定义post接口
`07:53` uv corn用于运行服务器
`07:56` HTTPX用于向open Ruler发起HTTP请求
`08:00` 安装好了依赖之后
`08:01` 我们再执行Python LLM log
`08:04` 点pi就可以启动我们的本地服务器了
`08:07` 可以看出端口是8000
`08:09` 然后呢我们来到client这里
`08:11` 点击配置API provider
`08:14` 选择OpenAI compatible base
`08:17` Ul
`08:17` 填HTTP冒号杠杠
`08:19` local host冒号8000
`08:21` 另外API key和model id与open ruder保持相同
`08:25` 因为我们会把这些信息都转发给open ruder
`08:29` 最后确保plan mode和ex mode全部都改好之后
`08:32` 我们点击save
`08:33` 然后随便打个招呼试试
`08:43` client回复完毕
`08:44` 我们打开日志文件
`08:48` 可以发现里面已经有内容了
`08:50` 而且格式跟我们想的一样
`08:51` 这个是模型的请求
`08:53` 这个呢是模型的返回日志的内容还是挺多的
`08:57` 这个对话没有用到任何的mc p server
`09:00` 或者是外部工具比较简单
`09:02` 很适合
`09:03` 我们先对模型的请求和返回的格式做一下分析
`09:06` 后面我们还会让模型用下mc p server
`09:09` 那个时候的日志内容
`09:10` 也会跟这种简单的情况差不多
`09:12` 只是会多出一些调用MCP two的地方
`09:15` 我们先把这种简单的情况弄懂
`09:17` 后面调用MCP two的链路就会好懂很多
`09:20` 那下面我们就来仔细看一下
`09:22` 这个日志文件里面到底写了些什么

### 简单场景下Cline发往模型的请求 `09:24`

`09:27` 我们首先来看一下模型请求
`09:29` 这里是我们要用的模型
`09:31` Deep seek
`09:32` 这里是消息列表里面
`09:34` 主要存放了client发给模型的请求以及模型的回答
`09:38` 这是其中的第一个消息
`09:40` 它的roll写的是system
`09:42` 对应的含义就是system prompt
`09:45` 也就是系统提示词
`09:46` 它的功能主要是设定模型
`09:48` 需要提前感知到的一些消息
`09:50` 比如说模型需要扮演的角色
`09:53` 模型可以用的工具列表
`09:54` 模型返回结果的格式等等
`09:57` 这些呢都是client写的
`09:59` 别看这里面的content只有一行
`10:01` 我把自动换行打开
`10:03` 大家就知道这有多长了
`10:05` 可以看出这个不是一般的长
`10:07` 所有的内容呢都被放到了一个字符串里面
`10:10` 看起来不是很方便
`10:12` 我来给大家提取出来
`10:14` 格式化一下
`10:24` 格式化之后是这个样子的
`10:26` 一共有626行
`10:28` 而且有些行还很长
`10:30` 再看看总字符数
`10:32` 总计是4万8670一个字符
`10:36` 我不知道大家看到这个是什么反应啊
`10:38` 反正是我第一次看到的时候都震惊了
`10:41` 这些可都是要发给模型占用token的啊
`10:44` 这都是白花花的银子
`10:46` 那这么多字到底写了些什么呢
`10:49` 它是英文的
`10:50` 看起来还是有些吃力
`10:51` 我再给大家转化成中文
`10:53` 我们先来把所有的内容都折叠起来
`10:55` 只显示小标题
`10:57` 看看这段消息的基本框架
`10:59` 小标题包括工具使用格式
`11:01` 工具列表
`11:02` 工具使用示例
`11:03` 工具使用指南
`11:05` 已连接的MCP服务器
`11:07` 然后还有两个函数
`11:08` 分别是write to file和replace infall
`11:11` 看起来分别用于写入文件和替换文件内容
`11:15` 都是很常用的功能
`11:16` 然后是client告诉模型如何选择合适的工具
`11:20` 一些自动格式化方面的注意事项
`11:23` 工作流程方面的提示
`11:24` 再就是首选语言了
`11:26` 有一点大家注意一下
`11:27` 这里所说的工具跟MCP的工具
`11:30` 并非完全是一回事
`11:31` 他这里所说的工具一共是包含两部分的内容
`11:34` 一个是client内置的工具
`11:37` 比如写入文件
`11:38` 替换文件内容
`11:39` 读取文件
`11:40` 运行终端命令等
`11:42` 还有一个呢才是MCP工具
`11:44` 比如说我们之前常用的天气预告和气象预警
`11:48` 所以模型在这里是可以用到这两种工具的
`11:51` 讲完了工具
`11:52` 我们再回来继续看一下client给出的system prompt
`11:56` 大家也都看到了
`11:57` 这里的内容非常多
`11:59` 我们逐行逐句分析是不可能的
`12:02` 我能做的就是给大家大体过一遍
`12:04` 并着重讲一下其中重要的内容
`12:07` 首先是工具的使用格式
`12:09` client规定了工具调用请求必须要使用XML格式传递
`12:13` 外层写工具的名称
`12:15` 内层写参数的名称和参数值
`12:17` 比如一个读取文件内容的工具
`12:20` 外层写的就是read ball里面的pass呢就是参数名了
`12:24` SRC杠main点JS就是参数值
`12:27` 它代表的意思就是
`12:29` 我要使用REFLOW这个工具去读取SRC杠
`12:33` MENT点JS这个文件的内容
`12:34` 有些同学可能还是不太明白
`12:36` 模型是怎么使用这个XML的
`12:39` 我们来举个具体点的例子
`12:41` 假如用户的问题是SRC杠main点JS
`12:44` 这个文件写了什么
`12:45` client会把这个问题发送给模型
`12:48` 模型接到问题后
`12:49` 发现它需要先调用REFLE这个工具
`12:52` 来读取慢点JS的文件内容
`12:54` 于是他就按照system prompts里面给出的这个XML格式
`12:58` 向client请求读取这个文件的内容
`13:01` client接到请求后读取了慢点JS的内容
`13:04` 并且返回给了模型模型
`13:06` 再往后呢就可以自己总结出答案了
`13:08` 不过那就是后面的事情了
`13:10` 流程图里也就不给大家展示了
`13:12` 所以大家也都看到了
`13:13` 只要是模型按照client规定的这种XML格式返回client
`13:17` 就可以帮助模型调用各种他想要调用的工具
`13:20` 那到底有哪些工具可以用呢
`13:23` 模型是怎么知道读取文件用的
`13:25` 工具名是REDFALL呢
`13:27` 很简单
`13:27` client在下一部分
`13:29` 也就是工具
`13:30` 这一部分会给模型详细解释有哪些工具可以用
`13:33` 它也会告诉模型每个工具的名称
`13:36` 参数格式用途等等之类的信息
`13:39` 我们来仔细看一下
`13:40` 比如execute command用于执行终端命令
`13:44` 它的参数一共是包含两部分
`13:46` 一个是命令内容
`13:47` 一个是是否需要用户同意
`13:49` 有一些命令呢可能会对系统产生影响
`13:52` 比如说是删除文件
`13:53` 安装文件等等
`13:55` 一般情况下
`13:56` 这都要经过用户的同意才能够继续
`13:58` 再后面呢就是其他的工具了
`14:00` 这里我大概过一遍
`14:02` 其中大部分工具我们在本期视频里面都用不到
`14:05` 我说一遍呢
`14:06` 是为了让你能够对client的工具用途
`14:08` 有个大致的概念和感觉
`14:10` 比如说是read fall
`14:12` 用来读取文件的内容
`14:14` Write to file
`14:15` 用来写入文件内容
`14:17` Replacing for all
`14:18` 用于替换文件内容
`14:20` Search false
`14:21` 用于搜索文件
`14:22` List false
`14:23` 用来列举当前项目目录中的文件列表呃
`14:27` List code definition names
`14:29` 用来列举指定目录顶层源代码
`14:31` 文件中使用到的定义名称
`14:33` 比如说是类函数方法等
`14:35` 这个工具呢有点绕
`14:36` 我们也不细说了
`14:37` 不重要
`14:38` 你不明白没有关系
`14:39` 还有一个是browser action
`14:42` 这个呢我们也跳过
`14:43` 我们要重点看的呢是后面的use MCP two
`14:47` 这是用来使用MCP工具的
`14:50` 这个与我们息息相关
`14:51` 所以请大家打起精神来
`14:53` 它的参数呢一共是分成三个呃
`14:56` 第一个是server name
`14:57` 也就是MCP服务器的名称
`14:59` 第二个是to name
`15:01` 是MCP工具的名称
`15:03` 第三个呢是arguments
`15:05` 是MCP工具的输入
`15:07` 比如说这个XML
`15:09` 它代表模型想要调用weather
`15:11` 这个mc p server下的get forecast工具
`15:15` 使用工具时的参数为
`15:17` Latitude41.7128
`15:20` longitude-74.006
`15:22` 在下面的access MCP resource呢
`15:26` 是用来获取MCP资源的
`15:28` 填入MCP服务器和资源的URI就行
`15:32` 我们的教程暂时不涉及MCP资源
`15:34` 所以呢这个略过呃
`15:36` 在后面的ask follow up
`15:38` question呢是模型向用户提问的时候
`15:41` 所使用的工具呃
`15:42` 也不是特别重要
`15:43` 我们也略过呃
`15:45` 再后面呢是attempt completion
`15:48` 这个我们后面会用到
`15:49` 所以请大家重点留意一下
`15:52` 这个工具用于返回最终结论
`15:54` 比如模型调用了一系列的工具之后
`15:56` 他可能会认为自己已经完成了用户给出的任务
`16:00` 或者已经知道了用户问题的答案
`16:02` 此时呢他就会把最终的结论放到attempt
`16:05` completion的result参数里面
`16:07` client接到这个XML标签之后
`16:09` 就会把result里面的结论给显示出来
`16:12` 对话到这里就结束了
`16:13` 举个具体点的例子
`16:15` 比如用户的问题是纽约明天的天气怎么样
`16:18` 模型可能接到问题之后呢
`16:20` 会先调用我们的get forecast MCP工具
`16:24` 用的就是我们前面所说的use MCP two这个标签
`16:28` client接到请求之后
`16:29` 调用了MCP server中对应的函数
`16:32` 并且给模型返回了结果
`16:34` 模型接到工具的调用结果之后
`16:37` 稍微总结了一下
`16:38` 然后把它的总结结果放到了attempt
`16:41` completion的result标签里面之后呢
`16:44` KLEIN就会把这个results里面的内容
`16:46` 原封不动的给用户查看
`16:48` 作为整个对话的总结
`16:50` 到此为止
`16:51` client的回答就算是结束了
`16:52` 那你可能还有个问题
`16:54` 如果模型使用了use MCP two之后
`16:57` 发现还是没有解决用户的问题
`16:58` 怎么办呢
`16:59` 很简单
`17:00` 模型继续使用别的工具就好了
`17:02` 这个呢可能是另外一个MCP工具
`17:05` 像是这样
`17:06` 当然呢也可以使用client的内置工具
`17:09` 比如说是读取文件
`17:11` 写入文件等等
`17:12` 模型呢可以一直这么调用下去
`17:14` 直到他认为他拿到了最终答案为止
`17:17` 到那个时候模型就可以使用attempt completion
`17:21` 返回最终的结论了
`17:22` 所以从这里也可以看出
`17:24` attempt completion基本上是标志着回答的结束
`17:27` 那看完了交互流程
`17:29` 我们再回头看一下ATTEMPLATION的参数列表呃
`17:33` 除了result之外
`17:34` attempts completion还有一个参数叫做command
`17:38` 它存储的是一条命令
`17:40` 用于展示最终的结果
`17:42` 这个参数呢是可选的
`17:44` 而且我们也用不到
`17:45` 所以暂时就忽略掉
`17:47` 不过这里面要注意一下
`17:49` KLEIN提到过
`17:50` 模型在使用attempts completion这个工具之前呢
`17:53` 必须要先使用thinking标签来思考一下
`17:56` 实际上不只是在使用attempts completion之前
`17:59` 需要thinking模型
`18:00` 做任何事情之前
`18:02` 都会使用thinking标签来思考
`18:03` 这相当于是client强行让模型思考
`18:07` 有利于提高模型角色的准确率
`18:09` 所以再回到刚才的这个图里面
`18:11` 实际上这个图并不特别准确
`18:13` 模型在使用use MCP two之前
`18:16` 会先加上thinking标签
`18:18` 把他的思考过程放到标签里面
`18:20` 而在最后使用attempt completion之前呢
`18:23` 也会使用thinking标签思考一下
`18:26` 说了这么多
`18:27` 你可能还觉得有些抽象
`18:29` 我们不如看个具体的例子
`18:30` 比如我们之前经常问的这个经典问题
`18:33` 纽约明天的天气怎么样
`18:35` 问完问题之后
`18:36` 这里呢就是thinking的内容了
`18:38` 对应的XML就是这个样子的
`18:41` 注意这两边的内容是一样的
`18:43` 模型先思考一下
`18:45` 而这里则是use MCP two的内容
`18:48` 对应的XML是这个样子的
`18:50` 注意两边的信息也是对等的
`18:52` 这个是MCP服务器
`18:54` 这个是MCP工具
`18:56` 这个则是参数值了
`19:00` 在后面的response呢则是client返回给模型的工具
`19:04` 执行结果
`19:05` 而拿到结果之后
`19:07` 模型还是照样先thinking了下
`19:09` 对应的XML就是这个样子的
`19:12` 同样这两者的内容是完全一致的
`19:15` 最后感觉自己已经得到了答案之后
`19:17` 模型就显示出了最终的总结
`19:19` 这个总结所对应的XML就是我们前面所说过的
`19:23` Attempt completion
`19:24` 注意看这两者的内容也是完全一样的
`19:27` 由于接到了attempt completion
`19:29` KLEIN就认为本次任务完成了
`19:32` 所以他就结束了对话
`19:34` 从这个流程里面
`19:35` 我们也可以看出模型返回了什么
`19:37` client其实就会显示什么
`19:39` 你在界面上所看到的大部分东西
`19:41` 都是模型返回的结果
`19:43` 在后面呢就是别的工具了
`19:45` 比如说new task
`19:46` plan mode respond之类的
`19:48` 我们都不细看了
`19:49` 不是很重要
`19:50` 然后呢这一部分呢是工具使用示例
`19:52` 是给模型看的一些例子
`19:54` 我们也直接跳过工具使用指南
`19:57` 这里面呃其实也不是特别的重要
`20:00` 我们也跳过呃
`20:02` 然后后面呢我们来到了比较重要的这一部分
`20:05` 是已连接的MCP服务器
`20:07` 他会把用户在client上配置好的MCP服务器
`20:11` 和每个MCP服务器所包含的MCP工具
`20:14` 都列举出来
`20:15` 供模型选用
`20:16` 具体来说呢就是对应了这一部分的配置内容
`20:22` 你看我这里面配置了一个MCP服务器
`20:25` 叫做weather
`20:27` 在system prompts里面呢
`20:28` 我们也能找到weather这个MCP服务器
`20:31` 之前可能有些同学不是特别清楚
`20:34` client是如何告诉模型
`20:35` 有哪些MCP服务器和MCP工具可用的
`20:39` 呃现在应该是比较清楚了
`20:40` client其实就是在这个地方告诉模型的
`20:43` 当然光是列举出MCP服务器是没有用的
`20:47` 模型
`20:47` 还需要知道每个MCP服务器下面有哪些
`20:50` MCP工具可以用这些信息呢
`20:52` CLILINE也都列举在了下面
`20:54` 比如说我们这边有两个可用的MCP工具
`20:57` 分别是叫做get alerts和get forecast呃
`21:04` 你看client把每个MCP工具的用途
`21:06` 参数和input chema都给了出来
`21:09` 其中用途和参数列表
`21:11` 用于帮助模型判断何时去调用这个MCP工具
`21:14` 而input schema呢则是告诉模型
`21:17` 调用MCP工具的时候
`21:18` 参数要以什么样的格式给出
`21:20` input schema本质上是一个JSON schema
`21:22` jason schema的具体含义
`21:24` 我们在CP终极指南的进阶篇里面已经讲过了
`21:27` 建议还没有看过的小伙伴可以看一下
`21:29` 这里就不再重复了
`21:31` 再往后的内容呢就是一些比较有用的工具
`21:34` 一些给模型看的运行指南
`21:36` 首选语言
`21:37` plan mode和act mode的区别等等
`21:39` 这些对于我们来说呢其实都不是很重要
`21:42` 这里就不再讲了
`21:44` 前面我们讲完了system prompt
`21:46` 后面呢我们来看一下用户的请求
`21:49` 也就是这一部分的内容
`21:50` 可以看到用户请求呢一共是分成了两个部分
`21:54` 第一部分呢是用户的原始问题
`21:56` 也就是我们的这个hi
`21:58` 可以看到这个high呢被放在了一个task标签里面
`22:03` 这里的task呢是在告诉模型
`22:05` 这就是你要完成的任务
`22:07` 当然严格来说啊
`22:08` 目前的这个hi并不能算作是一个任务
`22:10` 第二部分呢则是把当前的系统环境
`22:13` 告知给了模型
`22:14` 辅助模型来做决策
`22:16` 这里面的系统环境呢也是用XML来表示的
`22:19` 放在一个字符串里面
`22:21` 看着不是很清楚
`22:22` 我来给大家格式化一下
`22:31` 格式化之后是这个样子的
`22:33` 可以看到里面写了目前可见的一些文件
`22:37` 打开的tab
`22:39` 当前的时间等等
`22:41` 一系列的信息都会放在这里面
`22:43` 这些都是给模型的一些辅助信息
`22:46` 那看完了用户请求
`22:48` 我们再来看一下剩下的配置部分
`22:50` 这里面的temperature呢是来控制模型的确定性的
`22:54` 这个值越小
`22:56` 模型就更倾向于选择概率更高的词语
`22:59` 输出的结果就更加的稳定保守
`23:01` 重复性强
`23:02` 而更大的模型就更倾向于选择概率更低的词语
`23:06` 输入出的结果就越多样化
`23:08` 越富有想象力
`23:10` 但也不一定合理
`23:11` 一般情况下我们也不大用管这个配置
`23:14` stream true呢就代表流式输出了
`23:17` 也就是我们前面所说的
`23:19` 使用SSE的方式逐量返回
`23:21` 不知道大家还记不记得SSE是什么
`23:24` 我来帮助大家复习一下
`23:25` 简单来说SSE就是一次请求
`23:28` 多次响应
`23:29` 每次响应会携带一部分模型的回答
`23:32` 这里呢则是代表把使用量也返回回来
`23:36` 不重要
`23:37` 我们也不管了
`23:38` 那到这个地方请求呢就终于结束了
`23:42` 后面呢就是返回了

### 简单场景下模型发往Cline的返回 `23:45`

`23:49` 那下面我们就来看一看
`23:50` 模型到底返回了一些什么东西
`23:53` 正如前面所说呃
`23:55` 模型采用的是SS的方式逐量返回的
`23:58` 这里面的每一行都代表模型的一次返回
`24:01` 第一行的内容呢是冒号
`24:04` Open ruler processing
`24:05` 这个是什么东西呢
`24:07` 看起来好像是在告诉我们
`24:09` open Ruler正在处理我们的请求啊
`24:11` 没错就是这个意思
`24:13` 在SSE的返回结果里面
`24:15` 如果某一行以冒号开头的话
`24:18` 那就代表这一行呢是注释
`24:21` 这跟代码里面的注释呢是类似的
`24:23` 并不携带什么有效的内容
`24:25` 在SSE中注释的主要作用是保持连接
`24:29` 说的通俗一点
`24:31` 他就是在告诉调用方
`24:32` 我在处理你们的请求了
`24:34` 可能是需要点时间
`24:36` 不要急
`24:36` 不要断开连接哦
`24:38` 如果没有这几行注释
`24:40` 而等待时间又过长的话
`24:43` 我们的客户端也就是中转服务器
`24:46` 很有可能会因为超时而断开连接
`24:50` 注意啊
`24:50` 保持连接的关键是在于发送了注释
`24:54` 至于这个注释的内容到底是什么
`24:56` 其实不是很重要
`24:58` 比如说我们即使是把这里面的open Ruler processing
`25:01` 换成了什么ABC呃
`25:03` 他下一次又发了一个XYZ
`25:06` 这些呢都是可以的
`25:07` 因为注释的内容是无所谓的
`25:10` 这里面写什么都可以达到同样的效果
`25:13` 也就是保持连接几行注释之后呢
`25:17` 我们就拿到了实际的数据了
`25:20` 也就是data后面的这一段JSON呃
`25:24` 如果大家仔细看一下的话呢
`25:26` 就可以发现这些JASON的内容呢
`25:28` 基本上是一模一样的
`25:30` 比如说id都是这些provider
`25:33` 都是这些model
`25:35` 都是这些
`25:37` 那什么不一样呢
`25:38` 我们往后来翻一翻
`25:43` 好出现不一样的地方了
`25:44` 就是这个content
`25:46` content呢包含的就是模型实际返回的内容
`25:50` content的值是增量返回的意思
`25:53` 就是说每次返回的都是完整内容的一部分
`25:56` 我们把每一行的content拼起来
`25:59` 就可以发现模型实际返回的就是这么一段话了
`26:04` 所以你可以发现我们虽然只请求了服务器一次
`26:08` 但是我们拿到了这么多的响应
`26:10` 每个响应呢都携带着一部分的信息
`26:13` client接收到这些信息之后呢
`26:16` 会给用户挨个的展示出来
`26:18` 这里就是这段模型返回结果
`26:20` 所对应的client展示页面
`26:23` 最后的thinking内容呢就放到了这里面
`26:26` ask follow up question的内容呢就放到了这里
`26:30` ask follow up question呢
`26:32` 我们之前在system prompts里面见过
`26:34` 顾名思义就是模型给用户问问题用的标签
`26:38` 我们只打了个招呼
`26:40` 模型不知道我们要干什么
`26:41` 所以要问我们才行

### 调用MCP工具时模型的请求和返回 `26:44`

`26:46` 前面我们对KLEIN打了个招呼
`26:49` 并且分析了下整个链路所发生的事情
`26:51` 下面我们不妨试试让client调用一个MCP工具
`26:55` 并且看看日志里面会记录什么
`26:57` 首先重启一下我们的中转服务器
`27:03` 呃我们执行的呢还是跟之前一样的命令
`27:06` 中转服务器
`27:07` 每次在启动的时候都会自动清空日志文件
`27:10` 这样的话之前的日志就不会残留在里面了
`27:13` 方便我们分析
`27:14` 然后我们需要配置上我们之前用过的weather
`27:18` MCP服务器
`27:19` 是在这个地方配置的
`27:23` 大家可以看到我这里呢已经配置好了
`27:26` 所以呢就不再给大家重复配置了
`27:29` weather这个MCP服务器
`27:30` 在我们MCP终极指南的基础篇和进阶篇里面
`27:34` 都有提到
`27:35` 我已经重复过很多次了
`27:37` 这里呢也就不再给大家赘述了
`27:39` 这就是官方的一个MCP服务器
`27:41` 我们用它来获取某个地区的天气预报
`27:44` 紧接着我们新起一个对话
`27:46` 然后在这里面问一下我们的经典问题
`27:49` 纽约明天的天气怎么样
`27:58` 跟之前一样
`27:59` KLEIN找到了我们的MCP server
`28:01` 并且发现了其中的get forecast工具
`28:03` 是可以解决这个问题的
`28:05` 我们同意调用
`28:10` 工具执行完毕
`28:11` 模型先是思考了一段话
`28:13` 然后给出了最终的答案
`28:15` 到这里
`28:16` 整个任务就算是结束了
`28:17` 随后呢我们就来到日志文件里面
`28:25` 一开始的请求呢除了这个问题之外
`28:29` 别的都跟我们之前分析的链路是一模一样的
`28:33` 由于我们之前已经分析过了
`28:35` 所以我这里面也就不再重复分析模型请求了
`28:39` 我们重点来看一下模型的返回
`28:42` 模型的返回呢一开始是一段注释
`28:45` 就是这里面的冒号open Ruler processing呃
`28:49` 这跟我们之前分析的链路呢也是一样的
`28:52` 这段注释呢
`28:53` 就是为了保持模型和我们服务器之间的连接
`28:55` 防止超时断开
`28:57` 注释本身并不包含什么有效的信息
`28:59` 后面的这些data呢才是有效的信息
`29:03` 那我们来仔细看一下
`29:04` data里面到底写了些什么东西
`29:06` 当然啊我们之前也说过
`29:08` data的前面这一部分呢
`29:10` 其实也是没什么太大用处的
`29:12` 因为每一条消息都是一样的
`29:14` 我们要重点看的是每一条消息里面的content
`29:18` 它的值呢是增量返回的
`29:20` 每一条消息都不一样
`29:22` 我们把每一个消息的值都拼接在一起
`29:25` 就可以知道模型到底返回了些什么东西
`29:27` 可以看出他一开始呢是给出了两个空字符串
`29:31` 没有什么有效的内容
`29:32` 我们可以忽略前两条消息
`29:34` 后面紧接着一个thinking的标签
`29:37` 表示模型马上就要开始思考了
`29:39` 那thinking标签之后呢
`29:41` 就是具体的思考内容了
`29:43` 这么看很不方便
`29:44` 我来帮大家把这几条消息都给拼接起来
`29:47` 让大家看一下完整的模型返回是什么样子的
`29:50` 可以看出模型的回答呢一共是分为了两个部分
`29:54` 第一个部分是thinking
`29:55` 也就是模型思考
`29:57` 这个跟之前一样
`29:58` 模型做任何事情之前
`30:00` 都要在thinking标签里面思考一番
`30:02` 这个是KLEIN在system prompts里面要求的
`30:05` 我们在讲system prompt的时候也提过这点
`30:08` 第二部分则是
`30:09` 模型决定调用我们的get forecast工具
`30:12` 来获取纽约未来几天的天气
`30:14` 这里面的标签呢是use m cp two
`30:17` 我们之前在讲client的system prompt的时候
`30:21` 讲过这个标签
`30:22` 还说过这个标签很重要
`30:23` 大家应该都还记得吧
`30:25` 这段XML的意思就是说
`30:27` 他想要调用weather
`30:28` 这个MCP服务器下面的get forecast工具
`30:31` 参数呢是这个CLILINE用指定的参数
`30:35` 调用了指定的MCP服务器之后
`30:37` 会再次对模型发起请求
`30:40` 这个呢就是第二次请求的内容了
`30:42` 前面的部分呢是跟第一次请求一样
`30:45` 有system prompt用户的问题和环境信息等内容
`30:50` 因为模型是没有任何记忆的
`30:52` 所以我们必须把前面发生过的事情也告诉模型
`30:56` 这样模型才能够更好的做出判断
`30:59` 后面这段内容的roll呢设置为了assistant
`31:03` role是角色
`31:04` assistant就是助手
`31:06` 其实也就是模型的意思
`31:07` roll设置成了assistant
`31:09` 代表的意思就是说这段消息的提供者是模型
`31:12` 这里面的内容都被放在了一个字符串里面
`31:15` 换行符呢也被换成了杠N看起来很不方便
`31:19` 我来给大家把这段文本抽取出来
`31:21` 放到一个文件里面
`31:22` 并做一下格式化
`31:24` 这样看得更清楚一些
`31:35` 这段内容是不是很熟悉呢
`31:37` 模型先思考了下如何获取纽约的天气
`31:40` 然后就请求调用MCP工具
`31:42` 这段我们之前好像见过呀
`31:44` 对
`31:45` 就是之前模型流式返回结果中的
`31:47` constant的完整内容一模一样
`31:50` 别看这段内容之前是模型返回的
`31:53` 如果你不把这段内容再次发给模型的话
`31:55` 模型可记不清楚之前到底发生了些什么
`31:58` 因为模型是没有任何记忆的
`32:00` 再往后的这个消息呢
`32:02` roll是user
`32:03` 代表消息是用户提供的里面的内容呢
`32:06` 分为三个部分
`32:07` 第一个表示后面将会展示MCP工具的调用结果
`32:12` 第二个则是结果的具体内容
`32:16` 我把里面的杠N换成换行符
`32:18` 给大家看一下是什么样子的
`32:36` 可以看出这一段的返回与client
`32:38` 回答中的这一部分的内容是一模一样的
`32:44` 第三部分就是当前的环境信息了
`32:47` 比如说打开的tab
`32:48` 当前时间之类的
`32:50` 这些呢我们之前也讲过
`32:52` 这里呢就不再重复了
`32:53` 那总结一下client1开始是发送了system prompt
`32:57` 然后发送了用户的问题
`33:01` 紧接着发送了模型的工具调用请求
`33:03` 最后发送了工具的调用结果
`33:06` 其实啊只有工具的调用结果是新的消息
`33:09` 前面的都是历史消息
`33:11` 这些历史消息呢
`33:12` 就是为了告诉模型之前发生过什么事情
`33:15` 知道了请求内容
`33:16` 我们就紧接着看一下模型到底返回了些什么
`33:19` 模型接到了调用的结果之后
`33:21` 依然是做了流式的返回呃
`33:24` 跟之前一样
`33:25` 前面的这一部分呢我们都不看
`33:27` 我们要看的呢就是这里面content的内容
`33:30` 当然啊现在是流式返回
`33:32` 我们这么看呢不是很方便
`33:34` 所以呢我把它的结果放到了这个文件里面
`33:38` 这样大家看的会更清楚一些
`33:40` 可以看到整个结果呢依然是分成了两个部分
`33:44` 第一个部分还是thinking模型呢
`33:47` 还是在thinking标签里面思考了一番
`33:49` 然后在认为自己已经拿到了答案之后
`33:52` 在attempt completion标签里面返回了答案
`33:55` cliline in接到了模型的返回之后呢
`33:58` 是在这个地方显示了thinking的内容
`34:01` 在这个地方显示了attempt completion的内容
`34:05` 因为接到了attempt completion
`34:07` 所以呢对话就到此结束了

### Cline 的 XML 协议与 ReAct 的关系 `34:11`

`34:13` 我们再来画个流程图给大家复习一下整个流程
`34:17` 首先我们问了一个问题
`34:19` 纽约明天的天气怎么样
`34:20` 模型返回的答案其中包含两个XML标签
`34:24` 一个是thinking代表模型先思考了一番
`34:27` 另外一个是use MCP two
`34:30` 表示模型想要使用weather
`34:31` 这个MCP服务器下面的get forecast工具
`34:34` client根据这个请求调用了对应的工具之后
`34:37` 把工具的结果返回给了模型
`34:39` 模型接到结果之后做了第二次回复
`34:42` 这个回复同样是包含两个XML标签
`34:45` 其中第一个还是thinking
`34:47` 思考过后呢
`34:48` 模型认为自己已经得到了答案
`34:50` 因此就把最终的结论放到了attempt completion
`34:53` 里面
`34:53` 返回给了climb
`34:54` 整个过程呢就到此结束了
`34:56` 假如说我们把问题换一下
`34:58` 后面加上一句
`35:00` 把结果写入到results点MD文件中
`35:02` 此时会发生什么呢
`35:04` 前面这部分都一样
`35:06` 模型在拿到MCP工具调用结果之后
`35:09` 也会照例使用thinking去思考一番
`35:12` 但是思考过后他会发现任务还没有完成
`35:14` 因为结果还没有写入到文件中
`35:17` 所以呢此时他不会着急使用attempt completion
`35:20` 总结
`35:21` 他会再次请求调用工具
`35:23` 只不过这次调用的工具是client的内置工具
`35:26` Right to file
`35:27` 这个工具用于写入文件内容
`35:29` 它会把写入的文件路径和内容
`35:31` 放到这个工具的参数里面
`35:33` client接到这个工具的调用请求之后
`35:36` 会向指定的文件中写入指定内容
`35:38` 写完后通知模型写入成功
`35:41` 这个时候模型才会返回thinking和attempt completion
`35:44` 告诉client任务执行完成了
`35:46` 实际上如果我们仔细看一下
`35:49` 就会发现在每一轮的工具调用过程中
`35:51` CLILINE与模型的交互是有固定的模式的
`35:54` 首先是thinking
`35:55` 也就是思考
`35:56` 然后是获取或改变外部环境
`35:59` 比如说是获取天气信息
`36:01` 写入文件
`36:02` 读取文件之类的
`36:03` 我们暂时给这一步起个名字叫做行动模型
`36:07` 拿到行动的结果之后
`36:08` 对这个结果做了观察之后
`36:11` 他又重复了一遍这个流程
`36:12` 思考行动观察
`36:15` 在最后一轮的思考之后
`36:17` 模型会发现它已经完成任务了
`36:19` 他就会给出最终答案
`36:21` 思考行动观察
`36:24` 这三个中文词对应的英文名分别是thought action
`36:27` Observation
`36:28` 他们可是react思想中最重要的三个词
`36:31` 你没准儿会问什么是react
`36:33` react呢
`36:34` 是2022年的一篇论文所提出来的一个词
`36:37` 它是reasoning和acting两个单词的合体
`36:41` 这篇论文在当时可谓是名极一时
`36:43` 因为他所提出的这个react理念
`36:46` 可以在不需要人干预的情况下
`36:48` 让模型自主思考
`36:49` 自主调用各类外部工具
`36:51` 从而完成用户的诉求
`36:53` 就像我们的client一样
`36:55` 说白了呢
`36:56` 这就是一个agents
`36:57` 什么是agent呢
`36:58` 简单来说agent就是一种能持续思考
`37:01` 持续调用外部工具
`37:02` 直至解决用户问题的一个程序
`37:05` 所以呢从某种意义上来说
`37:07` climb就是一个agent
`37:09` 而且本质上克拉也是根据这个react的思想
`37:12` 来构建它的agent的流程的
`37:14` 那意思就是说
`37:15` react用的也是client这种XML格式来与模型沟通的咯
`37:19` 非也非也
`37:21` 大家要明白
`37:22` XML只是数据传输的一个格式而已
`37:25` react呢是个思想
`37:26` 它并不跟某个格式绑定
`37:29` react的精髓在于我们前面所提到的thought action
`37:32` observation这三个词
`37:34` 实际上在这篇论文中
`37:36` 作者并没有使用XML
`37:38` 也没有使用JASON
`37:39` 它使用了近乎于纯文本的方式
`37:42` 实现了类似client的效果
`37:43` 我们还是拿出之前client与模型交互的流程图
`37:47` 那假如说我们希望client
`37:48` 使用react论文中提到的格式
`37:50` 而不是我们之前研究过的那种XML格式
`37:54` 这个流程图该怎么改呢
`37:55` 我们来一个一个看
`37:57` 首先看模型的第一个回答
`37:59` 它原来返回的不是thinking标签吗
`38:02` 现在要换成thought冒号
`38:04` 后面接具体的思考内容
`38:06` 紧接着下一步不是use MCB two吗
`38:09` 那我们需要换成action冒号
`38:12` 后面接需要调用的工具名称和参数
`38:15` 工具调用完毕之后
`38:17` 返回的结果前面也要加上observation冒号
`38:20` 然后呢后面的流程也是照样改掉
`38:23` thinking改成thought
`38:25` 调用工具时使用action
`38:26` 我们假设right to ball
`38:28` 工具的执行结果是一个字符串
`38:30` 字符串的内容是写入成功
`38:32` 那这一行的内容呢就应该是observation写入成功
`38:36` 在接到成功的提示之后
`38:38` 模型会再次使用thought进行思考
`38:41` 最终用一个特殊的标记
`38:42` final answer来给出最终的总结
`38:45` 并结束此次对话
`38:47` 当然这里还要提一点
`38:49` 想要模型这么返回的话
`38:51` 我们也要给模型一个配套的system prompt
`38:54` 还记得我们之前看过的client的那个system prompt吗
`38:58` 那个system prompts告诉了模型具体的返回格式
`39:01` 工具调用示例
`39:02` 可用工具列表等等信息
`39:04` 如果要改成thought action observation这种格式的话
`39:08` 我们也需要有一个类似的system prompt
`39:10` 比如说是这个
`39:11` 这里面呢是说明了返回格式
`39:16` 这里面呢是给出了工具调用示例
`39:20` 这里面呢给出了注意事项
`39:22` 这里面呢给出了工具列表之后呢
`39:25` 模型就会按照指定的格式返回了
`39:28` 当然由于climb并没有给出
`39:30` 修改system prompt的地方
`39:31` 它的格式呢是写在代码里面的
`39:34` 所以我们实际上其实是没有办法让克莱
`39:36` 按照这种格式与模型进行沟通的
`39:39` 这里面呢只是给大家一个示例而已
`39:41` 不过话说回来
`39:43` 这种事例并不是没有任何意义的
`39:45` 比如说假设我们自己想写一个agent
`39:48` 我们是完全可以使用这种thought action
`39:51` observation的格式的
`39:52` 实际上我现在就可以用一个开源的模型
`39:55` 给你举个例子
`39:56` 首先我用欧拉马启动google的一个模型JA3
`39:59` 然后我们找到之前演示过的那个
`40:02` react的system prompt
`40:03` 在它的最后加上模型要解决的问题
`40:06` 然后把整个内容作为模型输入
`40:09` 直接给到模型
`40:10` 大家注意
`40:11` 其实system prompt和用户问题要分开给模型的
`40:14` 不过欧拉马设置system prompt有点麻烦
`40:17` 演示过多会增加这个视频的复杂度
`40:20` 不利于理解
`40:21` 所以我就把system prompt和用户问题合在一起
`40:24` 直接给到模型了
`40:26` 效果其实差不多的
`40:27` 我们回车看看模型会返回什么
`40:33` 可以看到模型返回了两部分的内容
`40:35` 一个是thought
`40:36` 一个是action
`40:38` 符合我们的要求之后呢
`40:40` 按照正常流程
`40:41` 我们应该有段代码去解析action中的内容
`40:43` 将里面的工具和参数提取出来
`40:46` 然后调用这个工具
`40:48` 就像client接到模型返回的
`40:50` use MCP two的时候那样做
`40:52` 不过因为我们现在是人工测试
`40:54` 并没有什么代码
`40:56` 所以我们这里直接假设
`40:57` 调换工具的事情已经做完了
`40:59` 并且拿到了工具的结果
`41:01` 我们把工具结果的前面加上observation
`41:04` 一起返回给模型
`41:05` 模型
`41:05` 拿到工具结果之后再次返回了salt和action
`41:09` 要求使用right to fall工具写入文件
`41:11` 照例我们假设写入已经完成了
`41:14` 返回给模型写入完成的消息
`41:16` 最后模型还是先思考一下
`41:18` 发现任务已经完成之后
`41:20` 就输出了final answer
`41:21` 整个流程就结束了
`41:23` 从这个流程可以看出
`41:24` thought action observation这个格式可以用来编写agent
`41:28` 只是你要注意了
`41:30` 我并不建议你使用这种格式
`41:32` 因为这种格式远远没有XML那种精确
`41:34` 当然我们前面所演示的那个交互流程
`41:37` 也可以用XML来浮现
`41:39` 我们用的GEMA3模型大小是4B的
`41:41` 难以产出符合格式的XML
`41:44` 因此这次呢我们就用deep seek来举例
`41:46` 先找到之前演示过的client的system prompt
`41:50` 最后加上用户的问题
`41:51` 纽约明天的天气怎么样
`41:53` 结果写入到results
`41:55` 点MD文件中
`41:57` 然后我们把整个文件的内容复制粘贴给deep sick
`42:01` deep seek返回了两部分的内容
`42:03` 一个是thinking
`42:04` 一个是use MCP two
`42:06` 跟我们在client那里见到的一模一样
`42:09` 如果是client的话
`42:10` 他就会去调用get forecast这个工具了
`42:13` 因为我们在模拟运行
`42:14` 所以呢我们直接假设工具调用完成了
`42:17` 返回结果给模型告诉他纽约明天的天气
`42:21` 模型接到工具调用结果之后
`42:23` 返回了thinking和write to fall两个标签
`42:25` 要求写入文件
`42:27` 同样我们模拟写入成功的消息
`42:33` 模型最后返回了attempt completion
`42:36` 整个流程就结束了
`42:37` 注意attempt completion之前呢应该还有个thinking
`42:41` Deep sick
`42:42` 失误了
`42:42` 没有把那一部分返回给我们
`42:44` 这点你知道就好了
`42:46` 所以大家也都看到了
`42:47` XML呢与我们之前所讲的那种source action observation
`42:51` 本质上都是一样的
`42:52` 都遵循react的模式
`42:54` 而且模型都能理解
`42:56` 只是从逻辑上来讲
`42:57` XML的效果更好
`42:59` 因为它的表达更为精确
`43:01` 但这也不代表大家就一定要使用XML来返回数据
`43:05` 实际上呢你可以使用任何格式
`43:08` 比如JASON就是一个很好的选择
`43:10` 只要这个格式本质上用的是react的模式
`43:13` 那都是可以的
`43:15` 所以做一个类似client这样的agent需要什么呢
`43:19` 首先我们需要告诉模型
`43:20` 返回结果的格式
`43:22` 是用我们前面提到的thought action observation
`43:25` 这样的格式
`43:26` 还是用KLEIN这样的XML格式
`43:28` 还是说使用JASON呢哪一个都行
`43:31` 但一定要明确告知模型
`43:33` 其次我们还需要告诉模型可用的工具列表
`43:36` 最后呢我们还需要告诉模型
`43:38` 一定要使用react模式
`43:41` 也就是说每次回答的时候呢
`43:43` 都要先思考一下
`43:44` 思考之后呢
`43:45` 再接上一个工具
`43:46` 这样请求或者是最终答案
`43:49` 我们把这些要求写入到system prompt里面
`43:52` 再加上一个用户问题作为触发
`43:54` 整个agent就可以跑起来了
`43:58` 今天的视频呢就到此结束了
`44:00` 别忘了点赞关注我们
`44:02` 下次再见拜拜
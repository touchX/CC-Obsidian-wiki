---
title: "Claude Code Agent Teams上手指南+项目实测"
url: "https://www.bilibili.com/video/BV1gwcAzkEhw?spm_id_from=333.788.recommend_more_video.4&trackid=web_related_0.router-related-2479604-t4rlm.1778458601960.726&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1gwcAzkEhw"
cid: "35921465016"
author: "程序员阿江-Relakkes"
upload_date: "2026-02-08"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116037064331269&bvid=BV1gwcAzkEhw&cid=35921465016&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

用Claude Code干复杂任务总碰到三个问题：

上下文越来越长开始遗忘、任务只能串行效率低、单Agent视角单一容易漏检。

Claude官方发布的Agent Teams功能正好解决这些痛点

一个Team Lead拆任务，多个Teammate并行执行，还能互相通信协调。

本期视频从核心概念、使用场景、底层架构到真实项目实战，带你完整搞懂Agent Teams的正确打开方式。

## 章节

- `00:00` 三大痛点
- `00:30` 它是什么
- `01:17` 最佳使用场景
- `01:52` 与SubAgent的区别
- `02:45` 如何启用
- `03:01` 触发团队协作示例
- `04:12` 架构与通信机制
- `07:04` 真实项目实战演示
- `09:59` 总结

## 字幕

### 三大痛点 `00:00`

`00:00` 大家用CLCODE干复杂的任务
`00:01` 你大概率有碰到过这三个问题
`00:03` 第一个这个上下文越聊越长
`00:05` 到后面他就开始忘记前面改了什么东西
`00:07` 第二个的话任务明明能并行
`00:09` 他却只能一个一个的串行去啃
`00:11` 那第三个的话
`00:12` 一个agent的只有一个视角看安全
`00:14` 他就顾不上性能
`00:16` 其实这不是problem
`00:17` 写的不够好
`00:17` 这就是单A镜它的一个结构性的瓶颈
`00:20` 那最近cloud官方发布了agent的teams
`00:23` 今天我们就通过这个视频来把它讲透
`00:25` 最后还通过一个真实的项目
`00:27` 带着大家去一步步看
`00:28` 这个agent teams到底是怎么协作的

### 它是什么 `00:30`

`00:30` 那这个A呢到底是什么呢
`00:31` 很简单
`00:32` 他就是可以允许多个cloud code这个实力去协同工作
`00:35` 有一个团队的管理者
`00:37` 然后去派发多个团队成员去干活啊
`00:40` 他们团队成员之间还可以互相去通信
`00:42` 并且这个团队老大也可以
`00:44` 实时的去跟这些成员去通信
`00:46` 那下面有一个这个图哈
`00:48` 就是你的用户嘛
`00:49` 就是我们嘛
`00:49` 然后你发了一个任务
`00:51` 那这个team leader就去猜
`00:53` 拆完了之后他就开始聚啊
`00:55` 就给了这个成员
`00:56` 让他去执行后端
`00:57` 让这一个团队成员去执行前端
`00:59` 让这个去做QA啊
`01:00` 然后呢他们共享这一个任务
`01:02` 就是他们会都会从这个任务列表里面
`01:04` 去接受任务
`01:05` 然后去更新任务的状态
`01:07` 然后回填
`01:08` 那他们团队成员之间也可以通过这一个去通信
`01:11` 那这样就打通了从团队到成员互相通信
`01:15` 成员与成员之间也可以互相通信

### 最佳使用场景 `01:17`

`01:17` 它的最佳的使用场景
`01:19` 第一个是研究与审查
`01:20` 第二个是一些新模块的开发
`01:22` 第三个是一些竞争性的假说
`01:24` 也就是说你发现一个bug啊
`01:26` 你开多个成员让他们互相讨论啊
`01:28` 我反对你
`01:29` 你反对
`01:29` 最终我们去老爆出来一个真正可能的一个答案
`01:33` 第四个的话是一个跨层协调
`01:34` 也就是说我们正常的一个开发流程中
`01:37` 前后端其实你是可以并行去做了嘛
`01:40` 因为当我们有架构完了之后
`01:41` 有设计文档
`01:42` 有一些什么API文档
`01:43` 有数据库的文档
`01:44` 这个时候前后端就可以干活了
`01:46` 那同时前后端干完之后
`01:47` 这个时候测试就可以跟上嘛
`01:48` 测试完了之后
`01:49` 那就可以去做后续的一些事情
`01:51` 下面我们来看一下这个agent

### 与SubAgent的区别 `01:52`

`01:53` teams跟sub agent的区别哈
`01:54` 这个核心的差异对比我都放在这块
`01:56` 大家可以截图看一下
`01:57` 我就讲两个核心的
`01:58` 第一个就是通信的方式
`02:00` sub a镜呢只能向主人镜的报告结果
`02:03` 那这个的话是这个队员他是可以互相去通信
`02:06` 发消息的
`02:07` 有一个选择的一个规则的话
`02:09` 就是如果需要去快速专注的工作者来完成任务
`02:12` 并报告结果的话
`02:13` 选SAA型的
`02:14` 如果你需要去分享
`02:15` 发现需要去互相质疑并自主协调
`02:18` 你就选agent的teams好
`02:19` 下面我们来做两个选择题
`02:21` 第一个大家可以暂停一下
`02:22` 看你选什么
`02:23` 那我来选一下啊
`02:24` 就是如果你在五个目录中
`02:26` 需要去搜索特定模式的文件
`02:28` 你应该选什么
`02:29` 这种就应该选sub agent啊
`02:30` 因为这是典型的一个SAMA型的场景
`02:33` 你不需要跨绘画去协调
`02:34` 那第二个你要做一个pr审查
`02:36` 就是你提的代码嘛
`02:37` 让一个人帮你去审代码
`02:39` 你希望有多个审查者互相质疑对方的发现
`02:41` 这种就要选agent teams
`02:43` OK那么点下一步启用的话也很简单

### 如何启用 `02:45`

`02:46` 大家去到这个文件里面去编辑一下
`02:49` 加一个这个配置
`02:50` 就是开启这个agent teams的一个实验性质
`02:52` 或者是你可以通过一个临时的变量开启
`02:55` 开启完了之后
`02:56` 把cloud code这个终端重启一下就OK了
`02:58` 好我们下面来看一下如何去触发

### 触发团队协作示例 `03:01`

`03:01` 让他去创建一个团队
`03:02` 并且团队成员之间互相去并行去干活
`03:06` 很简单
`03:07` 官网给了一些示例
`03:08` 我们来以这种代码审查的吧
`03:09` 就是我们有一个代码仓库
`03:11` 它有一些pr
`03:12` 然后我们让他去审核某一个好
`03:14` 我们给了一个提示
`03:15` 就是让他去创建A级的团队
`03:16` 去审核我们这一个的代码的提交
`03:18` 让他有三个角色
`03:19` 一个是专注安全
`03:20` 一个是检查性能
`03:21` 第三个是去做测试覆盖率
`03:24` Ok
`03:24` 可以看到这个时候他已经创建了一个审查团队
`03:27` 启动了三个A镜头
`03:28` 并且是并行去做的
`03:29` 然后我们可以进到某一个A进程里面
`03:32` 去看一下他的一个情况
`03:33` 可以看到其实它本质上就是一个cloud的实例嘛
`03:36` 然后给他一段提示词
`03:38` 完事之后这一个他的老大给他发了一个消息
`03:41` 让他去审查pr
`03:42` 然后他就开始去做这个事情了
`03:44` 同时你也可以换到另外一个安全的
`03:47` 这个审查A技能里面点一下
`03:49` 它其实也是跟刚刚那个一样的
`03:51` 就是并行
`03:52` 就把这A键的并行就这么简单
`03:53` 然后他们之间可以通信
`03:55` 这两个三个之间可以通信
`03:56` 然后他的老大跟他们之间也可以互相通信
`03:58` 就这么简单
`03:59` OK我们终止一下
`04:01` 好我们回来
`04:02` 刚刚只是演示了一个并行代码
`04:04` 去做审查的一个示例
`04:05` 其实你也可以去指定创建你的那个团队里面
`04:08` 成员有哪个
`04:09` 每个team他也可以指定一些单独的模型
`04:11` 好下面我们来看一下它整个的一个架构与通信

### 架构与通信机制 `04:12`

`04:15` 在最开始我们其实可以看到他有一个领导嘛
`04:17` 有个领导他会去创建整个团队的一个主要绘画
`04:21` 负责去派生出来各种团队的成员
`04:23` 就是各种agent的嘛
`04:24` 那这个时候每一个成员的话
`04:26` 他是一个独立的cloud code实例
`04:28` 他可以收到一段提示词
`04:31` 然后由这一个领导给他发的一条消息
`04:35` 那下面就是这一个任务列表
`04:37` 这个很关键
`04:37` 这一个是由领导者去创建任务了
`04:40` 然后这个任务之间的一些依赖关系
`04:42` 都由他来去指定
`04:44` 指定完了之后
`04:45` 成员之间就是我们的agent的嘛
`04:47` 他自己去领任务
`04:48` 领完之后去更新状态执行
`04:50` 然后去把消息回传给他
`04:52` 那这个过程中势必会有一些任务
`04:55` 他是需要有一个先后顺序的
`04:57` 那这一块全部由领导去分配好了
`04:59` 他去挨个执行就行了
`05:01` 如果他发现他要依赖任务A那就他就在阻塞等
`05:04` 等到任务A完成了之后
`05:06` 他就知道该他他就做这个事情了
`05:08` 还有的话他们还有一个消息系统
`05:10` 这个消息系统是用于agent之间直接通信的
`05:13` 也就是说两个agent的之间
`05:14` 它可以直接通过这一个组件来通信
`05:17` 那下面的话是它的一个存储的结构
`05:19` 它的团队配置任务信息全是放在本地的
`05:23` 就是本地的文件嘛
`05:24` 也是在这个点cloud文件下面的
`05:26` 我们下面来看一下整个的一个真实的配置
`05:28` 长什么样子哈
`05:29` 我们点开可以看到
`05:31` 我这里有一个啊UI重构的一个团队嘛
`05:34` 就是我给了他一段提示
`05:35` 让他帮我们去重构UI
`05:36` 那重构UI的话
`05:37` 你会涉及到他把主要的色调确定完了之后
`05:40` 其实你要改N个文件
`05:41` 完全是可以并行
`05:42` 一个A技能
`05:42` 他负责123个文件
`05:44` 那第二个A技能他负责456
`05:46` 那这样子的话他就可以通过组的一个agent呢
`05:49` 他可以派生出来更多的成员去做这个事情
`05:51` 那这块他就派生出来了呃
`05:54` 四个成员
`05:54` 第一个成员是他自己吗
`05:55` 他是一个领导性质的
`05:57` 那那紧接着其他的就是一些他的团团队成员嘛
`06:00` 然后加入的时间是什么
`06:01` 他的A进的id是什么
`06:02` 名字是什么
`06:03` 它的type类型是什么
`06:04` 你要用的模型这块都在你这一
`06:07` 这一切都在你说的那一句提示时之后
`06:09` 他默认在背后去帮你做的事情
`06:11` 看这个数据结构其实看起来也不是特别复杂
`06:13` 是不是OK好
`06:15` 那上下文跟通信的话
`06:17` 其实前面也讲了
`06:17` 就是每一个agent的实力嘛
`06:19` 他有他自己的一个独立上下文
`06:21` 并且他在启动的时候
`06:22` 他会收到他的老大给他派发的一个提示词
`06:25` 他根据这个提示词去干活啊
`06:28` 那权限这块的话
`06:29` 就是你的成员是继承你领导的一个权限嘛
`06:32` 如果你给他了一个最高权限
`06:33` 那你的团队成员也可以继承这个权限
`06:36` 好当领导创建完任务之后
`06:38` 任务其实有三种状态
`06:39` 一种是待处理
`06:41` 第二种是进行中
`06:42` 第三种是已完成
`06:43` 团队成员按自己的需要去领任务
`06:46` 然后去完成完成完了之后把消息通知给领导
`06:50` 完事了
`06:51` 那整个过程是通过一个文件所的机制
`06:53` 去防止多个成员去共同认领同一个任务
`06:57` O
`06:58` 那待会我会给大家看一下
`06:59` 一个做真实项目去开发功能的一个agent a team
`07:02` 协作的一个流程

### 真实项目实战演示 `07:04`

`07:04` 好我们现在开始
`07:05` 首先确保你的环境变量
`07:06` 配置了agent teams的实验功能
`07:08` 第二个要保证你的cloud code版本是最新的
`07:11` 我们把提示词给到他这个提示层里面的话
`07:13` 重要的一句话就是
`07:14` 这个你要创建一个五个人的agent的teams
`07:16` 它会自动去帮你创建
`07:17` 然后你只需要告诉他
`07:19` 你每一个成员就是你的团队成员嘛
`07:20` 有架构设计
`07:21` 有后端啊
`07:23` 有前端
`07:23` 有QA
`07:23` 有review
`07:24` 它会自动去创建团队
`07:25` 会给每一个agent的去分配一个名字
`07:27` 以及他所需要完成的任务
`07:29` 执行顺序的话
`07:29` 肯定是架构先设计嘛
`07:31` 设计完了之后
`07:31` 后端跟前端可以去做并行开发
`07:33` 最后再由QA去测试
`07:35` 最后再旅游代码
`07:36` 整个过程其实就跟我们传统做软件开发的流程
`07:38` 是一样的
`07:39` 好我们看到他这边给我们创建了团队和任务
`07:42` 先创建任务
`07:43` 再去设置好依赖关系
`07:45` 因为肯定是设计先行嘛
`07:46` 你得设计完了之
`07:47` 后
`07:47` 端跟前端才能去并行执行任务嘛
`07:49` 这时候他就开始执行一个架构的agent
`07:52` 去执行我们的第一个任务
`07:53` 可以看到这下边有个这下面哈
`07:54` 他这个是我们的主agent程嘛
`07:55` 下面是他的那个团队里面的成员
`07:57` 我们其实可以点进去看一下他在干嘛
`07:59` 这时候你可以从上面可以看到哈
`08:01` 因为这个这个就是一个cloud code的实例
`08:03` 这个时候有它的一个提示词
`08:05` 你的任务项目背景关键的文件
`08:07` 那他就开始去做了啊
`08:09` 这里从这里也可以看到
`08:10` 他其实是新开了一个那个CROCODE
`08:13` 好们回去后续的话
`08:16` 我们会在这个位置可以看到有多个进来
`08:19` 就是会有一个后端前端的进来
`08:21` OK从这块可以看到
`08:22` 它在这个地方启动了后端跟前端的A镜头
`08:25` 这个时候这两个在这下面就开始去单独的一个
`08:28` cloud code10里面去完成任务了
`08:30` 他会接收到他的老大派给他的一些任务
`08:33` 会交代他的一些背景提示词是什么
`08:35` 他要完成的任务是什么
`08:36` 我们点进去看一下
`08:38` 可以看到也是跟刚刚那个一样的
`08:40` 就是你要去做什么
`08:41` 你是这个团队的后端开发者
`08:43` 你的任务是干嘛的
`08:44` 你的核心要求是什么
`08:46` 实现步骤大概给了你一些
`08:47` 你就去干活吧
`08:48` 干完活之后你再向我汇报
`08:50` 就是项目经理那一套
`08:51` 项目经理去把把任务拆分完成之后
`08:54` 后端去领任务
`08:54` 前端去领任务
`08:55` 领完之后开发完了之后
`08:57` 最后再交给QAQA
`08:58` 测试完了之后
`08:59` 你再去做一些代码的审查
`09:01` 就是这一套流程
`09:04` 那前段其实也一样的
`09:05` 你知道吗
`09:09` 可以看到后端
`09:10` 现在我们可以看到后端跟前端都弄完了
`09:13` 弄完这个时候他就可以走到科A阶段了
`09:15` 所有都是由这个主A进的
`09:16` 去协调他的团队成员
`09:17` 后端前端q a review去完成任务
`09:19` 这QA他收到的任务就是一段
`09:21` 让他去做测试的嘛
`09:23` 包括单元测试
`09:24` 集成测试
`09:24` OK这个QA的agent他已经干完活了啊
`09:27` 他说结果非常好
`09:28` 跑完了他并且把他的任务标记完成了
`09:30` 这个时候我们的这个领导啊
`09:32` 老大接收到这一个他的队员哈
`09:34` 把这个任务完成了之后
`09:35` 他就下面开始安排另外一个小工去干活了
`09:37` 就这个代码review
`09:38` 马上下面就会创建一个review agent
`09:40` 特我也可以看到这里面已经开始干活了
`09:42` 也是一样的
`09:42` 开启了一个cloud code的实例
`09:45` 整体完成度的话80分吧
`09:47` 因为它中间会有一些bug
`09:49` 最开始不是一次可用的
`09:50` 有一个有两个前端的bug
`09:52` 有一个后端的bug是数据库那边相关的
`09:54` 就我们测试这个任务总共花了30000000token
`09:58` 大家觉得怎么样

### 总结 `09:59`

`09:59` OK这就是今天所有的视频内容了
`10:01` 其实核心就这一个图
`10:02` 如果你觉得这个视频做的不错的
`10:04` 可以给我一键三连
`10:05` 大家拜拜拜拜
---
title: "最近爆火的 Harness Engineering 到底是啥？一期讲透！"
url: "https://www.bilibili.com/video/BV1Zk9FBwELs/?spm_id_from=333.788.recommend_more_video.-1&trackid=web_related_0.router-related-2479604-s7xkd.1778479863959.922&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1Zk9FBwELs"
cid: "37177853344"
author: "code秘密花园"
upload_date: "2026-04-02"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116334943798885&bvid=BV1Zk9FBwELs&cid=37177853344&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Harness Engineering 是 AI 工程领域继 Prompt Engineering、Context Engineering 之后的第三次重心迁移。

这期视频我会从演进、构成、实践三个角度把它讲透，包括 OpenAI 和 Anthropic 的真实实践案例。

推荐资源：

《OpenClaw 完全指南（花园版）》：https://my.feishu.cn/wiki/QzGAwOH4LiZOYXktGyhcHoLUnRe

《code秘密花园 AI 教程资源合集》：https://my.feishu.cn/wiki/U9rYwRHQoil6vBkitY8cbh5tnL9

《Easy AI 项目》：https://github.com/ConardLi/easy-learn-ai

## 章节

- `00:00` 引言
- `01:43` Harness 的演进
- `08:38` Harness 的构成
- `12:15` Harness 的实践

## 字幕

### 引言 `00:00`

`00:00` 今天我们来聊一个最近在AI圈特别火
`00:02` 但很多人还没真正弄懂的词
`00:04` Harness engineering
`00:05` 如果你最近也在做agent或者关注AI应用的落地
`00:09` 或多或少可能都会遇到这样的问题
`00:10` 为什么同样的模型
`00:11` 别人做出来的agent可以连续跑很久
`00:14` 成功率很高
`00:15` 到了自己手里就总是差强人意呢
`00:17` 很多人可能会想
`00:18` 是不是模型不够强
`00:19` 是不是提示词没调好
`00:20` 是不是IG没调明白
`00:21` 当然啊
`00:22` 这些都有影响
`00:23` 但是啊越来越多的团队最后发现呢
`00:26` 真正决定我们的系统能不能稳定地跑起来
`00:28` 往往不是模型本身
`00:30` 而是模型外面那套运行的系统
`00:32` 那这套东西呢现在有了一个统一的名字
`00:35` 就叫harness
`00:36` 大家好
`00:37` 欢迎来到可乐秘密花园
`00:38` 我是花园老师
`00:39` 为什么想聊这个话题呢
`00:40` 因为年初的时候啊
`00:42` 有个朋友找我
`00:42` 帮他们调一个agent
`00:43` 他们团队之前已经做了很多努力
`00:45` 换上了最好的旗舰模型
`00:47` 提示词
`00:47` 改了上百版
`00:48` 各种参数也调了不少
`00:50` 但是一进真实的场景效果还是不稳定
`00:52` 有的时候很聪明
`00:53` 有的时候又莫名的跑偏
`00:55` 任务的成功率不到70%
`00:57` 那后来呢我去帮他们看了一下
`00:59` 最后的改动
`00:59` 最大的地方反而不是模型
`01:01` 也不是提示词
`01:02` 我的改进点呢在于任务是怎么拆的
`01:05` 状态是怎么管的
`01:06` 关键的步骤要怎么校验
`01:07` 失败之后要怎么恢复结果
`01:09` 新版本上线之后呢
`01:10` 还是同样的模型
`01:11` 同样的提示词
`01:12` 成功率拉到了95%以上
`01:14` 那当时这个朋友问我
`01:15` 你到底改了什么呢
`01:17` 说实话啊
`01:18` 那个时候我没有一个特别准确的词来形容
`01:21` 直到最近harness engineering这个概念越来越火
`01:24` 我才意识到我当时改的这些东西
`01:26` 本质上就是harness
`01:28` 所以今天这期视频呢
`01:29` 我想彻底把这个概念跟大家讲清楚
`01:32` 我们主要分三个部分
`01:33` harness是怎么一步步演进出来的
`01:35` 一个成熟的harness到底包括哪些部分
`01:38` Open ai astrobic
`01:39` 这些公司真实到底是怎么做的
`01:41` 过去两年呢
`01:42` AI工程其实经历了三次很明显的中心迁移

### Harness 的演进 `01:43`

`01:45` 从prompt engineering
`01:47` Context engineering
`01:48` 再到最近的harness engineering
`01:50` 表面上看好像只是换了几个新的名词
`01:52` 但如果你只是把它理解成术语流行式
`01:55` 那就完全低估它们了
`01:56` 那这三个词呢
`01:57` 分别对应了现在AI系统发展的三个阶段性问题
`02:01` 模型有没有听懂你在说什么
`02:03` 模型有没有拿到足够而且正确的信息模型
`02:06` 在真实的执行力能不能持续的做对
`02:09` 你会发现这些问题呢是一层一层往外扩张的
`02:14` 在大模型刚火起来的时候呢
`02:16` 大家最直观的感受就是同一个模型
`02:19` 你换一种说法
`02:20` 结果可能差很多
`02:21` 比如你说一句
`02:22` 帮我总结一下这篇文章
`02:23` 他可能只会给你一个很平的总结
`02:25` 但如果你换一种说法
`02:27` 效果马上就会不一样
`02:28` 所以那个阶段呢大家都相信一件事情
`02:30` 模型不是不会
`02:31` 而是你没有把问题说明白
`02:33` 于是大家开始疯狂的研究提示词
`02:36` 什么角色设定风格约束
`02:38` 非show的示例分布
`02:39` 引导输出格式等等等等
`02:41` 那为什么这些东西有效呢
`02:43` 因为大模型本质上是一个对上下文非常敏感的
`02:46` 概率生成系统
`02:47` 你给他什么身份
`02:48` 他很容易沿着那个身份去回答
`02:50` 你给他什么样的样例
`02:52` 他很容易沿着那个范式去补全
`02:54` 你强调什么样的约束
`02:55` 它就很容易把那部分当成重点
`02:57` 所以提示词工程的本质不是命令模型
`03:00` 而是塑造一个局部的概率空间
`03:03` 那这个阶段的最重要的能力不是系统的设计
`03:06` 而是语言的设计
`03:07` 但提示词工程很快就遇到了天花板
`03:10` 因为很多任务不是你说清楚就行
`03:12` 而是你真的得知道
`03:14` 比如你让模型分析一份公司的内部文档
`03:16` 回答一个产品的最新配置
`03:18` 按照一套非常长的规范去写代码
`03:20` 在多个工具之间完成复杂的任务
`03:22` 这个时候你会发现提示词写得再漂亮
`03:25` 也替代不了事实本身
`03:27` 所以呢提示词擅长的是长期任务约束输出
`03:30` 激发模型的已有能力
`03:32` 但是他不擅长凭空弥补缺失的知识管理
`03:36` 大量动态的信息处理
`03:37` 长链路任务里的状态
`03:38` 说白了提示词解决的是表达的问题
`03:41` 不是信息的问题
`03:42` 于是第二阶段开始了
`03:46` 当大家还只是做聊天机器人的时候呢
`03:48` 提示词的作用很大
`03:49` 因为任务短
`03:50` 链路短
`03:51` 状态少
`03:51` 很多问题确实靠把话说明白就可以解决了
`03:54` 但后来agent开始火了
`03:56` 模型不只是要回答问题
`03:58` 而是要进到真实的环境里面做事情
`04:00` 他要多轮对话
`04:01` 调浏览器
`04:01` 写代码
`04:02` 数据库
`04:02` 这些工具还要在多个步骤之间传递
`04:05` 中间结果还要根据外部的反馈不断修订计划
`04:07` 那这个时候问题就变了
`04:09` 系统面对的已经不是一次回答对不对
`04:11` 而是整条链路的任务能不能跑通
`04:13` 比如如果你不是简单的问一句
`04:15` 帮我总结一下这篇文章
`04:16` 而是让他做一个更真实的任务啊
`04:19` 比如说帮我分析这份需求文档
`04:20` 找出潜在风险
`04:21` 结合历史的评审意见给出改进建议
`04:23` 再生成一版发给产品经理的反馈稿
`04:26` 你会发现
`04:26` 这已经完全不是一句提示词就能解决的问题了
`04:29` 他至少要拿到当前的需求文档
`04:31` 历史的评审记录
`04:32` 相关规范
`04:32` 当前目标已经分析出来
`04:34` 中间结论输出的对象是谁
`04:35` 语气应该怎么调等等等等
`04:37` 所以context engineering的核心就变成一句话
`04:41` 模型未必是知道的
`04:42` 系统必须在合适的时机把正确的信息送进去
`04:45` 那这里的context呢也不只是几段背景的资料
`04:49` 在工程的意义上
`04:50` 它代表了所有影响模型当前决策的信息的总和
`04:54` 包括用户的输入历史对话
`04:55` 检索结果工具返回当当前任务的状态
`04:58` 中间产物系统规则
`04:59` 安全约束或者其他agent的传过来的结构化的结果
`05:03` 所以你会看到prompt其实只是context的一部分
`05:06` 也正因为如此呢
`05:07` 推到上下文的供给机制是非常重要的
`05:10` 那说到context engineering呢
`05:12` 我觉得IG也算是一个比较典型的实践
`05:15` IG的价值是很直接的
`05:17` 模型参数里面没有知识
`05:18` 怎么在运行时补进去呢
`05:20` 那做法大家都知道
`05:21` 先检索再把相关的内容塞到上下文
`05:24` 但是真正成熟的context engineering呢
`05:26` 关注的肯定不只是检索啊
`05:28` 他关注的是整条完整的链路
`05:30` 比如文档怎么切块
`05:31` 结果怎么排序
`05:32` 长文怎么压缩
`05:33` 历史对话什么时候要保留
`05:35` 什么时候要摘要
`05:36` 工具返回要不要全部暴露给模型
`05:38` 多个A证的之间
`05:39` 到底传原文摘要还是结构化的字段呢
`05:41` 包括最近很火的asian skills
`05:44` 我觉得本质上也是上下文工程的高级实践
`05:47` 因为它解决了一个特别现实的问题
`05:49` 如果你把十几个不同的工具工具的说明
`05:51` 所有的参数定义全部一上来就塞个模型
`05:54` 理论上模型会知道的更多
`05:56` 但是实践往往会更糟糕
`05:58` 为什么呢
`05:59` 因为上下文的窗口是非常稀缺的资源
`06:01` 信息一多
`06:02` 注意力就会涣散
`06:03` 所以skill采用的是一个非常典型的思路
`06:06` 叫渐进式披露
`06:07` 不是一开始就把能力全部给模型看
`06:09` 而是只给他看最少量的原信息
`06:11` 等他真正的要触发某些能力的时候
`06:14` 再把那部分的SOP详细的参考信息
`06:16` 脚本动态的加载进来
`06:17` 那这个思路呢其实非常重要
`06:19` 因为它告诉我们
`06:20` 上下文的优化不只是给的更多
`06:22` 而是按需给分层
`06:24` 给在正确的实际给
`06:26` 但是上下文工程其实也不只是终点
`06:29` 因为后来大家又发现了一个更麻烦的问题
`06:32` 就算信息给对了
`06:33` 模型也不一定能稳定的执行的正确
`06:36` 它可能计划做的很好
`06:37` 但是执行跑偏了
`06:39` 掉了工具
`06:39` 但理解错了
`06:40` 返回结果在一个很长的链路里已经慢慢偏航了
`06:43` 但是系统却没有发现哎
`06:44` 这个时候我们发现啊
`06:46` 提示词和上下文
`06:47` 其实主要的解决都在输入词的问题
`06:49` 提示词优化意图的表达
`06:51` 上下文优化的是信息的供给
`06:52` 但是复杂的任务里还有一个更难的问题
`06:55` 当模型开始连续行动的时候
`06:57` 谁来监督它
`06:58` 约束它和纠偏它呢
`07:00` 这个时候第三阶段来了
`07:05` ALICE这个词呢原本的意思是缰绳
`07:07` 马具约束装置的意思
`07:09` 放到AI系统里面
`07:10` 其实就是在提醒我们一件非常朴素的事情
`07:12` 当模型做
`07:12` 从回答问题走向执行任务
`07:14` 系统不只要能够负责V信息
`07:16` 还要能够驾驭整个过程
`07:18` 这个就是harness engineering的出发点
`07:21` 如果前两代工程关注的是怎么让模型更会想
`07:25` 那harness更关注的就是怎么让模型别跑偏
`07:28` 跑得稳
`07:29` 出了错还能拉回来
`07:30` 这里呢啊我用一个比较通俗的例子啊
`07:33` 来解释这三个概念
`07:34` 假设你要派一个新人
`07:35` 去完成一次很重要的客户拜访的工作
`07:38` prompt engineering呢
`07:39` 就是你要告诉他先把任务讲清楚
`07:41` 比如见面先寒暄
`07:42` 再介绍方案
`07:43` 载完需求
`07:44` 最后确认下一步啊
`07:45` 这个就是prompt
`07:46` 重点是把话说明白
`07:48` 那context engineering是啥呢
`07:49` 你要告诉他把资料要准备齐全啊
`07:52` 比如说这个客户的背景
`07:53` 过往的沟通记录产品的报价
`07:55` 竞品的情况啊
`07:56` 这次会议的目标
`07:57` 这些都是context
`07:59` 重点是把信息要给对
`08:01` 那如果这个会真的很重要啊
`08:02` 你还会继续做很多事情啊
`08:04` 比如说让他带着checklist去
`08:06` 让他在关键的节点实时汇报
`08:08` 汇后核实纪要和录音啊
`08:10` 如果发现偏差马上纠正
`08:12` 最后按照明确的标准去验收结果
`08:14` 这些啊就是harness
`08:16` 重点已经不是说清楚和资料齐不齐了
`08:19` 而是有没有一套持续观测
`08:20` 持续纠偏
`08:21` 最终验收结果的机制
`08:23` 所以呢这三者啊也不是替代的关系
`08:25` 而是包含的关系
`08:26` prompt是对指令的工程化
`08:28` context是对输入环境的工程化
`08:31` harness呢就是对整个运行系统的工程化
`08:34` 它们的边界是一层比一层大的

### Harness 的构成 `08:38`

`08:39` LUNCHA的工程师呢
`08:40` 给harness下了一个很典型的定义
`08:42` agent等于model加harness
`08:44` 那harness呢就等于agent减model
`08:46` 翻译成人话呢
`08:47` 就是在一个A卷的系统里面
`08:50` 除了模型本身以外
`08:51` 几乎所有能决定它能不能稳定交付的东西
`08:55` 都可以算进harness
`08:56` 那如果拆开来看呢
`08:58` 我自己会把一个成熟的harness engineering分成六层
`09:01` 第一层啊
`09:02` 就是我们重新站在哈里斯的视角去看context
`09:05` 模型能不能稳定发挥
`09:07` 很多时候不仅取决于他聪不聪明
`09:09` 而取决于他看到了什么
`09:10` 所以harness的第一职责
`09:11` 就是让模型能够在正确的一起边界内思考
`09:14` 第一层通常包括三件事情
`09:17` 首先啊角色的目标和定义模型
`09:19` 要知道自己是谁
`09:20` 任务是什么
`09:21` 成功的标准是什么
`09:22` 第二信息的裁剪和选择
`09:24` 上下文不是越多越好
`09:25` 而是越相关越好
`09:26` 第三啊
`09:27` 结构化的组织
`09:28` 固定的规则放在哪
`09:29` 当前的任务放在哪儿
`09:30` 任务运行的状态放在哪儿
`09:32` 外部的证据又放在哪儿
`09:33` 最好分层清除
`09:34` 因为信息一旦乱掉呢
`09:36` 模型就很容易漏重点
`09:37` 忘约束
`09:38` 甚至自我污染
`09:39` 第二层工具系统没有工具
`09:41` 大模型本质上还是一个文本预测器
`09:43` 会解释
`09:43` 会总结
`09:44` 但他接触不到真实的世界
`09:46` 一旦连上工具呢
`09:47` 模型才可以真正的做事
`09:49` 比如搜网页
`09:50` 读文档
`09:50` 写代码
`09:50` 调API等等等等
`09:52` 但是harness在这里做的
`09:53` 不是简单的把工具挂上去
`09:56` 而是啊也要解决三个问题
`09:58` 第一给他什么工具
`09:59` 工具太少
`10:00` 能力不够
`10:00` 工具太多
`10:01` 模型又会乱用
`10:02` 第二什么时候该调用工具
`10:04` 本来不需要查的时候别乱查
`10:06` 该查账的时候也别硬答
`10:07` 第三工具结果怎么重新喂回模型
`10:10` 搜索过来的几十条结果
`10:11` 不应该原封不动的塞回去
`10:13` 而是要提炼筛选
`10:14` 保持和任务的相关性
`10:15` 第三层执行编排
`10:17` 那这一层解决的核心问题呢
`10:19` 就是模型下一步该做什么
`10:21` 很多agent的问题呢
`10:22` 不是某一步不会
`10:23` 而是不会把所有的步骤给串起来
`10:25` 它会搜索
`10:26` 也会总结
`10:27` 也会写代码
`10:27` 但整个过程想到哪做到哪儿
`10:29` 最后交付出来一堆半成品
`10:31` 所以一个完整的任务呢通常需要有这样的轨道
`10:34` 首先理解目标
`10:35` 然后判断信息够不够不够
`10:36` 继续补
`10:37` 基于结果继续分析
`10:39` 然后生成输出
`10:40` 检查输出不满足要求就重新修正
`10:43` 或者从事
`10:43` 这个时候你会发现这已经非常接近人在工作了
`10:47` 区别在于人靠经验
`10:49` Agent
`10:49` 靠harness这套环境
`10:51` 第四层记忆和状态
`10:52` 那没有状态的agent呢
`10:53` 每一轮都会像失忆一样
`10:55` 他不知道自己刚做了啥
`10:56` 也不知道哪些结论已经确认了
`10:57` 哪些问题还没解决
`10:58` 所以harness还必须要管理状态
`11:01` 这里呢我们要至少让它分清三类东西
`11:04` 首先当前任务的状态
`11:05` 会话中的中间结果
`11:07` 长期的记忆和用户偏好
`11:08` 这三类呢如果混在一起
`11:10` 系统会越来越乱
`11:11` 看清楚之后呢
`11:12` agent的才会像一个稳定的写作者
`11:14` 第五层评估和观测啊
`11:16` 这个呀就是很多团队啊最容易忽视的一层
`11:19` 很多系统其实不是生成不出来
`11:20` 而是生成完了之后根本不知道自己做的好不好
`11:23` 那如果没有独立的评估和观测的能力
`11:26` agent就会长期停留在自我感觉良好的状态
`11:29` 这一层呢通常包括输出和验收环境的验证
`11:33` 自动的测试日志和指标
`11:35` 错误的归因等等
`11:36` 也就是说呢
`11:36` 系统不仅是要会做
`11:38` 还要知道自己有没有真的能够做
`11:40` 对第六层约束校验失败和恢复
`11:43` 那最后一层呢
`11:44` 往往才是真正决定这个系统能不能上线的
`11:47` 关键环节
`11:48` 因为在真实的环境里面
`11:50` 失败不是例外
`11:51` 而是常态
`11:51` 可能搜索不准
`11:52` 可能是API超时
`11:54` 也可能文档格式混乱或者模型误解了任务
`11:57` 那如果没有恢复的机制呢
`11:58` agent每次出错就只能从头再来
`12:01` 所以一个成熟的harness
`12:02` 一定要包括三件事情约束啊
`12:04` 哪些能做
`12:05` 哪些不能做校验啊
`12:06` 比如输出之前
`12:07` 输出之后要怎么检查
`12:09` 恢复失败之后怎么从事切入镜
`12:11` 回滚到稳定的状态

### Harness 的实践 `12:15`

`12:15` 专概念呢我们来看最有参考价值的部分啊
`12:17` 一线公司的真实实践
`12:19` 因为harness这个词最近之所以突然火起来呢
`12:22` 不是大家在空谈这个方法论
`12:24` 而是很多公司都已经把它做进了
`12:26` 产品和工程体系里面了啊
`12:28` 比如long cha在底层模型完全不变的情况下
`12:30` 只通过改造和迭代
`12:32` Harness
`12:32` 就把他自家的智能体验从一个榜单上的排名
`12:35` 直接从30开外杀到了前五
`12:37` open AI呢依靠一个只有几名人类工程师的团队
`12:39` 用agent从零
`12:40` 构建了一个超百万行代码的生产及应用
`12:43` 百分之百的代码都是由agent编写的
`12:46` 耗时呢只有纯人工开发的1/10
`12:48` 那ANTHROPIC呢
`12:49` 也构建了一个可以完全自主编码的系统
`12:52` 只凭一句自然语言的需求
`12:53` 就能在无需人类干预的情况下
`12:55` 连续运行几个小时
`12:57` 最后做出完整的游戏
`12:58` 完整的数字音频工作站
`13:00` 那我们先看看ATHROPIC的实践啊
`13:02` 首先啊他们在长城自主的任务上
`13:04` 总结了两个特别典型的问题
`13:06` 那第一个问题啊
`13:07` 我自己把它翻译成上下文
`13:08` 交易时间一长
`13:09` 上下文越来越满
`13:10` 模型就是模型就开始丢细节
`13:12` 丢重点
`13:13` 甚至呢还会出现一种很有意思的现象
`13:15` 他好像知道自己快装不下了
`13:17` 于是开始着急的去收尾
`13:19` 很多系统面对这种问题呢
`13:20` 都会做context complication
`13:22` 也就是啊把前面的历史上下文压缩一下
`13:24` 再继续跑
`13:25` 但ATHROPIC发现呢
`13:26` 对于一些模型来说
`13:27` 这还是不够的
`13:28` 因为压缩只是变短了
`13:30` 不代表那种负担感真的消失了
`13:32` 所以他们做了一件更激进的事情
`13:34` 叫context reset
`13:36` 不是在原上下文里面继续压
`13:38` 而是换了一个非常干净的新的agent
`13:40` 把工作交接给他
`13:42` 那这个思路很像什么呢
`13:43` 特别像工程里面遇到内存泄漏之后
`13:45` 不是继续清缓存
`13:46` 而是直接重启整个进程再恢复状态
`13:49` 这个其实就是一种非常典型的harness设计
`13:53` 那ANTHROPIC解决的第二个问题呢
`13:54` 就是自评失真的问题
`13:56` 首先模型自己干活啊
`13:57` 再让他自己给自己打分
`13:59` 往往会是会偏乐观的
`14:00` 那尤其是在设计体验
`14:02` 产品完整度这一类没有标准答案的问题上
`14:04` 偏差是更明显的
`14:06` 所以他们采用了一个非常关键的思路啊
`14:08` 把干活的人和验收的人分开
`14:10` 他们是这样拆分的啊
`14:11` planner负责把模糊的需求扩展成完整的规格
`14:14` generator负责逐步的去实现
`14:17` 那evaluator呢负责像QA一样去真实的测试
`14:20` 更关键的是这个evaluator他不只是会看代码
`14:23` 而是会真实的操作页面
`14:25` 看具体的交互检查实际的结果
`14:27` 也就是说啊
`14:27` 这不是一个抽象的审查
`14:29` 它是一个带具体环境的验证
`14:31` 那这个事情非常重要啊
`14:32` 因为它背后是一个很明确的工程原则
`14:36` 生产验收必须分离
`14:38` 只要评估者足够独立
`14:39` 系统就能形成一个真正的有效循环
`14:42` 生成检查修复再检查的这样的一个循环
`14:46` 那OpenAI呢在这方面给我的感觉是
`14:48` 他们重新定义了工程师在agent的时代的工作
`14:52` 他们做了一个非常有意思的思路啊
`14:54` 人类在这个环境里面不需要写一行的代码
`14:57` 人类只需要去负责设计环境
`14:59` 具体来说呢
`15:00` 工程师的工作变成了三件事情
`15:01` 首先把产品目标拆解成agent能理解的小任务
`15:05` 那agent失败的时候呢
`15:06` 不是让他更努力一点
`15:07` 而是问环境里面缺了什么能力
`15:09` 最后建立反馈的链路
`15:11` 让agent真正的能够看到自己的工作结果
`15:13` 那这句话我是非常认同的
`15:15` 当agent的出了问题的时候
`15:17` 修复方案几乎从来不是要更努力一点
`15:20` 而是确定他缺了什么样的结构性的能力
`15:23` 这个其实也是典型的harness思维
`15:25` Openai
`15:26` 还有一个特别典型的事件啊
`15:27` 也是渐进式披露
`15:29` 他们早期呢犯过一个很多团队都会犯的错误
`15:31` 写了一个巨大的agent词典MD
`15:34` 把所有的规范框架约定全部塞进去了
`15:37` 结果呢agent的更糊涂了
`15:38` 因为上下文窗口是一个稀缺的资源
`15:41` 塞的太满
`15:42` 其实等于什么都没说
`15:43` 那后来他们怎么改的呢
`15:44` 把agent点MD变成一个目录页啊
`15:47` 页面只保留核心的索引
`15:49` 更详细的内容呢查到架构文档设计文档
`15:51` 执行计划
`15:52` 质量评分
`15:52` 安全规则这些具体到子文档里面去了
`15:55` 那A整的先看目录
`15:56` 需要的时候再钻进去
`15:58` 那这个时候我们会发现啊
`15:59` 这个和我们前面说的skills本质上是一个思路
`16:02` 不是一次性全给
`16:04` 而是按需暴露
`16:05` 还有个实践啊
`16:06` 就是OpenAI不只是让agent的写代码
`16:08` 还会让agent呢看见整个应用
`16:11` 因为产业速度一旦上来呢
`16:12` 瓶颈其实就不再是血
`16:14` 而是验了
`16:15` 那人类呢根本是验不过来的
`16:17` 所以他们让agent自己去验
`16:19` 怎么验呢
`16:19` 首先接浏览器能截图
`16:21` 点页面能模拟用户的真实操作
`16:24` 然后去给A证接日志系统和指标系统
`16:27` 让他能够查log查监控
`16:28` 最后每个任务都独立隔离的环境在跑
`16:31` 互不影响
`16:32` 结果呢就是agent不再是写完代码就说是写完了
`16:35` 而是真正的可以跑起来看结果
`16:37` 发现bug修bug再验证
`16:38` 这个呢
`16:39` 其实就是harness里非常完整的一套攻击系统
`16:42` 执行编排评估和观测
`16:44` 约束和恢复
`16:45` 那还有一点需要注意的是呢
`16:46` OpenAI不止会靠
`16:48` 人类在最后的code review环节去兜底质量
`16:51` 因为agent的提交速度太快了
`16:53` 人类是盯不过来的
`16:54` 所以他们把很多资深工程师的经验
`16:56` 直接写成了系统规则
`16:58` 比如模块怎么分层
`16:59` 哪一层不能依赖哪一层
`17:00` 什么情况下必须拦截
`17:01` 发现问题之后应该怎么修
`17:03` 重点呢是这些规则不只是负责报错
`17:05` 而是会把怎么修也一起反馈给agent的
`17:08` 进入下一轮的上下文
`17:10` 那你会发现呢
`17:11` 这已经不是传统意义上的代码规范了
`17:13` 而是一套可持续运行的自动治理系统
`17:16` 这个呢也是harness的典型形态啊
`17:19` 最后呢我们说一下啊
`17:20` 首先prompt engineering呢解决的是怎么把任务讲清楚
`17:24` context engineering呢解决的是怎么把信息都给对
`17:27` 那harness engineer呢
`17:29` 解决的是怎么让模型在真实的执行中持续作对
`17:32` 所以harness不是在取代prompt
`17:35` 也不是在取代context
`17:36` 它是在更大的系统边界上
`17:38` 把前两者都包含进来
`17:40` 当任务还是简单的单轮生成的时候
`17:43` prompt是很重要的
`17:45` 那当任务开始依赖外部知识
`17:46` 去运行信息的时候呢
`17:47` context就很关键了
`17:49` 当模型真的进入了长链路
`17:51` 可执行
`17:51` 低容错的真实场景里面
`17:53` harness几乎就是不可避免的
`17:56` 这也是为什么同样的模型
`17:57` 在不同的产品里面表现差距会这么大
`18:00` 因为真正决定上限的可能是模型
`18:02` 但是真正决定能不能落地
`18:04` 能不能稳定交付的就是harness
`18:05` 那到了这个阶段呢
`18:06` 我们也看清了一个现实AI落地的核心挑战
`18:10` 正在从让模型看起来更聪明
`18:12` 转向让模型在真实世界里稳定的工作
`18:16` 如果你最近也在做agent
`18:18` 我觉得这件事情非常值得你趁早想明白好啊
`18:21` 本期教程的内容呢就是这么多
`18:24` 如果本期教程对你有所帮助呢
`18:25` 希望得到一个免费的三连和关注
`18:28` 感谢大家
`18:28` 我们下期见
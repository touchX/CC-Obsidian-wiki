---
title: 40分钟的Docker实战攻略，一期视频精通Docker
url: https://www.bilibili.com/video/BV1THKyzBER6/?spm_id_from=333.1387.homepage.video_card.click&vd_source=00c261324d44a5cd7366302129b53bde
bvid: BV1THKyzBER6
cid: "30737106148"
author: 技术爬爬虾
upload_date: 2025-06-28
subtitle_lang: 中文
created: 2026-05-11
tags:
  - clippings
  - bilibili
  - docker
---

<iframe src="https://player.bilibili.com/player.html?aid=114759127008838&bvid=BV1THKyzBER6&cid=30737106148&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

本视频是一个40分钟的Docker的实战教程。涵盖了Docker的所有常用功能与命令。
主要内容：

1. Docker的核心概念
2. 在Linux, windows 还有Mac电脑上安装docker
3. 下载镜像。配置镜像站解决下载镜像的网络问题
4. dokcer run命令创建并且运行容器。 docker run命令的重要参数
5. 进入容器内部进行调试
6. Docker网络，分别是bridge模式，创建子网，host模式与none模式
7. 轻量级的容器编排技术Docker Compose
8. AI辅助学习Docker


Docker安装 镜像站等教程：
https://github.com/tech-shrimp/docker_installer

## 章节

- `00:00` 核心概念
- `01:20` 安装
- `04:09` 下载镜像
- `09:33` 运行容器
- `13:01` 挂载卷
- `16:56` run其他参数
- `19:34` 调试容器
- `24:45` 构建镜像
- `29:16` Docker网络
- `33:31` Docker Compose

## 字幕

### 核心概念 `00:00`

`00:00` 本视频是一个40分钟的docker实战教程
`00:03` 涵盖了docker的所有常用功能与命令
`00:05` docker是目前最成熟高效的软件部署技术
`00:08` Docker
`00:09` 简单来说就是用容器化技术
`00:11` 给应用程序封装独立的运行环境
`00:14` 每个运行环境就是一个容器
`00:16` 运行容器的计算机被称为宿主机
`00:19` docker容器与虚拟机的最大区别是
`00:21` docker容器之间共用同一个系统内核
`00:24` 而每个虚拟机都包含一个操作系统的完整内核
`00:28` 所以docker容器比虚拟机更轻更小
`00:31` 启动速度更快
`00:32` docker另外一个重要概念是镜像
`00:35` 镜像是容器的模板
`00:37` 我们可以把镜像类比成软件安装包
`00:40` 而容器是安装出来的软件
`00:42` 举一个现实世界的例子
`00:43` 镜像与容器的关系就像是用模具做糕点
`00:47` docker镜像就像是模具
`00:49` 而容器就像是糕点
`00:50` 我们可以使用一个模具做出很多个糕点
`00:53` 当然我们也可以把模具分享给其他人
`00:56` 使用docker仓库就是用来存放分享镜像的地方
`01:00` 每个人都可以把自己的镜像上传到仓库里面
`01:03` 然后其他人就可以下载镜像
`01:05` 并且使用docker的官方仓库
`01:08` 就是docker hub上面存储了许多人分享的docker镜像
`01:12` 以上就是docker最核心的基础概念
`01:14` 了解这些概念就足够使用docker了
`01:17` 接下来我们进入实战环节

### 安装 `01:20`

`01:20` docker通常来说是基于LINUX容器化技术
`01:24` windows和Mac电脑上都是虚拟了一个LINUX
`01:27` 子系统来运行docker
`01:29` 所以docker最好的实战环境就是用LINUX系统的宿主机
`01:33` 弄一台LINUX主机并不麻烦
`01:35` 爬爬虾
`01:36` 之前有视频介绍
`01:37` 把家里的旧电脑
`01:38` 改造成乌邦图系统的家庭服务器
`01:40` 我们先看在LINUX系统上安装docker
`01:43` 然后分别在windows和Mac电脑上安装docker
`01:46` 这是我在上海的一台乌邦图系统的云服务器
`01:49` 我们使用它来安装docker
`01:51` 来到这个地址
`01:52` get docker点com
`01:53` 这里有一个安装步骤
`01:54` 我们只需要把把第一步跟第四步执行一下
`01:57` 这里把第一步的命令复制出来
`01:59` 粘贴进服务器回车
`02:01` 第四步的命令复制出来
`02:02` 粘贴进服务器
`02:03` 注意这里的速度指的是super user do
`02:06` 也就是使用用管理员权限来执行这个命令
`02:09` 如果您的LINUX用户不是root用户
`02:12` 比如像我这样是乌邦图用户
`02:14` 就需要在所有的docker命令前面都加一个速度
`02:17` 最后点击回车
`02:18` 等待个十几秒就完成了安装
`02:20` 接下来我们看在windows系统上安装docker
`02:22` 在任务栏搜索功能
`02:24` 点击启用或关闭windows功能
`02:27` 然后把virtual machine platform勾选上
`02:29` 有的电脑可能翻译叫做虚拟机平台
`02:32` 接下来把适用于LINUX的windows子系统的功能勾选上
`02:36` 也就是WSL
`02:37` 接下来按照提示重新启动电脑
`02:39` 电脑重启以后
`02:40` 我们开始安装WSL
`02:42` 这里要输入两个命令
`02:44` 首先在搜索栏中搜索CMD
`02:46` 右键以管理员身份打开命令提示符
`02:49` 我们先输入第一个命令
`02:51` Wsl set default version to
`02:54` 就是把WSL的默认版本设成二
`02:56` 这里执行第二个命令
`02:58` 安装一下WSL
`02:59` WSL两个横杠update
`03:02` 如果您的机器处于国内网络的话
`03:04` 我建议在后面加上两个横杠
`03:06` 外部download可以减少因为网络问题导致的下载失败
`03:09` 进度条走完WS就安装成功了
`03:12` 我们来到这个地址
`03:13` 把docker desktop下载一下
`03:15` 根据自己的CPU架构选择对应的安装包
`03:18` 一般windows都是AMD64的CPU
`03:21` 安装过程非常简单
`03:22` 一路点击下一步就行了
`03:24` 如果想自己指定安装目录
`03:26` 可以使用命令行的方式
`03:28` 那就是这一行命令最后加一个参数
`03:31` 可以指定它的安装位置
`03:33` 我们在windows上面使用docker
`03:35` 注意要把这个软件一直开着
`03:37` 我们打开一个windows终端来测试一下
`03:39` 输入docker两个横杠version
`03:41` 这里能够打印一个版本号就安装成功了
`03:44` 虽然docker desktop提供了一个可视化的界面
`03:47` 但是本期视频我们主要还是是用命令行来讲解
`03:50` 因为命令行在各个操作系统上是一致的
`03:53` 它的通用性更强
`03:54` 接下来我们来看在苹果电脑上
`03:56` 也就是使用Mac os安装docker
`03:58` Mac电脑是最简单的
`04:00` 根据自己的芯片下载对应的安装包
`04:03` 然后安装就可以了
`04:04` 下一个章节进入docker命令
`04:06` 我们来介绍几个最重要的docker命令

### 下载镜像 `04:09`

`04:09` docker put命令用来从仓库下载镜像
`04:12` 我们看到一个镜像有四部分的内容
`04:15` docker点IO这部分是docker仓库的注册表地址
`04:19` docker点IO表示这是docker hub的官方仓仓库
`04:22` 官方仓库可以省略仓库地址
`04:24` library是命名空间
`04:25` 因为docker hub是公共仓库
`04:28` 每个人都可以上传自己的镜像
`04:30` 如果每个人上传的镜像都叫同一个名字
`04:33` 比如都叫N进X
`04:34` 这样肯定会乱套
`04:35` 所以docker要求上传镜像的时候
`04:38` 要在镜像前面加上作者的名字
`04:40` 也就是命名空间
`04:41` library是docker官方仓库的命名空间
`04:44` 这个空间下面的所有镜像都是由官方管理的
`04:48` 如果一个镜像属于是官方的命名空间
`04:51` 那这部分可以省略不写
`04:52` 最后冒号后面latest这部分是docker镜像的标签名
`04:56` 也就是版本号
`04:58` docker镜像作为一个软件
`04:59` 当然是有版本号的
`05:01` 我们可以指定下载一个特定的版本
`05:03` 比如冒号1.28.0
`05:05` 当然也可以写作冒号latest
`05:07` 或者干脆不写
`05:08` 表示获取最新版本的镜像
`05:10` 简化后的命令是这样的
`05:12` docker pn gx表示从docker官方仓库的官方命名空间
`05:17` 里面下载最新版的ng docker镜像
`05:20` 我们再来看一个例子
`05:21` 这个是使用docker铺下载一个开源的AI工作流工具
`05:25` N8N爬爬虾
`05:26` 之前有一期完整视频介绍
`05:28` 关于N8N的使用
`05:30` 我们看到前面这一部分
`05:31` 表示
`05:32` 这个镜像是从N8N的一个私有仓库下载的
`05:34` 中间这一部分是命名空间
`05:36` 也就是作者的名字
`05:38` 最后镜像的名字叫做N8
`05:40` 我们来认识一下这个网站
`05:41` Docker hub
`05:42` 这个网站是docker的官方仓库
`05:44` 我们想要什么样的镜像
`05:46` 只需要过来搜索一下
`05:47` 比如我搜索N减
`05:49` 这里就列出了所有跟NGX相关的镜像
`05:52` 我们看到这里写了docker official images
`05:55` 这个就是由官方维护的镜像
`05:57` 这一个镜像下面有非常多的版本号
`05:59` 下面还有详细的使用说明
`06:01` 阅读这个文档可以帮助我们更好地使用镜像
`06:04` docker hub网站不需要登录就可以使用
`06:07` 如果因为网络问题打不开
`06:09` 也可以使用这个镜像站来搜索镜像
`06:11` 它们的功能是一样的
`06:12` 我们来看两个英文单词
`06:14` docker点IO这部分我们叫它registry
`06:17` 也就是仓库地址或者注册表
`06:19` Registry
`06:20` 加命名空间
`06:21` 加镜像的名字组合起来是一个repository
`06:24` 也就是镜像库
`06:25` 一个镜像库存放的是同一个镜像的不同版本
`06:29` 比如docker hub这整一个网站就是一个registry
`06:32` 而对应的NGX是一个repository
`06:34` 它存储了同一个镜像的不同的版本
`06:37` 好我们来试一下
`06:38` 回到我在上海的云服务器
`06:40` 输入这个命令
`06:41` docker pn g x回车
`06:43` 看到这里报了一个permission deny的错误
`06:46` 这种错误就是命令行前面没有加速度
`06:48` 我们在命令行前面加上速度
`06:51` 重新试一下
`06:52` 在国内的网络环境执行这个命令
`06:54` 可能会因为网络问题报错
`06:56` 我们可以通过修改配置文件的方式
`06:58` 设置镜像站来解决这个问题
`07:00` 我之前在get tap上面
`07:02` 写过一个配置镜像站的文档
`07:04` 这里我们先执行这个命令
`07:06` 修改一下配置文件
`07:07` 把这一段复制出来粘贴进去
`07:09` 点击ESC输入冒号WQ叹号回车
`07:12` 修改完配置文件以后
`07:14` 我们还需要这个命令来重启一下docker回车
`07:17` 镜像站配置好以后
`07:18` 我们再使用docker put命令
`07:20` 这样就不会报错了
`07:21` 成功的把镜像下载了下来
`07:23` 接下来我们在麦克或者windows电脑上配置镜像站
`07:27` 解决网络问题
`07:28` 我们打开docker desktop
`07:30` 找到设置docker engine
`07:32` 这里把配置文件的registry mirrors复制一下
`07:35` 在这里打一个逗号
`07:36` 然后把这一段粘贴过来
`07:38` 点击右下角的apply and restart
`07:40` 然后打开一个windows的命令行窗口
`07:43` docker p n减X可以成功的拉取到镜像
`07:46` 我们再来看两个跟docker镜像有关的命令
`07:49` 首先是docker images
`07:51` image就是镜像的英文
`07:53` 这个命令可以列出所有下载过的docker镜像
`07:56` 下一个命令是docker rmi RM
`08:00` 这里代表remove
`08:01` 也就是删除I是images的缩写
`08:04` 这样连起来就是删除镜像
`08:06` 接下来我们可以填写一个镜像的名字
`08:09` 或者填写一个id
`08:10` 这里我填写id再回车
`08:12` 这样就把服务器上的N键X的镜像删除了
`08:15` 再执行docker images
`08:17` 就看不到那个镜像了
`08:18` docker铺还有一个选项
`08:20` 两个横杠platform等号
`08:22` 某某某某表示拉取特定CPU架构的镜像
`08:26` docker镜像作为一个软件
`08:27` 在不同的CPU架构下面都有不同的版本
`08:30` 默认情况下
`08:31` docker铺命令会自动选择
`08:33` 最适合当前宿主机的CPU架构的镜像
`08:36` 大部分情况下我们不太需要关注镜像的CPU架构
`08:40` 不过有些个别情况
`08:41` 比如我们用香橙派这种迷你主机运行docker
`08:44` 香橙派作为一个低功率的小主机
`08:47` 它的CPU架构一般都是arm64
`08:49` 如果我们想在相城派上运行docker程序
`08:52` 我们就需要提前去docker hub上
`08:55` 确认一下镜像的架构
`08:56` 我们来到docker hub的官网
`08:58` 我现在想确认的是这个镜像
`09:00` reg flow的架构情况
`09:02` reg flow是一个AI大模型知识库的应用
`09:05` 第一个下载量最高的镜像
`09:06` 我们点击text
`09:08` 看到reg flow只提供AAMD64位架构的镜像
`09:11` 也就是说reg flow在香橙派上是不能运行的
`09:14` Mac电脑虽然也都是arm64架构的
`09:17` 但是Mac电脑有个好处
`09:19` docker desktop会自动使用QEUM来模拟X86
`09:23` 64指令集
`09:24` 从而可以兼容部分AMD64的镜像
`09:27` 不过代价就是可能会存在一些兼容性问题
`09:30` 或者带来额外的性能开销

### 运行容器 `09:33`

`09:34` docker run使用镜像创建并运行容器
`09:37` 这是本期视频最重要的命令了
`09:40` run后面可以接一个镜像的名字
`09:42` 或者接一个镜像的id
`09:43` 就像我们视频开头提到的
`09:45` 用模具做糕点的例子
`09:47` 这一步就是使用镜像创建一个容器
`09:50` 正如我们使用模具制作一个糕点
`09:52` 这样回车运行
`09:54` 这样我们就把容器启动起来了
`09:56` 然后我新开一个窗口
`09:57` 二我们可以执行这个命令
`09:59` docker PS PS也就是process status的缩写
`10:03` 查看进程状况
`10:05` 这个是一个LINUX上的经典命令
`10:07` 这个概念也被继承到docker里面
`10:10` 表示查看正在运行的容器
`10:12` 在这里有几个需要注意的
`10:13` 第一列container id容器的id
`10:16` 每创建一个容器就会自动分配一个唯一的id
`10:19` 接下来image就是基于哪个镜像创建出来的
`10:23` 对应视频开头的例子
`10:24` 就是这个高点是用哪个模具制作出来的
`10:27` 最后一个叫做names
`10:29` 也就是容器的名字
`10:30` 我们使用docker run命令的时候
`10:32` 如果没有手动设置名字
`10:34` 系统会自动分配一个随机的名字给它
`10:37` 我们再回到窗口一看到这里一直在刷日志
`10:41` 而且这个窗口已经被占用住了
`10:43` 没法做别的操作了
`10:45` 所以一种更常见的启动方式是
`10:47` 在docker run命令后面接一个横杠D
`10:50` 这里的D表示detached mode
`10:52` 也就是分离模式表示让容器在后台执行
`10:56` 不会阻塞当前的窗口
`10:58` 这里控制台只打印了一个容器id
`11:00` 后续容器的日志不会打印在控制台
`11:04` 也不会阻塞控制台的使用
`11:05` 我们还是可以使用docker PS命令
`11:08` 查看正在运行的容容器
`11:09` 我们注意到docker PS命令显示的容器id比较短
`11:13` 刚才那一串很长
`11:15` 这个短id其实是把长id截取了前面几位
`11:18` 但是它们的功能是一样的
`11:20` 在上面的例子里
`11:21` 我们先用docker put下载镜像
`11:23` 然后使用docker run创建并且运行容器
`11:26` 其实第一部里的docker铺可以省略
`11:29` 我们可以直接使用docker run命令
`11:31` 如果docker发现本地不存在镜像
`11:33` 它会先自动拉取一份
`11:35` 然后再创建并且运行容器
`11:37` 我们来看docker run的一个重要参数
`11:40` 杠P杠P的作用是端口映射
`11:42` 每个docker容器都运行在一个独立的虚拟环境里
`11:46` 面
`11:46` 容器的网络与宿主机是隔离的
`11:49` 默认情况下
`11:50` 并不能直接从宿主机访问到docker的内部网络
`11:53` 比如我在宿主机上直接安装一个N减X
`11:56` 在浏览器输入local house的八零
`11:59` 就能访问到这个NGX提供的网页
`12:01` 但是我使用docker来启动NGX
`12:04` 在浏览器输入local host
`12:06` 八零则无法访问到对应的网页
`12:08` 因为容器内的网络与宿主机的网络是隔离的
`12:12` 这时候我们需要添加一个启动参数
`12:14` 把容器内的端口与宿主机的端口进行一个映射
`12:17` 或者说绑定
`12:18` 我们可以添加这个杠P参数
`12:21` 在冒号前后各填写一个端口
`12:23` 冒号前面是宿主机的端口
`12:26` 冒号后面是容器内的端口
`12:28` 顺序是先外后内
`12:29` 也就是把宿主机的八零端口
`12:32` 转发到容器内的八零端口进行处理
`12:34` 这样我们在浏览器访问local host80
`12:37` 也就是访问宿主机端口的时候
`12:39` 就等于访问到了容器内的端口
`12:42` 网页也就正确的显示出来了
`12:44` 我用我的windows电脑来试一下docker run杠P
`12:48` 然后宿主机的端口八零容器内的端口
`12:51` 八零镜像的名字是NGX
`12:53` 然后我打开浏览器
`12:54` 输入local host端口八零回车
`12:57` 这样就打开了容器内部署的NGX

### 挂载卷 `13:01`

`13:02` 与杠P绑定端口类似的参数是杠V
`13:05` 杠P是把宿主机与容器的端口进行绑定
`13:08` 杠V则是把宿主机与容器的文件目录进行绑定
`13:12` 容器内对这个文件夹的修改
`13:14` 会影响宿主机的文件夹
`13:16` 而在宿主机对文件夹的修改
`13:18` 同样会影响容器内的文件夹
`13:20` 宿主机与容器
`13:21` 通过这个目录紧密地联系到了一起
`13:24` 这种目录也被称为是挂载卷
`13:26` 挂载卷的最大作用是数据的持久化保存
`13:30` 因为当我们删除容器的时候
`13:32` 容器内的所有数据会被同时删除掉
`13:35` 如果我们使用了挂载券
`13:37` 容器内对应目录的数据
`13:38` 就会保存在宿主机的对应目录里面
`13:41` 这样删除容器的时候
`13:43` 保证了数据不会被删除
`13:45` 相当于把数据做了一个持久化的保存
`13:48` 我们来看一个具体的例子
`13:49` 实战一下刚才那三个重要参数
`13:52` docker run杠D表示容器在后台运行
`13:55` 杠P表示
`13:56` 把宿主机的端口与容器内的端口做一个绑定
`14:00` 杠
`14:00` V表示
`14:01` 把宿主机的目录跟容器内的目录做一个绑定
`14:05` 我们看到容器内的这个目录一般是N减
`14:08` 存放静态网页的目录
`14:10` 这里又做了端口映射
`14:11` 我们访问宿主机的八零端口
`14:13` 也就等于访问我们容器的八零端口
`14:15` 这样启动起来
`14:16` 我们试着访问一下宿主机的八零端口
`14:19` 这里403这里的原因是使用绑定挂载的时候
`14:23` 宿主机的目录会覆盖掉容器内的目录
`14:25` 因为宿主机这边的目录是空的
`14:27` 所以容器内也没有任何的网页
`14:30` 所以访问的时候显示403
`14:32` 我们可以直接修改宿主机的目录
`14:34` 把文件加载宿主机的对应目录
`14:37` 下面就等于修改了容器内的目录
`14:39` 接下来我们进入宿主机的目录
`14:41` 然后我创建一个文件速度
`14:43` vi index点HTML
`14:46` 我把一个网页文件的代码粘贴过来
`14:48` 然后ESC冒号WQ叹号回车
`14:51` 这样我们再访问一下八零端口
`14:53` 可以看到就展现出了一个小网页
`14:55` 在刚才的例子里面
`14:57` 直接把宿主机的目录写在了docker run命令里面
`15:00` 这种挂载方式叫做绑定挂载
`15:03` 还有另外一种方式是
`15:04` 让docker自动创建一个存储空间
`15:07` 我们为这个存储空间起一个名字
`15:09` 然后挂载的时候直接使用名字就可以了
`15:12` 这种方式叫做命名卷挂载
`15:14` 载开始之前
`15:15` 我们需要先把刚才的容器清理掉
`15:18` 执行这个命令
`15:19` docker PS我们看到这是刚才创建的容器
`15:22` 然后docker RM后面接容器的id表示删除容器
`15:27` 不过删除这种正在运行的容器
`15:30` 我们需要再加一个横杠F强制删除
`15:33` 这里我再多提一嘴
`15:34` 删除容器是docker RM
`15:36` 删除镜像是docker rmi
`15:39` 回车
`15:39` 接下来我们使用命令新创建一个挂载卷
`15:42` Docker volume create
`15:43` 后面是卷的名字
`15:45` 这里叫NGXHTML
`15:47` 然后我们在docker run的时候
`15:49` 这里就不需要输入目录了
`15:51` 我们直接可以使用挂载券的名字回车
`15:55` 这个挂载卷在宿主机的真实目录是在哪里呢
`15:58` 我们可以这么执行
`15:59` Docker volume inspect
`16:02` 后面接挂载卷的名字回车
`16:04` 这里显示这个名字的挂载卷
`16:07` 它的真实目录是在这里
`16:08` 不过进入这个目录
`16:10` 我们需要先切换成root用户
`16:12` 这里速度杠I切换成root用户
`16:14` 然后进入这个目录
`16:16` 接下来我们看一下index文件
`16:18` 看到这里面已经有一些数据了
`16:20` 这个是命名卷的一个特别功能
`16:22` 命名卷第一次使用的时候
`16:24` docker会把容器的文件夹同步到命名卷里面
`16:27` 进行一个初始化
`16:29` 而绑定挂载就没有这个功能
`16:31` 我们还是把自己的网页文件替换上
`16:33` 最后保存一下
`16:35` 接下来再访问服务器的IP地址
`16:37` 加八零端口
`16:38` 就能看见我们创建的网页了
`16:40` 命令刀docker volume list可以列出所有创建过的卷
`16:44` docker volume remove可以删除一个卷
`16:47` Docker volume prune
`16:49` 后面接杠A删除所有没有任何容器在使用的券
`16:53` 输入Y回车

### run其他参数 `16:56`

`16:56` 下一个参数杠E用来往容器里面传递环境变量
`17:00` 比如现在我想启动一个MONGODB的容器
`17:03` MONGODB作为一个数据库使用的时候
`17:05` 一般需要设置账号名密码
`17:07` 所以我们可以像这样使用两个杠E参数
`17:10` 把数据库的账号名密码作为环境变量传递进去
`17:14` 这样启动的时候
`17:15` 数据库就会预设上账号名密码
`17:17` 启动成功以后
`17:18` 我从这台windows电脑上连接一下我们的数据库
`17:21` MONGOSH账号名冒号密码
`17:24` 后面是服务器的公网IP地址
`17:26` 接下来端口27017能够成功连接进来
`17:30` 这样账号名密码是生效的
`17:32` 可以成功连接进来
`17:33` 如果不清楚容器的环境变量有哪些
`17:36` 我们可以来到docker hub上面搜索一下
`17:38` 比如这里我搜索MONGO这个镜像
`17:41` 在下面的文档里就列出了可以传递的环境变量
`17:44` 如果是开源项目的话
`17:45` 我们可以去GITHUB仓库找一下
`17:47` 一般都会有详细的说明
`17:49` 我们来看下一个docker run命令
`17:51` 重要的参数
`17:52` 两个横杠
`17:53` 内幕后面可以给容容器起一个自定义的名字
`17:56` 这里要注意的是
`17:57` 这个名字在整个宿主机上必须是唯一的
`18:00` 不能重复
`18:01` 我们来执行一下
`18:02` 然后使用docker PS命令
`18:04` 可以看到正在运行的容器
`18:06` 看到容器names也就是名字
`18:08` 这里变成了我们自定义的那个名字
`18:10` 这个容器的名字跟前面的容器id的功能
`18:13` 是等价的
`18:14` 名字更加方便于记忆
`18:16` 比如现在我要把容器删除掉
`18:18` 我们执行这个命令
`18:19` docker RM后面接容器的名字就可以了
`18:23` 正在运行的容器需要接一个杠F
`18:25` 这样通过名字也成功删除了镜像
`18:28` 还有一对重要参数
`18:29` 我们一起来看一下
`18:31` 杠i it可以让我的控制台进入容器进行交互
`18:34` 杠杠RM指的是当容器停止的时候
`18:37` 就把容器删除掉
`18:39` 这次我用的镜像是LPAO盘
`18:41` 是一个轻量级的LINUX系统
`18:43` 我们来启动一下
`18:44` 看到这样直接进入到了容器内部
`18:47` 然后我们用exit退出以后
`18:49` 这个容器同时被从宿主机上删除了
`18:52` 所以这套命令一般连在一起用
`18:54` 用于临时调试一个容器
`18:56` 最后一个重要参数是两个横杠
`18:59` Restart
`18:59` 用来配置容器在停止时的重启策略
`19:02` 常用的有两个横杠
`19:04` restart后面跟always
`19:06` 也就是只要容器停止了
`19:08` 就会立即重启
`19:09` 包含容器
`19:10` 因为内部错误崩溃或者宿主机断电等等的场景
`19:13` 还有一个类似的是把always换成unless stopped的
`19:17` 这个跟always非常的相似
`19:19` 有一个关键区别是
`19:21` 手动停止的容器就不会尝试重启了
`19:24` 这对于生产环境非常有用
`19:26` docker会自动重启
`19:28` 因为意外原因停止的容器
`19:30` 而手动停止的容器就不会再重启了

### 调试容器 `19:34`

`19:35` 我们再来理解一下docker run命令
`19:37` docker run命令是从镜像创建并且运行容器
`19:40` 也就意味着我们每次执行docker run
`19:43` 都会创建一个全新的容器
`19:45` 如果我们不想创建容器
`19:47` 只想对原有的容器进行一个启停
`19:49` 那可以使用以下两个命令
`19:51` 我们先用docker run启动一个MONGODB的容器
`19:55` 接下来使用docker PS命令
`19:57` 可以查看这个正在运行的容器
`19:59` 使用docker stop命令后面接容器的id或者名字
`20:04` 可以停止这个容器的运行
`20:06` 然后再使用docker PS命令
`20:08` 就看不到那个容器了
`20:10` 因为PSS命令只能看到正在运行的容器
`20:13` 我们可以在PS命令后面加一个横杠A
`20:16` 横杠A的意思是all
`20:17` 查看所有的容器
`20:19` 包括正在运行的和已经停止的
`20:21` 可以使用docker start命令后面接容器的id或者名字
`20:26` 把一个容器重新启动起来
`20:28` 这里的status显示六秒之前启动起来了
`20:31` 我们使用stop还有start启停容器的时候
`20:34` 之前写过的端口映射挂载卷
`20:36` 还有环境变量等等的参数
`20:38` 都不需要再重新写一遍了
`20:40` docker已经为我们记录并且保存下来了
`20:43` 我们只需要重新启动就可以照原样运行
`20:46` 如果我们忘记了容器启动时填的参数是什么
`20:49` 可以执行这个命令
`20:51` docker inspect后面接容器的id
`20:54` 看到docker这里打印了非常长一串
`20:56` 但是这种我们也不需要自己分析
`20:58` 直接把它贴给AI就行了
`21:00` 我问AI容器有没有做端口映射
`21:03` 有没有做挂载卷
`21:04` 这里给出了答案
`21:05` 首先做了八零端口的端口映射
`21:07` 同样的也给出了挂载卷的答案
`21:09` 把宿主机的这个目录
`21:11` 挂载到了容器内的这个目录
`21:13` 虽然docker给的信息非常的复杂
`21:15` 但是AI还是能够精准的进行判断
`21:18` 当然这个docker inspect的命令还有很多功能
`21:21` 可以查看很多关于容器的信息
`21:23` 不过这里我就不挨个讲了
`21:25` 感兴趣的话可以多跟AI交流
`21:27` 询问他这些信息都是做什么用的
`21:30` docker create命令与docker run命令非常的类似
`21:33` 唯一点区别是docker create命令只创建容器
`21:36` 但不立即启动
`21:38` 如果想启动的话
`21:39` 还需要输入docker start命令
`21:41` 后面接容器的id就可以了
`21:43` docker locks命令后面接容器的id或者名字
`21:47` 可以查看容器的日志
`21:48` 还有一种更常见的方式
`21:50` 在logs后面加上杠F表示滚动查看日志
`21:54` 比如这里我登录一下MONGODB
`21:57` 我们看到后面的日志也同步进行了刷新
`22:00` 简单补充下docker的技术原理
`22:02` docker利用了LINUX内核的两大原功能
`22:05` 实现容器化
`22:06` c groups用来限制和隔离进程的资源使用
`22:09` 可以为每个容器设定CPU内存
`22:12` 网络带宽等资源的使用上限
`22:14` 确保了一个容器的资源消耗
`22:16` 不会影响到宿主机或者其他正在运行的容器器
`22:20` Name spaces
`22:21` 用来隔离进程的资源视图
`22:23` name spaces使得容器只能看到自己内部的进程
`22:26` id网络资源和文件目录
`22:28` 而看不到宿主机的
`22:30` 所以容器本质上还是一个特殊的进程
`22:33` 不过当我们进入到容器内部后
`22:35` 容器内部看起来就像一个独立的操作系统
`22:38` 每个docker容器都是一个独立的运行环境
`22:42` 每个容器内部表现的都像一个独立的LINUX系统
`22:45` docker ex e c可以在容器的内部执行LINUX命令
`22:49` 我想进入这个容器
`22:51` 查看一下里面正在运行的进程
`22:53` 我们可以执行这个命令
`22:54` docker ex e c后面是容器的id或者名字
`22:59` 最后是你想执行的LINUX命令
`23:01` 这里查看进程一般是PS横杠EF回车
`23:05` 这样就看到了容器里面的进程情况
`23:07` 因为容器内进行了资源视图的隔离
`23:10` 所以这里看不到宿主机的进程
`23:12` 还有一个常用用法
`23:13` 使用这个命令
`23:14` 可以进入一个正在运行的docker容器内部
`23:17` 获得一个交互式的命令行环境
`23:20` 容器内部表现出来的就像一个独立的操作系统
`23:23` 我们可以进入容器内部执行LINUX命令
`23:26` 查看文件系统
`23:27` 管理容器内的进程
`23:29` 或者更加深入地进行调试
`23:31` 我们先创建并且运行一个NGX容器
`23:34` 这里做了端口映射
`23:36` 从浏览器访问服务器的IP地址
`23:38` 就可以看到这个NG的网页
`23:40` 接下来我们进入容器
`23:42` 把这个网页修改一下
`23:43` docker e x e c杠T后面是容器的id
`23:47` 这里也可以用这个长id
`23:49` 接下来bean s h
`23:51` 接下来的所有命令都是在容器内执行的
`23:54` 我们进入到容器内存放静态网页的目录
`23:57` 目录
`23:57` 下面有两个网页文件
`23:59` 这里使用vi命令修改一下index
`24:01` 我们看到这里显示找不到vi命令
`24:04` docker为了尽量压缩镜像的大小
`24:06` 它内部一般是一个极简的操作系统
`24:09` 也就是很多系统工具可能都是缺失的
`24:12` 这时候我们自己来安安装一下就可以了
`24:14` 在容器内安装软件之前
`24:16` 我们需要先执行这个命令
`24:17` 查一下容器内的LINUX是什么发行版的
`24:20` 我们看到这里写的是待变的
`24:22` 待变安装软件的命令就是apt
`24:25` 先更新一下apt索引
`24:28` 接下来apt install vim
`24:31` 接下来使用vi命令修改一下index点HTML
`24:36` 这里改成welcome to tetrp
`24:38` 保存一下
`24:39` 再回到浏览器
`24:40` 刷新一下页面
`24:41` 看到网页的标题这里更改过来了

### 构建镜像 `24:45`

`24:45` docker file是什么
`24:46` 在视频开头的例子里面
`24:48` 容器是用镜像这个模具生产出来的糕点
`24:51` 镜像是制作糕点的模具
`24:53` 而docker file就是制作模具的图纸
`24:56` docker file是一个文件
`24:57` 里面详细的列出了镜像是如何制作的
`25:00` 接下来的例子里面
`25:01` 我们使用docker file制作一个镜像
`25:04` 并且把它推送到docker hub上面
`25:06` 我们来看这个例子
`25:07` 这是一个非常简单的Python程序
`25:09` 使用fast API创建了一个get接口
`25:12` 它运行在8000端口上面
`25:14` 还有一个requirements文件里面
`25:16` 列出了程序的Python依赖
`25:18` 我们想在本地把这个代码运行起来
`25:20` 只需要三个步骤
`25:22` 第一步给电脑安装Python环境
`25:24` 第二步安装依赖pip install杠r requirements
`25:28` 点TXT
`25:29` 第三步启动项目Python main点PY
`25:32` 然后浏览器访问local host
`25:34` 8000
`25:35` 打印出了hello world
`25:36` 我们的程序就运行成功了
`25:38` 把这么一个程序打包成docker镜像
`25:40` 原理也差不多
`25:41` 我们先创建一个docker file文件
`25:44` 注意D要大写
`25:45` 而且这个文件没有任何的后缀名
`25:47` 所有docker file第一行都是from
`25:50` 选择一个基础镜像
`25:52` 也就是我们的镜像
`25:54` 从哪个镜像的基础上面构建而来的
`25:56` 我们希望基础镜像里面自带Python
`25:59` 在docker hub上面搜索Python
`26:01` 我选择这个这个基础镜像的LINUX发行版是slim
`26:05` 里面内置了python3.13冒号
`26:07` 3.13slime
`26:09` 这个work dir有点像CD切换到镜像内的一个目录
`26:13` 作为工作目录
`26:14` 后面的命令都是在这个目录下面执行的
`26:16` 然后我们使用copy
`26:18` 把我们的代码文件拷贝到镜像内的工作目录
`26:21` 后面写两个点儿
`26:22` 第一个点代表我这台电脑的当前目录
`26:25` 第二个点代表镜像内的当前工作目录
`26:27` 也就是这个斜杠app
`26:29` 接下来安装依赖
`26:30` 我们安装依赖的命令就是pip install
`26:34` 在前面加一个大写的run
`26:35` 表示这个命令要在镜像里面执行
`26:38` Expose
`26:38` 8000
`26:39` 这里声明我镜像提供服务的端口是哪个
`26:42` 因为我的fast API启动的时候使用8000端口
`26:45` 这个expose并不是强制的expose
`26:48` 在这里只是做一个声明
`26:50` 给其他使用镜像的人做一个提示
`26:53` 即使不写
`26:54` 对镜像的功能也没有影响
`26:55` 实际使用的时候还是以杠P参数为准
`26:58` 接下来最后一行
`26:59` CMD是容器运行时的默认启动命令
`27:03` 也就是每当容器启动的时候
`27:05` 容器内部会自动执行这个命令
`27:07` 使得容器内有一个Python程序在运行命令
`27:10` 最好写成这种数组的形式
`27:12` 中间最好不要用空格
`27:14` 一个docker file文件里面只能写一个CMD
`27:17` 与CMD类似的是entry point
`27:20` entry point的优先级更高
`27:22` 它不容易被覆盖
`27:23` docker file文件准备好以后
`27:25` 我们就可以构建镜像
`27:26` 在当前目录执行docker build
`27:29` 后面杠T杠T后面可以给镜像起一个名字
`27:32` 我叫docker test
`27:34` 名字后面可以加冒号
`27:35` 输入一个版本号
`27:36` 这里也可以省略不写
`27:38` 最后点指的是在当前文件夹构建
`27:40` 这样我们的镜像就构建好了
`27:42` 然后我们可以使用docker run命令
`27:44` 基于这个镜像创建一个容器
`27:46` 并且运行起来
`27:47` docker run杠D后面杠P做一下端口映射
`27:51` 把容器内的8000端口映射到宿主机上
`27:54` 最后是镜像的名字叫docker test
`27:57` 回车在浏览器还是输入local host8000
`28:00` 这样看到了输出结果
`28:01` 在docker desktop这边
`28:03` images里面显示了我们构建出来的镜像
`28:06` container里面显示了正在运行的容器
`28:09` 接下来我们把镜像推送到docker hub上面
`28:12` 这里先来到docker hub的首页
`28:14` 没有账号的话
`28:15` 点击3up注册一个
`28:16` 我选择GITHUB登录
`28:18` 登录完成
`28:19` 给自己起一个用户名
`28:20` 这个就是我们自己在docker hub上面的命名空间
`28:23` 点击sign up
`28:24` 我们想把自己的镜像推送到docker hub上面
`28:27` 需要先登录一下
`28:28` 这里输入docker login
`28:30` 这里给了一个英文验证码
`28:32` 还有一个网站
`28:33` 我们打开这个网站
`28:34` 把验证码填写过来
`28:35` 回到命令行显示login succeed就成功了
`28:38` 接下来我们重新打一个镜像
`28:40` 因为我们要推送镜像的时候
`28:42` 必须在前面带上自己的用户名
`28:45` 所以这里我在前面填上自己的用户名回车
`28:48` 接下来最后一步
`28:49` Docker push
`28:50` 后面是镜像的名字
`28:52` 注意这里要带着自己的用户名回车
`28:55` 我们看到镜像就推送到了docker hub上面
`28:57` 这是仓库的地址
`28:59` 后面是命名空间
`29:00` 也就是我的用户名
`29:02` 最后是镜像的名字
`29:04` 推送成功以后
`29:05` 用我的命名空间加镜像的名字
`29:07` 在docker hub上面就能搜索到我们推送上来的镜像
`29:11` 其他小伙伴使用docker put命令
`29:13` 就能下载到这个镜像

### Docker网络 `29:16`

`29:16` docker网络默认是bridge
`29:18` 也就是桥接模式
`29:19` 所有的容器默认都连接到这个网络
`29:22` 每个容器都分配了一个内部IP地址
`29:25` 一般是172.17开头的
`29:27` 在这个内部子网里面
`29:29` 容器可以通过内部IP地址互相访问
`29:32` 但容器网络与宿主机的网络是隔离的
`29:35` 我们可以使用docker network create创建出子网
`29:39` 默认情况下创建出来的子网
`29:41` 也是属于桥接模式的一种
`29:43` 然后可以指定容器加入不同的子网
`29:46` 同一个子网的容器可以互相通信
`29:48` 而跨子网则不可以通信
`29:50` 创建子网
`29:51` 还有一个好处是
`29:53` 同一个子网的容器
`29:54` 可以使用容器的名字互相访问
`29:57` 而不必使用内部IP地址
`29:59` 我们来试一下
`30:00` 首先第一个命令创建一个叫做network1的子网
`30:03` 第二个命令创建并且运行一个MONGODB的容器
`30:07` 我们看到这里使用杠杠network指定它加入子网一
`30:11` 启动MONGODB的时候
`30:12` 没有使用杠P参数进行端口映射
`30:15` 也就是使用宿主机是没法访问到这个MONGODB的
`30:18` 我们先把它启动起来
`30:19` 接下来启动一个叫做mongo express的容器
`30:22` 这个容器就是MONGODB的网页客户端
`30:25` 同样的把它加入到子网一里面
`30:27` 因为跟MONGODB属于同一个子网
`30:30` 所以它们之间是可以互相访问的
`30:32` mongo express上面做了端口映射
`30:35` 我们注意到这里传了一个环境变量
`30:37` 这里没有传数据库容器在子网的IP地址
`30:40` 而是直接把名字告诉了他
`30:42` 在浏览器输入服务器的IP地址
`30:44` 端口8081
`30:46` 第一次访问的时候要登录一下
`30:48` 这样成功登录进来
`30:49` 我们可以创建数据库
`30:51` 数据库里面可以做查询
`30:53` 可以做新增等等等等的功能
`30:55` 我们来看一下网络的拓扑结构
`30:57` 两个容器分配在同一个子网里面
`31:00` 它们可以互相通信
`31:01` 这里注意到我启动第二个容器mongo express的时候
`31:05` 没有告诉第二个容器
`31:07` 关于第一个容器在子网里面的IP地址
`31:10` 而是仅仅告诉了他第一个容器的名字
`31:13` 借助于刀客内部的DNS机制
`31:15` 通过名字就可以在子网内进行互相的访问
`31:18` 我们来看一下它的效果
`31:20` 这里我进入到第二个容器mongo express里面
`31:24` 然后在容器里面执行这个命令
`31:26` ping后面接的是第一个容器的名字
`31:29` 我们看到这里打印出来了第一个容器的IP地址
`31:32` 换句话说
`31:33` docker子网内部有一个DNS机制
`31:36` 可以把名字转换成IP地址
`31:38` 接下来我又做了一个端口映射
`31:40` 让浏览器可以访问到mongo express
`31:43` 然后mongo express又可以通过内部子网访问到MONGODB
`31:48` 这样达成最终的效果
`31:50` 这样我们就看到了默认的桥接模式
`31:52` 还有创建子网的桥接模式
`31:54` 接下来我们看另外一种网络模式
`31:56` 另外一种常见的网络是host模式
`31:59` docker容器直接共享宿主机的网络
`32:01` 容器直接使用宿主机的IP地址
`32:04` 而且无需杠P参数进行端口映射
`32:07` 容器内的服务直接运行在宿主机的端口上
`32:10` 通过宿主机的IP和端口就能访问到容器
`32:14` host模式可以解决一些非常棘手的网络问题
`32:17` 我们来使用host模式启动一个N减X
`32:20` 这里在启动参数加上两个横杠network
`32:24` 后面host我们没有进行端口映射
`32:26` 在浏览器直接访问服务器的IP地址
`32:29` 加端口八零就可以访问到NGX
`32:32` 我们进入容器内看一下它的IP地址
`32:35` 在容器内部查看IP地址
`32:37` 我们需要装一些工具
`32:38` 首先a apt update
`32:40` 然后装这个工具
`32:41` Apt install ip router to
`32:44` 然后就可以查IP地址了
`32:46` Ip a d d r show
`32:48` 我们看到容器内显示的IP地址
`32:51` 主要是这个内网IP地址
`32:53` 跟我在云服务器的控制面板上看的内网IP地址
`32:56` 是一模一样的
`32:57` 从这个例子可以看出
`32:59` docker容器使用host模式
`33:01` 它直接共享了宿主机的网络空间
`33:03` 最后一种docker网络是NN模式
`33:05` 也就是不联网
`33:06` docker network list展示出所有docker网络
`33:10` 首先有默认的桥接模式
`33:12` host模式
`33:13` 还有NN模式
`33:14` 最后一个是我们刚才创建的子网
`33:16` 当然这个子网也是属于桥接网络的一种
`33:18` 这三种默认网络是不能删除的
`33:21` 我们可以使用命令docker network remove命令
`33:25` 删除自定义的子网
`33:26` 比如把它删除掉
`33:27` 再使用list命令就看不到刚才的子网了

### Docker Compose `33:31`

`33:31` 有些时候
`33:32` 一个完整的应用可能是很多部分组成的
`33:35` 比如前端后端数据库等等
`33:37` 这时候怎么用刀把它们容器化呢
`33:40` 我们可以很自然的想到
`33:42` 把这些模块都打包在一起
`33:44` 做成一个巨大的容器
`33:45` 但这样只要有一个模块发生了故障
`33:48` 比如后端出现了内存泄漏
`33:50` 整个容器都有可能崩溃
`33:51` 并且可伸缩性差
`33:53` 如果想给系统扩容
`33:55` 只能把整个大容器再复制一份
`33:57` 而做不到针对某和模块的精准性扩容
`34:00` 多应用的最佳实践是
`34:02` 把每一个模块都打包成一个独立的容器
`34:05` 不过这样使用多容器增加了很多的使用成本
`34:08` 因为我们想创建多个容器
`34:10` 就得多次执行docker run
`34:12` 还得把它们之间的网络配置好
`34:14` 尝试管理这些容器的时候
`34:16` 很多地方都会出错
`34:17` 这时候一个容器编排技术就很有用
`34:20` 也就是docker compose
`34:22` docker compose使用YML文件管理多个容器
`34:25` 里面列出了容器之间是如何创建
`34:28` 以及如何协同工作的
`34:30` 我们可以简单的把docker compose文件理解成
`34:33` 一个或者多个的docker run命令
`34:35` 按照特定的格式列到了一个文件里面
`34:38` 这是刚才视频里面展示的
`34:40` 使用docker命令创建MONGOD
`34:42` 比喻mongo express的例子
`34:44` 我们先创建一个子网
`34:46` 然后创建mongo dB和mongo express两个容器
`34:49` 并且把它们加入子网
`34:51` 右侧展示出的是对应的docker compose文件的写法
`34:55` 左右两边有非常工整的对应关系
`34:57` 右侧最顶级的是services元素
`35:00` 每个服务也就是一个service都对应一个容器
`35:04` 左侧的杠杠内幕
`35:06` 也就是容器名在右侧就变成了service名
`35:09` 左侧的镜像名在右侧写在了image后面
`35:12` 也表示镜像名
`35:13` 接下来左边的杠E参数对应右边的environment
`35:17` 都是环境变量的意思
`35:19` 左侧的杠V对应右侧的volume
`35:21` 也就是挂载卷
`35:22` 左侧的杠P对应右边的pose
`35:25` 也就是端口映射
`35:26` 左右两边唯一一点区别
`35:28` 就是左边自定义了一个子网network1而右边没有
`35:32` 因为docker会为每一个compose文件
`35:34` 都自动创建一个子网
`35:36` 同一个compose文件里面
`35:38` 定义的所有容器都会自动加入同一个子网
`35:41` 不用我们操心docker compose
`35:43` 还有一个额外功能是
`35:45` 可以自定义容器的启动顺序
`35:47` 比如我们可以在mongo express下面加上depend on
`35:50` 他的意思是这个mongo express容器依赖数据库
`35:54` 所以程序会先把mongo dB的容器启动起来
`35:58` 再去启动mongo express的容器
`36:00` 在AI时代也不用我们写docker compose文件了
`36:04` 我们只需要把想要执行的docker命令告诉AI
`36:07` 然后让它生成一个等价的docker
`36:09` compose文件就可以了
`36:13` 我们来实战一下
`36:14` 首先使用vi命令
`36:16` 在服务器上创建一个docker compose文件
`36:19` 把文件内容粘贴过来保存一下
`36:21` 然后我们执行docker compose up
`36:24` 这样就是启动这个文件里面定义的所有容器
`36:27` 同样的我们可以在后面接杠D让它在后台运行
`36:31` 我们看到这个命令做了两件事
`36:33` 首先他创建了一个子网
`36:34` 然后创建了两个容器
`36:36` 这个容器的名字跟我们在compose文件里面
`36:39` 定义的service名字不太一样
`36:41` 它在前面加了一个前缀
`36:43` 还加了一个编号
`36:44` 用来更加方便的管理
`36:46` 我们执行docker PS命令
`36:48` 可以看到正在运行的容器
`36:50` 在浏览器访问服务器的IP地址加端口
`36:53` 8081
`36:54` 看到mongo express就在正常运行了
`36:57` 我们可以新增数据库等等做各种操作
`37:00` 刚才我们看了compose up命令
`37:02` 与它相反的就是compose down
`37:04` 这个命令会停止并且删除容器
`37:07` 我们使用docker PS杠A看到容器不但被停止了
`37:11` 也被删除了
`37:13` 如果我们只想停止
`37:14` 但是不想删除容器
`37:16` 我们可以执行docker compose stop命令
`37:19` 这个命令只停止不删除
`37:21` 然后我们执行docker compose start命令
`37:24` 就可以把刚才stop命令停止的容器再启动起来
`37:28` 如果容器已经在运行了
`37:30` 我们重复执行compose up命令
`37:32` 并不会启动新的容器
`37:34` 它没有任何的效果
`37:35` 我们执行docker compose命令的时候
`37:38` 它会自动识别当前目录
`37:40` 下面严格叫做docker compose的这个文件
`37:43` 如果我们把这个文件改一个名字
`37:46` 让它变成一个非标准的文件名
`37:48` 这样再使用docker compose命令
`37:50` 它就识别不到了
`37:52` 对于这种非标准的文件名
`37:54` 我们可以在docker compose后面接一个杠F
`37:58` 杠F指的是file
`37:59` 也就是指定文件名
`38:01` 接下来我们把非标准的文件名指定给它
`38:04` 当然这个文件名也可以在其他的目录下面
`38:07` 使用杠F命令就可以识别到对应的文件好
`38:10` docker compose的内容就这么多
`38:12` 还是非常容易掌握的
`38:14` docker compose是一个轻量级的容器编排技术
`38:18` 它适合个人使用单机运行
`38:20` 对于企业级服务器集群大规模的容器编排需求
`38:24` 那就需要另外一个软件cooper nice登场
`38:27` cooper nice的功能非常的复杂
`38:29` 这个有机会我们再聊
`38:33` 本期视频我们先介绍了docker的核心概念
`38:36` 主要有容器镜像
`38:37` 还有镜像仓库
`38:39` 演示了如何在linux windows
`38:41` 还有Mac电脑上安装docker
`38:43` 使用docker铺命令下载镜像
`38:45` 以及如何配置镜像站
`38:47` 解决下载镜像的网络问题
`38:49` 接下来我们使用docker run命令创建并且运行了容器
`38:53` 介绍了docker run命令的几个重要参数
`38:55` 杠P参数
`38:56` 端口映射杠V挂载卷杠E设置环境变量等等
`39:00` 接下来介绍了如何进入容器内部进行调试
`39:04` 然后我们还看了docker的几种网络
`39:06` 分别是bridge模式
`39:08` 创建子网
`39:09` host模式与NN模式
`39:11` 我们学习了什么是docker file
`39:13` 如何使用docker file创建镜像
`39:15` 以及如何把镜像推送到docker hub上面
`39:18` 视频的最后
`39:19` 我们还介绍了一种轻量级的容器编排技术
`39:22` Docker compose
`39:23` 这就是本期视频的全部内容了
`39:26` 感谢大家
`39:26` 我们下期再见
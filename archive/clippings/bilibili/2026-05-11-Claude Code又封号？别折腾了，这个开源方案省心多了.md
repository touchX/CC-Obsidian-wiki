---
title: "Claude Code又封号？别折腾了，这个开源方案省心多了"
url: "https://www.bilibili.com/video/BV1u1cxzrEGM?spm_id_from=333.788.player.switch&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1u1cxzrEGM"
cid: "35989815519"
author: "程序员阿江-Relakkes"
upload_date: "2026-02-11"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116052012834111&bvid=BV1u1cxzrEGM&cid=35989815519&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Claude Code又封号？别折腾了，这个开源方案省心多了
被封号折腾够了之后，我找到了一个省心的替代方案——OpenCode + Oh My OpenCode + 接口AI。
功能差不多，但不用翻墙、不用担心封号、模型随便切。
⏱️ 时间戳：
00:00 被封号后的新选择
00:28 OpenCode是什么
00:32 OpenCode的独特优势
00:52 oh-my-opencode多Agent插件
01:12 OpenCode整体架构解析
01:50 多Agent多模型协作机制
02:21 为什么需要多个代理
03:25 安装OpenCode和插件
04:56 配置多模型和Agent
06:20 用接口AI解决封号问题
07:30 配置文件详解
08:46 实战演示多Agent协作
13:04 总结与推荐
🔗 相关链接：
• 接口AI（注册送$3）：https://jiekou.ai/referral?invited_code=HNUXWY
• OpenCode：https://github.com/anomalyco/opencode
• Oh My OpenCode：https://github.com/code-yeongyu/oh-my-opencode

## 章节

- `00:00` 开场
- `00:27` OpenCode介绍
- `00:51` oh-my-opencode插件
- `01:50` 多Agent机制
- `03:24` 安装教程
- `05:15` 模型配置
- `06:18` 接口AI中转站
- `08:46` 实战演示
- `13:04` 总结

## 字幕

### 开场 `00:00`

`00:00` 这是我被color code封的第三个号了
`00:02` 申诉也没有用
`00:02` 我现在已经准备放弃官方订阅了
`00:04` 不想折腾了
`00:05` 那open code一直很火
`00:07` 最近我也在开始用它了
`00:09` 尤其是cloud of4.6和GBT5.3codex出来之后
`00:13` 我现在是多个模型配合去干活
`00:16` 那发挥每个模型的优势
`00:17` 用4.6去做架构设计
`00:19` 用5.3codex去做代码执行
`00:22` 如果有前端的一个UI需求的话
`00:24` 再拿GEMINI3.0pro去做前端的一个UI
`00:26` 那open code到底是什么呢

### OpenCode介绍 `00:27`

`00:28` 用一句话
`00:28` 它其实就是一个开源版的cloud code
`00:30` 功能上的话
`00:31` 两者差不多
`00:32` 但open code有一些独特的优势
`00:34` 就是它是一个任意模型
`00:36` 你可以支持多家厂商的
`00:37` 你也可以用国产的
`00:39` 你也可以用国外的模型都支持
`00:40` 第二点就是代码完全开源免费
`00:43` 这一点非常重要
`00:44` 你可以自己去改
`00:45` 自己去定制
`00:46` 同时代码开源之后
`00:47` 那开源社区的人就可以去围绕它做很多功能
`00:50` 毕竟人多量大嘛

### oh-my-opencode插件 `00:51`

`00:51` 比如这个插件
`00:52` Oh my open code
`00:53` 这就是社区去搞的
`00:54` 现在已经30k star了
`00:56` 这个插件其实是一套多A进的
`00:58` 多模型去协作干活的一个插件
`01:01` 最近我体验下来也挺好用的
`01:02` 他这边准备了十个agent
`01:04` 特有主控A镜
`01:05` 特有去专门干代码的
`01:07` 专门去探索代码的
`01:08` 要专门去做前端UI的
`01:09` 挺好用的
`01:11` open code整个的架构的话
`01:13` 其实跟那个CLOUCODE差不多
`01:15` 用户输入层有会话管理嘛
`01:17` 然后有agent的
`01:18` 有plan就计划
`01:19` 那cloud code那边也有
`01:21` 那有build就是直接干活的嘛
`01:22` 还有一些探索的
`01:23` 其实CALLCODE那边不是都有吗
`01:25` 以及你自定义一些agent特都有
`01:27` 那调度层这边就有些差别了
`01:29` 关于大模型的调度
`01:29` 因为CLCODE那边它只支持ANTHROPIC协议的嘛
`01:33` 那这边的话就是实现了一套
`01:35` 支持其他各种供应商的整体的架构的话
`01:37` 其实有很多值得学习的地方
`01:39` 如果大家对A景的感兴趣的话
`01:41` 可以去他官方源码仓库去拉下来
`01:42` 去学一下
`01:43` 那个仓库现在已经100多K了
`01:45` 特别火
`01:46` 那下面就是这个ommy open code
`01:48` 这个是我们今天讲的重点

### 多Agent机制 `01:50`

`01:50` 因为他这一套多A进的多模型的一个机制
`01:53` 能让一个任务去拆解成各个阶段去开发
`01:57` 有复杂的又复杂的模型
`01:59` 简单的用简单的模型
`02:00` 这种才是一个理想状态下的一个协作的方式吗
`02:03` 那它本质上就是我刚才说的
`02:05` 它就是一个插件
`02:06` 通过open code暴露的一些钩子
`02:08` 去拦截这个AI的一些行为
`02:10` 他的整个工作流程就是来一个用户输入之后
`02:12` 他去拦截open code的一些钩子
`02:14` 这时候去做一些增强上下文呀
`02:16` 或者去选择多A镜头
`02:17` 有可能是多个模型去写作
`02:19` 最后返回结果
`02:19` 整体的一个流程就是这样子的
`02:21` 那为什么需要多个代理哈
`02:23` 他这边说的一个说法是
`02:24` 专业的代理去做专业的事情
`02:26` 然后规划和执行分离
`02:28` 可以并行执行
`02:29` 按需去选模型
`02:30` 并且节省成本
`02:31` 那他现在这一个多代理的话
`02:33` 就像最近cloud code新出的那个agent teams一样
`02:36` 只不过cloud code它是很久很久之后才出的嘛
`02:39` 这个是老早就有了ommy f code
`02:41` 那下面是它的一些AGENDER的一个分工
`02:44` 这个是它的一个主要agent的是用的cloud of4.6
`02:47` 这个模型
`02:47` 还有一些规划师
`02:49` 就是在规划方面以及编排方面
`02:50` 他用的cloud最顶尖的模型
`02:52` 那代码执行这一块的话
`02:54` 是用的是GBT5.3codex
`02:56` 如果是一些比较轻量的任务
`02:57` 你可以用gm啊或者mini max啊
`02:59` 以及其他都可以
`03:00` 那写前端这块的话
`03:02` 他用的GEMINI3flash或者pro
`03:04` 这样子的话就一个任务进来之后
`03:06` 他由这个去规划
`03:07` 规划完了之后
`03:08` 他让GBT5.3去做执行
`03:10` 然后发现有前端任务
`03:11` 他这个时候就用gem来
`03:12` 如果涉及到一些小修小改啊
`03:14` 比如说是一些国际化处理啊
`03:16` 多语言处理
`03:16` 这个时候他就完全可以用很低的那种模型
`03:18` 去做这个事情嘛
`03:19` 这样的一个搭配
`03:20` 其实我体验下来挺好的好

### 安装教程 `03:24`

`03:24` 下面我们就开始分别去安装open code和oh my open code
`03:27` 这个插件
`03:28` 桌面端它现在也支持了
`03:29` 支持Mac os
`03:30` Windows linux
`03:31` 我体验下来的话
`03:32` 它有一些断流的情况
`03:33` 所以说我更推荐大家在使用CLI版本
`03:36` 我们现在安装一下
`03:40` 好它提示我们已经安装了
`03:42` 我们来进去看一下
`03:45` 我们先切到一个免费的模型
`03:47` 你上面搜一个free
`03:49` 这些模型都免费
`03:50` 我就用mini max m2.1版
`03:51` 我们输入一下测试一下
`03:52` 看它安装好没有
`03:54` OK如果看到这一步
`03:55` 就证明你open code已经安装好了
`03:57` 这个时候我们就安装那个插件
`03:58` 因为现在在AI时代
`04:00` 其实你安装东西都非常方便
`04:01` 尤其是他如果有文档
`04:03` 有开源仓库的话
`04:04` 你看这一个插件哈
`04:06` ommy open code的一个官方仓库里面直接就告诉你了
`04:09` 你直接用coding agent的去装就行了
`04:11` 我就把这个复制一下
`04:12` 然后粘到刚刚我们开的这个open code里面
`04:14` 用免费的mini max让他给我们去装
`04:16` 那下面就是它会弹出来让你做一些选择
`04:20` 可以看到他让我们回答一些问题吗
`04:21` 你有没有订阅cloud code
`04:23` 有没有订阅OpenAI
`04:24` 有没有一些GEMINI的一些model
`04:26` 你就根据自己的情况去选择就行了
`04:29` 强烈建议你不要在OPENCODE里面去用cloud的模型
`04:32` 通过订阅的方式去用
`04:33` 封号的概率非常高
`04:35` 那这边我就说都没有
`04:38` 待会我也给大家一个配置
`04:39` 直接就可以用了
`04:40` 你只需要去搞一个APIK过来
`04:42` 你就可以直接把这些模型都用上了
`04:44` 这个时候我们都说都没有就完事了
`04:45` 因为这个配置后期你都可以去改的
`04:47` 可以看到我们把插件安装成功之后
`04:49` 下面的这一个agent呢就变了
`04:50` 变成了ommy open code
`04:52` 他的那一套多A型的体系
`04:53` 可以看到他有好几个agent的嘛都可以去用
`04:55` 并且这个插件它还有个好处
`04:58` 它的兼容性做的比较好
`04:59` 他已经把cod code那边你已有的一些skills m cp
`05:04` 还有那些自定义的命令全部弄过来了
`05:05` 比如说skills可以看到我的那些SKT
`05:08` 它全部弄过来了
`05:09` 这样非常好
`05:10` 也就是说你可以来回切嘛
`05:12` 你有时候你要回去考虑code里面用也可以
`05:14` 现在我们开始配模型

### 模型配置 `05:15`

`05:15` 你可以通过connect这个命令
`05:17` 去连接你想要的一些模型
`05:18` 如果你有ChatGPT语
`05:20` 就选chat GB t
`05:21` 你有其他的
`05:21` 你就选其他的
`05:22` 那我这边的话
`05:23` 平时会用JMI3.0去写前端嘛
`05:26` CLOUCODE4.6去写架构和具体的一些代码
`05:29` 然后GPT5.3codex去做一些代码之心
`05:32` 以及解决一些疑难杂症
`05:34` 我喜欢用每个模型擅长的能力哈
`05:36` 但是对大家来说的话
`05:37` 你如果像我一样去订阅三家的M模型
`05:40` 提示你要花三分钱
`05:41` 我订阅的100道的CROCODE
`05:43` 然后gm来我订阅了两个号
`05:45` 140刀
`05:46` Open ai
`05:47` 我订阅了20刀
`05:48` 那加起来就是160刀
`05:49` 并且哈我们刚刚也说了
`05:51` 如果你用cloud code去授权到这边open code去用的话
`05:54` 你封号的概率特别高
`05:56` 之前我还出了一期视频
`05:57` 专门去讲这个cloud code
`05:59` 大面积去封禁open code用户的一个事件
`06:02` 因为刚开始他是不管的
`06:04` 后面他发现这个open code日益壮大之后
`06:06` 他就开始封禁了
`06:07` 你就不能授权到这里面用
`06:09` 所以如果你要在open code里面去用cloud of
`06:12` 4.6的一个模型
`06:13` 我建议大家去用中转站
`06:15` 通过按API去计费
`06:16` 用多少就算多少
`06:17` 那我这边推荐一个我一直在用的中转站接口

### 接口AI中转站 `06:18`

`06:20` i open code这边也已经支持了接口
`06:22` I也就官方支持了嘛
`06:24` 那前面几期视频我也推荐过
`06:26` 如果我要用到一些国外的模型
`06:28` 按API调用的话
`06:29` 我都用的是接口I挺稳定的
`06:32` 最关键的一点
`06:33` 你不需要魔法网络
`06:34` 而且是国内付款的一个方式
`06:36` 最近他们还专门针对海外的顶尖模型
`06:40` 增加了一个套餐的方式
`06:41` 也就是说你可以用一份钱去用国外
`06:44` 这些顶尖模型呢
`06:45` 目前有三个档位哈
`06:47` 可以按自己的一个需要去选择
`06:49` 相较于官方API按量计费的话
`06:51` 这一个套餐模式帮你省了25%的钱
`06:55` 大家可以用我评论区的一个置顶链接
`06:58` 注册接口I
`06:59` 并且绑定GITHUB
`07:00` 他会送三美刀
`07:02` 你可以去体验体验一下这个open code
`07:04` 好
`07:04` 下面我们先来去创建一个APIK
`07:07` 在这个地方新建
`07:08` 我就随便取一个
`07:09` 我们就取open code
`07:10` 点击复制
`07:11` 这个时候我们回到open code这边
`07:13` 复制APIK之后
`07:14` 我们供应商选择接口
`07:16` 点AI敲击回车
`07:17` 把我们的APIK输入下
`07:19` 然后保存
`07:20` 这个时候你可以看到国内国外的模型都可以用
`07:22` 用一个APIK你就解决了多模型的一个适配问题
`07:26` 好下面我们来进行一些其他的配置
`07:29` 当你安装完open code以及ommy open code插件之后
`07:33` 你会在你的工作目录下面点configure
`07:36` 下面有一个open code目录
`07:37` 这里面有两个JSON的配置文件
`07:40` 一个是配置open code的一些模型的
`07:43` 另外一个是配置
`07:44` 我们这一个多A景的写作的时候
`07:46` 他具体用哪些模型
`07:48` 这边的话我给大家直接准备了
`07:49` 现成的
`07:50` 你直接到我的一个开源仓库里面
`07:52` 直接去复制就可以了
`07:53` 下面来我这一个开源仓库里面
`07:55` 直接把这两个配置可以复制一下
`07:59` 把它粘过去就完事了
`08:03` 好我们把它覆盖一下保存
`08:05` 再把ommy open code的也复制一下回来点击复制
`08:13` 那这一块的话你可以根据你自己的需要哈
`08:16` 因为GBT5.3这一个codex模型的话
`08:19` 官方是没有出API的版本
`08:21` 那后续如果open i那边出了API版本接口
`08:23` I这边也会跟上
`08:24` 你就根据自己的需要嘛
`08:26` 去看这块要不要注释这个内容
`08:28` 我们来复制一下
`08:32` 好我们把它覆盖完了
`08:34` 覆盖完了之后
`08:35` 我们重启一下
`08:35` 我们code试一下
`08:37` 你就可以看到cloud office4.6已经可用了
`08:40` 就这几个agent的它配置的模型都可用了
`08:43` 那下面我们开始进入实战的一个环节

### 实战演示 `08:46`

`08:46` 有了ommy open code插件之后
`08:48` 我们看一下多agent的多模型协作的一个流程
`08:51` 我这边有一个纹身图的agent
`08:53` 我希望让他去加一些功能
`08:55` 在这边加一些删除的动作
`08:57` 能把励志记录删除掉
`08:58` 同时在左下角做一些设置的动作
`09:02` 可以去更新你的一些头像啊
`09:03` 昵称啊
`09:04` 以及我们的一些历史提示词
`09:06` 能支持去快速的复制展开开干
`09:11` 那提示词的话也比较简单
`09:13` 我们这边加一个auto work
`09:14` 让他去更深入的去工作
`09:17` 去分析好
`09:18` 我们切到open code这边
`09:19` 这个就是它的整个架构agent
`09:21` 他用的是office4.6
`09:22` 那这个的话是干活的agent
`09:24` 他用了GPT5.3
`09:25` 还有一些其他的agent的
`09:26` 也就是说他是多agent的
`09:28` 多模型去写作的一个流程
`09:30` 好
`09:30` 我们把提示粘过来
`09:31` 让他干活
`09:33` 这里提示auto work已经开启了
`09:35` 这个时候可以看到他开启了一个探索的沙
`09:37` 杯技能吗
`09:38` 用的是很便宜的一个模型
`09:41` 这个过程就跟在cloud code是一样的
`09:46` 这里又开了一个新的
`09:49` 可以看到他一直在开一些新的沙倍镜的任务
`09:52` 去做探索号
`09:54` 好
`09:54` 这里又开启了一个计划的task
`09:57` 也就是一个SBA技能
`09:58` 我可以点进去看一下他在做什么
`10:01` 可以看到他就是把已有收集到的一些代码信息
`10:04` 整个代码空间来去做一轮分析
`10:08` 让这个op4.6做一个计划出来
`10:10` 这个时候他这个主要的协调A进程在等
`10:13` 等他这个后台的这个计划任务完成完了之后
`10:16` 这个时候他再去协调其他的A进的去干活
`10:20` 可以看到他这一个经过几轮的一个探索之后哈
`10:23` 他得到了一个突突像
`10:25` 那这个突突像的话
`10:26` 它根据后端前端以及一些小需求
`10:29` 拆成了很多突突
`10:30` 像这个时候他就开始去并行执行修改任务了
`10:34` 可以看到他这里又开启了几个SABA镜头
`10:36` 有一个点要看的话是他的这个沙杯技能
`10:39` 他开的时候
`10:40` 他其实这个时候是用的JMI3flash这个模型
`10:43` 也就是说他认为这个任务
`10:44` 其实你根本就没必要
`10:46` 用一个很高的模型去做这个事情
`10:47` 所以说它默认给你路由到了一个简单模型
`10:51` 他这种规划任务的方式
`10:53` 就能把模型最大化的利用起来
`10:56` 复杂的任务用复杂的模型
`10:57` 简单的任务用简单的模型就很完美
`11:00` 可以看到他开始去执行
`11:02` T4跟T6这两个任务了
`11:03` 而且他依赖关系也早出来了
`11:05` 这个就像前两天发布的cloud agent teams
`11:08` 的一个写作流程
`11:10` 只不过这个是老赵就有了这个插件
`11:12` 老早就有了这种多样性的协作
`11:14` 最终被CROCODE写到一个官方的库里面去了
`11:18` 然后他们的这种通信机制就跟A型的team是很像
`11:22` 你看嘛这个系统通知cr code
`11:24` 其实它很多方案是借鉴了社区的
`11:27` 因为社区搞出来之后
`11:29` 他觉得确实是OK可以的
`11:31` 那他就把它集成到cloud code里面
`11:33` 所以这CROLCODE从它诞生之后的很多功能迭代
`11:37` 都参考了开源社区的一些做法
`11:39` 而且他的也在他的一些技术博客里面
`11:41` 有感谢一些开源社区的人提供了一些解决方案
`11:44` 就像你做产品一样
`11:45` 你产品迭代的过程中
`11:47` 你光靠产品经理去
`11:48` 有时候发掘不了用户的一些真实的需求
`11:50` 那真实的需求暴露出来之后
`11:52` 社区它就解决了
`11:53` 那你就可以去把一些已经解决的痛点的问题
`11:57` 提升到自己的产品里面
`12:03` 可以看到这所有的图图像他全部完成了哈
`12:05` 包含三个任务
`12:07` 我们现在来看一下它整体的效果怎么样
`12:09` 首先是左侧的这个删除功能好
`12:13` 确认删除
`12:14` OK确认可以
`12:15` 并且弹窗也是正常的
`12:17` 其实我们看这个可以复制已有的提示词
`12:21` 可以复制折叠展开是OK的
`12:24` 下面就是这个左下角的设置
`12:26` 可以去更新昵称跟头像这一些啊
`12:30` 可以看到他这边其实也做好了好
`12:33` 我找一张图像试一下
`12:35` 看可以吗
`12:37` 前面加一个我的名字吧
`12:40` 看可以吗
`12:43` 可以已经保存了
`12:44` 可以的
`12:44` 这个看起来他做的挺好的
`12:46` 英文啊
`12:47` 英文设置也是OK的
`12:48` 多元设置也是OK的
`12:50` 整体这一次任务的话他全部搞定了呀
`12:52` 就是虽然不是特别难的
`12:54` 但是他协调了多个A镜能去干活
`12:57` 而且都能交付的很完美
`12:58` 中间我没有做任何的介入
`13:00` 所以说这个工具啊
`13:01` 真的推荐大家去用一下这个多A镜的协作
`13:03` 真的挺好用的
`13:03` 下面我来总结一下

### 总结 `13:04`

`13:05` 使用open code再加那个插件
`13:07` open open code再配合你用一个接口外的中转站
`13:11` 可以做到模型自由稳定省心
`13:14` 你也不怕被封号
`13:15` 第二个的话是成本灵活
`13:17` 这就是今天所有的视频内容了
`13:18` 如果你觉得这期视频对你有帮助的话
`13:20` 可以给我一键三连
`13:21` 谢谢大家
`13:22` 拜拜拜拜
---
title: "A2A协议深度解析 - 第 2 部分：流式返回 + 多Agent场景"
url: "https://www.bilibili.com/video/BV1dRTRzUED3?spm_id_from=333.788.videopod.sections&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1dRTRzUED3"
cid: "30390420492"
author: "马克的技术工作坊"
upload_date: "2025-06-08"
subtitle_lang: "中文"
created: "2026-05-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114647894070686&bvid=BV1dRTRzUED3&cid=30390420492&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

A2A 协议深度解析 (2) - 流式返回 + 多 Agent 场景 #a2a #Google #LLM #Agent #AI #编程
 
时间轴：
00:00 视频内容介绍
00:33 什么是流式返回
01:39 抓包分析 A2A 的流式响应规范
11:17 更多数量的 Agent 协作流程

## 章节

- `00:00` 视频内容介绍
- `00:34` 什么是流式返回
- `01:40` 抓包分析 A2A 的流式响应规范
- `11:18` 更多数量的 Agent 协作流程

## 字幕

### 视频内容介绍 `00:00`

`00:00` 今天我们继续聊聊google推出的a to a协议
`00:02` 也就是agent to agent的协议
`00:04` 这个协议用于让多个agent的互相沟通交流
`00:06` 完成一项复杂的任务
`00:08` 在上一个视频里面
`00:09` 我们讲述了a to a协议的基本使用场景
`00:12` 还通过两个agent的同步调用
`00:13` 梳理了协议的核心链路
`00:15` 那这期我们来看看两个更进阶的内容
`00:18` 1a to a协议是如何处理流失返回的
`00:21` 二更多数量的agent协作流程
`00:23` 我们之前的链路都只涉及两个agent
`00:25` 不够有代表性
`00:27` 因此我们把agent的数量扩大来
`00:28` 看看三个甚至更多的agent是如何协作的
`00:31` 好话不多说
`00:32` 我们直接开始

### 什么是流式返回 `00:34`

`00:36` 鉴于可能有人不太清楚流失返回是什么
`00:39` 我这里简单解释一下
`00:40` 已经了解的小伙伴可以直接跳过这一部分
`00:43` 一般情况下我们使用HTTP访问一个网站的时候
`00:46` 我们的浏览器会发送给目标服务器一个请求
`00:49` 比如说要访问一个HTML页面
`00:51` 一个图片
`00:52` 一个JS脚本之类的
`00:53` 目标服务器会返回对应的结果
`00:55` 一去一回一次交互就完成了
`00:58` 这个交互方式有个缺陷
`00:59` 他处理不了服务器连续发回多次响应的情况
`01:02` 比如说我们经常用的大模型
`01:04` 聊天页面模型的结果都是几个字
`01:06` 几个字的返回
`01:07` 只是一去一回的话
`01:08` 显然无法做到这种效果
`01:09` 所以目前主流的大模型聊天页面
`01:12` 用的都是流式返回
`01:13` 它的特点是浏览器只需要请求一次
`01:16` 服务器接收到请求之后
`01:17` 会连续发送多次响应
`01:19` 每次响应的内容都是几个字
`01:21` 而浏览器接收到几个字就显示几个字
`01:23` 这样用户就可以及时接收到模型的返回
`01:26` 出来几个字就看几个字
`01:27` 体验就会好很多
`01:29` 等到所有的结果都返回完毕之后
`01:31` 服务器会发送一个完成的标识
`01:33` 浏览器接收到标识之后
`01:35` 关闭连接页面
`01:36` 显示模型
`01:36` 回答完毕
`01:37` 整个流程也就结束了

### 抓包分析 A2A 的流式响应规范 `01:40`

`01:42` 那了解了流失返回的概念之后
`01:44` 我们就来看一看a to a是如何定义
`01:46` 流失返回协议的
`01:47` 这次我们用一个查询机票信息的场景举例
`01:50` 整个流程里面有三个角色
`01:51` 分别是用户调度agents和机票agents
`01:54` 用户询问
`01:55` 2025年5月1日
`01:56` 从西雅图飞往纽约的航班中
`01:58` 还有哪些航班有余票呢
`02:00` 用户的问题会首先发往调度一阵子
`02:02` 调度一阵子会转发给机票
`02:04` 一阵子
`02:04` 机票一阵子返回
`02:05` 航班信息
`02:06` 调度也正在接到这个信息之后
`02:08` 稍作加工就返回给用户
`02:09` 这样用户也就得到了最终答案
`02:11` 这个呢就是整个流程可以看出
`02:13` 这里面涉及到两个A
`02:15` 整次我们下面的任务就是去启动这两个agent层
`02:18` 然后跑通整个流程
`02:20` 首先是调度agent
`02:21` 这个agent写在a to a samples仓库里面
`02:23` 和平台放在一起
`02:25` 都是在demo ui这个文件夹里面
`02:27` 在上个视频里面我们已经演示过启动方法了
`02:30` 所以呢这里面我就不赘述了
`02:32` 我直接就给他启动开了
`02:40` 可以看出它开放的接口呢是1万2
`02:42` 我们直接打开这个地址
`02:46` 没问题
`02:47` 到这里平台和调度agent就创建完成了
`02:50` 下面呢我们来启动机票agent
`02:52` 这个gpl agent呢是我自己写的
`02:55` 放在这个GITHUB仓库下面
`02:56` 我们进入到这个GITHUB仓库里面
`03:05` 然后进入到这个目录中
`03:09` 打开flies文件夹
`03:10` 然后再执行uv run
`03:13` 点这个命令就可以启动这个机票agent了
`03:16` 这样的话呢我们两个agent都启动完毕了
`03:19` 我们下面打开WASHARK抓包软件
`03:23` 选择loop back
`03:24` 也就是说我们只抓本地的网络包
`03:27` 然后呢我们设定过滤条件为HTTP and TCP
`03:31` 点d s p d s t port等于1万01
`03:36` 为什么是1万01呢
`03:37` 因为我们刚才开放的这个机票agent的端口
`03:41` 就是1万01
`03:42` 所以呢我们就想抓
`03:43` 发往这个机票agent的各种网络包
`03:46` 然后到这里玩shark的操作呢也结束了
`03:49` 我们再回到平台这里
`03:52` 然后点击这个agents点
`03:55` 再点击这里
`03:55` 我们注册一下我们的机票agent
`04:02` 好这样g pl1阵子就注册成功了
`04:05` 然后呢
`04:06` 我们新建一个对话
`04:08` 输入问题
`04:11` 2025年5月1日
`04:12` 从西雅图飞往纽约的航班中
`04:14` 还有哪些航班有余票
`04:18` 可以看出平台直接给出了答案
`04:21` 有些观众可能会在这个时候产生一些疑惑啊
`04:23` 因为好像这个结果也没有流失返回啊
`04:26` 我们讲的不就是流失的这个场景吗
`04:28` 是不是搞错了呢
`04:29` 其实没有搞错
`04:31` 机票agent的确实是流失返回的
`04:34` 只不过调度agent会等这个流失返回的
`04:36` 所有结果都返回回来之后
`04:38` 才整理出一份答案
`04:40` 然后呢再给到我们
`04:41` 所以呢从我们用户的角度来看
`04:44` 好像这个过程并没有涉及到流失返回什么事情
`04:47` 但是呢机票l agent的返回确实是流失的
`04:50` 由于平台没有流失
`04:51` 显示机票agent的返回结果
`04:53` 所以呢你才有了整个过程并没有流失
`04:56` 进行的错觉
`04:57` 不信的话呢
`04:57` 我们就来看一下我们抓的网络包
`04:59` 验证一下我们的想法
`05:00` 再回到WISHARK这里
`05:03` 可以看到平台一共是向机票agent发送了三个请求
`05:07` 其中前两个是用来请求agent card的
`05:10` 这个呢是在注册agent的时候发送的
`05:13` 这两个请求其实本质上都是一样的
`05:16` 我们随便点开一个来看一下
`05:19` 这个没有格式化
`05:20` 我来给大家格式化一下
`05:24` agent card的结构
`05:26` 我们在上个视频已经聊过了
`05:28` 我这里只重点强调其中的两个地方
`05:30` 第一机票A整体有个skill
`05:32` 可以用来查询机票信息
`05:34` 也就是这个地方了
`05:35` 所以呢当我们问机票相关的问题的时候呢
`05:38` 调度agent就可以去调用这个机票agent
`05:41` 拿到相关的答案
`05:42` 第二点呢是这个GPLAH是支持流式返回的
`05:46` 对应的就是这里面的streaming值为true
`05:49` 所以你后面就可以看到
`05:50` 调度agent在请求机票agent的时候呢
`05:53` 会要求它流失返回结果
`05:55` 那agent card呢就看到这里
`05:57` 我们再来看看发往机票agent的问题
`06:04` 也就是这里面的第三个请求
`06:06` 我们来看一下
`06:08` 这个请求呢是我们问完问题之后
`06:10` 调度agent发给机票agent的
`06:12` 这整个界面分为两个部分
`06:14` 上面红色的是调度agent的请求
`06:16` 下面紫色的这一大片是GPLAH的的返回
`06:19` 可以看出啊
`06:20` 机票agent的返回呢有多个
`06:23` 每一个呢都包含一部分信息
`06:25` 这也就是流失返回了一次
`06:27` 返回一部分嘛
`06:28` 我们来一点点看
`06:29` 首先呢我们来看一下他的请求是什么样子的
`06:33` 我再来给大家格式化一下
`06:38` 请求的结构呢我们在上个视频里面也聊过了
`06:41` 我这里面呢只重点强调其中的几个部分
`06:45` 第一个要强调的就是这里面的method
`06:47` 请求中的method设置的是message stream
`06:51` 这个跟我们上个视频里面聊的是不一样的
`06:53` 在上个视频里面
`06:54` 调度agent请求天气agent的时候呢
`06:57` 使用的method是message send
`07:00` 这个呢就是期望天启agent一次性返回所有的结果
`07:05` 而这里面所使用的这个message stream呢
`07:08` 就代表调度agent希望得到流失的返回
`07:11` 因为机票agent在他的agent card里面写明了
`07:15` 他是支持流式返回的
`07:17` 所以呢调度agent就优先请求流失结果了
`07:20` 然后这个请求剩余的内容呢
`07:22` 就跟我们之前聊的差不多了
`07:24` 比如说这里面呢就是调度agent要问机票agent的问题
`07:29` 那请求呢我们就看到这里
`07:32` 下面我们来看一下机票agent的返回
`07:35` 首先呢我们来看一下第一个
`07:37` 我来给大家复制粘贴
`07:39` 然后再格式化一下
`07:43` 格式化之后呢是这个样子
`07:45` 这个结构体的大部分属性
`07:46` 我们在上个视频里面都已经聊过了
`07:48` 这里呢我们重点看一下status这个属性
`07:51` 他的state值被设成了submitted
`07:55` 意思就是说机票agent接收到了调度agent的请求了
`07:58` 他已经在内部提交了任务
`08:00` 如果任务状态有更新的话
`08:02` 那么GPU agent的后面还会接着返回
`08:04` 没错第一个返回要表达的意思呢
`08:07` 基本上就只是这样了
`08:08` 他还没有回答调度AH的问题
`08:10` 这里只是创建了一个任务
`08:11` 并且把任务的状态设置为了已提交
`08:14` 然后我们再看一下第二个
`08:23` 第二个返回呢开始有了些实质性的内容
`08:26` 首先是机票agent产出了一段文本内容
`08:29` 就是你要查询的机票
`08:31` 这里面的kind设置成为了artifacts update
`08:36` artifact就是这一部分的内容了
`08:38` 它代表机票AGH的产物
`08:39` 我们的产物呢主要是文本kind
`08:42` 设置成了artifacts update
`08:44` 意思就是说当前的这个消息的功能
`08:46` 是提供artifact的一部分内容
`08:48` 我们后面还会接到更多
`08:49` kind为artifacts update的消息
`08:51` 把这些消息连在一起
`08:52` 才能够拿到一个完整的artifacts
`08:54` last chunk的值设为了false
`08:56` 代表后面还会有更多
`08:57` kind为artifacts update的消息
`09:00` 第二个返回呢到这里面其实也就差不多了
`09:03` 其实也就是携带了这样的一部分的文本
`09:06` 我们再看第三个返回
`09:14` 第三个返回跟第二个返回很像
`09:16` 他的文本换成了如下
`09:19` 跟第二个返回的文本连起来
`09:21` 就是你要查询的机票如下
`09:23` kind呢是artifacts update last chunk
`09:27` 它的值是false
`09:28` 这两个呢跟第二个返回也是一样的
`09:31` 说明后面还有别的内容
`09:33` 那我们再来看第四个返回
`09:40` 第四个返回呢又追加了一些文本
`09:43` 也就是这里面的内容了
`09:45` 这些呢就是具体的航班信息了
`09:47` 这里写的航班号是fake001和fake002
`09:50` 可以看出都是假的
`09:52` 因为这些数据都是机票agent的造的
`09:54` 并不是真的为了简化链路
`09:56` GPLAH的内部
`09:57` 并没有真的去请求航空公司的API
`09:59` 它返回的都是固定文案
`10:00` 这个大家不必在意
`10:02` 我们只需要保证a to a协议的链路是真的
`10:04` 就可以了
`10:04` 呃另外啊
`10:05` last chunk变成了true
`10:08` 代表artifacts的所有信息都已经返回了
`10:11` 把前面第二个第三个和这一个的文本连在一起
`10:15` 就可以得到完整的文本内容了
`10:18` 不过大家注意啊
`10:19` last chunk为true
`10:20` 只是代表了artifacts的所有信息都返回完毕
`10:23` 这并不代表整个流失都结束了
`10:26` 你看这后面还有一个消息
`10:28` 也就是这第五个消息
`10:29` 我们还需要第五个消息来更新整个任务的状态
`10:33` 那下面我们就来一起看一下
`10:34` 第五个消息的具体内容
`10:36` 同样我给大家格式化一下
`10:38` 可以看到第五个返回的kind
`10:40` 所对应的值是status
`10:42` update代表当前的这个消息的功能是更新任务状态
`10:46` status里面的state值为completed
`10:48` 代表任务的状态已完成到这里
`10:51` 整个返回才算是真的结束了
`10:53` 所以总结一下机票agent一共是返回了五条消息
`10:57` 其中第一条和最后一条
`10:59` 也就是图示的蓝色部分是用来更新任务状态的
`11:02` 第一条用于把任务状态更新为已提交
`11:04` 最后一条用于把任务状态更新为已完成
`11:07` 中间的三个消息
`11:08` 则是各自携带了artifact的一部分信息
`11:11` 先提交任务
`11:12` 再更新artifacts
`11:13` 最后再完成任务
`11:14` 这个呢就是a to a协议里面
`11:16` 流失返回的一般模式了

### 更多数量的 Agent 协作流程 `11:18`

`11:19` 我们之前聊的都是两个agent的交互的场景
`11:23` 这个代表性不够高
`11:24` 我们再来看看三个agent之间是如何沟通的
`11:27` 还是照例啊
`11:28` 我们先把平台和调度agent的启动开
`11:30` 他们都在demo ui这个文件夹下面
`11:33` 我们按照之前的那种启动方法
`11:35` 启动它们就可以了
`11:40` 然后呢我们再把这个机票agent也给启动开
`11:46` 除了这两个程序之外呢
`11:48` 我们还需要再启动一个agent
`11:50` 就是天气agent了
`11:52` 这个agent在我们上个视频里面提到过
`11:54` 我们来把它启动开
`11:58` 那这些都启动完毕之后
`11:59` 我们再打开wr shark
`12:03` 选择这里面的LOOPBACK
`12:06` 然后填入我们的筛选条件
`12:13` 这个筛选条件的意思呢是我们要抓取HTTP请求
`12:17` 并且目标端口呢是1万或者1万01
`12:20` 这两个端口就分别对应了天气agents和机票agents
`12:26` 然后呢我们来到平台这里注册这两个agent
`12:31` 首先是天气agent
`12:33` 它的端口呢是1万
`12:37` 然后再注册机票agent
`12:39` 它的端口是1万01
`12:43` 都注册好了之后
`12:44` 我们来问一个复杂点的问题
`12:48` 让这三个agent都行动起来
`12:50` 我们的问题是
`12:51` 我计划在5月1日至3日之间
`12:54` 从西雅图飞往纽约
`12:56` 想选择出发当天阳光明媚的日子
`12:58` 请帮我查看这三天西雅图的天气
`13:01` 选择天气最好的那一天
`13:03` 并提供盖瑞的机票信息
`13:13` 这个问题呢是需要先调用天气agent
`13:16` 查询西雅图的天气
`13:17` 然后呢再调用机票agents查询机票相关的信息
`13:21` 呃
`13:22` 可以看出平台呢给出了答案
`13:24` 他从这三天里面挑选了5月1日
`13:27` 应该是因为天启AH的告诉他这天天气很好
`13:30` 然后呢他查询了5月1日这天的机票信息
`13:33` 并且呢在这里面都显示了出来
`13:35` 我们来到玩shark这里面大体看一下
`13:40` 可以看到呢我们一共是拿到了六个请求
`13:42` 其中前两个请求的是天气agent的agent card
`13:47` 这两个请求呢其实都一样
`13:49` 我们随便打开一个来看一下
`13:52` 这个呢就是天气AH的相关的信息了
`13:57` 中间的这两个呢则是请求了机票agent的agent card
`14:02` 同样他们两个也是基本类似
`14:04` 所以呢我们挑一个看一下
`14:05` 这个呢就是机票agent的相关信息了
`14:09` 然后后面两个请求呢
`14:11` 则分别是发往了天气agent和机票agent
`14:16` 我们来一个一个看一下
`14:18` 首先看看发往天气agent的这个请求是什么样子的
`14:23` 这个呢是给天气AGH的问题
`14:27` 请提供5月1日至3日的西雅图天气预报
`14:31` 可以看出
`14:31` 这个问题与我们的原始问题是不一样的
`14:34` 这个问题是调度agent整理出来
`14:36` 用于问天气agent的用户问题中的其他部分
`14:39` 比如说与机票相关的内容
`14:41` 其实与天气agent的并没有什么关系
`14:43` 所以呢不必包含在天启agent的请求里面
`14:46` 那再来看看天启agent的返回天气
`14:49` 也正的给我们的返回如下
`14:51` 您要查询的天气信息是这样子的
`14:53` 5月1日晴天
`14:54` 5月2日小雨
`14:55` 5月3日大雨
`14:57` 这就是为什么后面调度位置呢
`14:59` 会选择5月1日作为出发日期的一个原因
`15:02` 因为5月1日是这三天里面唯一一天是晴天的
`15:05` 这个呢就是天气agent的请求和返回了
`15:09` 我们再来看一下机票agent的请求
`15:13` 调度agent发往机票agent的问题是
`15:17` 请提供5月1日从西雅图飞往纽约的天气信息
`15:21` 因为之前天气AGH呢给出了
`15:23` 5月1日是一个晴天
`15:24` 所以呢他这里面只查询了5月1日的机票信息
`15:28` 然后我们的GPAGENT
`15:30` 就按照我们刚才讨论的这五个消息的内容
`15:34` 返回了
`15:35` 结果这个返回结果呢我们之前讨论过了
`15:39` 所以呢我这里也就不再赘述了
`15:42` 呃
`15:43` 接收到了天气agent和gpl agent的返回之后呃
`15:47` 调度agent就根据这两个返回整理出了一份答案
`15:51` 发回给了平台
`15:52` 然后呢平台发挥给了用户
`15:54` 整个流程到此也就结束了
`15:56` 我们来给这里面的问答部分画个流程图
`15:59` 整个流程图里面一共有五个角色
`16:01` 分别是用户平台调度agents
`16:04` 天气agents和机票agents
`16:07` 首先用户向平台发送了一个问题
`16:09` 这个问题简单来说就是查天气
`16:11` 选机票平台把问题转发给了调度agents
`16:14` 调度agent的发现
`16:15` 需要分别调用天气agent和机票agent
`16:18` 才能够解决用户的问题
`16:19` 因此呢他就先问了天气agent一个问题
`16:22` 获取5月1号到5月3号这三天的天气预告
`16:25` 然后呢他从结果中获知5月1号天气比较好
`16:28` 那就选5月1号出发了
`16:30` 后面查机票的时候也只查5月1号的就好了
`16:33` 所以紧接着他就问了机票agent的一个问题
`16:35` 获取5月1号那天的机票信息
`16:37` 机票agent的返回是流逝的
`16:39` 一共是返回了五个消息
`16:41` 在机票agent的五个消息都返回完毕之后
`16:44` 调度agent就根据天气agent和机票agent的返回
`16:47` 整理了一份答案
`16:48` 发给了平台
`16:49` 平台呢又发给了用户
`16:51` 整个流程就到此结束了
`16:53` 那有些小伙伴可能会问
`16:54` 如果agent的数量不止三个
`16:56` 有十个20个
`16:57` 这怎么处理呢
`16:59` 一样的
`16:59` 首先必须要有一个调度agent
`17:01` 他负责寻找并下发任务给其他的agent
`17:04` 并在最后根据其他agent的回复
`17:06` 总结答案发回给用户
`17:07` 所以呢只要agent的数量不是特别夸张
`17:10` 这个方案就是可用的
`17:11` 今天的视频呢就到此结束了
`17:13` 别忘了点赞关注我们
`17:15` 下次再见
`17:16` 拜拜
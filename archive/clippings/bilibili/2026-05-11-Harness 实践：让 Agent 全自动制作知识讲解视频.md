---
title: "Harness 实践：让 Agent 全自动制作知识讲解视频"
url: "https://www.bilibili.com/video/BV1ypdgBCE9B?spm_id_from=333.788.videopod.sections&vd_source=00c261324d44a5cd7366302129b53bde"
bvid: "BV1ypdgBCE9B"
cid: "38181407514"
author: "code秘密花园"
upload_date: "2026-05-08"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116538065684466&bvid=BV1ypdgBCE9B&cid=38181407514&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

今天这期教程，同时解答大家最近比较关注的两个问题：
Harness 有什么具体的工程实践？
我的视频效果到底是怎么做的？

相关资源链接：
Skill 开源地址：https://github.com/ConardLi/garden-skills/
Claude Code：https://code.claude.com/docs/zh-CN/overview
MiniMax CLI：https://github.com/MiniMax-AI/cli
CC Switch：https://github.com/farion1231/cc-switch/releases/tag/v3.14.1

## 字幕

`00:00` 前段时间我发了几条技术讲解视频
`00:02` 评论区好多同学问这些视频效果是怎么做的
`00:05` 那正好趁着五一假期
`00:07` 我把整套视频生成的流程封装成了一个skill
`00:10` 今天呢就来跟大家完整的聊一下
`00:12` 大家好
`00:12` 欢迎来到科乐美密花园
`00:14` 我是花园老师
`00:15` 今天这一期呢内容信息量很大啊
`00:17` 我们主要讲三件事
`00:18` 首先啊我们的视频到底是怎么做的
`00:20` 第二背后的这个skill是怎么设计的
`00:23` 第三手把手带大家走一遍
`00:25` 完整的实战流程
`00:26` 从一篇文章丢进去
`00:27` 到最后出来一个精美的知识讲解视频
`00:32` 啊然后先跟大家交个底
`00:34` 我的视频效果不是用视频生成模型做的
`00:36` 也没有用
`00:36` Notebook l o l m
`00:37` 其实呢它本身就是一个网页啊
`00:39` 我自己web coding出来的网页
`00:41` 那肯定有人要问了
`00:42` AI视频生成模型现在已经这么强了
`00:45` 为什么你还要折腾网页呢
`00:46` 答案啊就两个字可控啊
`00:49` 字体配色每一步停留几秒
`00:50` 某一帧要不要出现一个精确的数字
`00:52` 那这些东西在网页里改几行代码
`00:55` 就可以直接搞定
`00:56` 比用视频模型直接抽卡要稳定得多啊
`00:59` 成本也会更低
`01:00` 那notebook l m呢我之前也试过啊
`01:02` 他是做不了动画的演示效果的
`01:04` 出来的都是静态图啊
`01:06` 所以我没有选择使用它
`01:07` 那REMOTION这种框架呢
`01:09` 我觉得它反而限制了模型的发挥
`01:11` 有时候还不如直接让模型直接写要来的自由
`01:14` 那我上期这个视频举个例子啊
`01:16` 其实这期视频呢就是完全由AI生成的啊
`01:19` 然后是我专门为做这期教程做的一个演示啊
`01:22` 然后我的输入呢就是一个ANTHROPIC
`01:25` 最新发布的一篇文章啊
`01:26` 他的输出就是这样一个网页
`01:28` 然后他把原始文章拆成了13个章节啊
`01:31` 100多个细粒度的讲解的步骤
`01:34` 每一步呢都有完整的视觉演示效果啊
`01:36` 然后底部这是一个隐藏的进度条啊
`01:39` 然后鼠标浮现到底部的时候
`01:40` 他才会出现
`01:41` 然后我们也可以任意的跳转切换到任意的章节
`01:44` 还有任意的步骤
`01:45` 然后整个画面里呢没有页眉
`01:47` 页码
`01:47` 也没有明显的品牌标识
`01:49` 所以大家看起来就是一块干净的画面
`01:52` 然后它会看起来更像一个视频
`01:54` 而不会像是一个网页啊
`01:55` 然后想要做出这样的效果呢
`01:58` 有几个关键的要点
`01:59` 我我先跟大家说一下
`02:02` 第一步呢就是我们要把文章变成一个口播稿
`02:06` 因为原始的技术文章一般写的都比较书面化啊
`02:09` 比如该工具旨在提供高效的解决方案
`02:12` 这种话术在你的视频里面是没有办法念出来
`02:15` 我们得尽量的把它改成短句口语化
`02:17` 第二人称的感觉啊
`02:19` 就像你在跟人聊天一样
`02:20` 那第二点呢
`02:21` 就是要把口播稿拆成具体的开发计划啊
`02:24` 或者开发大纲
`02:25` 那每一段话都应该对应一个具体的画面步骤
`02:28` 每几个步骤呢组成一个章节
`02:29` 每个章节聚焦一个话题
`02:31` 因为后续整个开发任务会非常的复杂
`02:34` 提前做好这样的大纲是保持后续稳定性的关键
`02:37` 那第三啊就是要给每一步去做视觉的演示
`02:40` 这一步非常的重要啊
`02:42` 不是直接把口播稿上的文字糊弄上去
`02:43` 就完事了啊
`02:44` 我们得尽量让网页把抽象概念
`02:46` 用动态的效果演示出来
`02:48` 第四点呢就是网页的这个关键演示步骤
`02:51` 一定是要跟随口播稿的节奏去走的
`02:53` 这样做出的视频效果才能显得非常的自然
`02:55` 那上面这些事情呢
`02:56` 你也全部都可以一步步的交给agent的去完成
`02:59` 也确实都能做出来
`03:00` 因为模型本身并不缺这些能力
`03:03` 但问题来了
`03:03` 怎么让他每次都做得出来呢
`03:05` 怎么让不同的人
`03:06` 不同的文章
`03:07` 不同的主题都能稳定的产出呢
`03:09` 怎么让开发效率不靠运气呢
`03:11` 那你直接跟agent的说
`03:12` 帮我做个视频网页啊
`03:13` 他可能做的也不错
`03:15` 但是也有可能会跑偏
`03:16` 那比如说稿子和画面对不上了
`03:18` 章节和音频数不一致了
`03:20` 后面的章节风格突然跟前面完全不搭了
`03:22` 这些都是完全有可能发生的
`03:24` 模型是有能力的
`03:26` 但是你需要一套机制来驾驭这个过程
`03:28` 划定边界
`03:29` 设定检查点
`03:30` 自检和修复等等
`03:31` 这就是哈里斯驾驭工程需要负责的事情
`03:34` 所以今天我们要讲的这个skill呢
`03:37` 也是哈里斯的一个实践案例
`03:41` GIO本身就是一份给agent的看的操作手册
`03:44` 他告诉agent的什么时候该做什么
`03:46` 做到什么标准
`03:47` 哪些红线不能碰
`03:48` 那agent的加载到这个skill之后呢
`03:50` 就会按这些规则干活
`03:51` 你不用每次都重新复述这些规矩
`03:54` 那我们这个skill呢叫web video presentation啊
`03:58` 他和欢迎老师
`03:59` 其他的canon skill
`04:00` 一起托管在这个garden skills的这个仓库里面
`04:02` 那我们之前教程讲过一个成熟的harness
`04:05` 一般要包含六个部分
`04:08` 执行编排状态和记忆
`04:10` 评估和观测
`04:11` 约束和恢复
`04:12` 那下面我们就从harness的角度来拆解一下
`04:15` 这个skill的核心设计啊
`04:19` 那首先执行和编排要解决问题
`04:21` 就是让模型知道下一步该做什么啊
`04:24` 那整个skill呢
`04:25` 把从一篇文章到视频切成了四个阶段
`04:28` 中间卡了两个人工检查节点
`04:30` 阶段一啊就是内容的编写
`04:31` AI要同时产出两样东西
`04:33` 搞不搞和开发计划搞不搞呢
`04:35` 决定信息的描述方式和整体的节奏
`04:37` 开发计划决定每章做几部
`04:39` 每部屏幕上放什么内容
`04:40` 然后是第一个人物检查节点
`04:42` agent走到这里会被强制停下来啊
`04:44` 我们要确认几件事
`04:45` 稿子和开发计划还要不要改用哪套主题素材
`04:49` 怎么准备
`04:49` 后续的章节开发是逐章去做呢
`04:51` 还是并行去做呢
`04:52` 渲染完这些事情之后呢
`04:53` 就会进入开发阶段
`04:54` 那这里也需要注意的是
`04:56` 开发阶段并不是一次性全部完成的
`04:58` 第一个章节开发完成之后呢
`05:00` 是必须要经过人来验收的
`05:02` 确认一下视觉气质和节奏是不是都对上了啊
`05:04` 他会为后续的章节开发提供一个基准的参考
`05:07` 那后续的开发呢可以按顺序去做
`05:09` 也可以开多个子agent的去并行去写
`05:12` 每个章节写完之后
`05:13` 都要逐项对照检查清单进行自检
`05:15` 直到全部过关才能交付
`05:18` 整体开发完成之后
`05:19` 它会进入音频的检查节点
`05:21` 你需要去确认是不是需要自动合成音频
`05:23` 如果需要的话
`05:24` 走阶段三
`05:25` 如果不需要
`05:25` 就直接跳到最后一个阶段
`05:27` 用你自己的声音去录制在音频合成的阶段
`05:29` A整呢会从口播稿
`05:30` 对照网页的步骤
`05:31` 提取出一个具体的音频合成清单
`05:34` 然后调用TTS的能力
`05:36` 把口播稿合成成音频
`05:38` 最后一个阶段就是录屏啊
`05:39` 浏览器打开网页
`05:40` 开启音频自动播放
`05:41` 直接录屏
`05:42` 就可以产出一个音频和画面天然对齐
`05:45` 的成品视频了
`05:49` 它解决的是模型到底能看到什么问题
`05:52` 那这个skill的链路呢横跨了四个阶段
`05:54` 涉及一堆复杂的流程
`05:56` 那如果启动的时候
`05:56` 就把所有的文档一股脑全灌进去呢
`05:59` 模型的注意力会被严重的稀释
`06:00` 所以我们把所有的信息拆成了多份文档
`06:03` 每一份文档只在指定阶段进行读取
`06:06` 比如说口播稿的规范只在第一阶段读
`06:08` 章节开发指引
`06:09` 只在开发的阶段读
`06:10` 音频合成的文档
`06:11` 要在音频合成的流程才会读
`06:13` 那这样做的好处呢
`06:14` 就是模型在任何时候面前
`06:16` 摆的都是他最需要的信息
`06:18` 不会被无关的内容干扰
`06:22` 然后是状态和记忆啊
`06:23` 他要回答的问题是系统怎么样去跨多个步骤
`06:26` 还能保持连续性和稳定性
`06:28` 那在这个skill里面呢
`06:30` 最重要的记忆就是这个开发计划啊
`06:32` 这个outline点MD那一个项目可能有十几个章节
`06:35` 上百个步骤
`06:36` agent写到后面的章节的时候
`06:38` 前面定过什么样的结构
`06:39` 用户在检查点改过什么样的方向
`06:41` 很有可能已经不在当上下文里面了
`06:44` 那开发计划的作用呢
`06:45` 就是把这些关键的决定固定下来
`06:47` 成为开发阶段的持续记忆
`06:49` 在真正的开发阶段
`06:50` SQU还会让agent的强制去读取两个文件
`06:53` 口播稿呢负责整体的节奏啊
`06:55` 要先讲什么后讲什么
`06:56` 画面是一定要跟着口播稿去走的
`06:58` 原文章呢负责信息的密度
`07:00` 因为口播稿通常会更精简
`07:02` 画面里需要的数据案例可以从原文里面去补
`07:05` 三份文档
`07:05` 各有分工
`07:06` A卷呢开发每一个章节的时候都会同时去参考
`07:08` 他们就不容易会出现结构对了
`07:10` 但是画面空了
`07:11` 或者说信息很多
`07:12` 但是节奏乱这样的问题
`07:14` 那本质上呢这也是一种文件化的工作记忆啊
`07:17` 把关键的状态去写进这些文件
`07:19` 需要的时候再读回来啊
`07:22` 呃
`07:22` 然后下面工具系统解决的是
`07:24` 模型到底能做什么的问题
`07:26` 这个skill本身没有用什么特殊的工具啊
`07:29` 核心就是让模型把agent自带的这个
`07:32` 文件读写的工具用好就可以了
`07:34` 那这里我们要解决一个关键问题啊
`07:36` 在章节的具体开发流程中
`07:38` 它是支持多章节并行的开发的模式的
`07:40` 那多个agent呢去同时改一个项目呢
`07:42` 会不会相冲突呢
`07:43` 那skill with设计了严格的隔离机制啊
`07:46` 首先就是每张它必须是独立的文件夹物理隔离
`07:49` 然后每张是独立的CSS前缀
`07:51` 它不会互相抢类名
`07:52` 那主题token呢会兜底视觉的统一
`07:55` 字体颜色间距都会走全局的变量
`07:57` 所以即使是每个agent独立开发
`08:00` 也能保持视觉风格的一致
`08:02` 各个章节互不影响
`08:05` 然后约束和恢复
`08:06` 要回答的问题是出错了要怎么办
`08:08` 那在这个skill里面呢
`08:09` 出错最常见的方式可能不是代码报错
`08:12` 而是用户觉得不对啊
`08:14` 比如说这个章节节奏太快了啊
`08:16` 这个动画AEVER太重了啊
`08:18` 面对这种反馈呢
`08:19` agent的本能的反应可能是重做整个章节啊
`08:22` 毕竟重新生成对agent来说成本并不是很高
`08:25` 但是重做整章的问题是
`08:27` 他会把已经做对的部分也改掉
`08:29` 比如用户说某个步骤的节奏太快了
`08:31` 你直接重做了整章节
`08:32` 结果其他步骤原来很好的效果也变了
`08:35` 用户可能反而更不满意
`08:36` 所以呢skill里面有一条关键的原则
`08:38` 叫反馈修复的最小切片
`08:40` 先定位问题在哪一层
`08:41` 是节奏
`08:42` 视觉内容还是代码
`08:43` 然后只改最小的切片
`08:45` 不会重新做整章节啊
`08:48` 然后评估和观测要回答的问题是
`08:50` 系统怎么样知道自己做的对不对
`08:52` 那这块是我花的心思最多的啊
`08:54` 因为它直接对应ANTHROPIC
`08:56` 之前提出的一个核心问题
`08:57` 就是自评失真的问题
`08:58` 让写代码的agent呢自己来评价这章写的怎么样啊
`09:01` 这个结果大概率是还不错
`09:04` 那所以呢我设计了一套非常硬性的自检的规则
`09:07` 每个关键流程完成之后呢
`09:08` 必须要走自检修复再汇报的流程
`09:11` 每一个阶段都有一套完整的啊
`09:13` 自己的一套自建规则
`09:14` 那那最优的方案呢
`09:15` 就是用agent teams开一个独立的reviver agent
`09:19` 给它产出文件加对应的清单
`09:21` 让他从零开始逐项检查
`09:23` 那如果不支持agent teams呢
`09:24` 也可以用service agent最次的兜底啊
`09:27` 就是当前的agent要自己去去逐项核查
`09:29` 但也要必须严格逐项
`09:30` 不允许目测一遍就放行
`09:32` 那回过头来我们把这个skill的设计摊开来看
`09:35` 你就会发现它和我们之前分享的OpenAI
`09:38` ANTHROPIC的实践本质上是没有区别的
`09:41` 都是在搭建harness
`09:43` 只不过他们搭建的是工业级的运行系统
`09:45` 我们做的是一个skill级别的协作协议
`09:47` 那这里啊也要跟大家明确一个误区啊
`09:50` 做哈尼斯不是非得从零去做一个agent
`09:53` 能用一个skill约束agent
`09:55` 把一个垂直的工作做好
`09:56` 也是在做哈尼斯好
`09:58` 下面我们来进入实战环节
`10:02` 我们整套方案呢主要会用到四个工具
`10:05` 第一个啊就是cloud code
`10:06` 它是我们的核心执行的agent
`10:09` 不过啊如果你本地使用的是cursor codex
`10:11` 或者其他支持skill的agent都是可以的
`10:14` 那装过程我们就不演示了啊
`10:15` 大家可以直接拷贝这条命令去做安装
`10:18` 然后安装完成之后呢
`10:19` 我们在终端输出cloud杠V啊
`10:21` 能够显示出版本号
`10:22` 就说明安装成功了
`10:25` 啧大家都知道啊
`10:26` 在国内正常使用cloud code是非常困难的
`10:29` 那我自己也被封了好几个账号了
`10:31` 目前啊
`10:32` 推荐的做法一般是搭配一个国产模型来用
`10:35` 经过我的大量试用呢
`10:36` 啊我现在的选择是搭配mini max来用
`10:38` 那mini max的token plan呢和cloud code的适配是非常好的
`10:42` 那我现在订阅的这个plus极速版啊
`10:44` 速度快量大管够啊
`10:45` 性价比也非常高
`10:46` 日常的需求开发是完全够用的
`10:48` 那定义完成之后呢
`10:49` 啊它会生成一个APIK啊
`10:51` 这个我们提前保存好
`10:52` 后面会用得到
`10:53` 第三个我们用到的就是这个cc switch啊
`10:56` 它是一个桌面的配置工具啊
`10:57` 可以让cocode codex
`10:59` 专门来Sol i这些agent的切换为任意的字义模型
`11:02` 在这套流程里呢
`11:03` 我通过它来给克拉code配置mini max token plan啊
`11:06` 我们可以直接到他这个GITHUB的release页面啊
`11:09` 下载我们的指定系统的这个安装包
`11:11` 安装完成之后呢
`11:12` 我们直接打开它
`11:13` 然后在这个agent这里选择cloud
`11:15` 然后我们点击加号
`11:16` 然后选择它内置的这个mini max的这个提供商
`11:20` 然后在这里我们粘贴刚刚在mini max token plan
`11:23` 生成的这个APIK
`11:24` 然后拉到最下面啊
`11:26` 其他都不用改
`11:27` 然后在主模型这里
`11:28` 全部都改成这个mini max m2.7
`11:31` 然后点击添加就可以了啊
`11:32` 然后我们点击启用
`11:34` 切完成之后呢
`11:34` 我们打开终端启动一下cloud哎
`11:38` 然后我们在这看到他的模型已经切换到了
`11:40` mini max m2.7的模型
`11:41` 然后我们尝试对话测试一下
`11:44` 能正常输出
`11:45` 说明配置成功了
`11:48` 然后第四个我们需要配置一下mini max的CI
`11:51` mini max的token plan呢还有一个明显的优势啊
`11:53` 就是它附带了多模态的套餐
`11:55` 包括图片
`11:56` 语音视频全部都是包含的
`11:58` 那我们今天的教程里面有一个关键的节点啊
`12:01` 就是要把口播稿合成音频
`12:03` 那mini max的这个top pan
`12:05` 里面自带了每天9000字的语音合成额度啊
`12:08` 做两篇文章是完全都够用的
`12:10` 那它的官方呢提供了一个SI啊
`12:11` 我们不用写一行代码
`12:12` agent就可以直接调用完成语音的合成
`12:15` 然后它的安装方法非常简单啊
`12:17` 刚才我们已经装好了cloud code啊
`12:18` 然后我们直接把这句话发给cloud code
`12:20` 然后在这里把我们的这个密钥
`12:22` 换成你自己的那个tom plan的那个真实的密钥
`12:25` 然后它自动就可以安装成功了
`12:27` 安装完成之后呢
`12:28` 我们打开终端执行一下MMX这个命令啊
`12:30` 如果能正常输出这些信息
`12:32` 说明这个CI就安装成功了
`12:35` 然后我们装一下skill啊
`12:36` 这里我们演示一个最简单的方法啊
`12:39` 大家直接访问这个GITHUB的地址
`12:41` 然后拉到下面啊
`12:42` 找到这个web video presentation这个skill
`12:45` 然后直接点这个下载安装包
`12:48` 下载完成之后呢
`12:49` 我们直接解压这个安装包
`12:51` 然后就可以得到这个skill的具体目录
`12:53` 然后我们直接复制一下这个文件夹
`12:54` 然后我们在编辑器打开我们的工作目录啊
`12:57` 创建一个新的文件夹
`12:57` 点cloud
`12:58` 然后创建一个skills目录
`13:00` 然后把我们的skills粘贴进去
`13:04` 然后我们在这个目录下启动一下cloud code
`13:08` 如果啊我们输入这个斜杠web video啊
`13:11` 能够智能提示出这个skill
`13:12` 就说明我们配置成功了
`13:15` 最后呢我们还有一个可选的配置啊
`13:18` 就是agent teams和team max
`13:20` 那如果说只是做一个小demo啊
`13:22` 一个cloud code绘画可能就够了
`13:23` 但是如果你是要做一个十几张
`13:25` 100多部的视频项目
`13:27` 让一个A证呢从头写到尾会非常的慢
`13:30` 那skill在设计上对多章节做了严格的物理隔离
`13:33` 天然是支持多A症的并行去开发的
`13:36` 另外如果说需要去做质检的话
`13:38` 也需要去开独立A证的去做
`13:40` 可能有些同学还没用过啊
`13:41` 这里我们也简单讲一下
`13:43` cloud code支持的两种多A证能力
`13:45` SUSPAGENT和agent teams
`13:47` 首先啊是SOBEAGENT
`13:49` 它就像是一个紫禁城主agent把一个任务分出去
`13:52` 子agent在自己的一个完全独立的上下文里面完成
`13:56` 然后再把结果交回来
`13:58` 各个子A证的之间是互相看不到的
`14:00` 也不会互相讨论
`14:02` 比如啊
`14:02` 你可以让三个sub agent分别去编写三个章节
`14:05` 最后主agent在把他们所有的结果回收回来
`14:08` 这种方式非常适合结果导向性的任务
`14:11` 比如说并行开发多个章节
`14:13` 或者一次性检查某段代码啊
`14:15` 它比较省token也比较容易调度
`14:17` 然后a and teams呢就像是一个项目小组
`14:20` 一个组长加上几个组员
`14:22` 每个组员都是独立的绘画
`14:24` 他们之间可以互相发消息
`14:26` 也可以共享任务列表
`14:27` 那这种方式适合需要来回反馈的任务
`14:30` 比如说质检这个环节啊
`14:32` 我们的测试看到代码之后发现字号太小了
`14:35` 就让开发再去改大
`14:36` 然后开发改完之后呢
`14:38` 测试再去复查
`14:39` 这个过程用SUSPENT做会比较别扭
`14:42` 但是agent teams就很合适了
`14:44` 但是代价也很明显啊
`14:46` 就是token开销会更高一点
`14:48` 因为每个组员都是一个完整的绘画
`14:50` 我们可以这样去理解啊
`14:52` Suspent
`14:53` 就像你让三个人分别去三个城市去办不同事情
`14:56` 然后办完之后回来再汇报
`14:58` 那agent teams呢
`15:00` 就像你把三个人拉进了同一间会议室
`15:02` 让他们边讨论边改方案
`15:04` 那agent tims目前还是一个实验性的功能啊
`15:07` 如果需要在cr code里面启用啊
`15:09` 我们需要在它的全局配置里面
`15:11` 加上这个环境变量啊
`15:12` 然后为了更好的可视化演示整个过程呢
`15:15` 我也建议大家安装一下t max
`15:17` t max是一个开源的终端管理器
`15:20` 可以在一个窗口中同时管理多个终端会话
`15:23` 配合t max之后呢
`15:24` agent teams里面
`15:25` 每个组员都会出现在不同的终端面板里面
`15:29` 你可以同时看到测试员在审核哪里
`15:31` 开发者在开发
`15:32` 那这个也是卡拉克的官方推荐的使用方式啊
`15:36` 那安装大家直接去输入这个命令去安装
`15:38` 我们就不演示了
`15:39` 那如果我们想要在CLOUCODE的agent teams去适配t max
`15:42` 还需要在全局的配置里面
`15:44` 加上这个t max的这个参数啊
`15:47` 我们的环境准备工作就做完了啊
`15:50` 然后我们进入实战演示的章节
`15:52` 然后这里我们用一封邮件发出后的600ms
`15:55` 这篇文章来演示一下完整的视频生成的流程啊
`15:59` 首先我们在终端进入这个项目录啊
`16:01` 然后先输入tm max进入t max的模式啊
`16:03` 然后我们在下方看到这个小绿条
`16:05` 就说明进入t max模式了
`16:07` 然后我们启动color code啊
`16:09` 嗯大家可以注意一下啊
`16:10` 这里我们启动额外加了一个dangerously
`16:13` cape permissions这个参数啊
`16:14` 这个也就是cloud code的优乐模式
`16:17` 在后面执行任务的时候
`16:18` 会跳过所有的工具调用的人工确认的环节啊
`16:22` 这里我们是为了方便演示啊
`16:23` 大家使用的话还是需要注意安全问题
`16:25` 我们直接把原始文章丢给cloud
`16:27` 告诉他使用我们的skill完成第一步
`16:29` 那我们首先看到他开始用这篇文章
`16:32` 编写口播稿和开发计划
`16:36` 编译完成之后呢
`16:37` 我们看到他分别创建了两个审核的agent
`16:40` 分别进行质检
`16:41` 那由于我们开了t max
`16:43` 它会自动的分屏展示
`16:49` 质检通过之后呢
`16:50` 主agent呢会根据评审的结论进行更改
`16:53` 然后再校对输出最终的版本
`16:56` 然后我们看到本地多了两个文件啊
`16:58` 首先这个script md呢就是基于原始文章改写的
`17:03` 更适合视频的表达的一个口播的脚本
`17:06` 然后这个句子会更短一点
`17:07` 然后节奏也会更快一点
`17:09` 这种句子听起来就像是真人在聊天
`17:11` 而不是念文章啊
`17:13` 然后这个开发大纲就是把刚刚的脚本
`17:15` 还有我们的原始文章拆成了细粒度的章节
`17:18` 还有步骤
`17:19` 并且呢标注每一步
`17:20` 屏幕上应该出现什么样的信息
`17:22` 他负责的主要就是把控后续的开发节奏
`17:25` 然后以及每个章节的边界
`17:26` 每一步的信息密度啊
`17:28` 以及这频具体要讲清楚什么样的内容好
`17:30` 第一步的流程完成之后呢
`17:31` 他会向我们询问几个关键的问题啊
`17:34` 首先是这个稿子
`17:35` 还有这个开发计划要不要改
`17:36` 那如果我们觉得这个稿子AI味比较重啊
`17:38` 我们就可以适当调整一下
`17:39` 改成我们自己讲话的这种风格
`17:41` 然后另外我们需要确认一下这个规划的大纲
`17:43` 有没有明显的节奏问题啊
`17:45` 那如果改了稿子呢
`17:46` 可以让AI在下一步同时去调整
`17:48` 这个开发的计划啊
`17:49` 然后下一个问题就是选什么样的主题
`17:51` 然后skill name呢定义了几套不同风格的主题
`17:53` 它呢会根据我们本次要编写的内容
`17:56` 来推荐几个主题
`17:57` 我们也可以自己去向他说明一些啊
`17:59` 我们想要的风格
`18:00` 如果不想用默认的主题呢
`18:02` 我们也可以自己去向他说明我们想要的风格
`18:04` 然后下面就是素材怎么准备啊
`18:06` 就是在后续生成的网页中
`18:08` 可能会用到一些图片素材
`18:10` 那skill可能会让我们去选啊
`18:12` 比如说啊是要我们自己去手动提供呢
`18:15` 还是他自己绘制啊
`18:16` 比如这个例子里
`18:17` 其实所有素材都是可以AI自己去生成的啊
`18:20` 然后就是后续的开发模式
`18:22` skill一共支持三种开发模式啊
`18:24` 然后第一种就是默认的啊
`18:25` 他需要逐张去确认
`18:27` 每一章开发完成之后
`18:28` 我们都需要停下来去人工验收一下
`18:30` 然后第二种就是第一章开发完成之后啊
`18:32` 我们去验收
`18:33` 然后续的所有的章节按顺序去做完
`18:35` 然后最后一种就是第一章开发完成之后
`18:37` 仍然需要验收
`18:38` 那后续的所有章节用SUBPON去并行开发
`18:42` 然后我们把原始的文章口播的脚本
`18:45` 整体的大纲
`18:45` 视觉主题开发模式这些都全部确认完成之后呢
`18:48` 就可以进入具体的开发阶段了
`18:50` 在开发阶段呢
`18:51` skill不会一上来就直接并行做完所有的章节
`18:54` 而是会先做好第一章
`18:56` 让我们进行验收啊
`18:57` 因为第一章定下来之后呢
`18:58` 我们已经能看到整个项目的页面密度
`19:01` 字体大小
`19:01` 动效的节奏
`19:02` 信息呈现的方式是不是合理了
`19:04` 那如果跟预期差距比较大呢
`19:06` 我们还能及时的调整啊
`19:08` 第一章就是后续所有的章节的基准哎
`19:13` 第一章开发完成了
`19:14` 我们可以看到工作区中的这个项目录
`19:16` 已经搭建起来了
`19:17` 并且已经有了第一章的代码
`19:19` 然后我们打开它给出的这个地址
`19:21` 在浏览器直接验收一下
`19:23` 这个第一章的验收非常重要啊
`19:25` 我们最好是亲自在浏览器里面从头点到尾
`19:28` 然后确认一下整个画面是不是看起来很舒服
`19:30` 节奏是不是顺畅
`19:32` 然后信息有没有太密
`19:33` 然后有没有明显的AI ever
`19:34` 那如果第一张你认为还有问题呢
`19:36` 可以发给agent的
`19:37` 然后让他继续去调整
`19:39` 如果没有问题啊
`19:39` 我们就可以去让他开发后续的章节了
`19:42` 下面agent teams上场
`19:43` 剩下所有的章节都可以并行开发
`19:46` 那我的经验是
`19:46` 最多同时并行跑三个agent是比较合适的
`19:50` 我们可以直观的看到这三个agent啊
`19:52` 各自负责几个章节
`19:54` 每个agent拿到自己负责的章节大纲
`19:56` skill的规范以及第一章的代码作为参考
`19:59` 那各自写各自的互相不会干扰
`20:00` 每章完成之后都会进入质检的环节
`20:02` 这里需要注意的是呢
`20:04` 我们一定要选一个能力比较强的模型
`20:06` 才能完成这样的任务啊
`20:07` 不然多一帧的调度很容易会出错的
`20:10` 然后所有的agent的开发
`20:12` 并且质检完成之后
`20:13` 他才会向我们汇报任务
`20:14` 完成所有章节开发完成之后呢
`20:16` 我们来整体看一下效果啊
`20:18` 呃我们需要检查一下页面上有没有空白页
`20:21` 动画的卡顿
`20:22` 步骤对不上啊
`20:23` 或者说内容的错位
`20:25` 这种情况一共50多部的一个项目
`20:27` 中间某一步来出现一些问题是很正常的啊
`20:30` 然后或者说某一步啊
`20:32` 如果你觉得效果不是很好的话
`20:33` 可以让他重新来改一下啊
`20:36` 那如果我们觉得整体效果还不错
`20:38` 就可以进入下一步音频合成的阶段了啊
`20:41` 在这一步他会用我们刚才配置的mini max ci
`20:44` 把口播脚本批量合成为音频的文件
`20:47` 然后在合成之前呢
`20:48` 他会先从所有章节里抽出一个
`20:50` 详细的口播文本清单啊
`20:52` 我们也可以大概扫一遍
`20:53` 有没有错字或者漏句的情况
`20:55` 然后他开始逐条的合成音频
`20:57` 如果遇到速率限制
`20:59` 这样的报错呢
`21:00` 它会把错误的文件记录下来
`21:01` 重新合成
`21:02` 已经生成过的文件会自动跳过
`21:03` 中途中断了也不用重来啊
`21:05` 然后合成完成之后
`21:06` 我们就可以在项目录里看到这些音频
`21:08` 每个章节有个子目录啊
`21:09` 每一步都有对应的音频的文件
`21:11` 文件名和步骤的编号也是一一对应的啊
`21:14` 音频合成完成之后呢
`21:16` 我们就可以录制视频了
`21:17` 网页一共提供了三种模式啊
`21:20` 第一种啊就是默认的这个手动模式
`21:22` 这种模式下不会播放音频啊
`21:24` 我们可以自己自由的来切换任意的步骤
`21:27` 然后我们自己来录制自己的声音就可以了
`21:30` 那如果我们在URL上加上audio等于一
`21:32` 这个参数就会自动的进入这个音频播放的模式
`21:38` 你的设备得先去查DNS
`21:40` 但查的不是普通地址
`21:42` 是一个叫ma
`21:43` 可以开始正经记性了
`21:45` 知道了对方IP下一步是建立连接啊
`21:47` 我们还是可以手动去推荐这个步骤啊
`21:49` 但是每一步是自动会播放音频的
`21:52` 然后我们可以自段旅程里节奏最长
`21:54` 最凶险的一段是反垃圾审判
`21:56` 然后我们在幼儿上如果加上凹凸等于一
`21:59` 它就会进入自动模式啊
`22:02` 然后我们只要按下空格页面
`22:04` 就会自动的按照音频的节奏去自动的播放啊
`22:07` 整段旅程里最长最凶险的一段是反垃圾审判
`22:10` 你的邮件会被扔进一条多层过滤管道
`22:12` 第一关IP信誉大约5ms
`22:15` 发送方的IP跟全球黑名单对一遍
`22:17` 第二关身份验证三件套
`22:19` 20ms搞定
`22:19` 然后大家啊可以自己选择适合自己的方式
`22:22` 去录制就可以了
`22:24` 最后我们总结一下啊
`22:25` 表面上看我们讲的是一套文章变成视频的流程
`22:29` 但是本质上它是一次harness的具体实践
`22:32` 模型本身的能力已经很强了
`22:34` 考试code能写代码
`22:36` mini max提供稳定的推理和生成的能力
`22:38` 以及语音合成的能力
`22:39` 但真正我们要关注的问题是
`22:41` 怎么样把这些能力稳定的编排起来
`22:44` 让agent能够稳定地完成
`22:46` 所以这个skill做的事情啊
`22:48` 就是把一次复杂的内容生产的任务拆成有流程
`22:52` 有状态
`22:52` 有检查点
`22:53` 有自检
`22:53` 有恢复机制的工程系统
`22:55` 这也是HAR尼斯作为核心的价值
`22:58` 把模型能力
`22:58` 工具能力和人的判断
`23:00` 组织成一条稳定可控的生产流程
`23:03` 大家也可以用类似的思路
`23:04` 在你的真实工作中实践一下
`23:06` 希望可以给大家带来一些启发啊
`23:09` skill已经放到garden skills的这个仓库里了
`23:11` 感兴趣的同学呢
`23:12` 可以拿一篇自己的文章来试一试
`23:14` 好今天的教程我们就到这里
`23:16` 如果本期对你有所帮助呢
`23:18` 希望得到一个免费的三连啊
`23:19` 我们下期见